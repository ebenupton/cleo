# Byte grind: 728 candidates from 85 regions

## r000:0  src/engine.s:526-529  (window i0110)  M -18 B -77  [high]  review: accept

Cycles: Per spnext expansion. No-carry path (bcc :++ taken) unchanged on both. Model B carry path: no fold 3+11(inca)+3+3(bcc)+3=23 -> 3+2+3+3+3=14; fold 43 -> 3+2+3+2+5+2+5+3=25. Master carry path: no fold lda3+inc a2+cmp2+bcc3+sta3=13 -> inc5+bpl3=8; fold 3+2+2+2+2+3=14 -> 5+2+2+3=12.

Assumption: spnext is expanded 7x on Model B (TILCODE 4: engine.s 952,1129,2607,2657; SPR4CODE and SPR6CODE 1 each: 2372 via SPRITE_LOOPS; MNUCODE 1: menu.s 132) and 6x on Master (CODE 5, LOGIC 1). I measured this with ca65/ld65 map diffs. (1) On the carry path sp = old sp + 8 - 256 is in 0..7 whatever the caller (sp+8 <= $107), so the Model B low-byte fold (sp - $80 with borrow) is exactly sp+$80 and always borrows. (2) C = 1 at `lda sp+1` because bcc :++ fell through. (3) ringehi (zp, defs.inc:56) is $44 or $80 (select_backbuf), so A >= $44 on the fold path and A - $3A never borrows. (4) Master: sp+1 is a screen address high byte in $30..$7F (ring $3000-$7FFF, RINGEND = $8000, engine.s:153), so inc gives <= $80 and the fold input is exactly $80. (5) Exit state that changes: Model B fold path N=1 (was 0) and V may differ; mtmp is no longer written. Master carry path: A = sp low (no fold) or $30 (fold) instead of new sp+1; C = 1 (was 0 when no fold); N/V differ. Every caller is followed by dec/dex (N,Z rewritten) and A/C are dead: 952 -> dec tmp2 / bne @sc (ldy #7; lda (tp),y) or bra @runend (lda cnt; sec); 1129 -> dec tmp2 / bne @fsc (ldy/lda tp) or jmp @runend; 2372 -> dec sp_cnt / ds_colloop -> jmp sprFN (SPRFULL/SPRMSK entries start lda tmp2; no adc/sbc/rol/ror before a cmp in SPRLINE/MPAIR) or ds_rowdone (lda sp_row); 2607 -> dex / beq @done (rts from copy_partial -> engine.s:3125 jsr blank_below: lda wfine) else lda ptr; clc; 2657 -> dex / bne @char (lda #0) or rts from blank_below -> stza NSPR (Master stz, A/flags kept) -> t_build_sections (pha/pla, then build_sections: lda curbuf; asl), or PARALLAX pl_draw (ldx/lda); menu.s:132 -> dec tmp4 / bne @pair (lda #0) or lda tp. No bvc/bvs anywhere near (only engine.s:3700 in the ISR after its own bit).

```
        lda sp+1
        inca
        ringup sp
        sta sp+1
```
->
```
  .if ::MODELB
        lda sp+1
        adc #0                      ; C = 1 (bcc :++ not taken): + 1
        cmp ringehi
        bcc :+
        rol sp                      ; sp is 0..7 here: C in 1, out 0
        sbc #>RINGBYTES             ; C = 0: A - $3A (the $80 fold's borrow); C = 1
        ror sp                      ; sp + $80: the low byte's fold; C = 1
:       sta sp+1
  .else
        inc sp+1                    ; at most $80: sp+1 < $80 (RINGEND = $8000)
        bpl :+
        lda #>RINGBASE              ; $80 folds to $30 = $80 - >RINGBYTES
        sta sp+1
:
  .endif
```

Model B: adc #0 with C=1 == inca (+1; A <= $80 so no carry out); cmp ringehi/bcc as in ringup. Fold: rol sp (C in 1) -> sp'=2sp+1 <= 15, C out 0 (bit 7 of sp is 0); sbc #$39 with C=0 -> A-$3A == original (A-$39) then sbc #0 with the borrow of sp-$80 (always, sp<$80); C out 1; ror sp with C=1 -> $80|(2sp+1)>>1 = sp+$80 == original sp-$80 mod 256, C out = bit 0 of 2sp+1 = 1 == original C=1 after sbc #0; sta sp+1 identical. A at exit identical on all paths. Master: inc sp+1 then bpl == lda/inc a/cmp #$80/bcc since sp+1+1 <= $80 and >= $80 means exactly $80; fold stores $30 == $80 - $50. sp+1 value identical on all paths. Checked in a scratch copy with all 4 candidates applied: both targets built (full build.sh + modelb/build.sh). tools/gate.sh at 200 and 800 frames: every Master check and Model B lockstep L0/L5 and pix L0/L6 identical. B title's time-sampled RAM compare differs only during boot staging (code bytes in main RAM) and matches from 16M cycles on; any 1-byte change in a bank does the same. B lockstep L12 at 800 frames times out identically on the unmodified source.

Review: Original lines 526-529 match. The carry path always has sp in 0..7 (lda sp/clc/adc #8), and C=1 at lda sp+1. Model B: adc #0 = inca, and cmp/bcc are unchanged. On the fold path, rol sp (C in 1, out 0) / sbc #$39 (C=0, so A-$3A with no borrow because A>=$44) / ror sp (C in 1) gives sp+$80 and A-$3A with C=1, the same as the original. Only N (now 1) and V (1 when A=$80) change on the fold path. Master: inc sp+1 <= $80, so bpl gives the same fold test and $30 is the fold of $80. A and C change on the carry path. I traced every consumer on both targets and nothing reads them. 952 (dec tmp2 -> ldy/lda, or lda cnt;sec), 1129 (Master @fsc lda tp; @runend sec), 2372 (dec sp_cnt -> jmp sprFN, whose entries are lda tmp2;cmp #7; ds_rowdone lda), 2607 (dex -> rts -> blank_below lda wfine, whose @no path goes to stz NSPR -> t_build_sections pha/pla -> lda curbuf;asl; else lda ptr;clc), 2657 (dex -> lda #0, or rts as above), menu.s:132 (dec tmp4 -> lda #0/asl, or lda tp). Every bvc/bvs (logic.s macros, display.s:141, engine 3700) sets V itself first. The ISR does not touch sp. Each target still emits 2 ':' per spnext, and ca65's warnings are identical. Measured by assembling both targets: Master CODE 15 + LOGIC 3 = 18; Model B TILCODE 44, SPR4CODE 11, SPR6CODE 11, MNUCODE 11 = 77. Cycles checked: Model B 23->14 / 43->25, Master 13->8 / 14->12.

## r000:1  src/engine.s:499-501  (window i0092)  M -0 B -14  [high]  review: accept

Cycles: Model B ringup fold path -2 (sec gone), non-fold unchanged; Master unchanged (MODELB branch only). 14 bytes = 7 direct ringup sites + the 7 inside spnext; if the spnext candidate (i0110) is applied first, 7 (TILCODE 3, LGCCODE 1, SPR4CODE 1, SPR6CODE 1, MNUCODE 1).

Assumption: None outside the macro: the fall-through of `bcc :+` after `cmp ringehi` has C=1; `sbc #>RINGBYTES` ($39) from A >= ringehi >= $44 does not borrow so C stays 1 (the macro's own comment says so); pha and lda p do not touch C.

```
        lda p                       ; borrows from A when p is below $80
        sec
        sbc #<RINGBYTES
```
->
```
        lda p                       ; borrows from A when p is below $80 (C = 1 still)
        sbc #<RINGBYTES
```

sec only ever runs with C already 1, so it does nothing; everything else is identical.

Review: Lines 499-501 match. On the fold path, cmp ringehi leaves C=1. sbc #$39 from A >= ringehi >= $44 cannot borrow, and pha and lda p leave C alone, so sec is a no-op. The fold path is 2 cycles faster. The Model B saving, measured, is 7 bytes applied after index 0 (TILCODE 3, LGCCODE 1, SPR4 1, SPR6 1, MNUCODE 1). Applied on its own it is 14, because the spnext copies are included.

## r000:2  src/engine.s:507-509  (window i0098)  M -11 B -0  [high]  review: accept

Cycles: Master only: no fold cmp2+bcc3=5 -> bpl3; fold cmp2+bcc2+sbc2=6 -> bpl2+sec2+sbc2=6. 11 bytes if applied alone (5 direct sites: engine.s 554, 963, 2405, 2617, menu.s 106; + 6 spnext expansions); 5 (CODE 4, LOGIC 1) if the spnext candidate (i0110) is applied first. Both figures measured.

Assumption: (1) RINGEND = RINGBASE + RINGBYTES = $3000 + $5000 = $8000 on the Master (engine.s:140-153; RINGROWS=32 fixed), so A >= >RINGEND <=> bit 7 of A. (2) N reflects A at every Master expansion: 554 `adc RINGHI,x`; 963 `adc #>ROWBYTES`; 2405 `adc #>ROWBYTES`; 2617 `inca` (= inc a); 3310 (PARALLAX) `adc #0`; menu.s:106 `adc #>ROWBYTES`; spnext `inc a`; 4841 is inside the MODELB branch (engine.s:4801 .else of 4742 .if .not MODELB). (3) Exit flags change only on the no-fold path: N=0 (was 1), C = entry C (was 0). Entry C is 0 at every adc site (sum < $100) and 1 at 2617 and in spnext. Consumers: every site stores A and then goes to: 554 rts -> ringaddr callers 628 (sta/lda), 1878 (lda sp), 2513 (lda sp; clc), 2650 (ldx), 3324 (lda sp), menu m_ringaddr (ldx #0); 963 rts -> inc rc_y; 2405 jmp ds_rowloop (lda); 2617 bra @fjmp -> jmp @g4 (ldy/lda/sta/dey only, then spnext's clc); 3310 jsr @tile (lda pl_cx+1, C=0 as before since entry C=0); menu.s:106 -> lda GLYPHBUF,x; spnext -> dec/dex (see i0110). Fold path flags identical (sec+sbc == cmp-set C=1 + sbc).

```
        cmp #>RINGEND
        bcc :+
        sbc #>RINGBYTES
```
->
```
        bpl :+                      ; RINGEND = $8000: N from A (every caller's adc / inc a)
        sec
        sbc #>RINGBYTES
```

bpl :+ skips iff A < $80 == cmp #$80/bcc; on the fold path sec supplies the C=1 the cmp used to, so sbc #$50 gives the same A and flags.

Review: Lines 507-509 match. The Master's RINGEND is $8000, so for an 8-bit A, A>=$80 exactly when bit 7 is set. At every Master site N reflects A: 554 adc RINGHI,x, 963/2405/menu:106 adc #>ROWBYTES, 2617 inc a, 3310 adc #0 (PARALLAX), spnext inc a if index 0 is not applied. 4841 is Model B only. On the fold path, sec+sbc matches the flags and A of cmp(C=1)+sbc. On the no-fold path, C is now the entry C instead of 0, N=0 instead of 1, and Z is unchanged (A>=$30). The entry C is 0 at every adc site, so only 2617 (C=1) differs. That path goes bra @fjmp -> @g4/@g2/@g0 (ldy/lda/sta), then spnext's clc, so nothing reads the carry. The N consumers are all reloads: ringaddr callers use A, rts->inc rc_y, jmp ds_rowloop lda, @tile lda/lsr, lda GLYPHBUF. Measured: 5 bytes on the Master after index 0 (CODE 4, LOGIC 1), 11 on its own. Model B unchanged. Cycles: 5->3 with no fold, 6->6 on the fold.

## r000:3  src/engine.s:548-550  (window i0124)  M -1 B -1  [high]  review: accept

Cycles: -2 on both (ringaddr, every call); Model B TILCODE -1, Master CODE -1 (measured)

Assumption: C after the third `rol sp+1` is bit 13 of cx (w16). Every caller passes a map char column: rc_x (drawrect, engine.s:602-604), wcx+sp_c0 (1755-1766), wcx+tmp4 (2488-2496), wcx (2645), pl_cx (PARALLAX). Maps are <= 256 tiles = 1024 chars wide (engine.s:610 comment), so w16+1 <= 4. Stronger still: if bit 13 of cx were set, cx*8 would already be cut to 16 bits and the original's single-fold ring address would be wrong. So every correct run of the original has C=0 here.

```
        rol sp+1                    ; sp+1:A = cx*8
        clc
        adc RINGLO,x
```
->
```
        rol sp+1                    ; sp+1:A = cx*8; C = 0 (cx < 8192)
        adc RINGLO,x
```

clc only ever runs with C already 0, so it does nothing; adc RINGLO,x and everything after it are identical.

Review: Lines 548-550 match. After the third rol, C is bit 13 of w16. Every caller passes a non-negative map column below 1024. drawrect's rc_x comes from wcx, is clipped to >= wcx by drawrect_clip, or is wcx+... with no borrow. sp_c0 and tmp4 are added to wcx with a zero high byte (adc #0). wcx is used directly, pl_cx=wcx, and the menu sets w16+1=0. So C=0 and clc is redundant, saving 2 cycles. Measured: Master CODE -1, Model B TILCODE -1. ringaddr7 (engine.s:4819) is a separate copy and is untouched.

## r001:0  src/engine.s:527-527  (window i0128)  M -0 B -28  [high]  review: accept

Cycles: Model B: page-crossing path 11 -> 2 (inca = sta/inc/lda mtmp); bcc-taken path unchanged. Master: identical code.

Assumption: Inside spnext only: the path reaching this line is the fall-through of `bcc :++` after `adc #8`, so C = 1; `lda sp+1` leaves C alone. D is clear (the game uses binary adc everywhere). The flags/C out of the increment are dead: ringup's first instruction (cmp ringehi) overwrites N,Z,C. mtmp is scratch owned by the expansions (nothing reads it after). 4 bytes per expansion x 7 Model B expansions: engine.s 952, 1129, 2372 (SPRITE_LOOPS, expanded in spr4 SPR4CODE and spr6 SPR6CODE), 2607, 2657, menu.s 132. The change is outside the unrolled computed-entry sprite line code (sprnext is the column advance).

```
        inca
```
->
```
  .if ::MODELB
        adc #0                      ; C = 1 (the bcc fell through): +1, 2 bytes not 6
  .else
        inca
  .endif
```

inca (6502: sta mtmp / inc mtmp / lda mtmp) gives A = sp+1 + 1 with C preserved (=1). adc #0 with C = 1 gives A = sp+1 + 1 (C out differs only when sp+1 = $FF, and is immediately overwritten by ringup's cmp). Same A into ringup, same ringup outcome, same sta sp+1. The bcc :++ target count is unchanged (no labels added). Uses ::MODELB since spnext is expanded inside .scope spr4/spr6.

Review: Line 527 is the inca in spnext. The only way to it is the fall-through of bcc :++ after clc/adc #8, so C = 1, and lda sp+1 leaves C alone. No code in the tree uses sed, so D is clear. adc #0 gives A = sp+1 + 1, the same as inca. N, Z and C are overwritten by ringup's cmp before anything reads them. V is 0 on both versions: adc #8 cannot overflow with sp >= $F8, and adc #0 cannot overflow with sp+1 <= $7E on the no-fold path; on the fold path ringup's sbc sets V either way. No labels are added. Built: the Master CLEO is byte-identical; on the Model B TILCODE is 16 bytes smaller (4 expansions), SPR4CODE 4, SPR6CODE 4 and MNUCODE 4, 28 in all, and every link assert passes.

## r001:1  src/engine.s:548-550  (window i0128)  M -1 B -1  [high]  review: accept

Cycles: single path 2 cycles faster on both targets

Assumption: After the third `rol sp+1`, C = bit 5 of w16+1, which is 0 because every caller's w16 is a map char column < 1024 (maps are 32..256 tiles wide, low.s:86): drawrect (rc_x, window clipped by drawrect_clip / scroll_validate), copy_partial engine.s:2493-2498 (wcx + col), blank_below 2644-2648 (wcx), pl walker 3324 (pl_cx = w16, walked from wcx), menu.s:91-96 (tx>>1, w16+1 = 0), Master sprite path 1878 (wcx + sp_c0), the TOMAIN far thunk (same callers). ringaddr7 (bank 7 copy) is separate and not changed.

```
        rol sp+1                    ; sp+1:A = cx*8
        clc
        adc RINGLO,x
```
->
```
        rol sp+1                    ; sp+1:A = cx*8 (C = 0: cx < 8192)
        adc RINGLO,x
```

asl (C=bit7 of w16) / rol sp+1 (C out = bit7 of w16+1) / asl / rol (C=bit6 of w16+1) / asl / rol (C = bit5 of w16+1 = 0). So clc is a no-op; adc RINGLO,x adds with C = 0 as before.

Review: After the three asl/rol pairs, C = bit 5 of w16+1, which is 0 whenever cx < 8192. I checked every caller. drawrect gets rc_x = wcx + 0..79 through drawrect_clip or scroll_validate. copy_partial passes wcx + col, blank_below passes wcx, and the pl walker passes wcx plus at most VISLINES/4. The Master sprite path passes wcx + sp_c0 with sp_c0 in 0..79. The menu passes tx>>1 with a zero high byte. Every map is under 1024 chars wide. The adc sees the same C = 0, so clc is a no-op. Built: CODE and TILCODE are each 1 byte smaller.

## r001:2  src/engine.s:590-592  (window i0155)  M -1 B -1  [high]  review: accept

Cycles: this path 2 cycles faster on both; other paths untouched

Assumption: None outside the window: the only way here is the fall-through of `cmp wcy / bne :+` (line 587-588), i.e. rc_y = wcy, so C = 1; `ldx curbuf` and `lda rc_x` do not touch C. No label on the sec line.

```
        lda rc_x                    ; dirty column range (rects are window-clipped, so
        sec                         ; the low byte of rc_x - wcx is the column)
        sbc wcx
```
->
```
        lda rc_x                    ; dirty column range (rects are window-clipped, so
        sbc wcx                     ; the low byte of rc_x - wcx is the column; C = 1 from cmp wcy)
```

cmp with equal operands sets C = 1; bne not taken means equal. sec is therefore redundant; sbc wcx computes the same value and flags.

Review: The only way to line 590 is the fall-through of cmp wcy / bne :+, where rc_y == wcy, so C = 1. ldx curbuf and lda rc_x leave C alone, and the sec line has no label. Built: 1 byte smaller on both targets.

## r001:3  src/engine.s:596-598  (window i0162)  M -0 B -4  [high]  review: accept

Cycles: Model B: 11 -> 2 (deca = sta/dec/lda mtmp); Master identical code

Assumption: col + rc_w < 256 so adc leaves C = 0: col = rc_x - wcx is 0..79 and col + rc_w <= 80 for every drawrect entry (drawrect_clip engine.s:3366-3432 clips to ROWCHARS; scroll_validate 1180-1232 builds col 0 or 80-dx with rc_w = dx, or col 0 w 80). Only C/N/Z differ afterwards and `cmp PART_HI,x` (line 599) overwrites them all.

```
@plo:   clc
        adc rc_w
        deca
```
->
```
@plo:   clc
        adc rc_w
  .if MODELB
        sbc #0                      ; C = 0 (col + rc_w <= 80): the -1
  .else
        deca
  .endif
```

deca on the 6502 = A-1 with C preserved; sbc #0 with C = 0 = A - 0 - 1 = A-1 (mod 256, including A = 0 -> $FF when rc_w = 0 and col = 0). Same A into cmp PART_HI,x / sta PART_HI,x.

Review: col = rc_x - wcx is 0..79 and col + rc_w <= 80 on every path in: drawrect_clip's @right and rel<0 clips, and scroll_validate's dx-/dx+/@dorows/@full rects. So adc rc_w leaves C = 0, and sbc #0 gives A - 1, which matches deca, including 0 -> $FF. cmp PART_HI,x overwrites N, Z and C. This is Model B only: the Master CLEO is byte-identical, and TILCODE is 4 bytes smaller.

## r001:4  src/engine.s:603-610  (window i0172)  M -1 B -1  [high]  review: accept

Cycles: single path: sta tmp 3 -> tax 2, lsr tmp 5 -> cpx #1 2: 4 cycles faster on both

Assumption: rc_x+1 <= 3 (rc_x < 1024: maps are at most 256 tiles = 1024 chars wide, low.s:86, and the existing comment already assumes tx0 fits a byte), so X = rc_x+1 >> 1 is 0 or 1 and cpx #1 sets C = that bit. X is dead: nothing reads X between here and ringaddr, whose ringmod/tax overwrites it first (Master RINGROWS=32: and #31 then tax; Model B: ringmod's n2: tax). tmp is dead: its next uses in drawrect (@ghalf, @hcopy, @tpset, @hfill, @fillgo) and in maprow5/maprow/mapstrip all write it before reading.

```
        lda rc_x+1
        sta w16+1                   ; the ring address's copy, from this load too
        lsr
        sta tmp
        lda rc_x
        ror
        lsr tmp
        ror                         ; A = tx0 (map width <= 256 tiles)
```
->
```
        lda rc_x+1
        sta w16+1                   ; the ring address's copy, from this load too
        lsr                         ; C = bit 0, A = bit 1 (rc_x+1 <= 3)
        tax
        lda rc_x
        ror
        cpx #1                      ; C = bit 1
        ror                         ; A = tx0 (map width <= 256 tiles)
```

Original: C = hi bit0 into first ror, then lsr tmp puts hi bit1 into C for the second ror; tmp ends = hi>>2 (unused). New: lsr leaves A = hi>>1 in {0,1}, kept in X; cpx #1 gives C = (X >= 1) = hi bit1 without touching A. Same A = tx0; the C out of the final ror is bit1 of rc_x in both.

Review: rc_x < 1024 (it is window-clipped, and every map is at most 1024 chars wide), so rc_x+1 <= 3. After lsr, X is 0 or 1, and cpx #1 sets C to bit 1 of the high byte, as lsr tmp did. A and the final C are identical. X is dead: nothing in 611-627 reads it, and ringaddr's ringmod/tax writes it first on both targets. tmp is dead: every drawrect that gets this far writes tmp in @tpset or @solid/@fillgo (Master and Model B) before it reads it, so what the callers see on return is unchanged. Built: 1 byte smaller on both.

## r001:5  src/engine.s:612-626  (window i0189)  M -1 B -5  [high]  review: accept

Cycles: single path: Master 2 faster (clc gone); Model B 11 faster (clc gone, deca 11 -> sbc #0 2)

Assumption: rc_w <= 80 (drawrect_clip / scroll_validate), so (rc_x & 3) + rc_w <= 83 and the adc leaves C = 0 for the Model B sbc #0. Nothing between here and ringaddr reads rc_nt or rc_ro0, so storing ro0 before nt is invisible; the C/N/Z left at the end are dead (lda rc_y then ringaddr: ringmod's cmp (Model B) / the asl's (Master) set C before any use).

```
        lda rc_x                    ; tiles-1 = tx1 - tx0 = ((rc_x & 3) + rc_w - 1) >> 2,
        sta w16                     ; the copy the ring address needs, from the same load
        and #3                      ; so tx1 never needs building: max 3 + 80 - 1 = 82,
        sta rc_sc0
        clc                         ; one byte, no 16-bit shift, no w16
        adc rc_w
        deca
        lsr
        lsr
        sta rc_nt
        lda rc_sc0
        asl
        asl
        asl
        sta rc_ro0
```
->
```
        lda rc_x                    ; tiles-1 = tx1 - tx0 = ((rc_x & 3) + rc_w - 1) >> 2,
        sta w16                     ; the copy the ring address needs, from the same load
        and #3                      ; so tx1 never needs building: max 3 + 80 - 1 = 82,
        sta rc_sc0                  ; one byte, no 16-bit shift, no w16
        asl
        asl
        asl                         ; C = 0 (rc_sc0 < 4): the adc's clc
        sta rc_ro0
        lda rc_sc0
        adc rc_w
  .if MODELB
        sbc #0                      ; C = 0 (<= 83, no carry): the -1
  .else
        deca
  .endif
        lsr
        lsr
        sta rc_nt
```

Reordered: ro0 = sc0<<3 first; three asl of a value < 4 shift out zeros, so C = 0 exactly where the old clc stood (sta/lda do not touch C). lda rc_sc0 restores A = sc0; adc rc_w with C = 0 = sc0 + w; Master dec a / Model B sbc #0 (C = 0, no carry out of the adc) = sc0 + w - 1; lsr lsr; sta rc_nt. Same stored values in rc_sc0, rc_ro0, rc_nt, w16.

Review: ro0 = sc0<<3 is stored first. Three asl of a value under 4 shift out zeros, so C = 0 where the clc was. lda rc_sc0 / adc rc_w: the sum is at most 83, so C = 0 after it. On the Model B, sbc #0 is therefore -1, the same as deca. rc_nt, rc_ro0, rc_sc0 and w16 get the same stores. The A left at the end differs but is dead (lda rc_y is next), and ringaddr sets C before using it (Master asl, Model B ringmod cmp). rc_w = 0 gives the same result as well. Built: CODE is 1 byte smaller and TILCODE 5.

## r001:6  src/engine.s:653-657  (window i0209)  M -0 B -1  [medium]  review: accept

Cycles: Model B first tile row: 12 faster (one jsr/rts pair gone); every later row: jmp @rowy + jsr mapstrip -> jsr mapstrip + jmp @rowy, identical. Master unchanged.

Assumption: maprow5's only caller is engine.s:639 (grep: no other reference); mapstrip immediately follows maprow5 in the same segment LOWCODE (low.s:98-100). mapstrip's other caller dirfetch (low.s:120) is unaffected. mapstrip only needs ptr and rc_nt, both set before maprow5 is called (rc_nt at 621, ptr by maprow5 itself); it does not inspect the stack. The saved byte is in LOWCODE (low RAM, full). wrsel's recorded site is neither moved relative to its label nor resized; A at @rowy is the bank from mapstrip on both entry paths (jmp does not touch A).

```
@rowy:
  .if MODELB
        jsr mapstrip                ; the row's rc_nt+1 map bytes into MAPBUF, likewise
        wrsel BANK_TILES, BANK_TILES ; (mapstrip comes back with A = this bank: the
  .endif                            ;  write bank too, done here rather than in low RAM)
```
->
```
THREE HUNKS (one change):
--- HUNK 1: src/engine.s lines 653-657 become:
@rowy:
  .if MODELB
        wrsel BANK_TILES, BANK_TILES ; (mapstrip comes back with A = this bank: the
  .endif                            ;  write bank too, done here rather than in low RAM)
--- HUNK 2: src/engine.s lines 777-780, original:
  .if .not MODELB
        setbank BANK_MAP            ; @drawrow left the tile bank selected
  .endif
        jmp @rowy
--- become:
  .if .not MODELB
        setbank BANK_MAP            ; @drawrow left the tile bank selected
  .else
        jsr mapstrip                ; the next row's map bytes (maprow5 fetched the first)
  .endif
        jmp @rowy
--- HUNK 3: modelb/src/low.s line 98 (`        rts`, maprow5's last line, directly before mapstrip:) is deleted, so maprow5 falls into mapstrip; update maprow5's comment (line 89) to say it also fetches the first strip.
```

Before: maprow5 ... rts; @rowy: jsr mapstrip; wrsel. After: maprow5 ... falls into mapstrip ... rts back to drawrect directly before @rowy: wrsel -- same sequence of effects for the first row. For later rows @nextrow now calls mapstrip itself before jmp @rowy, so @rowy still runs wrsel with the fresh strip in MAPBUF. bank 5 bytes net 0 (jsr moved), low RAM -1.

Review: I applied all three hunks and built. The Master CLEO is byte-identical. On the Model B, LOWCODE is 1 byte smaller and TILCODE is the same size. maprow5's only caller is engine.s:639, and mapstrip directly follows it in LOWCODE. For the first row, drawrect runs no bank-5 code between the two calls (the jsr maprow5 is the last instruction before @rowy), so the order of effects is the same. Later rows run jsr mapstrip before jmp @rowy. @rowy's only reference is the jmp at 780. dirfetch is unaffected. The BANKREF/WRREC records are recomputed by the assembler, and neither wrsel nor bankimm changes size. LOWCODE has no fixed offsets (only the size < 256 assert), and ldprog/loader take addresses from labels.txt on every pass.

## r001:7  src/engine.s:708-714  (window i0250)  M -0 B -2  [high]  review: accept

Cycles: Model B half-tile path 2 faster; Master code not assembled (inside .if MODELB at 660)

Assumption: C out of the last asl (now bit 3 of k instead of 0) is dead: `ora #4` then `cpx half1` (line 716) sets C before any use; N/Z are set by ora. GATHERL's value is unchanged, so @hfill's lsr x5 (line 979) reads the same k&7.

```
        lda tmp
        and #7
        asl
        asl
        asl
        asl
        asl                         ; (k & 7) << 5
```
->
```
        lda tmp
        asl
        asl
        asl
        asl
        asl                         ; (k & 7) << 5: the shifts drop the rest
```

Five asl shift k left by 5 within 8 bits: bits 3..7 of k fall out and zeros fill bits 0..4, so the result is (k & 7) << 5 with or without the and #7.

Review: This is Model B only (inside .if MODELB). Five asl of an 8-bit value give (k & 7) << 5 whether or not the and #7 is there. Only the carry out differs, and cpx half1 sets C before bcs reads it. ora sets N and Z, so the always-taken bne after ora #1 still holds. GATHERL is unchanged. Built: the Master is identical and TILCODE is 2 bytes smaller.

## r002:0  src/engine.s:708-714  (window i0256)  M -0 B -2  [high]  review: accept

Cycles: Model B only (inside .if MODELB): @ghalf path 2 cycles faster (and #7 removed); no other path touched

Assumption: none outside the window: the carry the last asl leaves is overwritten by cpx half1 (line 716, or by the replacement's cpx half1) before anything reads it; N,Z are overwritten by ora/cpx

```
        lda tmp
        and #7
        asl
        asl
        asl
        asl
        asl                         ; (k & 7) << 5
```
->
```
        lda tmp
        asl
        asl
        asl
        asl
        asl                         ; (k & 7) << 5: the rest shifts out
```

asl x5 of an 8-bit value keeps only bits 0-2 (in bits 5-7) and zero-fills bits 0-4, so (k<<5)&$FF == (k&7)<<5 and the and #7 is redundant. The only difference is C after the last asl (bit 3 of k instead of 0), and C is next written by cpx half1 without being read.

Review: Lines match. tmp is 8-bit, so five asl keep only bits 0-2 and and #7 is redundant. C after the last asl is overwritten by cpx half1 (the original, or cand 1's first instruction) before anything reads it.

## r002:1  src/engine.s:715-722  (window i0262)  M -0 B -6  [high]  review: accept

Cycles: Model B only. id<half1: orig ora2+cpx4+bcs2+ora2+bne3=13, new cpx4+adc2+cpx4+bcc(taken)3=13; half1<=id<half2: 17->13; id>=half2: ora2+cpx4+bcs3+cpx4+bcs3=16 -> cpx4+adc2+cpx4+bcc2+eor2=14

Assumption: half1 <= half2 always: modelb/tools/assets.py:300-301 builds half1 = half0 + len(top), half2 = half1 + len(bot), and ldprog.s:326-330 copies them unchanged into half1/half2. A on entry has bits 0-4 clear (it is (k&7)<<5 from the asl chain), so +5/+6 never carries out of bits 0-2. C after the new code is not read (sta/dey/bpl then lda MAPBUF,y/cmp).

```
        ora #4                      ; a half: bit 2; the fill row's flag by range:
        cpx half1                   ; [top fills][bottom fills][both rows stored]
        bcs :+
        ora #1
        bne @gh2                    ; (always)
:       cpx half2
        bcs @gh2
        ora #2
```
->
```
        cpx half1                   ; a half: bit 2, the fill row's flag by range:
        adc #5                      ; below half1 4|1 (top fills), else 4|2 (C from cpx)
        cpx half2
        bcc @gh2                    ; below half2: done
:       eor #2                      ; from half2 (so from half1): 6 -> 4, both stored
```

Original: A|=4; if X<half1 A|=1 else if X<half2 A|=2. New: cpx half1 sets C = (X>=half1); adc #5 gives A+5 (=A|5) when X<half1, A+6 (=A|6) otherwise. cpx half2: if X<half2 done -> A|5 for X<half1 (and X<half1<=half2 so this is the only case X<half1 reaches), A|6 for half1<=X<half2. If X>=half2 then X>=half1 (half1<=half2) so A was A|6 and eor #2 makes it A|4. All three results equal the original.

Review: Lines match. half1 = half0+len(top) and half2 = half1+len(bot) (assets.py:300-301, hdr +25/+26), copied unchanged by ldprog.s:326-330, so half1<=half2. A has bits 0-4 clear, so adc #5/#6 = ora and there is no carry out. All three ranges give the original value. half1/half2 are absolute (TILBSS), so cpx is 3 bytes: 18 -> 12. No path is slower (13/17/16 -> 13/13/14). Final C matches the original's in every case and is not read (dey/bpl/lda/cmp). The anonymous label count is unchanged.

## r002:2  src/engine.s:744-745  (window i0278)  M -0 B -2  [high]  review: accept

Cycles: falls-through path: Master stz zp 3 -> sta zp 3; Model B lda#0 2 + sta 3 -> sta 3 (2 faster); taken path untouched

Assumption: none outside: A = rc_y & 1 = 0 on the fall-through of bne @second (lines 742-744); the next instruction lda rc_ro0 rewrites N,Z so the Model B stz's flags were not consumed

```
        bne @second
        stz rc_sub
```
->
```
        bne @second
        sta rc_sub                  ; A = 0: rc_y & 1 fell through
```

On fall-through of bne after and #1, A == 0, so storing A stores 0 exactly as stz does; A is reloaded by lda rc_ro0 next.

Review: A = rc_y&1 = 0 on the fall-through on both targets. rc_sub is zp: the Master stays 2 bytes, 3 cycles, and the Model B loses lda #0. The next lda rc_ro0 overwrites the flags. Preferred over candidate 3, which conflicts with it.

## r002:3  src/engine.s:742-744  (window i0277)  M -1 B -1  [medium]  review: reject

Cycles: lsr 2 = and #1 2; bcs/bne same taken/untaken cycles, both targets

Assumption: MUTUALLY EXCLUSIVE with the sta rc_sub candidate at lines 744-745 (that one needs A = 0 after the test; with lsr A = rc_y>>1 and stz rc_sub must stay stz). Prefer the sta rc_sub one on the Model B (2 bytes vs 1). Nothing after reads A or C before reloading: fall-through does stz rc_sub (Model B lda #0), @second does lda #32; C next consumed by... @drawrow's cmp sets C before rol.

```
        lda rc_y                    ; only a rect's first tile row can start on an odd
        and #1                      ; char row: after that @nextrow always lands even
        bne @second
```
->
```
        lda rc_y                    ; only a rect's first tile row can start on an odd
        lsr                         ; char row: after that @nextrow always lands even
        bcs @second
```

lsr moves bit 0 of rc_y into C; bcs taken iff rc_y odd, same as and #1/bne. A differs (rc_y>>1 vs rc_y&1) but is dead: both successors overwrite A (stz/ lda #32) before reading it; C differs but @drawrow's first C consumer (rol) follows a cmp.

Review: Correct alone, but it conflicts with candidate 2 (accepted, which saves more on the Model B), and both can be applied: 3's lines 742-744 are untouched by 2. With both, sta rc_sub stores rc_y>>1 (not 0) whenever rc_y is even and >=2, and rowoff/rc_sub go wrong on both targets. A Master-only lsr/bcs split (keeping stz on the Master) could save 1 Master byte as a separate follow-up.

## r002:4  src/engine.s:752-784  (window i0307)  M -3 B -2  [high]  review: accept

Cycles: identical on every path: one inc rc_y per @drawrow call either way; Master jmp @rowy 3 -> bne taken 3

Assumption: rc_y is read nowhere in @drawrow or what it calls (grep rc_y: only lines 564-642, 742 and the other routines at 1203+/3368+/3678; @drawrow spans 782-1130 and calls only halfpair_tp, which does not touch it); @drawrow is entered only by the two jsr @drawrow at lines 752/765; no interrupt code reads rc_y. Also folds in the Master jmp @rowy -> bne @rowy (Master setbank ends lda #BANK_MAP=6 / sta / sta so Z=0; @rowy -89 bytes away); the standalone 777-780 candidate is its fallback

```
        jsr @drawrow
        inc rc_y
        dec rc_h
        beq @done
@second:
        lda #32
        sta rc_sub
        ora rc_ro0
        sta rowoff
  .if MODELB
        lda #2
        sta rowbit
  .endif
        jsr @drawrow
        inc rc_y
        dec rc_h
        beq @done
@nextrow:                           ; one map row on
        lda ptr
        clc
        adc MAPSTRIDE
        sta ptr
        lda ptr+1
        adc MAPSTRIDE+1
        sta ptr+1
  .if .not MODELB
        setbank BANK_MAP            ; @drawrow left the tile bank selected
  .endif
        jmp @rowy
@done:  rts
@drawrow:
        ; ---- screen base (per-rect ringaddr, +640 per row)
        lda rc_sp
```
->
```
        jsr @drawrow
        dec rc_h
        beq @done
@second:
        lda #32
        sta rc_sub
        ora rc_ro0
        sta rowoff
  .if MODELB
        lda #2
        sta rowbit
  .endif
        jsr @drawrow
        dec rc_h
        beq @done
@nextrow:                           ; one map row on
        lda ptr
        clc
        adc MAPSTRIDE
        sta ptr
        lda ptr+1
        adc MAPSTRIDE+1
        sta ptr+1
  .if .not MODELB
        setbank BANK_MAP            ; @drawrow left the tile bank selected
        bne @rowy                   ; (always: A = BANK_MAP, not zero)
  .else
        jmp @rowy
  .endif
@done:  rts
@drawrow:
        inc rc_y                    ; the row this draws: nothing in @drawrow reads rc_y
        ; ---- screen base (per-rect ringaddr, +640 per row)
        lda rc_sp
```

Both call sites did jsr @drawrow / inc rc_y / dec rc_h / beq. Doing the inc at the start of @drawrow instead of right after it returns changes only when rc_y changes during the call, and nothing reads it there. dec rc_h stays at the call sites so beq still tests it. The first instruction of @drawrow, lda rc_sp, rewrites N,Z; no C use. The Master's bne after setbank is always taken (Z clear from lda #6).

Review: Lines match. @drawrow (782 to its rts at ~965, plus @solid/@hfill/@fslow and halfpair_tp) never reads rc_y, and the only readers of rc_y are drawrect's prologue, 742 (after the gather, when the inc has already happened in both schemes) and other routines. @drawrow is entered only by the two jsrs. inc does not touch C, and N,Z are overwritten by lda rc_sp. The count of incs per path is unchanged. On the Master, setbank ends lda #6 / sta / sta (wrsel is empty), so Z=0 and bne is always taken. The distance is 89 bytes in build/main.lst, less after the change. rc_y is zp.

## r002:5  src/engine.s:777-780  (window i0305)  M -1 B -0  [high]  review: accept

Cycles: Master: jmp 3 -> bne taken 3 (same); Model B unchanged

Assumption: Master setbank = lda #BANK_MAP / sta ROMSEL_CPY / sta ROMSEL (bankimm/wrsel empty on the Master), BANK_MAP = 6 (engine.s:48) so Z = 0; @rowy is -89 bytes away in build/main.lst ($F1 -> $9A). FALLBACK: superseded by the 752-784 candidate, which includes this change

```
  .if .not MODELB
        setbank BANK_MAP            ; @drawrow left the tile bank selected
  .endif
        jmp @rowy
```
->
```
  .if .not MODELB
        setbank BANK_MAP            ; @drawrow left the tile bank selected
        bne @rowy                   ; (always: A = BANK_MAP, not zero)
  .else
        jmp @rowy
  .endif
```

After lda #6 and two stores Z is clear, so bne is always taken to the same target as the jmp.

Review: This is the fallback, a subset of candidate 4. After lda #BANK_MAP(6)/sta/sta, Z=0, so bne always goes to @rowy, which is in range. Same cycles.

## r002:6  src/engine.s:794-801  (window i0320)  M -0 B -2  [high]  review: accept

Cycles: straight line: Master stz zp 3 -> sta zp 3; Model B saves lda #0 (2 cycles)

Assumption: rc_gi is not read between (only at @run, line 805); sta does not affect C, so rol still takes C from the cmp at line 790/792; the sec at 802 follows

```
        lda #0
        rol
        sta rc_wrap
        lda rc_sc0
        sta rc_subc
        lda rc_w
        sta cnt
        stz rc_gi
```
->
```
        lda #0
        sta rc_gi
        rol
        sta rc_wrap
        lda rc_sc0
        sta rc_subc
        lda rc_w
        sta cnt
```

The store of 0 to rc_gi moves up to where A already holds 0 (before the rol); sta alters no flags and C is still the compare's for the rol.

Review: Lines match. sta does not touch C, so rol still takes the cmp's C. rc_gi (zp) is not read until @run. The Model B stz flags feed only sec/ldx. The Master is the same size and cycles.

## r002:7  src/engine.s:806-809  (window i0326)  M -1 B -1  [high]  review: accept

Cycles: asl 2 = and #$40 2; bpl/beq same both ways; both targets

Assumption: C must stay 1 on the non-solid path (the sbc rc_subc at @tpset and any bcs-always on @hcopy rely on it): GATHERH bit 7 is set for every entry. Master: build_tileaddr (engine.s:4629-4640) writes $80|id>>2 or $C0. Model B gather: full $81+id>>2 (id<FLAT0=$F0 -> <= $BC), half halfhi+k>>3 (the halves' page in bank 5 below B5X $B620, assets.py:327), fill $C0. Solid paths reset C themselves (Master and Model B @fillgo sec; @hfill sec).

```
        lda GATHERH,x               ; bit 6: solid cyan/black tile, filled not copied
        sta tp+1                    ; it is also the tile pointer's high byte, so keep it
        and #$40                    ; now rather than load it a second time below (the
        beq :+                      ; solid path does not read tp+1, so writing it is free)
```
->
```
        lda GATHERH,x               ; bit 6: solid cyan/black tile, filled not copied
        sta tp+1                    ; it is also the tile pointer's high byte, so keep it
        asl                         ; bit 6 to N; C = bit 7, set for every entry (a
        bpl :+                      ; tile page is $80-$BF, a fill $C0): the sec holds
```

asl moves bit 6 into bit 7 so bpl is taken exactly when bit 6 was clear, as beq after and #$40 was. A is reloaded by lda GATHERL,x on the taken path and ignored by @solid (which reloads). C becomes bit 7 of GATHERH, which is 1 for every value the gather writes, so C = 1 is preserved into @tpset.

Review: asl puts bit 6 in N, so bpl is taken exactly when beq was. C becomes GATHERH bit 7, which is set for every value written. Master: $80|id>>2 or $C0 (build_tileaddr, LV_PAGE0 constant). Model B: >TILES+id>>2 with TILES_BASE $8100, halfhi+k>>3 below B5X $B620, fill $C0. So C stays 1, and the solid paths do sec themselves anyway. A is reloaded on both paths.

## r002:8  src/engine.s:818-831  (window i0344)  M -2 B -9  [high]  review: accept

Cycles: Model B @hcopy: 23 -> 18 (lda3+and2+sta3+lda4+and2+ora3+sta3+jmp3 vs lda4+eor3+and2+eor3+bcs3+sta3); full tile path: and #$C0 removed, 2 cycles faster on both targets

Assumption: C = 1 at @hcopy: set before @run (sec line 802, and @runend's sbc on the loop back per the comment at 802), and nothing from @run to @hcopy writes C (ldx/lda/sta/and/beq/and/beq; with the asl candidate C = GATHERH bit 7 = 1 too). tmp is dead: @tpset writes it (sta tmp) before any read. rowoff zp (engine.s:231). GATHERL for a tile reaching the and #$C0: Master (id&3)<<6 (build_tileaddr, engine.s:4623-4629; solids take @solid); Model B @full only when GATHERL&7 = 0 and GATHERH bit 6 clear = a full tile, lo (id&3)<<6 (line 684-687); halves always have bit 2 set.

```
@hcopy: lda rowoff                  ; no: the stored row -- this run's char offset,
        and #$1F                    ; without the row's 32
        sta tmp
        lda GATHERL,x
        and #$E0
        ora tmp
        sta tp
        jmp @tpset
@full:  lda GATHERL,x
  .endif
        and #$C0
        ora rowoff
        sta tp
@tpset:
```
->
```
@hcopy: lda GATHERL,x               ; no: the stored row -- (k&7)<<5 with this run's
        eor rowoff                  ; char offset less the row's 32: rowoff's bits
        and #$E0                    ; outside $E0 through the two eors
        eor rowoff
        bcs @tpsta                  ; (always: C is set at every entry to @run)
@full:  lda GATHERL,x
  .endif
        ora rowoff                  ; (a full tile's lo byte is (id&3)<<6: bits 0-5 clear)
@tpsta: sta tp
@tpset:
```

hcopy: ((G^R)&$E0)^R has G's bits in $E0 and R's bits outside it, = (G&$E0)|(R&$1F), the original tp. bcs is always taken (C=1) to the shared sta tp, which falls into @tpset exactly as the jmp @tpset did after its own sta tp. full/Master: G has bits 0-5 clear so G&$C0 = G; ora/sta unchanged. The new cheap label @tpsta does not end any scope.

Review: Lines match. ((G^R)&$E0)^R = (G&$E0)|(R&$1F), the original tp. C=1 at @hcopy: set by sec at 802, and @runend's sec/sbc with rc_n<=cnt leaves it set on the loop back. Nothing in @run up to @hcopy writes C (with cand 7, C = bit 7 = 1). tmp is written at 843 before any read. X is still rc_gi. @full/Master: GATHERL of any non-solid tile is (id&3)<<6 (bits 0-5 clear), and Model B @full is only full tiles (halves have bit 2 set; flats go to @solid), so and #$C0 is the identity. @tpsta is a cheap label. Cycles 23->18 and -2 on the full path.

## r002:9  src/engine.s:818-825  (window i0340)  M -0 B -5  [high]  review: accept

Cycles: Model B @hcopy: 23 -> 18 cycles

Assumption: FALLBACK for the merged 818-831 candidate (superseded if that applies). C = 1 at @hcopy (see merged candidate); tmp dead (@tpset writes it first).

```
@hcopy: lda rowoff                  ; no: the stored row -- this run's char offset,
        and #$1F                    ; without the row's 32
        sta tmp
        lda GATHERL,x
        and #$E0
        ora tmp
        sta tp
        jmp @tpset
```
->
```
@hcopy: lda GATHERL,x               ; no: the stored row -- (k&7)<<5 with this run's
        eor rowoff                  ; char offset less the row's 32: rowoff's bits
        and #$E0                    ; outside $E0 through the two eors
        eor rowoff
        sta tp
        bcs @tpset                  ; (always: C is set at every entry to @run)
```

((G^R)&$E0)^R = (G&$E0)|(R&$1F); bcs always taken with C = 1, like the jmp.

Review: This is the fallback, a subset of candidate 8. Same reasoning: the eor-mask identity, C=1 always at @hcopy, and tmp dead until 843.

## r002:10  src/engine.s:826-830  (window i0344)  M -2 B -2  [high]  review: accept

Cycles: full-tile path 2 cycles faster, both targets

Assumption: FALLBACK for the merged 818-831 candidate. GATHERL of every tile reaching here has bits 0-5 clear (see merged candidate).

```
@full:  lda GATHERL,x
  .endif
        and #$C0
        ora rowoff
        sta tp
```
->
```
@full:  lda GATHERL,x
  .endif
        ora rowoff                  ; (a full tile's lo byte is (id&3)<<6: bits 0-5 clear)
        sta tp
```

and #$C0 of a value with bits 0-5 clear is the identity; ora then sets the same flags.

Review: This is the fallback, a subset of candidate 8. Every GATHERL reaching here has bits 0-5 clear on both targets.

## r002:11  src/engine.s:855-855  (window i0362)  M -0 B -2  [high]  review: accept

Cycles: Model B: 7 cycles faster (pha 3 + pla 4 gone); Master unchanged

Assumption: A dead at the four targets: @b31/@b23/@b15 start ldy #8c / lda (tp),y, @b7 starts ldaz tp (ldy #0 / lda (tp),y) or its replacement ldy/lda; none reads A, N or Z first. C is untouched either way (pla does not write C), so C clear into @advsp holds.

```
:       jmpx @jt-2
```
->
```
:
  .if MODELB
        lda @jt-2,x                 ; jmpx less its pha/pla: every @b entry
        sta jv                      ; loads A before it reads it
        lda @jt-1,x
        sta jv+1
        jmp (jv)
  .else
        jmpx @jt-2
  .endif
```

Identical to the jmpx macro expansion (cpu.inc) minus pha and pla; A's value restored by pla was never read.

Review: Matches the current cpu.inc jmpx expansion minus pha/pla. The only targets are @b7/@b15/@b23/@b31 (X=2*rc_n, 1..4). Each starts with ldy then lda (tp),y, so A, N and Z are dead. C is untouched either way. The ':' is kept, so the anonymous count is unchanged. build/mainb.lst is stale (13 Sep), so its jmpx differs, but the current macro has pha/pla.

## r002:12  src/engine.s:866-869  (window i0368)  M -0 B -3  [high]  review: accept

Cycles: Model B @b7 entry: ldy2+lda5+ldy2+sta6+ldy2 = 17 -> ldy2+lda5+sta6+iny2 = 15; Master unchanged

Assumption: CHARCPY 0 is expanded only at @b7 (line 915), entered by the @jt word table (labels, not computed offsets); Y is 0 after lda (tp),y so staz's ldy #0 was redundant and iny gives 1 as ldy #1 did; N,Z after iny (Y=1) are overwritten by the following CPYN's lda

```
.if c = 0
        ldaz tp                     ; line 0 non-indexed
        staz sp
        ldy #1
```
->
```
.if c = 0
  .if ::MODELB
        ldy #0                      ; line 0 (staz would reload the same 0 into Y)
        lda (tp),y
        sta (sp),y
        iny
  .else
        ldaz tp                     ; line 0 non-indexed
        staz sp
        ldy #1
  .endif
```

Model B ldaz tp / staz sp / ldy #1 expands to ldy #0 / lda (tp),y / ldy #0 / sta (sp),y / ldy #1; dropping the repeated ldy #0 and using iny for 0->1 gives the same memory effects and Y=1.

Review: CHARCPY 0 is used only at @b7 (CHARPER is unused). The Model B ldaz/staz leave Y=0, so the second ldy #0 is redundant and iny gives Y=1 with the same flags as ldy #1. The Master is unchanged. Saves 3 bytes, 17->15 cycles.

## r003:0  src/engine.s:916-916  (window i0405)  M -3 B -3  [high]  review: accept

Cycles: copy path through @b7 (every char-copy run): 3 cycles faster on both; nothing else changes

Assumption: none: @advsp is the very next instruction (listing t_b.lst: 4C at $2A2, @advsp at $2A5); the jmp is the leftover of the MODE 2 @t/@p blocks deleted in bdeb55b

```
        jmp @advsp
```
->
```

```

jmp @advsp immediately followed by @advsp: is a no-op jump; deleting it only removes 3 bytes. Also reported by r004 (same edit) -- apply once.

Review: Line 916 is `jmp @advsp` and @advsp is the next line, so the jump is a no-op. It has no effect on flags or registers. Measured alone: Master CODE -3, Model B TILCODE -3. Same edit as r004 #3. It is also folded into candidate 9's fixed proposal, so it goes stale once that applies. GATE NOTE for the orchestrator: tools/gate.sh's 'B title' check (modelb/tools/btitlediff.mjs) samples display RAM at fixed cycle counts from boot, so ANY change to boot-loaded Model B code (BANKS size/timing) fails it. Candidate 0 alone (a provably no-op jmp) gives 'DIFFERS on 7/30'; the original source gives 'identical on 30'. Every other gate check passed on a scratch build of candidates 9(fixed)+3+4+5+6+7+11(fixed)+12 together (Master CODE -16, Model B -45): Master state/pix/dmg/title, Model B lockstep L0/5/12, pix L0/6.

## r003:1  src/engine.s:939-954  (window i0434)  M -8 B -8  [high]  review: accept

Cycles: @slow (ring-crossing copy run, <= once per row): setup lda rc_n/sta tmp2 (6) gone; per char: bcc :+ taken 3 (or not-taken 2 + inc 5) gone, dec tmp2 (5) -> dex/dex (4); every path faster, both targets

Assumption: X = 2*rc_n on entry to @slow: @tpset's asl/tax (engine.s:839-840), then asl/asl/sta/ldy rc_wrap/beq/adc/lda/adc/bcc/jmp -- no X write; nothing in the loop writes X: lda/sta (zp),y, dey, spnext (engine.s:520-530: lda/clc/adc/sta/bcc/lda/inca(mtmp)/ringup(cmp/bcc/sbc/pha/lda/sec/sbc/sta/pla/sbc)/sta, Master inc a) -- also true of r001/r004's spnext rewrites. X dead after: @runend -> jmp @run -> ldx rc_gi (805); @rowdone -> rts -> drawrect (inc rc_y..., @second -> @drawrow -> @run ldx; @nextrow -> @rowy: Model B mapstrip (low.s:100, Y only) then gather writes X with tax before any read; Master tax) or @done rts -> callers: scroll_validate 1206/1232 (lda w16b.../ldx curbuf), erase_old 1363 via callbank/pagelogic (X passed through, then @next lda rp/inc/dec/bne @l: ldx lidx), draw_dirty 3683 (dec lcnt/bne @l: ldy lidx; ldx curbuf), bank5_entry tail. tmp2 no longer written: its value after drawrect already depends on the path (only this path wrote it), and @fslow writes it before reading. tp+1: a run's source is the 32-byte char row of one tile (tp = (GATHERL&$C0 or &$E0) | rowoff, rowoff&$1F = 8*rc_subc via rc_ro0 = rc_sc0<<3 (engine.s:622-626) or 0, and rc_n <= 4-rc_subc), so tp+8 can carry only after the LAST char; tp is referenced only in drawrect/halfpair_tp and menu.s (which writes it first), and @run writes tp+1 (sta tp+1, 807) and tp (@tpset/@hcopy/@solid) before any read

```
        lda rc_n
        sta tmp2
@sc:    ldy #7
:       lda (tp),y
        sta (sp),y
        dey
        bpl :-
        lda tp
        clc
        adc #8
        sta tp
        bcc :+
        inc tp+1
:       spnext
        dec tmp2
        bne @sc
```
->
```
@sc:    ldy #7                      ; X = 2*rc_n (@tpset's tax) counts the chars down
:       lda (tp),y
        sta (sp),y
        dey
        bpl :-
        lda tp                      ; a run stays inside its tile's 32-byte char row, so
        clc                         ; this never carries before the last char, and after
        adc #8                      ; that tp is dead (@run rewrites both bytes)
        sta tp
:       spnext                      ; (the label KEPT, unreferenced: anonymous count)
        dex
        dex
        bne @sc
```

lda rc_n/sta tmp2 + dec tmp2/bne @sc (count rc_n chars) == dex/dex/bne @sc with X = 2*rc_n (rc_n in 1..4, X 2..8, reaches 0 after exactly rc_n iterations; Z=1 on fall-through as before). bcc :+/inc tp+1 removed: never taken before the last char (32-byte-row argument), and after the last char tp is dead. C/N/Z at exit: bne falls through with Z=1 as before; C comes from spnext either way; the following bra @runend (or r003's beq) sees Z=1 in both.

Review: Original matches 939-954. X = 2*rc_n at @slow's only entry (the tax at 840, and nothing after it writes X). The loop body and spnext never write X, both today and under the r000/r001/r004 rewrites, so dex/dex/bne counts rc_n chars (rc_n 1..4). tp+8 carries only after the last char. Full tiles have tp=(GATHERL&$C0)|rowoff, with rowoff=32*sub+8*rc_subc (rc_ro0=rc_sc0<<3, later rc_sub). Model B halves have tp=(GATHERL&$E0)|(rowoff&$1F). And rc_n<=4-rc_subc. tp/tp+1 are rewritten at @run/@tpset/@solid/halfpair before any read. X is dead after drawrect because every caller reloads it: scroll_validate @done ldx curbuf, erase_old @l ldx lidx then scroll_validate ldx curbuf, draw_dirty ldx curbuf, and far calls load X. The gather writes X before it reads it. tmp2 only loses a write. Every path is faster. Measured 8/8. Folded into candidate 9's fixed proposal.

## r003:2  src/engine.s:955-955  (window i0435)  M -0 B -1  [high]  review: accept

Cycles: 3 -> 3 (Model B jmp vs taken beq); Master bra 3 -> beq taken 3

Assumption: Z = 1 here: the bne @sc (engine.s:954, or dex/dex/bne of the candidate above) has just fallen through; @runend is $5A bytes back in the Model B listing (in reach). MUTUALLY EXCLUSIVE with the @slow move candidate (853-955), which puts @runend out of reach and is worth more: apply this only if that one is rejected

```
        bra @runend
```
->
```
        beq @runend                 ; (Z = 1: the count ran out)
```

bra on the Model B is a 3-byte jmp; beq on a Z proven 1 is the same unconditional transfer in 2 bytes.

Review: Z=1 after bne @sc falls through, and beq reaches @runend (built). Correct on its own (Model B -1). It becomes MOOT once candidate 9's fixed proposal applies: the `bra @runend` line disappears, and since it is unique this candidate just goes stale.

## r003:3  src/engine.s:957-959  (window i0438)  M -1 B -1  [high]  review: accept

Cycles: @rowdone (once per char row): 2 cycles faster on both

Assumption: @rowdone's only entry is beq @rowdone at engine.s:930, right after lda cnt/sec/sbc rc_n/sta cnt: no borrow because rc_n <= cnt (rc_n = min(4-rc_subc, cnt), 833-838, both unchanged since), so C = 1 there; beq/sta do not write C. <ROWBYTES = $80 on both targets (ROWCHARS 80 * 8 = 640)

```
        clc
        lda rc_sp                   ; next char row: +640 with ring wrap
        adc #<ROWBYTES
```
->
```
        lda rc_sp                   ; next char row: +640 with ring wrap
        adc #(<ROWBYTES)-1          ; C = 1: @runend's sbc cannot borrow (rc_n <= cnt)
```

clc/adc #$80 == (C=1) adc #$7F: same sum (A+$80) and the same carry out into the adc #>ROWBYTES. N/Z/V are not read (lda rc_sp+1 follows).

Review: @rowdone's only entry is beq @rowdone after lda cnt/sec/sbc rc_n. rc_n=min(4-rc_subc,cnt)<=cnt, so C=1 there. adc #$7F with C=1 equals adc #$80 with C=0: same A and same carry into adc #>ROWBYTES. Measured 1/1. It lies outside candidate 9's range, so the two compose.

## r003:4  src/engine.s:973-973  (window i0450)  M -0 B -1  [high]  review: accept

Cycles: Model B @solid: jmp 3 -> bcs taken 3; Master unaffected (.if MODELB)

Assumption: C = 1 at every entry to @run: the sec at engine.s:802 precedes it and the only other entry is @runend's jmp @run (engine.s:935) after lda cnt/sec/sbc rc_n (no borrow: rc_n = min(4-rc_subc, cnt) <= cnt, engine.s:833-838) then stz (Model B lda #0/sta)/lda/sta/inc, none of which write C; @run up to jmp @solid / jmp @hfill is ldx/lda/sta/and/beq/and/beq only. (If r002's asl/bpl rewrite of 806-809 lands, C = bit 7 of GATHERH there, which is 1 for every entry: tiles $80-$BF, halves' page in bank 5, fills $C0 -- still C = 1.) @solid (Model B) is ldy/lda/sta/lda/sta, none write C.

```
        jmp @fillgo
```
->
```
        bcs @fillgo                 ; (always: C is set at every entry to @run)
```

jmp @fillgo -> bcs @fillgo, C provably 1; @fillgo is ~25 bytes on (in reach; assembled).

Review: C=1 at every @run entry: the sec at 802, and @runend's sbc with rc_n<=cnt. Nothing from @run to jmp @solid writes C, and neither does the Model B @solid (ldy/lda/sta/lda/sta). Under r002 #7's asl, C = GATHERH bit 7, which is 1 for fills ($C0). bcs taken is 3 cycles, the same as jmp. Measured Model B -1.

## r003:5  src/engine.s:974-989  (window i0466)  M -0 B -2  [high]  review: accept

Cycles: Model B @hfill: sec (2) gone, asl x3 + lsr x5 + asl (18) -> asl x4 + lsr x4 (16): 4 cycles faster

Assumption: C = 1 at every entry to @run: the sec at engine.s:802 precedes it and the only other entry is @runend's jmp @run (engine.s:935) after lda cnt/sec/sbc rc_n (no borrow: rc_n = min(4-rc_subc, cnt) <= cnt, engine.s:833-838) then stz (Model B lda #0/sta)/lda/sta/inc, none of which write C; @run up to jmp @solid / jmp @hfill is ldx/lda/sta/and/beq/and/beq only. (If r002's asl/bpl rewrite of 806-809 lands, C = bit 7 of GATHERH there, which is 1 for every entry: tiles $80-$BF, halves' page in bank 5, fills $C0 -- still C = 1.) A half tile's GATHERL (the only kind reaching @hfill: GATHERL&7 != 0) is ((k&7)<<5)|4|(1 or 2) with bits 3,4 clear (gather, engine.s:708-722; still true under r001/r002's rewrites of 708-722: (k&7)<<5 then +5/+6/eor #2 touch bits 0-2 only). C after is not read: jsr halfpair_tp (lda/sta/rts) then @fillgo's sec

```
@hfill: lda GATHERH,x               ; the half's pair: k back out of its address
        sec
        sbc halfhi
        asl
        asl
        asl
        sta tmp
        lda GATHERL,x
        lsr
        lsr
        lsr
        lsr
        lsr
        ora tmp
        asl
        tay
```
->
```
@hfill: lda GATHERH,x               ; the half's pair: k back out of its address
        sbc halfhi                  ; (C is set at every entry to @run)
        asl
        asl
        asl
        asl
        sta tmp                     ; (k >> 3) << 4
        lda GATHERL,x
        lsr
        lsr
        lsr
        lsr                         ; (k & 7) << 1: a half's GATHERL has bits 4 and 3 clear
        ora tmp
        tay
```

Y = ((((H-hh)<<3) | (L>>5)) << 1) & $FF = ((H-hh)<<4 | (L>>5)<<1) & $FF, and (L>>5)<<1 = L>>4 when bit 4 of L is 0, so asl x4 on H-hh and lsr x4 on L give the same Y. sbc halfhi without sec: C = 1 at entry. N,Z after: from tay (Y) in both. tmp is overwritten at @fillgo either way.

Review: C=1 at @hfill by the same argument. The halves' GATHERH is in bank 5, so bit 7 is set under r002 #7 too. Y = ((H-hh)<<4 | (L>>5)<<1)&$FF. A half's GATHERL = (k&7)<<5|4|{0,1,2} has bits 3 and 4 clear, also under the r001/r002 rewrites of 708-722, so L>>4 = (L>>5)<<1. C afterwards is dead (@fillgo keeps its sec on the Model B), and @fillgo rewrites tmp. 4 cycles faster. Measured Model B -2.

## r003:6  src/engine.s:996-996  (window i0471)  M -1 B -0  [high]  review: accept

Cycles: Master @solid: lda #imm 2 -> dec a 2; Model B untouched (Master-only .else block)

Assumption: A = $10 here: and #$10 then beq not taken. In the .else (Master) arm deca is dec a

```
        lda #$0F                    ; four dots of logical 1 (cyan); else 0 = black
```
->
```
        deca                        ; $10 -> $0F: four dots of logical 1 (cyan); else 0 = black
```

A = $10 -> dec a -> $0F, the same fill value; N,Z are overwritten by nothing that reads them (sta tp, then lda #4). Master only.

Review: Master-only arm. A=$10 after and #$10 when beq is not taken, and dec a gives $0F. C is untouched, which candidate 7 relies on. N/Z come from $0F either way, and sta tp/lda #4 follow. Measured Master -1, Model B identical. The line is carried verbatim in candidate 11's fixed proposal, so it still applies afterwards.

## r003:7  src/engine.s:1000-1000  (window i0473)  M -1 B -0  [high]  review: accept

Cycles: Master @fillgo: sec (2) gone; Model B identical (keeps sec)

Assumption: C = 1 at every entry to @run: the sec at engine.s:802 precedes it and the only other entry is @runend's jmp @run (engine.s:935) after lda cnt/sec/sbc rc_n (no borrow: rc_n = min(4-rc_subc, cnt) <= cnt, engine.s:833-838) then stz (Model B lda #0/sta)/lda/sta/inc, none of which write C; @run up to jmp @solid / jmp @hfill is ldx/lda/sta/and/beq/and/beq only. (If r002's asl/bpl rewrite of 806-809 lands, C = bit 7 of GATHERH there, which is 1 for every entry: tiles $80-$BF, halves' page in bank 5, fills $C0 -- still C = 1.) On the Master @fillgo is reached only by falling out of @solid (lda GATHERL,x/and/beq/lda or deca/sta: no C write), and @solid only by jmp @solid (810). The Model B keeps the sec: its @hfill path arrives with C from shifts

```
        sec
```
->
```
  .if MODELB
        sec                         ; (@hfill's path; the Master's one entry has C = 1)
  .endif
```

sbc rc_subc sees C = 1 with or without the sec on the Master.

Review: On the Master @fillgo has no label, because it sits inside .if MODELB. Line 999 is reached only by falling out of the Master @solid. @solid is entered only by jmp @solid (810), with C=1 from @run's entry, and it keeps C (lda/and/beq/lda-or-dec a/sta). The Model B keeps its sec. Measured Master -1. Note for apply: the original is a bare `sec`, and nearest-match picks line 1000. The other bare secs at 927 and 1161 are more than 70 lines away. Candidate 11's fixed proposal keeps the line count, and its +3 lines above 1000 put the estimate out by only 3 lines.

## r003:8  src/engine.s:1024-1029  (window i0492)  M -0 B -4  [high]  review: accept

Cycles: Model B fill dispatch: lda tp+1 (3) + pha (3) + pla (4) gone: 10 cycles faster; Master text unchanged

Assumption: A is dead at every @ft entry on the Model B: @f31/@f23/@f15/@f7 each begin with FIL1 (engine.s:1032-1036: ldy #k / lda tp+1 / sta (sp),y), which loads tp+1 itself. X = 2*rc_n from @fillgo's tax. jv is the jmpx macro's own vector (cpu.inc), used identically

```
  .if MODELB
:       lda tp+1                    ; the entries are odd lines
  .else
:       lda tp
  .endif
        jmpx @ft-2
```
->
```
  .if MODELB
:       lda @ft-2,x                 ; jmpx less its pha/pla, and no load: A is dead,
        sta jv                      ; every entry is a FIL1, which loads tp+1 itself
        lda @ft-1,x
        sta jv+1
        jmp (jv)
  .else
:       lda tp
        jmpx @ft-2
  .endif
```

jmpx on the Model B = pha/lda tab,x/sta jv/lda tab+1,x/sta jv+1/pla/jmp (jv); with A dead the pha/pla and the preceding lda tp+1 do nothing. If r004's bundle (1012-1132) is applied instead, this still applies to its result: delete the 'lda tp+1 ; the entries are odd lines' it keeps before jmp (jv) (+2 bytes Model B beyond r004's).

Review: Model B: every @ft target begins with FIL1 (ldy #k / lda tp+1), so A and N/Z at the dispatch are dead. The lda tp+1 and jmpx's pha/pla can go. C is untouched, so C=0 into @advsp still holds. The ':' stays for the beq :+ at 1012. Measured Model B -4 alone. r004 #0 (7 bytes) overlaps and would make this stale; both are folded into candidate 11's fixed proposal.

## r003:9  src/engine.s:853-955  (window i0392)  M -2 B -3  [high]  review: fix

Cycles: copy run, no crossing: bcc taken 3 -> bcs not taken 2; crossing: bcc 2 + jmp 3 -> bcs 3; @slow's exit bra 3 -> jmp 3 (Master), jmp -> jmp (Model B). Faster or equal everywhere

Assumption: @slow has one entry (jmp @slow, 854) and ends in bra @runend; nothing falls into or out of it. The copy blocks @b31.. are entered only through @jt (jmpx), so code placed between @jt and @b31 is never fallen into from above (jmpx ends in a jump). Only local anonymous refs in the moved span (bpl :-, bcc :+, spnext/ringup internals); 845's beq :+ still reaches 855's ':' first. Mutually exclusive with the beq @runend candidate (955)

```
        bcc :+
        jmp @slow
:       jmpx @jt-2
@jt:    .word @b7, @b15, @b23, @b31
        ; unrolled copy, one block per char in descending char order so that entry at
        ; char n-1 copies chars n-1..0.  A cell whose first byte has bit 7 set repeats
        ; lines 0..3 as 4..7 (flagged by convert.py): 4 loads, 8 stores.
.macro CPYN                         ; next line: A = (tp),y -> (sp),y ; y++
        lda (tp),y
        sta (sp),y
        iny
.endmacro
.macro CHARCPY c, per
.if c = 0
        ldaz tp                     ; line 0 non-indexed
        staz sp
        ldy #1
.else
        ldy #8*c
        lda (tp),y
        sta (sp),y
        iny
.endif
        CPYN
        CPYN
        CPYN
        CPYN
        CPYN
        CPYN
        lda (tp),y                  ; line 7
        sta (sp),y
.endmacro
.macro CHARPER c, next              ; A = line 0 (Y = 8c unless c = 0)
.if c = 0
        staz sp
.else
        sta (sp),y
.endif
        ldy #8*c+4
        sta (sp),y
        ldy #8*c+1
        lda (tp),y
        sta (sp),y
        ldy #8*c+5
        sta (sp),y
        ldy #8*c+2
        lda (tp),y
        sta (sp),y
        ldy #8*c+6
        sta (sp),y
        ldy #8*c+3
        lda (tp),y
        sta (sp),y
        ldy #8*c+7
        sta (sp),y
.if .paramcount = 2                 ; @p0 omits it: @advsp is the next instruction
        jmp next
.endif
.endmacro
@b31:   CHARCPY 3, @t3
@b23:   CHARCPY 2, @t2
@b15:   CHARCPY 1, @t1
@b7:    CHARCPY 0, @t0
        jmp @advsp
@advsp: lda sp
        adc tmp                     ; C is already clear at every entry to @advsp
        sta sp
        bcc :++
        inc sp+1                    ; no ringup: both entries to @advsp have already
:                                   ; proved the run stays below RINGEND (rc_wrap).
        ; KEPT, unreferenced, so the anonymous-label count through drawrect is unchanged
:
@runend:
        lda cnt
        sec
        sbc rc_n
        sta cnt
        beq @rowdone
        stz rc_subc
        lda rc_sub
        sta rowoff                  ; later tiles in the row start at column 0
        inc rc_gi
        jmp @run
@slow:  ; a run that crosses the ring end: copy a char at a time through the fold.  This
        ; sits after the common path so @advsp can fall into @runend -- it is reached at
        ; most once per row, so the jump back is free and the branch saved is not.
        lda rc_n
        sta tmp2
@sc:    ldy #7
:       lda (tp),y
        sta (sp),y
        dey
        bpl :-
        lda tp
        clc
        adc #8
        sta tp
        bcc :+
        inc tp+1
:       spnext
        dec tmp2
        bne @sc
        bra @runend
```
->
```
        bcs @slow
:       jmpx @jt-2
@jt:    .word @b7, @b15, @b23, @b31
@slow:  ; a run that crosses the ring end: copy a char at a time through the fold.  It
        ; sits beside its test so that a branch reaches it (at most once per row).
        lda rc_n
        sta tmp2
@sc:    ldy #7
:       lda (tp),y
        sta (sp),y
        dey
        bpl :-
        lda tp
        clc
        adc #8
        sta tp
        bcc :+
        inc tp+1
:       spnext
        dec tmp2
        bne @sc
        jmp @runend                 ; (out of bra's reach from here)
        ; unrolled copy, one block per char in descending char order so that entry at
        ; char n-1 copies chars n-1..0.  A cell whose first byte has bit 7 set repeats
        ; lines 0..3 as 4..7 (flagged by convert.py): 4 loads, 8 stores.
.macro CPYN                         ; next line: A = (tp),y -> (sp),y ; y++
        lda (tp),y
        sta (sp),y
        iny
.endmacro
.macro CHARCPY c, per
.if c = 0
        ldaz tp                     ; line 0 non-indexed
        staz sp
        ldy #1
.else
        ldy #8*c
        lda (tp),y
        sta (sp),y
        iny
.endif
        CPYN
        CPYN
        CPYN
        CPYN
        CPYN
        CPYN
        lda (tp),y                  ; line 7
        sta (sp),y
.endmacro
.macro CHARPER c, next              ; A = line 0 (Y = 8c unless c = 0)
.if c = 0
        staz sp
.else
        sta (sp),y
.endif
        ldy #8*c+4
        sta (sp),y
        ldy #8*c+1
        lda (tp),y
        sta (sp),y
        ldy #8*c+5
        sta (sp),y
        ldy #8*c+2
        lda (tp),y
        sta (sp),y
        ldy #8*c+6
        sta (sp),y
        ldy #8*c+3
        lda (tp),y
        sta (sp),y
        ldy #8*c+7
        sta (sp),y
.if .paramcount = 2                 ; @p0 omits it: @advsp is the next instruction
        jmp next
.endif
.endmacro
@b31:   CHARCPY 3, @t3
@b23:   CHARCPY 2, @t2
@b15:   CHARCPY 1, @t1
@b7:    CHARCPY 0, @t0
        jmp @advsp
@advsp: lda sp
        adc tmp                     ; C is already clear at every entry to @advsp
        sta sp
        bcc :++
        inc sp+1                    ; no ringup: both entries to @advsp have already
:                                   ; proved the run stays below RINGEND (rc_wrap).
        ; KEPT, unreferenced, so the anonymous-label count through drawrect is unchanged
:
@runend:
        lda cnt
        sec
        sbc rc_n
        sta cnt
        beq @rowdone
        stz rc_subc
        lda rc_sub
        sta rowoff                  ; later tiles in the row start at column 0
        inc rc_gi
        jmp @run
```

adc ringneg/adc #$80 sets C iff the run crosses the ring end; bcc :+/jmp @slow/: == bcs @slow when @slow is within reach, which it is once placed after the @jt table (+11 bytes). @slow's final bra @runend is now ~170 bytes before @runend, so it is spelled jmp (Model B already a jmp; Master +1). Assembled alone: Master CODE -2, Model B TILCODE -3. COMPOSES with 916/939-954 (and r002's 855/866-869 edits): carry those edits into the text; the moved block is @slow verbatim (or as edited) with bra -> jmp.

Review: The move is correct. bcs @slow is equivalent to bcc :+/jmp @slow. Nothing falls into @slow, because jmpx ends in a jump and the table follows it, and @slow ends in jmp @runend. The macro definitions emit no code. Anonymous references checked in the listing: 845 beq :+ -> the jmpx ':', @advsp bcc :++ -> @runend, bpl :- stays local. As submitted, though, it can never apply: candidates 1 (8 bytes) and 0 (3/3) sort ahead of it and edit lines inside its original, which makes it stale. FIX: the same move with candidate 0 (drop jmp @advsp) and candidate 1 (dex/dex count, no tp+1 carry) folded in, as the candidate itself says to do. Those two then go stale instead. Measured on the combined text: Master CODE -13, Model B TILCODE -14, against 11/12 for 0+1+2. Lines 855 and 866-869 are kept verbatim so r002 #11/#12 still apply afterwards. Built and gated: every check passes except the cycle-sampled B title (see candidate 0's note).

Fixed:
```
        bcs @slow
:       jmpx @jt-2
@jt:    .word @b7, @b15, @b23, @b31
@slow:  ; a run that crosses the ring end: copy a char at a time through the fold.  It
        ; sits beside its test so that a branch reaches it (at most once per row).
@sc:    ldy #7                      ; X = 2*rc_n (@tpset's tax) counts the chars down
:       lda (tp),y
        sta (sp),y
        dey
        bpl :-
        lda tp                      ; a run stays inside its tile's 32-byte char row, so
        clc                         ; this never carries before the last char, and after
        adc #8                      ; that tp is dead (@run rewrites both bytes)
        sta tp
:       spnext                      ; (the label KEPT, unreferenced: anonymous count)
        dex
        dex
        bne @sc
        jmp @runend                 ; (out of bra's reach from here)
        ; unrolled copy, one block per char in descending char order so that entry at
        ; char n-1 copies chars n-1..0.  A cell whose first byte has bit 7 set repeats
        ; lines 0..3 as 4..7 (flagged by convert.py): 4 loads, 8 stores.
.macro CPYN                         ; next line: A = (tp),y -> (sp),y ; y++
        lda (tp),y
        sta (sp),y
        iny
.endmacro
.macro CHARCPY c, per
.if c = 0
        ldaz tp                     ; line 0 non-indexed
        staz sp
        ldy #1
.else
        ldy #8*c
        lda (tp),y
        sta (sp),y
        iny
.endif
        CPYN
        CPYN
        CPYN
        CPYN
        CPYN
        CPYN
        lda (tp),y                  ; line 7
        sta (sp),y
.endmacro
.macro CHARPER c, next              ; A = line 0 (Y = 8c unless c = 0)
.if c = 0
        staz sp
.else
        sta (sp),y
.endif
        ldy #8*c+4
        sta (sp),y
        ldy #8*c+1
        lda (tp),y
        sta (sp),y
        ldy #8*c+5
        sta (sp),y
        ldy #8*c+2
        lda (tp),y
        sta (sp),y
        ldy #8*c+6
        sta (sp),y
        ldy #8*c+3
        lda (tp),y
        sta (sp),y
        ldy #8*c+7
        sta (sp),y
.if .paramcount = 2                 ; @p0 omits it: @advsp is the next instruction
        jmp next
.endif
.endmacro
@b31:   CHARCPY 3, @t3
@b23:   CHARCPY 2, @t2
@b15:   CHARCPY 1, @t1
@b7:    CHARCPY 0, @t0
@advsp: lda sp
        adc tmp                     ; C is already clear at every entry to @advsp
        sta sp
        bcc :++
        inc sp+1                    ; no ringup: both entries to @advsp have already
:                                   ; proved the run stays below RINGEND (rc_wrap).
        ; KEPT, unreferenced, so the anonymous-label count through drawrect is unchanged
:
@runend:
        lda cnt
        sec
        sbc rc_n
        sta cnt
        beq @rowdone
        stz rc_subc
        lda rc_sub
        sta rowoff                  ; later tiles in the row start at column 0
        inc rc_gi
        jmp @run
```

## r003:10  src/engine.s:1015-1132  (window i0488)  M -0 B -3  [high]  review: accept

Cycles: Model B fill run, no crossing: bcc taken 3 -> bcs not taken 2; crossing: bcc 2 + jmp 3 -> bcs 3; Master unchanged

Assumption: @fslow has one entry (1017/1022) and ends in jmp @runend; the cascade @f31..@f7 is entered only through @ft (jmpx) and ends in jmp @advsp, so @fslow placed between the FIL macro definitions and @f31 is never fallen into. It must follow the macro definitions (it uses FIL1/FILN/FILO on the Model B)

```
  .if MODELB
        adc ringneg
        bcc @fnear                  ; (the 6502 spellings put @fslow out of reach)
        jmp @fslow
@fnear:
  .else
        adc #(256 - >RINGEND)
        bcs @fslow
  .endif
  .if MODELB
:       lda tp+1                    ; the entries are odd lines
  .else
:       lda tp
  .endif
        jmpx @ft-2
@ft:    .word @f7, @f15, @f23, @f31
  .if MODELB                        ; the pair alternates down the lines: each entry
.macro FIL1 k                       ; is an odd line, so the cascade's parity is fixed
        ldy #k
        lda tp+1
        sta (sp),y
.endmacro
.macro FILN                         ; the next line down is even: tp
        dey
        lda tp
        sta (sp),y
.endmacro
.macro FILO                         ; and the one below odd: tp+1
        dey
        lda tp+1
        sta (sp),y
.endmacro
  .else
.macro FIL1 k
        ldy #k
        sta (sp),y
.endmacro
.macro FILN                         ; next line down: y-- ; A -> (sp),y
        dey
        sta (sp),y
.endmacro
.macro FILO
        dey
        sta (sp),y
.endmacro
  .endif
@f31:   FIL1 31
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f23:   FIL1 23
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f15:   FIL1 15
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f7:    FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
  .if MODELB
        lda tp                      ; line 0 is even
  .endif
        staz sp                     ; line 0 non-indexed
        jmp @advsp
@fslow: lda rc_n
        sta tmp2
@fsc:
  .if MODELB
        FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        lda tp
  .else
        lda tp
        ldy #7
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
  .endif
        staz sp                     ; line 0 non-indexed
:                                   ; KEPT, unreferenced, so the anonymous-label
        ; count through drawrect is unchanged
        spnext
        dec tmp2
        bne @fsc
        jmp @runend
```
->
```
  .if MODELB
        adc ringneg
  .else
        adc #(256 - >RINGEND)
  .endif
        bcs @fslow                  ; (@fslow sits after the table: in reach on both)
  .if MODELB
:       lda tp+1                    ; the entries are odd lines
  .else
:       lda tp
  .endif
        jmpx @ft-2
@ft:    .word @f7, @f15, @f23, @f31
  .if MODELB                        ; the pair alternates down the lines: each entry
.macro FIL1 k                       ; is an odd line, so the cascade's parity is fixed
        ldy #k
        lda tp+1
        sta (sp),y
.endmacro
.macro FILN                         ; the next line down is even: tp
        dey
        lda tp
        sta (sp),y
.endmacro
.macro FILO                         ; and the one below odd: tp+1
        dey
        lda tp+1
        sta (sp),y
.endmacro
  .else
.macro FIL1 k
        ldy #k
        sta (sp),y
.endmacro
.macro FILN                         ; next line down: y-- ; A -> (sp),y
        dey
        sta (sp),y
.endmacro
.macro FILO
        dey
        sta (sp),y
.endmacro
  .endif
@fslow: lda rc_n
        sta tmp2
@fsc:
  .if MODELB
        FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        lda tp
  .else
        lda tp
        ldy #7
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
  .endif
        staz sp                     ; line 0 non-indexed
:                                   ; KEPT, unreferenced, so the anonymous-label
        ; count through drawrect is unchanged
        spnext
        dec tmp2
        bne @fsc
        jmp @runend
@f31:   FIL1 31
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f23:   FIL1 23
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f15:   FIL1 15
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f7:    FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
  .if MODELB
        lda tp                      ; line 0 is even
  .endif
        staz sp                     ; line 0 non-indexed
        jmp @advsp
```

Model B adc ringneg/bcc @fnear/jmp @fslow == adc/bcs @fslow once @fslow is within reach (it sits after the table and the macros, ~25 bytes on). Master already had bcs @fslow. Assembled: Model B TILCODE -3, Master 0. SAME MOVE as r004's bundle (1012-1132), which also includes the jmpx change and the FILN tail -- apply one; see the 1024-1029 candidate for the extra 2 bytes on top of r004's.

Review: Correct. @fslow has one entry and ends in jmp @runend. The cascade is entered only through @ft and ends in jmp @advsp. @fslow comes after the FIL macro definitions. Its ':' and spnext's labels move ahead of a cascade that has no labels, so no reference is retargeted. The bcs reaches (built). Measured Model B -3, Master 0. It will go stale: r004 #0 (7) supersedes it, and candidate 11's fixed proposal contains the same move.

## r003:11  src/engine.s:990-1146  (window i0467)  M -0 B -4  [high]  review: fix

Cycles: Model B @hfill: jsr (6) + rts (6) gone: 12 cycles faster; Master unaffected (.if MODELB)

Assumption: halfpair_tp has exactly one caller, @hfill (grep: engine.s:990); HPAIR0/HPAIR1 are written only by ldprog.s:410-418 through their labels, which build.sh takes from labels.txt 'al' lines -- := assignments are emitted there (checked: al .HPAIR0 = @hp0+1, .HPAIR1 = @hp1+1 in a linked Model B build). TILCODE is RAM (B5X rw), as before. A '=' or ':=' symbol ends the @ scope in ca65, which is why the definitions sit after the row loop and HPAIR1 is written relative to HPAIR0 (an .assert guards the 5)

```
        jsr halfpair_tp             ; the pair, from where the loader put the table
@fillgo:
  .else
@solid: lda GATHERL,x
        and #$10
        beq :+
        lda #$0F                    ; four dots of logical 1 (cyan); else 0 = black
:       sta tp                      ; fill value (tp is otherwise unused on this path)
  .endif
        lda #4                      ; (@fillgo)
        sec
        sbc rc_subc
        cmp cnt
        bcc :+
        lda cnt
:       sta rc_n
        asl
        tax
        asl
        asl
        sta tmp
        ldy rc_wrap                 ; test without destroying A (= 8*rc_n)
        beq :+
        adc sp                      ; C is clear: the asl's above shifted out zeros (rc_n <= 4)
        lda sp+1
  .if MODELB
        adc ringneg
        bcc @fnear                  ; (the 6502 spellings put @fslow out of reach)
        jmp @fslow
@fnear:
  .else
        adc #(256 - >RINGEND)
        bcs @fslow
  .endif
  .if MODELB
:       lda tp+1                    ; the entries are odd lines
  .else
:       lda tp
  .endif
        jmpx @ft-2
@ft:    .word @f7, @f15, @f23, @f31
  .if MODELB                        ; the pair alternates down the lines: each entry
.macro FIL1 k                       ; is an odd line, so the cascade's parity is fixed
        ldy #k
        lda tp+1
        sta (sp),y
.endmacro
.macro FILN                         ; the next line down is even: tp
        dey
        lda tp
        sta (sp),y
.endmacro
.macro FILO                         ; and the one below odd: tp+1
        dey
        lda tp+1
        sta (sp),y
.endmacro
  .else
.macro FIL1 k
        ldy #k
        sta (sp),y
.endmacro
.macro FILN                         ; next line down: y-- ; A -> (sp),y
        dey
        sta (sp),y
.endmacro
.macro FILO
        dey
        sta (sp),y
.endmacro
  .endif
@f31:   FIL1 31
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f23:   FIL1 23
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f15:   FIL1 15
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f7:    FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
  .if MODELB
        lda tp                      ; line 0 is even
  .endif
        staz sp                     ; line 0 non-indexed
        jmp @advsp
@fslow: lda rc_n
        sta tmp2
@fsc:
  .if MODELB
        FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        lda tp
  .else
        lda tp
        ldy #7
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
  .endif
        staz sp                     ; line 0 non-indexed
:                                   ; KEPT, unreferenced, so the anonymous-label
        ; count through drawrect is unchanged
        spnext
        dec tmp2
        bne @fsc
        jmp @runend

  .if MODELB
; Y = a half tile's index * 2 -> tp, tp+1 = its pair.  The table sits above the
; halves, wherever the level's tiles ended: the loader patches these two operands.
; (A routine of its own, after the row loop: a label inside it would end its scope.)
halfpair_tp:
        .byte $B9                   ; lda HALFPAIR,y
HPAIR0: .word $FFFF
        sta tp
        .byte $B9                   ; lda HALFPAIR+1,y
HPAIR1: .word $FFFF
        sta tp+1
        rts
  .endif
```
->
```
@hp0:   lda $FFFF,y                 ; lda HALFPAIR,y: the pair, from where the loader
        sta tp                      ; put the table (it patches both operands: HPAIR0,
@hp1:   lda $FFFF,y                 ; HPAIR1, defined after the row loop)
        sta tp+1
@fillgo:
  .else
@solid: lda GATHERL,x
        and #$10
        beq :+
        lda #$0F                    ; four dots of logical 1 (cyan); else 0 = black
:       sta tp                      ; fill value (tp is otherwise unused on this path)
  .endif
        lda #4                      ; (@fillgo)
        sec
        sbc rc_subc
        cmp cnt
        bcc :+
        lda cnt
:       sta rc_n
        asl
        tax
        asl
        asl
        sta tmp
        ldy rc_wrap                 ; test without destroying A (= 8*rc_n)
        beq :+
        adc sp                      ; C is clear: the asl's above shifted out zeros (rc_n <= 4)
        lda sp+1
  .if MODELB
        adc ringneg
        bcc @fnear                  ; (the 6502 spellings put @fslow out of reach)
        jmp @fslow
@fnear:
  .else
        adc #(256 - >RINGEND)
        bcs @fslow
  .endif
  .if MODELB
:       lda tp+1                    ; the entries are odd lines
  .else
:       lda tp
  .endif
        jmpx @ft-2
@ft:    .word @f7, @f15, @f23, @f31
  .if MODELB                        ; the pair alternates down the lines: each entry
.macro FIL1 k                       ; is an odd line, so the cascade's parity is fixed
        ldy #k
        lda tp+1
        sta (sp),y
.endmacro
.macro FILN                         ; the next line down is even: tp
        dey
        lda tp
        sta (sp),y
.endmacro
.macro FILO                         ; and the one below odd: tp+1
        dey
        lda tp+1
        sta (sp),y
.endmacro
  .else
.macro FIL1 k
        ldy #k
        sta (sp),y
.endmacro
.macro FILN                         ; next line down: y-- ; A -> (sp),y
        dey
        sta (sp),y
.endmacro
.macro FILO
        dey
        sta (sp),y
.endmacro
  .endif
@f31:   FIL1 31
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f23:   FIL1 23
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f15:   FIL1 15
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f7:    FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
  .if MODELB
        lda tp                      ; line 0 is even
  .endif
        staz sp                     ; line 0 non-indexed
        jmp @advsp
@fslow: lda rc_n
        sta tmp2
@fsc:
  .if MODELB
        FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        lda tp
  .else
        lda tp
        ldy #7
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
  .endif
        staz sp                     ; line 0 non-indexed
:                                   ; KEPT, unreferenced, so the anonymous-label
        ; count through drawrect is unchanged
        spnext
        dec tmp2
        bne @fsc
        jmp @runend

  .if MODELB
; @hfill's two loads of a half tile's pair: the table sits above the halves, wherever
; the level's tiles ended, and the loader patches the operands.  (Defined here, after
; the row loop: a label would end its @ scope, and := puts them in labels.txt.)
        .assert @hp1 = @hp0 + 5, error, "HPAIR1 must be 5 bytes past HPAIR0"
HPAIR0  := @hp0 + 1                 ; (the first := ends the @ scope: HPAIR1 goes by it)
HPAIR1  := HPAIR0 + 5
  .endif
```

The subroutine body (lda HALFPAIR,y / sta tp / lda HALFPAIR+1,y / sta tp+1) is placed inline at the call, the rts goes, and the two patched-operand labels become := HPAIR0/HPAIR1 at the operands' addresses. Y, the loads and the stores are identical; flags after: from lda HALFPAIR+1,y in both. Assembled and linked: Model B TILCODE -4. Only lines 990 and 1134-1146 change; 991-1133 are carried verbatim, so this COMPOSES with 1015-1132/1024-1029 (or r004's bundle): take the middle from whatever is current.

Review: The inlining is correct. halfpair_tp's only caller is line 990. ldprog.s:410-418 patches HPAIR0/HPAIR1 through labels.txt 'al' lines, and the only tool that names them is modelb/build.sh's want list. They come out at @hp0+1/@hp1+1: checked, labels $B970/$B975 = TILCODE $B620 + $34F/$354 + 1. The := after the row loop ends the @ scope harmlessly. Flags come from the last lda either way, and it is 12 cycles faster. As submitted, though, it spans 990-1146, and r004 #0 (1012-1132, 7 bytes, accepted) sorts ahead of it and would leave it stale. FIX: this candidate with r004 #0 and candidate 8 folded in. r004 #0 is bcs @fslow, @fslow moved before the cascade, jmpx without pha/pla, and FILN for the line-0 tails: dey/lda tp/sta equals lda tp/ldy #0/sta at 11=11 cycles, with Y=0 and C unchanged. Candidate 8 drops the lda tp+1 at the dispatch, because every entry's FIL1 reloads it. Measured Model B TILCODE -13, Master identical. Lines 996 and 1000 are verbatim so candidates 6/7 still apply.

Fixed:
```
@hp0:   lda $FFFF,y                 ; lda HALFPAIR,y: the pair, from where the loader
        sta tp                      ; put the table (it patches both operands: HPAIR0,
@hp1:   lda $FFFF,y                 ; HPAIR1, defined after the row loop)
        sta tp+1
@fillgo:
  .else
@solid: lda GATHERL,x
        and #$10
        beq :+
        lda #$0F                    ; four dots of logical 1 (cyan); else 0 = black
:       sta tp                      ; fill value (tp is otherwise unused on this path)
  .endif
        lda #4                      ; (@fillgo)
        sec
        sbc rc_subc
        cmp cnt
        bcc :+
        lda cnt
:       sta rc_n
        asl
        tax
        asl
        asl
        sta tmp
        ldy rc_wrap                 ; test without destroying A (= 8*rc_n)
        beq :+
        adc sp                      ; C is clear: the asl's above shifted out zeros (rc_n <= 4)
        lda sp+1
  .if MODELB
        adc ringneg
  .else
        adc #(256 - >RINGEND)
  .endif
        bcs @fslow                  ; (@fslow sits before the cascade: in reach on both)
  .if MODELB
:       lda @ft-2,x                 ; jmpx @ft-2 less its pha/pla, and no load: A is
        sta jv                      ; dead, every entry is a FIL1, which loads tp+1
        lda @ft-1,x
        sta jv+1
        jmp (jv)
  .else
:       lda tp
        jmpx @ft-2
  .endif
@ft:    .word @f7, @f15, @f23, @f31
  .if MODELB                        ; the pair alternates down the lines: each entry
.macro FIL1 k                       ; is an odd line, so the cascade's parity is fixed
        ldy #k
        lda tp+1
        sta (sp),y
.endmacro
.macro FILN                         ; the next line down is even: tp
        dey
        lda tp
        sta (sp),y
.endmacro
.macro FILO                         ; and the one below odd: tp+1
        dey
        lda tp+1
        sta (sp),y
.endmacro
  .else
.macro FIL1 k
        ldy #k
        sta (sp),y
.endmacro
.macro FILN                         ; next line down: y-- ; A -> (sp),y
        dey
        sta (sp),y
.endmacro
.macro FILO
        dey
        sta (sp),y
.endmacro
  .endif
@fslow: lda rc_n
        sta tmp2
@fsc:
  .if MODELB
        FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN                        ; line 0 (even): Y = 1 -> 0
  .else
        lda tp
        ldy #7
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        staz sp                     ; line 0 non-indexed
  .endif
:                                   ; KEPT, unreferenced, so the anonymous-label
        ; count through drawrect is unchanged
        spnext
        dec tmp2
        bne @fsc
        jmp @runend
@f31:   FIL1 31
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f23:   FIL1 23
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f15:   FIL1 15
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f7:    FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
  .if MODELB
        FILN                        ; line 0 (even): Y = 1 -> 0
  .else
        staz sp                     ; line 0 non-indexed
  .endif
        jmp @advsp

  .if MODELB
; @hfill's two loads of a half tile's pair: the table sits above the halves, wherever
; the level's tiles ended, and the loader patches the operands.  (Defined here, after
; the row loop: a label would end its @ scope, and := puts them in labels.txt.)
        .assert @hp1 = @hp0 + 5, error, "HPAIR1 must be 5 bytes past HPAIR0"
HPAIR0  := @hp0 + 1                 ; (the first := ends the @ scope: HPAIR1 goes by it)
HPAIR1  := HPAIR0 + 5
  .endif
```

## r003:12  src/engine.s:493-511  (window i0473)  M -0 B -14  [high]  review: accept

Cycles: every Model B ringup fold (fold path only): sec (2) gone; Master ringup has no sec (unchanged)

Assumption: ringehi is >RINGEND of the buffer ($44 or $80, select_backbuf engine.s:3035-3036) and >RINGBYTES = $39 (23*640 = $3980). On the fold path cmp ringehi/bcc not taken gives C = 1 and A >= ringehi >= $44, so sbc #>RINGBYTES does not borrow: C = 1; pha and lda p do not write C. No expansion site is inside a computed-entry block (sites: 528, 554, 963, 2372/2405 sprite tails, 2607/2617/2657, 3310, 4841, menu.s 106/132)

```
.macro ringup p                     ; p names the pointer whose high byte A holds;
  .if ::MODELB                        ; the Master's fold never needs it
        cmp ringehi                 ; the buffer's ring end, high byte (select_backbuf)
        bcc :+
        sbc #>RINGBYTES             ; C = 1 from the compare, and stays 1: A >= >RINGEND
        pha                         ; > >RINGBYTES.  The low byte folds by $80, which
        lda p                       ; borrows from A when p is below $80
        sec
        sbc #<RINGBYTES
        sta p
        pla
        sbc #0
:
  .else
        cmp #>RINGEND
        bcc :+
        sbc #>RINGBYTES
:
  .endif
```
->
```
.macro ringup p                     ; p names the pointer whose high byte A holds;
  .if ::MODELB                        ; the Master's fold never needs it
        cmp ringehi                 ; the buffer's ring end, high byte (select_backbuf)
        bcc :+
        sbc #>RINGBYTES             ; C = 1 from the compare, and stays 1: A >= >RINGEND
        pha                         ; > >RINGBYTES.  The low byte folds by $80, which
        lda p                       ; borrows from A when p is below $80 (C still 1)
        sbc #<RINGBYTES
        sta p
        pla
        sbc #0
:
  .else
        cmp #>RINGEND
        bcc :+
        sbc #>RINGBYTES
:
  .endif
```

The sec before sbc #<RINGBYTES re-sets a carry that is provably already 1 (the comment on the sbc #>RINGBYTES line says so). NOTE: the macro body (engine.s:493-511) lies in r000's windows, reached here through 'ringup rc_sp' at 963; dedupe with r000 if it reports it. Assembled and linked (Model B): TILCODE -7, LGCCODE -1, MNUCODE -2, SPR4CODE -2, SPR6CODE -2 (14 total); Master identical.

Review: On the fold path, cmp ringehi with bcc not taken leaves C=1 and A>=ringehi. ringehi is $44 or $80 (RINGEND_A=$4400, RINGEND_B=$8000), both above >RINGBYTES=$39, so sbc #$39 cannot borrow. pha/lda p keep C, so the sec is a no-op. No expansion site is inside a computed-entry block: the sprite dispatch and copy_partial jump through label tables. The Master arm is unchanged. Measured alone Model B -14 (TILCODE 7, LGCCODE 1, MNUCODE 2, SPR4CODE 2, SPR6CODE 2). The saving shrinks once r000 #0/r001 #0 remove spnext's ringup. Same edit as r000 #1, so whichever applies first makes the other stale. r000 #2's Master lines 507-509 are kept verbatim.

## r004:0  src/engine.s:1012-1132  (window i0484)  M -0 B -7  [high]  review: accept

Cycles: Model B: fast fill path bcc-taken 3 -> bcs-not-taken 2, dispatch pha/pla removed (-7); slow fill path bcc-not-taken 2 + jmp 3 -> bcs-taken 3; cascade/slow-loop last line: lda tp 3 + ldy #0 2 + sta 6 -> dey 2 + lda tp 3 + sta 6 (equal). Master: every instruction identical (only the @fslow block is moved), same cycles.

Assumption: The @fslow block is entered only by the branch at 1017/1022 (grep @fslow/@fsc: no other references) and ends in jmp @runend, so it can sit anywhere without fall-in; the cascade @f31..@f7 is reached only through the @ft jump table (jmpx), so moving code above it changes nothing; the FIL macros emit no code so the block must follow their definitions (it does). Y = 1 after FIL1 7 + six FILN/FILO (7 - 6), so dey reaches 0 as ldy #0 did; Y = 0 afterwards on both paths (spnext / @advsp do not read Y anyway). jv is the jmpx macro's own zp vector (cpu.inc) -- same stores as the macro. Model B bcs @fslow reaches: +$17 in the test build.

```
        beq :+
        adc sp                      ; C is clear: the asl's above shifted out zeros (rc_n <= 4)
        lda sp+1
  .if MODELB
        adc ringneg
        bcc @fnear                  ; (the 6502 spellings put @fslow out of reach)
        jmp @fslow
@fnear:
  .else
        adc #(256 - >RINGEND)
        bcs @fslow
  .endif
  .if MODELB
:       lda tp+1                    ; the entries are odd lines
  .else
:       lda tp
  .endif
        jmpx @ft-2
@ft:    .word @f7, @f15, @f23, @f31
  .if MODELB                        ; the pair alternates down the lines: each entry
.macro FIL1 k                       ; is an odd line, so the cascade's parity is fixed
        ldy #k
        lda tp+1
        sta (sp),y
.endmacro
.macro FILN                         ; the next line down is even: tp
        dey
        lda tp
        sta (sp),y
.endmacro
.macro FILO                         ; and the one below odd: tp+1
        dey
        lda tp+1
        sta (sp),y
.endmacro
  .else
.macro FIL1 k
        ldy #k
        sta (sp),y
.endmacro
.macro FILN                         ; next line down: y-- ; A -> (sp),y
        dey
        sta (sp),y
.endmacro
.macro FILO
        dey
        sta (sp),y
.endmacro
  .endif
@f31:   FIL1 31
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f23:   FIL1 23
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f15:   FIL1 15
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f7:    FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
  .if MODELB
        lda tp                      ; line 0 is even
  .endif
        staz sp                     ; line 0 non-indexed
        jmp @advsp
@fslow: lda rc_n
        sta tmp2
@fsc:
  .if MODELB
        FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        lda tp
  .else
        lda tp
        ldy #7
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
  .endif
        staz sp                     ; line 0 non-indexed
:                                   ; KEPT, unreferenced, so the anonymous-label
        ; count through drawrect is unchanged
        spnext
        dec tmp2
        bne @fsc
        jmp @runend
```
->
```
        beq :+
        adc sp                      ; C is clear: the asl's above shifted out zeros (rc_n <= 4)
        lda sp+1
  .if MODELB
        adc ringneg
  .else
        adc #(256 - >RINGEND)
  .endif
        bcs @fslow                  ; (@fslow sits before the cascade: in reach on both)
  .if MODELB
:       lda @ft-2,x                 ; jmpx @ft-2, less its pha/pla: A is loaded last
        sta jv
        lda @ft-1,x
        sta jv+1
        lda tp+1                    ; the entries are odd lines
        jmp (jv)
  .else
:       lda tp
        jmpx @ft-2
  .endif
@ft:    .word @f7, @f15, @f23, @f31
  .if MODELB                        ; the pair alternates down the lines: each entry
.macro FIL1 k                       ; is an odd line, so the cascade's parity is fixed
        ldy #k
        lda tp+1
        sta (sp),y
.endmacro
.macro FILN                         ; the next line down is even: tp
        dey
        lda tp
        sta (sp),y
.endmacro
.macro FILO                         ; and the one below odd: tp+1
        dey
        lda tp+1
        sta (sp),y
.endmacro
  .else
.macro FIL1 k
        ldy #k
        sta (sp),y
.endmacro
.macro FILN                         ; next line down: y-- ; A -> (sp),y
        dey
        sta (sp),y
.endmacro
.macro FILO
        dey
        sta (sp),y
.endmacro
  .endif
@fslow: lda rc_n
        sta tmp2
@fsc:
  .if MODELB
        FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN                        ; line 0 (even): Y = 1 -> 0
  .else
        lda tp
        ldy #7
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        dey
        sta (sp),y
        staz sp                     ; line 0 non-indexed
  .endif
:                                   ; KEPT, unreferenced, so the anonymous-label
        ; count through drawrect is unchanged
        spnext
        dec tmp2
        bne @fsc
        jmp @runend
@f31:   FIL1 31
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f23:   FIL1 23
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f15:   FIL1 15
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
        FILN
@f7:    FIL1 7
        FILN
        FILO
        FILN
        FILO
        FILN
        FILO
  .if MODELB
        FILN                        ; line 0 (even): Y = 1 -> 0
  .else
        staz sp                     ; line 0 non-indexed
  .endif
        jmp @advsp
```

(1) Model B: adc ringneg / bcc @fnear / jmp @fslow becomes adc ringneg / bcs @fslow -- same condition, now in reach because the 89-byte @fslow block is moved from after the 168-byte cascade to just after the @ft table; Master's spelling (adc #(256->RINGEND) / bcs @fslow) is unchanged, so the .if now covers only the adc. (2) Model B jmpx @ft-2 expands to pha / lda @ft-2,x / sta jv / lda @ft-1,x / sta jv+1 / pla / jmp (jv) with A = tp+1 saved round it; loading tp+1 after the vector stores instead gives the same A, the same N,Z (lda vs pla of the same value) and untouched C. (3) Model B cascade end and slow-loop end: 'lda tp / staz sp' (= lda tp / ldy #0 / sta (sp),y) is 'FILN' (= dey / lda tp / sta (sp),y) because Y = 1 there. Assembled and linked both targets: Master CODE unchanged in size, Model B TILCODE $900 -> $8F9.

Review: Original matches lines 1012-1132. Model B: bcs @fslow is the same condition as bcc @fnear/jmp @fslow and assembles in reach; dropping jmpx's pha/pla and loading tp+1 after the vector stores gives the same A and N,Z, and C stays unchanged (C=0 still reaches @advsp's adc tmp on both entries). The FILN tail is correct because Y=1 after FIL1 7 plus six dey. Y=0 and X are unchanged afterwards. Anonymous-label order is kept because the cascade has no ':' and the dispatch ':' still follows beq :+. Master is the same code in a new order. Measured on both builds: Master 0, Model B TILCODE -7. With all four candidates applied the gate passes M state/pix/dmg/title and B lockstep/pix; only B title differs, and it differs for the pure no-op candidate 3 as well, so that check is cycle-sensitive and is not failing on behaviour.

## r004:1  src/engine.s:1154-1239  (window i0578)  M -5 B -17  [high]  review: accept

Cycles: Valid entry: bne-taken 3 -> beq-not-taken 2 (both). Invalid entry: bne 2 + jmp 3 -> beq-taken 3 (both). dx<0: Model B eor+inca 2+11 -> eor+adc 2+2, jmp 3 -> bcc 3; Master identical. dy<0: same as dx<0. @full: bra/jmp 3 -> bne 3 (BUFROWS<>0). @dorows exit: jsr drawrect / bra @done (3) -> falls into @done (0), both. All other paths identical.

Assumption: C = 1 on the fall-throughs of 'cmp #<-79 / bcc @full' and 'cmp #<-30 / bcc @full', so eor #$FF / adc #0 = -A; A (w16 in $B1..$FF, w16b in $E2..$FF) is nonzero so ~A <= $4E and the adc leaves C = 0, which the following bcc uses (lda/sta between do not touch C). V/N/Z/C at drawrect entry are not consumed: drawrect (engine.s:563) starts lda rc_h / bne, then lda mrow / sec (Model B) or lda rc_y / cmp. @done starts with ldx curbuf and does no adc/sbc before its asl, so the flags/C arriving there are dead. BUFROWS = $16 (nonzero, < $80) so 'lda #BUFROWS / bne' always branches. mtmp (written by the removed inca) is only ever read by the cpu.inc expansions, each after its own write (grep: no other mtmp use). No external references to scroll_validate's @labels (cheap).

```
        bne :+
        jmp @full
:       txa
        asl
        tay
        ; dy = wcy - BUF_CY
        lda wcy
        sec
        sbc BUF_CY,x
        sta w16b
        ; dx = wcx - BUF_CX
        lda wcx
        sec
        sbc BUF_CX,y
        sta w16
        lda wcx+1
        sbc BUF_CX+1,y
        sta w16+1
        ; |dx| >= 80 -> full  (A still holds w16+1, flags still from the sbc)
        beq @dxpos
        cmp #$FF
        bne @full
:       lda w16                     ; (anonymous label kept: it holds the label count)
        cmp #<-79
        bcc @full
:       ; dx negative: draw cols wcx .. wcx+(-dx)-1, rows wcy..wcy+BUFROWS-1
        eor #$FF                    ; (A still holds w16)
        inca
        sta rc_w
        lda wcx
        sta rc_x
        lda wcx+1
        sta rc_x+1
        bra @docols
@dxpos: lda w16
        beq @dyc
        cmp #ROWCHARS
        bcs @full                   ; not taken: C = 0 for the adc
        ; dx positive: cols (oldcx+80) .. wcx+79 = dx cols starting at wcx+80-dx
        sta rc_w
        eor #$FF                    ; A = 255 - rc_w, C still 0 from the cmp above
        adc #ROWCHARS               ; A = ROWCHARS-1-rc_w, C = 1 (rc_w <= 79)
        adc wcx                     ; + wcx + 1 -> wcx + ROWCHARS - rc_w
        sta rc_x
        lda wcx+1
        adc #0
        sta rc_x+1
@docols:
        lda wcy
        sta rc_y
        lda #BUFROWS
        sta rc_h
        jsr drawrect
@dyc:   lda w16b
        beq @done
        bpl @dypos
        cmp #<-30
        bcc @full
        eor #$FF
        inca
        sta rc_h
        lda wcy
        sta rc_y
        bra @dorows
@dypos: cmp #BUFROWS
        bcs @full                   ; not taken: C = 0 for the adc
        sta rc_h
        eor #$FF                    ; A = 255 - rc_h, C still 0 from the cmp above
        adc #BUFROWS                ; A = BUFROWS-1-rc_h, C = 1 (rc_h <= BUFROWS-1)
        adc wcy                     ; + wcy + 1 -> wcy + BUFROWS - rc_h
        sta rc_y
@dorows:
        lda wcx
        sta rc_x
        lda wcx+1
        sta rc_x+1
        lda #ROWCHARS
        sta rc_w
        jsr drawrect
        bra @done
@full:
        lda wcy
        sta rc_y
        lda #BUFROWS
        sta rc_h
        bra @dorows
```
->
```
        beq @full
:       txa                         ; (anonymous label kept: it holds the label count)
        asl
        tay
        ; dy = wcy - BUF_CY
        lda wcy
        sec
        sbc BUF_CY,x
        sta w16b
        ; dx = wcx - BUF_CX
        lda wcx
        sec
        sbc BUF_CX,y
        sta w16
        lda wcx+1
        sbc BUF_CX+1,y
        sta w16+1
        ; |dx| >= 80 -> full  (A still holds w16+1, flags still from the sbc)
        beq @dxpos
        cmp #$FF
        bne @full
:       lda w16                     ; (anonymous label kept: it holds the label count)
        cmp #<-79
        bcc @full
:       ; dx negative: draw cols wcx .. wcx+(-dx)-1, rows wcy..wcy+BUFROWS-1
        eor #$FF                    ; (A still holds w16)
  .if MODELB
        adc #0                      ; C = 1 from the cmp: A = -w16, and C = 0 (w16 <> 0)
  .else
        inca
  .endif
        sta rc_w
        lda wcx
        sta rc_x
        lda wcx+1
        sta rc_x+1
  .if MODELB
        bcc @docols                 ; C = 0 from the adc
  .else
        bra @docols
  .endif
@full:  ; (here, between two unconditional exits, in reach of every branch to it)
        lda wcy
        sta rc_y
        lda #BUFROWS
        sta rc_h
        bne @dorows                 ; BUFROWS <> 0
@dxpos: lda w16
        beq @dyc
        cmp #ROWCHARS
        bcs @full                   ; not taken: C = 0 for the adc
        ; dx positive: cols (oldcx+80) .. wcx+79 = dx cols starting at wcx+80-dx
        sta rc_w
        eor #$FF                    ; A = 255 - rc_w, C still 0 from the cmp above
        adc #ROWCHARS               ; A = ROWCHARS-1-rc_w, C = 1 (rc_w <= 79)
        adc wcx                     ; + wcx + 1 -> wcx + ROWCHARS - rc_w
        sta rc_x
        lda wcx+1
        adc #0
        sta rc_x+1
@docols:
        lda wcy
        sta rc_y
        lda #BUFROWS
        sta rc_h
        jsr drawrect
@dyc:   lda w16b
        beq @done
        bpl @dypos
        cmp #<-30
        bcc @full
        eor #$FF
  .if MODELB
        adc #0                      ; C = 1 from the cmp: A = -w16b, and C = 0
  .else
        inca
  .endif
        sta rc_h
        lda wcy
        sta rc_y
  .if MODELB
        bcc @dorows                 ; C = 0 from the adc
  .else
        bra @dorows
  .endif
@dypos: cmp #BUFROWS
        bcs @full                   ; not taken: C = 0 for the adc
        sta rc_h
        eor #$FF                    ; A = 255 - rc_h, C still 0 from the cmp above
        adc #BUFROWS                ; A = BUFROWS-1-rc_h, C = 1 (rc_h <= BUFROWS-1)
        adc wcy                     ; + wcy + 1 -> wcy + BUFROWS - rc_h
        sta rc_y
@dorows:
        lda wcx
        sta rc_x
        lda wcx+1
        sta rc_x+1
        lda #ROWCHARS
        sta rc_w
        jsr drawrect                ; (falls into @done)
```

@full's 5 instructions (lda wcy / sta rc_y / lda #BUFROWS / sta rc_h / bra @dorows) move from after '@dorows ... jsr drawrect / bra @done' to after the dx<0 path's unconditional branch to @docols, where nothing falls into them; there every branch to @full is in reach (Model B: +$16, +$10, -$12, -$37, -$47), so the entry test 'bne :+ / jmp @full' becomes 'beq @full', and @dorows' 'bra @done' now jumps to the next instruction and is deleted. The @full exit 'bra @dorows' becomes 'bne @dorows' (Z = 0 from lda #BUFROWS): 2 bytes on the Model B instead of jmp's 3. Model B only: the two negations 'eor #$FF / inca' (inca = sta mtmp / inc mtmp / lda mtmp, 6 bytes) become 'eor #$FF / adc #0' with the known C = 1, and the following 'bra' (jmp) becomes 'bcc' on the resulting C = 0. Master keeps inc a / bra (1 and 2 bytes). Assembled and linked both: Master CODE -5, Model B TILCODE -17.

Review: Original matches lines 1154-1239. On each bcc @full fall-through C=1 and A is in $B1..$FF (dx) or $E2..$FF (dy), so eor/adc #0 gives -A with C=0, and bcc then always branches. drawrect and @done set their own flags before they use any. Nothing falls into @full at its new place. BUFROWS is VISROWS+1, which is nonzero, so the bne always branches. @dorows now falls into @done. All branches assemble in reach. No path is slower. Measured: Master CODE -5, Model B TILCODE -17.

## r004:2  src/engine.s:525-527  (window i0108)  M -0 B -28  [high]  review: accept

Cycles: Model B, carry-out path only: lda sp+1 3 + inca (sta mtmp 3 + inc mtmp 5 + lda mtmp 3) = 14 -> inc sp+1 5 + lda sp+1 3 = 8. No-carry path untouched. Master: identical code.

Assumption: Outside my windows (spnext macro, engine.s:520; owned by r000/r001). Reported because the drawrect slow loop at 1129 uses it, so it may duplicate another agent's candidate. sp is main-thread only: no use in irq_handler (engine.s:3696 on) or in modelb/src display/low/disc code (grep -w sp). ringup (Model B) reads only 'sp' (the low byte, 'lda p') and A, and spnext's own 'sta sp+1' after it writes the final value, so the interim incremented-but-unfolded sp+1 is never observed. inca's preserved carry is dead: ringup begins with cmp ringehi. sp is zero page (A5 in the listing).

```
        bcc :++                     ; past the fold's own anonymous label
        lda sp+1
        inca
```
->
```
        bcc :++                     ; past the fold's own anonymous label
  .if ::MODELB
        inc sp+1                    ; (8 cycles, 4 bytes: the 6502's inca is 11 and 6)
        lda sp+1
  .else
        lda sp+1
        inca
  .endif
```

A = sp+1 + 1 either way at the ringup; N,Z from lda sp+1 vs lda mtmp are the same value and are overwritten by ringup's cmp anyway; C preserved by inca but ringup's cmp sets it. 7 expansions on the Model B (TILCODE 4 x 4 = 16, MNUCODE 4, SPR4CODE 4, SPR6CODE 4): linked, all asserts pass.

Review: Original matches lines 525-527. Model B only: inc sp+1 / lda sp+1 gives the same A. ringup's cmp overwrites the carry that inca preserved, and ringup reads only sp's low byte before the final sta sp+1. sp is not touched by the IRQ, and nothing reads mtmp without writing it first. All 7 expansion sites checked: menu.s:132, engine.s:952, 1129, 2372, 2607 and 2657, and none is timing-critical. Measured: Model B -28 (TILCODE 16, MNUCODE 4, SPR4CODE 4, SPR6CODE 4), 6 cycles faster on the carry path.

## r004:3  src/engine.s:916-916  (window i0405)  M -3 B -3  [high]  review: accept

Cycles: @b7 exit: jmp 3 -> fall through 0, both targets.

Assumption: Outside my windows (r003 owns i0436), reported in passing. The line is 'jmp @advsp' immediately followed by '@advsp:'. CHARCPY's second parameter is unused (engine.s:865-883, no jmp in it) and CHARPER, the macro that emits 'jmp next', is never expanded.

```
        jmp @advsp
```
->
```

```

A jmp to the very next instruction is a no-op; deleting it changes nothing but the size (verified in the Model B listing: 4C at $2A2, @advsp at $2A5). Linked: Master CODE -3, Model B TILCODE -3.

Review: Line 916 is jmp @advsp, and @advsp is the next instruction, so the jmp is a no-op. No carry or register effect. Measured: Master CODE -3, Model B TILCODE -3.

## r005:0  src/engine.s:1207-1239  (window i0653)  M -8 B -10  [high]  review: accept

Cycles: Master: the dy<0 path is 3 cycles faster (bra becomes bne, same cost; the bra @done after drawrect is gone). The dy>0 path is unchanged (it gains bra @dorows, 3, and loses bra @done, 3). @full, from every source, is 6 faster (its bra @dorows and the bra @done are both gone). The dy=0 path (beq @done) is unchanged. Model B is the same, with jmp = 3.

Assumption: @done directly follows line 1239 (at line 1240), so jsr drawrect can fall into it. drawrect does not read C or Z on entry: it starts with lda rc_h (engine.s:563). @seth is a new cheap label that nothing else in scroll_validate's scope uses. The branches into @full from lines 1160-1191 get shorter: the @full block now sits before @dorows, and the dy<0 block shrank by more than @dypos grew. Checked by assembly: Master CODE 6861->6853; Model B TILCODE -10 (-15 together with the i0640 adc #0 and the i0657 lsr).

```
@dyc:   lda w16b
        beq @done
        bpl @dypos
        cmp #<-30
        bcc @full
        eor #$FF
        inca
        sta rc_h
        lda wcy
        sta rc_y
        bra @dorows
@dypos: cmp #BUFROWS
        bcs @full                   ; not taken: C = 0 for the adc
        sta rc_h
        eor #$FF                    ; A = 255 - rc_h, C still 0 from the cmp above
        adc #BUFROWS                ; A = BUFROWS-1-rc_h, C = 1 (rc_h <= BUFROWS-1)
        adc wcy                     ; + wcy + 1 -> wcy + BUFROWS - rc_h
        sta rc_y
@dorows:
        lda wcx
        sta rc_x
        lda wcx+1
        sta rc_x+1
        lda #ROWCHARS
        sta rc_w
        jsr drawrect
        bra @done
@full:
        lda wcy
        sta rc_y
        lda #BUFROWS
        sta rc_h
        bra @dorows
```
->
```
@dyc:   lda w16b
        beq @done
        bpl @dypos
        cmp #<-30
        bcc @full
        eor #$FF
        inca
        bne @seth                   ; always: A = -dy = 1..30
@dypos: cmp #BUFROWS
        bcs @full                   ; not taken: C = 0 for the adc
        sta rc_h
        eor #$FF                    ; A = 255 - rc_h, C still 0 from the cmp above
        adc #BUFROWS                ; A = BUFROWS-1-rc_h, C = 1 (rc_h <= BUFROWS-1)
        adc wcy                     ; + wcy + 1 -> wcy + BUFROWS - rc_h
        sta rc_y
        bra @dorows
@full:
        lda #BUFROWS
@seth:  sta rc_h                    ; (dy negative joins with A = -dy)
        lda wcy
        sta rc_y
@dorows:
        lda wcx
        sta rc_x
        lda wcx+1
        sta rc_x+1
        lda #ROWCHARS
        sta rc_w
        jsr drawrect
```

Layout change. @full moves before @dorows so it falls through into it, and the jsr drawrect in @dorows falls into @done, which drops the bra @done. @dypos no longer falls into @dorows, so it gains a bra @dorows; on its path this cancels the removed bra @done. @full now stores rc_h first, so the dy<0 path (A = -dy after eor/inca) can enter at @seth with sta rc_h and share the lda wcy / sta rc_y. The bne is always taken because A = -dy is 1..30: the Master's inc a and the lda mtmp in the Model B's inca both set Z from A. The stores are zero page, so their order does not matter. Bytes: Master 62->54, Model B 70->60.

Review: Original matches 1207-1239. Every path stores the same rc_* values: @full (from 1155 jmp, 1175/1178/1191/1211/1219) now does lda #BUFROWS/sta rc_h/lda wcy/sta rc_y and falls into @dorows; dy<0 has A=-dy in 1..30, so Z=0 after inc a (Master) or after inca's lda mtmp (Model B), and the bne is always taken; dy>0 gains bra @dorows and loses the bra @done. jsr drawrect falls into @done (line 1240), which reads no flags before ldx/asl. drawrect's flags on entry are the same as before (lda #ROWCHARS/sta). No ':' labels added or removed; @seth is unique in scope. Cycles: dy<0 -3, @full -6, dy>0 and dy=0 unchanged, on both targets. Assembled: Master CODE -8, Model B TILCODE -10.

## r005:1  src/engine.s:1210-1213  (window i0640)  M -0 B -4  [high]  review: accept

Cycles: Model B: the inca expansion (sta mtmp 3, inc mtmp 5, lda mtmp 3 = 11) becomes adc #0 (2), 9 cycles faster. Master unchanged.

Assumption: C = 1 at the eor, because the bcc @full just above was not taken. A = w16b is $E2..$FF, so ~A is 0..$1D and there is no carry out. C leaves as 0 instead of 1, and nothing reads C before drawrect's own sec/cmp. The path is sta rc_h, lda wcy, sta rc_y, the rc_x loads and stores, lda #ROWCHARS, sta rc_w, jsr drawrect; drawrect (engine.s:563) starts lda rc_h / bne, then on the Model B lda mrow / sec. Z after the adc = (A = 0) = 0, the same as after inca's lda mtmp, so the bne @seth of the layout candidate (or the original bra) is unaffected. mtmp is no longer written; it is scratch owned by the macro expansions and nothing reads it across this point.

```
        cmp #<-30
        bcc @full
        eor #$FF
        inca
```
->
```
        cmp #<-30
        bcc @full
        eor #$FF
  .if MODELB
        adc #0                      ; C = 1 from the cmp: A = -A (inca is 6 bytes here)
  .else
        inca
  .endif
```

eor #$FF then +1 is a two's-complement negate; with C = 1 known, adc #0 adds exactly 1. This applies whether or not the layout candidate (window i0653) is taken, since that candidate keeps these lines verbatim.

Review: C=1 after the bcc @full is not taken; A=$E2..$FF, so eor gives 0..$1D and adc #0 gives 1..$1E with C=0, Z=0, N=0, V=0. Nothing reads C or V before drawrect, which begins lda rc_h/bne, then (Model B) lda mrow/sec/sbc. Z=0 keeps i0653's bne @seth taken. Model B: 11 cycles become 2. Assembled: TILCODE -4 (-15 with i0653 and i0657).

## r005:2  src/engine.s:1242-1244  (window i0657)  M -0 B -1  [high]  review: accept

Cycles: Model B: lda #0 (2 cycles) becomes lsr (2 cycles), so no change. Master unchanged.

Assumption: A = 1 from the lda #1 two lines up. Nothing reads C before the asl at line 1248 overwrites it (only lda wcy / sta BUF_CY,x / txa in between), so the C = 1 left by lsr is dead.

```
        lda #1
        sta BUF_VALID,x
        stz BUF_BOTOK,x             ; the window moved: the slot below is stale again
```
->
```
        lda #1
        sta BUF_VALID,x
  .if MODELB
        lsr                         ; A = 0: a byte shorter than stz's lda #0
        sta BUF_BOTOK,x             ; the window moved: the slot below is stale again
  .else
        stz BUF_BOTOK,x             ; the window moved: the slot below is stale again
  .endif
```

lsr of 1 gives 0 with N = 0 and Z = 1, exactly as lda #0 does; the sta BUF_BOTOK,x is then the same as the stz expansion.

Review: A=1 from lda #1, so lsr gives A=0, Z=1, N=0 (as lda #0 does) and C=1. C is dead: it is overwritten by the asl at 1248 before any read (lda wcy/sta/txa come between). 2 cycles become 2. Assembled: Model B TILCODE -1.

## r005:3  src/engine.s:1268-1268  (window i0674)  M -0 B -2  [high]  review: accept

Cycles: Model B: pha / lda #0 / sta / pla (12 cycles) becomes lda #0 / sta (5), 7 faster. Master identical (stz zp).

Assumption: A is dead: the next instruction (line 1269) is lda recp, which also overwrites N and Z.

```
        stza tmp4                   ; i
```
->
```
        stz tmp4                    ; i
```

stza differs from stz only in preserving A, and A is reloaded straight away.

Review: lda recp comes next and overwrites A, N and Z. On the Master both spellings are stz zp; on the Model B the pha/pla pair goes. Assembled: LGCCODE -2.

## r005:4  src/engine.s:1276-1276  (window i0682)  M -0 B -2  [high]  review: accept

Cycles: Model B: 14 cycles become 7. Master identical (stz abs,x).

Assumption: A is dead afterwards: both successors reload A before reading it, the fall-through with lda sprmul5,x (1279) and bcs @next with lda rp (1315). The cpx cnt at 1277 rewrites N, Z and C straight away.

```
        stza KEEP,x
```
->
```
        stz KEEP,x
```

stza differs from stz only in preserving A (pha/pla) and in the N and Z left by the pla; neither is observed.

Review: The cpx cnt that follows rewrites N, Z and C. Both successors reload A (lda sprmul5,x at 1279; lda rp at @next 1315) and nothing else branches in between. Master is identical; Model B loses pha/pla, 14 cycles become 7. Assembled: LGCCODE -2.

## r005:5  src/engine.s:1289-1289  (window i0692)  M -0 B -2  [high]  review: accept

Cycles: Model B: ldy #0 + lda (rp),y (7 cycles) becomes lda (rp),y (5). Master unchanged.

Assumption: On the Model B, the cmpz rp at line 1282 expands to ldy #0 / cmp (rp),y. Between it and line 1289 there are only beq, cmp # and bcc, none of which touch Y. Y's final value (0) is unchanged.

```
        ldaz rp
```
->
```
  .if MODELB
        lda (rp),y                  ; Y = 0 from the cmpz above
  .else
        ldaz rp
  .endif
```

The same load, with Y already 0.

Review: Model B cmpz rp at 1282 is ldy #0/cmp (rp),y. Only beq/cmp #/bcc lie between it and 1289, there are no labels or branch-ins, and Y stays 0. Master unchanged. Assembled: LGCCODE -2.

## r005:6  src/engine.s:1293-1293  (window i0696)  M -0 B -1  [high]  review: accept

Cycles: bra (Master 3 cycles; Model B jmp, 3 cycles) becomes a taken bne, 3 cycles on both.

Assumption: A = 1 from the lda #1 on the line before, so Z = 0 and the bne is always taken.

```
        bra @pos
```
->
```
        bne @pos
```

An unconditional branch replaced by a branch on a flag whose value is known.

Review: The lda #1 just before sets Z=0, so the bne is always taken: 3 cycles, as bra/jmp. Assembled: Model B LGCCODE -1; Master unchanged.

## r005:7  src/engine.s:1296-1296  (window i0699)  M -0 B -1  [high]  review: accept

Cycles: Model B: ldy #1 (2 cycles) becomes iny (2 cycles). Master unchanged.

Assumption: On the Model B, @same is reached only by the beq at 1283, straight after cmpz's ldy #0, so Y = 0. The other way in comes through ldaz rp at 1289 (ldy #0 / lda (rp),y, or, with the i0692 candidate, lda (rp),y with Y still 0 from the cmpz). Nothing in between touches Y: lda #1, lda #2 and sta tmp3 leave it alone. The lda SPRLIST+1,x that follows overwrites the N and Z set by iny.

```
        ldy #1
```
->
```
  .if MODELB
        iny                         ; Y = 0 on both ways in (cmpz, ldaz)
  .else
        ldy #1
  .endif
```

With Y = 0, iny gives Y = 1, the same as ldy #1.

Review: There are two ways into @pos: fall-through from 1293 (after ldaz's ldy #0, or cmpz's with i0692) and @same, reached only from the beq at 1283 after cmpz's ldy #0. Y=0 on both on the Model B, and lda #1/#2 and sta tmp3 leave Y alone. The iny flags are overwritten by lda SPRLIST+1,x. Assembled: LGCCODE -1.

## r005:8  src/engine.s:1321-1322  (window i0724)  M -0 B -1  [high]  review: accept

Cycles: bra (3 cycles; jmp 3 on the Model B) becomes a taken bne, 3 cycles on both.

Assumption: tmp4 = i < NSPR <= MAXSPR (24 on the Model B build, 32 on the Master; addsprite at engine.s:1382 caps NSPR at MAXSPR), so inc tmp4 gives 1..MAXSPR and never 0.

```
:       inc tmp4
        bra @l
```
->
```
:       inc tmp4
        bne @l                      ; always: i+1 <= MAXSPR
```

inc sets Z from its result, which is nonzero. The anonymous ':' on the inc line is kept.

Review: Every path to the ':' at 1321 passes the cpx NSPR/bcs @done check with tmp4=i<NSPR<=255, and the loop never writes tmp4 elsewhere. So i+1 is 1..255 and Z=0: the bne is always taken, 3 cycles. The ':' is kept, so the count is unchanged. Assembled: LGCCODE -1.

## r006:0  src/engine.s:1406-1406  (window i0789)  M -0 B -2  [high]  review: accept

Cycles: Master identical (stz zp, 3). Model B: stza=pha/lda#0/sta zp/pla 12 cy -> lda#0/sta zp 5 cy.

Assumption: A dead at @pass: the next instruction (engine.s:1407) is lda recp; both entries (fall-through from sta dpass at 1405, jmp @pass at 1464) carry no live A. N,Z from stz's 6502 expansion are overwritten by lda recp.

```
@pass:  stza spi
```
->
```
@pass:  stz spi                     ; A dead: lda recp next
```

stza keeps A through pha/pla; A is overwritten by the very next lda recp, so the A-preserving spelling is unneeded; stz (lda #0/sta spi on 6502, stz spi on 65C02) stores the same zero. Flags: lda recp sets N,Z anew; C/V untouched by both forms.

Review: A is dead at @pass. The next instruction is lda recp on both entries (the fall-through and the jmp/bpl from @endpass). stz spi stores the same 0, lda recp sets N,Z again, and C/V are untouched. Master output is byte-identical; a scratch assemble shows Model B LGCCODE 2 bytes smaller.

## r006:1  src/engine.s:1411-1425  (window i0808)  M -4 B -4  [high]  review: accept

Cycles: loop head: ldx spi/cpx/lda sprmul5,x/tax (3+c+4+2) -> ldy spi/cpy/ldx sprmul5,y (3+c+4): -2 cy; lda SPRLIST,x/cmp -> ldy SPRLIST,x/cpy: same (4+2); second lda SPRLIST,x (4 cy) removed: -4 cy on every path that reaches it. bcs @endpass path: same. Both targets identical counts (no macros).

Assumption: X and Y are not live into @l from either entry (jmp/bne @l at 1460, fall-through from 1410): X is recomputed from sprmul5 and Y reloaded before every use (@write does ldy #1 at 1431, the KEEP path does ldy spi, kept). cpy abs/zp, ldx abs,y, ldy abs,x, cpy #imm all exist on the NMOS 6502. The @endpass path (1462-1468) reloads X (ldx curbuf) and uses no Y.

```
@l:     ldx spi
        cpx NSPR
        bcs @endpass
        lda sprmul5,x
        tax
        lda SPRLIST,x
        cmp #BOXID0
        lda dpass
        adc #$FF
        beq @next
        lda SPRLIST,x
        cmp #BOXID0+BOXN            ; a box star the logic says nothing can disturb, and
        bcc @write                  ; the same frame already in the same place: if
        ldy spi                     ; nothing has been repainted under it, its pixels
        lda KEEP,y                  ; are still right, so leave it alone
```
->
```
@l:     ldy spi
        cpy NSPR
        bcs @endpass
        ldx sprmul5,y
        ldy SPRLIST,x               ; Y = id for both compares
        cpy #BOXID0
        lda dpass
        adc #$FF
        beq @next
        cpy #BOXID0+BOXN            ; a box star the logic says nothing can disturb, and
        bcc @write                  ; the same frame already in the same place: if
        ldy spi                     ; nothing has been repainted under it, its pixels
        lda KEEP,y                  ; are still right, so leave it alone
```

ldy spi/cpy NSPR sets C exactly as ldx spi/cpx NSPR. ldx sprmul5,y loads the same byte that lda sprmul5,x + tax put in X (index spi in both). ldy SPRLIST,x/cpy #BOXID0 gives the same C as lda SPRLIST,x/cmp #BOXID0 (feeds adc #$FF, unchanged). lda dpass/adc/beq do not touch Y, so cpy #BOXID0+BOXN compares the same id the removed lda SPRLIST,x reloaded; bcc @write consumes the same C. After that the code is unchanged (ldy spi reloads Y before KEEP,y; @write sets Y itself; A at @write is not read before lda SPRLIST+1,x).

Review: X and Y are not live into @l. Every path to @write reloads Y with ldy #1, the KEEP path reloads it with ldy spi, and @endpass/@next use neither. ldx sprmul5,y and ldy SPRLIST,x exist on the NMOS 6502. cpy #BOXID0 gives the same C into adc, and Y survives lda dpass/adc, so cpy #BOXID0+BOXN compares the same id. A is not read at @write before lda SPRLIST+1,x. Faster on every path. Scratch assemble: Master -4, Model B -4.

## r006:2  src/engine.s:1460-1460  (window i0842)  M -1 B -1  [high]  review: accept

Cycles: jmp 3 -> bne taken 3, both targets (always taken).

Assumption: Z is clear after inc spi at 1459: spi < NSPR on entry to the body (cpx NSPR/bcs @endpass at 1412-1413) and NSPR <= MAXSPR (32 / MAXSPRDEF) < 255, so inc gives 1..NSPR, never 0. Range: t_b.lst has jmp @l at $15A and @l at $F0 -> offset -108 (Model B), Master -107; other candidates only shrink it.

```
        jmp @l
```
->
```
        bne @l                      ; spi <= NSPR: never wraps to 0
```

inc spi sets Z only if spi became 0, which cannot happen, so bne is always taken to the same target the jmp reached.

Review: spi < NSPR (unsigned) in the loop body, so spi <= 254 and inc spi never gives 0. Nothing else writes spi and it has no aliases. The bne is always taken, at 3 cycles like the jmp. In range on both targets by scratch assemble (Model B encodes D0 C0 alone). Saves 1 byte on each.

## r006:3  src/engine.s:1462-1465  (window i0845)  M -3 B -3  [high]  review: accept

Cycles: second-pass path: dec + bmi not taken 2 + jmp 3 -> dec + bpl taken 3 (-2 cy); exit path: bmi taken 3 -> bpl not taken 2 (-1 cy). Both targets.

Assumption: Branch range: Model B listing (build/grind/lst/t_b.lst) has @pass at $E2 and the bpl would sit at $160 with next PC $162: offset exactly -128, legal, and every other candidate here only shrinks it (-122 with all applied; verified by a scratch assemble); Master: @pass $4E8, next PC $563, offset -123. ca65 rejects an out-of-range branch, so a miscount cannot be silent.

```
        dec dpass
        bmi :+
        jmp @pass                   ; (out of branch range on the 6502 build)
:       ldx curbuf
```
->
```
        dec dpass
        bpl @pass                   ; (in range on both builds)
:       ldx curbuf
```

dec dpass sets N from the result; bmi :+ went to ldx curbuf when negative, else jmp @pass. bpl @pass branches on the opposite condition to the same target and falls through to ldx curbuf otherwise. No flag consumed after ldx curbuf.

Review: bpl @pass is the exact inverse of bmi :+ over jmp @pass. The ':' stays, so the anonymous-label count is unchanged. Range checked by scratch assemble: on the Model B alone it encodes 10 80 (exactly -128, which is legal), and the offset gets smaller with the other candidates applied. Master is in range. Both paths are faster. Saves 3 bytes on each.

## r006:4  src/engine.s:1477-1483  (window i0855)  M -0 B -6  [high]  review: accept

Cycles: Master: identical code. Model B: stza spclip (abs, LGCLOBSS: pha3+lda#2+sta4+pla4=13) + stza ptr+1 (zp: 12) = 25 cy -> ldx #0 2 + stx abs 4 + stx zp 3 = 9 cy.

Assumption: X is not an input to drawsprite: its first X use is ldx spbank (engine.s:1490) and nothing between 1477 and 1490 reads X. Callers are draw_sprites (jsr at 1452; X not read afterwards, @next reloads) and the far entry F_DRAWSPR (farcall destroys X by contract, cpu.inc). spclip is absolute in bank 7 on the Model B (LGCLOBSS, engine.s:414); ptr is zp. N,Z after ldx/stx vs stza's pla: both overwritten by cmp #BOXID0+BOXN before any branch; C untouched by both.

```
        stza spclip                 ; set at every window edge the sprite is cut against
        cmp #BOXID0+BOXN            ; the "nothing can disturb it" aliases draw the same
        bcc :+                      ; picture as the ids BOXN below them
        sbc #BOXN
:
        sta sp_id
        stza ptr+1
```
->
```
  .if MODELB
        ldx #0                      ; X is dead on entry (ldx spbank below)
        stx spclip                  ; set at every window edge the sprite is cut against
  .else
        stza spclip                 ; set at every window edge the sprite is cut against
  .endif
        cmp #BOXID0+BOXN            ; the "nothing can disturb it" aliases draw the same
        bcc :+                      ; picture as the ids BOXN below them
        sbc #BOXN
:
        sta sp_id
  .if MODELB
        stx ptr+1                   ; X = 0 still
  .else
        stza ptr+1
  .endif
```

Both stza stores write 0 while keeping A = id; on the Model B, X=0 does the same without touching A. Between the two stores cmp/bcc/sbc/sta do not modify X, so stx ptr+1 still stores 0. ptr+1 is then shifted by rol as before. X is overwritten by ldx spbank at 1490.

Review: X is not an input to drawsprite. Its first X use is ldx spbank; the callers are draw_sprites and the far/TOMAIN wrappers, and drawsprite overwrites X anyway. cmp/bcc/sbc/sta do not touch X, so stx ptr+1 stores 0. cmp overwrites the N,Z,C that either spelling sets. Master output is byte-identical. Model B LGCCODE is 6 bytes smaller (spclip is absolute in LGCLOBSS, ptr is zero page), and the stores drop from 25 to 9 cycles.

## r006:5  src/engine.s:1517-1521  (window i0887)  M -0 B -11  [high]  review: accept

Cycles: Master identical. Model B: bitimm 4 (15 cy) + beq -> lsr x3 (6) + bcc: -9 cy; bitimm $10 (15) + beq -> and #2 (2) + beq: -13 cy. The taken/not-taken structure is the same.

Assumption: A dead after the second test: at 1523-1524 'stx sp_dbank / lda sp_id' reloads A (the flags byte is also saved in sp_flags at 1515). Nothing reads mtmp without writing it first (only the cpu.inc macros use mtmp). C after 1522: the next C consumer follows asl at 1525, which overwrites it. The bankimm ldx sites keep their size and are recorded by their own @bf_ labels, which move with them.

```
        bitimm 4
        beq :+
        bankimm ldx, (BANK_SPR|$80), BANK_LVL
:       bitimm $10              ; A still holds the flags byte
        beq :+
```
->
```
  .if MODELB
        lsr                         ; C = flags bit 2
        lsr
        lsr                         ; and bit 1 = flags bit 4
        bcc :+
  .else
        bitimm 4
        beq :+
  .endif
        bankimm ldx, (BANK_SPR|$80), BANK_LVL
:
  .if MODELB
        and #2
  .else
        bitimm $10              ; A still holds the flags byte
  .endif
        beq :+
```

Model B: A = flags (from lda (ptr),y at 1514). Three lsr put flags bit 2 in C and leave A = flags>>3, whose bit 1 is flags bit 4. bcc skips the SPR|$80 ldx exactly when bit 2 is clear, as bitimm 4/beq did. and #2 gives Z when flags bit 4 is clear, as bitimm $10 did. lsr/and do not touch X, so the ldx results reach stx sp_dbank unchanged. The Master assembles exactly the original.

Review: On the Model B, after three lsr, C = flags bit 2 and A bit 1 = flags bit 4, so bcc/beq take the same paths as bitimm 4/beq and bitimm $10/beq. A is dead (lda sp_id follows stx sp_dbank), C is overwritten by asl, and neither form touches V. The bankimm sites are unchanged and sit outside the conditionals. The ':' count is unchanged. Master output is byte-identical. A scratch assemble shows Model B LGCCODE 11 bytes smaller, and it is faster.

## r007:0  src/engine.s:1498-1507  (window i0896)  M -1 B -0  [high]  review: accept

Cycles: Master: clc(2) removed, adc #imm 2->2: 2 cycles faster on the one path; Model B identical code

Assumption: C = 1 on arrival (Master): the fall-through of `bankimm cpx, BANK_SPR, BANK_LVL / bne @titledir` (engine.s:1491-1492) means X = BANK_SPR, so cpx left C = 1; ldx #BANK_LVL / stx ROMSEL_CPY / stx ROMSEL (1494-1496) do not touch C. <SPR_TABLE is nonzero (labels.txt: SPR_TABLE = $A9C8); guarded by the added link-time .assert.

```
        clc
  .if MODELB
        adc sprtab                  ; the directory sits above the map: where the
        sta ptr                     ; loader put it (low.s)
        lda ptr+1
        adc sprtab+1
        sta ptr+1
        jsr dirfetch                ; the directory is in bank 6: its eight bytes come
  .else                             ; into low RAM and ptr points there
        adc #<SPR_TABLE
```
->
```
  .if MODELB
        clc
        adc sprtab                  ; the directory sits above the map: where the
        sta ptr                     ; loader put it (low.s)
        lda ptr+1
        adc sprtab+1
        sta ptr+1
        jsr dirfetch                ; the directory is in bank 6: its eight bytes come
  .else                             ; into low RAM and ptr points there
        adc #<(SPR_TABLE-1)         ; C = 1 from the cpx (X = BANK_SPR): the -1 takes it back
        .assert <SPR_TABLE <> 0, error, "drawsprite: adc #<(SPR_TABLE-1) needs <SPR_TABLE nonzero"
```

Master: A + <(SPR_TABLE-1) + C(=1) = A + <SPR_TABLE exactly (as a 9-bit sum) whenever <SPR_TABLE >= 1, so the low byte and the carry into `adc #>SPR_TABLE` are identical. Model B: the clc just moves inside its own branch (it needs it: adc sprtab is a variable).

Review: Text matches 1498-1507. Master: fall-through of cpx #BANK_SPR/bne means Z=1 so C=1; ldx/stx/stx and BANKREF (no code on Master) keep C. A+<(SPR_TABLE-1)+1 = A+<SPR_TABLE as a 9-bit sum when <SPR_TABLE>=1 ($C8 in labels.txt), guarded by the .assert. Model B: clc just moves into its arm. Master -1 byte, -2 cycles.

## r007:1  src/engine.s:1513-1514  (window i0896)  M -0 B -1  [high]  review: accept

Cycles: Model B: ldy#6(2)+lda (zp),y(5)=7 -> lda abs 4; Master unchanged

Assumption: Model B: this point is reached only after `jsr dirfetch` (1505), which ends with ptr = MAPBUF (modelb/src/low.s:117-125; pagelogic, engine.s:4734, keeps A/X/Y/C and touches neither ptr nor MAPBUF). Y dead: the next Y reads are `ldaz ptr` at @entry2 (Model B: ldy #0 first) and ldy #7/ldy #4 later; the W=0 exit returns to draw_sprites (reloads Y: ldy spi / ldy #1) or draw_piece (menu.s:255, callers reload), and drawsprite already clobbers Y on every path.

```
        ldy #6
        lda (ptr),y
```
->
```
  .if MODELB
        lda MAPBUF+6                ; dirfetch left ptr = MAPBUF
  .else
        ldy #6
        lda (ptr),y
  .endif
```

(ptr),6 with ptr = MAPBUF is MAPBUF+6: same byte, same N,Z; only Y (dead) differs.

Review: Model B only reaches 1513 after jsr dirfetch (ptr=MAPBUF, low.s:121-124; pagelogic keeps regs). MAPBUF=$1DD absolute: lda abs 3 bytes vs ldy#/lda(zp),y 4. Y dead: next Y is ldaz(ldy #0)/ldy #7/#4; W=0 exit: callers (draw_sprites @next, draw_piece and its menu.s callers) never read Y.

## r007:2  src/engine.s:1517-1521  (window i0896)  M -0 B -11  [high]  review: accept

Cycles: Model B: bitimm 4 (sta/and/php/lda/plp = 8 bytes, 15 cycles) -> 3 lsr = 6; bitimm $10 15 -> 2 lsr = 4; branches same cost on both arms. Master unchanged

Assumption: A = sp_flags on entry (lda (ptr),y / sta sp_flags at 1514-1515; bankimm ldx leaves A). A dead after the second test: line 1523 `stx sp_dbank` then 1524 `lda sp_id`. mtmp's value (written by the old bitimm) is scratch owned by the macros, never read across.

```
        bitimm 4
        beq :+
        bankimm ldx, (BANK_SPR|$80), BANK_LVL
:       bitimm $10              ; A still holds the flags byte
        beq :+
```
->
```
  .if MODELB
        lsr
        lsr
        lsr                         ; C = flags bit 2
        bcc :+
  .else
        bitimm 4
        beq :+
  .endif
        bankimm ldx, (BANK_SPR|$80), BANK_LVL
:
  .if MODELB
        lsr
        lsr                         ; C = flags bit 4
        bcc :+
  .else
        bitimm $10                  ; A still holds the flags byte
        beq :+
  .endif
```

Model B: after 3 lsr C = old bit 2, so bcc skips exactly when (A & 4) = 0 (was beq after bit #4); two more lsr put old bit 4 in C, so bcc skips exactly when (A & $10) = 0. The bank-site ldx's (recorded instructions) are not moved or resized. X and the flags consumed later (none: stx, lda sp_id) are unchanged.

Review: Text matches. Model B bitimm = sta mtmp($DF zp)/and/php/lda/plp = 8 bytes 15 cy; 3 lsr puts bit2 in C, bcc skips iff bit clear; bankimm ldx leaves A; 2 more lsr put bit4 in C. A dead (stx sp_dbank; lda sp_id), mtmp not read later. Anonymous count unchanged (single ':' kept, none inside .if arms). Bank-site ldx instructions unchanged. 5+6=11 bytes.

## r007:3  src/engine.s:1531-1531  (window i0896)  M -0 B -1  [medium]  review: accept

Cycles: Model B: jmp 3 -> bcc taken 3; Master: bra 3 -> bcc taken 3

Assumption: sp_id < 128 on this (game sprite) path: drawsprite folds ids >= BOXID0+BOXN (118) down by BOXN (1478-1480), the directory has 118 entries (SPR_TABLE is 944 bytes, ldprog.s:497 nt=118) and the Master's `lda SPRMASK,x` with x = 2*id already requires 2*id < 256. So `asl` at 1525 leaves C = 0, and tax / lda abs,x / sta do not touch C.

```
        bra @entry2
```
->
```
        bcc @entry2                 ; C = 0 from the asl: sp_id < 128
```

The branch is always taken (C = 0), so it is an unconditional jump to @entry2; @entry2 is 62 bytes on (Model B listing $1D2->$213) - in range.

Review: sp_id <= 117 (fold at BOXID0+BOXN=118; directory has 118 entries), so asl leaves C=0; tax/lda/sta keep C; bcc always taken. Master bra->bcc same size/cycles; Model B jmp(3)->bcc(2). Range fine (assembler would error otherwise).

## r007:4  src/engine.s:1534-1562  (window i0896)  M -3 B -16  [medium]  review: accept

Cycles: Master: lda ptr+1/clc/adc/sta (3+2+2+3) -> lda #/sta (2+3): 5 faster. Model B: removes sta ptr, lda ptr+1, clc, adc, sta ptr+1, lda ptr, lda ptr+1 (the pha of the high byte becomes a pla-free constant), clc, lda #0 -> net many cycles faster (pla/sta ptr+1 7 -> lda #/sta 5)

Assumption: Title piece ids are < 32 (in fact 0..12: build/assets.inc TP_LOGO..TP_CLEO8, and menu.s draw_piece callers pass TP_* or 4+(0..7)); the directory holds 16 entries of 8 bytes before the mask table at TITLE_ADDR+$80, so id < 16 structurally. Hence on entry ptr+1 = id>>5 = 0 (stza ptr+1 then three rol, 1482-1488) and sp_id = id (< BOXID0+BOXN, no fold). TITLE_ADDR = $8900 (build/assets.inc, modelb/build/assets.inc): <TITLE_ADDR = 0 (asserted). After dirfetch ptr is overwritten, so no intermediate ptr value is read.

```
        sta ptr                     ; only this path uses id*8 as the low byte unadjusted
        lda ptr+1
        clc
        adc #>TITLE_ADDR
        sta ptr+1
        lda spbank              ; title pieces keep directory and data in one bank
        sta sp_dbank
  .if MODELB                        ; bank 6 is not this one: the entry and the mask
        lda ptr                     ; address come across through low RAM, the mask
        pha                         ; first (dirfetch reuses ptr and MAPBUF)
        lda ptr+1
        pha
        lda sp_id
        asl
        clc
        adc #<(TITLE_ADDR+$80)
        sta ptr
        lda #0
        adc #>(TITLE_ADDR+$80)
        sta ptr+1
        jsr dirfetch
        lda MAPBUF
        sta sp_mbase
        lda MAPBUF+1
        sta sp_mbase+1
        pla
        sta ptr+1
        pla
        sta ptr
```
->
```
  .if MODELB
        pha                         ; the entry's low byte, kept across the mask fetch
  .else
        sta ptr                     ; only this path uses id*8 as the low byte unadjusted
        lda #>TITLE_ADDR            ; title ids are < 16: id*8 has no high byte
        sta ptr+1
  .endif
        lda spbank              ; title pieces keep directory and data in one bank
        sta sp_dbank
        .assert <TITLE_ADDR = 0, error, "drawsprite: the title directory/mask arithmetic needs a page-aligned TITLE_ADDR"
  .if MODELB                        ; bank 6 is not this one: the entry and the mask
        lda sp_id                   ; address come across through low RAM, the mask
        asl                         ; first (dirfetch reuses ptr and MAPBUF); C = 0
        adc #<(TITLE_ADDR+$80)
        sta ptr
        lda #>(TITLE_ADDR+$80)      ; no carry: 2*id < $80
        sta ptr+1
        jsr dirfetch
        lda MAPBUF
        sta sp_mbase
        lda MAPBUF+1
        sta sp_mbase+1
        pla
        sta ptr
        lda #>TITLE_ADDR
        sta ptr+1
```

Master: ptr+1 was 0 + >TITLE_ADDR + C(0) = >TITLE_ADDR: now loaded directly; ptr unchanged. Model B: the directory entry pointer is (id*8, >TITLE_ADDR); its low byte is pushed straight from A (was sta ptr / lda ptr / pha) and its high byte is the constant, rebuilt after the mask fetch (was two pushes/pulls). Mask pointer: asl of id < 128 leaves C = 0 (the clc was redundant), $80 + 2*id < $100 so the high byte is >(TITLE_ADDR+$80) with no carry (was lda #0 / adc). Stack depth balances (one pha, one pla). dirfetch and lines 1563-1566 then see ptr exactly as before. SUPERSEDES the two fallback candidates at 1534-1545 and 1547-1553 (apply this first; they will not match afterwards).

Review: Text matches 1534-1562. Title ids 0..12 (assets.inc TP_*, draw_piece callers pass TP_* or TP_CLEO0+0..3), <118 so no fold, and id*8<256 so ptr+1=0 on entry: Master ptr+1 = >TITLE_ADDR directly (-3 bytes). Model B: push id*8 from A, mask ptr = $80+2*id with C=0 from asl and no carry (2*id<$80, <TITLE_ADDR=0 asserted, TITLE_ADDR=$8900), restore ptr=(id*8,>TITLE_ADDR) after mask fetch; one pha/one pla balanced across dirfetch (no stack inspection). Recount Model B: 33 unique bytes -> 17 = 16.

## r007:5  src/engine.s:1534-1545  (window i0896)  M -0 B -8  [high]  review: accept

Cycles: Model B: drops sta ptr, sta ptr+1, lda ptr, lda ptr+1 (12 cycles); Master identical

Assumption: ptr is not read between here and line 1550 (sta ptr) / dirfetch; the pulled values restore ptr at 1559-1562 exactly as before. (Fallback for the 1534-1562 candidate, which supersedes it.)

```
        sta ptr                     ; only this path uses id*8 as the low byte unadjusted
        lda ptr+1
        clc
        adc #>TITLE_ADDR
        sta ptr+1
        lda spbank              ; title pieces keep directory and data in one bank
        sta sp_dbank
  .if MODELB                        ; bank 6 is not this one: the entry and the mask
        lda ptr                     ; address come across through low RAM, the mask
        pha                         ; first (dirfetch reuses ptr and MAPBUF)
        lda ptr+1
        pha
```
->
```
  .if MODELB
        pha                         ; the entry's low byte, kept across the mask fetch
        lda ptr+1
        clc
        adc #>TITLE_ADDR
        pha                         ; and its high byte (dirfetch reuses ptr and MAPBUF)
  .else
        sta ptr                     ; only this path uses id*8 as the low byte unadjusted
        lda ptr+1
        clc
        adc #>TITLE_ADDR
        sta ptr+1
  .endif
        lda spbank              ; title pieces keep directory and data in one bank
        sta sp_dbank
  .if MODELB                        ; bank 6 is not this one: the entry and the mask
```

Model B: the low byte id*8 is pushed straight from A instead of via ptr; the high byte is computed in A and pushed instead of via ptr+1. Same two values pushed in the same order, so pla/sta ptr+1, pla/sta ptr at 1559-1562 leave ptr as before; ptr's intermediate contents are overwritten at 1550/1553 before any read. spbank/sp_dbank unchanged. Master unchanged.

Review: Correct fallback (same two values pushed in the same order; ptr not read before 1550/1553), but SUPERSEDED by index 4: apply only if 4 is not applied; will not match after 4.

## r007:6  src/engine.s:1547-1553  (window i0896)  M -0 B -3  [medium]  review: accept

Cycles: Model B only (inside .if MODELB): clc 2 + lda #0/adc 4 -> lda # 2: 4 cycles faster

Assumption: Title ids < 16 (build/assets.inc TP_* 0..12; draw_piece callers menu.s:410-636; 16 8-byte entries sit below the mask table at TITLE_ADDR+$80). TITLE_ADDR = $8900 so <(TITLE_ADDR+$80) = $80 (asserted). (Fallback for the 1534-1562 candidate, which supersedes it.)

```
        asl
        clc
        adc #<(TITLE_ADDR+$80)
        sta ptr
        lda #0
        adc #>(TITLE_ADDR+$80)
        sta ptr+1
```
->
```
        asl                         ; C = 0: title ids are < 16
        adc #<(TITLE_ADDR+$80)
        sta ptr
        lda #>(TITLE_ADDR+$80)      ; no carry out: <TITLE_ADDR = 0 and 2*id < $80
        sta ptr+1
        .assert <TITLE_ADDR = 0, error, "drawsprite: the title mask table must not cross a page"
```

asl of sp_id < 128 leaves C = 0, so the clc was redundant; $80 + 2*id <= $98 never carries, so lda #0 / adc #>(TITLE_ADDR+$80) is just the constant. C after (no carry in either) is not read: dirfetch's mapstrip overwrites flags.

Review: Correct fallback (asl of id<16 leaves C=0; $80+2*id never carries; <TITLE_ADDR=0 asserted), but SUPERSEDED by index 4: will not match after 4.

## r007:7  src/engine.s:1564-1565  (window i0928)  M -0 B -1  [high]  review: accept

Cycles: Model B only: 7 -> 4

Assumption: Inside .if MODELB: immediately after `jsr dirfetch` (1563), which leaves ptr = MAPBUF (modelb/src/low.s:117-125). Y dead: at @entry2 Model B `ldaz ptr` sets Y = 0 first (or the MAPBUF candidate there, which reads no Y); later ldy #7 / ldy #4.

```
        ldy #6
        lda (ptr),y
```
->
```
        lda MAPBUF+6                ; dirfetch left ptr = MAPBUF
```

(ptr),6 = MAPBUF+6; same A and N,Z. Only Y (dead) differs.

Review: Inside .if MODELB right after jsr dirfetch (ptr=MAPBUF absolute $1DD). Y dead (next: ldy #0 in ldaz at @entry2, or MAPBUF loads, then ldy #7/#4; exits' callers never read Y).

## r007:8  src/engine.s:1582-1589  (window i0951)  M -0 B -2  [high]  review: accept

Cycles: Model B: 7+2+5+2+5 (loads) -> 4+4+4: 9 faster; Master unchanged

Assumption: Model B: @entry2 is reached only via `bra @entry2` (1531, after jsr dirfetch at 1505) and the fall-through from the title path (after jsr dirfetch at 1563): grep shows no other reference. dirfetch leaves ptr = MAPBUF. Y dead: the next Y use is ldy #7 (1598) / ldy #4 (1609); the W = 0 exit returns to callers that do not read Y (draw_sprites @next/@l reloads Y; draw_piece's callers in menu.s reload).

```
        ldaz ptr
        sta sp_ptr
        ldy #1
        lda (ptr),y
        sta sp_ptr+1
        iny
        lda (ptr),y
        sta sp_w
```
->
```
  .if MODELB                        ; both ways in end with dirfetch: ptr = MAPBUF
        lda MAPBUF
        sta sp_ptr
        lda MAPBUF+1
        sta sp_ptr+1
        lda MAPBUF+2
        sta sp_w
  .else
        ldaz ptr
        sta sp_ptr
        ldy #1
        lda (ptr),y
        sta sp_ptr+1
        iny
        lda (ptr),y
        sta sp_w
  .endif
```

(ptr),0/1/2 = MAPBUF+0/1/2; the last load sets Z from W for the bne @w_ok that follows, as before. Only Y (was 2) differs.

Review: @entry2 referenced only by bra at 1531 and the Model B fall-through, both after dirfetch; ptr not written between. Z from lda MAPBUF+2 feeds the W test as before. Model B 11->9 bytes, 21->12 cycles. Y only differs, dead (ldy #7/#4 next; callers ignore Y).

## r007:9  src/engine.s:1590-1596  (window i0959)  M -0 B -3  [high]  review: accept

Cycles: Model B: W != 0: bne taken 3 -> beq not taken 2; W = 0: bne 2 + jmp 3 -> beq taken 3

Assumption: DEPENDS on at least 10 bytes of Model B savings between @w_ok ($229) and @out0 ($2B2) being applied first: the listing has @out0 137 bytes past the new branch's next PC; this region's candidates at 1598-1599 (1), 1631 (1), 1636-1639 (5), 1643-1649 (9), 1655 (4), 1661-1662 (3) supply 23. An out-of-range branch is an assembly error, so a premature application fails the build, not silently. @w_ok is referenced nowhere else (grep).

```
  .if MODELB
        bne @w_ok                   ; (the 6502 spellings put @out0 out of reach)
        jmp @out0
@w_ok:
  .else
        beq @out0
  .endif
```
->
```
        beq @out0
```

Same control flow: Z from lda (ptr),2 / sta sp_w; W = 0 -> @out0, else fall into 1597.

Review: Same control flow; @w_ok unreferenced elsewhere; bare ':' kept. Range: in current listing beq at $224 would reach @out0 at 137 bytes; needs >=10 bytes saved between, which indices 10-15 (23 bytes, all accepted) supply. MUST be applied after them; premature application is a build error, not a silent fault. Model B -3 bytes, faster on both paths.

## r007:10  src/engine.s:1598-1599  (window i0966)  M -0 B -1  [high]  review: accept

Cycles: Model B: 7 -> 4; Master unchanged

Assumption: Model B: ptr = MAPBUF here (both ways into @entry2 end with dirfetch; nothing between writes ptr). Y dead: next Y use is ldy #4 at 1609; no Y read at 1600-1607.

```
        ldy #7
        lda (ptr),y
```
->
```
  .if MODELB
        lda MAPBUF+7                ; ptr = MAPBUF (dirfetch)
  .else
        ldy #7
        lda (ptr),y
  .endif
```

(ptr),7 = MAPBUF+7; N,Z from the load are overwritten at 1602 (lsr) before any use.

Review: ptr=MAPBUF on Model B at 1598 (both ways into @entry2 end in dirfetch). Y dead until ldy #4 at 1609; N,Z overwritten by lsr.

## r007:11  src/engine.s:1631-1631  (window i0999)  M -1 B -1  [high]  review: accept

Cycles: cmp #0 2 -> tax 2 on both

Assumption: X dead: last written by tax at 1618, last read by txa at 1622. Paths after: @cpos -> @vert: no X read before `tax` at 1672 (sext touches only A/tmp3); negative arm: no X read before @vert; @out0 rts: X was already clobbered (sx low) on every path here, and the callers do not read X (draw_sprites: jmp @l -> ldx spi; draw_piece: ldpbank lda/sta/rts, menu callers reload; Model B farcall destroys X anyway). C: cmp #0 set C = 1, tax leaves ror's C, but C is overwritten by cmp #ROWCHARS (@cpos) or cmp #$FF before any read.

```
        cmp #0                      ; A still holds w16+1: just restore N,Z
```
->
```
        tax                         ; A still holds w16+1: just restore N,Z (X is dead)
```

tax sets N,Z from A exactly as cmp #0 does (A - 0); A unchanged, so beq @cpos / cmp #$FF see the same.

Review: tax sets N,Z from A as cmp #0 does. X dead: overwritten by tax at 1672 before any read on the @cpos/negative paths (sext uses only A/tmp3); at the @out0 rts X was already clobbered and callers (draw_sprites: ldx spi; draw_piece and menu callers) never read X. C differs but cmp #ROWCHARS / cmp #$FF recompute before any use. 2->1 byte both targets.

## r007:12  src/engine.s:1636-1639  (window i1004)  M -0 B -5  [high]  review: accept

Cycles: Master: lda 3/clc 2/adc 3/dec a 2 = 10 -> 3+2+3 = 8. Model B: deca 11 -> 19 total -> 8

Assumption: C = 1 on entry: the only way here is the fall-through of `cmp #$FF / bne @out0` (1633-1634), i.e. A = $FF, so C = 1. w16 = c0 + 256 is in $80..$FF (c0 = -128..-1), so sbc #2 cannot borrow. V is not read before the sbc at 1671 (fall-through) and the callers do not test V after drawsprite. W <= 255.

```
        lda w16
        clc
        adc sp_w
        deca
```
->
```
        lda w16
        sbc #2                      ; C = 1 from the cmp #$FF: w16 >= $80, so C stays 1
        adc sp_w                    ; w16 - 2 + W + 1 = c0 + W - 1
```

Result byte = (w16 - 2 + W + 1) mod 256 = (w16 + W - 1) mod 256: same A, same N,Z for bmi @out0. Carry after: original C = (c0 + W >= 0), new C = (c0 + W >= 1); on the bmi fall-through the result is 0..127, which needs c0 + W - 1 >= 0, so both give C = 1 there (the following candidate relies only on that). On the taken branch C differs only when c0 + W = 0, and @out0 is an rts whose callers do not read C (draw_sprites @next: clc; draw_piece: lda/sta/rts).

Review: Only arrival is cmp #$FF equal => C=1; w16 in $80..$FF so sbc #2 cannot borrow; result = w16+W-1 mod 256, same A,N,Z. On bmi fall-through both give C=1; C differs only when c0+W=0 which exits via rts, and no caller reads C or V. Master clc+dec a (2) vs sbc # (2): 0; Model B clc+deca(1+6) -> 2 = 5.

## r007:13  src/engine.s:1643-1649  (window i1011)  M -1 B -9  [high]  review: accept

Cycles: Master: stz 3/lda 3/eor 2/inc a 2/sta 3/bra 3 = 16 -> lda# 2/sta 3/sbc 3/sta 3/bne taken 3 = 14. Model B: 5+3+2+11+3+3 = 27 -> 14

Assumption: C = 1 here: after `adc sp_w` (1638) C = (c0 + W >= 0) and deca (Master dec a / Model B's inca-style macro) preserves it; the bmi fall-through (result 0..127) implies c0 + W >= 1, so C = 1 (also true with the sbc #2 candidate at 1636-1639). inc spclip / sta sp_c1 / lda #0 / sta sp_c0 do not touch C. w16 is in $80..$FF. A dead at @vert (ldy #5 / lda (ptr),y); C/V/N from the sbc are overwritten (sec/sbc at 1670-1671) before any read.

```
        stz sp_c0
        ; first visible column index = -c0
        lda w16
        eor #$FF
        inca
        sta sp_c                    ; starting image column
        bra @vert
```
->
```
        lda #0                      ; stz sp_c0, with the zero kept for the negate
        sta sp_c0
        ; first visible column index = -c0
        sbc w16                     ; C = 1 (bmi fall-through): A = -c0, 1..128
        sta sp_c                    ; starting image column
        bne @vert                   ; always: -c0 is never 0
```

sp_c0 := 0 as before. 0 - w16 with C = 1 = two's complement of w16 = (w16 ^ $FF) + 1, the old eor/inc; stored to sp_c. Since w16 is $80..$FF the result is $01..$80, never 0, so bne is always taken: an unconditional jump to @vert (in range: the @cpos block is 36 bytes on Model B).

Review: C=1 at 1643 (bmi fall-through implies w16+W>=257, with or without index 12; inc spclip/sta keep C). 0-w16 with C=1 = (w16^$FF)+1 = $01..$80, never zero so bne always taken to @vert (in range). A dead at @vert (ldy #5/lda). Master 11->10, Model B 19->10 bytes.

## r007:14  src/engine.s:1655-1655  (window i1023)  M -0 B -4  [medium]  review: accept

Cycles: Model B: deca 11 -> sbc #0 2; Master unchanged

Assumption: C = 0 before `adc sp_w` (bcs @out0 not taken, 1652), and c0 + W < 256 so the adc leaves C = 0: c0 < ROWCHARS = 80 here and W <= 40 in every directory (build/SPRTAB max 14, build/TITLE entries max 40, modelb/build/sprdir.bin max 14). N,Z,C,V after are overwritten by cmp #ROWCHARS (1656) / later sbc before any read.

```
        deca
```
->
```
  .if MODELB
        sbc #0                      ; C = 0 still (c0 + W < 256): A - 1, as deca
  .else
        deca
  .endif
```

With C = 0, sbc #0 = A - 0 - 1 = A - 1 (mod 256), the same A that deca produced; the cmp #ROWCHARS that follows recomputes all the flags its consumers read. (If W could reach 177 the case c0 + W = 256 would differ: the original then clamps, this would not; a safe 3-byte alternative is sec / sbc #1.)

Review: C=0 after bcs not taken; c0<80 and W<=40 (SPRTAB/sprdir.bin max 14, TITLE max 40) so adc leaves C=0 and sbc #0 = A-1 exactly; cmp #ROWCHARS recomputes N,Z,C; V overwritten by the sbc in @vert before any use. Model B deca 6 bytes/11 cy -> 2/2.

## r007:15  src/engine.s:1661-1662  (window i1023)  M -0 B -3  [high]  review: accept

Cycles: Model B: pha 3/lda# 2/sta 3/pla 4/jmp 3 = 15 -> 2+3+3 = 8; Master unchanged

Assumption: A dead at @vert: its first instructions are ldy #5 / lda (ptr),y (1666-1667). @vert is 1 byte past (after @out0's rts): in range.

```
        stza sp_c
        bra @vert
```
->
```
  .if MODELB
        lda #0                      ; A is dead at @vert: no need to keep it
        sta sp_c
        beq @vert                   ; always (Z from the lda #0)
  .else
        stza sp_c
        bra @vert
  .endif
```

sp_c := 0 as before; the pha/pla only preserved A, which @vert immediately overwrites. lda #0 sets Z, so beq is an unconditional jump to @vert.

Review: Model B stza = pha/lda#0/sta/pla; A dead at @vert (ldy #5/lda (ptr),y) so the save is useless; lda #0 sets Z so beq always taken, 1 byte to @vert. 10->7 bytes, 15->8 cycles. Master unchanged.

## r008:0  src/engine.s:1643-1647  (window i1024)  M -0 B -7  [high]  review: accept

Cycles: Master 10->10 (stz 3, lda 3, eor 2, inc a 2 -> lda# 2, sta 3, sec 2, sbc 3); Model B 21->10 (stz 5, lda 3, eor 2, inca 11)

Assumption: A is dead after sta sp_c1 (the Model B stz already destroyed it, and nothing reads it before the lda). C is dead: the next consumer is @vert's jsr sext (engine.s:4720 is and/beq/lda/sta, no carry use), then the explicit sec at 1670. N,Z are dead (bra, then ldy/lda at @vert). mtmp is scratch.

```
        stz sp_c0
        ; first visible column index = -c0
        lda w16
        eor #$FF
        inca
```
->
```
        lda #0                      ; sp_c0 = 0 and sp_c = -c0 (A is dead: stz's
        sta sp_c0                   ;  Model B form destroyed it)
        sec
        sbc w16
```

stz sp_c0 -> lda #0/sta sp_c0 (the same store). lda w16/eor #$FF/inc = -w16 = 0 - w16 with C=1, so sec/sbc w16 from the A = 0 just loaded gives it. Same sp_c0, same sp_c; only C differs, and it is dead. NOTE: together with the candidates at 1655 and 1661-1662 this brings @out0 within reach of the Model B `bne @w_ok / jmp @out0` at engine.s:1590-1596 (see the i0980 candidate).

Review: Original matches engine.s 1643-1647. -w16 = 0 - w16 with C=1, so lda #0/sta sp_c0/sec/sbc w16 leaves sp_c0 = 0 and A = -w16. Only C and V differ, and both are dead: sta sp_c/bra @vert, then ldy/lda/jsr sext (and/beq/lda/sta tmp3/rts: no C), then lda spy/sec. Master: 7 bytes both, 10 cycles both. Model B: 14 -> 7 bytes, 21 -> 10 cycles. Built in a scratch copy: Master 0, Model B LGCCODE -7.

## r008:1  src/engine.s:1655-1655  (window i1024)  M -0 B -3  [high]  review: accept

Cycles: Master identical (deca kept); Model B deca 11 -> tax/dex/txa 6

Assumption: X is dead from 1655 until the tax at 1672. The path is cmp/bcc/inc spclip/lda/sta sp_c1/stza sp_c/bra @vert, then ldy #5, lda (ptr),y, jsr sext (engine.s:4720 does not touch X), lda spy, sec, sbc (ptr),y, tax. C after is dead: cmp #ROWCHARS follows immediately.

```
        deca
```
->
```
  .if MODELB
        tax                         ; X is dead (tax at @vert): A - 1, 3 bytes not 6
        dex
        txa
  .else
        deca
  .endif
```

Model B only: tax/dex/txa computes A-1 like the deca expansion (sta mtmp/dec mtmp/lda mtmp). N,Z from txa equal N,Z from lda mtmp, and cmp overwrites them anyway. Neither form touches the carry.

Review: X is dead from 1655 to the tax at 1672. The path is cmp/bcc/inc/lda/sta/stza/bra @vert, then ldy/lda/jsr sext (sext does not touch X)/lda/sec/sbc/tax. tax/dex/txa gives A-1 with C kept and N,Z from A, like deca; cmp overwrites N,Z anyway. Master text unchanged. Built: Model B -3.

## r008:2  src/engine.s:1661-1662  (window i1024)  M -0 B -3  [high]  review: accept

Cycles: Master identical (stza = stz on the 65C02; bra kept); Model B stza 12 + jmp 3 = 15 -> lda# 2 + sta 3 + beq taken 3 = 8

Assumption: A is dead after the store: @vert (the only destination) begins ldy #5 / lda (ptr),y. Z = 1 from lda #0 makes the beq always taken. @vert is 1 byte past (after @out0's rts), so it is in range.

```
        stza sp_c
        bra @vert
```
->
```
  .if MODELB
        lda #0                      ; A is dead: @vert loads it
        sta sp_c
        beq @vert                   ; Z = 1 from the lda
  .else
        stz sp_c
        bra @vert
  .endif
```

On the Model B, stza is pha/lda #0/sta/pla only to keep A, and nothing reads A. lda #0/sta sp_c stores the same 0, and the Z it leaves turns the 3-byte jmp into a 2-byte always-taken beq. The Master branch reproduces the original bytes exactly.

Review: @vert is the only target and it loads A at once (ldy #5 / lda (ptr),y), so A is dead. lda #0 sets Z, so beq is always taken, and @vert is 1 byte away. Master branch is byte-identical (stza = stz there). Model B 15 -> 8 cycles. Built: Model B -3.

## r008:3  src/engine.s:1699-1700  (window i1044)  M -0 B -4  [high]  review: accept

Cycles: Master identical; Model B lda 3 + deca 11 = 14 -> ldx 3 + dex 2 + txa 2 = 7

Assumption: X is dead: tax at 1684 last wrote it and txa at 1688 consumed it. Nothing reads X again before ldx curbuf (1746) or ldx spclip (1783), and @out0's rts returns an X that was already garbage to the caller. C = 0 into the adc is preserved (dex/txa do not touch C, as deca's contract promises).

```
        lda sp_ext
        deca
```
->
```
  .if MODELB
        ldx sp_ext                  ; X is dead here: ext - 1, carry kept
        dex
        txa
  .else
        lda sp_ext
        deca
  .endif
```

Model B only: ldx sp_ext/dex/txa leaves A = sp_ext-1 (mod 256, the same as deca, including sp_ext = 0 -> $FF) and C unchanged. N,Z come from A, which does not matter because adc follows.

Review: X is dead: next reads are ldx curbuf (1746) and ldx spclip (1783). @out0 exits already return data-dependent garbage X (tax at 1618/1684); the callers (engine.s 1452 loop reloads X at @l; menu draw_piece through a far call) do not read it. dex/txa keep C = 0 for the adc. sp_ext (= sp_id alias) was written at 1601. Built: Model B -4.

## r008:4  src/engine.s:1714-1714  (window i1057)  M -0 B -1  [high]  review: accept

Cycles: Master bra 3 -> bcc taken 3; Model B jmp 3 -> bcc taken 3

Assumption: C = 0 here: the preceding cmp #BUFROWS*8 / bcs @out0 was not taken, and sta tmp does not change C. @ck is 7 bytes on, so it is in range.

```
        bra @ck
```
->
```
        bcc @ck                     ; C = 0: the bcs above was not taken
```

An always-taken bcc replaces the unconditional branch. On the Master bra and bcc are both 2 bytes; on the Model B, bra is a 3-byte jmp.

Review: C = 0: bcs @out0 after cmp #BUFROWS*8 was not taken, and sta tmp keeps C. @ck is in range. Master 2 bytes and 3 cycles either way. Model B jmp 3 bytes -> bcc 2. Built: Model B -1.

## r008:5  src/engine.s:1718-1718  (window i1061)  M -0 B -2  [high]  review: accept

Cycles: Master stz zp 3 -> sta zp 3; Model B lda#/sta 5 -> sta 3

Assumption: A = w16+1 = 0 at this point. @top is reached with sp_lb0+1 negative, and w16+1 = sp_lb0+1 + (carry of the low add, 0 or 1) (lines 1703-1705). bmi @out0 not taken means w16+1 >= 0, which is only possible for sp_lb0+1 = $FF with carry 1, i.e. w16+1 = $00. inc spclip does not change A.

```
        stz tmp
```
->
```
        sta tmp                     ; A = w16+1 = 0 here: lb0 < 0 <= lb1 < 256
```

sta tmp stores the 0 that A already holds. Flags: stz sets none on the Master and sta sets none either. The Model B stz set N,Z from lda #0, but @ck's lda w16+1 reloads them immediately.

Review: @top is reached only from bmi @top at 1708, with sp_lb0+1 in $80..$FF. w16+1 = sp_lb0+1 + carry of the adc at 1701 (0 or 1; lda/sta between leave C). bmi @out0 not taken forces w16+1 = $00, and inc spclip leaves A alone, so A = 0. @ck reloads A and the flags at once. Built: Master 0, Model B -2.

## r008:6  src/engine.s:1727-1732  (window i1073)  M -2 B -2  [high]  review: accept

Cycles: both: sta zp 3 -> tax 2, lda zp 3 -> txa 2 (-2 cycles on every path past the bcc)

Assumption: tmp2 (lend) is not read again. In the row loop, ds_rowloop's `stx tmp2` (~2305) sets it before sprFN/partial read it at 2066/2133/2207/2255. ringaddr, ringaddr7, mirdirty and callbank do not read tmp2. X is dead: the next X uses are ldx curbuf (1746) and ldx spclip (1783), and the @out0 exit already returns a garbage X.

```
:       sta tmp2                    ; lend
        cmp tmp
        bcc @out0
        and #7                      ; cmp/bcc leave A = tmp2: no reload needed
        sta sp_ra1
        lda tmp2
```
->
```
:       tax                         ; lend (X: tmp2 is not read again before the row loop sets it)
        cmp tmp
        bcc @out0
        and #7
        sta sp_ra1
        txa
```

Keep lend in X instead of tmp2. tax leaves A and every flag except N,Z as sta did, and cmp tmp overwrites N,Z. cmp tmp / bcc / and #7 / sta sp_ra1 are unchanged. txa reloads lend as lda tmp2 did, with the same N,Z (lsr follows anyway).

Review: tmp2 has no reader between 1732 and ds_rowloop's stx tmp2 (2305). That covers the Master fall-through into SPRITE_LOOPS and the Model B callbank to ds_entry -> ds_rowloop. The sprFN/partial reads at 2066/2133/2207/2255 all come after the dispatch. ringaddr/ringaddr7/mirdirty/callbank do not touch tmp2. The callers' tmp2 users (game.s, menu.s, display.s) write it before reading. X is dead to ldx curbuf/ldx spclip; the Model B mirdirty block reloads X. The ':' stays on the first line. Built: Master -2, Model B -2.

## r008:7  src/engine.s:1769-1770  (window i1108)  M -1 B -1  [high]  review: accept

Cycles: both -2 (clc removed)

Assumption: C = 0 after `lda wcx+1 / adc #0` (1764-1765). wcx+1 is only ever written as wx+1 >> 1 (engine.s:3096-3098) or 0 (main.s:107, menu.s:209). So wcx+1 <= $7F, and adding a carry of at most 1 cannot carry out. The sta/iny/lda between do not touch C.

```
        lda wcy
        clc
```
->
```
        lda wcy                     ; C = 0: wcx+1 <= $7F, so the adc #0 above cannot carry
```

The clc sets a carry that is already clear.

Review: wcx+1 is written only at engine.s 3098 (wx+1 lsr, so <= $7F) and by stza at main.s 107 and menu.s 209. adc #0 with C <= 1 therefore cannot carry out, and sta/iny/lda between keep C = 0. Built: Master -1, Model B -1.

## r008:8  src/engine.s:1774-1782  (window i1120)  M -0 B -8  [high]  review: accept

Cycles: Master identical; Model B width: lda 3 + sec 2 + sbc 3 + inca 11 = 19 -> ldx 3 + inx 2 + txa 2 + sec 2 + sbc 3 = 12; height: inca 11 -> adc #0 2

Assumption: sp_c1 >= sp_c0 and sp_c1 <= 127. On the c0-negative path c0 = 0 and the bmi at 1640 bounds c1 to 0..127; on the @cpos path c1 = min(c0+W-1, 79) with c0 < 80. This is the same invariant the original's comment at 1781 relies on for C = 1 into sbc sp_r0. sp_r1 >= sp_r0 because the bcc @out0 at 1729 guarantees lend >= lstart. X is dead: ldx spclip at 1783 reloads it. C after the height is dead on the Model B: the next instruction that reads C comes after `lda mrow / sec` (1788-1789).

```
        lda sp_c1
        sec
        sbc sp_c0
        inca
        sta (rp),y
        iny
        lda sp_r1
        sbc sp_r0                   ; C still set by the width sbc above (sp_c1 >= sp_c0)
        inca
```
->
```
  .if MODELB
        ldx sp_c1                   ; width = c1 + 1 - c0 (X dead: ldx spclip below)
        inx
        txa
        sec
        sbc sp_c0
  .else
        lda sp_c1
        sec
        sbc sp_c0
        inca
  .endif
        sta (rp),y
        iny
        lda sp_r1
        sbc sp_r0                   ; C still set by the width sbc above (sp_c1 >= sp_c0)
  .if MODELB
        adc #0                      ; and set by this one (sp_r1 >= sp_r0): + 1
  .else
        inca
  .endif
```

Width: (c1+1) - c0 with C=1 = c1 - c0 + 1, and C = 1 out (no borrow, and c1+1 does not wrap), exactly the C the original inca preserved. Height: sbc sp_r0 with C=1 leaves C = 1 (r1 >= r0), so adc #0 adds exactly 1, as inca did. The result (<= 32) sets the same N,Z as inca's lda mtmp. C ends 0 instead of 1, but the Model B sec at 1789 is the next C consumer. The Master branches reproduce the original bytes.

Review: c1 <= 127 on both paths: c0-negative bmi bound, @cpos clamp to 79. So inx never wraps, and (c1+1)-c0 gives the same width with C = 1, as the original sbc did (c1 >= c0 is the original's own invariant). The height sbc leaves C = 1 (r1 >= r0 by the bcc at 1729), so adc #0 adds exactly 1. C ends 0 rather than 1, but on the Model B the next C reader is after lda mrow/sec (1788-1789). X is dead to ldx spclip. Master text unchanged. Built: Model B -8.

## r008:9  src/engine.s:1802-1828  (window i1149)  M -0 B -15  [high]  review: accept

Cycles: Master identical; Model B every path faster: no-mirror/no-copy 48 -> 27; mirror/copy 59 -> 31 (bitimm is 15 cycles each)

Assumption: After the block only X (-> sp_disp) is consumed: lda sp_ptr+1 follows (1830) and ldx sp_c (1833) reloads X. C, V and N are dead: the next C use is after clc (1836) or the lsr at 1856 on the @mdone path, and there is no bvc/bvs. mtmp (bitimm's scratch) is not read afterwards.

```
        lda sp_flags
        bitimm 1
        beq @nomirror
        ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
        lda sp_flags                ; only the mirror arm clobbers A
@nomirror:
        ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
        bitimm 8                    ; bit3: copy blitter
        beq :+
        ldx #8
        bne :++
:       and #3                      ; A is still sp_flags: bit #imm does not alter A
        asl
        tax
:
  .if MODELB
        stx sp_disp                 ; the loop copy in the data's bank patches its own jump
  .else
        lda sprdisp_tab,x
        sta ds_dispatch+1
        lda sprdisp_tab+1,x
        sta ds_dispatch+2
  .endif
```
->
```
  .if MODELB
        lda sp_flags
        lsr                         ; C = bit 0: mirror
        bcc @nomirror
        ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
@nomirror:
        ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
        lda #8                      ; bit3: copy blitter
        bit sp_flags
        bne :+                      ; A = 8: the copy blitter's index
        lda sp_flags
        and #3
        asl
:       tax
:                                   ; keep the bare label: the anonymous-label count
        stx sp_disp                 ; the loop copy in the data's bank patches its own jump
  .else
        lda sp_flags
        bitimm 1
        beq @nomirror
        ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
        lda sp_flags                ; only the mirror arm clobbers A
@nomirror:
        ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
        bitimm 8                    ; bit3: copy blitter
        beq :+
        ldx #8
        bne :++
:       and #3                      ; A is still sp_flags: bit #imm does not alter A
        asl
        tax
:
        lda sprdisp_tab,x
        sta ds_dispatch+1
        lda sprdisp_tab+1,x
        sta ds_dispatch+2
  .endif
```

Mirror test: lsr puts bit 0 in C and bcc skips the arm when it is clear, exactly as bitimm 1/beq did. The arm still does clc/lda/sbc/sta; its lda sp_flags reload goes, because nothing needs A = flags now. Copy test: lda #8/bit sp_flags gives Z = !(flags & 8). When bit 3 is set, A = 8 is already the index, so tax gives X = 8 as ldx #8 did. Otherwise lda sp_flags/and #3/asl/tax gives X = (flags&3)*2 as before. stx sp_disp is unchanged.

Review: lsr/bcc tests bit 0 as bitimm 1/beq did. lda #8/bit sp_flags/bne gives X = 8 via tax on copy; otherwise (flags&3)*2 via the same and/asl/tax. V/N/C changes are dead: no bvc/bvs in drawsprite or SPRITE_LOOPS, and C is rewritten by clc (1835) or the lsr at 1854 before any read. mtmp has no direct reader anywhere (defined only in modelb/src/defs.inc). There are still two anonymous labels, both inside the block, and the only reference to them is the internal bne :+. No outside :- or :+ crosses the block (1784 beq :+ -> 1786, 1837 bcc :+ -> 1840). Master branch is byte-identical to the original. Built: Model B -15 (39 -> 24 bytes plus stx); every path faster.

## r008:10  src/engine.s:1590-1596  (window i0980)  M -0 B -3  [medium]  review: accept

Cycles: Model B: W=0 path bne 2 + jmp 3 = 5 -> beq taken 3; W>0 path bne taken 3 -> beq not taken 2. Master unchanged (it already assembles beq @out0)

Assumption: DEPENDS on the r008 candidates at engine.s 1643-1647 (7 bytes), 1655 (3) and 1661-1662 (3) being applied first. With them, the Model B branch distance to @out0 is 124 (verified by assembly). Without them it is 137, which the assembler rejects loudly as a range error. This candidate is OUTSIDE this region's windows (it falls in r007's i0980); it is reported because only this region's savings make it possible.

```
  .if MODELB
        bne @w_ok                   ; (the 6502 spellings put @out0 out of reach)
        jmp @out0
@w_ok:
  .else
        beq @out0
  .endif
```
->
```
        beq @out0
```

Replace the .if MODELB `bne @w_ok / jmp @out0 / @w_ok:` with the shared beq @out0, which is the Master's existing spelling. @w_ok is referenced nowhere else (a local cheap label).

Review: Correct, but only once candidates 0 and 2 are in. Built alone: range error (137). With 0,1,2: fits, total Model B -16, so -3 for this one. With 0 and 2 only: also fits (127). Candidates after @out0 do not affect the distance. The W=0 path is 5 -> 3 cycles and W>0 is 3 -> 2. @w_ok has no other reference and the ':' at 1597 is untouched. If the applier tries it before 0/2, it fails loudly at assembly and must be retried after them.

## r009:0  src/engine.s:1788-1790  (window i1152)  M -0 B -1  [high]  review: accept

Cycles: Model B only code: 2 cycles faster on every path; Master unaffected (inside .if MODELB)

Assumption: C = 1 on entry: engine.s:1774-1776 lda sp_c1/sec/sbc sp_c0 (c1 >= c0), then 1780-1781 sbc sp_r0 with C=1 and r1 >= r0 (lend >= lstart: 'cmp tmp / bcc @out0' at ~1727) leaves C=1; inca on the Model B (cpu.inc: sta/inc/lda mtmp) keeps the carry; ldx/beq/ora/sta do not touch it; both paths into the ':' at 1786 have it. Gate-tested in a scratch copy (both builds + tools/gate.sh): every check passes except 'B title', which fails for ANY change to the Model B image -- a same-size, same-cycle sec->nop swap at engine.s:1789 fails it too: the title's display-RAM compare ($0300-$7FFF) sees a residue of the loaded code at $4636, not a behaviour difference.

```
        lda mrow                    ; the mirror's row, relative to the window: if the
        sec                         ; sprite covers it, note the columns (see drawrect)
        sbc wcy
```
->
```
        lda mrow                    ; the mirror's row, relative to the window: if the
        sbc wcy                     ; sprite covers it, note the columns (see drawrect); C = 1 from the height sbc
```

sec is redundant: the carry is already 1 on both paths into 1788 (see assumption), so sbc wcy computes the same A and flags.

Review: Original matches 1788-1790. C=1 at 1788 on both arrivals: sec/sbc sp_c0 (c1>=c0, the existing comment at 1781 relies on it), inca keeps C on the Model B (sta/inc/lda mtmp), sbc sp_r0 with C=1 and r1>=r0 (tmp2>=tmp enforced by cmp tmp/bcc @out0 at 1728), then ldx/beq/ora/sta leave C. Model B only. Assembled: Model B LGCCODE -1, Master 0.

## r009:1  src/engine.s:1802-1820  (window i1152)  M -0 B -15  [high]  review: accept

Cycles: Model B: nomirror 21->8, mirror arm 34->18, copy select 22->10, non-copy select 24->16 (bitimm is 15 cycles). Master: the original text, unchanged (.else arm)

Assumption: A, flags and carry dead after the block: the next instructions are stx sp_disp (1822) then lda sp_ptr+1 (1830); the next carry consumer is behind a clc (1835) or lsr (1854). mtmp (written by bitimm) is macro scratch nothing reads later. sp_disp encoding kept: X = 8 for bit3 else (flags&3)*2 (read by SPRITE_LOOPS ds_entry, engine.s:2273). Gate-tested in a scratch copy (both builds + tools/gate.sh): every check passes except 'B title', which fails for ANY change to the Model B image -- a same-size, same-cycle sec->nop swap at engine.s:1789 fails it too: the title's display-RAM compare ($0300-$7FFF) sees a residue of the loaded code at $4636, not a behaviour difference.

```
        lda sp_flags
        bitimm 1
        beq @nomirror
        ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
        lda sp_flags                ; only the mirror arm clobbers A
@nomirror:
        ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
        bitimm 8                    ; bit3: copy blitter
        beq :+
        ldx #8
        bne :++
:       and #3                      ; A is still sp_flags: bit #imm does not alter A
        asl
        tax
:
```
->
```
  .if MODELB                        ; (no bit #imm here: lsr and and, not bitimm)
        lda sp_flags
        lsr                         ; C = bit0: mirrored
        bcc :+
                ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
                ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
:       lda sp_flags
        and #8                      ; bit3: copy blitter, and 8 is its index
        bne :+
        lda sp_flags
        and #3
        asl
:       tax
  .else
        lda sp_flags
        bitimm 1
        beq @nomirror
                ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
        lda sp_flags                ; only the mirror arm clobbers A
@nomirror:
        ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
        bitimm 8                    ; bit3: copy blitter
        beq :+
        ldx #8
        bne :++
:       and #3                      ; A is still sp_flags: bit #imm does not alter A
        asl
        tax
:
  .endif
```

Model B arm: lsr puts flags bit0 in C (bcc = bitimm 1/beq); mirror arm identical (clc/lda sp_w/sbc sp_c/sta sp_c); then lda sp_flags/and #8 is bitimm 8's Z test and leaves A = 8 = the old ldx #8 value when taken (tax); else reload flags and #3/asl/tax as before. Same X on all paths, same sp_c.

Review: Original matches 1802-1820. Model B arm: lsr/bcc = bitimm 1/beq; mirror arm identical; lda sp_flags/and #8/bne leaves A=8 -> tax gives X=8 as ldx #8; else (flags&3)*2. Two ':' per target, bcc :+ and bne :+ hit the right ones; @nomirror only referenced in its own (Master) arm. A/N/Z/C/V dead after (stx sp_disp, lda sp_ptr+1, ... lsr before any carry use); mtmp is scratch. Cycles Model B 45->24 nomirror non-copy, all paths faster. Master text unchanged. Assembled: Model B LGCCODE -15, Master 0.

## r009:2  src/engine.s:1843-1843  (window i1166)  M -0 B -2  [high]  review: accept

Cycles: Master stz zp 3 -> stx zp 3; Model B lda #0/sta zp 5 -> stx 3

Assumption: X = 0 at @mdone (1842): the only arrivals are beq @mdone with X = sp_c = 0 (1833-1834) and the dex/bne @mul exit (1840-1841). A and flags after: next is lda sp_c (1844).

```
        stz mtab                    ; the MASKTAB pages are indexed by the mask byte
```
->
```
        stx mtab                    ; X = 0 (sp_c or the loop's count): MASKTAB pages are indexed by the mask byte
```

stores the same 0 to mtab (tmp3); Model B's stz expansion clobbered A/N/Z which are reloaded at 1844.

Review: @mdone is reached only by beq @mdone after ldx sp_c (X=0) and the dex/bne fall-through (X=0). A/flags reloaded by lda sp_c. Master stz zp -> stx zp same size/cycles; Model B lda #0/sta -> stx. Assembled: Model B -2, Master 0.

## r009:3  src/engine.s:1868-1874  (window i1192)  M -2 B -2  [high]  review: accept

Cycles: both: lda wcy 3 + clc 2 + adc sp_r0 3 -> clc 2 + adc wcy 3 (3 faster)

Assumption: none beyond the window: A = sp_r0 after sta sp_row

```
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        lda wcy
        clc
        adc sp_r0
```
->
```
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        clc
        adc wcy
```

sp_r0 + wcy with C=0 is wcy + sp_r0: same A, same flags into ringaddr.

Review: A = sp_r0 after sta sp_row; clc/adc wcy gives the same A and flags as lda wcy/clc/adc sp_r0 (wcy, sp_r0 zp on both). @rows has no other reference in drawsprite's scope. Assembled: Master -2, Model B -2.

## r009:4  src/engine.s:1880-1883  (window i1198)  M -2 B -2  [high]  review: accept

Cycles: both: 3 faster (lda sp+1 gone)

Assumption: ringaddr (engine.s:537-557, Master) and ringaddr7 (engine.s:4819-4843, Model B, its only caller is 1876) both end 'ringup sp / sta sp+1 / rts', so A = sp+1 on return (the Model B ringup ends pla/sbc #0 into A). Flags after: next is lda tmp (1886).

```
        lda sp
        sta sp_rb
        lda sp+1
        sta sp_rb+1
```
->
```
        sta sp_rb+1                 ; A = sp+1: ringaddr's last store
        lda sp
        sta sp_rb
```

same two stores; only their order and the redundant reload change.

Review: ringaddr (537-557) and ringaddr7 (4819-4843) both end ringup sp / sta sp+1 / rts on every path, so A = sp+1 on return. Stores reordered only; flags dead (lda tmp next). Assembled: Master -2, Model B -2.

## r009:5  src/engine.s:1898-1899  (window i1212)  M -1 B -1  [high]  review: accept

Cycles: cmp #imm 2 -> asl a 2: identical

Assumption: A dead after: ldx #4 then ':' stx sp_rinc / lda sp_col (1902-1904); N/Z are rewritten by the ror w16 that follows

```
        lda w16+1
        cmp #$80
```
->
```
        lda w16+1
        asl                         ; C = the sign: an arithmetic >> 1
```

asl sets C = bit 7 of w16+1, exactly what cmp #$80 set; the rors read only C and memory.

Review: asl sets C = bit7 of w16+1 exactly as cmp #$80; A dead (ldx #4, stx, lda sp_col after; the bne :+ path skips it), N/Z rewritten by ror. 2 cycles both. Assembled: Master -1, Model B -1.

## r009:6  src/engine.s:1911-1912  (window i1224)  M -1 B -1  [high]  review: accept

Cycles: identical (2 -> 2)

Assumption: A dead after: lda sp_mrp at 1915; N/Z rewritten by ror w16

```
        lda w16+1                   ; the same offset in pixel rows (signed >> 1)
        cmp #$80
```
->
```
        lda w16+1                   ; the same offset in pixel rows (signed >> 1)
        asl                         ; C = the sign
```

as the 1898 case: asl gives C = sign bit, same as cmp #$80.

Review: Same as 1898: asl gives C = sign; A reloaded by lda sp_mrp, N/Z rewritten by ror. Assembled: Master -1, Model B -1.

## r009:7  src/engine.s:1948-1952  (window i1245)  M -0 B -2  [high]  review: accept

Cycles: Model B: 2 faster per k=0 line of the copy blitter (ldy #0 gone); Master unchanged

Assumption: SPRLINE is only instantiated as SPRFULL sprFC, 0, 1 (engine.s:2422 when SPR4_COPY, 2430, 2446): copy=1, mirror=0, so this k=0 store follows ldaz ptr directly, whose Model B expansion (cpu.inc) is ldy #0/lda (ptr),y; l0 is only entered at the ldaz (beq l0 / jmpx et). Saves 2 bytes per Model B instance: 1 today (SPR6CODE; SPR4_COPY = 0 in modelb/build/assets.inc). Gate-tested in a scratch copy (both builds + tools/gate.sh): every check passes except 'B title', which fails for ANY change to the Model B image -- a same-size, same-cycle sec->nop swap at engine.s:1789 fails it too: the title's display-RAM compare ($0300-$7FFF) sees a residue of the loaded code at $4636, not a behaviour difference.

```
  .if k = 0
        staz sp                     ; box sprite: every byte opaque, plain copy
  .else
        sta (sp),y
  .endif
```
->
```
  .if k = 0
    .if ::MODELB
        sta (sp),y                  ; box sprite: Y = 0 still, from ldaz's ldy #0
    .else
        staz sp                     ; box sprite: every byte opaque, plain copy
    .endif
  .else
        sta (sp),y
  .endif
```

Y is still 0 at the store on the Model B (the mirror form's tax/lda abs,x would not touch it either), so sta (sp),y = staz sp's ldy #0/sta (sp),y minus the reload.

Review: SPRLINE is only instantiated inside SPRFULL, and SPRFULL only as sprFC, 0, 1 (copy=1, mirror=0). Model B ldaz ptr = ldy #0/lda (ptr),y, so Y=0 at the store (even the mirror form's tax/lda SWAPTAB,x would keep Y); l0 is entered only at its first instruction (beq l0, et table). Master branch unchanged. Assembled: Model B SPR6CODE -2 (SPR4_COPY=0), Master 0.

## r009:8  src/engine.s:1829-1910  (window i1185)  M -6 B -8  [high]  review: accept

Cycles: both: old fixed cost 9 (sp_ptr copy) + 3 (ldx) + 3 (sta sp_col) + 20 (the sp_col + w16 add) = 35 -> 21 (sp_ptr + w16 via tay/tya) + 3 + 3 = 27; the @mul loop is the same instructions (inc sp_rp+1 zp for inc sp_col+1 zp); c = 0 path 38 -> 30; Model B also stz 5 -> stx 3

Assumption: sp_col is a temporary: only engine.s:1831,1838,1842,1904,1908 touch it (grep src, modelb/src). sp_c and sp_lines unchanged between 1833 and 1904. mtab (tmp3) is not read or written by ringaddr/ringaddr7 or 1844-1903, so zeroing it later is equivalent. Y dead after drawsprite's prologue: Master falls into ds_rowloop (SPRITE_LOOPS at engine.s:2438), Model B goes lda sp_dbank/jmp callbank (low.s:132) -> ds_entry (wrsel = sta ROMSEL, ldx sp_disp); every blitter path sets Y before reading it (MPAIR ldy #k/2, SPRMSK partial tay, SPRFULL partial ldy tmp, SPRLINE ldaz/ldy #k). X = 0 at the new @mdone (beq with sp_c = 0, or the dex/bne exit). Gate-tested in a scratch copy (both builds + tools/gate.sh): every check passes except 'B title', which fails for ANY change to the Model B image -- a same-size, same-cycle sec->nop swap at engine.s:1789 fails it too: the title's display-RAM compare ($0300-$7FFF) sees a residue of the loaded code at $4636, not a behaviour difference.

```
        ; sp_col = sp_ptr + sp_c * lines
        lda sp_ptr+1
        sta sp_col+1
        lda sp_ptr
        ldx sp_c
        beq @mdone
        clc
@mul:   adc sp_lines
        bcc :+
        inc sp_col+1
        clc
:       dex
        bne @mul
@mdone: sta sp_col
        stz mtab                    ; the MASKTAB pages are indexed by the mask byte
        lda sp_c
        and #3                      ; phase of the first column drawn, and its page:
        ora #>MASKTAB0              ; MASKTAB0 is 1K aligned, so phase = page & 3
        sta sp_mpg0
        ; mask column base = mask plane + (first image column / 4) * pixel rows
        lda sp_mbase
        sta sp_mrp
        lda sp_mbase+1
        sta sp_mrp+1
        lda sp_c
        lsr
        lsr
        beq @mgdone
        tax
@mgrp:  lda sp_mrp
        clc
        adc sp_mh
        sta sp_mrp
        bcc @mgnc
        inc sp_mrp+1
@mgnc:  dex
        bne @mgrp
@mgdone:
@rows:
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        lda wcy
        clc
        adc sp_r0
  .if MODELB
        jsr ringaddr7               ; this bank's own copy (no crossing)
  .else
        jsr ringaddr
  .endif
        lda sp
        sta sp_rb
        lda sp+1
        sta sp_rb+1
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        lda tmp                     ; tmp is still lstart, and sp_r0 = lstart >> 3,
        and #$F8                    ; so r0*8 is lstart & $F8: no reload and no shifts
        sec
        sbc sp_lb0
        sta w16
        lda #0
        sbc sp_lb0+1
        sta w16+1
        ldx #8
        lda sp_flags
        and #2
        bne :+
        lda w16+1
        cmp #$80
        ror w16+1
        ror w16
        ldx #4
:       stx sp_rinc
        lda sp_col
        clc
        adc w16
        sta sp_rp
        lda sp_col+1
        adc w16+1
        sta sp_rp+1
```
->
```
        lda sp_c
        and #3                      ; phase of the first column drawn, and its page:
        ora #>MASKTAB0              ; MASKTAB0 is 1K aligned, so phase = page & 3
        sta sp_mpg0
        ; mask column base = mask plane + (first image column / 4) * pixel rows
        lda sp_mbase
        sta sp_mrp
        lda sp_mbase+1
        sta sp_mrp+1
        lda sp_c
        lsr
        lsr
        beq @mgdone
        tax
@mgrp:  lda sp_mrp
        clc
        adc sp_mh
        sta sp_mrp
        bcc @mgnc
        inc sp_mrp+1
@mgnc:  dex
        bne @mgrp
@mgdone:
@rows:
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        lda wcy
        clc
        adc sp_r0
  .if MODELB
        jsr ringaddr7               ; this bank's own copy (no crossing)
  .else
        jsr ringaddr
  .endif
        lda sp
        sta sp_rb
        lda sp+1
        sta sp_rb+1
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        lda tmp                     ; tmp is still lstart, and sp_r0 = lstart >> 3,
        and #$F8                    ; so r0*8 is lstart & $F8: no reload and no shifts
        sec
        sbc sp_lb0
        sta w16
        lda #0
        sbc sp_lb0+1
        sta w16+1
        ldx #8
        lda sp_flags
        and #2
        bne :+
        lda w16+1
        cmp #$80
        ror w16+1
        ror w16
        ldx #4
:       stx sp_rinc
        ; sp_rp = sp_ptr + w16 + sp_c * lines (the column base needs no copy of its own)
        lda sp_ptr
        clc
        adc w16
        tay
        lda sp_ptr+1
        adc w16+1
        sta sp_rp+1
        tya
        ldx sp_c
        beq @mdone
        clc
@mul:   adc sp_lines
        bcc :+
        inc sp_rp+1
        clc
:       dex
        bne @mul
@mdone: sta sp_rp
        stx mtab                    ; X = 0 here: the MASKTAB pages are indexed by the mask byte
```

sp_rp = (sp_ptr + sp_c*lines) + w16 is computed as (sp_ptr + w16) + sp_c*lines: 16-bit addition is associative mod 65536. The column-base loop now accumulates straight into A/sp_rp+1 starting from sp_ptr + w16, so sp_col and its final add disappear. stz mtab moves after the loop as stx mtab (X = 0 there).

Review: sp_col is only touched at 1831/1838/1842/1904/1908 (grep src, modelb/src). (sp_ptr + w16) + c*lines == (sp_ptr + c*lines) + w16 mod 65536; loop carries go to sp_rp+1 (zp like sp_col). X=0 at new @mdone -> stx mtab; tmp3 not used by ringaddr/ringaddr7 or the lines between. Y dead: drawsprite's caller (1452) does not read Y after, the blitters set Y before use, callbank/pagelogic do not read it. Anon labels: bne :+ (1897) still reaches ': stx sp_rinc', the loop's bcc :+ its own dex; nothing references across. No path slower. Assembled: Master -6, Model B -8.

## r009:9  src/engine.s:1848-1921  (window i1202)  M -13 B -7  [high]  review: accept

Cycles: n = sp_c>>2. n=0: Model B 42 -> 38, Master 50 -> 42. n>0: fixed Model B 43 -> 39, Master 51 -> 43; per iteration Model B 19 -> 11 (carry 23 -> 17), Master 22 -> 12 (carry 27 -> 19)

Assumption: sp_mrp is not read between 1849 and 1915 (only SPRITE_LOOPS reads it). sp_c, sp_mbase, sp_mh unchanged in between. X dead after 1914 (it held sp_rinc, already stored) and X dead after it: ds_rowloop loads X (ldx sp_ra0 / ldx #7) before any read; Model B ds_entry does ldx sp_disp. Y dead after drawsprite's prologue: Master falls into ds_rowloop (SPRITE_LOOPS at engine.s:2438), Model B goes lda sp_dbank/jmp callbank (low.s:132) -> ds_entry (wrsel = sta ROMSEL, ldx sp_disp); every blitter path sets Y before reading it (MPAIR ldy #k/2, SPRMSK partial tay, SPRFULL partial ldy tmp, SPRLINE ldaz/ldy #k). Gate-tested in a scratch copy (both builds + tools/gate.sh): every check passes except 'B title', which fails for ANY change to the Model B image -- a same-size, same-cycle sec->nop swap at engine.s:1789 fails it too: the title's display-RAM compare ($0300-$7FFF) sees a residue of the loaded code at $4636, not a behaviour difference.

```
        ; mask column base = mask plane + (first image column / 4) * pixel rows
        lda sp_mbase
        sta sp_mrp
        lda sp_mbase+1
        sta sp_mrp+1
        lda sp_c
        lsr
        lsr
        beq @mgdone
        tax
@mgrp:  lda sp_mrp
        clc
        adc sp_mh
        sta sp_mrp
        bcc @mgnc
        inc sp_mrp+1
@mgnc:  dex
        bne @mgrp
@mgdone:
@rows:
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        lda wcy
        clc
        adc sp_r0
  .if MODELB
        jsr ringaddr7               ; this bank's own copy (no crossing)
  .else
        jsr ringaddr
  .endif
        lda sp
        sta sp_rb
        lda sp+1
        sta sp_rb+1
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        lda tmp                     ; tmp is still lstart, and sp_r0 = lstart >> 3,
        and #$F8                    ; so r0*8 is lstart & $F8: no reload and no shifts
        sec
        sbc sp_lb0
        sta w16
        lda #0
        sbc sp_lb0+1
        sta w16+1
        ldx #8
        lda sp_flags
        and #2
        bne :+
        lda w16+1
        cmp #$80
        ror w16+1
        ror w16
        ldx #4
:       stx sp_rinc
        lda sp_col
        clc
        adc w16
        sta sp_rp
        lda sp_col+1
        adc w16+1
        sta sp_rp+1
        lda w16+1                   ; the same offset in pixel rows (signed >> 1)
        cmp #$80
        ror w16+1
        ror w16
        lda sp_mrp
        clc
        adc w16
        sta sp_mrp
        lda sp_mrp+1
        adc w16+1
        sta sp_mrp+1
```
->
```
@rows:
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        lda wcy
        clc
        adc sp_r0
  .if MODELB
        jsr ringaddr7               ; this bank's own copy (no crossing)
  .else
        jsr ringaddr
  .endif
        lda sp
        sta sp_rb
        lda sp+1
        sta sp_rb+1
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        lda tmp                     ; tmp is still lstart, and sp_r0 = lstart >> 3,
        and #$F8                    ; so r0*8 is lstart & $F8: no reload and no shifts
        sec
        sbc sp_lb0
        sta w16
        lda #0
        sbc sp_lb0+1
        sta w16+1
        ldx #8
        lda sp_flags
        and #2
        bne :+
        lda w16+1
        cmp #$80
        ror w16+1
        ror w16
        ldx #4
:       stx sp_rinc
        lda sp_col
        clc
        adc w16
        sta sp_rp
        lda sp_col+1
        adc w16+1
        sta sp_rp+1
        lda w16+1                   ; the same offset in pixel rows (signed >> 1)
        cmp #$80
        ror w16+1
        ror w16
        ; mask row pointer = mask plane + (first image column / 4) * pixel rows + that offset
        lda sp_c
        lsr
        lsr
        tay                         ; column groups to step over
        lda sp_mbase
        clc
        adc w16
        tax
        lda sp_mbase+1
        adc w16+1
        sta sp_mrp+1
        txa
        cpy #0
        beq @mgdone
        clc
@mgrp:  adc sp_mh
        bcc @mgnc
        inc sp_mrp+1
        clc
@mgnc:  dey
        bne @mgrp
@mgdone: sta sp_mrp
```

sp_mrp = sp_mbase + n*sp_mh + (w16 >> 1) computed as (sp_mbase + w16') + n*sp_mh (associative mod 65536): the sum is formed once in A (low, parked in X) and sp_mrp+1, then the group loop adds sp_mh in A with Y = n as the count, so neither the running reload/store of sp_mrp nor the final add is needed. cpy #0 replaces the Z the old lsr gave.

Review: sp_mrp not read between 1850 and 1915 (ringaddr/ringaddr7 do not touch it); sp_c/sp_mbase/sp_mh unchanged. mbase + n*mh + w16' computed once, loop carries into sp_mrp+1 with clc after inc. X and Y dead after (callbank path: ds_entry ldx sp_disp; Master ds_rowloop loads X before use; Y as candidate 8). cpy #0 supplies the Z the lsr gave. Master sp_mbase/sp_mh/sp_mrp are absolute: recounted n=0 50->42, per iteration 22->12 (carry 27->19); Model B zp also faster. Assembled: Master -13, Model B -7.

## r009:10  src/engine.s:1829-1921  (window i1185)  M -25 B -21  [high]  review: accept

Cycles: sum of the component rewrites, each no slower on every path (see the SPCOL, MASK, stx mtab, r0, ringaddr-return and asl candidates); all faster or equal on both targets

Assumption: Combination of candidates 1843 (stx mtab), 1868-1874, 1880-1883, 1898-1899, 1911-1912, the sp_col fold (1829-1910) and the mask fold (1848-1921): their assumptions together. Offered so the whole region applies at once; the parts go stale after it. Gate-tested in a scratch copy (both builds + tools/gate.sh): every check passes except 'B title', which fails for ANY change to the Model B image -- a same-size, same-cycle sec->nop swap at engine.s:1789 fails it too: the title's display-RAM compare ($0300-$7FFF) sees a residue of the loaded code at $4636, not a behaviour difference.

```
        ; sp_col = sp_ptr + sp_c * lines
        lda sp_ptr+1
        sta sp_col+1
        lda sp_ptr
        ldx sp_c
        beq @mdone
        clc
@mul:   adc sp_lines
        bcc :+
        inc sp_col+1
        clc
:       dex
        bne @mul
@mdone: sta sp_col
        stz mtab                    ; the MASKTAB pages are indexed by the mask byte
        lda sp_c
        and #3                      ; phase of the first column drawn, and its page:
        ora #>MASKTAB0              ; MASKTAB0 is 1K aligned, so phase = page & 3
        sta sp_mpg0
        ; mask column base = mask plane + (first image column / 4) * pixel rows
        lda sp_mbase
        sta sp_mrp
        lda sp_mbase+1
        sta sp_mrp+1
        lda sp_c
        lsr
        lsr
        beq @mgdone
        tax
@mgrp:  lda sp_mrp
        clc
        adc sp_mh
        sta sp_mrp
        bcc @mgnc
        inc sp_mrp+1
@mgnc:  dex
        bne @mgrp
@mgdone:
@rows:
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        lda wcy
        clc
        adc sp_r0
  .if MODELB
        jsr ringaddr7               ; this bank's own copy (no crossing)
  .else
        jsr ringaddr
  .endif
        lda sp
        sta sp_rb
        lda sp+1
        sta sp_rb+1
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        lda tmp                     ; tmp is still lstart, and sp_r0 = lstart >> 3,
        and #$F8                    ; so r0*8 is lstart & $F8: no reload and no shifts
        sec
        sbc sp_lb0
        sta w16
        lda #0
        sbc sp_lb0+1
        sta w16+1
        ldx #8
        lda sp_flags
        and #2
        bne :+
        lda w16+1
        cmp #$80
        ror w16+1
        ror w16
        ldx #4
:       stx sp_rinc
        lda sp_col
        clc
        adc w16
        sta sp_rp
        lda sp_col+1
        adc w16+1
        sta sp_rp+1
        lda w16+1                   ; the same offset in pixel rows (signed >> 1)
        cmp #$80
        ror w16+1
        ror w16
        lda sp_mrp
        clc
        adc w16
        sta sp_mrp
        lda sp_mrp+1
        adc w16+1
        sta sp_mrp+1
```
->
```
        lda sp_c
        and #3                      ; phase of the first column drawn, and its page:
        ora #>MASKTAB0              ; MASKTAB0 is 1K aligned, so phase = page & 3
        sta sp_mpg0
@rows:
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        clc
        adc wcy
  .if MODELB
        jsr ringaddr7               ; this bank's own copy (no crossing)
  .else
        jsr ringaddr
  .endif
        sta sp_rb+1                 ; A = sp+1: ringaddr's last store
        lda sp
        sta sp_rb
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        lda tmp                     ; tmp is still lstart, and sp_r0 = lstart >> 3,
        and #$F8                    ; so r0*8 is lstart & $F8: no reload and no shifts
        sec
        sbc sp_lb0
        sta w16
        lda #0
        sbc sp_lb0+1
        sta w16+1
        ldx #8
        lda sp_flags
        and #2
        bne :+
        lda w16+1
        asl                         ; C = the sign: an arithmetic >> 1
        ror w16+1
        ror w16
        ldx #4
:       stx sp_rinc
        ; sp_rp = sp_ptr + w16 + sp_c * lines (the column base needs no copy of its own)
        lda sp_ptr
        clc
        adc w16
        tay
        lda sp_ptr+1
        adc w16+1
        sta sp_rp+1
        tya
        ldx sp_c
        beq @mdone
        clc
@mul:   adc sp_lines
        bcc :+
        inc sp_rp+1
        clc
:       dex
        bne @mul
@mdone: sta sp_rp
        stx mtab                    ; X = 0 here: the MASKTAB pages are indexed by the mask byte
        lda w16+1                   ; the same offset in pixel rows (signed >> 1)
        asl                         ; C = the sign
        ror w16+1
        ror w16
        ; mask row pointer = mask plane + (first image column / 4) * pixel rows + that offset
        lda sp_c
        lsr
        lsr
        tay                         ; column groups to step over
        lda sp_mbase
        clc
        adc w16
        tax
        lda sp_mbase+1
        adc w16+1
        sta sp_mrp+1
        txa
        cpy #0
        beq @mgdone
        clc
@mgrp:  adc sp_mh
        bcc @mgnc
        inc sp_mrp+1
        clc
@mgnc:  dey
        bne @mgrp
@mgdone: sta sp_mrp
```

see the component candidates; ordering: mpg0, ringaddr + sp_rb, w16 offset (and the half-res shift), sp_rp = sp_ptr + w16 + c*lines, stx mtab (X = 0), w16 >>= 1, sp_mrp = sp_mbase + w16 + (c>>2)*mh.

Review: Composition of 2,3,4,5,6,8,9 (plus removing the duplicated comment line); re-checked as a whole: order mpg0, ringaddr+sp_rb (A=sp+1), w16 offset, sp_rp fold, stx mtab (X=0), w16>>1, mask fold. Anon labels: bne :+ -> ': stx sp_rinc', loop bcc :+ -> ': dex'. Assembled: Master -25, Model B -21.

## r009:11  src/engine.s:1769-1925  (window i1152)  M -33 B -45  [medium]  review: accept

Cycles: everything in the region candidates, plus: ringaddr is called from right after the record's wcy+r0 store, so lda wcy/clc/adc sp_r0 at @rows (8 cycles) goes; sp_ncol is stored from the width sbc (+3) instead of lda sp_c1/sec/sbc sp_c0/sta sp_ncol (-11). No path slower on either target

Assumption: Crosses into r008's lines 1769-1782. (1) ringaddr hoist: at 1772 A = wcy + sp_r0 (sta leaves A) and w16 = wcx + sp_c0 (1760-1766, and w16 is not written again until 1890); ringaddr/ringaddr7 touch only A, X, flags, sp (and the stack, balanced) -- Y (the record index, iny next) survives; nothing between 1772 and 1886 reads sp (mirdirty, modelb/src/banks.s:113-137, uses A/X/Y/mirdty/mirlo/mirhi only); ROMSEL is not changed between (the Master switched to the data bank at @vert). (2) sp_ncol: only written at 1925 and read at 2306; sp_c0/sp_c1 not written between 1776 and 1922. (3) the carry into the Model B mirror test stays 1 (sta sp_ncol, jsr come before 1776 or leave C alone). Plus every region candidate's assumptions. Gate-tested in a scratch copy (both builds + tools/gate.sh): every check passes except 'B title', which fails for ANY change to the Model B image -- a same-size, same-cycle sec->nop swap at engine.s:1789 fails it too: the title's display-RAM compare ($0300-$7FFF) sees a residue of the loaded code at $4636, not a behaviour difference.

```
        lda wcy
        clc
        adc sp_r0
        sta (rp),y
        iny
        lda sp_c1
        sec
        sbc sp_c0
        inca
        sta (rp),y
        iny
        lda sp_r1
        sbc sp_r0                   ; C still set by the width sbc above (sp_c1 >= sp_c0)
        inca
        ldx spclip
        beq :+                      ; may be visible next time and it has to be redrawn
        ora #$80
:       sta (rp),y
  .if MODELB
        lda mrow                    ; the mirror's row, relative to the window: if the
        sec                         ; sprite covers it, note the columns (see drawrect)
        sbc wcy
        cmp sp_r0
        bcc @nomir
        cmp sp_r1
        beq @mir
        bcs @nomir
@mir:   lda sp_c0
        ldx sp_c1
        jsr mirdirty
@nomir:
  .endif
        ; ---- column base pointer & step
        lda sp_flags
        bitimm 1
        beq @nomirror
        ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
        lda sp_flags                ; only the mirror arm clobbers A
@nomirror:
        ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
        bitimm 8                    ; bit3: copy blitter
        beq :+
        ldx #8
        bne :++
:       and #3                      ; A is still sp_flags: bit #imm does not alter A
        asl
        tax
:
  .if MODELB
        stx sp_disp                 ; the loop copy in the data's bank patches its own jump
  .else
        lda sprdisp_tab,x
        sta ds_dispatch+1
        lda sprdisp_tab+1,x
        sta ds_dispatch+2
  .endif
        ; sp_col = sp_ptr + sp_c * lines
        lda sp_ptr+1
        sta sp_col+1
        lda sp_ptr
        ldx sp_c
        beq @mdone
        clc
@mul:   adc sp_lines
        bcc :+
        inc sp_col+1
        clc
:       dex
        bne @mul
@mdone: sta sp_col
        stz mtab                    ; the MASKTAB pages are indexed by the mask byte
        lda sp_c
        and #3                      ; phase of the first column drawn, and its page:
        ora #>MASKTAB0              ; MASKTAB0 is 1K aligned, so phase = page & 3
        sta sp_mpg0
        ; mask column base = mask plane + (first image column / 4) * pixel rows
        lda sp_mbase
        sta sp_mrp
        lda sp_mbase+1
        sta sp_mrp+1
        lda sp_c
        lsr
        lsr
        beq @mgdone
        tax
@mgrp:  lda sp_mrp
        clc
        adc sp_mh
        sta sp_mrp
        bcc @mgnc
        inc sp_mrp+1
@mgnc:  dex
        bne @mgrp
@mgdone:
@rows:
        lda sp_r0
        sta sp_row
        ; screen base for (wcx + c0, wcy + r0): one ringaddr, then +80 chars per row
        ; (w16 = wcx + sp_c0 was already built when the record rect was written)
        lda wcy
        clc
        adc sp_r0
  .if MODELB
        jsr ringaddr7               ; this bank's own copy (no crossing)
  .else
        jsr ringaddr
  .endif
        lda sp
        sta sp_rb
        lda sp+1
        sta sp_rb+1
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        lda tmp                     ; tmp is still lstart, and sp_r0 = lstart >> 3,
        and #$F8                    ; so r0*8 is lstart & $F8: no reload and no shifts
        sec
        sbc sp_lb0
        sta w16
        lda #0
        sbc sp_lb0+1
        sta w16+1
        ldx #8
        lda sp_flags
        and #2
        bne :+
        lda w16+1
        cmp #$80
        ror w16+1
        ror w16
        ldx #4
:       stx sp_rinc
        lda sp_col
        clc
        adc w16
        sta sp_rp
        lda sp_col+1
        adc w16+1
        sta sp_rp+1
        lda w16+1                   ; the same offset in pixel rows (signed >> 1)
        cmp #$80
        ror w16+1
        ror w16
        lda sp_mrp
        clc
        adc w16
        sta sp_mrp
        lda sp_mrp+1
        adc w16+1
        sta sp_mrp+1
        lda sp_c1
        sec
        sbc sp_c0
        sta sp_ncol                 ; columns-1
```
->
```
        lda wcy
        clc
        adc sp_r0
        sta (rp),y
        ; screen base for (wcx + c0, wcy + r0) while A and w16 hold them (Y survives it):
        ; one ringaddr, then +80 chars per row
  .if MODELB
        jsr ringaddr7               ; this bank's own copy (no crossing)
  .else
        jsr ringaddr
  .endif
        sta sp_rb+1                 ; A = sp+1: ringaddr's last store
        lda sp
        sta sp_rb
        iny
        lda sp_c1
        sec
        sbc sp_c0
        sta sp_ncol                 ; columns-1, for the row loop
        inca
        sta (rp),y
        iny
        lda sp_r1
        sbc sp_r0                   ; C still set by the width sbc above (sp_c1 >= sp_c0)
        inca
        ldx spclip
        beq :+                      ; may be visible next time and it has to be redrawn
        ora #$80
:       sta (rp),y
  .if MODELB
        lda mrow                    ; the mirror's row, relative to the window: if the
        sbc wcy                     ; sprite covers it, note the columns (see drawrect); C = 1 from the height sbc
        cmp sp_r0
        bcc @nomir
        cmp sp_r1
        beq @mir
        bcs @nomir
@mir:   lda sp_c0
        ldx sp_c1
        jsr mirdirty
@nomir:
  .endif
        ; ---- column base pointer & step
  .if MODELB                        ; (no bit #imm here: lsr and and, not bitimm)
        lda sp_flags
        lsr                         ; C = bit0: mirrored
        bcc :+
                ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
                ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
:       lda sp_flags
        and #8                      ; bit3: copy blitter, and 8 is its index
        bne :+
        lda sp_flags
        and #3
        asl
:       tax
  .else
        lda sp_flags
        bitimm 1
        beq @nomirror
                ; mirror: image column = W-1-c (the column loop then steps backwards)
        clc
        lda sp_w
        sbc sp_c                    ; C=0 subtracts the extra 1: sp_w - sp_c - 1
        sta sp_c
        lda sp_flags                ; only the mirror arm clobbers A
@nomirror:
        ; ---- select the inner blitter once per sprite (patched jmp in the column loop)
        bitimm 8                    ; bit3: copy blitter
        beq :+
        ldx #8
        bne :++
:       and #3                      ; A is still sp_flags: bit #imm does not alter A
        asl
        tax
:
  .endif
  .if MODELB
        stx sp_disp                 ; the loop copy in the data's bank patches its own jump
  .else
        lda sprdisp_tab,x
        sta ds_dispatch+1
        lda sprdisp_tab+1,x
        sta ds_dispatch+2
  .endif
        lda sp_c
        and #3                      ; phase of the first column drawn, and its page:
        ora #>MASKTAB0              ; MASKTAB0 is 1K aligned, so phase = page & 3
        sta sp_mpg0
@rows:
        lda sp_r0
        sta sp_row
        ; source row pointer = column base + r0*8 - lb0 (>>1 for half res); +8 (+4) per row
        lda tmp                     ; tmp is still lstart, and sp_r0 = lstart >> 3,
        and #$F8                    ; so r0*8 is lstart & $F8: no reload and no shifts
        sec
        sbc sp_lb0
        sta w16
        lda #0
        sbc sp_lb0+1
        sta w16+1
        ldx #8
        lda sp_flags
        and #2
        bne :+
        lda w16+1
        asl                         ; C = the sign: an arithmetic >> 1
        ror w16+1
        ror w16
        ldx #4
:       stx sp_rinc
        ; sp_rp = sp_ptr + w16 + sp_c * lines (the column base needs no copy of its own)
        lda sp_ptr
        clc
        adc w16
        tay
        lda sp_ptr+1
        adc w16+1
        sta sp_rp+1
        tya
        ldx sp_c
        beq @mdone
        clc
@mul:   adc sp_lines
        bcc :+
        inc sp_rp+1
        clc
:       dex
        bne @mul
@mdone: sta sp_rp
        stx mtab                    ; X = 0 here: the MASKTAB pages are indexed by the mask byte
        lda w16+1                   ; the same offset in pixel rows (signed >> 1)
        asl                         ; C = the sign
        ror w16+1
        ror w16
        ; mask row pointer = mask plane + (first image column / 4) * pixel rows + that offset
        lda sp_c
        lsr
        lsr
        tay                         ; column groups to step over
        lda sp_mbase
        clc
        adc w16
        tax
        lda sp_mbase+1
        adc w16+1
        sta sp_mrp+1
        txa
        cpy #0
        beq @mgdone
        clc
@mgrp:  adc sp_mh
        bcc @mgnc
        inc sp_mrp+1
        clc
@mgnc:  dey
        bne @mgrp
@mgdone: sta sp_mrp
```

Combination: record-rect hoists (ringaddr + sp_rb copy after the wcy+r0 store; sta sp_ncol between sbc sp_c0 and inca, which leaves A and C for inca), the Model B sec removal, the Model B dispatch rewrite, and the MID block. Offered so all apply together; the individual candidates are the fallbacks.

Review: Original matches 1769-1925. ringaddr hoist: at the store A = wcy+sp_r0, w16 = wcx+sp_c0 (1761-1766, not rewritten until 1890); ringaddr/ringaddr7 use A, X, flags, sp, stack only (ringmod/ringmod7/ringup: no Y), so Y and rp survive for the following iny/sta (rp),y; X reloaded by ldx spclip; sec follows. The bank is already the data bank (switched at 1677, before 1769); nothing between reads/writes sp or sp_rb (mirdirty uses A/X/Y/mirdty/mirlo/mirhi/wcxm/curbuf; it clobbers Y but after the last record store, as before). sta sp_ncol between sbc and inca leaves A/C; sp_c0/sp_c1 not written after 1660; sp_ncol read only at 2306. Model B C=1 into sbc wcy from the height sbc (inca keeps it). Rest as candidates 1 and 10. Assembled: Master -33, Model B -45 (LGCCODE). Built both disc images in a scratch copy with this plus candidate 7 and ran tools/gate.sh: all checks pass except 'B title'; confirmed that check is layout-sensitive, not behavioural: an unreachable '.byte 0' after jmp callbank alone also fails it, while the unmodified rebuild passes.

## r010:0  src/engine.s:2068-2075  (window i1320)  M -5 B -7  [high]  review: accept

Cycles: Master: tmp2!=7 path bne@np(3)+jmp partial(3)=6 -> bne partial 3; tmp2=7,tmp=0 unchanged; tmp!=0 unchanged (jmp (et-2,x) 6 = jmp (et,x) 6). Model B: tmp2!=7 path 6->3; tmp!=0 path jmpx 26 -> 19 (pha 3 + pla 4 dropped); tmp=0 unchanged

Assumption: SPRFULL is instantiated only as sprFC,0,1 (engine.s:2422 (SPR4_COPY=0: not assembled), 2430, 2446), so copy=1/mirror=0 is the live form; partial is within branch reach from bne there (verified by assembling both targets: 74 bytes Master, 90 Model B); A is dead at l1..l7 (each begins ldy #k / lda (ptr),y) so jmpx's pha/pla is not needed; X still = tmp*2 at the target as before; the et table is only read by this dispatch (local label) and entry 0 was unreachable because tmp=0 takes beq l0; jv is the same zp vector the jmpx macro uses

```
        bne @np
        lda tmp
        beq l0                      ; the whole cell: the overwhelmingly common case, and
        asl                         ; it needs no table at all (was lda/asl/tax/jmpx, 21
        tax                         ; cycles of dispatch to reach the same place)
        jmpx et
@np:    jmp partial
et:     .word l0,l1,l2,l3,l4,l5,l6,l7
```
->
```
  .if copy
        bne partial                 ; in reach: the copy form's lines are short
  .else
        bne @np
  .endif
        lda tmp
        beq l0                      ; the whole cell: the overwhelmingly common case, and
        asl                         ; it needs no table at all (was lda/asl/tax/jmpx, 21
        tax                         ; cycles of dispatch to reach the same place)
  .if ::MODELB
        lda et-2,x                  ; jmpx without its pha/pla: A is dead at l1..l7
        sta jv
        lda et-1,x
        sta jv+1
        jmp (jv)
  .else
        jmpx et-2                   ; X = 2..14: l0 is reached by the beq, not the table
  .endif
  .if .not copy
@np:    jmp partial
  .endif
et:     .word l1,l2,l3,l4,l5,l6,l7
```

(1) copy form: bne partial replaces bne @np -> @np: jmp partial, same target, @np kept for the (uninstantiated) non-copy form. (2) Model B: jmpx expands to pha/lda et,x/sta jv/lda et+1,x/sta jv+1/pla/jmp (jv); dropping pha/pla only changes A at the target, which l1..l7 overwrite at once. (3) X = 2..14 on the table path (tmp=1..7, asl), so jmp (et-2,x) with et = l1..l7 reaches the same entries; the l0 word was never read. Measured: Master CODE -5, Model B SPR6CODE -7; disassembly of spr6 sprFC checked (lda $BF91,x with table at $BF93 = l1). If the jump-table trim is ruled out as a 'data table' change, apply the next candidate (same lines, table kept) instead: 3 Master / 5 Model B.

Review: Original matches engine.s:2068-2075. SPRFULL is only instantiated copy=1,mirror=0 (engine.s:2430,2446; 2422 under SPR4_COPY=0). tmp=sp_ra0 or 0 in 0..7 and tmp<=tmp2 (engine.s:1726-1741 bcc @out0), so the table path has X=2..14 and entry 0 was never read; jmp (et-2,x) reaches l1..l7. A is dead at l1..l7 (ldy #k/lda (ptr),y), X is still tmp*2 at the target, jv is the zero-page vector of the jmpx macro. bne partial assembles in range on both targets. Scratch build: Master CODE -5, Model B SPR6CODE -7. Cycles: tmp2!=7 path 6->3 both; Model B table path 26->19; nothing slower.

## r010:1  src/engine.s:2068-2075  (window i1320)  M -3 B -5  [high]  review: accept

Cycles: as the previous candidate: tmp2!=7 path 6->3 both targets; Model B table path -7 (no pha/pla); Master table path unchanged

Assumption: FALLBACK for the previous candidate (same lines; apply only if that one is rejected -- it will not match once that is applied). SPRFULL instantiated only as copy=1/mirror=0 (engine.s:2430, 2446); partial in bne reach in that form (assembled); A dead at l1..l7; X unchanged

```
        bne @np
        lda tmp
        beq l0                      ; the whole cell: the overwhelmingly common case, and
        asl                         ; it needs no table at all (was lda/asl/tax/jmpx, 21
        tax                         ; cycles of dispatch to reach the same place)
        jmpx et
@np:    jmp partial
et:     .word l0,l1,l2,l3,l4,l5,l6,l7
```
->
```
  .if copy
        bne partial                 ; in reach: the copy form's lines are short
  .else
        bne @np
  .endif
        lda tmp
        beq l0                      ; the whole cell: the overwhelmingly common case, and
        asl                         ; it needs no table at all (was lda/asl/tax/jmpx, 21
        tax                         ; cycles of dispatch to reach the same place)
  .if ::MODELB
        lda et,x                    ; jmpx without its pha/pla: A is dead at l1..l7
        sta jv
        lda et+1,x
        sta jv+1
        jmp (jv)
  .else
        jmpx et
  .endif
  .if .not copy
@np:    jmp partial
  .endif
et:     .word l0,l1,l2,l3,l4,l5,l6,l7
```

bne partial replaces bne->jmp partial for the copy form; Model B jmpx inlined without pha/pla since A is dead at every entry. Table unchanged. Measured: Master CODE -3, Model B SPR6CODE -5.

Review: Fallback for #0 (same lines, et table kept). Correct for the same reasons: bne partial is in reach, and the Model B jmpx without pha/pla is safe because A is dead at l1..l7. Scratch build: Master -3, Model B SPR6CODE -5. It goes stale once #0 is applied, and the applier records it as stale.

## r010:2  src/engine.s:2210-2215  (window i1402)  M -6 B -15  [high]  review: accept

Cycles: Master: tmp2=7 paths lda tmp 3 + tax 2 -> ldx tmp 3 (-2 cycles; beq/jmp (,x) same). Model B: -2 and jmpx 26 -> 19 (-7) on the table path; tmp=0 path -2; tmp2!=7 path unchanged

Assumption: A is dead at p0..p3 (MPAIR begins ldy #k/2 / lda (mptr),y), so A holding 7 (from lda tmp2) instead of tmp at p0..p3 is harmless; X = tmp at the target as before (ldx instead of lda/tax); ldx sets Z as lda did for the beq p0; tmp is even 0,2,4,6 (comment at 2210 and ds_rowloop: tmp=0 or sp_ra0) so the table path has X=2,4,6 and the p0 word was never read; et is a local label used only here; jv is the jmpx macro's own vector. SPRMSK instances: Master sprFN+sprFM, Model B spr4 sprFN+sprFM, spr6 sprFN (SPR6_MIRROR=0)

```
        lda tmp                     ; even: 0,2,4,6 -> entry p0..p3
        beq p0
        tax
        jmpx et
np:     jmp partial
et:     .word p0, p1, p2, p3
```
->
```
        ldx tmp                     ; even: 0,2,4,6 -> entry p0..p3
        beq p0
  .if ::MODELB
        lda et-2,x                  ; jmpx without its pha/pla: A is dead at p1..p3
        sta jv
        lda et-1,x
        sta jv+1
        jmp (jv)
  .else
        jmpx et-2                   ; X = 2,4,6: p0 is reached by the beq, not the table
  .endif
np:     jmp partial
et:     .word p1, p2, p3
```

lda tmp/beq/tax -> ldx tmp/beq: same Z, same X. Model B jmpx without pha/pla (A dead). jmp (et-2,x) with the table starting at p1 reaches p1..p3 for X=2,4,6. Measured: Master CODE -6, Model B SPR4CODE -10, SPR6CODE -5; Master disassembly checked (jmp ($170A,x), table at $170C = p1). If the jump-table trim is ruled out, apply the next candidate instead (2 Master / 9 Model B).

Review: Original matches engine.s:2210-2215. ldx tmp sets Z the same way lda did. At p0 A is now 7 rather than 0, but MPAIR opens with ldy/lda, so A is dead. X is now 0 at p0 where it used to keep a leftover value; nothing reads X before setting it (MPAIR, MLINE/CLINE, sprretPk/Mk, spnext/ringup, ds_rowloop's ldx, draw_sprites after jsr drawsprite), and X already comes back clobbered on the table and mirror paths. tmp is even 0..6, so X=2,4,6 reaches p1..p3 through et-2. Scratch build: Master CODE -6, Model B SPR4CODE -10 and SPR6CODE -5 (15). Cycles -2 on the tmp2=7 paths, and -7 more on the Model B table path.

## r010:3  src/engine.s:2210-2215  (window i1402)  M -2 B -9  [high]  review: accept

Cycles: Master -2 on tmp2=7 paths; Model B -2, and -7 more on the table path

Assumption: FALLBACK for the previous candidate (same lines; apply only if that is rejected). A dead at p0..p3; X = tmp as before; Z from ldx same as from lda

```
        lda tmp                     ; even: 0,2,4,6 -> entry p0..p3
        beq p0
        tax
        jmpx et
np:     jmp partial
et:     .word p0, p1, p2, p3
```
->
```
        ldx tmp                     ; even: 0,2,4,6 -> entry p0..p3
        beq p0
  .if ::MODELB
        lda et,x                    ; jmpx without its pha/pla: A is dead at p1..p3
        sta jv
        lda et+1,x
        sta jv+1
        jmp (jv)
  .else
        jmpx et
  .endif
np:     jmp partial
et:     .word p0, p1, p2, p3
```

ldx tmp replaces lda tmp/tax; Model B jmpx inlined without pha/pla; table unchanged. Measured: Master CODE -2, Model B SPR4CODE -6, SPR6CODE -3.

Review: Fallback for #2 (table kept). Correct for the same reasons. Scratch build: Master -2, Model B SPR4 -6 and SPR6 -3 (9). It goes stale once #2 is applied.

## r010:4  src/engine.s:2092-2142  (window i1360)  M -3 B -4  [high]  review: accept

Cycles: exit of the partial loop: beq pd (3) + jmp (3) = 6 -> beq to l7's jmp (3) + jmp (3) = 6, both targets. Loop back-edge: Master bra 3 -> bne 3 (taken); Model B jmp 3 -> bne 3. l7 path unchanged (label only)

Assumption: in partial, tmp <= tmp2 and tmp2 != 7 (partial is reached only by bne when tmp2 != 7; ds_rowloop sets tmp = 0 or sp_ra0 and tmp2 = 7 or sp_ra1, with ra0 <= ra1 when first row = last row -- otherwise the original loop would run 256+ lines), so at iny Y < tmp2 <= 6 and iny gives 1..6: bne always taken; pd is a .local label of the macro, referenced only by the beq pd lines inside it; the non-copy forms (not instantiated anywhere) were assembled in scratch to check the backward beq pd reach

```
        .if mirror
        jmp sprretM
        .else
        jmp sprretP
        .endif
partial:
        ldy tmp
.if copy
pl:     lda (ptr),y
  .if mirror
        tax
        lda SWAPTAB,x
  .endif
        sta (sp),y
.else
pl:     lda (ptr),y
        bmi po                      ; bit 7: both pixels opaque.  Tested before the
        beq ps                      ; transparent case for the same reason SPRLINE does
        ; it -- 45.7% opaque against 24.1% transparent, and
        ; both read this load's flags ($00 is never negative)
        cmp #$41
        beq pd
        tax
.if mirror
        lda MASKTAB+$80,x
        and (sp),y
        ora IDENT+$80,x
.else
        lda MASKTAB,x
        and (sp),y
        ora IDENT,x
.endif
        sta (sp),y
        bra ps
po:
.if mirror
        tax
        lda SWAPTAB,x
.endif
        sta (sp),y
.endif
ps:     cpy tmp2
        beq pd
        iny
        bra pl
pd:
        .if mirror
        jmp sprretM
        .else
        jmp sprretP
        .endif
```
->
```
pd:                                 ; l7's exit, and the partial loop's
        .if mirror
        jmp sprretM
        .else
        jmp sprretP
        .endif
partial:
        ldy tmp
.if copy
pl:     lda (ptr),y
  .if mirror
        tax
        lda SWAPTAB,x
  .endif
        sta (sp),y
.else
pl:     lda (ptr),y
        bmi po                      ; bit 7: both pixels opaque.  Tested before the
        beq ps                      ; transparent case for the same reason SPRLINE does
        ; it -- 45.7% opaque against 24.1% transparent, and
        ; both read this load's flags ($00 is never negative)
        cmp #$41
        beq pd
        tax
.if mirror
        lda MASKTAB+$80,x
        and (sp),y
        ora IDENT+$80,x
.else
        lda MASKTAB,x
        and (sp),y
        ora IDENT,x
.endif
        sta (sp),y
        bra ps
po:
.if mirror
        tax
        lda SWAPTAB,x
.endif
        sta (sp),y
.endif
ps:     cpy tmp2
        beq pd
        iny                         ; Y < tmp2 <= 6: never wraps to 0
        bne pl
```

The two jmp sprretP/sprretM after l7 and at pd are identical; pd moves to the first one (which precedes partial, so beq pd becomes a short backward branch) and the second is deleted. bra pl -> bne pl on the proven nonzero Y (Z from iny). Nothing followed the old pd jmp except .endmacro, and bne never falls through. Measured: Master CODE -3, Model B SPR6CODE -4; Model B disassembly checked (beq $BFD1 = the jmp sprretP after l7; bne $BFD6).

Review: Original matches engine.s:2092-2142. The two jmp sprretP/M are identical, pd is local to the macro, and the only references are the beq pd lines inside it. beq pd becomes a short backward branch, same cycles. bra pl -> bne pl: partial is reached only when tmp2!=7, and tmp<=tmp2<=7 is guaranteed by the clip's bcc @out0 (engine.s:1728), so Y<tmp2<=6 at the iny, iny gives 1..6, and bne is always taken. Nothing needs the fall-through. Scratch build: Master CODE -3, Model B SPR6CODE -4. Cycles equal on Master, and the Model B loop edge is jmp 3 -> bne 3.

## r010:5  src/engine.s:1948-1952  (window i1246)  M -0 B -2  [high]  review: accept

Cycles: Model B l0 of the copy blitter: ldy #0 (2) removed, -2 cycles; Master identical code

Assumption: on the Model B, ldaz ptr expands to ldy #0 / lda (ptr),y (cpu.inc MODELB ldaz), so Y = 0 at the store; the mirror copy form in between only does tax / lda SWAPTAB,x (Y untouched). On the Master ldaz is lda (ptr), Y unknown, so staz kept there. Note: these lines are in SPRLINE's text (r009's windows, i1276) but assembled via l0: SPRLINE 0 at i1354 in this region; may duplicate an r009 candidate

```
  .if k = 0
        staz sp                     ; box sprite: every byte opaque, plain copy
  .else
        sta (sp),y
  .endif
```
->
```
  .if k = 0 .and (.not ::MODELB)
        staz sp                     ; box sprite: every byte opaque, plain copy
  .else
        sta (sp),y                  ; (Model B: ldaz left Y = 0)
  .endif
```

Model B staz sp = ldy #0 / sta (sp),y with Y already 0 from ldaz's ldy #0: the ldy is redundant. Master unchanged. Measured: Model B SPR6CODE -2, Master 0; spr6 sprFC disassembly checked (ldy #0 / lda ($02),y / sta ($06),y).

Review: Original matches engine.s:1948-1952. It is only in the copy form of SPRLINE, which is reached only from SPRFULL copy. On the Model B, ldaz ptr expands to ldy #0 / lda (ptr),y, and the mirror form in between is only tax / lda SWAPTAB,x, so Y=0 at the store and the ldy #0 of staz is redundant. The Master keeps sta (sp). The .if precedence works out as (k=0) .and (.not MODELB). Scratch build: Master 0, Model B SPR6CODE -2, and -2 cycles on l0.

## r011:0  src/engine.s:2204-2262  (window i1426)  M -10 B -90  [high]  review: accept

Cycles: Full-cell entry (tmp2=7): unchanged up to `beq p0`. Master dispatch unchanged (tax+jmp(abs,x)=8). Model B dispatch tax+jmpx (2+26=28) -> tmp=2: cmp+bcc 5; tmp=4: 7 (no mirror) / 10 (mirror); tmp=6: 9 both. Partial entry: bne np(3)+jmp(3)=6 -> bne partial 3 (backward; page crossing ignored). Partial loop, per pair: `pl: lda sp_lim` removed (-3 both); masked-pair exit bra->bcc 3->3 (MB jmp 3->bcc 3); Model B pnext lda+inca+inca+sta 3+11+11+3=28 -> inc+inc+lda 5+5+3=13; Master pnext unchanged. No path is slower on either target.

Assumption: (a) Nothing falls into the moved partial block: in SPRITE_LOOPS (engine.s:2407-2411) `SPRMSK sprFN` follows `ds_done: rts` and `SPRMSK sprFM` follows sprFN's final `jmp sprretPk`. (b) name (sprFN/sprFM) is reached only by `jmp` through the patched ds_dispatch/sprdisp_tab, so its address can move. (c) X after the dispatch: the Model B chain no longer sets X=tmp, and nothing reads it. MPAIR/MLINE/CLINE use X only after their own tax (mirror). sprretMk/Pk/sprnext/ds_rowdone never read X. ds_rowloop reloads it (ldx #7). After ds_done's rts the path is callbank->pagelogic->drawsprite->draw_sprites @next/@l, which reloads X (engine.s:1410), and farjsr documents X as destroyed. (d) A after the dispatch is dead: every MPAIR starts ldy/lda (the jmpx pha/pla preserved an A that nothing reads). (e) sp_lim, sp_msk, tmp and tmp2 keep their meaning. mtmp (written by the old inca) is read only inside cpu.inc's expansions. (f) Branch ranges were checked by assembling both targets: the backward bne partial and bcc p1/beq p2 are all in range (the Model B mirror copy uses the p2j trampoline because p2 is more than 127 bytes away).

```
.macro SPRMSK name, mirror
        .local partial, np, et, p0, p1, p2, p3, pl, pop, pnext
name:
        lda tmp2
        cmp #7
        bne np
        lda tmp                     ; even: 0,2,4,6 -> entry p0..p3
        beq p0
        tax
        jmpx et
np:     jmp partial
et:     .word p0, p1, p2, p3
p0:     MPAIR 0, mirror
p1:     MPAIR 2, mirror
p2:     MPAIR 4, mirror
p3:     MPAIR 6, mirror
  .if mirror
        jmp sprretMk
  .else
        jmp sprretPk
  .endif
partial:                            ; lines tmp..tmp2: tmp even, tmp2 odd
        lda tmp
        sta sp_lim
pl:     lda sp_lim
        lsr
        tay
        lda (mptr),y
        tay
        lda (mtab),y
        beq pop
        cmp #$FF
        beq pnext
  .if mirror
        tax
        lda SWAPTAB,x
  .endif
        sta sp_msk
        ldy sp_lim
        MLINE mirror
        iny
        MLINE mirror
        bra pnext
pop:    ldy sp_lim
        CLINE mirror
        iny
        CLINE mirror
pnext:  lda sp_lim
        inca
        inca
        sta sp_lim
        cmp tmp2
        bcc pl
  .if mirror
        jmp sprretMk
  .else
        jmp sprretPk
  .endif
.endmacro
```
->
```
.macro SPRMSK name, mirror
        .local partial, et, p0, p1, p2, p3, p2j, pl, pop, pnext
partial:                            ; lines tmp..tmp2: tmp even, tmp2 odd (above name, so
        lda tmp                     ; name's bne reaches it)
        sta sp_lim
pl:     lsr                         ; A = sp_lim on both ways in
        tay
        lda (mptr),y
        tay
        lda (mtab),y
        beq pop
        cmp #$FF
        beq pnext
  .if mirror
        tax
        lda SWAPTAB,x
  .endif
        sta sp_msk
        ldy sp_lim
        MLINE mirror
        iny
        MLINE mirror
        bcc pnext                   ; C = 0: the cmp #$FF above was not equal
pop:    ldy sp_lim
        CLINE mirror
        iny
        CLINE mirror
pnext:
  .if ::MODELB
        inc sp_lim
        inc sp_lim
        lda sp_lim
  .else
        lda sp_lim
        inca
        inca
        sta sp_lim
  .endif
        cmp tmp2
        bcc pl
  .if mirror
        jmp sprretMk
  .else
        jmp sprretPk
  .endif
name:
        lda tmp2
        cmp #7
        bne partial
        lda tmp                     ; even: 0,2,4,6 -> entry p0..p3
        beq p0
  .if ::MODELB
        cmp #4
        bcc p1
    .if mirror
        beq p2j
        jmp p3
p2j:    jmp p2
    .else
        beq p2
        jmp p3
    .endif
  .else
        tax
        jmpx et
et:     .word p0, p1, p2, p3
  .endif
p0:     MPAIR 0, mirror
p1:     MPAIR 2, mirror
p2:     MPAIR 4, mirror
p3:     MPAIR 6, mirror
  .if mirror
        jmp sprretMk
  .else
        jmp sprretPk
  .endif
.endmacro
```

Four changes in one macro. The whole macro is replaced because the partial block moves. (1) `partial` moves above `name:`, so `bne np`/`np: jmp partial` becomes `bne partial` (-3 per instance, both targets). (2) `pl: lda sp_lim` is deleted. On first entry A = tmp, which was just stored to sp_lim. On loop-back A = sp_lim (Master: sta sp_lim/cmp/bcc; Model B: lda sp_lim/cmp/bcc), and cmp does not change A (-2, both targets). (3) `bra pnext` -> `bcc pnext`: this is reached only after `cmp #$FF` was not-equal, so A<$FF gives C=0. tax/lda SWAPTAB/sta/ldy/MLINE(lda,tax,lda,sta,and,ora,sta)/iny never touch C (MB -1). (4) Model B only: pnext's `lda sp_lim/inca/inca/sta sp_lim` -> `inc sp_lim/inc sp_lim/lda sp_lim`. A and sp_lim are the same, and the carry is dead because `cmp tmp2` follows (-10). Also the jmpx dispatch becomes a compare chain on A=tmp in {2,4,6}: after cmp #4, C=0 means 2 -> p1; Z means 4 -> p2; otherwise 6 -> p3. The et table is assembled only on the Master, which keeps jmp (et,x) (-15 no-mirror, -12 mirror). Assembled and measured: Master CODE -10 (sprFN, sprFM), Model B SPR4CODE -59, SPR6CODE -31. Gate run in a scratch copy with all r011 candidates applied: M state L0/1/5, M dmg, M pix L3/6, M title, B lockstep L0/5/12 and B pix L0/6 all pass. The B title check differs on 7/30 samples, but it does so for ANY change to bank code, including a lone 1-byte clc removal: it samples main RAM during the load, where BANKS is staged.

Review: The original text matches lines 2204-2262. Checked: the moved partial block sits after ds_done's rts and after sprFN's final jmp, so nothing falls into it. sprFN and sprFM are reached only through the patched ds_dispatch and sprdisp_tab. A = sp_lim on both ways into pl (sta sp_lim before; the cmp keeps A on the loop-back). The MLINE/SWAPTAB path never touches C, so bcc pnext is always taken after cmp #$FF was not equal. tmp is in {2,4,6} on the chain path (an odd value would already crash the jmpx). X after the dispatch is never read: drawsprite's @next reloads it. Assembled: Master CODE -10, Model B SPR4 -59, SPR6 -31. The gate with all r011 candidates applied passes every check except B title. That check also fails for the trivially correct 1-byte candidate 5 on its own (btitlediff flags any change to bank code), so it is not evidence against this candidate.

## r011:1  src/engine.s:2197-2197  (window i1408)  M -0 B -12  [high]  review: accept

Cycles: Master bra 3 -> bcc taken 3; Model B jmp 3 -> bcc taken 3. Other paths unchanged.

Assumption: Nothing outside the macro: C comes from the `cmp #$FF` at the top of this MPAIR expansion.

```
        bra done
```
->
```
        bcc done                    ; C = 0: the cmp #$FF above was not equal
```

This is reached only on the masked path: lda (mtab),y was neither 0 (beq opq) nor $FF (beq done), so `cmp #$FF` left C=0 (A<$FF). Between that cmp and the branch come [tax, lda SWAPTAB,x], sta sp_msk, ldy #k, MLINE (lda/[tax/lda/sta]/lda/and/ora/sta), iny and MLINE, and none of them changes C. So bcc always branches to done. There are 4 MPAIRs per SPRMSK and 3 SPRMSK instances on the Model B: -12 bytes. The Master's bra is already 2 bytes. Verified by assembly (SPR4 -8, SPR6 -4).

Review: The masked path is reached only after cmp #$FF was not equal, so C=0. tax, lda SWAPTAB, sta, ldy, MLINE and iny all leave C unchanged. Measured: SPR4 -8, SPR6 -4, Master 0.

## r011:2  src/engine.s:2181-2182  (window i1377)  M -4 B -0  [high]  review: accept

Cycles: Master, MPAIR 0 only: ldy #0 + lda (zp),y = 2+5 -> lda (zp) = 5. Model B: ldaz expands to ldy #0/lda (mptr),y, identical bytes and cycles. k>0 unchanged.

Assumption: Y is dead after this load: the next instruction is `tay` (engine.s:2183), which overwrites Y. ldaz's contract in cpu.inc is 'Y destroyed', which holds here.

```
        ldy #k/2
        lda (mptr),y
```
->
```
  .if k = 0
        ldaz mptr                   ; (Master: lda (mptr); the tay reloads Y)
  .else
        ldy #k/2
        lda (mptr),y
  .endif
```

For k=0 the original is ldy #0 / lda (mptr),y. On the 65C02, lda (mptr) loads the same byte and sets the same N,Z. Y is left unchanged instead of 0, but tay reloads it immediately. On the Model B, ldaz expands to exactly the original two instructions. The Master saves 2 bytes in MPAIR 0 of both sprFN and sprFM = 4 (verified: CODE -4). NOTE: the edit spans i1407-i1408, so the lowest window containing it (i1377) is in the neighbouring region. It is reported here because it was found here.

Review: For k=0 on the Master, lda (mptr) loads the same byte and sets the same flags, and the following tay overwrites Y. On the Model B, ldaz expands to the identical ldy #0 / lda (mptr),y. Measured: Master CODE -4, Model B 0.

## r011:3  src/engine.s:2295-2301  (window i1491)  M -0 B -4  [high]  review: accept

Cycles: Model B: not the first row: lda #0/sta tmp(5)+bne taken(3)=8 -> ldx #0(2)+bne taken(3)+stx(3)=8; first row: 5+2+3+3=13 -> 2+2+3+3=10. Master: identical code.

Assumption: X is dead at ds_rowloop: every path overwrites it with `ldx #7` / `ldx sp_ra1` before use (engine.s:2301-2304). tmp is zero page (stx zp). The flags at `cmp sp_r1` come from that cmp only.

```
        stz tmp                     ; ra0' = 0 unless this is the first row
        lda sp_row
        cmp sp_r0
        bne :+
        ldx sp_ra0
        stx tmp
:       ldx #7
```
->
```
  .if ::MODELB
        ldx #0                      ; ra0' = 0 unless this is the first row (X is
        lda sp_row                  ; reloaded below)
        cmp sp_r0
        bne :+
        ldx sp_ra0
:       stx tmp
        ldx #7
  .else
        stz tmp                     ; ra0' = 0 unless this is the first row
        lda sp_row
        cmp sp_r0
        bne :+
        ldx sp_ra0
        stx tmp
:       ldx #7
  .endif
```

On the Model B, stz tmp = lda #0/sta tmp (4 bytes) plus stx tmp (2) becomes ldx #0 (2) plus one stx tmp (2) on the join. tmp = sp_ra0 on the first row and 0 otherwise, as before, and A is still sp_row for both compares. The Master keeps its code because its stz (2 bytes, 3 cycles) is cheaper there. -2 per SPRITE_LOOPS instance; there are 2 instances on the Model B.

Review: X is dead at ds_rowloop: ldx #7 or ldx sp_ra1 follows, and ds_entry's ldx sp_disp is used before this point. The anonymous-label count is unchanged (2 in each branch of the .if). The first bne :+ now lands on stx tmp; the second still lands on stx tmp2. tmp = sp_ra0 or 0, as before. Measured: SPR4 -2, SPR6 -2.

## r011:4  src/engine.s:2323-2334  (window i1503)  M -6 B -6  [high]  review: accept

Cycles: No-wrap: dec5+lda3+and2+cmp2+bne3=15 -> dec5+lda3+cmp2+bne3=13. Wrap: ...+lda3+clc2+adc2+sta3+lda3+sec2+sbc3 -> ...+lda#2+sta3+lda3+sbc3 (-9). Same on both targets.

Assumption: mtab+1 is always one of the four MASKTAB pages >MASKTAB0..>MASKTAB3. It is set only from sp_mpg0 = (sp_c&3)|>MASKTAB0 (engine.s:1844-1847, via ds_rowloop 2292-2293) and changed only here and in sprretPk, both of which wrap within the group. MASKTAB0 is 1K aligned (.assert at engine.s:308; Model B defs.inc $8400). MASKTAB3 = MASKTAB0+$300 is defined on both targets.

```
        dec mtab+1                  ; so the phase (= page & 3) does too; below phase 0
        lda mtab+1                  ; it is the previous group's phase 3
        and #3
        cmp #3
        bne @mk
        lda mtab+1
        clc
        adc #4
        sta mtab+1
        lda mptr
        sec
        sbc sp_mh
```
->
```
        dec mtab+1                  ; so the phase (= page & 3) does too; below phase 0
        lda mtab+1                  ; it is the previous group's phase 3
        cmp #>(MASKTAB0-$100)       ; (mtab+1 stays in MASKTAB0..3: only below 0 wraps)
        bne @mk
        lda #>MASKTAB3
        sta mtab+1
        lda mptr                    ; C = 1 from the cmp (equal)
        sbc sp_mh
```

After the dec, mtab+1 is in >MASKTAB0-1 .. >MASKTAB0+2, and because of the 1K alignment (page&3)=3 holds only for >MASKTAB0-1. So `and #3/cmp #3` is equivalent to `cmp #>(MASKTAB0-$100)`. On wrap the old code stored (>MASKTAB0-1)+4 = >MASKTAB3; that is now loaded as a constant. The following `sec` is dropped: the equal cmp left C=1, and lda #/sta/lda do not touch C, so sbc sp_mh subtracts exactly as before. The flags after sta mtab+1 are not used (lda mptr follows). This code exists only in withmirror copies: Master 1, Model B spr4 only. -6 bytes each (verified).

Review: mtab+1 (= tmp4) is set only from sp_mpg0 = (sp_c&3)|>MASKTAB0 (engine.s:1844-1847, its only writer). Only sprretMk and sprretPk change it, and both wrap within the group. MASKTAB0 is 1K aligned on both targets (the .assert at :308; Model B $8400), so after the dec only >MASKTAB0-1 has page&3 = 3. The equal cmp leaves C=1 for the sbc. Measured: Master -6, SPR4 -6.

## r011:5  src/engine.s:2345-2346  (window i1503)  M -0 B -1  [high]  review: accept

Cycles: Master bra 3 -> bcc 3; Model B jmp 3 -> bcc 3.

Assumption: Nothing outside the window.

```
        dec ptr+1
        bra sprnext
```
->
```
        dec ptr+1
        bcc sprnext                 ; C = 0: the bcs was not taken
```

This is reached only when `bcs sprnext` fell through, which means C=0 after sbc sp_lines. dec ptr+1 does not change C, so bcc always branches. sprnext is 20-40 bytes ahead, within the reach the bcs already uses. -1 on the Model B (spr4, the only withmirror copy).

Review: This is reached only when bcs sprnext fell through (C=0), and dec ptr+1 does not change C. Measured: SPR4 -1.

## r011:6  src/engine.s:2352-2356  (window i1525)  M -3 B -6  [high]  review: accept

Cycles: No-wrap path unchanged. Wrap: lda3+sec2+sbc2+sta3=10 -> lda#2+sta3=5. Both targets.

Assumption: mtab+1 is always one of >MASKTAB0..>MASKTAB3 (see the sprretMk candidate: it is set only from sp_mpg0, engine.s:1844-1847). So after the inc, the wrap case is exactly >MASKTAB0+4.

```
        bne @pk
        lda mtab+1
        sec
        sbc #4
        sta mtab+1
```
->
```
        bne @pk
        lda #>MASKTAB0              ; past MASKTAB3: back to MASKTAB0
        sta mtab+1
```

The wrap happens only when the inc took mtab+1 from >MASKTAB3 to >MASKTAB0+4, the only in-range value with page&3=0 after an inc. The old code stored that value minus 4, which is >MASKTAB0. The carry left by sec/sbc was not used (`clc` precedes the next adc), and neither were N/Z (lda mptr follows). Master 1 copy, Model B spr4+spr6: -3 each (verified).

Review: Given the invariant, the wrap happens only from >MASKTAB3 to >MASKTAB0+4, and the new code stores >MASKTAB0 as the old one did. C, N and Z afterwards are dead (lda mptr / clc / adc follow). Measured: Master -3, SPR4 -3, SPR6 -3.

## r011:7  src/engine.s:2373-2379  (window i1546)  M -0 B -6  [high]  review: accept

Cycles: Model B: next column bmi(2)+jmp(3)=5 -> bpl taken 3; last column bmi taken 3 -> bpl not taken 2. Master identical.

Assumption: Branch range. spr6 (withmirror=0): ds_colloop is 90 bytes back even in the current build ($BDA9 vs bpl at $BE01). spr4 (withmirror=1): it is 132 bytes back today, so at least 4 bytes must be saved between ds_colloop and here. The sprretMk candidate (-6) provides that; with all r011 candidates applied it is 118 back. APPLY AFTER the sprretMk candidate. Applied too early, it fails with a range error at assembly rather than a silent fault.

```
        dec sp_cnt
  .if ::MODELB
        bmi ds_rowdone              ; (the 6502 spellings put ds_colloop out of reach)
        jmp ds_colloop
  .else
        bpl ds_colloop
  .endif
```
->
```
        dec sp_cnt
        bpl ds_colloop
```

Same test (N from dec sp_cnt) and same targets: bpl ds_colloop is the Master's spelling, and it is now within reach on the 6502 build too. The ds_rowdone label stays (fall-through). -3 per copy, 2 copies on the Model B.

Review: The semantics are identical (N comes from dec sp_cnt). ORDER DEPENDENT: applied alone it fails to assemble (Range error -132 in spr4). It builds once candidate 4 is applied (SPR4 -3, SPR6 -3), or once 5 and 6 are both applied. Applied too early it fails with an assembly error, never silently. It must be applied after candidate 4, or after both 5 and 6.

## r011:8  src/engine.s:2384-2387  (window i1550)  M -1 B -2  [high]  review: accept

Cycles: -2 cycles per row, both targets.

Assumption: sp_row runs from sp_r0 up to sp_r1 with sp_r0 <= sp_r1, all small unsigned values. sp_r0 = lstart>>3 and sp_r1 = lend>>3, with lstart <= lend < BUFROWS*8 (engine.s:1717-1744: `cmp tmp / bcc @out0` rejects lend<lstart). sp_row = sp_r0 at @rows (1868-1869), is only incremented here, and stops at equality. sp_r1 is written only at 1736.

```
        inc sp_row
        lda sp_rp
        clc
        adc sp_rinc
```
->
```
        inc sp_row
        lda sp_rp                   ; C = 0: sp_row < sp_r1 (the rows count up to it)
        adc sp_rinc
```

`cmp sp_r1` / `beq ds_done` fell through, so sp_row != sp_r1, hence sp_row < sp_r1 unsigned and the cmp left C=0. inc sp_row and lda sp_rp do not touch C, so the clc did nothing. Master 1, Model B 2 copies (verified).

Review: sp_r0 <= sp_r1, because lend >= lstart is enforced by cmp/bcc @out0 at engine.s:1728-1729. sp_row starts at sp_r0 (1868-1869) and is only incremented here, until it equals sp_r1. So when cmp sp_r1 is not equal, C=0, and inc and lda keep C. Measured: Master -1, SPR4 -1, SPR6 -1.

## r011:9  src/engine.s:527-527  (window i1545)  M -0 B -28  [high]  review: accept

Cycles: Model B: inca = sta mtmp(3)+inc mtmp(5)+lda mtmp(3)=11 -> adc #0 = 2, per page-crossing char. Master identical.

Assumption: Macro spnext (engine.s:520-531) is used at engine.s:952, 1129, 2372 (twice on the Model B: spr4 and spr6), 2607, 2657 and menu.s:132. Nothing reads V after spnext: the only bvs/bvc in engine.s/menu.s is the IRQ's bvs at 3700, after its own flag-setting, and there are none in modelb/src near these sites. V was already clobbered by spnext's adc #8. mtmp is read only inside cpu.inc expansions, and each of them writes it first.

```
        inca
```
->
```
  .if ::MODELB
        adc #0                      ; C = 1 (the bcc above): + 1, and ringup's cmp sets C
  .else
        inca
  .endif
```

The line is reached only when `bcc :++` fell through, so C=1 and adc #0 gives A+1 exactly as inca does. inca's guarantee that the carry is preserved is not needed here: ringup's first instruction (Model B `cmp ringehi`, Master `cmp #>RINGEND`) overwrites C, N and Z. The only other difference is V, which adc sets; V is dead (see assumption). The macro's anonymous labels are untouched. -4 bytes per expansion on the Model B: TILCODE -16, MNUCODE -4, SPR4CODE -4, SPR6CODE -4 (verified). NOTE: the edited line is in the macro definition, outside this region; the window given is this region's expansion site.

Review: C=1 on the fall-through of bcc :++, so adc #0 adds 1. On the Model B, ringup starts with cmp ringehi, which overwrites C, N and Z. V now differs (A=$7F is possible), but nothing reads V after any spnext site: the only bvc/bvs are in the IRQ and ldstop5, each after its own bit, and in the logic.s macros, each after its own sbc. There is no sed anywhere. Nothing reads mtmp afterwards. Measured: TILCODE -16, SPR4 -4, SPR6 -4, MNUCODE -4.

## r012:0  src/engine.s:2373-2379  (window i1545)  M -0 B -3  [high]  review: accept

Cycles: Model B (where the branch reaches): column continue 5 -> 3 (bmi not taken 2 + jmp 3 -> bpl taken 3), row end 3 -> 2. Master: identical code (bpl already)

Assumption: The only dependency is ca65 evaluating `* + 2 - ds_colloop` as a constant (both labels are in the same segment). Verified: it assembles, picks bpl in spr6 today, and picks it in spr4 too once r001's spnext inca->adc #0 candidate lands (spr4's distance is then exactly -128). If the distance ever grows, the code falls back to the old bmi/jmp by itself and never gives an out-of-range error.

```
        dec sp_cnt
  .if ::MODELB
        bmi ds_rowdone              ; (the 6502 spellings put ds_colloop out of reach)
        jmp ds_colloop
  .else
        bpl ds_colloop
  .endif
```
->
```
        dec sp_cnt
  .if ::MODELB .and (* + 2 - ds_colloop > 128)
        bmi ds_rowdone              ; (the 6502 spellings can put ds_colloop out of reach)
        jmp ds_colloop
  .else
        bpl ds_colloop
  .endif
```

bmi ds_rowdone / jmp ds_colloop, with ds_rowdone as the next instruction, is exactly bpl ds_colloop: the N flag from dec sp_cnt decides both the same way. The .if only picks the spelling the branch range allows; no flags or registers change. The Master path (::MODELB = 0) is the existing bpl.

Review: Original matches 2373-2379. bmi ds_rowdone/jmp ds_colloop with ds_rowdone next is bpl ds_colloop on the N flag of dec sp_cnt; the .if only picks a spelling that is in range (backward reference, constant in-segment difference). Built: spr6 picks bpl (SPR6CODE -3), spr4 keeps bmi/jmp today; Master unchanged. Column continue 5->3, row end 3->2 cycles. Gate (all r012 applied): Master state/pix/dmg/title and Model B lockstep/pix pass; B title differs for every r012 candidate individually, including a lone clc removal, so it tracks disc layout/timing, not correctness.

## r012:1  src/engine.s:2385-2387  (window i1550)  M -1 B -2  [high]  review: accept

Cycles: every row but the last: 2 cycles faster (no clc), both targets; last row (beq ds_done) unchanged. Model B: the macro is expanded twice (spr4, spr6), 1 byte each

Assumption: sp_row <= sp_r1 at every ds_rowdone. sp_row is set to sp_r0 (engine.s:1868-1869) and only incremented here, after the beq ds_done test. sp_r0 = lstart>>3 and sp_r1 = lend>>3, with lend >= lstart (engine.s:1727-1744, `cmp tmp / bcc @out0`). grep: sp_row/sp_r0/sp_r1 have no other writers or aliases in src/ or modelb/src/. The row loop's only entries are the prologue fall-through (Master) and ds_entry via callbank (Model B), both after that setup. The blitters in between (SPRFULL/SPRMSK) touch tmp/tmp2/sp_lim, not these.

```
        lda sp_rp
        clc
        adc sp_rinc
```
->
```
        lda sp_rp                   ; C = 0: sp_row < sp_r1 (the rows run r0..r1)
        adc sp_rinc
```

lda sp_row / cmp sp_r1 / beq ds_done: falling through means sp_row != sp_r1. With sp_row <= sp_r1 that means sp_row < sp_r1, so the cmp leaves C = 0. inc sp_row and lda sp_rp leave C alone, so the clc before adc sp_rinc does nothing. Same A, same flags from the adc.

Review: Original matches 2385-2387. sp_r0 = lstart>>3 <= sp_r1 = lend>>3 (cmp tmp / bcc @out0 at 1729), sp_row starts at sp_r0 (1868) and is only incremented after the beq ds_done test; sp_r0/sp_r1/sp_row are distinct .res bytes with no aliases or other writers (grep). ds_rowdone is only reached from the column loop. So cmp sp_r1 falls through with sp_row < sp_r1 and C=0; inc/lda keep C. Measured Master CODE -1, Model B SPR4CODE -1 and SPR6CODE -1.

## r012:2  src/engine.s:2467-2486  (window i1591)  M -2 B -7  [high]  review: accept

Cycles: (a) @all split: PART_F mismatch path 3 cycles faster, PART_CY mismatch path 11 faster, both targets; (b) inca -> adc #0: Model B 11 -> 2, Master identical; (c) bra @go -> bpl @go: 3 -> 3 cycles on both targets (Model B: 3-byte jmp -> 2-byte bpl)

Assumption: (a) At the first `bne @all` (2464), A = wfine (lda wfine at 2459; ldx curbuf and cmp leave A). That bne is unchanged and now lands on `sta PART_F,x` with A = wfine. At the second bne (2467), A = wcy and PART_F,x already equals wfine (the first cmp was equal). (b) The anonymous `:` is reached only by the taken `bcs :+` at 2477, so C = 1. The carry is dead afterwards (sta cnt / lda / branch / sta tmp4 / clc). (c) PART_LO,x <= 79 on this path: sbc PART_LO,x did not borrow from min(PART_HI,79), so lda PART_LO,x leaves N = 0.

```
        bne @all
        ; same source row and lines as last time: only the columns drawn since.  A
        ; horizontal scroll does not matter: the composed row is a ring row at a fixed
        ; offset from its source, so it moves with the ring and stays valid
        lda PART_HI,x
        cmp #ROWCHARS
        bcc :+
        lda #(ROWCHARS-1)
:       sec
        sbc PART_LO,x
        bcs :+
        rts                         ; nothing drawn in the top row
:       inca
        sta cnt
        lda PART_LO,x
        bra @go
@all:   lda wfine
        sta PART_F,x
        lda wcy
        sta PART_CY,x
```
->
```
        bne @allcy                  ; A = wcy, and PART_F is already wfine
        ; same source row and lines as last time: only the columns drawn since.  A
        ; horizontal scroll does not matter: the composed row is a ring row at a fixed
        ; offset from its source, so it moves with the ring and stays valid
        lda PART_HI,x
        cmp #ROWCHARS
        bcc :+
        lda #(ROWCHARS-1)
:       sec
        sbc PART_LO,x
        bcs :+
        rts                         ; nothing drawn in the top row
:
  .if MODELB
        adc #0                      ; C = 1 from the bcs: +1
  .else
        inca
  .endif
        sta cnt
        lda PART_LO,x
        bpl @go                     ; LO <= 79 (it is <= min(HI, 79)): N = 0
@all:   sta PART_F,x                ; A = wfine (the first bne)
        lda wcy
@allcy: sta PART_CY,x
```

Three independent micro-edits in one span, merged so they do not overlap. (a) The second mismatch branch goes to a new cheap label @allcy on `sta PART_CY,x`: A = wcy there already and PART_F needs no rewrite. @all drops `lda wfine` because its only remaining entry arrives with A = wfine. PART_F,x and PART_CY,x end with the same values on every path. (b) Model B: adc #0 with C = 1 is A+1 (A <= 79, no wrap), 2 bytes instead of inca's 6. The `:` stays on its own line, so the anonymous-label count is unchanged. (c) bra (Model B jmp) -> bpl, always taken because N = 0. Assembled together with every other r012 candidate: this one is Master CODE -2, Model B TILCODE -7.

Review: Original matches 2467-2486. (a) First bne @all has A=wfine (lda wfine / ldx / cmp); second has A=wcy with PART_F,x already equal to wfine; @all has no other references. (b) The ':' is reached only by bcs taken, so C=1 and adc #0 = +1 (A<=79); C is dead (sta/lda/branch/sta tmp4/clc); the ':' label count is unchanged. (c) LO <= min(HI,79) because the sbc did not borrow, so N=0 after lda PART_LO,x and bpl is always taken. Measured Master CODE -2, Model B TILCODE -7. No path is slower.

## r012:3  src/engine.s:2499-2499  (window i1605)  M -0 B -2  [high]  review: accept

Cycles: Model B 14 -> 7 (pha/lda/sta/pla -> lda/sta); Master identical (stz)

Assumption: A is dead after the store: the next instruction is lda barq (Model B, 2501) or lda wcy (Master, 2512). Both spellings preserve C, and the next candidate (2503) needs C = 0 to survive here: it does.

```
        stza PART_HI, x
```
->
```
        stz PART_HI, x              ; A dead: reloaded below
```

stza keeps A across the zero store, but A ($FF) is never read afterwards, so the A-dead stz spelling (cpu.inc contract) is valid. N,Z differ (0 vs $FF from pla), but lda barq / lda wcy overwrite them.

Review: Original matches 2499. A is dead: the next instruction is lda barq (Model B) or lda wcy (Master). Neither stz spelling touches C, so candidate 4's C=0 survives. Measured Model B TILCODE -2, 14->7 cycles; Master unchanged.

## r012:4  src/engine.s:2503-2505  (window i1614)  M -0 B -1  [high]  review: accept

Cycles: Model B 2 cycles faster on the barq = 0 path; Master: code not assembled

Assumption: C = 0 after `lda wcx+1 / adc #0` (2494-2495): wcx+1 <= $7F because wcx = wx>>1 (engine.s:3094-3098, the only writer). Nothing in between touches C: sta w16+1, lda #$FF, sta PART_LO,x, stza/stz (pha/lda/sta/pla or lda/sta), lda barq, bne, lda tmp4.

```
        lda tmp4
        clc
        adc cnt
```
->
```
        lda tmp4                    ; C = 0 from the w16+1 adc (wcx < $8000)
        adc cnt
```

adc cnt with C = 0 gives the same result with or without the clc: X = tmp4 + cnt - 1 as before.

Review: Original matches 2503-2505 (Model B only). wcx+1 = wx+1>>1 <= $7F (engine.s:3096-3098; the other writers store zero), so lda wcx+1 / adc #0 leaves C=0. sta, lda #$FF, sta, stza/stz, lda barq and bne keep C. Measured TILCODE -1.

## r012:5  src/engine.s:2523-2574  (window i1614)  M -21 B -30  [high]  review: accept

Cycles: Master: no-fold 82 -> 45, fold 83 -> 46. Model B: >= 89 on every path (three fold branches) -> 58 (d >= 0) / 71 (d < 0). Faster on every path; copy_partial is not timing critical

Assumption: (1) C = 0 at lda tmp4. The preceding `lda sp / clc / adc wfine / sta sp` cannot carry: sp comes from ringaddr as a char address, whose low byte is 8-aligned (RINGLO/RING_A/RING_B are xx80, and ROWBYTES and the ringup folds are multiples of 8). wfine is 2/4/6 (see the @ftab dispatch and the bne at 2460). (2) tmp4 <= 79: 0 on the @all path, PART_LO,x on the other, where LO <= min(HI,79) because the sbc at 2476 did not borrow. (3) X is dead: ldx wfine at 2579 reloads it before any use. (4) ptr+1 is not read in between: the old intermediate stores only served the reload. Verified exhaustively in Python against the original fold, for every ringS in [0,RINGCHARS) and tmp4 in [0,79]: Master (2560 chars, base $3000) and Model B (1840 chars, base xx80, several ringbhi). Lines 2517-2520 (lda sp / clc / adc wfine / sta sp) must stay an adc: an `ora wfine` spelling there would leave C unknown here.

```
        lda tmp4
        clc
        adc ringS
        sta ptr
        lda ringS+1
        adc #0
        sta ptr+1
        lda ptr
        clc
        adc #<(RINGCHARS-ROWCHARS)
        sta ptr
        lda ptr+1
        adc #>(RINGCHARS-ROWCHARS)
  .if MODELB
        sta ptr+1                   ; 23 rows of chars is not whole pages: 16-bit fold
        cmp #>RINGCHARS
        bcc @pnf
        bne @pfold
        lda ptr
        cmp #<RINGCHARS
        bcc @pnf
@pfold: lda ptr
        sec
        sbc #<RINGCHARS
        sta ptr
        lda ptr+1
        sbc #>RINGCHARS
        sta ptr+1
@pnf:
  .else
        cmp #>RINGCHARS             ; RINGCHARS is whole pages, so the fold is a
        bcc @pnf                    ; high-byte compare
        sbc #>RINGCHARS
@pnf:   sta ptr+1
  .endif
        asl ptr                     ; char -> byte address, + RINGBASE (the rols leave
        rol ptr+1                   ; C clear: the offset is under $5000)
        asl ptr
        rol ptr+1
        asl ptr
        rol ptr+1
  .if MODELB
        lda ptr                     ; the base is xx80, and which xx is the buffer's
        adc #<RING_A
        sta ptr
        lda ptr+1
        adc ringbhi
  .else
        lda ptr+1
        adc #>RINGBASE
  .endif
        sta ptr+1
```
->
```
        lda tmp4                    ; C = 0: sp was a char (8-aligned) + wfine (< 8)
        adc #<(-ROWCHARS)           ; ringS + tmp4 - 80: this low add cannot carry (tmp4 <= 79)
        adc ringS
        sta ptr
        lda ringS+1
        adc #$FF                    ; C = 1: >= 0, already in the ring
        bcs @pnf
  .if MODELB
        tax                         ; < 0: + RINGCHARS, 16 bit (23 rows is not whole pages)
        lda ptr
        adc #<RINGCHARS
        sta ptr
        txa
  .endif
        adc #>RINGCHARS
@pnf:   asl ptr                     ; char -> byte address (A:ptr), + RINGBASE (the rols
        rol                         ; leave C clear: the offset is under $5000)
        asl ptr
        rol
        asl ptr
        rol
  .if MODELB
        tax
        lda ptr                     ; the base is xx80, and which xx is the buffer's
        adc #<RING_A
        sta ptr
        txa
        adc ringbhi
  .else
        adc #>RINGBASE
  .endif
        sta ptr+1
```

Original: v = ringS + tmp4 + (RINGCHARS-80), minus RINGCHARS if v >= RINGCHARS; then ptr = v*8 + base, shifted in memory with asl/rol. New: d = ringS + tmp4 - 80. The low byte is tmp4 + $B0 (cannot carry, since tmp4 <= $4F) plus ringS; the high byte is ringS+1 + $FF + C. The carry out of adc #$FF is 1 exactly when d >= 0, which is exactly when the original folded (v - RINGCHARS = d). When d < 0 the code adds RINGCHARS, giving v: 16-bit on the Model B, high byte only on the Master, where <RINGCHARS = 0. The three shifts then keep the high byte in A (rol A instead of rol ptr+1): same bits, and the same final C = 0 since the offset is under $5000. The base add is the original's, with A or X holding the high byte. Final ptr/ptr+1, A, and the flags of the last adc are identical. X is clobbered on the Model B, where it is dead.

Review: Original matches 2523-2574. C=0 at lda tmp4: sp is a char address (8-aligned, bases xx80) plus wfine in {2,4,6} (wy&3)*2, so there is no carry. tmp4 <= 79 on both paths, so tmp4+$B0 cannot carry, and the carry out of adc #$FF is exactly (ringS+tmp4 >= 80), which is exactly the original's fold condition v >= RINGCHARS, with v-RINGCHARS = d. The d<0 path adds RINGCHARS (16-bit on the Model B, high byte on the Master where <RINGCHARS = 0). rol A gives the same bits and final C=0 as rol ptr+1. The base add is identical and ptr+1 is not read in between. X is clobbered on the Model B only, and ldx wfine follows. Measured Master CODE -21, Model B TILCODE -30. Faster on every path. The gate passes with it applied (B title aside, see 0).

## r012:6  src/engine.s:2579-2586  (window i1663)  M -8 B -8  [medium]  review: accept

Cycles: setup 8 cycles faster (drops lda @ftab-1,x / sta @fjmp+2); every arrival at @fjmp: jmp 3 -> bne taken 3 (+1 only if the branch crosses a page, ignored per the brief)

Assumption: Z = 0 at every arrival at @fjmp. (1) Fall-through from ldx cnt: cnt is 1..80 (ROWCHARS, or min(HI,79) - LO + 1). (2) `bcc @fjmp` at 2614, after `lda ptr / clc / adc #8 / sta ptr`: no carry means the sum is 8..255, so nonzero. (3) `bra @fjmp` at 2619, after ringup ptr / sta ptr+1: A is the new high byte of a screen address (Master >= $30, Model B >= $0A). ringup's last flag-setter is either cmp (A < limit, so Z = 0) or pla/sbc of that nonzero high byte. grep: nothing else writes @fjmp+1/+2. Displacements 5/16/27 are all < 128. NOTE: this depends on the loop tail (r013's lines 2607-2619) keeping Z = 0 at its branches to @fjmp.

```
        ldx wfine
        lda @ftab-2,x
        sta @fjmp+1
        lda @ftab-1,x
        sta @fjmp+2
        ldx cnt                     ; char counter in X: dex/beq is 3 cycles cheaper
@fjmp:  jmp @g4
@ftab:  .word @g4, @g2, @g0         ; wfine 2: six lines, 4: four, 6: two
```
->
```
        ldx wfine
        lda @ftab-2,x               ; the branch displacement for this wfine
        sta @fjmp+1
        ldx cnt                     ; char counter in X: dex/beq is 3 cycles cheaper
@fjmp:  bne @g4                     ; patched; always taken: Z = 0 at every arrival
@ftab:  .byte @g4-@ftab, 0, @g2-@ftab, 0, @g0-@ftab   ; wfine 2: six lines, 4: four, 6: two
```

The self-modified absolute jmp becomes a self-modified relative bne. The table holds each entry's displacement from @ftab (= @fjmp+2) as bytes at the same wfine-2 offsets 0/2/4 (the gap bytes are unused), so one lda/sta patches it. With Z = 0 at every arrival, the bne always goes to the patched @g4/@g2/@g0, exactly as the jmp did. Y, X and A at the targets are unchanged (A is overwritten by lda (sp),y). This is a code-local jump table, not a game data table.

Review: Original matches 2579-2586. wfine is 2/4/6, so the byte table at offsets 0/2/4 holds displacements from @ftab = @fjmp+2 (5/16/27, in range). Z=0 at all three arrivals: fall-through after ldx cnt (cnt 1..80); bcc @fjmp after adc #8 with no carry (result 8..255); bra @fjmp after ringup/sta, where A is the nonzero ring high byte (Master: cmp not-equal or sbc giving >= $30; Model B: cmp not-equal or the final sbc #0 of the nonzero high byte). Nothing else writes @fjmp+1/+2. Measured Master CODE -8, Model B TILCODE -8; setup is 8 cycles faster and each dispatch 3->3. CAVEAT for serial apply: this depends on r013 keeping Z=0 at the bcc/bra into @fjmp (2607-2619); any r013 edit there must be rechecked against the bne.

## r013:0  src/engine.s:2551-2574  (window i1664)  M -7 B -3  [high]  review: accept

Cycles: Master: 15 cycles faster on both the fold and no-fold paths (drops sta ptr+1 3cy; rol zp->rol A saves 3x3; drops lda ptr+1 3cy). Model B: 9 cycles faster on every path (lda ptr moves ahead of the shifts; asl zp 5 -> asl A 2, x3).

Assumption: Master: at @pnf, A holds the value the old code stored to ptr+1 on both paths (bcc taken: A = high byte from adc #>(RINGCHARS-ROWCHARS) at engine.s:2548; fall-through: A = that minus >RINGCHARS). ptr+1 is not read between @pnf and the final sta ptr+1. Model B: ptr+1 is already stored on all three paths into @pnf (lines 2530/2550 in the Model B block), unchanged. ptr and ptr+1 are zero page (listing A5 rr). No anonymous labels touched.

```
@pnf:
  .else
        cmp #>RINGCHARS             ; RINGCHARS is whole pages, so the fold is a
        bcc @pnf                    ; high-byte compare
        sbc #>RINGCHARS
@pnf:   sta ptr+1
  .endif
        asl ptr                     ; char -> byte address, + RINGBASE (the rols leave
        rol ptr+1                   ; C clear: the offset is under $5000)
        asl ptr
        rol ptr+1
        asl ptr
        rol ptr+1
  .if MODELB
        lda ptr                     ; the base is xx80, and which xx is the buffer's
        adc #<RING_A
        sta ptr
        lda ptr+1
        adc ringbhi
  .else
        lda ptr+1
        adc #>RINGBASE
  .endif
        sta ptr+1
```
->
```
@pnf:
  .else
        cmp #>RINGCHARS             ; RINGCHARS is whole pages, so the fold is a
        bcc @pnf                    ; high-byte compare
        sbc #>RINGCHARS
@pnf:                               ; A = the high byte (not yet stored)
  .endif
  .if MODELB
        lda ptr                     ; char -> byte address, + RINGBASE (the rols leave
        asl                         ; C clear: the offset is under $5000)
        rol ptr+1
        asl
        rol ptr+1
        asl
        rol ptr+1
        adc #<RING_A                ; the base is xx80, and which xx is the buffer's
        sta ptr
        lda ptr+1
        adc ringbhi
  .else
        asl ptr                     ; char -> byte address, + RINGBASE (the rols leave
        rol                         ; C clear: the offset is under $5000)
        asl ptr
        rol
        asl ptr
        rol
        adc #>RINGBASE
  .endif
        sta ptr+1
```

Master: old = sta ptr+1; 3x(asl ptr; rol ptr+1); lda ptr+1; adc; sta ptr+1. The high byte does not need to be in memory until the end. Shifting it in A (asl ptr puts ptr bit 7 into C, rol A takes it) gives the same 16-bit shift and the same final C (bit 7 of the high byte before the last rol, clear per the comment). Then adc #>RINGBASE/sta ptr+1 as before. Model B: old = 3x(asl ptr; rol ptr+1); lda ptr; adc #<RING_A; sta ptr; ... New keeps the low byte in A through the shifts (asl A == asl ptr for the value and C), ptr+1 still rotated in memory, then adc #<RING_A with the same C and value, sta ptr, and the high-byte add unchanged. Final ptr, ptr+1, A, C, N and Z identical. (This spans i1669..i1683, so windows i1652..i1669 contain it and it may duplicate r012.) [Sizes verified by assembling a scratch copy of both targets with this change alone.]

Review: Original matches lines 2551-2574. Master: A at @pnf holds the value the old code stored (both the bcc path and the sbc fall-through); asl ptr / rol A x3 is the same 16-bit shift with the same final C, and ptr+1 is not read before the final sta. Model B: ptr+1 already stored on all paths into @pnf; shifting the low byte in A gives the same value and C into adc #<RING_A. No labels inside, flags overwritten by ldx wfine. Assembled: Master CODE -7, Model B TILCODE -3; faster on both.

## r013:1  src/engine.s:527-527  (window i1678)  M -0 B -28  [high]  review: accept

Cycles: Model B: inca 11cy/6 bytes -> adc #0 2cy/2 bytes on the fold path of every spnext (9cy faster). The no-fold path is untouched. Master unchanged.

Assumption: This is the spnext macro body (engine.s:520-531), and the fact is local to the macro. Line 527 is reached only by fall-through of the macro's own bcc :++ at 525, so C = 1. The next instruction is ringup's cmp (ringehi on Model B), which overwrites N, Z and C; V is never read. Model B assembles 7 expansions: 4 in TILCODE (engine.s:952, 1129, 2607, 2657), sprnext (engine.s:2372) in both SPR4CODE and SPR6CODE, and menu.s:132 in MNUCODE. At 4 bytes each that is 28, verified by assembling a scratch copy: TILCODE -16, SPR4CODE -4, SPR6CODE -4, MNUCODE -4, Master unchanged. None of the expansions is in timing-critical code (blitters/menus), so the change only makes them faster. Branch ranges only shrink.

```
        inca
```
->
```
  .if ::MODELB
        adc #0                      ; C = 1: the bcc fell through; ringup's cmp resets it
  .else
        inca
  .endif
```

A = sp+1 + 0 + C(1) = sp+1 + 1, the same result as inca. The carry that inca preserves is overwritten by ringup's cmp before any use, and so are the flags adc sets. mtmp is inca's scratch; nothing after the macro depends on inca having written it (every expansion that uses mtmp writes it first). [Sizes verified by assembling a scratch copy of both targets with this change alone.]

Review: Line 527 is the inca in the spnext macro, reached only by fall-through of the macro's bcc :++, so C=1 and adc #0 gives sp+1+1. Model B ringup starts with cmp ringehi, which overwrites C/N/Z; V never read. mtmp is only used inside cpu.inc expansions that write it first (grep: no other use in src or modelb/src). Anonymous-label count unchanged. Assembled: Model B TILCODE -16, SPR4CODE -4, SPR6CODE -4, MNUCODE -4 (28); Master identical. 9 cycles faster on the fold path.

## r013:2  src/engine.s:2616-2616  (window i1685)  M -0 B -4  [high]  review: accept

Cycles: Model B: inca (sta/inc/lda mtmp, 3+5+3=11cy, 6 bytes) -> adc #0 (2cy, 2 bytes), 9 cycles faster on the one path through it. Master unchanged (inc a).

Assumption: C = 1 at line 2616: the only way in is the fall-through of bcc @fjmp at 2614, with no label between 2614 and 2616. ringup (Model B) begins with cmp ringehi, which sets N, Z and C from A alone, so neither the carry inca preserved nor the flags adc sets (V included) are consumed. mtmp is not read afterwards (it is inca's own scratch).

```
        inca
```
->
```
  .if MODELB
        adc #0                      ; C = 1: the bcc fell through; ringup's cmp resets it
  .else
        inca
  .endif
```

A = ptr+1 + 0 + C(=1) = ptr+1 + 1, the same result as inca. inca preserved C=1; adc #0 leaves C=0 unless ptr+1 = $FF, which is impossible for a screen address. Either way the next instruction is ringup's cmp ringehi, which overwrites C, N and Z, and V is never read. So A and all consumed state are identical. [Sizes verified by assembling a scratch copy of both targets with this change alone.]

Review: Line 2616 is reached only by fall-through of bcc @fjmp at 2614 (C=1; lda ptr+1 keeps C), so adc #0 = +1. ringup ptr (cmp ringehi) overwrites every flag consumed afterwards; mtmp not live. Assembled: Model B TILCODE -4; Master identical.

## r013:3  src/engine.s:2619-2619  (window i1690)  M -0 B -1  [high]  review: accept

Cycles: Taken path: Model B bra (jmp abs, 3cy) -> bne taken 3cy; Master bra 3cy -> bne 3cy. Same, ignoring page crossing. The branch is always taken.

Assumption: Z = 0 after ringup + sta ptr+1 on both targets. Model B ringup either takes bcc after cmp ringehi (A < ringehi, so A != ringehi and Z = 0) or ends with sbc #0, leaving A = the folded high byte, a screen-memory page ($2B..$7F) and so nonzero. Master: bcc after cmp #>RINGEND (A != >RINGEND, Z = 0), or sbc #>RINGBYTES leaving a screen page, nonzero. sta does not touch flags. Branch distance on Model B: currently -119 to @fjmp (listing $6AE -> $637); the inca candidate shortens it, so it stays within range.

```
        bra @fjmp
```
->
```
        bne @fjmp                   ; Z = 0: A is a ring high byte, never 0
```

bra @fjmp is an unconditional jump. With Z provably 0 at that point on every path, bne @fjmp goes to the same place in the same cycles. Model B saves the jmp's third byte; on the Master bra and bne are both 2 bytes. [Sizes verified by assembling a scratch copy of both targets with this change alone.]

Review: After ringup + sta ptr+1, Z=0 on every path: bcc taken after cmp means A != the compared value; the fold path ends in sbc leaving a ring page high byte (nonzero on both targets). So bne always branches, 3 cycles like bra (Model B jmp is also 3). Branch range fine: assembled alone and with all the others, no range error. Model B TILCODE -1, Master identical.

## r013:4  src/engine.s:2648-2649  (window i1711)  M -1 B -1  [high]  review: accept

Cycles: clc (2) + adc (2) = 4cy -> adc 2cy, both targets, single path.

Assumption: C = 1 on entry to line 2648. The only path reaching it passes cmp maxwy+1 with Z = 1 (bne @no at 2638 not taken), so the values are equal and C = 1. The ldx, lda abs,x, bne, inc abs,x and lda/sta zp in between do not touch C, and there is no label between 2638 and 2648. ringaddr (engine.s:537) does not read C on entry: ringmod is and#/cmp-based, and both of those paths overwrite C.

```
        clc
        adc #VISROWS
```
->
```
        adc #VISROWS-1              ; C = 1 from cmp maxwy+1 (equal)
```

wcy + (VISROWS-1) + 1 = wcy + VISROWS: same A, same carry out, same N/Z. V is also the same because both operands are small positive constants. ringaddr receives the same A. [Sizes verified by assembling a scratch copy of both targets with this change alone.]

Review: The only path to 2648 passes cmp maxwy+1 with bne @no not taken (equal => C=1); ldx, lda abs,x, bne, inc abs,x, lda/sta don't touch C, and no label in between. adc #VISROWS-1 with C=1 gives the same A, C, N, Z; V differs only in theory and ringaddr (and #31 / asl / clc) reads none of them. Assembled: Master CODE -1, Model B TILCODE -1.

## r013:5  src/engine.s:2687-2687  (window i1734)  M -1 B -0  [high]  review: accept

Cycles: ldx #0 (2cy) -> tax (2cy). Master only (inside the .else of .if MODELB).

Assumption: A = 0 from lda #0 on the line before (2686). X is not read in between.

```
        ldx #0                      ; five abs,x stores cover the 5 pages in one pass
```
->
```
        tax                         ; five abs,x stores cover the 5 pages in one pass
```

tax copies the 0 just loaded into X and sets N=0, Z=1, the same flags ldx #0 would set (they are not consumed before sta abs,x anyway). [Sizes verified by assembling a scratch copy of both targets with this change alone.]

Review: Master-only (.else of .if MODELB). lda #0 on the line before, so tax = ldx #0 in value, flags and cycles. Assembled: Master CODE -1.

## r013:6  src/engine.s:2699-2733  (window i1766)  M -11 B -0  [high]  review: accept

Cycles: Master only. Terminator path: identical (ldy, lda, cmp, beq). Per span: ldy #2 -> iny (0cy); removed lda w16/clc/adc #3/sta w16/bcc (13cy, or 17 with the inc) and ldy #0 (2cy); added adc #3 (2) and iny (2). Net 11..15 cycles faster per span. The @bc loop body is unchanged (page crossings of the indexed modes may move; ignored per the brief). The step-past-data tail is unchanged.

Assumption: Master only: the block is the .else of .if MODELB (lines 2681-2734). ldaz on the Master is lda (w16), which leaves Y = 1. BARADDR + offset + len < $10000 (BARADDR = $2B00, the bar is 5 pages, max offset in the data is 908), so the high-byte adc clears C before adc #3. Every span has len <= 252, so len+3 does not wrap: the span list built by tools/convert.py:1237-1248 into build/BOX currently has 20 spans, max len 39 (checked). w16, w16b and tmp2 are zero page (listing). tmp2 and X are not read after @bsdone (rts).

```
@bs:    ldy #1
        lda (w16),y                 ; offset high ($FF = end)
        cmp #$FF
        beq @bsdone
        tax                         ; X is free: the @bf counter ran down to 0
        ldaz w16                    ; offset low, (zp): no dey needed
        clc
        adc #<BARADDR
        sta w16b
        txa
        adc #>BARADDR
        sta w16b+1
        ldy #2
        lda (w16),y
        sta tmp2                    ; len
        lda w16                     ; step past the 3-byte header
        clc
        adc #3
        sta w16
        bcc :+
        inc w16+1
:       ldy #0
@bc:    lda (w16),y
        sta (w16b),y
        iny
        cpy tmp2
        bne @bc
        tya                         ; step past the data
        clc
        adc w16
        sta w16
        bcc @bs
        inc w16+1
        bra @bs
@bsdone: rts
```
->
```
@bs:    ldy #1
        lda (w16),y                 ; offset high ($FF = end)
        cmp #$FF
        beq @bsdone
        tax                         ; X is free: the @bf counter ran down to 0
        ldaz w16                    ; offset low, (zp): Y stays 1
        clc
        adc #<(BARADDR-3)           ; dest - 3: the copy's Y runs 3..len+2, over the
        sta w16b                    ; header, so w16 needs no step past it
        txa
        adc #>(BARADDR-3)
        sta w16b+1
        iny                         ; Y = 2
        lda (w16),y
        adc #3                      ; C clear: no carry out of the high byte above
        sta tmp2                    ; len + 3
        iny                         ; Y = 3
@bc:    lda (w16),y
        sta (w16b),y
        iny
        cpy tmp2
        bne @bc
        tya                         ; step past the header and the data (Y = len + 3)
        clc
        adc w16
        sta w16
        bcc @bs
        inc w16+1
        bra @bs
@bsdone: rts
```

Old: w16b = BARADDR + off; tmp2 = len; w16 += 3; copy (w16)+0..len-1 -> (w16b)+0..len-1; w16 += len. New: w16b = BARADDR - 3 + off (low add with C=0, high add with the low carry, so an exact 16-bit add); tmp2 = len + 3 (C = 0 from the high add); Y = 3; copy (w16)+3..len+2 -> (w16b)+3..len+2, i.e. source w16+3+k -> dest BARADDR+off+k for k = 0..len-1, exactly the old bytes. The loop exits with Y = len+3, so w16 += len+3, which equals the old +3 then +len; the same bcc/inc handles the carry into w16+1. A and X at exit do not matter because the loop back to @bs reloads them. iny replaces ldy #2 because Y = 1 from ldy #1 (lda (zp) on the 65C02 keeps Y). [Sizes verified by assembling a scratch copy of both targets with this change alone.]

Review: Master-only block, original matches 2699-2733. On the 65C02 lda (w16) keeps Y=1, so iny gives 2; the high-byte adc of BARADDR-3+off leaves C=0, so adc #3 makes tmp2=len+3; the copy for Y=3..len+2 moves source w16+3+k to BARADDR+off+k, the same bytes as before, and exit Y=len+3 steps w16 past header and data in one add. The removed ':' at 2720 and its only reference bcc :+ at 2718 go together; no other anonymous reference crosses it (next is beq :+ at 2770 forward to 2772, no anon-emitting macros between). Precondition: every span len <= 252 (the old code allowed 255): the current span list in build/BOX (SPR_BAR=$BC60) has 20 spans, len 1..39, so it holds with a big margin. Assembled: Master CODE -11; Model B does not assemble it. 11-15 cycles faster per span, not timing-critical.

## r014:0  src/engine.s:2705-2727  (window i1773)  M -13 B -0  [high]  review: accept

Cycles: Per span, outside the loop: 29..33 -> 10 (removed: clc, the lda/clc/adc/sta/bcc[/inc] header step, ldy #0 and the tail clc; added: adc #2). Copy loop: 19 per iteration -> 19, same iteration count. Model B: code not assembled (.if MODELB rts .else).

Assumption: Master only: bar_bg's span code is inside .if MODELB/.else at engine.s:2680-2734. C=0 at 2705: the cmp #$FF at 2701 with A != $FF (beq @bsdone not taken) leaves C=0, and tax and ldaz (Master lda (zp)) keep it. BARADDR=$2B00 on the Master (engine.s:172) and span offsets are < 1280 (tools/convert.py:1237-1248, 2 rows x 640), so the high-byte add never carries. Every span len is 1..39 in the built data (build/BOX at SPR_BAR $BC60: 20 spans, max 39; convert.py never emits len 0), so len+2 <= 254 fits in Y and never wraps. tmp2 is scratch and is not read after bar_bg.

```
        clc
        adc #<BARADDR
        sta w16b
        txa
        adc #>BARADDR
        sta w16b+1
        ldy #2
        lda (w16),y
        sta tmp2                    ; len
        lda w16                     ; step past the 3-byte header
        clc
        adc #3
        sta w16
        bcc :+
        inc w16+1
:       ldy #0
@bc:    lda (w16),y
        sta (w16b),y
        iny
        cpy tmp2
        bne @bc
        tya                         ; step past the data
        clc
```
->
```
        adc #<(BARADDR-3)           ; C = 0: the cmp #$FF above failed.  w16b = the
        sta w16b                    ; span's address less 3, so data byte k sits at
        txa                         ; Y = 3+k, the Y that reads it from the record
        adc #>(BARADDR-3)
        sta w16b+1                  ; C = 0: the sum is an address
        ldy #2
        lda (w16),y
        adc #2                      ; len + 2 (len <= 39): the Y of the last data byte
        sta tmp2
@bc:    iny
        lda (w16),y
        sta (w16b),y
        cpy tmp2
        bne @bc
        tya                         ; C = 1 from the cpy: step past header and data
```

Old: w16b = BARADDR+off; w16 += 3; copy Y=0..len-1 from (w16),y to (w16b),y; then w16 += len (tail tya/clc/adc). New: w16b = BARADDR+off-3 and w16 is not stepped. Y=2 after reading len, and the loop pre-increments, so it copies Y=3..len+2. Source byte header+3+k goes to (BARADDR+off-3)+3+k = BARADDR+off+k: the same bytes to the same places. The loop exits on cpy equal with Y=len+2 and C=1, so tya/adc w16 adds len+3, the same total as the old 3+len. The carry into w16+1 is identical because the 8-bit sum is the same. The first clc (2705) is dead because the cmp #$FF failed. The adc #2 on len needs C=0, which the high-byte address add leaves. Bytes: removed clc(1) lda w16(2) clc(1) adc #3(2) sta(2) bcc(2) inc(2) ldy #0(2) clc(1) = 15; added adc #2 (2); saving 13.

Review: Master-only (.if MODELB rts .else). C=0 at 2705 (cmp #$FF failed, A<$FF); high add X(<=3, max offset 908 in build/BOX)+$2A cannot carry, so adc #2 sees C=0; spans in build/BOX are 20, len 1..39, so len+2 fits in Y; loop copies Y=3..len+2 to (BARADDR+off-3)+Y, same bytes; cpy-equal leaves C=1 so tya/adc w16 adds len+3; tmp2/Y dead after (caller 3086 does stz BARBG). Anon ":" at 2720 only served the removed bcc. Emulated (py65) bar_bg before/after: $2B00-$2FFF identical. Loop 19cy/iter both; outside loop fewer. Linked CODE -13. Caveat: a future bar span of 254/255 bytes would break it (old code handled up to 255); art gives max 39.

## r014:1  src/engine.s:2769-2769  (window i1794)  M -1 B -0  [high]  review: accept

Cycles: 3 -> 2 (ldx zp -> txa) for both curbuf values. Model B: not assembled.

Assumption: Master only (.if .not MODELB at engine.s:2737). X = curbuf*2 from lda curbuf/asl/tax at 2758-2760, and the four sta abs,x stores at 2762-2768 do not change it. curbuf is 0 or 1 (engine.s:217). A is dead: 2772 reloads it with lda #1.

```
        ldx curbuf
```
->
```
        txa                         ; X = curbuf*2: Z iff curbuf = 0, and then X is 0
```

Old: ldx curbuf sets Z iff curbuf=0 and X=curbuf, then beq skips ldx #48 when it is 0. New: txa sets Z from X=curbuf*2, which is zero iff curbuf=0. curbuf=0: X is already 0 and the branch is taken, so X=0 as before. curbuf=1: X=2, Z is clear, so ldx #48 runs as before. A is clobbered but reloaded at 2772. Flags after the beq/ldx are the same as before: ldx #48 sets them identically, and on the taken path nothing reads Z/N before lda #1.

Review: X = curbuf*2 (curbuf only 0/1: main.s, engine.s:3151 eor #1), unchanged by the sta abs,x; txa gives Z iff curbuf=0 with X already 0; A reloaded by lda #1. Verified by emulation.

## r014:2  src/engine.s:2799-2801  (window i1823)  M -1 B -0  [high]  review: accept

Cycles: -2 (clc gone) on the single path. Model B: not assembled.

Assumption: Master only. @addr (engine.s:2933-2941) does clc/adc #<CRTCBASE, then tya/adc #>CRTCBASE with w16+1 <= $09 (w16 < RINGCHARS = $A00: here w16 = ringS-80 folded, and ringS < 2560) and >CRTCBASE = $06 (RINGBASE $3000/8). So it returns C=0, and the comment at 2929-2932 says the sum is under $1000.

```
        txa
        clc
        adc #8
```
->
```
        txa
        adc #8                      ; C = 0 out of @addr: its sum is under $1000
```

clc before adc #8 is redundant because C is already 0. The last instruction in @addr that sets C is adc #>CRTCBASE, which cannot carry (hi <= $0F), and sta abs,x and rts do not touch C. The result X+8 is identical, and C after adc #8 is 0 either way (X <= 56).

Review: @addr returns C=0: w16 hi <=$09 here (ringS-80 folded), +$06 cannot carry. clc dead.

## r014:3  src/engine.s:2811-2813  (window i1835)  M -1 B -0  [high]  review: accept

Cycles: -2 on the single path. Model B: not assembled.

Assumption: Master only. C=0 on entry: txa/(clc)/adc #8/tax at 2799-2802 with X <= 48 leaves C=0, whether or not the 2800 candidate is applied. The stz (native 65C02 on the Master), lda, eor and sta between 2803 and 2810 do not touch C.

```
        lda ringS                   ; the run starts one row into the window
        clc
        adc #ROWCHARS
```
->
```
        lda ringS                   ; the run starts one row into the window
        adc #ROWCHARS               ; C = 0 from the adc #8 above
```

Carry is provably clear at 2812, so the clc does nothing. The 16-bit add ringS+80 is unchanged.

Review: C=0 after adc #8 (X<=56); stz (native), lda, eor, sta keep C. Holds whether or not candidate 2 is applied.

## r014:4  src/engine.s:2819-2843  (window i1858)  M -8 B -0  [high]  review: accept

Cycles: f>0 path: 27 -> 21 (28 -> 21 when barq = 31). f=0 path: 29 -> 26. Model B: not assembled.

Assumption: Master only. RINGROWS = 32 on the Master (engine.s:105; the proposal asserts it). barq = ringS/80 with ringS <= 2559 (engine.s:279-280), so barq is 0..31. @run's only reference is the bra at 2827; the other @run, at 804, is in a different scope. tmp4 is still written before @past reads it (2845).

```
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        lda barq
        inca
        cmp #RINGROWS
        bcc :+
        lda #0
:       sta tmp3
        bra @run
@coarse:                            ; ---- f = 0 : T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda #VISROWS
        sta tmp4
        lda barq                    ; the run starts on the window's ring row
        sta tmp3
@run:   ; w16 = the run's ring offset, tmp4 = its rows, X = the entry of the section
        ; before it.  Entry i carries section i+1's address and duration, and section
        ; i's own R4/R9/R6/R7, so each section is written across two entries.
        ; The run is never split any more: a row that straddles the ring end is
        ; folded by the CRTC, so the playfield is one section however the window sits.
        lda tmp4
        sta tmp2
```
->
```
        lda barq
        inca
        and #RINGROWS-1             ; barq < 32 = RINGROWS: the next slot, mod 32
  .assert RINGROWS = 32, error, "the and needs RINGROWS a power of two"
        sta tmp3
        lda #VISROWS-1              ; rows in the run
        bra @run
@coarse:                            ; ---- f = 0 : T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda barq                    ; the run starts on the window's ring row
        sta tmp3
        lda #VISROWS
@run:   ; A = the run's rows (kept in tmp4), w16 = its ring offset, X = the entry of the
        ; section before it.  Entry i carries section i+1's address and duration, and section
        ; i's own R4/R9/R6/R7, so each section is written across two entries.
        ; The run is never split any more: a row that straddles the ring end is
        ; folded by the CRTC, so the playfield is one section however the window sits.
        sta tmp4
        sta tmp2
```

(1) inc a / cmp #32 / bcc / lda #0 equals inc a / and #31 for barq in 0..31: 32 becomes 0 and everything else is unchanged. C and N/Z differ, but nothing reads them before @emit's jsr @addr (which does clc); the instructions in between are sta tmp3, lda #, bra, sta, sta, jsr. (2) Both paths now reach @run with A = the row count instead of storing it to tmp4 and reloading it: the fine path with lda #VISROWS-1, the coarse path with lda #VISROWS (moved after the independent barq/tmp3 store). @run stores A to tmp4 and tmp2, so tmp2, tmp3, tmp4, w16 and X are exactly as before at jsr @emit. Bytes: removed the fine-path sta tmp4 (2), cmp (2), bcc (2), lda #0 (2) and lda tmp4 (2); added and (2); saving 8.

Review: RINGROWS=32 on Master, barq = ringS/80 <= 31 (engine.s:3473), so and #31 == wrap. C/NZ after differ but nothing reads them before @addr clc. Both paths reach @run with A = row count, stored to tmp4 and tmp2. @run only referenced by the bra (the other @run is another scope). Anon ":" at 2826 only served removed bcc; no :- counts across. Emulated all wfine/curbuf and ringS: SECTAB identical.

## r014:5  src/engine.s:2847-2873  (window i1887)  M -7 B -0  [high]  review: accept

Cycles: f>0 path: -6 (one lda wfine (3) and the bra (3) are gone; jsr @addr just moves ahead of the test). f=0 path: lda 3 + beq 3 + jsr @addr -> jsr @addr + lda 3 + beq 3, equal. Model B: not assembled.

Assumption: Master only. @setq's only reference is the beq at 2848, and @sq2's only reference is the bra at 2872 (grep engine.s). @addr (2933) reads w16 and X, writes SECTAB+0/1,x and clobbers A and Y. Both old paths called it first thing, at the same X and with the same w16 (set by @advance at 2846), so moving it above the wfine test changes nothing. @addr does not touch wfine.

```
        lda wfine
        beq @setq
        ; --- P2 : the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2,x             ; stz abs,x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved since the P2 @addr above, so the
        sta SECTAB,x                ; address it left in the previous entry is this one's
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
```
->
```
        jsr @addr                   ; P2, or else Q, starts on the row below the playfield
        lda wfine
        beq @sq2
        ; --- P2 : the top f lines of that row
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2,x             ; stz abs,x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved since the P2 @addr above, so the
        sta SECTAB,x                ; address it left in the previous entry is this one's
        lda SECTAB-8+1,x
        sta SECTAB+1,x
```

Old P2 path: lda wfine / beq / jsr @addr / lda wfine / jsr @dur ... Old f=0 path: lda wfine / beq @setq / jsr @addr / (@sq2). Both call @addr at the same X before anything else happens, so it is called once before the test. The test's lda wfine then also serves as P2's argument to @dur, so the second lda wfine goes. With @setq's jsr @addr gone, P2's closing bra @sq2 would target the very next instruction, so it is deleted and P2 falls into @sq2. Z for the beq comes from lda wfine as before. C into @sq2 stays 0 on both paths (from @addr, or from P2's adc #8), which the candidate at 2874 relies on either way.

Review: @addr is called first on both old paths with same X/w16 and does not touch wfine; C=0 into @sq2 on both paths. @setq/@sq2 each singly referenced. Correct, BUT textually conflicts with candidate 8 (both rewrite 2853-2855); candidate 8 sorts first and the fix of 8 folds this edit in, so this one will go stale -- kept as fallback if 8 fails its gate.

## r014:6  src/engine.s:2874-2896  (window i1910)  M -5 B -0  [high]  review: accept

Cycles: -6 on the single path: two lda # (4) and one clc (2) are gone, and the two new sta abs,x (5+5) replace the two old ones (5+5). Model B: not assembled.

Assumption: Master only. C=0 at @sq2 on both entries: via @setq/jsr @addr (@addr returns C=0, since the high byte is <= $09 + $06), or via the P2 fall-through/bra, whose last C-writer is the adc #8 at 2858 with X < 96 (stz, lda, dec a and sta leave C alone). SECTAB is 2*48 bytes (engine.s:2444). X here is base+8 or base+24, so SECTAB+15,x <= base+39 stays within this buffer's 48 bytes, and it is exactly the byte the old code wrote as SECTAB+7 after X+=8. The table is built for the back buffer and handed over after build_sections (engine.s:3132-3140), so the order of stores within it cannot be observed.

```
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
```
->
```
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        sta SECTAB+8+6,x            ; the bar's entry (next) carries the same duration
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        sta SECTAB+8+7,x
        txa                         ; C = 0 on both ways in: @addr's sum is under $1000,
        adc #8                      ; and the P2 path's adc #8 left it clear
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
```

The old code wrote 40*LINE-2 into entry X (+6/+7) and then, after X += 8, into entry X+8 (+6/+7) with a second pair of lda #. Writing SECTAB+14/15,x from the same A before the increment stores the same values at the same addresses and drops the reload pair. The clc before adc #8 is redundant (C=0, shown above). After adc #8, C is still 0 (X < 96), so the tail's add at 2903 sees what it saw before.

Review: C=0 at @sq2 on both entries (@addr sum < $1000; P2 adc #8 X<96). SECTAB+14/15,x at old X is exactly the old SECTAB+6/7 after X+=8; intermediate stores hit +8..+13 only. X<=48+24 so +15 stays in table. Emulated identical.

## r014:7  src/engine.s:2902-2904  (window i1916)  M -1 B -0  [high]  review: accept

Cycles: -2 for both curbuf paths. Model B: not assembled.

Assumption: Master only. C=0 at 2903: the last instruction to write C is the adc #8 at 2880 (X+8 < 96, so no carry, with or without its clc). It is followed only by lda, sta, stz (65C02 stz), ldx, beq and ldx #, none of which touch C.

```
@e0:    lda SECTAB+6,x
        clc
        adc #BARLEAD
```
->
```
@e0:    lda SECTAB+6,x
        adc #BARLEAD                ; C = 0: the last carry-writer was the adc #8 above
```

Carry is provably clear, so the clc does nothing. The 16-bit add of BARLEAD into entry 0's duration is unchanged, and the bcc/inc below still sees the true carry out.

Review: Last C writer before @e0 is adc #8 at 2880 (no carry); stza on Master is plain stz; ldx/beq/lda/sta keep C.

## r014:8  src/engine.s:2785-2983  (window i1811)  M -16 B -0  [high]  review: fix

Cycles: Identical on every path: the same three instructions (sta abs,x 5 / lda zp 3 / sta abs,x 5) now run in @dur before its rts instead of after each return. Model B: not assembled.

Assumption: Master only (.if .not MODELB, engine.s:2737-3009). @dur's only callers are the jsr @dur at 2785 and 2852 and the jmp @dur in @lines (2946); @lines's only caller is the jsr @lines in @emit (2912), per grep '@dur\|@lines' engine.s. Each of the three sites is immediately followed by 'sta SECTAB+6,x / lda tmp3 / sta SECTAB+7,x' with the same X it called with (@dur and @lines do not touch X). After those stores the sites continue with lda ringS (2789), txa (2856) and txa (2916), none of which reads the flags or the A that @dur used to return.

```
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        lda ringS                   ; section A shows the composed row: the ring row
        sec                         ; above the window, ringS - 80 mod RINGCHARS
        sbc #ROWCHARS
        sta w16
        lda ringS+1
        sbc #0
        bpl :+
        adc #>RINGCHARS             ; went below 0: + RINGCHARS (whole pages; C clear)
:       sta w16+1
        jsr @addr
        txa
        clc
        adc #8
        tax                         ; A's entry
        stz SECTAB+2,x
        lda wfine
        eor #7                      ; 7 - wfine, wfine in 0..7
        sta SECTAB+3,x
        lda #2
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda ringS                   ; the run starts one row into the window
        clc
        adc #ROWCHARS
        sta w16
        lda ringS+1
        adc #0
        sta w16+1
        jsr @wrap
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        lda barq
        inca
        cmp #RINGROWS
        bcc :+
        lda #0
:       sta tmp3
        bra @run
@coarse:                            ; ---- f = 0 : T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda #VISROWS
        sta tmp4
        lda barq                    ; the run starts on the window's ring row
        sta tmp3
@run:   ; w16 = the run's ring offset, tmp4 = its rows, X = the entry of the section
        ; before it.  Entry i carries section i+1's address and duration, and section
        ; i's own R4/R9/R6/R7, so each section is written across two entries.
        ; The run is never split any more: a row that straddles the ring end is
        ; folded by the CRTC, so the playfield is one section however the window sits.
        lda tmp4
        sta tmp2
        jsr @emit
@past:  lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2 : the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2,x             ; stz abs,x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved since the P2 @addr above, so the
        sta SECTAB,x                ; address it left in the previous entry is this one's
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        ; the bar's step fired BARLEAD early, so the section after the bar -- whose
        ; duration entry 0 carries -- runs BARLEAD longer to end where it should
        ldx curbuf
        beq @e0
        ldx #48
@e0:    lda SECTAB+6,x
        clc
        adc #BARLEAD
        sta SECTAB+6,x
        bcc @e1
        inc SECTAB+7,x
@e1:    rts
; --- emit a run of tmp2 rows starting at w16 (a ring offset), following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address for the row at ring offset w16.  Ring offsets are
; 0..RINGCHARS-1 and CRTCBASE is $600, so the sum is always under $1000 and MA12 is
; clear: a section's START address never needs folding.  The fold happens mid-scan, in
; hardware, which is the whole reason the ring begins at $3000.
@addr:  lda w16
        ldy w16+1
        clc
        adc #<CRTCBASE
        sta SECTAB+1,x
        tya
        adc #>CRTCBASE
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
        ; fall through
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
; A = lines -> A/tmp3 = lines*64-2
@dur:   sta tmp3                    ; n*64 == (n*256)>>2: start from hi=n, lo=0 and
        lda #0                      ; shift right twice instead of left six times
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair, so this is A - 2
        bcs :+
        dec tmp3
:       rts
```
->
```
        jsr @dur
        lda ringS                   ; section A shows the composed row: the ring row
        sec                         ; above the window, ringS - 80 mod RINGCHARS
        sbc #ROWCHARS
        sta w16
        lda ringS+1
        sbc #0
        bpl :+
        adc #>RINGCHARS             ; went below 0: + RINGCHARS (whole pages; C clear)
:       sta w16+1
        jsr @addr
        txa
        clc
        adc #8
        tax                         ; A's entry
        stz SECTAB+2,x
        lda wfine
        eor #7                      ; 7 - wfine, wfine in 0..7
        sta SECTAB+3,x
        lda #2
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda ringS                   ; the run starts one row into the window
        clc
        adc #ROWCHARS
        sta w16
        lda ringS+1
        adc #0
        sta w16+1
        jsr @wrap
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        lda barq
        inca
        cmp #RINGROWS
        bcc :+
        lda #0
:       sta tmp3
        bra @run
@coarse:                            ; ---- f = 0 : T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda #VISROWS
        sta tmp4
        lda barq                    ; the run starts on the window's ring row
        sta tmp3
@run:   ; w16 = the run's ring offset, tmp4 = its rows, X = the entry of the section
        ; before it.  Entry i carries section i+1's address and duration, and section
        ; i's own R4/R9/R6/R7, so each section is written across two entries.
        ; The run is never split any more: a row that straddles the ring end is
        ; folded by the CRTC, so the playfield is one section however the window sits.
        lda tmp4
        sta tmp2
        jsr @emit
@past:  lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2 : the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        txa
        clc
        adc #8
        tax
        stza SECTAB+2,x             ; stz abs,x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved since the P2 @addr above, so the
        sta SECTAB,x                ; address it left in the previous entry is this one's
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        ; the bar's step fired BARLEAD early, so the section after the bar -- whose
        ; duration entry 0 carries -- runs BARLEAD longer to end where it should
        ldx curbuf
        beq @e0
        ldx #48
@e0:    lda SECTAB+6,x
        clc
        adc #BARLEAD
        sta SECTAB+6,x
        bcc @e1
        inc SECTAB+7,x
@e1:    rts
; --- emit a run of tmp2 rows starting at w16 (a ring offset), following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address for the row at ring offset w16.  Ring offsets are
; 0..RINGCHARS-1 and CRTCBASE is $600, so the sum is always under $1000 and MA12 is
; clear: a section's START address never needs folding.  The fold happens mid-scan, in
; hardware, which is the whole reason the ring begins at $3000.
@addr:  lda w16
        ldy w16+1
        clc
        adc #<CRTCBASE
        sta SECTAB+1,x
        tya
        adc #>CRTCBASE
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
        ; fall through
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
; A = lines -> SECTAB+6/7,x = lines*64-2 (every caller stored it), A = tmp3 = hi
@dur:   sta tmp3                    ; n*64 == (n*256)>>2: start from hi=n, lo=0 and
        lda #0                      ; shift right twice instead of left six times
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair, so this is A - 2
        bcs :+
        dec tmp3
:       sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        rts
```

MULTI-SITE, NOT ONE WINDOW. This is four separate line edits: delete the three-line store sequence after each @dur call (2786-2788, 2853-2855, 2913-2915) and insert that sequence in @dur between the ':' and the rts (2983). The other candidates in 2785-2983 do not change those lines, so this can be applied by hand after them. Equivalence: each caller ran the jsr/jmp chain to @dur, then sta SECTAB+6,x / lda tmp3 / sta SECTAB+7,x. Moving those three instructions into the end of @dur runs them identically, with the same X, A (lo) and tmp3 (hi), in the same order relative to everything else. On return A = tmp3 instead of lo, and N/Z come from lda tmp3, which is exactly what the callers had after their own lda tmp3/sta. sta and lda do not touch C, so C after the call is unchanged. Bytes: 3 sites x 8 (sta abs,x 3 + lda zp 2 + sta abs,x 3) removed, 8 added, saving 16.

Review: Correct as proposed (callers @2785, @2852, jmp from @lines only; X unchanged by @dur/@lines; A/NZ/C after return identical to after the callers old lda tmp3/sta; ":" still on the first moved store, bcs :+ skips only dec tmp3). But it rewrites 2852-2855, which candidate 5 also rewrites, and grind_apply sorts this one (16) before 5 (7), so 5 would go stale and its 7 bytes lost. Fixed proposal = this candidate plus candidate 5 folded in (jsr @addr hoisted above the wfine test, second lda wfine, bra @sq2 and @setq dropped). Scratch build of all r014 edits (0,1,2,3,4,6,7 + fixed 8) links with CODE -13 and LOGIC -40; py65 emulation of build_sections over curbuf 0/1, wfine 0..7, ringS 0..2559 gives identical SECTAB/BUF_SEC0 to the original. Bytes Master 16+7=23, Model B not assembled.

Fixed:
```
        jsr @dur
        lda ringS                   ; section A shows the composed row: the ring row
        sec                         ; above the window, ringS - 80 mod RINGCHARS
        sbc #ROWCHARS
        sta w16
        lda ringS+1
        sbc #0
        bpl :+
        adc #>RINGCHARS             ; went below 0: + RINGCHARS (whole pages; C clear)
:       sta w16+1
        jsr @addr
        txa
        clc
        adc #8
        tax                         ; A's entry
        stz SECTAB+2,x
        lda wfine
        eor #7                      ; 7 - wfine, wfine in 0..7
        sta SECTAB+3,x
        lda #2
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda ringS                   ; the run starts one row into the window
        clc
        adc #ROWCHARS
        sta w16
        lda ringS+1
        adc #0
        sta w16+1
        jsr @wrap
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        lda barq
        inca
        cmp #RINGROWS
        bcc :+
        lda #0
:       sta tmp3
        bra @run
@coarse:                            ; ---- f = 0 : T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda #VISROWS
        sta tmp4
        lda barq                    ; the run starts on the window's ring row
        sta tmp3
@run:   ; w16 = the run's ring offset, tmp4 = its rows, X = the entry of the section
        ; before it.  Entry i carries section i+1's address and duration, and section
        ; i's own R4/R9/R6/R7, so each section is written across two entries.
        ; The run is never split any more: a row that straddles the ring end is
        ; folded by the CRTC, so the playfield is one section however the window sits.
        lda tmp4
        sta tmp2
        jsr @emit
@past:  lda tmp4
        jsr @advance                ; now the row below the playfield
        jsr @addr                   ; P2, or else Q, starts on the row below the playfield
        lda wfine
        beq @sq2
        ; --- P2 : the top f lines of that row
        jsr @dur
        txa
        clc
        adc #8
        tax
        stza SECTAB+2,x             ; stz abs,x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved since the P2 @addr above, so the
        sta SECTAB,x                ; address it left in the previous entry is this one's
        lda SECTAB-8+1,x
        sta SECTAB+1,x
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        ; the bar's step fired BARLEAD early, so the section after the bar -- whose
        ; duration entry 0 carries -- runs BARLEAD longer to end where it should
        ldx curbuf
        beq @e0
        ldx #48
@e0:    lda SECTAB+6,x
        clc
        adc #BARLEAD
        sta SECTAB+6,x
        bcc @e1
        inc SECTAB+7,x
@e1:    rts
; --- emit a run of tmp2 rows starting at w16 (a ring offset), following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address for the row at ring offset w16.  Ring offsets are
; 0..RINGCHARS-1 and CRTCBASE is $600, so the sum is always under $1000 and MA12 is
; clear: a section's START address never needs folding.  The fold happens mid-scan, in
; hardware, which is the whole reason the ring begins at $3000.
@addr:  lda w16
        ldy w16+1
        clc
        adc #<CRTCBASE
        sta SECTAB+1,x
        tya
        adc #>CRTCBASE
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
        ; fall through
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
; A = lines -> SECTAB+6/7,x = lines*64-2 (every caller stored it), A = tmp3 = hi
@dur:   sta tmp3                    ; n*64 == (n*256)>>2: start from hi=n, lo=0 and
        lda #0                      ; shift right twice instead of left six times
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair, so this is A - 2
        bcs :+
        dec tmp3
:       sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        rts
```

## r015:0  src/engine.s:2821-2826  (window i1847)  M -11 B -0  [high]  review: accept

Cycles: Master only (inside .if .not MODELB). On the f>0 path, removing lda barq/inc a/cmp/bcc/(lda #0)/sta tmp3 saves 12-14 cycles. Nothing else changes.

Assumption: The tmp3 written here is dead. The path runs bra @run -> lda tmp4/sta tmp2/jsr @emit -> jsr @addr (reads only w16 and X) -> lda tmp2 -> jsr @lines -> @dur, whose first instruction is sta tmp3. Nothing reads tmp3 in between (engine.s:2837-2946). barq is plain RAM (engine.s:280), so reading it has no side effect. The bra @run that follows is unconditional, so the flags from cmp/lda are unused.

```
        lda barq
        inca
        cmp #RINGROWS
        bcc :+
        lda #0
:       sta tmp3
```
->
```

```

The six lines compute (barq+1) mod RINGROWS into tmp3. This is left over from when the run was split. @emit now overwrites tmp3 in @dur before any read, so the store and its computation are dead. A and the flags at the bra are irrelevant because @run begins with lda tmp4.

Review: Lines 2821-2826 match. Master-only (inside .if .not MODELB at 2737). The tmp3 store is dead: bra @run -> lda tmp4/sta tmp2/jsr @emit -> @addr (w16, X, Y only) -> lda tmp2/jsr @lines -> jmp @dur, whose first instruction is sta tmp3. Nothing reads tmp3 in between. A and flags are dead because @run starts with lda tmp4. Removing the ':' at 2826 is safe: the only refs near it are 2770->2772 and 2795->2797 (local), and everything after it counts forward. Assembled: Master LOGIC -11, Model B unchanged.

## r015:1  src/engine.s:2835-2836  (window i1856)  M -4 B -0  [high]  review: accept

Cycles: Master only. Saves 6 cycles on the coarse path.

Assumption: Same as K1: @dur writes tmp3 (via @emit -> @lines) before anything reads it. @run starts with lda tmp4, so A is dead (engine.s:2842).

```
        lda barq                    ; the run starts on the window's ring row
        sta tmp3
```
->
```

```

lda barq/sta tmp3 is a dead store. The path is @run -> @emit -> @addr (does not touch tmp3) -> @lines -> @dur, which does sta tmp3.

Review: Lines 2835-2836 match. Same dead store as index 0, on the coarse path: @run reloads A from tmp4, and @dur writes tmp3 before any read. Compatible with 0 and 2. Assembled: Master LOGIC -4.

## r015:2  src/engine.s:2842-2928  (window i1888)  M -17 B -0  [high]  review: accept

Cycles: Master only. Every path is faster. In the run, dropping jsr @emit/rts and lda tmp4/sta tmp2 saves 18 cycles. In the Q entry, dropping txa/clc/adc #8/tax and two lda # saves 12 cycles.

Assumption: @emit has exactly one caller: jsr @emit at engine.s:2844 (checked by grepping @emit within build_sections' scope). tmp2 is only read inside @emit (engine.s:2911, 2920). @addr, @lines and @dur do not change tmp4, so lda tmp4 reads the value tmp2 held. tmp2 is general scratch, and no caller of build_sections (render_frame, main.s:112-115, menu_show) reads it before writing it. After the Q entry, ldx curbuf reloads X and clc sets C, so dropping the X += 8 changes nothing downstream. SECTAB+8+n,x addresses the same bytes as SECTAB+n,(x+8). The order of stores to the back buffer's entries changes; the ISR reads the displayed buffer's entries. Measured: 17 bytes on the Master build. This supersedes candidate A.

```
        lda tmp4
        sta tmp2
        jsr @emit
@past:  lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2 : the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2,x             ; stz abs,x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved since the P2 @addr above, so the
        sta SECTAB,x                ; address it left in the previous entry is this one's
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        ; the bar's step fired BARLEAD early, so the section after the bar -- whose
        ; duration entry 0 carries -- runs BARLEAD longer to end where it should
        ldx curbuf
        beq @e0
        ldx #48
@e0:    lda SECTAB+6,x
        clc
        adc #BARLEAD
        sta SECTAB+6,x
        bcc @e1
        inc SECTAB+7,x
@e1:    rts
; --- emit a run of tmp2 rows starting at w16 (a ring offset), following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
```
->
```
        jsr @addr                   ; (the old @emit, inlined: its one caller)
        lda tmp4
        jsr @lines
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda tmp4
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
@past:  lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2 : the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2,x             ; stz abs,x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved since the P2 @addr above, so the
        sta SECTAB,x                ; address it left in the previous entry is this one's
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        sta SECTAB+8+6,x            ; Q's own entry (X+8) carries the same duration
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        sta SECTAB+8+7,x
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB+8,x
        lda #<BARCRTC
        sta SECTAB+8+1,x
        lda #QROWS-1
        sta SECTAB+8+2,x
        lda #7
        sta SECTAB+8+3,x
        stza SECTAB+8+4, x
        lda #QVSYNC
        sta SECTAB+8+5, x
        ; the bar's step fired BARLEAD early, so the section after the bar -- whose
        ; duration entry 0 carries -- runs BARLEAD longer to end where it should
        ldx curbuf
        beq @e0
        ldx #48
@e0:    lda SECTAB+6,x
        clc
        adc #BARLEAD
        sta SECTAB+6,x
        bcc @e1
        inc SECTAB+7,x
@e1:    rts
```

(1) @emit is inlined at its single call site. The body is identical except that lda tmp2 becomes lda tmp4 (tmp2 was a copy of tmp4), so lda tmp4/sta tmp2 go, and the rts goes. The @past label is kept. (2) In the Q entry, the stores use SECTAB+8+n,x instead of adding 8 to X. The new entry's duration lo/hi equal the values just written to entry X, so each lda # serves two stores. The same bytes land at the same addresses.

Review: Lines 2842-2928 match. @emit has one reference (2844) in build_sections' cheap scope; there is no normal label between 2757 and 3009. tmp2 is read only at 2911/2920, and tmp4 is untouched by @addr/@lines/@dur, so lda tmp4 is equivalent. Nothing after build_sections reads tmp2 (render_frame tail, main.s:112-116, menu_show). In the Q entry, SECTAB+8+n,x equals SECTAB+n,(X+8) (no 8-bit wrap: X is at most about 48+32). The reused A values match the stores. X, A and C are reloaded or reset afterwards (ldx curbuf, lda SECTAB+6,x, clc). The stza expansion is unchanged. Assembled: Master LOGIC -17. Supersedes index 3.

## r015:3  src/engine.s:2875-2896  (window i1910)  M -9 B -0  [high]  review: accept

Cycles: Master only. Single path, 12 cycles saved: 8 from txa/clc/adc/tax and 4 from the two lda #.

Assumption: FALLBACK for M: apply only if M was not applied (M contains this change). After line 2896, X is reloaded (ldx curbuf, 2899), C is set by clc (2903) and A is reloaded (2902).

```
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
```
->
```
        sta SECTAB+6,x
        sta SECTAB+8+6,x            ; Q's own entry (X+8) carries the same duration
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        sta SECTAB+8+7,x
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB+8,x
        lda #<BARCRTC
        sta SECTAB+8+1,x
        lda #QROWS-1
        sta SECTAB+8+2,x
        lda #7
        sta SECTAB+8+3,x
        stza SECTAB+8+4, x
        lda #QVSYNC
        sta SECTAB+8+5, x
```

SECTAB+8+n,x is the same address as SECTAB+n,(x+8). The Q entry's duration equals the one just stored to entry X, so A is stored twice instead of being reloaded.

Review: FALLBACK: correct on its own, but only if index 2 is not applied (2 contains it). Lines 2875-2896 match. After the span, X is reloaded (ldx curbuf), C is set by clc and A is reloaded. Assembled: Master LOGIC -9.

## r015:4  src/engine.s:2933-2941  (window i1948)  M -3 B -0  [high]  review: accept

Cycles: Master only. 30 -> 26 cycles, including the rts.

Assumption: <CRTCBASE = 0: on the Master, CRTCBASE = $3000/8 = $600, and the proposal asserts it. Nothing reads Y after @addr. The only Y reader in build_sections is @advance, which sets Y itself with tay (engine.s:2949). After build_sections returns, render_frame, main.s:112-115 and menu_show do not read Y before writing it.

```
@addr:  lda w16
        ldy w16+1
        clc
        adc #<CRTCBASE
        sta SECTAB+1,x
        tya
        adc #>CRTCBASE
        sta SECTAB,x
        rts
```
->
```
        .assert <CRTCBASE = 0, error, "@addr adds the high byte only"
@addr:  lda w16
        sta SECTAB+1,x
        lda w16+1
        clc
        adc #>CRTCBASE
        sta SECTAB,x
        rts
```

Original: lo = w16 + 0 (carry out 0), hi = w16+1 + 6 + 0. New: lo = w16 stored directly, hi = w16+1 + 6 after clc. On exit A, N, Z, C and V are identical, because both versions end in adc #6 with carry in 0. The only difference is that Y (previously w16+1) is no longer written.

Review: Lines 2933-2941 match. <CRTCBASE = <($3000/8) = 0, and the proposal asserts it. Exit A, N, Z, C and V are identical (adc #6 with carry in 0 in both versions). Only Y differs, and nothing reads it: after the @addr calls at 2798, 2850 and 2873 no Y read comes before a write. The callers after build_sections do not read Y (render_frame -> game.s loop/set_palette, which writes Y; main.s -> take_over/set_palette; menu_show -> m_set_palette). Assembled: Master LOGIC -3.

## r015:5  src/engine.s:2942-2983  (window i1980)  M -22 B -0  [high]  review: accept

Cycles: Master only. In @wrap every path is faster: no fold goes from 8 cycles to 5 (plus rts), and the fold from about 31 to about 12. Moving @lines removes jmp @dur, saving 3 cycles.

Assumption: RINGCHARS & $FF = 0: asserted at engine.s:141, and on the Master RINGCHARS = 80*32 = $A00. At both entries to @wrap, A = w16+1. @advance falls through right after sta w16+1, and the only jsr @wrap (engine.s:2815-2818) follows lda ringS+1/adc #0/sta w16+1. @lines has one caller (@emit, or the inlined run after M). Moving @dur does not affect its other callers (2785, 2852).

```
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
        ; fall through
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
; A = lines -> A/tmp3 = lines*64-2
@dur:   sta tmp3                    ; n*64 == (n*256)>>2: start from hi=n, lo=0 and
        lda #0                      ; shift right twice instead of left six times
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair, so this is A - 2
        bcs :+
        dec tmp3
:       rts
```
->
```
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
        ; fall through
        ; @wrap: A = w16+1 on entry (both callers have just stored it).  RINGCHARS is
        ; whole pages (asserted), so the fold is a high-byte compare and subtract
@wrap:  cmp #>RINGCHARS
        bcc :+
        sbc #>RINGCHARS             ; C = 1 from the cmp
        sta w16+1
:       rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        ; fall through
        ; A = lines -> A/tmp3 = lines*64-2
@dur:   sta tmp3                    ; n*64 == (n*256)>>2: start from hi=n, lo=0 and
        lda #0                      ; shift right twice instead of left six times
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair, so this is A - 2
        bcs :+
        dec tmp3
:       rts
```

@wrap: because <RINGCHARS = 0, w16 >= RINGCHARS exactly when w16+1 >= >RINGCHARS. The low-byte test (cmp #0) never branches, and the low-byte subtract (sec/sbc #0) leaves w16 unchanged with C=1. So the fold reduces to sbc #>RINGCHARS, with C=1 from the cmp, then sta. On both paths the exit A, N, Z and C are identical. lda w16+1 is dropped because A already holds it at both entries. @lines now sits directly above @dur, so its jmp @dur becomes a fall-through; the carry from the last asl reaches @dur the same way as through the jmp.

Review: Lines 2942-2983 match. RINGCHARS = $A00, low byte 0 (asserted at engine.s:141). Both entries to @wrap have A = w16+1 (the fall-through from @advance after sta w16+1, and jsr @wrap at 2818 after sta w16+1 at 2817). Fold iff hi >= $0A in both versions, with a single fold as before (the max w16 after @advance is under 2*RINGCHARS). Exit A and flags are identical on both paths. Moving @lines above @dur is safe: @dur's C=0 after the ror pair does not depend on the entry carry, and nothing else falls into @lines. The anonymous labels go from 3 to 2 inside the span, all references are internal, and no outside :+/:- crosses the span (the next :- after it is at 3181). Assembled: Master LOGIC -22.

## r015:6  src/engine.s:2984-3008  (window i2002)  M -44 B -0  [high]  review: accept

Cycles: Dead code: no path executes it.

Assumption: @subrow and @addrow are cheap labels scoped to build_sections (engine.s:2757-3009), and nothing references them: grep -rn 'subrow\|addrow' over src/, modelb/src/ and tools/ finds only their definitions. Nothing falls through into them, because @dur's rts at 2983 comes first.

```
; w16 = (w16 - 80) with ring wrap (w16 holds CRTC address S+$600 in $600..$FFF)
@subrow: lda w16
        sec
        sbc #ROWCHARS
        sta w16
        bcs :+
        dec w16+1
:       lda w16+1
        cmp #6
        bcs :+                      ; not taken: C = 0 for the adc
        adc #$0A
        sta w16+1
:       rts
@addrow: lda w16
        clc
        adc #ROWCHARS
        sta w16
        bcc :+
        inc w16+1
:       lda w16+1
        cmp #$10
        bcc :+                      ; not taken: C = 1 for the sbc
        sbc #$0A
        sta w16+1
:       rts
```
->
```

```

These are unreferenced subroutines left over from the ring before hardware wrap. Deleting them changes no executed instruction.

Review: Lines 2984-3008 match. @subrow/@addrow are cheap labels in build_sections' scope with no references (grep over src, modelb/src and tools). @dur's rts precedes them, so nothing falls in. The four ':' removed are all internal targets. After 3009, no :- occurs before the Master ':' at 3057 (the first :- is at 3181), and the Model B does not assemble this block. Assembled: Master LOGIC -44.

## r015:7  src/engine.s:3030-3071  (window i2039)  M -7 B -12  [high]  review: accept

Cycles: Master: the recp part goes from 21/22 cycles to 20 (table loads 4+3+4+3, rts 6). Model B: dropping sec saves 2 cycles. Outside build_ring's body, replacing jsr build_ring/rts, jmp @recs and lda curbuf/beq with the table loads and a tail jmp takes about 36 cycles down to 23.

Assumption: curbuf is 0 or 1 (engine.s:217; render_done flips it with eor #1). X = curbuf at the recp loads on both targets: on the Master, ldx curbuf runs inside the php/plp and plp keeps X; on the Model B, ldx curbuf is at the top and nothing in between changes X. On the Model B, the RINGEND_A/B high bytes ($44, $80) are >= 3, so C = 1 after sec/sbc #3. build_ring (engine.s:4673, TILCODE, the same bank) neither reads nor writes recp and does not inspect the stack, so the tail jmp is safe. On the Model B, exit A and flags change: A becomes build_ring's last w16+1 instead of >recp. Neither caller reads A or the flags. render_core does farjsr F_SELBB then jsr calc_ring, which starts with lda wcy (engine.s:3439). m_select_backbuf (TOMAIN: jsr, then jmp pagelogic) is followed by menu.s:214 jsr m_calc_ring. Exit X is RINGROWS in both versions. On the Master, exit A, X and flags are identical. Measured: Master CODE -7, Model B TILCODE -12.

```
select_backbuf:
  .if MODELB
        ldx curbuf                  ; the buffer's ring: its base and end, the two
        lda @bhi,x                  ; derived constants the blitters' wrap tests use,
        sta ringbhi                 ; and the row table from its base
        lda @ehi,x
        sta ringehi
        sec
        sbc #3
        sta ringe3
        lda #0
        sec
        sbc ringehi
        sta ringneg
        jsr build_ring
        jmp @recs
@bhi:   .byte >RING_A, >RING_B
@ehi:   .byte >RINGEND_A, >RINGEND_B
@recs:
  .else
        php                         ; the ISR writes ACCCON's D bit; this read-modify-
        sei                         ; write of the X bit must not straddle one
        lda ACCCON
        and #$FB
        ldx curbuf
        beq :+
        ora #$04
:       sta ACCCON
        plp
  .endif
        lda curbuf
        beq :+
        lda #<(SPRREC+MAXREC*10)
        sta recp
        lda #>(SPRREC+MAXREC*10)
        sta recp+1
        rts
:       lda #<SPRREC
        sta recp
        lda #>SPRREC
        sta recp+1
        rts
```
->
```
select_backbuf:
  .if MODELB
        ldx curbuf                  ; the buffer's ring: its base and end, the two
        lda @bhi,x                  ; derived constants the blitters' wrap tests use,
        sta ringbhi                 ; and the row table from its base
        lda @ehi,x
        sta ringehi
        sec
        sbc #3
        sta ringe3
        lda #0
        sbc ringehi                 ; C = 1 still: ringehi >= 3, so the sbc #3 kept it
        sta ringneg
  .else
        php                         ; the ISR writes ACCCON's D bit; this read-modify-
        sei                         ; write of the X bit must not straddle one
        lda ACCCON
        and #$FB
        ldx curbuf
        beq :+
        ora #$04
:       sta ACCCON
        plp
  .endif
        lda @rlo,x                  ; X = curbuf (0/1): its sprite record base
        sta recp
        lda @rhi,x
        sta recp+1
  .if MODELB
        jmp build_ring              ; the row table from the base (recp untouched)
@bhi:   .byte >RING_A, >RING_B
@ehi:   .byte >RINGEND_A, >RINGEND_B
  .else
        rts
  .endif
@rlo:   .byte <SPRREC, <(SPRREC+MAXREC*10)
@rhi:   .byte >SPRREC, >(SPRREC+MAXREC*10)
```

recp = SPRREC + curbuf*MAXREC*10 is read from a two-entry table indexed by X = curbuf. This replaces the branch and the two pairs of immediate loads. The Model B does the loads while X is still curbuf, before build_ring, and then tail-jumps into build_ring. That removes jsr/rts, jmp @recs and lda curbuf/beq, and the @bhi/@ehi tables move behind the jmp. The Model B's second sec is dropped because C=1 is already known.

Review: Lines 3030-3071 match. curbuf is 0/1. X = curbuf at the table loads on both targets (Master: plp keeps X; Model B: nothing after ldx curbuf changes X). Master exit A, X, N, Z, C and V are identical (A = >recp via the table; C from plp in both). Model B: C=1 after sbc #3 because ringehi is $44 or $80, so dropping the sec is safe. build_ring (TILCODE, the same bank) does not read recp or A on entry and does not touch the stack, so the tail jmp returns to farcall/menu's jsr as before. Exit A and flags now come from build_ring, but the next code is calc_ring (lda wcy) and, for the menu, jsr m_calc_ring (farjsr = ldx #/jsr farcall), so neither is read. Exit X and Y are unchanged (build_ring's). One ':' goes with its only beq; the Master's 3055->3057 is kept, and no :- before 3181. Assembled: Master CODE -7, Model B TILCODE -12. Supersedes 8 and 9.

## r015:8  src/engine.s:3040-3042  (window i2019)  M -0 B -1  [high]  review: accept

Cycles: Model B only: saves 2 cycles.

Assumption: FALLBACK for F: apply only if F was not applied. C = 1 after sec/sbc #3 at 3037-3038 because ringehi >= 3 (the RINGEND_A/B high bytes are $44 and $80). sta and lda #0 preserve C.

```
        lda #0
        sec
        sbc ringehi
```
->
```
        lda #0
        sbc ringehi                 ; C = 1 still: ringehi >= 3, so the sbc #3 kept it
```

lda #0/sec/sbc ringehi is equivalent to lda #0/sbc ringehi when C is already 1.

Review: FALLBACK for index 7: correct on its own, but only if 7 is not applied. Model B only: after sec/sbc #3 with ringehi >= 3, C=1, and sta/lda #0 preserve it. Assembled: Model B TILCODE -1.

## r015:9  src/engine.s:3058-3060  (window i2031)  M -1 B -0  [high]  review: accept

Cycles: Master: lda curbuf (3 cycles) becomes txa (2 cycles).

Assumption: FALLBACK for F: apply only if F was not applied. On the Master, X = curbuf from ldx curbuf at 3054 and plp preserves it. The Model B keeps lda curbuf.

```
        plp
  .endif
        lda curbuf
```
->
```
        plp
        txa                         ; X = curbuf: Z for the beq
  .endif
  .if MODELB
        lda curbuf
  .endif
```

txa sets Z from X = curbuf exactly as lda curbuf does, and A gets the same value.

Review: FALLBACK for index 7: correct on its own, but only if 7 is not applied. Master: X = curbuf from ldx curbuf at 3054, and plp preserves X. txa gives the same A and N/Z as lda curbuf, and C is untouched either way. The Model B keeps lda curbuf. Assembled: Master CODE -1, Model B unchanged.

## r016:0  src/engine.s:3029-3048  (window i2048)  M -0 B -7  [high]  review: accept

Cycles: Model B: original ldx..jmp @recs = 3+4+3+4+3+2+2+3+2+2+3+3+jsr6+jmp3 = 43; proposal 3+4+3+2+2+3+2+3+2+2+3+jsr6 = 35 (8 faster). Master: all inside .if MODELB, unchanged.

Assumption: RING_A=$0A80, RING_B=$4680, RINGEND_A=$4400, RINGEND_B=$8000 (engine.s:129-135): >RINGEND - >RING = $3A for both, asserted; $0A+$3A=$44 and $46+$3A=$80 so the adc never carries (C=0), $44-3/$80-3 never borrow (C=1). build_ring (engine.s:4673) reads no flags on entry (lda #<RING_A first) and does not touch ringbhi..ringneg. The TILCODE byte before select_backbuf is an rts (t_b.lst: 00070C '@no: rts'), so nothing falls through into the table. select_backbuf is reached only by symbol (FAR table banks.s:20, TOMAIN, render_frame on Master): no fixed address or .assert on it. Cheap labels @bhi/@ehi/@recs referenced only inside this block (removed together).

```
        PLACE "CODE", "TILCODE"     ; Model B: bank 5 (the menus are there too)
select_backbuf:
  .if MODELB
        ldx curbuf                  ; the buffer's ring: its base and end, the two
        lda @bhi,x                  ; derived constants the blitters' wrap tests use,
        sta ringbhi                 ; and the row table from its base
        lda @ehi,x
        sta ringehi
        sec
        sbc #3
        sta ringe3
        lda #0
        sec
        sbc ringehi
        sta ringneg
        jsr build_ring
        jmp @recs
@bhi:   .byte >RING_A, >RING_B
@ehi:   .byte >RINGEND_A, >RINGEND_B
@recs:
```
->
```
        PLACE "CODE", "TILCODE"     ; Model B: bank 5 (the menus are there too)
  .if MODELB
sb_bhi: .byte >RING_A, >RING_B      ; ahead of the entry, so no jmp over it
.assert >RINGEND_B - >RING_B = >RINGEND_A - >RING_A, error, "ring ends a fixed distance above the bases"
  .endif
select_backbuf:
  .if MODELB
        ldx curbuf                  ; the buffer's ring: its base and end, the two
        lda sb_bhi,x                ; derived constants the blitters' wrap tests use,
        sta ringbhi                 ; and the row table from its base
        clc
        adc #>RINGEND_A - >RING_A   ; the end: the same distance above either base
        sta ringehi
        sbc #2                      ; C = 0 from the adc (no carry out): A - 3
        sta ringe3
        eor #$FF                    ; C = 1 (no borrow): 2 - ringehi
        sbc #2                      ; -ringehi
        sta ringneg
        jsr build_ring
```

ringbhi: same table value, table renamed sb_bhi (a normal label placed before select_backbuf, so select_backbuf's @-scope is unaffected). ringehi: table >RINGEND_x == ringbhi + $3A (asserted); clc/adc gives it. ringe3: original sec/sbc #3 = ringehi-3; sbc #2 with C=0 = ringehi-3. ringneg: original 0-ringehi; eor #$FF of (ringehi-3) = 2-ringehi, sbc #2 with C=1 = -ringehi (checked: $44 -> $BC, $80 -> $80). jsr build_ring unchanged; jmp @recs removed because the table no longer sits after it, so execution falls straight into the shared lda curbuf. Flags leaving: build_ring's, as before. Assembled in scratch: TILCODE -7.

Review: Original matches lines 3029-3048. RINGBYTES = $3980, so >RINGEND - >RING = $3A for both buffers ($0A->$44, $46->$80). I assembled a test with ca65: '#>RINGEND_A - >RING_A' comes out as $3A, because unary > binds tighter than binary -. The adc never carries, so C = 0. With C = 0, sbc #2 gives ehi-3 and does not borrow, so C = 1. eor #$FF then gives 2-ehi, and sbc #2 with C = 1 gives -ehi ($44->$BC, $80->$80). build_ring (engine.s:4673) starts with lda #<RING_A and reads no flags. The last TILCODE byte before select_backbuf is an rts (t_b.lst 00070C @no: rts), so the moved table is never executed. @bhi, @ehi and @recs are referenced only here. No anonymous labels are touched, and sb_bhi is a unique name. Model B goes from 34 to 27 bytes and from 43 to 35 cycles. The Master emits exactly what it did before. The removed @ehi table is private to this routine, not shared data.

## r016:1  src/engine.s:3049-3060  (window i2054)  M -1 B -0  [high]  review: accept

Cycles: Master: lda zp 3 -> txa 2 (1 faster). Model B: identical instruction.

Assumption: curbuf is zero page (engine.s:217, listing A5 rr). X = curbuf from ldx curbuf at line 3054; beq/ora/sta/plp do not touch X. Only the following beq reads the flags; txa sets Z from X exactly as lda curbuf does.

```
  .else
        php                         ; the ISR writes ACCCON's D bit; this read-modify-
        sei                         ; write of the X bit must not straddle one
        lda ACCCON
        and #$FB
        ldx curbuf
        beq :+
        ora #$04
:       sta ACCCON
        plp
  .endif
        lda curbuf
```
->
```
  .else
        php                         ; the ISR writes ACCCON's D bit; this read-modify-
        sei                         ; write of the X bit must not straddle one
        lda ACCCON
        and #$FB
        ldx curbuf
        beq :+
        ora #$04
:       sta ACCCON
        plp
  .endif
  .if MODELB
        lda curbuf
  .else
        txa                         ; X = curbuf still (plp restores only P)
  .endif
```

On the Master path X was loaded from curbuf and nothing in between writes X or curbuf, so txa gives A = curbuf with N,Z identical to lda curbuf. Model B keeps lda curbuf. Independent of the i2048 candidate (touches lines 3049-3060 only).

Review: Original matches lines 3049-3060. On the Master path, ldx curbuf sets X = curbuf, and beq/ora/sta/plp leave X alone. No ISR writes curbuf: the only writers are engine.s:3151/3596, main.s, menu.s and init.s. txa gives the same A and the same N,Z for the following beq. lda zp (2 bytes, 3 cycles) becomes txa (1 byte, 2 cycles). Model B is unchanged.

## r016:2  src/engine.s:3084-3091  (window i2076)  M -0 B -4  [medium]  review: accept

Cycles: Model B: stz zp = lda #0 (2) + sta zp (3) = 5 -> dec zp 5, at each of two sites; 4 bytes -> 2 bytes each. Master unchanged (stz abs kept: dec abs would be 6 vs 4).

Assumption: Model B BARBG=$A5, BARDIRTY=$A4 zero page (modelb/src/defs.inc:51-52), no other equate on those addresses. Every write of a nonzero value is lda #1/sta: BARBG game.s:248; BARDIRTY game.s:249, logic.s:3225 (bar_touch), menu.s:221, menu.s:519; the only other writes are stz (engine.s:3087,3091,3603,3604). bar_bg and redraw_hud do not clear the flag (grep); if redraw_hud reaches bar_touch it writes 1, still 1 -> 0. The ISR never writes them. A is dead after each: next is lda BARDIRTY (the : target) and lda wx.

```
        lda BARBG
        beq :+
        jsr bar_bg
        stz BARBG
:       lda BARDIRTY
        beq :+
        jsr t_redraw_hud
        stz BARDIRTY
```
->
```
        lda BARBG
        beq :+
        jsr bar_bg
  .if MODELB
        dec BARBG                   ; only ever set to 1 (game.s:248): 1 -> 0
  .else
        stz BARBG
  .endif
:       lda BARDIRTY
        beq :+
        jsr t_redraw_hud
  .if MODELB
        dec BARDIRTY                ; only ever set to 1: 1 -> 0
  .else
        stz BARDIRTY
  .endif
```

The flag is exactly 1 when the beq falls through, so dec leaves 0 as stz did. Flags after: stz on Model B (lda #0) gives Z=1,N=0; dec 1->0 gives Z=1,N=0 (not consumed anyway). A differs (bar_bg's return value instead of 0) but the next instruction reloads it on both paths. Depends on the invariant 'set only to 1' (medium: a future writer of another nonzero value would break it).

Review: Original matches lines 3084-3091. Every nonzero write to these flags is lda #1/sta (game.s:248-249, menu.s:221, menu.s:519, logic.s:3225 bar_touch). All other writes are stz, there is no inc, and nothing else is equated to $A4/$A5 (defs.inc:51-52). The code only falls through when the value is 1, so dec leaves 0, as stz did. On Model B, stz zp (lda #0/sta zp, 4 bytes, 5 cycles) becomes dec zp (2 bytes, 5 cycles), at two sites. A is dead (the next instruction is lda BARDIRTY or lda wx) and the flags are not consumed. The Master keeps stz. The change relies on the flags only ever being set to 1, which is true today.

## r016:3  src/engine.s:3094-3099  (window i2083)  M -1 B -1  [high]  review: accept

Cycles: 3+3+3+2+3+5=19 -> 3+2+3+3+2+3=16 on both targets (3 faster)

Assumption: wx, wcx zero page on both (engine.s:212-214, listings show 2-byte ops). No ISR reads wcx (grep wcx). Next instruction is lda wy (A reloaded), then and/asl (asl overwrites C).

```
        lda wx
        sta wcx
        lda wx+1
        lsr
        sta wcx+1
        ror wcx
```
->
```
        lda wx+1
        lsr
        sta wcx+1
        lda wx
        ror
        sta wcx
```

wcx+1 = wx+1 >> 1 with C = bit 8; wcx = (C<<7)|(wx>>1) via ror A instead of ror wcx in memory. Same results; final C = bit 0 of wx in both. A ends as wcx instead of wcx+1 but lda wy reloads it, and lda wy also overwrites N,Z.

Review: Original matches lines 3094-3099. wx and wcx are zero page on both targets (A5/85/66 rr in the listings). wcx, wcx+1 and the final C (bit 0 of wx) come out the same. A ends with a different value, but the next lda wy reloads it and sets N,Z, and asl overwrites C before anything reads it. No ISR reads wcx. Both targets go from 11 bytes and 19 cycles to 10 bytes and 16 cycles.

## r016:4  src/engine.s:3130-3130  (window i2090)  M -0 B -2  [high]  review: accept

Cycles: Model B: pha3+lda2+sta zp3+pla4 = 12 -> lda2+sta3 = 5. Master: stza and stz both assemble to stz (identical).

Assumption: Model B NSPR=$A3 zp. A is dead after: next is jsr build_sections (Model B, modelb/src/display.s:178 begins ldx curbuf / lda @cbl,x) and on Master jsr t_build_sections -> engine.s:2757 begins lda curbuf. Flags: both entries overwrite N,Z before any branch; neither macro touches C/V.

```
        stza NSPR
```
->
```
        stz NSPR                    ; A dead: build_sections starts ldx/lda
```

stza preserves A round the store. The callee overwrites A at its first instructions without reading it, so the plain stz macro (A dead) stores the same 0 into NSPR.

Review: Original matches line 3130. On Model B the next instruction is jsr build_sections, which starts with ldx curbuf / lda @cbl,x (display.s:178-180), so A and N,Z are dead. Neither macro touches C or V. stza (pha/lda/sta/pla, 6 bytes, 12 cycles) becomes stz (lda/sta, 4 bytes, 5 cycles). On the Master, stza and stz both emit stz, so the Master path is byte-identical. That path (t_build_sections, then the TOBANKI trampoline's pha/.../pla/jmp build_sections, which starts with lda curbuf) does not use A either.

## r016:5  src/engine.s:3144-3151  (window i2119)  M -2 B -2  [high]  review: accept

Cycles: lda zp 3 + eor # 2 = 5 -> eor zp 3, both targets (2 faster)

Assumption: curbuf is zero page on both. render_done is only a PC probe for tools (tools/phase.mjs, harness.mjs, drawcost.mjs, fbdiff.mjs: pc === A.render_done). Nothing in src or modelb/src does jmp/branch/jsr to it, so the only way in is the fall-through from sta flipreq with A = 1. curbuf is not yet changed at render_done in either version, so harness reads there see the same state.

```
        lda #1
        sta flipreq
render_done:                        ; (label for the phase timer harness)
        ; no wait here: the next logic step runs while the flip is pending and
        ; the next render_frame waits for it before touching the buffer
        lda curbuf
        eor #1
        sta curbuf
```
->
```
        lda #1
        sta flipreq
render_done:                        ; (label for the phase timer harness)
        ; no wait here: the next logic step runs while the flip is pending and
        ; the next render_frame waits for it before touching the buffer
        eor curbuf                  ; A = 1: curbuf ^ 1
        sta curbuf
```

A = 1 from lda #1 (sta leaves A alone). 1 EOR curbuf == curbuf EOR 1 for any value, and N,Z come from the same result. The sta curbuf/rts that follow are unchanged. The harness label still marks the same point (after the flip request, before curbuf toggles).

Review: Original matches lines 3144-3151. Nothing in src or modelb/src references render_done apart from its definition; the tools only compare PC against it. The only way in is the fall-through with A = 1 from lda #1: sta leaves A alone, and the ISR preserves A. 1 EOR curbuf equals curbuf EOR 1, with the same N,Z. lda zp + eor # (4 bytes, 5 cycles) becomes eor zp (2 bytes, 3 cycles) on both targets.

## r016:6  src/engine.s:3253-3256  (window i2155)  M -0 B -0  [high]  review: reject

Cycles: Master (PARALLAX build only): 2+5+2+4=13 -> 2+5+4=11

Assumption: Assembled only under -D PARALLAX, which is not in the default build (build.sh passes it only when PARALLAX is set; Master only). pl_valid is read only by pl_erase's lda/beq (a zero test) and starts as .byte 0; pl_mode is read only by bit pl_mode/bmi.

```
        lda #1
        sta pl_valid,x
        lda #$80
        sta pl_mode
```
->
```
        lda #$80                    ; nonzero: valid; bit 7: draw
        sta pl_valid,x
        sta pl_mode
```

pl_valid only needs to be nonzero, so $80 serves both stores and the lda #1 goes (2 bytes, 2 cycles) in the PARALLAX build. It saves 0 bytes in the shipped builds; recorded for completeness.

Review: I could not break it: pl_valid is only tested by lda/beq in pl_erase, pl_mode only by bit/bmi, and pl_walk reloads A. But it is assembled only under -D PARALLAX, and neither shipped target is built with that flag. It saves 0 bytes on both targets, so it does not meet the brief's 'fewer bytes on at least one target', and the gate never builds or runs it. Not worth the churn.

## r017:0  src/engine.s:3392-3432  (window i2284)  M -9 B -9  [high]  review: accept

Cycles: Common prefix: sta w16/sta w16+1 (6) -> tay/tax (4). Right path: lda w16+1 is dropped (-3) and lda w16 -> tya (-1). The right clamp goes from lda #80/sbc w16 (5) to sbc #80/eor/adc rc_w (7), so right clamp is -4 net, right no-clamp -6, and the right @none exits are faster. Left path: lda w16 -> tya (-1); the sta rc_w before beq is removed (-3); lda w16+1/adc #0/bne (7) -> inx/bne/bcc (6); the lda rc_w reload is removed (-3); lda/sta wcx -> ldx/stx costs the same; the one sta rc_w after the clamp label replaces the pre-store. Every path is 2 to 11 cycles faster and no path is slower. Identical on both targets (no macros).

Assumption: X and Y are dead at entry to drawrect_clip and at every exit. The callers: erase_old (engine.s ~1345-1365): after the call, @next never reads X or Y, @l reloads X from lidx and @erase loads Y. draw_dirty (engine.s ~3680): after the call, dec lcnt/bne @l, then @l does ldy lidx and the loop exit does ldx curbuf. On the Model B, bank5_entry (banks.s:158) does jmp drawrect_clip and returns through callbank -> jsr BANKENTRY / jmp pagelogic (low.s:131); pagelogic passes A, X and Y through untouched. drawrect (engine.s:563) writes X (tax, ldx curbuf, tax) and Y (ldy rc_nt) before reading them, and MIRDIRTY_BODY (banks.s:114) also writes X and Y before reading. w16 and w16+1 are no longer written here: drawrect writes w16+1 (engine.s:604) and w16 (engine.s:613) before ringaddr reads them, nothing in between reads them, and no caller reads w16 after drawrect_clip. rc_w is no longer written on the @none paths: every caller writes rc_w before each call (engine.s 1345, 3680), and only drawrect reads it. drawrect does not use C, A or the flags on entry (it starts with lda rc_h, then sec / cmp). wcx, rc_x and rc_w are all zero page, so ldx/stx zp work.

```
        lda rc_x
        sec
        sbc wcx
        sta w16
        lda rc_x+1
        sbc wcx+1
        sta w16+1
        bpl @right
        ; rel < 0: the visible width is w + rel, and rel is already two's complement,
        ; so add rather than negate-and-subtract.  Only a result whose high byte comes
        ; out exactly 0 survives: anything else is the whole rect off the left edge.
        lda w16
        clc
        adc rc_w
        sta rc_w
        beq @none                   ; Z is still the low sum: sta sets no flags
        lda w16+1
        adc #0                      ; branches do not touch C
        bne @none
        lda wcx
        sta rc_x
        lda wcx+1
        sta rc_x+1
        lda rc_w                    ; rel is now exactly 0, so the right clip is just
        cmp #ROWCHARS+1             ; min(rc_w, ROWCHARS): no need to store it and read
        bcc :+                      ; it back through the general test below
        lda #ROWCHARS
        sta rc_w
:       jmp drawrect
@right: lda w16+1
        bne @none                   ; rel >= 256 -> off right
        lda w16
        cmp #ROWCHARS
        bcs @none                   ; not taken: C = 0 for the adc
        adc rc_w
        cmp #ROWCHARS+1
        bcc :+                      ; not taken: C = 1 for the sbc
        lda #ROWCHARS
        sbc w16
        sta rc_w
:       jmp drawrect
```
->
```
        lda rc_x
        sec
        sbc wcx
        tay                         ; rel lo, kept in Y
        lda rc_x+1
        sbc wcx+1
        tax                         ; rel hi, kept in X (N,Z as the sbc left them)
        bpl @right
        ; rel < 0: the visible width is w + rel, and rel is already two's complement,
        ; so add rather than negate-and-subtract.  Only a result whose high byte comes
        ; out exactly 0 survives: anything else is the whole rect off the left edge.
        tya
        clc
        adc rc_w                    ; the low sum: the width, if it survives
        beq @none
        inx                         ; hi + C = 0 only for hi = $FF with a carry out
        bne @none
        bcc @none                   ; (inx and branches leave C alone)
        ldx wcx                     ; rel is now exactly 0, so the right clip is just
        stx rc_x                    ; min(width, ROWCHARS): the width is still in A
        ldx wcx+1
        stx rc_x+1
        cmp #ROWCHARS+1
        bcc :+
        lda #ROWCHARS
:       sta rc_w
        jmp drawrect
@right: bne @none                   ; Z from the tax: rel >= 256 -> off right
        tya
        cmp #ROWCHARS
        bcs @none                   ; not taken: C = 0 for the adc
        adc rc_w
        cmp #ROWCHARS+1
        bcc :+                      ; not taken: C = 1 for the sbc
        sbc #ROWCHARS               ; the excess e = lo + w - ROWCHARS (C stays 1)
        eor #$FF
        adc rc_w                    ; w - e = ROWCHARS - lo
        sta rc_w
:       jmp drawrect
```

The low byte of rel is kept in Y (tay) instead of w16, and the high byte in X (tax) instead of w16+1. tax sets N and Z from the high byte exactly as the sbc did, so bpl @right and the new @right bne @none (which replaces lda w16+1/bne) test the same flags. Left path: tya gives lo, and adc rc_w gives the same low sum and carry; beq @none is unchanged. The original survives only if (hi + C) & 255 == 0 with hi in 80..FF, which means hi = FF and C = 1: inx/bne tests hi = FF and bcc tests C (inx does not touch C). The width stays in A, because ldx/stx move wcx into rc_x without touching A, so rc_w = min(sum, 80) is stored once after the clamp instead of being stored, reloaded and stored again. Right path: tya replaces lda w16. The clamp needs 80 - lo, which equals w - e where e = lo + w - 80. sbc #80 gives e: C = 1 from the failed bcc, and there is no borrow because A >= 81. eor #FF / adc rc_w with C = 1 then gives ~e + w + 1 = w - e, the same mod 256 as lda #80 / sbc w16. Checked exhaustively in C over every hi, lo and w (16.7M cases): the none/draw decision, rc_w and the rc_x update all match. Assembled in a scratch copy for both targets: drawrect_clip goes from 118 to 109 bytes on each.

Review: Original matches engine.s 3392..3432 exactly. The scratch copy assembles and links on both targets: Master LOW goes from $B6 to $AD bytes and Model B TILCODE from $900 to $8F7, so 9 bytes are saved on each. An exhaustive C check over hi, lo and w (16.7M cases) matches the original on the draw/none decision, on rc_w when drawing and on the rc_x update. The right clamp is correct even when lo+w wraps past 255. X and Y are dead at every exit: erase_old's @next and @l, draw_dirty's loop (ldy lidx) and its exit (ldx curbuf), and pagelogic, which only writes them. drawrect writes X and Y before reading them and does not read flags, C or w16 on entry. w16 is written before it is read everywhere else, and rc_w is written by every caller before each call, so dropping those writes on the @none paths is harmless. The anonymous-label count is unchanged, there are no macros or bank sites here, and the code is not timing-critical. Cycles: every path is equal or faster (right clamp -4, right no-clamp -6, left paths -2 to -11).

## r018:0  src/engine.s:3420-3420  (window i2304)  M -1 B -1  [high]  review: reject

Cycles: jmp 3 -> bne taken 3 on both ways in; both targets same

Assumption: Z = 0 on both entries to line 3420: via 'bcc :+' at 3417 the cmp #ROWCHARS+1 left C=0 so A<81, A!=81, Z=0; via fall-through 'lda #ROWCHARS' (80) sets Z=0 and 'sta rc_w' keeps it. Target line 3432 ':       jmp drawrect' is 22 bytes on (w16, rc_w zp). ca65 ':+' on a line that itself carries ':' refers to the NEXT unnamed label (3432) -- verified in both listings (D0 14 -> jmp drawrect).

```
:       jmp drawrect
```
->
```
:       bne :+                      ; Z = 0 both ways in: to the jmp drawrect below
```

The first 'jmp drawrect' is replaced by a branch always taken to the identical 'jmp drawrect' 22 bytes below. No register or flag changes. Note: this line has no [i] index in the region listing (lines starting ':' were not counted); an earlier region may also report it.

Review: Slower: the original jmp drawrect is 3 cycles; the proposal is bne taken (3) then the jmp drawrect at 3432 (3) = 6 cycles on both targets. The cycle claim leaves out the second jmp.

## r018:1  src/engine.s:3421-3422  (window i2304)  M -2 B -2  [high]  review: accept

Cycles: @right path: lda zp 3 + bne -> bne only: 3 fewer; both targets

Assumption: @right is referenced only by 'bpl @right' at engine.s:3399 (the only @right reference in drawrect_clip's scope 3366..3433), which follows 'lda rc_x+1 / sbc wcx+1 / sta w16+1': at @right A = w16+1 and Z,N are those of the sbc result (sta and bpl do not touch flags).

```
@right: lda w16+1
        bne @none                   ; rel >= 256 -> off right
```
->
```
@right: bne @none                   ; A = w16+1, Z from its sbc: rel >= 256 -> off right
```

lda w16+1 reloads the value A already holds and sets Z to the value Z already has. Following code 'lda w16' reloads A.

Review: @right is reached only from bpl @right at 3399, after lda rc_x+1/sbc wcx+1/sta w16+1, so A = w16+1 and Z comes from that value. The lda reloads the same value and sets the same Z. w16 is zp, so 2 bytes on both targets and 3 cycles fewer.

## r018:2  src/engine.s:3448-3464  (window i2322)  M -20 B -6  [high]  review: accept

Cycles: From after 'adc wcx+1' to the ldx #$FF. Master: hi<$0A 14->13; hi>=$0A 34/40->14. Model B (RINGCHARS=$0730): hi<7 14->13; hi>7 34->29; hi=7,lo<$30 23->19; hi=7,lo>=$30 40->32.

Assumption: Master: RINGCHARS=$0A00 (RINGROWS=32), so <RINGCHARS=0 -- already asserted at engine.s:141 '.assert (RINGCHARS & $FF) = 0'. Model B: RINGCHARS=23*80=$0730, low byte nonzero, takes the .else. ringS/ringS+1 are zp (engine.s:279). X (the row index) is dead: the division reloads it with ldx #$FF. The division's own sec follows, so C at the join is irrelevant. A=low, Y=high on every path at the join, as the division needs. calc_ring is the last thing in LOW (Master) so shrinking moves nothing fixed.

```
        sta ringS+1
        cmp #>RINGCHARS
        bcc :+
        bne @sub
        lda ringS
        cmp #<RINGCHARS
        bcc :+
@sub:   lda ringS
        sec
        sbc #<RINGCHARS
        sta ringS
        lda ringS+1
        sbc #>RINGCHARS
        sta ringS+1
:       ; q = S / 80
        lda ringS                   ; running value: low in A, high in Y
        ldy ringS+1
```
->
```
  .if (RINGCHARS & $FF) = 0         ; Master: the fold is on the high byte alone
        cmp #>RINGCHARS
        bcc :+
        sbc #>RINGCHARS             ; C = 1 from the cmp
:       sta ringS+1                 ; q = S / 80
        tay                         ; running value: low in A, high in Y
        lda ringS
  .else
        tay                         ; Y = high
        lda ringS                   ; A = low
        cpy #>RINGCHARS
        bcc :+
        bne @sub                    ; C = 1 from the cpy
        cmp #<RINGCHARS
        bcc :+
@sub:   sbc #<RINGCHARS             ; C = 1 on both ways in
        sta ringS
        tya
        sbc #>RINGCHARS
        tay
        lda ringS
:       sty ringS+1                 ; q = S / 80: low in A, high in Y
  .endif
```

Original: store hi; if hi<>RC keep; if hi>>RC or (hi==>RC and lo>=<RC) subtract RINGCHARS (16-bit); then A=ringS, Y=ringS+1. Master: with <RC=0 the lo compare always gives C=1 and the low subtraction is lo-0 with C=1 (unchanged, no borrow), so the result is 'hi>=>RC -> hi-=>RC', done in A using the cmp's C=1; stored, then tay/lda ringS gives the same A,Y. Model B: same decision tree; @sub's sec is redundant since both entries have C=1 (bne after cpy with hi>=; fall-through of bcc after cmp); lo already in A on both entries; high taken from Y with the borrow; ringS+1 stored once at the join from Y on every path. Registers at rts unchanged (division unchanged; Y=$FF).

Review: Verified. Master: <RINGCHARS=0 (asserted at 141), so the fold is hi-=$0A when hi>=$0A. A=hi on entry, and cmp leaves C=1 for sbc. At the join A=ringS, Y=hi, ringS+1 is stored, and ldx/sec overwrite the flags. Model B: RINGCHARS=$0730. C=1 on both ways into @sub, and A=lo on both (the lda comes before cpy). Y=hi or hi-borrow, stored once at the join. Byte counts: 31->11 (Master), 31->25 (Model B). Cycle counts rechecked on every path; none slower. Exactly one ':' is assembled per target, same as before. RINGCHARS is defined at line 121, before the .if.

## r018:3  src/engine.s:3475-3480  (window i2356)  M -0 B -2  [high]  review: accept

Cycles: Model B only (inside .if MODELB): 4 fewer (clc 2 + sec 2 removed); single path

Assumption: @dd (3473) is reached only by falling out of 'bpl @div' (no other @dd reference), and 'sec' at 3470 precedes 'dey/bpl' which do not touch C, so C=1 at @dd. The division stops at the first negative 16-bit value, so Y:A is in [-80,-1] and A = rem+176 in [176,255] (needs initial S < 32768: S <= 1839+wcx). So A+80 carries out: C=1 after the adc, making the sec before sbc barq redundant. Bank 7 LGCCODE.

```
        clc                         ; the remainder, A + 80, is where the window starts
        adc #ROWCHARS               ; in its slot: the first char the mirror is read for
        sta wcxm
        lda #RINGROWS-1             ; the window row that lives in the last slot, and
        sec                         ; the map row it shows: the row the mirror follows
        sbc barq
```
->
```
        adc #ROWCHARS-1             ; C = 1 (the sec before dey): A + 80, the remainder,
        sta wcxm                    ; where the window starts in its slot
        lda #RINGROWS-1             ; the window row that lives in the last slot, and
        sbc barq                    ; the map row it shows.  C = 1: the adc carried
```

clc/adc #80 == adc #79 with C=1: same sum, same carry-out, same N/Z/V. sta wcxm and lda # do not touch C, so sbc barq sees C=1 exactly as after the removed sec. sta rstar / clc / adc wcy / sta mrow / rts unchanged; final A, X, Y, flags identical.

Review: @dd is reached only by falling through bpl @div. The sec at 3470 with dey between gives C=1. The loop stops at the first negative 16-bit value with Y starting >= 0 (S is small), so A is in [176,255]. adc #79 with C=1 matches clc/adc #80 in value and flags and carries out, so the sec before sbc barq is redundant. Model B only: 2 bytes, 4 cycles.

## r018:4  src/engine.s:3498-3505  (window i2367)  M -1 B -0  [high]  review: accept

Cycles: Master only (init_tables is .if .not MODELB): 16 -> 12 per iteration

Assumption: init_tables is inside '.if .not MODELB' (engine.s:3488), Master only. C after the sequence is not read: next are lda mask4,y / sta / txa / lsr (overwrites C).

```
@mt:    txa                         ; MASKTABk[x] = mask4[(x >> (6 - 2k)) & 3]
        lsr
        lsr
        lsr
        lsr
        lsr
        lsr
        tay
```
->
```
@mt:    txa                         ; MASKTABk[x] = mask4[(x >> (6 - 2k)) & 3]
        asl                         ; x >> 6 as a 9-bit rotate: bits 7,6 -> 1,0
        rol
        rol
        and #3
        tay
```

asl: C=b7, A=b6..b0,0. rol: C=b6, A=b5..b0,0,b7. rol: C=b5, A=b4..b0,0,b7,b6. and #3 -> b7b6 = x>>6, same as six lsr. Y gets the same value.

Review: asl/rol/rol/and #3 gives b7b6 in bits 1..0, the same as six lsr. The following lda mask4,y and the next txa/lsr overwrite the flags and C. Master only: 6->5 bytes, 12->8 cycles.

## r018:5  src/engine.s:3531-3531  (window i2401)  M -2 B -0  [high]  review: accept

Cycles: Master only: 2 fewer, once

Assumption: Line 3531 is reached only by falling out of 'inx / bne @mt' (3529-3530), so X=0. Master only.

```
        ldx #0
```
->
```

```

ldx #0 when X=0 is a no-op for X; its flags are overwritten by '@t: txa' before any branch.

Review: Reached only by falling out of inx/bne @mt, so X=0. The next instruction, @t: txa, resets the flags. No label on the line. Master only.

## r018:6  src/engine.s:3535-3535  (window i2401)  M -1 B -0  [high]  review: accept

Cycles: Master only: 2 fewer per iteration of @t

Assumption: Immediately preceded by '@t: txa' (3532); lines 3533-3534 are comments. Master only.

```
        txa
```
->
```

```

Duplicate txa: A and flags already equal X after the first. The label @t stays on 3532.

Review: Duplicate txa right after @t: txa (only comments between). A and the flags are already the same.

## r018:7  src/engine.s:3562-3562  (window i2428)  M -2 B -0  [medium]  review: accept

Cycles: Master only: 3 fewer, once

Assumption: build_ring on the Master (engine.s:4673-4697, .else branch) does 'stz w16' and then adds <ROWBYTES = $80 to w16 RINGROWS = 32 times (even), so on return w16 = 32*$80 mod 256 = 0. Depends on build_ring's internals; worth an .assert <(RINGROWS*ROWBYTES) = 0.

```
        stz w16
```
->
```

```

stz w16 writes the 0 that w16 already holds.

Review: The Master build_ring does stz w16 and then adds <ROWBYTES=$80 to it 32 times (RINGROWS=32): 32*$80=$1000, so the low byte is 0 on return. Nothing between the jsr and 3562. Correct as it stands. The suggested .assert <(RINGROWS*<ROWBYTES)=0 would guard against future changes and costs nothing.

## r018:8  src/engine.s:3563-3576  (window i2429)  M -2 B -0  [high]  review: accept

Cycles: Master only: init stz zp 3 -> ldy 2; per iteration lda zp 3 -> tya 2, inc zp 5 -> iny 2

Assumption: Y dead from here to init_tables' rts and after: 3577-3609 use no Y; t_init_tables (TOBANK: jsr pagelogic / jmp init_tables) returns to main.s:84, then jsr build_tileaddr (engine.s:4613, never reads Y) and loadfile (A argument). w16+1 is no longer left at $0A: nothing reads it before writing (scratch). Master only.

```
        stz w16+1
        ldx #0
@m80:   lda w16+1
        sta mulrowhi,x
        lda w16
        sta mulrowlo,x
        clc
        adc #ROWCHARS
        sta w16
        bcc :+
        inc w16+1
:       inx
        cpx #RINGROWS
        bne @m80
```
->
```
        ldy #0                      ; Y = the running high byte
        ldx #0
@m80:   tya
        sta mulrowhi,x
        lda w16
        sta mulrowlo,x
        clc
        adc #ROWCHARS
        sta w16
        bcc :+
        iny
:       inx
        cpx #RINGROWS
        bne @m80
```

High byte kept in Y instead of w16+1: tya replaces lda w16+1 (same A, N/Z), iny replaces inc w16+1 (flags unused: next is inx). Same table contents; A at loop exit unchanged.

Review: Y is not read from 3563 to init_tables' rts. The caller is t_init_tables (jsr pagelogic/jmp), then main.s:85 jsr build_tileaddr (ldx/txa only, no Y) and lda #FI_SPR/loadfile. w16+1 is scratch and never read before being written. tya/iny give the same table contents, and the inc flags were not used. Master: stz zp 2 -> ldy 2, lda zp 2 -> tya 1, inc zp 2 -> iny 1: 2 bytes, and no path slower.

## r018:9  src/engine.s:3577-3578  (window i2431)  M -3 B -0  [high]  review: accept

Cycles: Master only: 4 -> 2, once

Assumption: At @m80's exit A is the last value stored to w16: <(RINGROWS*ROWCHARS) = <RINGCHARS = 0 on the Master, asserted at engine.s:141. Master only. Holds with or without the @m80 Y candidate.

```
        ldx #0
        lda #0
```
->
```
        tax                         ; A = <RINGCHARS = 0 (engine.s:141): X = A = 0
```

Original: X=0, A=0, Z=1, N=0, C unchanged. tax with A=0: same. @m loop identical.

Review: At @m80's exit A = the last adc result = <(32*80) = 0 (Master RINGCHARS low byte asserted 0; w16 starts at 0 with or without cand 7; cpx/inx do not touch A). tax gives X=0, A=0, Z=1, N=0 with C untouched, the same as ldx #0/lda #0. 4->1 bytes.

## r019:0  src/engine.s:3577-3584  (window i2432)  M -4 B -0  [high]  review: accept

Cycles: Master only (inside .if .not MODELB at engine.s:3488): ldx #0 + lda #0 (4 cy) removed; loop body identical (sta abs,x is always 5 cy, page crossing or not); 4 cycles faster once. Model B: not assembled.

Assumption: init_tables is Master-only (.if .not MODELB engine.s:3488..3613), where RINGROWS = 32 (engine.s:105) and ROWCHARS = 80, so the @m80 loop's last `adc #ROWCHARS` leaves A = <(32*80) = <$0A00 = 0 (the bcc/inc w16+1/inx/cpx/bne after it do not touch A); the .assert guards it. X = RINGROWS at the loop exit (cpx #RINGROWS / bne). sprmul5 is absolute in TABLES ($0C60 in build/labels.txt), so sprmul5-RINGROWS = $0C40 is still absolute,x (no zp wrap); RINGROWS+MAXSPR = 64 fits cpx #imm. Verified by assembling a scratch copy: Master LOGIC 9572 -> 9568.

```
        ldx #0
        lda #0
@m:     sta sprmul5,x
        clc
        adc #5
        inx
        cpx #MAXSPR
        bne @m
```
->
```
        .assert <(RINGROWS*ROWCHARS) = 0, error, "init_tables: A = 0 after the mulrow loop"
@m:     sta sprmul5-RINGROWS,x      ; X runs on from RINGROWS, A = 0 from the last adc
        clc
        adc #5
        inx
        cpx #RINGROWS+MAXSPR
        bne @m
```

Original: X=0, A=0, then stores A at sprmul5+X for X=0..MAXSPR-1 with A += 5. New: X runs RINGROWS..RINGROWS+MAXSPR-1 and stores at (sprmul5-RINGROWS)+X = sprmul5+(X-RINGROWS), i.e. the same addresses 0..MAXSPR-1 in the same order with the same A sequence 0,5,10,...; A starts at 0 because the previous loop's final low byte of w16 is <(RINGROWS*ROWCHARS) = 0. clc per iteration unchanged. Exit state differs only in X (RINGROWS+MAXSPR vs MAXSPR), and the next instructions are stz/lda/sta that never read X (engine.s:3585-3609, rts); the carry/A exit values are identical.

Review: The original text matches. This code is Master-only (.if .not MODELB), where RINGROWS=32 and ROWCHARS=80, so @m80 exits with X=32 and A=<(32*80)=0. sprmul5-32 ($0C40) stays absolute,x. The stores and the A sequence are the same, and X is not read afterwards (stz/lda/sta, then rts). Scratch assembly: Master LOGIC -4, Model B unchanged.

## r019:1  src/engine.s:3621-3649  (window i2446)  M -6 B -10  [high]  review: accept

Cycles: Per buffer, store path: asl/sta tmp3/txa/asl x5/adc tmp3 = 20 cy -> asl/cpx/bcc = 7 (buffer 0) or asl/cpx/bcc/adc = 8 (buffer 1). Loop tail inx/cpx #2/bne (7 taken, 6 out) -> dex/bpl (5 taken, 4 out). Full path unchanged (lda/cmp/bcs). Model B additionally: entry ldx farx (4) + stx tmp2 (3) removed, lda farx (abs, 4) replaces lda tmp2 (zp, 3) on each store path: at most +2, net -5 or better. Faster on every path on both targets.

Assumption: DIRTYCNT is 0..16 (capped by the cmp #16 here, zeroed by draw_dirty/init) so cnt*2 < 32 and C = 0 after the asl; the two buffers' lists are independent (buffer x only touches DIRTYCNT+x and DIRTYLIST+32x..), so processing 1 then 0 gives identical memory. Model B: mark_dirty is reached only through the far table entry mark_dirty_x (modelb/src/banks.s:34; TOMAIN "mark_dirty" is inside .if .not MODELB, engine.s:4742) and farx is written only by m_mark_dirty (engine.s:4813 stx farx, grep: no other writer, not touched by farcall or the IRQ), so farx still holds ty when the stores read it; tmp2 is then never written on the Model B, and nothing reads it after (logic.s has no tmp/tmp2 reference). Exit state after the change: X = $FF (was 2), C = 0 or 1 (was 1), N/Z differ, A = buffer 0's value (was buffer 1's), Y = buffer 0's index (was buffer 1's), tmp3 no longer written. All dead: mark_dirty's only caller is m_mark_dirty (Master: TOMAIN jsr mark_dirty / jmp pagelogic, engine.s:4766, pagelogic preserves A/X/Y/C, pla sets N,Z; Model B: farcall F_MARKDIRTY, low.s:42 fcret returns X = caller bank, A = callee A, Y and C intact). m_mark_dirty call sites logic.s:3095,3099,3141,3145: 3095 -> lda q4 / inca (C preserved) / ldx q5 / jsr m_mark_dirty (mark_dirty consumes no C/Y on entry); 3099 -> lda fe / cmp #48 (C redefined) / stza fe / rts to process_object: staged path logic.s:1984 does setbank / ldy obj, tail path returns to logic.s:1059 (setbank; ldx bent; lda LV_BNEXT,x; @walk: cmp; ... tax; lda LV_BOBJ,x; tay -- Y and X reloaded; @cellend lda/cmp) or logic.s:1100 (inc BINI; @rl2: ldx BINI; ldy LV_BINOTH,x); 3141 -> lda fa / inca / ldx q5 / jsr; 3145 -> inc q5 / dec q4 / bne @rl: ldx fa / lda q5 / jsr maptile (maprow, mapbyte, mapput consume no carry: engine.s:4513-4560) or @set: lda #1 / sta fd / lda fd / clc / adc #100 / jmp m_addsprite (addsprite: ldx NSPR; ldy sprmul5,x). X already differs between the targets on return (Master 2, Model B the bank number) and they run lock-step, so no caller reads it. tmp3: not referenced in logic.s; its readers (renderer mtab, modelb display.s build_sections) write it first. Verified by assembling a scratch copy of both targets: Master LOW2 -6, Model B TILCODE -10.

```
  .if MODELB                        ; far table, which takes X: m_mark_dirty parks it)
mark_dirty_x:
        ldx farx
  .endif
mark_dirty:                         ; A = tx, X = ty  (adds to both buffers' lists)
        sta tmp
        stx tmp2
        ldx #0
@b:     lda DIRTYCNT,x
        cmp #16
        bcs @next
        asl
        sta tmp3
        txa
        asl
        asl
        asl
        asl
        asl                         ; x*32 -- X is 0 or 1, so the fifth asl cannot
        adc tmp3                    ; carry out and the clc is dead
        tay
        lda tmp
        sta DIRTYLIST,y
        lda tmp2
        sta DIRTYLIST+1,y
        inc DIRTYCNT,x
@next:  inx
        cpx #2
        bne @b
```
->
```
  .if MODELB                        ; far table, which takes X: m_mark_dirty parks it)
mark_dirty_x:                       ; (the only entry here: ty stays in farx)
  .endif
mark_dirty:                         ; A = tx, X = ty  (adds to both buffers' lists)
        sta tmp
  .if .not MODELB
        stx tmp2
  .endif
        ldx #1
@b:     lda DIRTYCNT,x
        cmp #16
        bcs @next
        asl                         ; cnt*2, C = 0 (cnt < 16)
        cpx #1
        bcc @b0                     ; buffer 0: its list is at 0
        adc #31                     ; buffer 1: C = 1, so this adds 32
@b0:    tay
        lda tmp
        sta DIRTYLIST,y
  .if MODELB
        lda farx                    ; ty, where m_mark_dirty parked it
  .else
        lda tmp2
  .endif
        sta DIRTYLIST+1,y
        inc DIRTYCNT,x
@next:  dex
        bpl @b
```

Y index: original computes Y = cnt*2 + x*32 via tmp3 (x in {0,1}); new: asl gives cnt*2 with C = 0 (cnt<16 after bcs not taken); cpx #1 sets C = (x>=1); for x=0 bcc skips (Y = cnt*2), for x=1 adc #31 with C=1 adds 32 (result <= 62, no carry). Stores and inc identical. Loop: ldx #1 ... dex / bpl visits x=1 then x=0 instead of 0 then 1; per-buffer work is independent so the final memory is identical. Model B: instead of copying farx to X then X to tmp2, the store reads ty straight from farx; Master keeps stx tmp2 / lda tmp2. New cheap label @b0 is inside mark_dirty's scope (only @b, @next there). No bank-site macros in the routine. Fallbacks follow (same lines, overlapping): apply this one first; if it fails, the next.

Review: The index math is right: cnt<16 on the store path, so asl leaves C=0; cpx #1 gives C=x; adc #31+C adds 32. Buffer x only touches DIRTYCNT+x and DIRTYLIST+32x, so doing 1 before 0 gives the same memory. Model B: the only entry is the FARTAB mark_dirty_x (banks.s:34). farx is written only by m_mark_dirty (engine.s:4813) and is not touched by farcall or the IRQ. The exit A/X/Y/C change (X=$FF, C varies, Y/A are buffer 0's) is dead at every site. Master: pagelogic passes A/X/Y/C through. Model B: fcret passes C and Y. Sites logic.s:3095/3141 reload A/X (inca keeps C, and mark_dirty reads no C/Y on entry). Site 3099 does cmp #48 then rts into process_object's staged path, which does setbank / ldy obj and returns to logic.s:1059 (setbank; ldx bent; lda) or 1100 (inc BINI; ldx BINI). Site 3145 loops to ldx fa / lda q5 / jsr maptile (maprow tay or lsr/ror, mapbyte) or goes to @set (lda/clc/adc) then addsprite (ldx NSPR/ldy sprmul5,x). tmp3 and tmp2 readers all write them first. Scratch assembly of both targets: Master LOW2 -6, Model B TILCODE -10.

## r019:2  src/engine.s:3628-3649  (window i2446)  M -6 B -6  [high]  review: accept

Cycles: Same as the combined candidate without the farx part: store path 20 -> 7/8 cy per buffer, loop tail 7/6 -> 5/4; both targets.

Assumption: FALLBACK for the combined mark_dirty candidate (overlaps it: apply only if that one was rejected). DIRTYCNT <= 16; buffers independent. Exit state after the change: X = $FF (was 2), C = 0 or 1 (was 1), N/Z differ, A = buffer 0's value (was buffer 1's), Y = buffer 0's index (was buffer 1's), tmp3 no longer written. All dead: mark_dirty's only caller is m_mark_dirty (Master: TOMAIN jsr mark_dirty / jmp pagelogic, engine.s:4766, pagelogic preserves A/X/Y/C; Model B: farcall fcret, low.s:42, returns X = caller bank, A = callee A, Y and C intact). Call sites logic.s:3095,3099,3141,3145 reload A/X/Y or redefine C before use (traced through process_object logic.s:1983-2007, 1059-1100, maptile/maprow/mapbyte/mapput engine.s:4513-4560, @set -> addsprite). tmp3: not referenced in logic.s; its readers write it first.

```
        ldx #0
@b:     lda DIRTYCNT,x
        cmp #16
        bcs @next
        asl
        sta tmp3
        txa
        asl
        asl
        asl
        asl
        asl                         ; x*32 -- X is 0 or 1, so the fifth asl cannot
        adc tmp3                    ; carry out and the clc is dead
        tay
        lda tmp
        sta DIRTYLIST,y
        lda tmp2
        sta DIRTYLIST+1,y
        inc DIRTYCNT,x
@next:  inx
        cpx #2
        bne @b
```
->
```
        ldx #1
@b:     lda DIRTYCNT,x
        cmp #16
        bcs @next
        asl                         ; cnt*2, C = 0 (cnt < 16)
        cpx #1
        bcc @b0                     ; buffer 0: its list is at 0
        adc #31                     ; buffer 1: C = 1, so this adds 32
@b0:    tay
        lda tmp
        sta DIRTYLIST,y
        lda tmp2
        sta DIRTYLIST+1,y
        inc DIRTYCNT,x
@next:  dex
        bpl @b
```

As the combined candidate, minus the Model B farx change: index via asl/cpx #1/bcc/adc #31 (C=1 -> +32), loop reversed with dex/bpl.

Review: This is the fallback for index 1 and is correct by the same argument, without the farx change. Scratch assembly: LOW2 -6 on the Master and TILCODE -6 on the Model B. It overlaps index 1: apply it only if 1 is not applied.

## r019:3  src/engine.s:3632-3641  (window i2446)  M -4 B -4  [high]  review: accept

Cycles: store path 20 -> 7 (x=0) / 8 (x=1) cycles, both targets; other paths unchanged.

Assumption: FALLBACK (overlaps both mark_dirty candidates above: apply only if neither was). X is 0 or 1 (ldx #0 / cpx #2 loop); DIRTYCNT,x < 16 on this path (bcs @next above), so asl leaves C = 0. tmp3 no longer written: not referenced in logic.s; the renderer and modelb display.s write it before reading. The carry after the replaced sequence was 0 and is now 0 (x=0: from cpx; x=1: adc result <= 62), and nothing before the next cmp/cpx reads it.

```
        asl
        sta tmp3
        txa
        asl
        asl
        asl
        asl
        asl                         ; x*32 -- X is 0 or 1, so the fifth asl cannot
        adc tmp3                    ; carry out and the clc is dead
        tay
```
->
```
        asl                         ; cnt*2, C = 0 (cnt < 16)
        cpx #1
        bcc @b0                     ; buffer 0: its list is at 0
        adc #31                     ; buffer 1: C = 1, so this adds 32
@b0:    tay
```

Y = cnt*2 + 32*x either way: asl -> cnt*2 (C=0); cpx #1 -> C = x; x=0 skips; x=1 adc #31 + C = +32.

Review: This fallback is correct: Y = cnt*2 + 32x. The exit carry is 0 as before (the adc result is at most 63), and nothing reads it before inx/cpx. tmp3 is dead. Assembly confirms -4 on both targets. It overlaps indexes 1 and 2: apply it only if neither was applied.

## r019:4  src/engine.s:3659-3663  (window i2477)  M -1 B -1  [high]  review: accept

Cycles: asl x5 = 10 cy -> lsr/ror/lsr/lsr = 8 cy, both targets.

Assumption: curbuf is 0 or 1 (engine.s:217 '0 = main, 1 = shadow'; it indexes DIRTYCNT: .res 2). The carry out is 0 in both versions and is not read (sta lidx, then the loop begins with ldy / lda / asl).

```
        asl
        asl
        asl
        asl
        asl
```
->
```
        lsr                         ; C = curbuf (0 or 1), A = 0
        ror                         ; curbuf * $80, C = 0
        lsr
        lsr                         ; curbuf * 32
```

A = x in {0,1} (txa). lsr: A = 0, C = x. ror: A = x<<7, C = 0. lsr, lsr: A = x<<5 = x*32, C = 0. Original asl x5: x*32, C = 0. Same A, same C, N = Z = (x==0) in both.

Review: curbuf is only ever 0 or 1 (its writers are stz/stza, inc from 0, eor #1 and lda #1). lsr/ror/lsr/lsr gives x*32 with C=0 and N/Z the same as asl x5. It takes 8 cycles against 10 and saves 1 byte on each target (confirmed in the combined assembly).

## r019:5  src/engine.s:3665-3667  (window i2477)  M -0 B -2  [high]  review: accept

Cycles: Master: stz zp 3 cy either way, identical. Model B: stza (pha/lda #0/sta zp/pla = 12 cy) -> stz (lda #0/sta zp = 5 cy): 7 cycles faster per dirty tile.

Assumption: A is dead at @l: both ways in (fall-through after sta lidx, and bne @l after dec lcnt following jsr drawrect_clip) are followed by ldy lidx / lda DIRTYLIST,y, which overwrite A before any use; flags at @l are not read (ldy sets N,Z). rc_x+1 is not read by ldy/lda, so zeroing it two instructions earlier is equivalent.

```
@l:     ldy lidx
        lda DIRTYLIST,y
        stza rc_x+1
```
->
```
@l:     stz rc_x+1                  ; A is dead here: loaded just below
        ldy lidx
        lda DIRTYLIST,y
```

Only the position of the zeroing of rc_x+1 moves (before ldy/lda instead of after); on the Model B that frees the macro to be the A-destroying stz (lda #0 / sta) instead of stza (pha/lda/sta/pla), which is legal because A is loaded straight after. The Master's stz/stza are the same instruction.

Review: A is dead at @l on both entries (fall-through and bne @l), because ldy lidx and lda DIRTYLIST,y follow. rc_x+1 ($21) does not alias lidx ($55) or DIRTYLIST. On the Model B, stz expands to lda #0/sta zp, which is 4 bytes and 5 cycles against stza's 6 bytes and 12 cycles. The Master is identical.

## r019:6  src/engine.s:3681-3681  (window i2477)  M -1 B -1  [high]  review: accept

Cycles: lda #2 (2 cy) -> lsr A (2 cy); identical, both targets.

Assumption: A = 4 from the preceding lda #4 / sta rc_w. drawrect_clip (engine.s:3366) starts lda rc_y / sec, so it reads none of the entry flags (the carry changes from ty's bit 7 to 0; N=0,Z=0 as before).

```
        lda #2
```
->
```
        lsr                         ; 4 >> 1 = 2
```

A = 4; lsr gives 2 exactly as lda #2 does; N = 0 and Z = 0 as with lda #2; only C differs (now 0), and the next consumer, drawrect_clip, loads and sets it (sec) before any use.

Review: A=4 from lda #4, so lsr gives 2 with N=0 and Z=0 as before. C becomes 0, but drawrect_clip starts lda rc_y / sec, so nothing reads it. lda #imm (2 bytes) becomes lsr (1 byte), saving 1 byte on each target at the same cycle count.

## r019:7  src/engine.s:3687-3687  (window i2477)  M -0 B -2  [high]  review: accept

Cycles: Master: same stz abs,x (5 cy). Model B: stza (pha 3 / lda #0 2 / sta abs,x 5 / pla 4 = 14) -> stz (lda #0 / sta abs,x = 7): 7 cycles faster.

Assumption: A and N/Z are dead after draw_dirty returns: its only callers are engine.s:3122 (Master: jsr draw_dirty / jsr draw_sprites; draw_sprites starts lda #1, engine.s:1404) and engine.s:3358 (Model B render5: jsr draw_dirty / jmp blank_below; blank_below starts lda wfine, engine.s:2631). The @done early exit is unaffected.

```
        stza DIRTYCNT,x
```
->
```
        stz DIRTYCNT,x              ; A is dead: both callers reload it at once
```

The store of 0 to DIRTYCNT,curbuf is identical; the Model B's stza preserved A (and set N,Z from it via pla), the stz form leaves A = 0, Z = 1 -- neither is read by any caller. X, Y, C unchanged.

Review: draw_dirty's callers are Master engine.s:3122, followed by jsr draw_sprites (lda #1), and Model B render5 at engine.s:3358, followed by jmp blank_below (lda wfine). So A and N/Z are dead on return. The Model B stz form is 2 bytes and 7 cycles smaller; the Master is identical. The combined scratch assembly of indexes 0,1,4,5,6,7 gives Master LOW2 -8 and LOGIC -4, and Model B TILCODE -16, which agrees with the per-candidate counts.

## r020:0  src/engine.s:3779-3899  (window i2593)  M -9 B -0  [high]  review: accept

Cycles: Master only (irq_handler is inside .if .not MODELB). @ldsw exit: jmp @exit 3 -> bne @xit taken 3, then the identical ldy/ldx/lda/rti. @ldt1: lda VIA_T1CL + jmp @notT1 3 -> lda VIA_T1CL + bit irq_x (zp) 3: identical cycle count to the vsync T1 restart. @ldvsync: inc/jsr/jsr/jmp @exit(3) -> inc/jmp @sk(3)/jsr/jsr/fall into @exit: identical. @notT1 no-vsync exit: bne not taken 2 + jmp @exit 3 -> bne not taken 2 + beq @xit taken 3: identical; vsync path (bne taken) untouched. Every path cycle-exact, ignoring +1 page crossing on the new taken branches (in the current Master listing @xit $110A and the branches at $1149/$115C all lie in page $11).

Assumption: (1) Z=0 at line 3810: set by lda #2 at 3808; sta LOADREQ does not touch flags. (2) Z=1 at line 3822: the bne :+ at 3821 fell through. (3) V/N/Z from the new bit irq_x are dead: @notT1 begins lda VIA_IFR (N,Z) and nothing on any path to rti reads V (the only adc, at 3856, rewrites V; rti restores P). irq_x is zero page (engine.s:239; listing shows 86 rr), so bit is 2 bytes/3 cycles. (4) The inlined exit at 3779-3782 is byte-for-byte the @exit sequence at 3902-3905. (5) @xit and @sk are new cheap labels in irq_handler's scope; no clash (assembled clean; Master scan_keys moves $1218->$120F). (6) Branch reach verified in an assembled Master listing: bne @xit -$41, beq @xit -$54, bne @ldt1 +$3B, beq @ldvsync -$2B.

```
        ldy irq_y                   ; @exit inlined: no jmp on the chain-step path
        ldx irq_x
        lda $FC
        rti
@ldcheck:
        cmp #1
        bne @ldt1
        ldx SECIDX                  ; stop asked: only the bar step, a frame boundary,
        cpx DISPSECT                ; makes the switch
        beq @ldsw
        jmp @chain
@ldsw:  ; ---- this restart is a standard frame, not the bar: see load_begin.  The same
        ; hold as the bar's, so R4 lands in the first scanline; R9 = 7 and R6 = BARROWS
        ; are the vsync's pre-arm already, and R12/R13 hold the bar.
        ldy #5
@ldhold: dey
        bne @ldhold
        lda #4
        sta CRTC_IDX
        lda #LDR4
        sta CRTC_DAT
        lda #7
        sta CRTC_IDX
        lda #LDR7
        sta CRTC_DAT
        sta curR7                   ; load_end's first vsync re-phases to LDR4 from this
        lda #$40
        sta VIA_IER                 ; T1 off: the chain is stopped
        lda VIA_T1CL
        lda #2
        sta LOADREQ
        jmp @exit
@ldt1:  lda VIA_T1CL                ; stopped or ending: T1 runs on with its interrupt
        jmp @notT1                  ; off, so its flag is stale: was this the vsync?
@ldvsync:                           ; stopped: the standard frame free-runs; count the
        inc vsyncs                  ; vsync and keep the keys and the sound alive
        jsr scan_keys
        jsr sound_tick
        jmp @exit
@notT1:
        lda VIA_IFR
        and #$02
        bne :+
        jmp @exit
:       ; ---- vsync: restart T1 first (constant latency), counter = vsync->T.  The
        ; latch (how long section 0 lasts) is programmed further down, after the
        ; flip, because with no status bar section 0's length depends on the fine
        ; scroll and so belongs to the buffer that is about to be displayed.
        lda VS2T
        sta VIA_T1LL
        lda VS2T+1
        sta VIA_T1CH
        lda #$02
        sta VIA_IFR
        lda LOADREQ                 ; (after the restart: it sets the chain's phase)
        cmp #2
        beq @ldvsync                ; stopped: T1 runs on with its interrupt off
        cmp #3
        bne :+
        stz LOADREQ                 ; resume: T1's interrupt on again, below
:       lda #$C0
        sta VIA_IER
        ; re-phase: the vsync fired at row curR7, so end this frame at row curR7+5 with
        ; 8-line rows -> T starts exactly 40 lines after the vsync even if the CRTC row
        ; counter had run past its vertical total (which otherwise never recovers)
        lda #9
        sta CRTC_IDX
        lda #7
        sta CRTC_DAT
        lda #6                      ; pre-arm the bar's R6 now, in Q, where the display
        sta CRTC_IDX                ; is already off and a new R6 cannot show: the step
        lda #BARROWS                ; ISR at the bar's start is too close to the second
        sta CRTC_DAT                ; scanline to be trusted with it
        lda #4
        sta CRTC_IDX
        lda curR7
        clc
        adc #QROWS-1-QVSYNC
        and #$7F
        sta CRTC_DAT
        inc vsyncs
        lda flipreq
        beq @noflip
        lda vsyncs
        sec
        sbc flipvs
        cmp #2
        bcc @noflip
        lda vsyncs
        sta flipvs
        lda NEXTSECT
        sta DISPSECT
        lda NEXTBUF                 ; the flip is the section chain moving to the other
        sta dispD                   ; buffer's rows; D follows it, but only from the
        stz flipreq                 ; first playfield section -- the bar needs D = 0
@noflip:
        ; everything section 0 needs comes from the buffer that is about to be
        ; displayed -- its start address, and (with no status bar) its length
        ldx #0
        ldy DISPSECT                ; held in Y: SECIDX wants the same byte below
        beq :+
        ldx #2
:       lda BUF_SEC0T1,x
        sta VIA_T1LL
        lda BUF_SEC0T1+1,x
        sta VIA_T1LH
        lda #12
        sta CRTC_IDX
        lda BUF_SEC0,x
        sta CRTC_DAT
        lda #13
        sta CRTC_IDX
        lda BUF_SEC0+1,x
        sta CRTC_DAT
        lda #$40
        sta VIA_IFR
        sty SECIDX
        lda ACCCON                  ; the bar is below $3000: it is only main RAM to the
        and #$FE                    ; CRTC while D = 0
        sta ACCCON
        jsr scan_keys
```
->
```
@xit:   ldy irq_y                   ; @exit inlined: no jmp on the chain-step path
        ldx irq_x
        lda $FC
        rti
@ldcheck:
        cmp #1
        bne @ldt1
        ldx SECIDX                  ; stop asked: only the bar step, a frame boundary,
        cpx DISPSECT                ; makes the switch
        beq @ldsw
        jmp @chain
@ldsw:  ; ---- this restart is a standard frame, not the bar: see load_begin.  The same
        ; hold as the bar's, so R4 lands in the first scanline; R9 = 7 and R6 = BARROWS
        ; are the vsync's pre-arm already, and R12/R13 hold the bar.
        ldy #5
@ldhold: dey
        bne @ldhold
        lda #4
        sta CRTC_IDX
        lda #LDR4
        sta CRTC_DAT
        lda #7
        sta CRTC_IDX
        lda #LDR7
        sta CRTC_DAT
        sta curR7                   ; load_end's first vsync re-phases to LDR4 from this
        lda #$40
        sta VIA_IER                 ; T1 off: the chain is stopped
        lda VIA_T1CL
        lda #2
        sta LOADREQ
        bne @xit                    ; (Z = 0: lda #2)
@ldvsync:                           ; stopped: the standard frame free-runs; count the
        inc vsyncs                  ; vsync and keep the keys and the sound alive
        jmp @sk
@ldt1:  lda VIA_T1CL                ; stopped or ending: T1 runs on with its interrupt
        bit irq_x                   ; 3-cycle pad for the jmp it replaces: off, so its flag
                                    ; is stale: was this the vsync?
@notT1:
        lda VIA_IFR
        and #$02
        bne :+
        beq @xit                    ; (Z = 1: the bne fell through)
:       ; ---- vsync: restart T1 first (constant latency), counter = vsync->T.  The
        ; latch (how long section 0 lasts) is programmed further down, after the
        ; flip, because with no status bar section 0's length depends on the fine
        ; scroll and so belongs to the buffer that is about to be displayed.
        lda VS2T
        sta VIA_T1LL
        lda VS2T+1
        sta VIA_T1CH
        lda #$02
        sta VIA_IFR
        lda LOADREQ                 ; (after the restart: it sets the chain's phase)
        cmp #2
        beq @ldvsync                ; stopped: T1 runs on with its interrupt off
        cmp #3
        bne :+
        stz LOADREQ                 ; resume: T1's interrupt on again, below
:       lda #$C0
        sta VIA_IER
        ; re-phase: the vsync fired at row curR7, so end this frame at row curR7+5 with
        ; 8-line rows -> T starts exactly 40 lines after the vsync even if the CRTC row
        ; counter had run past its vertical total (which otherwise never recovers)
        lda #9
        sta CRTC_IDX
        lda #7
        sta CRTC_DAT
        lda #6                      ; pre-arm the bar's R6 now, in Q, where the display
        sta CRTC_IDX                ; is already off and a new R6 cannot show: the step
        lda #BARROWS                ; ISR at the bar's start is too close to the second
        sta CRTC_DAT                ; scanline to be trusted with it
        lda #4
        sta CRTC_IDX
        lda curR7
        clc
        adc #QROWS-1-QVSYNC
        and #$7F
        sta CRTC_DAT
        inc vsyncs
        lda flipreq
        beq @noflip
        lda vsyncs
        sec
        sbc flipvs
        cmp #2
        bcc @noflip
        lda vsyncs
        sta flipvs
        lda NEXTSECT
        sta DISPSECT
        lda NEXTBUF                 ; the flip is the section chain moving to the other
        sta dispD                   ; buffer's rows; D follows it, but only from the
        stz flipreq                 ; first playfield section -- the bar needs D = 0
@noflip:
        ; everything section 0 needs comes from the buffer that is about to be
        ; displayed -- its start address, and (with no status bar) its length
        ldx #0
        ldy DISPSECT                ; held in Y: SECIDX wants the same byte below
        beq :+
        ldx #2
:       lda BUF_SEC0T1,x
        sta VIA_T1LL
        lda BUF_SEC0T1+1,x
        sta VIA_T1LH
        lda #12
        sta CRTC_IDX
        lda BUF_SEC0,x
        sta CRTC_DAT
        lda #13
        sta CRTC_IDX
        lda BUF_SEC0+1,x
        sta CRTC_DAT
        lda #$40
        sta VIA_IFR
        sty SECIDX
        lda ACCCON                  ; the bar is below $3000: it is only main RAM to the
        and #$FE                    ; CRTC while D = 0
        sta ACCCON
@sk:    jsr scan_keys
```

Four cycle-exact rewrites in the load/exit paths, none touching an instruction before a hardware write: (a) line 3779 gets cheap label @xit on the chain step's inlined exit; (b) @ldsw's 'jmp @exit' becomes 'bne @xit' (Z known 0 from lda #2), same 3 cycles, same exit code; (c) the @ldvsync block moves above @ldt1 and becomes 'inc vsyncs / jmp @sk', where @sk labels the main vsync path's 'jsr scan_keys / jsr sound_tick', which falls into @exit: the same instruction stream as the old jsr/jsr/jmp @exit; (d) @ldt1 now falls into @notT1, with 'bit irq_x' (3 cycles, 2 bytes) replacing 'jmp @notT1' (3 cycles, 3 bytes), so the T1 restart latency on that path does not move; (e) @notT1's 'jmp @exit' after 'bne :+' becomes 'beq @xit' (Z known 1), same 3 cycles. Assembled for the Master: -9 bytes. Line 3899 is only relabelled; lines 3896-3898 are left verbatim (a separate candidate touches them).

Review: Original matches lines 3779-3899 exactly. Master-only (irq_handler is inside .if .not MODELB at engine.s:3690; LGCCODE unchanged when assembled). Z=0 at bne @xit (lda #2; sta), Z=1 at beq @xit (bne fell through); V from bit irq_x is dead (no bvc/bvs in the vsync path, scan_keys or sound_tick/music code; adc rewrites V; rti restores P). irq_x is zp (24 rr). Every path cycle-identical: jmp->taken branch 3=3, jmp @notT1 -> bit zp 3=3, jsr/jsr/jmp @exit -> jmp @sk/jsr/jsr/fallthrough. Checked the real linked Master build (irq_handler at $1E7D, not page $11 as the proposer said): every branch in irq_handler, including bne @ldt1, bne :+ at @notT1, bne/beq @xit and beq @ldvsync, stays within page $1F, so no page-crossing change on the timing-critical vsync T1-restart paths. Anonymous label count unchanged; @xit/@sk assemble clean. Measured: Master CODE $1ACD -> $1AC4 (-9), Model B 0.

## r020:1  src/engine.s:3896-3898  (window i2663)  M -3 B -0  [medium]  review: accept

Cycles: Master only (inside .if .not MODELB). lda abs 4 + and # 2 + sta abs 4 = 10 -> lda # 2 + trb abs 6 = 8: 2 cycles faster. This comes after every CRTC write and the T1 latch/IFR writes of the vsync handler, so the ACCCON D write only lands earlier (D = 0 is wanted before the bar, so earlier is safe).

Assumption: A dead after: next is jsr scan_keys, whose first instruction is lda #$7F (engine.s:3916). Flags dead: scan_keys sets its own before any branch. ACCCON ($FE34) reads have no side effects, so the 65C02 TRB read/re-read/write is equivalent to lda/and/sta (same value written: old & $FE). If the combined candidate at 3779-3899 is applied, it leaves these three lines verbatim, so this one still matches afterwards (apply that one first).

```
        lda ACCCON                  ; the bar is below $3000: it is only main RAM to the
        and #$FE                    ; CRTC while D = 0
        sta ACCCON
```
->
```
        lda #1                      ; the bar is below $3000: it is only main RAM to the
        trb ACCCON                  ; CRTC while D = 0
```

trb M with A=1 writes M & ~1 = M & $FE, exactly what lda/and #$FE/sta writes. Only A (1 instead of the old ACCCON&$FE) and Z differ, and both are dead at jsr scan_keys.

Review: Original matches 3896-3898 exactly and still matches verbatim after candidate 0 is applied (applied serially, builds). Master-only. trb abs with A=1 writes ACCCON & $FE, the same as lda/and/sta; the 65C02 RMW dummy read of ACCCON has no side effect. Assembles as A9 01 / 1C 34 FE. A and flags are dead: next is jsr scan_keys (lda #$7F), and @ldvsync's jmp @sk skips it as before. 10 -> 8 cycles; the write comes after every CRTC and T1 write, in Q with the display off, so 2 cycles earlier is harmless. Measured Master -3, Model B 0.

## r020:2  src/engine.s:3920-3936  (window i2688)  M -7 B -8  [high]  review: accept

Cycles: Faster on both. Master: stz abs 4 -> stz zp 3; tsb abs 6 -> tsb zp 5 per pressed key; lda abs 4 + sta zp 3 removed. Model B: stz macro lda#/sta abs 6 -> lda#/sta zp 5; ora abs+sta abs 8 -> ora zp+sta zp 6 per pressed key; lda KEYSCAN/sta keys 7 removed. Not timing-critical (called after all CRTC/VIA timing writes).

Assumption: keys is zero page on both targets (engine.s:294; Model B listing sta keys = 85 rr). KEYSCAN has no other reader or writer (grep src/ and modelb/src: only scan_keys). scan_keys runs only in interrupt context with I set (engine.s:3815, 3899, modelb/src/display.s:123; no cli in either handler), so main code (game.s, logic.s, menu.s, which only read keys) never sees the partial value; no NMI code reads keys. The test tools patch scan_keys to rts and poke keys directly, which is unchanged. KEYSCAN's .res is left in place (layout unchanged). A dead after the rts: the Master goes on to jsr sound_tick then @exit, and the Model B display.s does jmp sound_tick; sound_tick starts lda SFXREQ. The candidate includes instruction i2719 (sta keys), so its lowest full window is i2688, just past this region; r021 may report it too.

```
        stz KEYSCAN
        ldx #10
@k:     lda keytab,x
        sta VIA_ORANH
        lda VIA_ORANH
        bpl :+
        lda keybits,x
  .if MODELB
        ora KEYSCAN
        sta KEYSCAN
  .else
        tsb KEYSCAN
  .endif
:       dex
        bpl @k
        lda KEYSCAN
        sta keys
```
->
```
        stz keys                    ; built in place: the interrupt is atomic to its readers
        ldx #10
@k:     lda keytab,x
        sta VIA_ORANH
        lda VIA_ORANH
        bpl :+
        lda keybits,x
  .if MODELB
        ora keys
        sta keys
  .else
        tsb keys
  .endif
:       dex
        bpl @k
```

Build the result straight into keys instead of into KEYSCAN and then copying it. stz keys followed by tsb keys (or ora keys / sta keys) leaves the same final byte in keys as the old KEYSCAN, so the trailing lda KEYSCAN / sta keys is dropped. The old tail left A = keys, but A is dead at every caller. Assembled: Master scan_keys -7 bytes, Model B (bank 7 LGCCODE) -8 bytes.

Review: Original matches 3920-3936 exactly. keys is zp on both targets (assembles 64 rr on the Master, A9 00 85 rr / 05 rr / 85 rr on the Model B). scan_keys is called only from interrupt context (engine.s:3815, 3899; modelb/src/display.s:123), and every reader of keys (game.s, logic.s, menu.s) is main code, so the partial value built in place is never observed; nothing writes keys except scan_keys and the harness (which patches scan_keys to rts). KEYSCAN has no other user. A dead after rts (Master falls to jsr sound_tick; Model B jmp sound_tick, which starts lda SFXREQ). Faster on both targets. Measured: Master CODE -7, Model B LGCCODE (bank 7) $2A0A -> $2A02 = -8.

## r021:0  src/engine.s:3920-3936  (window i2688)  M -7 B -8  [high]  review: accept

Cycles: Master: stz abs 4 -> stz zp 3; per pressed key tsb abs 6 -> tsb zp 5; tail lda abs 4 + sta zp 3 removed (-7). Model B: lda#0/sta abs 6 -> lda#0/sta zp 5; per pressed key ora abs 4 + sta abs 4 -> ora zp 3 + sta zp 3; tail -7. Every path is faster. The sta ORANH -> lda ORANH spacing is unchanged.

Assumption: keys is zero page on both targets (engine.s:294; listings t_b.lst/t_m.lst: 'sta keys' = 85 rr). KEYSCAN is read nowhere else (grep: only engine.s 3920-3935). This routine is the only writer of keys (grep src/*.s modelb/src/*.s). The readers are main-line code (game.s 263-373, logic.s 1332-1516, menu.s 247-250) and cannot see it mid-scan: scan_keys runs only from the IRQ handlers (engine.s 3815, 3899; modelb/src/display.s 123), and they never cli (grep cli: only engine.s:4239 takeover, disc.s, loader.s). The harness patches scan_keys' first byte with rts (tools/harness.mjs:168) and writes keys itself, so it is unaffected. A and flags are dead on return: every caller goes straight to sound_tick, which starts with lda SFXREQ. [All 10 candidates applied together to a scratch copy assemble and link on both targets: Master CODE -17 bytes, Model B LGCCODE -19, MNUCODE -1, as claimed.]

```
        stz KEYSCAN
        ldx #10
@k:     lda keytab,x
        sta VIA_ORANH
        lda VIA_ORANH
        bpl :+
        lda keybits,x
  .if MODELB
        ora KEYSCAN
        sta KEYSCAN
  .else
        tsb KEYSCAN
  .endif
:       dex
        bpl @k
        lda KEYSCAN
        sta keys
```
->
```
        stz keys
        ldx #10
@k:     lda keytab,x
        sta VIA_ORANH
        lda VIA_ORANH
        bpl :+
        lda keybits,x
  .if MODELB
        ora keys
        sta keys
  .else
        tsb keys
  .endif
:       dex
        bpl @k
```

Build the key bits in keys directly instead of staging them in KEYSCAN and copying them across. stz keys == stz KEYSCAN (start at 0). tsb, or ora+sta, on keys == the same on KEYSCAN. The final lda KEYSCAN/sta keys is then already done. The only difference is A/N/Z on return (A = the last ORANH read instead of keys), which is dead at every caller. KEYSCAN's .res stays in place, unused; it can be reclaimed separately.

Review: Original matches lines 3920-3936. keys is in zero page (listing: sta keys = 85 rr) and KEYSCAN is absolute on both targets. scan_keys is called only from IRQ paths (engine.s 3815, 3899; display.s 123). Each of those goes on to sound_tick (lda SFXREQ), so A and the flags are dead afterwards, and X is unchanged. Main-line code only reads keys and cannot see a half-finished scan. The harness patches scan_keys to rts. Master: stz 3->2, tsb 3->2, and the 5-byte tail goes, so 7 bytes. Model B: 5->4, 6->4, and the tail goes, so 8 bytes. Every path is faster.

## r021:1  src/engine.s:3896-3898  (window i2688)  M -3 B -0  [high]  review: accept

Cycles: Master only: lda abs 4 + and# 2 + sta abs 4 = 10 -> lda# 2 + trb abs 6 = 8. The ACCCON write lands 2 cycles earlier, in vertical blanking, after the last CRTC write.

Assumption: This is Master-only code (inside .if .not MODELB, engine.s:3690-3911), so trb is the native 65C02 opcode. A and flags are dead afterwards: the next instruction is jsr scan_keys, which begins with lda #$7F. Reading ACCCON has no side effect (the original already reads it). A 65C02 RMW re-reads rather than double-writing, so ACCCON gets exactly one write of (ACCCON & $FE), as before.

```
        lda ACCCON                  ; the bar is below $3000: it is only main RAM to the
        and #$FE                    ; CRTC while D = 0
        sta ACCCON
```
->
```
        lda #1                      ; the bar is below $3000: it is only main RAM to the
        trb ACCCON                  ; CRTC while D = 0
```

trb ACCCON with A=1 writes ACCCON AND NOT 1 = ACCCON & $FE, the same single store as lda/and/sta. This is outside the cycle-exact section: it comes after the vsync handler's T1 reload and CRTC writes (3875-3892). The only effect is that the write lands 2 cycles earlier.

Review: The code is inside .if .not MODELB (3690-3911). It comes after the T1 reload and the CRTC writes, so it is outside the cycle-exact section; the D=0 write lands 2 cycles earlier, still in vertical blanking. trb with A=1 writes ACCCON&$FE in a single write (the 65C02 RMW's extra access is a read). The next instruction is jsr scan_keys (lda #$7F), so A and the flags are dead. 8->5 bytes, 10->8 cycles.

## r021:2  src/engine.s:3949-3960  (window i2705)  M -3 B -3  [high]  review: accept

Cycles: Start path, from the stz to ldaz: Master 4+2+4+3+2+6+2 = 23 -> 4+3 = 7; Model B 2+3+2+4+3+2+6+2 = 24 -> 2+3+3 = 8. Play and idle paths unchanged.

Assumption: sfxtab entries (game.s:394) are the addresses of sfx_* data in game.s (Master CODE, Model B LGCCODE $8xxx), so sfxtab-1,x is never 0 and Z=0 at the bne. SFXDUR is read only by the dec at 3958 (grep SFXDUR: 3955, 3958, 3972). Only main-line code writes SFXREQ, and this is IRQ context, so clearing it before the pointer loads makes no visible difference. On Model B stz expands to lda #0/sta zp (SFXREQ=$A6), and tax has already copied A to X.

```
        lda sfxtab-2,x
        sta SFXPTR
        lda sfxtab-1,x
        sta SFXPTR+1
        stz SFXREQ
        lda #1
        sta SFXDUR
@play:  lda SFXPTR+1
        beq @music
        dec SFXDUR
        bne @music
        ldaz SFXPTR                 ; (zp): the first byte needs no index
```
->
```
        stz SFXREQ                  ; (Model B: A = 0 -- the index is in X)
        lda sfxtab-2,x
        sta SFXPTR
        lda sfxtab-1,x
        sta SFXPTR+1
        bne @go                     ; an sfx is never in page 0: straight to its first step
@play:  lda SFXPTR+1
        beq @music
        dec SFXDUR
        bne @music
@go:    ldaz SFXPTR                 ; (zp): the first byte needs no index
```

The original start path sets SFXDUR=1; @play then sees SFXPTR+1<>0, decrements SFXDUR to 0 and falls through to the ldaz. The new path branches there directly. The only state difference is SFXDUR (stale instead of 0), and it is dead. The step path overwrites it at 3972 before it can be decremented again. On @end, SFXPTR+1=0, so the dec is not reached until the next start, which again goes straight to @go. bne @go is 2 bytes, replacing lda #1/sta SFXDUR (5).

Review: tax has already copied the index to X, so the Model B lda #0 from the moved stz destroys nothing live. Every sfxtab entry is a code-segment address with a nonzero high byte, so bne @go is always taken. SFXDUR is referenced only at 3955, 3958 and 3972 (plus its .res). Leaving it stale is harmless: the dec is reached only after a step has set it, and @end zeroes SFXPTR+1. Only main-line code writes SFXREQ, and this runs in IRQ context. @go is a new cheap label in sound_tick's scope and clashes with nothing. Net -3 bytes on both targets (-5 +2), and the start path is much faster.

## r021:3  src/engine.s:3974-3974  (window i2719)  M -1 B -1  [high]  review: accept

Cycles: Step path: -2 cycles on both targets. No other path passes here.

Assumption: sndwrite (SNDWRITE_BODY, engine.s:4007-4029) does not touch carry: it is pha/lda/sta/pla/nop/rts only. The body is the same on both targets (jsr sndwrite resolves to engine.s:4031 in both builds).

```
        clc
```
->
```

```

Delete the clc. The only way here is ldaz / cmp #$FF / beq @end not taken, so A<$FF and the cmp left C=0. Between the cmp and the adc come jsr sndwrite x3, ldy, lda (),y, iny, sta SFXDUR and lda SFXPTR, and none of them alters C. (Model B's ldaz is ldy #0/lda (zp),y, and it comes before the cmp anyway.) So adc #4 sees C=0, as it did with the clc.

Review: The only way here is cmp #$FF with A<$FF, which leaves C=0. Neither the sndwrite body (pha/lda/sta/pla/nop/rts) nor the ldy/lda (),y/iny/sta/lda that follow touch C, so adc #4 still sees C=0.

## r021:4  src/engine.s:3979-3979  (window i2724)  M -0 B -1  [high]  review: accept

Cycles: Master bra 3 -> bne taken 3; Model B jmp 3 -> bne taken 3. Equal.

Assumption: SFXPTR always points into the sfx data in game.s:395-400 (Master CODE, Model B bank 7 $8000-$BFFF), so the inc's result is at most $C0 and never 0.

```
        bra @music
```
->
```
        bne @music                  ; SFXPTR+1 <> 0 after the inc
```

After inc SFXPTR+1, Z reflects the new high byte, which is nonzero, so the bne is always taken, just like the bra. Model B's bra is a 3-byte jmp; bne is 2 bytes.

Review: After inc SFXPTR+1 the high byte points into the sfx data in CODE/LGCCODE and never wraps to 0, so bne is always taken. Master 2->2 bytes; Model B jmp (3 bytes) -> bne (2). Cycles 3=3.

## r021:5  src/engine.s:3980-3984  (window i2729)  M -2 B -2  [medium]  review: accept

Cycles: -2 cycles on both targets (the lda #$FF goes).

Assumption: @end is reached only by beq @end after ldaz/cmp #$FF, so A=$FF on entry (3962 is the only reference to @end). A and flags are dead at @music: Model B reloads with lda MUSON (3987); Master's jmp music_tick reaches lda MUSON (4091); DBGSND also starts with lda MUSON. On Model B stz is lda #0/sta zp, which leaves A=0, still dead.

```
@end:   stz SFXPTR+1
        lda #$DF                    ; channel 2 off
        jsr sndwrite
        lda #$FF
        jsr sndwrite
```
->
```
@end:   jsr sndwrite                ; A = $FF (the end mark): noise off
        lda #$DF                    ; channel 2 off
        jsr sndwrite
        stz SFXPTR+1
```

The two SN76489 writes ($DF = tone 2 volume off, $FF = noise volume off) are each a complete latch+data byte and independent of each other, so their order does not matter. The first write can use the $FF already in A. SFXPTR+1 is now cleared after the writes instead of before; this is IRQ context and nothing reads it in between.

Review: @end is reached only from beq @end after cmp #$FF, so A=$FF on entry. $FF and $DF are each a complete latch+volume byte. Every sfx and music write sends a latch followed by its data byte in the same sequence (sfx b0/b1, set_voice), so it does not matter which register is left latched. A and the flags are dead at @music: Model B does lda MUSON, the Master's music_tick does lda MUSON, and so does DBGSND. Nothing reads SFXPTR+1 in between. -2 bytes and -2 cycles on both targets.

## r021:6  src/engine.s:3990-4002  (window i2741)  M -0 B -3  [high]  review: accept

Cycles: The removed code is unreachable on the Model B; Master unchanged.

Assumption: On the Model B the .if MODELB block ends in rts (3989), with no label between it and the jmp music_tick at 4002 (listing t_b.lst: 5F5 60 rts, 5F6 4C rr rr jmp music_tick). So the jmp, whose target is in MNUCODE (another bank), is dead.

```
  .endif
  .ifdef DBGSND                     ; diagnostic build (DBGSND=1 sh build.sh): while no
        lda MUSON                   ; music plays, re-silence one of the channels play
        bne :+                      ; never writes -- channel 0, channel 1, noise, in
        lda vsyncs                  ; turn, a frame each.  A tone that survives this on
        and #3                      ; hardware is not in the SN76489's registers.
        tax
        lda @dbgsil,x
        beq :+
        jsr sndwrite
:
  .endif
        jmp music_tick
```
->
```
  .else
  .ifdef DBGSND                     ; diagnostic build (DBGSND=1 sh build.sh): while no
        lda MUSON                   ; music plays, re-silence one of the channels play
        bne :+                      ; never writes -- channel 0, channel 1, noise, in
        lda vsyncs                  ; turn, a frame each.  A tone that survives this on
        and #3                      ; hardware is not in the SN76489's registers.
        tax
        lda @dbgsil,x
        beq :+
        jsr sndwrite
:
  .endif
        jmp music_tick
  .endif
```

Turn the .endif at 3990 into .else and close it after the jmp, so the DBGSND diagnostic and jmp music_tick become Master-only. This removes 3 bytes of dead code from bank 7 LGCCODE.

Review: On the Model B the .if MODELB block ends in rts with no label before jmp music_tick, so the jmp and the DBGSND diagnostic are dead there. The nesting (.if / .else { .ifdef / .endif ; jmp } / .endif) is correct. @dbgsil stays defined but unreferenced in Model B DBGSND builds, which is harmless. The anonymous-label change affects only Model B DBGSND builds and stays inside the block.

## r021:7  src/engine.s:4015-4016  (window i2750)  M -1 B -0  [medium]  review: accept

Cycles: Master: lda #0 (2) + sta abs (write on cycle 4) -> nop (2) + stz abs (write on cycle 4). Every hardware write in sndwrite lands on exactly the same cycle as before. Model B unchanged.

Assumption: A is dead after the store: the next use of A is lda #8 (4025), which also resets the flags. 65C02 stz abs ($9C) takes 4 cycles, with a single write on the last cycle and no read of the target.

```
        lda #0
        sta VIA_ORB
```
->
```
  .if .not MODELB
        nop                         ; = lda #0: the store lands on the same cycle
        stz VIA_ORB
  .else
        lda #0
        sta VIA_ORB
  .endif
```

The timing is exact. The ORB=0 write (the SN76489 WE strobe) lands at the same cycle offset from sta VIA_ORANH, and the nop delay up to lda #8/sta VIA_ORB is unchanged. Only A differs (the value pla'd instead of 0), and it is overwritten before use. Model B keeps lda/sta because its stz macro would be no smaller. The Master assembles sndwrite once (sndwrite_m is Model B only).

Review: Master: lda #0 (2 cycles, 2 bytes) + sta abs (4 cycles, write on the last) -> nop (2 cycles, 1 byte) + stz abs (4 cycles, write on the last, no read). The VIA write timing is identical. A is next used at lda #8. Model B is unchanged. The .if .not MODELB inside the macro body resolves at global scope.

## r021:8  src/engine.s:4060-4063  (window i2771)  M -0 B -1  [medium]  review: accept

Cycles: Model B: jsr music_init still costs jsr + rts (12 cycles). Master unchanged.

Assumption: On the Model B, init_maprows (engine.s:4563-4566, segment LGCLO, bank 7) is a bare rts. The only caller of music_init, modelb/src/init.s:80, runs in LGCCODE with bank 7 paged in. ca65 resolves the forward equate (and init.s is included after engine.s anyway). No tool references music_init (grep tools).

```
music_init:
  .if MODELB
        rts
  .else
```
->
```
  .if MODELB
music_init = init_maprows           ; Model B: nothing to decode -- an rts in bank 7
  .else
music_init:
```

Point Model B's music_init at an existing rts in the same bank instead of assembling another one, saving 1 byte of LGCCODE. No normal label that scopes any Model B @label is lost (there is nothing between 4063 and sndwrite_m). A bigger saving lies outside this region: also deleting jsr music_init at modelb/src/init.s:80 saves 3 more bank-7 bytes, and then this line needs nothing under '.if MODELB'.

Review: On the Model B, init_maprows (engine.s 4565, segment LGCLO, bank 7) is a bare rts. The only Model B caller, modelb/src/init.s:80, runs in LGCCODE start7 with bank 7 paged in. No tool references music_init. No @-label scope is lost on the Model B (the next code is sndwrite_m). The Master keeps its label. -1 byte of LGCCODE; cycles equal.

## r021:9  src/engine.s:4087-4087  (window i2788)  M -0 B -1  [high]  review: accept

Cycles: Model B only: jmp 3 -> bcc taken 3. Equal.

Assumption: PBOARD only ever holds BOARD_STD/WATFORD/SOLIDISK = 0/1/2 (defs.inc:89-91; loader.s findram 349-376 stores only those values, which are copied via dsk_board). This path is reached only when X=PBOARD is neither 0 (beq @wr) nor 2 (cpx #2/beq @sol). So X=1, cpx #2 left C=0, and ldx PBANK+1 and sta WRSEL_WATFORD,x leave C alone.

```
        jmp @wr
```
->
```
        bcc @wr                     ; C = 0: PBOARD = 1 < BOARD_SOLIDISK
```

The Watford arm now ends with an always-taken bcc instead of a jmp. @wr then does lda MUSON, so no flag leaks out. MNUCODE (the bank 5 overlay) shrinks by 1 byte.

Review: PBOARD only ever holds 0, 1 or 2 (defs.inc 89-91, loader.s 349-373). Reaching 4085 means X=1, so cpx #2 left C=0, and the ldx and sta keep C. So bcc is always taken, in 3 cycles like the jmp. @wr then does lda MUSON. -1 byte of MNUCODE.

## r022:0  src/engine.s:4272-4289  (window i2943)  M -10 B -10  [high]  review: accept

Cycles: per loop iteration (16x): Master and Model B 42 -> 26 cycles; ldx/sta ULA_PAL/dex/bpl/rts untouched

Assumption: tmp is not read after set_palette by any caller (game.s:256 then stz NSPR; main.s:118 then jmp game_main; menu.s:240 tail jmp m_set_palette / F_SETPAL farcall): original left tmp = @cmyk[0], new leaves tmp untouched. tmp is scratch, ISR does not use it (engine.s:362 comment).

```
:       txa
        and #8
        lsr
        lsr
        sta tmp
        txa
        and #2
        lsr
        ora tmp
        tay
        lda @cmyk,y
        sta tmp
        txa
        asl
        asl
        asl
        asl
        ora tmp
```
->
```
:       txa
        lsr
        lsr                         ; C = bit 1
        and #2                      ; bit 3, as bit 1
        adc #0                      ; + bit 1: the logical colour
        tay
        txa
        asl
        asl
        asl
        asl
        ora @cmyk,y
```

Original index y = (x&8)>>2 | (x&2)>>1 = 2*b3 + b1, via tmp. New: txa; lsr -> A = x>>1, C = b0; lsr -> A = x>>2 (b3 at bit 1, b2 at bit 0), C = b1; and #2 -> A = 2*b3 (C kept); adc #0 -> 2*b3 + b1 (no overflow, max 3). tay as before. Then instead of lda @cmyk,y / sta tmp / txa / asl x4 / ora tmp, do txa / asl x4 / ora @cmyk,y: same A = x<<4 | cmyk[y] (OR commutes). Exit state identical: A = cmyk[0] | 0, Y = 0, X = $FF, C = 0 (asl x4 of x=0 is last), N/Z from dex/bpl; only tmp differs. Anonymous ':' stays on the first txa. Model B: LGCCODE (bank 7). Assembled and measured: Master CODE -10, Model B LGCCODE -10.

Review: Original matches 4272-4289. txa/lsr/lsr leaves C = bit 1, A = x>>2; and #2 keeps bit 3 at bit 1; adc #0 adds bit 1: the index 2*b3+b1, the same as before. ora @cmyk,y gives the same x<<4 | cmyk[y] as the old lda/sta tmp/ora tmp. The ':' label stays on the first txa. The exit state is the same (A, X=$FF, Y=0, C from the last asl) except tmp, which is dead: game.s:256, main.s:118 and menu.s:240 (menu_show, whose callers at menu.s:311/458/637 go to menu_keys, and then store tmp before any read) never read it. Assembled: Master CODE -10, Model B LGCCODE -10. Each iteration is 16 cycles faster, and it is not timing code.

## r022:1  src/engine.s:4199-4199  (window i2879)  M -0 B -2  [high]  review: accept

Cycles: Model B: lda #0 (2) + sta abs (4) = 6 -> lsr abs = 6. Master unchanged (stz kept).

Assumption: MUSON (Model B: low.s:150, LOWBSS, abs) only ever holds 0 or 1: start6 (init.s:37) zeroes LOWBSS; writers are stz MUSON (init.s:74, engine.s:3594, here) and music_start's sta MUSON with A = 1 (engine.s:4194, A preserved through stza). The IRQ only reads it. C after music_stop is not consumed: callers disc.s:291 (-> jsr ld_begin: lda/cmp), engine.s:4418 (Master-only; unaffected), menu.s:528 via farcall F_MUSSTOP (farcall does not use C; then jsr menu_begin whose first ops are jsr/stza). A/flags overwritten by lda #$9F next.

```
        stz MUSON
```
->
```
  .if MODELB
        lsr MUSON                   ; MUSON is 0 or 1: 6 cycles, as lda #0 / sta
  .else
        stz MUSON
  .endif
```

lsr of 0 or 1 leaves 0, exactly the stz result. N/Z are overwritten by lda #$9F; sndwrite does not touch C, so music_stop now returns C = old MUSON bit 0 instead of the caller's C, which nobody reads. Bank 7 LGCCODE -2 (measured).

Review: MUSON is only ever 0 or 1. Its writers are stz (init.s:74, engine.s:3594, engine.s:4199) and music_start's sta with A=1 (stza keeps A). LOWBSS is zeroed at start6. So lsr MUSON leaves 0. On the Model B, lda #0/sta abs (5 bytes, 6 cycles) becomes lsr abs (3 bytes, 6 cycles). lsr is one instruction, so the IRQ cannot see a torn value. N/Z are overwritten by lda #$9F. The C that music_stop now returns is read by nobody: disc.s:291 goes to ld_begin (lda/cmp), and menu.s:528 goes through farcall (fcret reloads A and does not use C) to menu_begin. The Master keeps stz. Assembled: Model B LGCCODE -2.

## r022:2  src/engine.s:4110-4181  (window i2837)  M -13 B -16  [high]  review: accept

Cycles: rest path: saves jsr set_voice/rts (12), phx+plx (Master 7, B 11); adds bne 3 + ora #$90 2: Master -14, B -18. Note path: saves jsr/rts (12), phx+plx (7 / 11), sec+sbc (4): Master -23, B -27. musbyte: cmp #0 (2) -> tay (2) equal. Unchanged elsewhere.

Assumption: set_voice has one caller (engine.s:4115, grep). X is untouched by set_voice's body and by SNDWRITE_BODY (engine.s:4007-4029: pha/lda/sta/pla/nop/rts only), so phx/plx was a no-op for X. Notes in the stream are 0 or 24..95 (tools/midi2snd.py spec; build/music.bin actually 0,38..74), so (note-24)*2 = 2*note-48 with no wrap and MUSIC_TAB-48+2n is the same address. Y is dead after music_tick on both targets (Master irq exit engine.s:3902 reloads Y; Model B low.s:79 ldy irq_y), and on the Master set_voice already clobbered Y, so musbyte's tay is safe. musbyte's callers (only music_tick) use Z/N (bne, sta, cmp) and never C. No anonymous label inside set_voice or sndw.

```
        ldx #0
@v:     jsr musbyte
        cmp MUSNOTE,x
        beq :+
        sta MUSNOTE,x
        jsr set_voice
:       inx
        cpx #3
        bne @v
  .if .not MODELB
        pla
        sta ROMSEL_CPY
        sta ROMSEL
  .endif
@done:  rts

; A = next music byte; MUSPTR += 1.  Preserves X.  Z reflects A.  Bank 6 selected.
musbyte:
        ldaz MUSPTR
        inc MUSPTR
        bne :+
        inc MUSPTR+1
:       cmp #0
        rts


; X = voice (0..2), A = MIDI note (0 = rest)
set_voice:
        tay                         ; the note goes in Y before X is pushed: on a 6502
        phx                         ; phx is txa/pha and would land on it
        txa
        asl
        asl
        asl
        asl
        asl
        sta ISRT1                   ; ch << 5
        cpy #0
        bne @note
        ora #$9F                    ; A is still ch<<5
        sndw
        plx
        rts
@note:  tya
        sec
        sbc #24
        asl
        tay
        lda MUSIC_TAB,y
        and #15
        ora ISRT1
        ora #$80
        sndw
        lda MUSIC_TAB,y
        lsr
        lsr
        lsr
        lsr
        sta ISRT2
        lda MUSIC_TAB+1,y
        asl
        asl
        asl
        asl
        ora ISRT2
        sndw
        plx
        lda musvol,x
        ora ISRT1
        ora #$90
        sndw
        rts
```
->
```
        ldx #0
@v:     jsr musbyte
        cmp MUSNOTE,x
        beq :+
        sta MUSNOTE,x
        tay                         ; set the voice: Y = the note (0 = rest), X = the voice
        txa                         ; (X is not touched: sndw keeps it)
        asl
        asl
        asl
        asl
        asl
        sta ISRT1                   ; ch << 5
        cpy #0
        bne @note
        ora #$0F                    ; rest: A is still ch<<5; attenuation 15
        bne @vol                    ; (always)
@note:  tya
        asl                         ; notes are 24..95: the table indexed from 2*24
        tay
        lda MUSIC_TAB-48,y
        and #15
        ora ISRT1
        ora #$80
        sndw
        lda MUSIC_TAB-48,y
        lsr
        lsr
        lsr
        lsr
        sta ISRT2
        lda MUSIC_TAB-47,y
        asl
        asl
        asl
        asl
        ora ISRT2
        sndw
        lda musvol,x
        ora ISRT1
@vol:   ora #$90
        sndw
:       inx
        cpx #3
        bne @v
  .if .not MODELB
        pla
        sta ROMSEL_CPY
        sta ROMSEL
  .endif
@done:  rts

; A = next music byte; MUSPTR += 1.  Preserves X.  Z reflects A; Y = A.  Bank 6 selected.
musbyte:
        ldaz MUSPTR
        inc MUSPTR
        bne :+
        inc MUSPTR+1
:       tay
        rts
```

Inlines set_voice (one caller) into music_tick's voice loop. Removed phx/plx (X never changes: body + sndwrite preserve it). cpy #0 fall-through only matters for Z; the note path needs no carry any more because sec/sbc #24 is folded into the base: tya/asl/tay gives 2n and MUSIC_TAB-48/-47 give the same addresses as MUSIC_TAB/+1 indexed by 2(n-24). Rest path: A = ch<<5, ora #$0F gives nonzero so bne @vol always, ora #$90 -> $9F|ch<<5, the same single write as ora #$9F. Note path writes the same three bytes; the last sndw then falls into ': inx' (no jsr/rts). musbyte: tay sets N,Z from A like cmp #0 (C no longer forced to 1: unused). Cheap labels @note/@vol are in music_tick's scope (no clash: @sol @wr @v @done). Branch ranges fine (beq :+ ~71 bytes). Measured: Master CODE -13, Model B MNUCODE -16. If this fails, candidates 'set_voice only' and 'musbyte only' below are the fallbacks (they overlap this one).

Review: Original matches 4110-4181. set_voice has one caller (grep over src, modelb/src and tools: no other reference). X is never changed by the set_voice body or by SNDWRITE_BODY (pha/lda/sta/pla/nop/rts), so phx/plx were no-ops for X. On the Model B phx also clobbered A, but txa follows. Notes are 0 or 24..95 (midi2snd.py; the built music.bin uses 0 and 38..74). So MUSIC_TAB-48+2n and -47+2n are the same addresses as the old +2(n-24), with Y <= 190. Rest path: ch<<5|$0F is nonzero, so bne @vol is always taken, and ora #$90 gives $9F|ch<<5, the same single write. Y is kept across sndw, so the note path's second and third table reads are right. musbyte's tay sets N/Z as cmp #0 did. Its callers (only music_tick) never read C, and Y is dead: the Master IRQ exit reloads it from irq_y (engine.s:3902) and so does the Model B stub (low.s:79). music_tick runs at the end of the vsync IRQ, after every timing-critical hardware write, so faster is fine. Anonymous labels: the inlined code defines no ':'; beq :+ still reaches ': inx' (68 bytes on the B), and musbyte's bne :+ still reaches its own ':'. @note/@vol do not clash with @wr/@sol/@v/@done. Assembled: Master CODE -13, Model B MNUCODE -16. Cycles: rest path -14 (M)/-18 (B), note path -23/-27, and no other path changes.

## r022:3  src/engine.s:4137-4181  (window i2854)  M -9 B -12  [high]  review: accept

Cycles: fallback to the inline candidate. rest: Master 43+S -> 32+S, B 47+S -> 32+S (S = sndwrite incl. its rts). note path: Master -20, B -24.

Assumption: Same as the inline candidate: X untouched by set_voice and sndwrite; notes 0/24..95; sole caller engine.s:4115 needs only X preserved (A reloaded by musbyte; flags from inx). sndwrite_m (Model B) and sndwrite (Master) are plain routines that do not inspect the stack, so the tail jmp is safe; the removed rts are not branch targets.

```
set_voice:
        tay                         ; the note goes in Y before X is pushed: on a 6502
        phx                         ; phx is txa/pha and would land on it
        txa
        asl
        asl
        asl
        asl
        asl
        sta ISRT1                   ; ch << 5
        cpy #0
        bne @note
        ora #$9F                    ; A is still ch<<5
        sndw
        plx
        rts
@note:  tya
        sec
        sbc #24
        asl
        tay
        lda MUSIC_TAB,y
        and #15
        ora ISRT1
        ora #$80
        sndw
        lda MUSIC_TAB,y
        lsr
        lsr
        lsr
        lsr
        sta ISRT2
        lda MUSIC_TAB+1,y
        asl
        asl
        asl
        asl
        ora ISRT2
        sndw
        plx
        lda musvol,x
        ora ISRT1
        ora #$90
        sndw
        rts
```
->
```
set_voice:
        tay                         ; the note; X (the voice) is never touched below
        txa
        asl
        asl
        asl
        asl
        asl
        sta ISRT1                   ; ch << 5
        cpy #0
        bne @note
        ora #$0F                    ; rest: A is still ch<<5; attenuation 15
        bne @vol                    ; (always)
@note:  tya
        asl                         ; notes are 24..95: the table indexed from 2*24
        tay
        lda MUSIC_TAB-48,y
        and #15
        ora ISRT1
        ora #$80
        sndw
        lda MUSIC_TAB-48,y
        lsr
        lsr
        lsr
        lsr
        sta ISRT2
        lda MUSIC_TAB-47,y
        asl
        asl
        asl
        asl
        ora ISRT2
        sndw
        lda musvol,x
        ora ISRT1
@vol:   ora #$90
  .if MODELB
        jmp sndwrite_m
  .else
        jmp sndwrite
  .endif
```

phx/plx removed (X preserved anyway). sec/sbc #24 removed by rebasing the table to MUSIC_TAB-48/-47. Both exits become tail jumps into sndwrite(_m); the rest path reaches the shared ora #$90 with A = ch<<5|$0F via an always-taken bne. Measured Master -9, Model B -12. ALTERNATIVE to the inline candidate (overlapping lines): apply only if that one is rejected.

Review: FALLBACK ONLY, for index 2, which covers these lines: apply it only if index 2 is not applied. Once 2 is applied this original no longer matches. It is correct on the same facts as index 2 (X preserved, notes 24..95, rest path ora #$0F/bne always taken to ora #$90). The tail jmp into sndwrite/sndwrite_m is safe: neither routine looks at the stack, and the removed rts's are not branch targets. Assembled: Master CODE -9, Model B MNUCODE -12.

## r022:4  src/engine.s:4132-4132  (window i2849)  M -1 B -1  [high]  review: accept

Cycles: cmp #0 2 -> tay 2, both targets

Assumption: Y dead across musbyte's callers (only music_tick: lines 4102/4108/4111; Y is next set by set_voice's tay; Y restored at IRQ exit on both targets). C after musbyte unused (bne / sta MUSDUR / cmp MUSNOTE,x).

```
:       cmp #0
```
->
```
:       tay                         ; Z (and N) from A; Y is free (music_tick)
```

tay sets N,Z from A exactly as cmp #0 does; only C (cmp set it to 1) and Y differ, neither consumed. Keeps the ':' on the same line. ALTERNATIVE: already included in the inline candidate.

Review: FALLBACK: index 2 already contains this change, so it goes stale once 2 is applied. Taken alone it is correct: tay sets N/Z like cmp #0. C (which cmp #0 forced to 1) is never read by music_tick. Y is dead: on the Master set_voice's first instruction is tay, and the IRQ exit reloads Y on both targets. Assembled: -1/-1, with no change in cycles.

## r022:5  src/engine.s:4191-4193  (window i2875)  M -0 B -10  [high]  review: accept

Cycles: Model B: 3 x stza (pha3 lda2 sta4 pla4 = 13) = 39 -> lsr 2 + 3 x sta 4 + rol 2 = 16. Master unchanged (stz, which is what stza is there).

Assumption: A = 1 on entry to the block (lda #1 at 4189). C after music_start not consumed: caller menu.s:408 falls into menu_begin (reached also by the bne :+ path with arbitrary C; its first ops are jsr m_blank_palette which asl's).

```
        stza MUSNOTE
        stza MUSNOTE+1
        stza MUSNOTE+2
```
->
```
  .if MODELB
        lsr                         ; A = 0, C = 1
        sta MUSNOTE
        sta MUSNOTE+1
        sta MUSNOTE+2
        rol                         ; A = 1
  .else
        stz MUSNOTE
        stz MUSNOTE+1
        stz MUSNOTE+2
  .endif
```

Model B: lsr turns A = 1 into A = 0, C = 1; three sta clear MUSNOTE; rol brings A back to 1 (C in, C = 0 out) for sta MUSON. Order of stores kept (MUSNOTE before MUSON, which the IRQ stub reads). Exit A = 1, N = Z = 0 as before (original pla gave the same); C now 0 instead of the caller's. Master .else keeps stz x3. Measured Model B MNUCODE -10.

Review: A=1 here (lda #1 / sta MUSDUR). lsr gives A=0 and C=1; three sta clear MUSNOTE; rol brings A back to 1 with C=0; then sta MUSON. The stores keep their order (MUSNOTE before MUSON). The exit A and N/Z are the same. C now leaves as 0, which is never read: the only caller is menu.s:408, and menu_begin starts with a jsr through farcall. The Master keeps stz x3, which is exactly what stza was. Assembled: Model B MNUCODE -10. Cycles on the B go from 39 to 16.

## r022:6  src/engine.s:4231-4238  (window i2913)  M -0 B -4  [high]  review: accept

Cycles: Master: stz zp 3 -> sta zp 3. Model B: stza (pha3 lda2 sta3 pla4 = 12) -> sta zp 3. All with I set (sei at entry), so the moved store is invisible to the IRQ.

Assumption: <(40*LINE) = 0 (LINE = 64, engine.s:2750 / defs.inc:72): pinned by the added .assert. A is dead after take_over: Master main.s:117-118 jsr set_palette (ldx/txa); Model B init.s:93 farjsr F_INIT5 (init5 ends jmp take_over) then jsr disc_init which starts lda dsk_type. Flags at exit are not consumed (farcall's fcret reloads A).

```
        sta VIA_T1LL
        lda #>(40*LINE)
        sta VIA_T1CH
        lda #$C2                    ; enable CA1 (vsync) + T1
        sta VIA_IER
        lda #$7F
        sta VIA_IFR
        stza flipreq
```
->
```
        sta VIA_T1LL
        .assert <(40*LINE) = 0, error, "take_over clears flipreq with the latch's low byte"
        sta flipreq                 ; A = 0
        lda #>(40*LINE)
        sta VIA_T1CH
        lda #$C2                    ; enable CA1 (vsync) + T1
        sta VIA_IER
        lda #$7F
        sta VIA_IFR
```

lda #<(40*LINE) leaves A = 0, so sta flipreq there clears flipreq with no stza wrapper; flipreq is still 0 before cli. Exit: A = $7F as before on the Master path (stza kept A anyway) and on Model B (stza preserved $7F; now no stza, A = $7F from lda #$7F). Measured Model B TILLOW -4, Master 0.

Review: The original matches. LINE=64, so <(40*LINE) = 0, which the added .assert pins: A=0 at the new sta flipreq. It runs under the sei at take_over's entry, so flipreq is 0 before cli exactly as before. The exit A is $7F on both targets (it was $7F before: stza kept it), and the N/Z from lda #$7F match what stza's pla gave. Callers do not use A: main.s:118 is set_palette (ldx/txa), and on the B F_INIT5 goes through farcall's fcret and then disc_init loads A itself. TILLOW links, so its asserts hold. Assembled: Model B TILLOW -4, Master 0. Combined with index 7 it measured -2/-6.

## r022:7  src/engine.s:4235-4237  (window i2912)  M -2 B -2  [medium]  review: accept

Cycles: saves lda #$7F: 2 cycles, both targets (init, not timing-critical)

Assumption: Only T1 and CA1 are ever enabled in the system VIA IER (writes: $7F/$C2 here, $40/$C0 at engine.s:3806/3840, display.s:74/158), and every IFR reader looks at bit 6 or bit 1 or masks with IER (engine.s:3699, 3819; display.s:12-16, 140). OLDIRQ is saved but never chained to (grep). The other flags (CA2 keyboard, CB1, CB2, SR, T2) are raised by hardware activity at any time anyway, so leaving them set is a state that already occurs.

```
        sta VIA_IER
        lda #$7F
        sta VIA_IFR
```
->
```
        sta VIA_IER
        sta VIA_IFR                 ; $C2: T1 and CA1, the only sources ever enabled
```

Writing $C2 to IFR clears T1 (bit 6) and CA1 (bit 1), the only flags that can interrupt or are read; bit 7 of an IFR write is ignored. The stale-free state of the two enabled sources before cli is identical. Lines appear verbatim inside the flipreq candidate's proposal, so the two compose (measured together: Master -2, Model B TILLOW -6).

Review: Writing $C2 to IFR clears bits 6 (T1) and 1 (CA1); bit 7 is ignored. IER only ever enables T1 and CA1: the writes here, engine.s:3806/3840 and display.s:74/158 are only ever $40/$C0/$C2/$7F. The UVIA is disabled. So the flags left uncleared (CA2, CB1, CB2, SR, T2) cannot raise an IRQ. No reader looks at them: the Master reads bit 6 (bit/bvs) and bit 1 (and #2), and the B masks with IER, then reads bit 1. No OS routine runs after take_over: the Master's own disc driver replaces OSFILE, OSGBPB is used only before it, OLDIRQ is never restored, and there is no exit to the OS. Take_over is init code, not timing-critical. The new exit A is $C2, which is dead (see index 6). It composes with index 6: its original lines appear verbatim in 6's proposal. Assembled: -2/-2.

## r022:8  src/engine.s:4087-4087  (window i2816)  M -0 B -1  [high]  review: accept

Cycles: Watford path: jmp 3 -> bcc taken 3. Model B only (MODELB block).

Assumption: PBOARD is only ever BOARD_STD 0 / BOARD_WATFORD 1 / BOARD_SOLIDISK 2 (defs.inc:89-91; loader.s:349-376 are the only writers of board, copied via dsk_board to PBOARD). X = 0 is taken by the beq @wr before, X = 2 by beq @sol; so on this path X = 1 and cpx #2 left C = 0. ldx PBANK+1 and sta abs,x do not touch C.

```
        jmp @wr
```
->
```
        bcc @wr                     ; always: X = BOARD_WATFORD (1) < BOARD_SOLIDISK from the cpx
```

bcc @wr is always taken here, reaching @wr with the same registers and flags (C = 0 as it was). Measured Model B MNUCODE -1.

Review: PBOARD is only ever 0/1/2: loader.s findram writes board only from BOARD_STD/WATFORD/SOLIDISK, and it is copied through dsk_board. X=0 takes beq @wr and X=2 takes beq @sol, so this path has X=1, and cpx #2 left C=0. ldx and sta abs,x keep C, so bcc @wr is always taken, 3 cycles like jmp, and it reaches @wr with the same state. The code is Model B only. Assembled: MNUCODE -1.

## r022:9  src/engine.s:4248-4254  (window i2921)  M -2 B -0  [medium]  review: accept

Cycles: Master only: loop 14 x (stx4 lda4 sta4 inx2 cpx2 bne3 = 19) -> 14 x (stx4 lda4 sta4 dex2 bpl3 = 17); last iteration likewise

Assumption: X is dead after crtc_init (main.s:101 is followed by stza's and jsr calc_ring, which loads A/X itself). The CRTC register ORDER changes (R13 down to R0 instead of R0 up to R13): the final register state is identical; this is the one-off start-up setup with the palette already blanked (main.s:80 blank_palette), before take_over starts the chain, not the chain step whose R12/R13-last ordering matters.

```
        ldx #0
:       stx CRTC_IDX
        lda crtctab,x
        sta CRTC_DAT
        inx
        cpx #14
        bne :-
```
->
```
        ldx #13
:       stx CRTC_IDX
        lda crtctab,x
        sta CRTC_DAT
        dex
        bpl :-
```

Same 14 (index, value) pairs written, in descending order; dex/bpl replaces inx/cpx #14/bne. lda crtctab+7 / sta curR7 untouched. Exit X = $FF instead of 14 (dead). Measured Master CODE -2.

Review: Master only. The same 14 (index, value) pairs are written in descending order, and X is dead afterwards: stza does not use it, and calc_ring sets X by tax/ldx before any read. The order does not matter here. main.s has just done VDU 22,1, so the CRTC holds the MOS's MODE 1 values, and crtctab differs only in R8 (interlace) and R10 (cursor). R12/R13 are rewritten with the same $0600, and the palette is blank (blank_palette at main.s:80). This is not the chain step. lda crtctab+7 is kept. Assembled: Master CODE -2, and 28 cycles faster.

## r023:0  src/engine.s:4272-4289  (window i2957)  M -10 B -10  [high]  review: accept

Cycles: per iteration 51->36 (x16), both targets; not timing-critical (palette set-up)

Assumption: tmp no longer written (was left = 7): callers main.s:118 (jmp game_main), game.s:256 (stz NSPR/lda vsyncs), menu.s:240 tail of menu_show (callers menu.s:311/458/637 go to menu_keys, which does not read tmp; 313 writes it) do not read tmp. V at exit now 0 (adc) instead of entry V: no caller branches on V. D flag clear (never set in the game).

```
:       txa
        and #8
        lsr
        lsr
        sta tmp
        txa
        and #2
        lsr
        ora tmp
        tay
        lda @cmyk,y
        sta tmp
        txa
        asl
        asl
        asl
        asl
        ora tmp
```
->
```
:       txa
        lsr
        lsr                         ; C = bit 1
        and #2                      ; bit 3 -> bit 1
        adc #0                      ; Y = bit3*2 + bit1
        tay
        txa
        asl
        asl
        asl
        asl
        ora @cmyk,y
```

txa/lsr/lsr leaves A=x>>2 with C=bit1 of x; and #2 keeps bit3 of x as value 2 (and leaves C); adc #0 adds bit1 -> Y = bit3*2+bit1, identical to ((x&8)>>2)|((x&2)>>1) (checked for all 16 x). Then x<<4 ora @cmyk,y is the same byte as cmyk[y] ora x<<4 via tmp. Exit A=7, X=$FF, Y=0, C=0 (asl of 0), N/Z from dex: unchanged.

Review: Original matches the file. Simulated all 16 x: lsr/lsr leaves C = bit 1, and #2 keeps bit 3 as 2, and adc #0 makes Y = bit3*2+bit1; ora @cmyk,y gives the same byte as the old route through tmp. tmp and ptr are zero page. No caller reads tmp afterwards: main.s:118 goes to jmp game_main (stza...), game.s:256 goes to stz NSPR / lda vsyncs, and after menu.s:240 control goes to menu_keys or rts, with line 313 writing tmp before any read. Exit state is unchanged: A=7, Y=0, C=0. V is now 0 from the adc, and nothing reads V. 26 bytes -> 16 on both targets.

## r023:1  src/engine.s:4297-4307  (window i2979)  M -5 B -5  [high]  review: accept

Cycles: setup 2->4, per iteration 21->9 (last 8); total 337->147, both targets

Assumption: X no longer set to $FF and exit A=$F7 not 7, V=0: every caller reloads/ignores them -- main.s:80 -> disc_init (lda #$20), game.s:232 -> load_begin (Master, lda #1) / stza title_res; ldx level; load_level_b (stx, jsr music_stop), game.s:354 -> load_title_b (jsr ld_begin), menu.s:185/202 via m_blank_palette (Master TOMAIN / Model B farcall, X replaced) then lda title_res / m_wait_flip (lda), modelb/src/init.s:85 -> crtc_init (ldx #8, lda #0).

```
        ldx #15
:       txa
        asl
        asl
        asl
        asl
        ora #7
        sta ULA_PAL
        dex
        bpl :-
        rts
```
->
```
        lda #$F7                    ; (i << 4) | 7, i = 15 down to 0
        sec
:       sta ULA_PAL
        sbc #$10
        bcs :-
        rts
```

Writes the same 16 values $F7,$E7..$07 in the same order (15 down to 0). sec once: each bcs-taken implies C=1 for the next sbc; $07-$10 borrows and exits.

Review: Makes the same 16 ULA writes, $F7 down to $07, in the same order; bcs exits after $07 borrows. Every caller reloads X and A: main.s:80 (disc_init lda), game.s:232 (load_begin lda / stza / lda / ldx level), game.s:354 (load_title_b -> ld_begin lda vsyncs), menu.s:185/202 (lda title_res / m_wait_flip lda) and modelb init.s:85 (crtc_init ldx/lda). The anonymous-label count is unchanged. 16 bytes -> 11 on both targets.

## r023:2  src/engine.s:4212-4238  (window i2944)  M -0 B -2  [high]  review: accept

Cycles: Master 3->3 (stz zp either place); Model B stza 12 (pha3 lda2 sta3 pla4) -> stz 5

Assumption: flipreq is zero page (engine.s:292, .zeropage at 198). Interrupts are off from sei to cli, so storing flipreq before the vector/VIA set-up instead of after is invisible to the IRQ handler; NMI handler does not touch flipreq. TILLOW (Model B B5C) has no size assert (map: 0x88 bytes, init5 + take_over).

```
        sei
  .if .not MODELB
        lda IRQ1V
        sta OLDIRQ
        lda IRQ1V+1
        sta OLDIRQ+1
  .endif
        lda #<irq_handler
        sta IRQ1V
        lda #>irq_handler
        sta IRQ1V+1
        lda #$7F
        sta VIA_IER
        sta UVIA_IER
        lda VIA_ACR
        and #$3F
        ora #$40                    ; T1 continuous
        sta VIA_ACR
        lda #<(40*LINE)
        sta VIA_T1LL
        lda #>(40*LINE)
        sta VIA_T1CH
        lda #$C2                    ; enable CA1 (vsync) + T1
        sta VIA_IER
        lda #$7F
        sta VIA_IFR
        stza flipreq
```
->
```
        sei
        stz flipreq                 ; (A is reloaded next)
  .if .not MODELB
        lda IRQ1V
        sta OLDIRQ
        lda IRQ1V+1
        sta OLDIRQ+1
  .endif
        lda #<irq_handler
        sta IRQ1V
        lda #>irq_handler
        sta IRQ1V+1
        lda #$7F
        sta VIA_IER
        sta UVIA_IER
        lda VIA_ACR
        and #$3F
        ora #$40                    ; T1 continuous
        sta VIA_ACR
        lda #<(40*LINE)
        sta VIA_T1LL
        lda #>(40*LINE)
        sta VIA_T1CH
        lda #$C2                    ; enable CA1 (vsync) + T1
        sta VIA_IER
        lda #$7F
        sta VIA_IFR
```

Model B: stz macro = lda #0 / sta flipreq with A dead (next lda IRQ1V on Master / lda #<irq_handler on Model B reloads A). Exit state identical to original: A=$7F from lda #$7F, N=0/Z=0 from that load (original: pla of $7F), flipreq=0. Master bytes identical.

Review: flipreq is zero page (engine.s:292, inside .zeropage). Interrupts are off from sei to cli, so storing it earlier cannot be seen. A is reloaded next on both targets. Exit A=$7F with N/Z from lda #$7F, the same as after the old pla. TILLOW sits in B5C ($100, fill), which has room and no offset asserts. Model B: stza zp is 6 bytes, stz is 4. Master: stz zp either way (2 bytes).

## r023:3  src/engine.s:4248-4254  (window i2947)  M -2 B -0  [medium]  review: accept

Cycles: per iteration -2 (cpx dropped); Master only

Assumption: Order of the 14 CRTC register writes at boot does not matter: called once from main.s:101 before take_over, the chain is not running (the R12/R13-last rule of 3f6db56 is for the chain step). X dead after: caller does stza (stz) then calc_ring, which loads A and does tax.

```
        ldx #0
:       stx CRTC_IDX
        lda crtctab,x
        sta CRTC_DAT
        inx
        cpx #14
        bne :-
```
->
```
        ldx #13
:       stx CRTC_IDX
        lda crtctab,x
        sta CRTC_DAT
        dex
        bpl :-
```

Writes the same value to each of R13..R0 (reverse order). Exit X=$FF not 14, flags N=1 not Z=1; lda crtctab+7 / sta curR7 follow unchanged.

Review: Master-only CRTC set-up, run once at boot before take_over, with the chain not running. The same values go to R0..R13, in reverse order; only a transient boot frame differs. X is dead afterwards (main.s:101 is followed by stz ... and calc_ring, which reloads). 16 bytes -> 14.

## r023:4  src/engine.s:4353-4360  (window i2995)  M -4 B -0  [high]  review: accept

Cycles: both sites lda #1 (2) + sta abs (4) = 6 -> inc abs 6: cycle-exact; and after the last hardware write (sta FDC_CMD). Master only

Assumption: ld_done's only reader is engine.s:4485 (lda ld_done / bne): any non-zero is done. It is cleared by stz ld_done (4479) before every read command; between that and the test only this read's completion NMIs (a few) can increment it, so it cannot wrap to 0. Seek/restore completion NMIs earlier may increment it too, harmlessly, as the stz follows. grep: no other ld_done use in src/.

```
        lda #1
        sta ld_done
:       pla
        rti
nmi_nd: and #1
        bne :+
        lda #1
        sta ld_done
```
->
```
        inc ld_done                 ; 0 (stz before the command) -> non-zero
:       pla
        rti
nmi_nd: and #1
        bne :+
        inc ld_done
```

inc abs leaves ld_done non-zero exactly where the store of 1 did; A is restored by pla so its value (1) was dead; flags restored by rti.

Review: lda #1 / sta abs (6 cycles) becomes inc abs (6 cycles), so the NMI handler stays cycle-exact. ld_done is only read at 4485, as zero or non-zero, and is cleared by stz before every read command. Between that stz and the test, only one completion increment (plus rare busy-clear NMIs) can land, so it cannot wrap. A is restored by pla and the flags by rti. Master only; 2 bytes per site.

## r023:5  src/engine.s:4401-4403  (window i3028)  M -1 B -0  [high]  review: accept

Cycles: 16 -> 7 plus fdc_wait unchanged; Master only

Assumption: fdc_wait (4405) does not read cur_trk; its leading delay is a minimum after the command write, so starting it 4 cycles later is harmless. stza = stz on the Master (no A/flag effect). Only caller main.s:81.

```
        jsr fdc_wait
        stza cur_trk
        rts
```
->
```
        stz cur_trk
        jmp fdc_wait
```

Exit state is fdc_wait's (A, X=0, Z from and #1) in both; cur_trk=0 in both.

Review: On the Master stza is stz abs, which does not touch A or the flags. fdc_wait does not read cur_trk; its leading delay only gets 4 cycles longer. The tail call is safe because nothing inspects the stack. main.s:81 is the only caller. 7 bytes -> 6.

## r023:6  src/engine.s:4399-4400  (window i3028)  M -2 B -0  [medium]  review: fix

Cycles: 6 -> 4; Master only; the command write lands 2 cycles sooner after the reset-release write to FDC_CTRL

Assumption: A dead: fdc_wait loads A before use (ldx, dex loop, lda FDC_STAT). The 1770 accepts a command 2 cycles (1us) sooner after MR is released (original gap is only 6 cycles anyway).

```
        lda #$00                    ; restore, spin up, 6ms
        sta FDC_CMD
```
->
```
        stz FDC_CMD                 ; restore, spin up, 6ms
```

65C02 stz abs writes 0 in 4 cycles with no dummy read; same register, same value.

Review: The result is right, but the rewrite moves the 1770 command write 2 cycles closer to the write that releases reset (FDC_CTRL). At that moment the 1770 is starting its own restore, emulation cannot check this timing, and the brief requires exact timing in FDC code. A leading nop keeps the write on the same cycle: nop (2) + stz abs (write on cycle 4) matches lda #0 (2) + sta abs (write on cycle 4), with identical bus cycles. The fixed version still saves 1 byte.

Fixed:
```
        nop                         ; (keeps the reset-to-command gap)
        stz FDC_CMD                 ; restore, spin up, 6ms
```

## r023:7  src/engine.s:4417-4420  (window i3043)  M -2 B -0  [high]  review: accept

Cycles: -7 (pha3 pla4); Master only

Assumption: Master music_stop (engine.s:4198) is stz MUSON + lda/jsr sndwrite x4, and sndwrite is SNDWRITE_BODY (4007-4029) which touches only A: X survives. A reloaded by lda ft_seclo,x.

```
        pha
        jsr music_stop
        pla
        tax
```
->
```
        tax
        jsr music_stop
```

X = file index either way when lda ft_seclo,x runs.

Review: On the Master, music_stop is stz MUSON plus lda / jsr sndwrite four times. SNDWRITE_BODY touches only A (pha/pla), so X survives. A is reloaded by lda ft_seclo,x. 6 bytes -> 4.

## r023:8  src/engine.s:4442-4455  (window i3059)  M -7 B -0  [high]  review: accept

Cycles: ld_sec<10: 31->27; each quotient step 35->12 (17 when it borrows); total 31+35q -> <=27+17q; Master only

Assumption: ld_sec < 800 (80-track disc, as the original comment says) so ld_sec+1 <= 3 and bmi on X is right. ld_sec is not read after (grep: only 4422-4454); original left it = remainder. X and Y dead after: ldr_trk..ldr_end reloads X (fdc_wait, ldx #20) and never reads Y; loadfile's callers do not use Y (original left junk in it).

```
        stz ld_trk
@d10:   lda ld_sec
        sec
        sbc #10
        tay
        lda ld_sec+1
        sbc #0
        bcc @drem                   ; ld_sec < 10: what is left is the sector
        sty ld_sec
        sta ld_sec+1
        inc ld_trk
        bne @d10                    ; (unconditional: ld_trk stays under 80)
@drem:  lda ld_sec
        sta ld_sc
```
->
```
        ldy #0                      ; Y = ld_sec / 10, A = ld_sec mod 10
        lda ld_sec
        ldx ld_sec+1
@d10:   cmp #10
        bcs @sub
        dex                         ; borrow from the high byte
        bmi @done                   ; none left: A is the sector
        sec
@sub:   sbc #10
        iny
        bne @d10                    ; (unconditional: Y stays under 80)
@done:  sta ld_sc
        sty ld_trk
```

Repeated subtraction of 10 from the 16-bit value (A lo, X hi): cmp #10 C=1 -> sbc #10; lo<10 with hi>0 -> dex, sec, sbc #10 (lo+246, the borrow); lo<10 with hi=0 -> done. Y counts subtractions. Simulated for all 0..799: quotient and remainder match. ld_trk = Y, ld_sc = A as before.

Review: Simulated all ld_sec 0..799: Y is the quotient and A the remainder in every case. The high byte is at most 3, so bmi after dex is correct. On the Master ld_sec is not read afterwards (only lines 4422-4454 use it). fdc_wait at 4490 always runs and leaves X=0, so exit X is 0 as before. Y held junk before, and neither ldr_* nor any caller reads it. The new cheap labels @sub/@done are in ldread's scope and clash with nothing; @drem was only referenced inside the block. 34 bytes -> 27, and faster on every path.

## r023:9  src/engine.s:4475-4476  (window i3071)  M -2 B -0  [high]  review: accept

Cycles: 7 -> 4; Master only; before the read command write, not in the sector loop

Assumption: ptr low = 0 throughout ldread: set by stz ptr (4433), only ptr+1 is advanced (4491-4494), ldread is only reached by fall-through from loadfile (no other references), and irq_handler never touches ptr (grep 3600-3990). A dead: next is lda ptr+1.

```
        lda ptr
        sta nmi_sta+1
```
->
```
        stz nmi_sta+1               ; ptr's low byte is always 0 here
```

Stores the same value (0) to the same self-modified operand.

Review: ptr's low byte is set to 0 at 4433, and ldread..ldr_end only changes ptr+1. fdc_wait and the irq_handler/music code (lines 3600-4200) never touch ptr. ldread is only reached by falling through. A is dead because lda ptr+1 follows. The change is outside the sector loop. lda zp (2) + sta abs (3) becomes stz abs (3).

## r024:0  src/engine.s:4475-4476  (window i3072)  M -2 B -0  [high]  review: accept

Cycles: Master only (.if .not MODELB block 4308-4504): lda zp 3 + sta abs 4 = 7 -> stz abs 4; not a sector loop or handler (FDC idle: the previous command finished in fdc_wait)

Assumption: ptr (low byte) is 0 on every entry to this code: ldread/ldr_trk/ldr_rd are only reached from loadfile (grep: no other reference to ldread, ldr_trk, ldr_rd), which does stz ptr at engine.s:4433 and falls in; nothing in ldread writes ptr (only ptr+1 at 4494). The NMI handler's inc nmi_sta+1/bne sector count (engine.s:4346) already requires a page-aligned destination. A is dead: next instruction is lda ptr+1; no flags consumed.

```
        lda ptr
        sta nmi_sta+1
```
->
```
        stz nmi_sta+1               ; ptr is page aligned (loadfile's stz ptr)
```

nmi_sta+1 = ptr = 0, so the store of ptr's value is the store of zero; stz abs is 65C02 and this block is Master-only.

Review: Original matches 4475-4476. Master-only (.if .not MODELB). ldread/ldr_trk/ldr_rd are only reached by fall-through from loadfile (grep), which does stz ptr at 4433; nothing in ldread writes ptr (only ptr+1), so ptr=0 and stz nmi_sta+1 stores the same value. A dead (next lda ptr+1), no flags consumed (stz sets none on 65C02). Assembled: Master CODE 6861->6859; Model B unchanged. 7->4 cycles, FDC idle.

## r024:1  src/engine.s:4477-4494  (window i3075)  M -2 B -0  [high]  review: accept

Cycles: Master only: removes one lda ptr+1 (zp, 3 cycles); clc/adc/sta move from after the wait to before the FDC command (8 cycles earlier than the command write, same count overall). -3 cycles per track.

Assumption: ptr+1 and tmp are not written between the old and new positions: the moved span is stz ld_done, the FDC command write, the dex delay, the ld_done/FDC_STAT poll and fdc_wait (engine.s:4403-4410, touches X and A only); the NMI handler (4337-4362) uses nmi_sta self-mod and ld_secs/ld_done, not ptr or tmp. A after sta ptr+1 is dead (stz ld_done is the Master stz, then lda #$94). Carry after adc is not consumed: the next carry user is the sec/sbc at 4496. The FDC is idle when the add now runs (the previous seek/read ended in fdc_wait), so issuing the command 8 cycles later is harmless; the delay loop and poll are unchanged.

```
        lda ptr+1
        sta nmi_sta+2
        stz ld_done
        lda #$94                    ; read multiple sectors with head settle (NMI handler transfers and stops)
        sta FDC_CMD
        ldx #20
:       dex
        bne :-
:       lda ld_done
        bne :+
        lda FDC_STAT                ; fallback: command finished without a completion NMI
        and #1
        bne :-
:       jsr fdc_wait                ; the abort takes a moment to clear busy
        lda ptr+1
        clc
        adc tmp
        sta ptr+1
```
->
```
        lda ptr+1
        sta nmi_sta+2
        clc                         ; ptr moves on now: the read does not use it
        adc tmp
        sta ptr+1
        stz ld_done
        lda #$94                    ; read multiple sectors with head settle (NMI handler transfers and stops)
        sta FDC_CMD
        ldx #20
:       dex
        bne :-
:       lda ld_done
        bne :+
        lda FDC_STAT                ; fallback: command finished without a completion NMI
        and #1
        bne :-
:       jsr fdc_wait                ; the abort takes a moment to clear busy
```

Original loads ptr+1 twice with the same value (nothing writes ptr+1 in between); the second load is dropped by doing the add while A still holds ptr+1 from the nmi_sta+2 store. Memory state after the block identical (ptr+1 = old ptr+1 + tmp; nmi_sta+2 = old ptr+1). A after jsr fdc_wait was then overwritten by the add; now by lda ld_n anyway.

Review: Original matches 4477-4494. The moved span (stz ld_done, FDC command, delay, poll, fdc_wait) touches only A/X/ld_done and never C; NMI handler uses nmi_sta/ld_secs/ld_done only; ptr+1/tmp not written in between (an IRQ clobbering tmp would already break the original's sbc tmp). Carry from the moved adc is dead (next C user is sec/sbc at 4496); A after fdc_wait is dead (lda ld_n). Anonymous labels unchanged in order. The 8 extra cycles precede the command write to an idle 1770 (sector register already written, no inter-write timing), not a sector transfer loop. Assembled: Master CODE -2; Model B unchanged; -3 cycles per track.

## r024:2  src/engine.s:4513-4531  (window i3101)  M -0 B -12  [high]  review: accept

Cycles: Model B only (LOWCODE, full). mapptr is forward-referenced from logic.s so assembles absolute. Fixed part 43 -> 24 (+beq same); per shift iteration lsr abs 6 + ror abs 6 + dex 2 + bne 3 = 17 -> lsr A 2 + ror abs 6 + dey 2 + bne 3 = 13. Faster on every mapshr (0..3).

Assumption: Y is dead after every call of maprow on the Model B: logic.s:306 maptile does jsr maprow / txa / tay (Y overwritten); modelb/src/low.s:90 maprow5 does not use Y and its only caller engine.s:639 goes straight to @rowy: jsr mapstrip, whose first Y use is ldy rc_nt (low.s:108); engine.s:3333 (PARALLAX block) does lda/lsr/.../tay before jsr mapbyte. grep 'maprow\b' finds no other caller. X preserved as before (not touched at all now). mapshr (low.s:148) is read only.

```
maprow:                             ; row * 2^lw = (row << 8) >> (8 - lw): mapshr is
        sta mapptr+1                ; the loader's (low.s); no table to page in.  X
        txa                         ; is kept: the logic calls this with it live, as
        pha                         ; the Master's table lookup allows
        lda #0
        sta mapptr
        ldx mapshr
        beq :++
:       lsr mapptr+1
        ror mapptr
        dex
        bne :-
:       pla
        tax
        lda mapptr+1
        clc
        adc #>LV_MAP
        sta mapptr+1
        rts
```
->
```
maprow:                             ; row * 2^lw = (row << 8) >> (8 - lw): mapshr is
        ldy #0                      ; the loader's (low.s); no table to page in.  X
        sty mapptr                  ; is kept (the logic calls this with it live);
        ldy mapshr                  ; Y comes back 0
        beq :++
:       lsr
        ror mapptr
        dey
        bne :-
:       clc
        adc #>LV_MAP
        sta mapptr+1
        rts
```

Original: mapptr+1=row, mapptr=0, shift the 16-bit pair right mapshr times, then A=mapptr+1, clc, adc #>LV_MAP, sta mapptr+1. New: keep the high byte in A (lsr A instead of lsr mapptr+1), mapptr zeroed via Y, counter in Y instead of X (so X needs no save/restore). Exit A, N, Z, C, V come from the same adc on the same inputs; mapptr/mapptr+1 identical (verified exhaustively for row 0..255, shr 0..3 in a Python model). Y exits 0 instead of preserved.

Review: Original matches 4513-4531. Callers of the Model B maprow (grep): logic.s:306 maptile (txa/tay overwrites Y), low.s:90 maprow5 (no Y use; its caller engine.s:639 goes to @rowy jsr mapstrip whose first Y use is ldy rc_nt), engine.s:3333 (@tile ... tay before jsr mapbyte). X now untouched (preserved as required). High byte shifted in A, low via ror mapptr; same exit A/flags from the same clc/adc and same mapptr/mapptr+1. Anon count unchanged (2). Assembled: Model B LOWCODE 250->238 (-12), links with all asserts; Master unchanged. Faster on every mapshr.

## r024:3  src/engine.s:4568-4604  (window i3156)  M -32 B -0  [high]  review: accept

Cycles: Master only (LOW2), runs once per level. t16/t16b forward-referenced -> absolute. Row loop per iteration 41 -> 36 (x256 = -1280). Stride setup: lw=5..7 faster (45+(7lw-1) -> 27+(7lw-1)); lw=8 50 -> 84 (+34) but the loop's -1280 dominates, so every path through the routine is faster (lw=8 net -1250).

Assumption: maplw is 1..8 (game.s:37 loops dex/bne on it, so never 0; widths 32..256 are 5..8); for lw=8 the last asl shifts $80 out into C with A=0, for lw<8 C=0 -- so txa (X=0 after the dex loop) / rol gives the high byte. Y and t16 are dead after the only caller: logic.s:443 level_init next does lda LV_HDR+2 / jsr @x8, and @x8 (logic.s:851) starts with tay; t16 is next written at logic.s:~502 before any read. The Model B stub (just rts) shows no shared code relies on t16/t16b from here. A at exit (the final hi after the last adc) and C/V are unchanged; pagelogic's pla gives N,Z from A as before; X = 0 as before.

```
init_maprows:
        lda #BANK_MAP
        sta ROMSEL_CPY
        sta ROMSEL
        stz t16
        lda #>LV_MAP
        sta t16+1
        stz t16b                    ; row stride = 1 << maplw bytes
        stz t16b+1
        ldx maplw
        cpx #8
        beq :++
        lda #1
:       asl
        dex
        bne :-
        sta t16b
        bra :++
:       lda #1
        sta t16b+1
:       lda t16b                    ; (this ':' is counted by the beq/bra :++ above)
        sta MAPSTRIDE
        lda t16b+1
        sta MAPSTRIDE+1
        ldx #0
:       lda t16
        sta LV_MAPROWLO,x
        clc
        adc t16b
        sta t16
        lda t16+1
        sta LV_MAPROWHI,x
        adc t16b+1
        sta t16+1
        inx
        bne :-
        jmp pagelogic
```
->
```
init_maprows:
        lda #BANK_MAP
        sta ROMSEL_CPY
        sta ROMSEL
        ldx maplw                   ; row stride = 1 << maplw bytes: lw = 8 shifts
        lda #1                      ; the 1 out into the carry, the high byte
:       asl
        dex
        bne :-
        sta t16b
        sta MAPSTRIDE
        txa                         ; (X = 0)
        rol
        sta t16b+1
        sta MAPSTRIDE+1
        ldy #<LV_MAP                ; Y = row address lo, A = hi, X = row
        lda #>LV_MAP
:       sta LV_MAPROWHI,x
        pha
        tya
        sta LV_MAPROWLO,x
        clc
        adc t16b
        tay
        pla
        adc t16b+1
        inx
        bne :-
        jmp pagelogic
```

Stride: 1<<lw computed as a 9-bit shift (A low byte, C high byte) instead of the lw=8 special case; stored to t16b/t16b+1 and MAPSTRIDE as before. Row loop: hi in A, lo in Y instead of both in t16 (stack holds hi across the lo add); stores LV_MAPROWHI,x = hi and LV_MAPROWLO,x = lo before the add, then lo += t16b (C out) and hi += t16b+1 + C, identical to the original 16-bit add. Verified exhaustively in a Python model for lw 1..8 against LV_MAP + x*stride.

Review: Original matches 4568-4604. Master-only, once per level. Stride: 9-bit shift of 1 by maplw (1..8; game.s:37 dex loop requires >=1) leaves low in A and the high bit in C; txa (X=0)/rol gives the high byte. Row loop: hi in A (stacked), lo in Y; stores then 16-bit add with carry preserved through tay/pla; LV_MAP=$8900 so <LV_MAP=0 matches the old stz t16. Exit A (last hi), C, X=0 and Z (from inx) identical into pagelogic (which preserves A/X/Y/C). Y dead: level_init next does lda LV_HDR+2 / jsr @x8 whose first instruction is tay. t16 is no longer left at its end value: the Model B stub never set it and the shared logic runs the same there, so nothing reads it. Anon labels 4->2, all refs internal; no outside branch counts across. Assembled: Master LOW2 481->449 (-32); Model B unchanged. Row loop 41->36 cy; lw=8 setup +34 but net -1250.

## r024:4  src/engine.s:4618-4635  (window i3175)  M -2 B -0  [high]  review: accept

Cycles: Master only (runs once at boot). Per iteration 32 -> 28 (6 asl 12 -> 3 ror + and 8; lsr/ora #$80 4 -> sec/ror 4).

Assumption: None outside the loop: C and the flags from the loop are not consumed (the tail loads A). C at loop exit is also unchanged (= bit 1 of 255 = 1, both forms). The .if MODELB arm is unreachable inside the enclosing .if .not MODELB and is kept equivalent.

```
@t:     txa
        asl                         ; (id & 3) << 6: the rest shifts out
        asl
        asl
        asl
        asl
        asl
        sta LV_PAGE0,x
        txa
        lsr
        lsr
  .if MODELB
        clc                         ; the tiles sit at TILES (page aligned), not $8000
        adc #>TILES
  .else
        ora #$80                    ; $80 | id >> 2
  .endif
        sta LV_PAGE0+256,x
```
->
```
@t:     txa
        ror                         ; (id & 3) << 6: three rotates put bits 1,0 at 7,6
        ror
        ror
        and #$C0
        sta LV_PAGE0,x
        txa
        lsr
  .if MODELB
        lsr
        clc                         ; the tiles sit at TILES (page aligned), not $8000
        adc #>TILES
  .else
        sec                         ; $80 | id >> 2
        ror
  .endif
        sta LV_PAGE0+256,x
```

(id&3)<<6: three rotates through carry leave x1,x0 in bits 7,6 (whatever came in from C lands in bit 5, masked off). $80|id>>2: lsr then sec/ror shifts in a 1 at bit 7 -- same value as lsr/lsr/ora #$80, one byte shorter, same cycles. Verified for all 256 ids and both incoming carries.

Review: Original matches 4618-4635. Master-only (inside .if .not MODELB; the MODELB arm is unreachable but kept equivalent). txa/ror x3/and #$C0 = (x&3)<<6 for any incoming C; txa/lsr/sec/ror = $80|x>>2. C at iteration end = bit1 of x in both forms, so the loop exit carry is unchanged; tail loads A. Assembled: Master LOW2 -2; Model B unchanged. Faster per iteration.

## r024:5  src/logic.s:442-443  (window i3155)  M -0 B -3  [high]  review: accept

Cycles: Model B: jsr init_maprows (6) + its rts (6) removed: -12. Master identical (jsr kept).

Assumption: The Model B init_maprows (engine.s:4565) is a bare rts with no side effects; level_init is its only caller (grep). level_init on the Model B is in LGCCODE (bank 7, full). No stack inspection depends on this call (it is a leaf).

```
level_init:
        jsr init_maprows            ; main RAM: the row tables live with the map
```
->
```
level_init:
  .if .not MODELB
        jsr init_maprows            ; main RAM: the row tables live with the map
  .endif                            ; (Model B: none -- the row address is arithmetic)
```

On the Model B the call does nothing; skipping it leaves every register, flag and memory byte the same. Pairs with the next candidate (engine.s 4563-4567), which deletes the now-unreferenced stub for one more byte in LGCLO; this one is valid on its own.

Review: Original matches logic.s 442-443. The Model B init_maprows is a bare rts; skipping jsr/rts changes no register, flag or memory. Only caller is level_init (grep). Master assembles identically. Assembled: Model B LGCCODE 10762->10759 (-3); Master unchanged. -12 cycles.

## r024:6  src/engine.s:4563-4567  (window i3124)  M -0 B -1  [high]  review: accept

Cycles: none executed (dead code removed)

Assumption: DEPENDS on the logic.s 442-443 candidate (level_init no longer calls init_maprows on the Model B); without it the build fails with an undefined symbol, not silently. Nothing else references init_maprows (grep). On the Model B nothing is assembled between the old .else and the next .segment "MNUCODE" (build_tileaddr and the Master getglyph are .if .not MODELB), so dropping the .segment "LGCLO" switch changes no placement.

```
  .if MODELB
        .segment "LGCLO"
init_maprows:                       ; the row address is arithmetic here (maprow,
        rts                         ; maprow5): there is no table to build
  .else
```
->
```
  .if .not MODELB                   ; (Model B: none -- maprow's row address is arithmetic)
```

Removes the Model B arm (a 1-byte rts in LGCLO) of the .if; the Master arm is unchanged.

Review: Original matches 4563-4567. Depends on index 5 (alone it fails to assemble with 'init_maprows undefined', verified -- a loud failure, not silent). With 5 applied: nothing else references init_maprows; on the Model B nothing is assembled between the removed .segment LGCLO and the MNUCODE switch at 4661, confirmed by segment sizes: only LGCLO 154->153 changes. Master arm unchanged. Apply after index 5.

## r025:0  src/engine.s:4822-4840  (window i3319)  M -0 B -7  [high]  review: accept

Cycles: Model B only (ringaddr7 is in the MODELB branch): single path 68 -> 56 (clc 2, clc 2, sta sp+1 3, 3x rol zp 15, lda sp+1 3 removed = 25; 3x rol a 6, pha 3, pla 4 added = 13). ringmod7's own loop unchanged. Master: not assembled.

Assumption: (1) C = 0 after ringmod7: the macro (cpu/engine macros, MODELB form) leaves only by `bcc :+`; its `sbc #RINGROWS` runs with C = 1 from the cmp on A >= RINGROWS so its `bcs :-` is always taken; `tax` keeps C. (2) Carry out of the third `rol` is bit 13 of v = mulrow[slot] + w16: slot <= 22 so mulrow <= 1760; w16 = wcx + sp_c0 (engine.s:1757-1765), wcx = wx/2 map chars and the map is <= 256 tiles of 8 px (engine.s:610, game.s:33) so wcx <= 1024, sp_c0 < 80: v < 2900 < 8192, so C = 0 into `adc #<RING_A`, as the clc gave. (3) ringup sp reads only sp (the low byte), never sp+1, so the high byte need not be in memory until the final `sta sp+1` (unchanged, after ringup). (4) The only caller (engine.s:1876) reads sp/sp+1 after; X (slot) and Y are preserved (pha/pla, not tax), A and all flags at rts identical.

```
        lda mulrowlo,x              ; slot * 80 + cx: the ring char (the tables are
        clc                         ; in chars, as the chain wants them)
        adc w16
        sta sp
        lda mulrowhi,x
        adc w16+1
        sta sp+1
        asl sp                      ; x 8: bytes from the ring base
        rol sp+1
        asl sp
        rol sp+1
        asl sp
        rol sp+1
        lda sp
        clc
        adc #<RING_A
        sta sp
        lda sp+1
        adc ringbhi
```
->
```
        lda mulrowlo,x              ; slot * 80 + cx (C = 0: ringmod7 leaves by its bcc)
        adc w16
        sta sp
        lda mulrowhi,x
        adc w16+1
        asl sp                      ; x 8, the high byte kept in A
        rol
        asl sp
        rol
        asl sp
        rol                         ; C = 0: bit 13 of the char (< 8192)
        pha
        lda sp
        adc #<RING_A
        sta sp
        pla
        adc ringbhi
```

Original: sp:sp+1 = mulrow+w16 (clc redundant), shift the 16-bit pair left 3 in memory, then lo += $80 and hi = sp+1 + ringbhi + carry. New: the high byte stays in A through the shifts (asl sp / rol a is the same 16-bit shift as asl sp / rol sp+1), is pushed while the low byte gets + <RING_A (C = 0 proven, see assumption 2), pulled (pla does not touch C) and added to ringbhi with that carry: A and C entering ringup are identical to the original, as is V (same operands and carry into adc ringbhi), and sp holds the same low byte. sp+1 is written by the unchanged `sta sp+1` after ringup. Stack: balanced, ringup's own pha/pla nests.

Review: Lines 4822..4840 match. Model B only (ringaddr7 is in the .else of `.if .not MODELB`). ringmod7 (MODELB form) exits only via bcc :+ (C=0); its sbc/bcs loop always branches back since A>=RINGROWS; tax keeps C, so the first clc is redundant. asl sp / rol a is the same 16-bit shift with the high byte in A; C out of the third rol = bit 13 of v = mulrow[slot]+w16 <= 1760 + wcx(<1024) + c0(<80) < 2900 < 8192 (map <= 256 tiles x 4 chars, engine.s:610; w16 set at 1759-1765, mirdirty does not touch it), so C=0 into adc #<RING_A as the clc gave. pla does not touch C; A and C into ringup identical. ringup (MODELB) reads only sp (lda p), never sp+1; sp+1 written by the unchanged sta after it. Only caller engine.s:1876. Listing: all operands zp; 38 -> 31 bytes (-7), 71 -> 56 cycles. No anon change.

## r025:1  src/engine.s:4822-4840  (window i3319)  M -0 B -6  [high]  review: accept

Cycles: ALTERNATIVE to the previous candidate (same lines; apply only if that one is rejected). Model B only: 68 -> 58. Master: not assembled.

Assumption: C = 0 after ringmod7 (it exits only via bcc; its sbc/bcs loop always branches back), tax keeps C. ringup reads only sp. Caller engine.s:1876 uses sp/sp+1; X, Y preserved.

```
        lda mulrowlo,x              ; slot * 80 + cx: the ring char (the tables are
        clc                         ; in chars, as the chain wants them)
        adc w16
        sta sp
        lda mulrowhi,x
        adc w16+1
        sta sp+1
        asl sp                      ; x 8: bytes from the ring base
        rol sp+1
        asl sp
        rol sp+1
        asl sp
        rol sp+1
        lda sp
        clc
        adc #<RING_A
        sta sp
        lda sp+1
        adc ringbhi
```
->
```
        lda mulrowlo,x              ; slot * 80 + cx (C = 0: ringmod7 leaves by its bcc)
        adc w16
        sta sp
        lda mulrowhi,x
        adc w16+1
        asl sp                      ; x 8, the high byte kept in A
        rol
        asl sp
        rol
        asl sp
        rol
        pha
        lda sp
        clc
        adc #<RING_A
        sta sp
        pla
        adc ringbhi
```

As the previous candidate but keeps the clc before adc #<RING_A, so it does not depend on the range of w16: the high byte is carried in A through asl sp / rol a (identical 16-bit shift), pushed across the low-byte add, pulled (C untouched) and added to ringbhi. A, C, V, sp entering ringup identical; sp+1 written after ringup as before.

Review: Correct (same reasoning as 0, keeping the clc before adc #<RING_A): -6 bytes Model B, 71 -> 58 cycles. ALTERNATIVE to candidate 0 on the same lines: apply only if 0 fails (its original will not match once 0 is in).

## r025:2  src/engine.s:4674-4696  (window i3230)  M -3 B -3  [medium]  review: accept

Cycles: Faster on both: init Master 10 -> 12 (stz/sta abs), Model B 13 -> 15; loop 38/iter x RINGROWS -> 29/iter x (RINGROWS-1) (Master 32 rows: ~1215 -> ~900; Model B 23 rows: ~873 -> ~638). Not timing-critical (built once per buffer select).

Assumption: (1) w16 is no longer written: no caller reads it after -- Master's one call (engine.s:3560, Master-only block from 3488) is followed by `stz w16 / stz w16+1`; Model B's one call (select_backbuf engine.s:3044) is followed by @recs (curbuf/recp only, rts), and select_backbuf's callers (engine.s:3347 render_core -> calc_ring, menu.s:213 -> m_calc_ring) never read w16 before writing it (calc_ring reads wcx/wcy/mulrow). (2) C = 0 at entry: Master caller ends `txa / and #$11 / asl / asl / asl / ora tmp / sta SWAPTAB,x / inx / bne @t` -- the third asl shifts out bit 5 of (x & $11) = 0, ora/sta/inx/bne keep C; Model B caller ends `lda #0 / sec / sbc ringehi / sta ringneg` with ringehi = >RINGEND_A ($44) or >RINGEND_B ($80), nonzero, so the borrow leaves C = 0. Nothing before @r in build_ring touches C. (3) RINGLO+1,x never passes RINGLO+RINGROWS-1. build_ring has no other callers (grep src, modelb/src).

```
  .if MODELB
        lda #<RING_A                ; both bases are xx80: the high byte is the buffer's
        sta w16
        lda ringbhi
  .else
        stz w16
        lda #>RINGBASE
  .endif
        sta w16+1
        ldx #0
@r:     lda w16+1
        sta RINGHI,x
        lda w16
        sta RINGLO,x
        clc
        adc #<ROWBYTES
        sta w16
        lda w16+1
        adc #>ROWBYTES
        sta w16+1
        inx
        cpx #RINGROWS
        bne @r
```
->
```
  .if MODELB
        lda #<RING_A                ; both bases are xx80: the high byte is the buffer's
        sta RINGLO
        lda ringbhi
  .else
        stz RINGLO
        lda #>RINGBASE
  .endif
        sta RINGHI
        ldx #0                      ; C = 0 from both callers; then from the cpx
@r:     lda RINGLO,x                ; each row from the one before it
        adc #<ROWBYTES
        sta RINGLO+1,x
        lda RINGHI,x
        adc #>ROWBYTES
        sta RINGHI+1,x
        inx
        cpx #RINGROWS-1
        bne @r
```

Original: w16 = base; for x in 0..RINGROWS-1: RINGHI[x], RINGLO[x] = w16; w16 += ROWBYTES. New: RINGLO[0], RINGHI[0] = base, then for x in 0..RINGROWS-2: entry x+1 = entry x + ROWBYTES (16-bit, lo carry into hi). Same table contents, Y untouched, X ends at RINGROWS-1 instead of RINGROWS (callers: Master's next line is `stz w16` then `ldx #0`; Model B jumps to @recs which does not read X before rts -- select_backbuf's X was already clobbered by the original's ldx #0). Carry into the lo adc: first iteration C = 0 (assumption 2), later iterations C = 0 because cpx #RINGROWS-1 with X < RINGROWS-1 on the looping path. The clc in the original loop body is gone. A at rts: original = w16+1 after the last add; new = the last adc result -- callers ignore A (Master: stz; Model B: jmp @recs -> lda curbuf).

Review: Lines 4674..4696 match. Callers: Master engine.s:3560 (inside `.if .not MODELB` 3488..3613) enters with C=0 ((x&$11) asl x3 shifts out only zero bits; ora/sta/inx/bne keep C) and follows with stz w16/stz w16+1/ldx #0. Model B select_backbuf (3044) enters after lda #0/sec/sbc ringehi with ringehi = >RINGEND_A/B != 0 -> borrow -> C=0; the prologue stores/loads keep C. Later iterations: cpx #RINGROWS-1 with X < RINGROWS-1 on the looping path -> C=0. w16 no longer written: after select_backbuf, @recs touches only curbuf/recp; its callers (render_core 3347 -> calc_ring, menu.s:213 -> m_calc_ring -> clear_ring) never read w16 before writing it. X exits RINGROWS-1: fcret reloads X, calc_ring/farjsr overwrite it. RINGLO/RINGHI are absolute (9D in both listings); max index RINGLO+RINGROWS-1 in range. Bytes: init +2 (abs vs zp), loop 26 -> 21: -3 on both. Cycles: init +2, loop 38 -> 29 per iteration and one fewer iteration; not timing-critical.

## r025:3  src/engine.s:4674-4696  (window i3230)  M -2 B -2  [high]  review: accept

Cycles: ALTERNATIVE to the previous candidate (same lines; apply only if that one is rejected). Loop 38 -> 31 per iteration and one fewer iteration; init +2. Faster on both.

Assumption: w16 not read after build_ring by any caller (Master engine.s:3560 is followed by stz w16/stz w16+1; Model B select_backbuf -> @recs -> rts, and its callers render_core/menu.s:213 go to calc_ring, which does not read w16). X at exit RINGROWS-1 instead of RINGROWS: neither caller reads X before rewriting it. A at exit differs (callers overwrite it).

```
  .if MODELB
        lda #<RING_A                ; both bases are xx80: the high byte is the buffer's
        sta w16
        lda ringbhi
  .else
        stz w16
        lda #>RINGBASE
  .endif
        sta w16+1
        ldx #0
@r:     lda w16+1
        sta RINGHI,x
        lda w16
        sta RINGLO,x
        clc
        adc #<ROWBYTES
        sta w16
        lda w16+1
        adc #>ROWBYTES
        sta w16+1
        inx
        cpx #RINGROWS
        bne @r
```
->
```
  .if MODELB
        lda #<RING_A                ; both bases are xx80: the high byte is the buffer's
        sta RINGLO
        lda ringbhi
  .else
        stz RINGLO
        lda #>RINGBASE
  .endif
        sta RINGHI
        ldx #0
@r:     lda RINGLO,x                ; each row from the one before it
        clc
        adc #<ROWBYTES
        sta RINGLO+1,x
        lda RINGHI,x
        adc #>ROWBYTES
        sta RINGHI+1,x
        inx
        cpx #RINGROWS-1
        bne @r
```

Same table: entry 0 = base, entry x+1 = entry x + ROWBYTES with a 16-bit carry; clc kept per iteration so no entry-carry assumption.

Review: Correct without the entry-carry argument (clc kept per iteration); same w16/X/A caller analysis as candidate 2. -2 bytes on both targets, faster. ALTERNATIVE to candidate 2 on the same lines: apply only if 2 fails.

## r025:4  src/engine.s:4619-4624  (window i3192)  M -1 B -0  [high]  review: accept

Cycles: Master only (build_tileaddr is `.if .not MODELB`): per iteration 12 -> 8 cycles. Model B: not assembled. (Window i3192 belongs to r024; reported in case it is not found there.)

Assumption: None beyond the loop: C entering is arbitrary (lands in bit 5 and is masked off); the C left behind (bit 2 of id) is overwritten by the lsr two instructions later; the sta that follows ignores flags.

```
        asl                         ; (id & 3) << 6: the rest shifts out
        asl
        asl
        asl
        asl
        asl
```
->
```
        ror                         ; (id & 3) << 6: bits 1,0 rotate to 7,6
        ror
        ror
        and #$C0
```

A = x, C = c. ror x3: A = x1 x0 c x7 x6 x5 x4 x3 (bit 7..0), C = x2. and #$C0 -> x1<<7 | x0<<6 = (x & 3) << 6, the same value as six asl's. LOW2 on the Master has 25 bytes free.

Review: Lines 4619..4624 match; build_tileaddr is `.if .not MODELB` (Master only, LOW2). Three rors of x with carry-in c give x1 x0 c x7 x6 x5 x4 x3, and and #$C0 = (x&3)<<6, the same as six asls. The carry out (x2) is dead: sta, then txa/lsr/lsr overwrite it; N/Z unused. 6 -> 5 bytes, 12 -> 8 cycles. (The window belongs to r024; a duplicate there is harmless because the second copy goes stale.)

## r025:5  src/engine.s:4720-4724  (window i3260)  M -4 B -4  [medium]  review: reject

Cycles: TWO-SITE EDIT: must be applied together with the companion edit at src/engine.s:1668 (below); alone it fails to link. The one call path: jsr 6 + and 2 + beq 2/3 + lda 2 + sta 3 + rts 6 -> and 2 + beq 2/3 + lda 2 + sta 3: 12 cycles faster on both targets.

Assumption: Companion edit, src/engine.s line 1668 `        jsr sext` becomes the 4 lines `        and #$80                    ; sext inlined` / `        beq @syp` / `        lda #$FF` / `@syp:   sta tmp3` (the same form as the horizontal one at engine.s:1611-1614; @syp is unused in the file, and a cheap label adds no ':' so no anonymous branch retargets). sext's only caller is engine.s:1668 (grep of src/ and modelb/src/). Bytes: Model B, both sites in bank 7 LGCCODE: sext 9 bytes out, the 3-byte jsr replaced by 8 inline bytes (+5): net -4. Master: LOW2 -9, CODE +5 (CODE has ~$233 free per SPACE.md, LOW2 gains 9).

```
sext:   and #$80
        beq :+
        lda #$FF
:       sta tmp3
        rts
```
->
```
; (sext: inlined at its one caller, the sprite prologue's @vert)
```

sext computes tmp3 = A & $80 ? $FF : 0 and returns with A = tmp3. The inline copy computes the same into tmp3; after it the caller does `lda spy / sec / sbc (ptr),y / tax / lda spy+1 / sbc tmp3`, which overwrites A and every flag before any use, and uses Y (still 5) and tmp3 only. Deleting the sext body leaves the two PLACE lines adjacent, which is harmless.

Review: Not applicable as submitted. A candidate is one line range, and tools/grind_apply.py replaces only one site. Deleting sext at 4720..4724 on its own leaves `jsr sext` at engine.s:1668 unresolved, so the link fails. The PAIR is sound as logic. sext's only caller is 1668. The inline and #$80 / beq @syp / lda #$FF / @syp: sta tmp3 leaves A=tmp3 and Y=5, and lda spy/sec/sbc then overwrite every flag. @syp is unused. The deleted ':' at 4723 is referenced only by sext's own beq. Net saving is 4 bytes on each target (Master: LOW2 -9, CODE +5 with $233 free) and 12 cycles faster. If wanted, apply it by hand as a two-site edit.

## r026:0  src/engine.s:4849-4850  (window i3328)  M -0 B -12  [high]  review: accept

Cycles: per FARSUB call: jsr farcall(6)+rts(6) -> jmp farcall(3), 9 cycles faster; Master: not assembled (inside the .else of `.if .not MODELB` at 4742/4801)

Assumption: farcall (modelb/src/low.s:22) only inspects its own pushes (tsx / lda $0106,x = the A it pushed six bytes earlier), not its caller's frame; the only tsx/txs in the game besides it is init.s:30. The same file already tail-calls farcall at engine.s:4815 (m_mark_dirty: ldx #F_MARKDIRTY / jmp farcall). farjsr (src/cpu.inc:285) is plain ldx/jsr and records no bank site. No FARSUB's rts is a label or branch target (macro body).

```
        farjsr idx
        rts
```
->
```
        ldx #idx                    ; tail call: fcret returns to our caller
        jmp farcall
```

12 instances (4 in LGCCODE = bank 7, 8 in MNUCODE = bank 5 overlay), 6 -> 5 bytes each. farcall pushes A, ROMSEL_CPY (= the FARSUB's bank, which is its caller's bank), fcret-1, target-1. fcret restores that bank and returns with A = fcA (flags from lda fcA), X = bank, Y untouched; the original FARSUB rts then returned to its caller with exactly those registers and flags. With jmp, fcret's rts goes straight to the caller: same registers, same flags, same bank, 2 bytes less stack. Nesting (a FARSUB reached through the far table) is unchanged because every level's return state is on the stack.

Review: Lines 4849-4850 match (FARSUB body). farjsr (cpu.inc:285) is plain ldx/jsr and records no bank site. farcall's tsx/lda $0106,x reads only its own six pushes, so a jmp entry reads the same A. The pushed ROMSEL_CPY is the FARSUB's bank, which is also the caller's bank (the same-bank callers in game.s/menu.s, or an outer farcall's target bank when nested). fcret restores that bank and returns with A=fcA, flags from lda fcA, X=bank, Y untouched: exactly what the original rts passed back. The stack is 2 bytes shallower. The only txs is init.s:30 and nothing reads the caller's frame. No +offset references to FARSUB labels. menu.s:240's `jmp m_set_palette` is already a tail call and is unaffected. Each instance is 6->5 bytes, x12 (4 LGCCODE, 8 MNUCODE) = 12 bytes on the Model B; the code is Model B only. 9 cycles faster per call.

## r026:1  src/engine.s:4828-4840  (window i3308)  M -0 B -6  [high]  review: accept

Cycles: single path 49 -> 36 (sta sp+1 3, 3x(asl zp 5 + rol zp 5), lda 3, clc 2, adc# 2, sta 3, lda 3, adc zp 3 -> 3x(asl zp 5 + rol a 2), tax 2, lda 3, adc# 2, sta 3, txa 2, adc zp 3); Master: not assembled (Model B only, .else branch)

Assumption: Extends the i3328 candidate below by the dead `sta sp+1` at 4828 (outside r026's windows; reported here as the combined form -- apply this OR the i3328 one). X dead on return: ringaddr7's only caller is engine.s:1876, which reads sp/sp+1, tmp, sp_lb0 and then `ldx #8` (1890) before any X read; ringup (Model B) does not use X. C = 0 after the third rol: the pre-shift value slot*80 + w16 has slot <= 22 and w16 = wcx + sp_c0 (1758-1765) with sp_c0 < 80 and wcx < 1024 (maps are <= 256 tiles of 4 chars), so it is < 2864 < $2000 and all three rol carry-outs are 0 (the same argument the code already makes at engine.s:2559, 'the rols leave C clear').

```
        sta sp+1
        asl sp                      ; x 8: bytes from the ring base
        rol sp+1
        asl sp
        rol sp+1
        asl sp
        rol sp+1
        lda sp
        clc
        adc #<RING_A
        sta sp
        lda sp+1
        adc ringbhi
```
->
```
        asl sp                      ; x 8: bytes from the ring base (A = high)
        rol a
        asl sp
        rol a
        asl sp
        rol a                       ; C = 0: the offset is under $2000 chars
        tax                         ; (X dead: the one caller reloads it)
        lda sp
        adc #<RING_A
        sta sp
        txa
        adc ringbhi
```

Entry: A = high byte (from adc w16+1), sp = low byte. The high byte is shifted in A instead of memory: asl sp/rol a x3 leaves sp = low*8 and A = high*8 with C = 0 (above), exactly the (sp, sp+1) the original held after its rols, whose final C was also 0 -- so the removed clc was already redundant. tax parks the high byte; lda sp / adc #<RING_A (C=0) / sta sp is the original low add and leaves the same carry; txa / adc ringbhi gives the same A and flags as lda sp+1 / adc ringbhi. sp+1 is not read before ringup's final sta sp+1 (ringup reads only p = sp low byte), so the removed sta sp+1 at 4828 was a dead store. X ends holding the unshifted high byte instead of the slot: dead at the caller.

Review: Lines 4828-4840 match, and A holds the high byte from `adc w16+1` at 4827. asl sp/rol a x3 gives the same sp and high byte and the same final C, which is 0: the value slot*80+w16 is at most 22*80 + (wcx<=~1024) + 79, well under $2000, so the removed clc was redundant. tax/txa park the high byte, and the flags into ringup come from the same adc ringbhi. sp+1 is not read before ringup's final sta sp+1 (the Model B ringup reads only p=sp and uses the stack, not X), so the sta at 4828 is dead. X is dead: the only caller (engine.s:1876) reads sp/sp+1/tmp/sp_lb0 and loads `ldx #8` before any X read. No anonymous labels change. sp is zp. Bytes 23->17 (+ adc ringbhi, unchanged) = 6 on the Model B; Master does not assemble it. 43->33 cycles. Mutually exclusive with indices 2 and 3.

## r026:2  src/engine.s:4829-4840  (window i3328)  M -0 B -4  [high]  review: accept

Cycles: single path 46 -> 36; Master: not assembled (Model B only)

Assumption: Alternative to the i3308 combined candidate (same lines minus 4828; do not apply both). A still holds the high byte on entry: line 4828 is `sta sp+1`, which leaves A unchanged, and 4827 is `adc w16+1`. X dead on return: sole caller engine.s:1876 reloads X (`ldx #8`, 1890) before reading it. C = 0 after the third rol: pre-shift offset < 2864 chars (slot <= 22, w16 = wcx + sp_c0, wcx < 1024, sp_c0 < 80).

```
        asl sp                      ; x 8: bytes from the ring base
        rol sp+1
        asl sp
        rol sp+1
        asl sp
        rol sp+1
        lda sp
        clc
        adc #<RING_A
        sta sp
        lda sp+1
        adc ringbhi
```
->
```
        asl sp                      ; x 8: bytes from the ring base (A = high)
        rol a
        asl sp
        rol a
        asl sp
        rol a                       ; C = 0: the offset is under $2000 chars
        tax                         ; (X dead: the one caller reloads it)
        lda sp
        adc #<RING_A
        sta sp
        txa
        adc ringbhi
```

As the i3308 candidate: the shifts act on A (= the value just stored to sp+1) instead of sp+1; C = 0 after them as after the original rols, so clc goes; tax/txa replace sta/lda of the high byte around the low add; flags into ringup identical (from adc ringbhi). The sta sp+1 at 4828 survives as a now-dead store (a further 2 bytes for whoever owns it).

Review: Correct: this is the same reasoning as index 1 with the sta sp+1 kept (a dead store then). Bytes 21->17 = 4 on the Model B. It is a FALLBACK ONLY: it covers the same lines as index 1 (4829-4840), so apply it only if index 1 is not applied. Once index 1 is applied, its original text no longer matches.

## r026:3  src/engine.s:4835-4837  (window i3328)  M -0 B -1  [high]  review: accept

Cycles: single path 7 -> 5; Master: not assembled

Assumption: Minimal fallback if the two larger ringaddr7 candidates are rejected (do not apply with them). C after the last `rol sp+1` = bit 13 of slot*80 + w16, which is 0 because that value is < 2864 (slot <= 22, w16 = wcx + sp_c0 with wcx < 1024 and sp_c0 < 80); lda sp does not touch C. Same reasoning as the existing code at engine.s:2559.

```
        lda sp
        clc
        adc #<RING_A
```
->
```
        lda sp                      ; C = 0: the rols shifted out zeros
        adc #<RING_A
```

clc sets C = 0, which it already is; nothing between the rol and the adc reads or alters C.

Review: Correct: C=0 after the third rol sp+1, because the char offset is under $2000, and lda sp leaves C alone. 1 byte on the Model B and 2 cycles faster. FALLBACK ONLY: it is subsumed by index 1 or 2, so apply it only if neither of those is applied.

## r027:0  src/logic.s:32-34  (window i0015)  M -8 B -8  [high]  review: accept

Cycles: per expanded site (zp dst): original 18; new carry-clear 13, carry-set 17 (clc2 lda3 adc2 sta3 bcc2 inc5). Same on both targets. Applies at 4 sites: add16i vy,80 (logic.s:1270), fb,16 (2369), fd,16 (2599), fa,120 (2815); dpx,128 (1289) and ptr,640 (3211) keep the long form

Assumption: All add16i uses (grep src, modelb/src): 1270 gravity: next is sec/lda #0 (A,flags dead); 1289 vy_step uses A = high byte ('sta dpx' next) -> excluded by the .xmatch guard; 2369 'bra @coll' -> @coll: add16 rx,fb starts clc/lda (A,flags dead; Model B bra is jmp, flag-neutral); 2599 'bra :++' -> ldx #54 / bgt16 (lda/cmp/sbc) dead; 2815 next is lda fa/cmp #$80 dead; 3211 ptr,640 has hi byte 2 so unchanged. vy (zeropage seg, defined line 221 before use), fa/fb/fd ($AC/$AE/$B2) are zero page so inc is 2 bytes and bcc *+4 lands after it (verified in listing: 90 02 E6 xx). Measured in a scratch build together with sub16i: LGCCODE (Model B) and Master LOGIC both -14.

```
        lda dst+1
        adc #>(imm)
        sta dst+1
```
->
```
  .if (>(imm) = 0) .and (.not .xmatch({dst}, {dpx}))  ; vy_step reads the high byte from A
        .assert (dst) < $100, error, "add16i: bcc *+4 skips a zero-page inc"
        bcc *+4                     ; no carry: the high byte stands
        inc dst+1
  .else
        lda dst+1
        adc #>(imm)
        sta dst+1
  .endif
```

High byte of dst + 0 + C equals dst+1 incremented iff the low add carried: bcc skips the inc when C=0, inc adds 1 when C=1. Memory results identical. Differences: A ends as the low sum not the high sum, and N/Z/C/V differ -- all dead at the four affected sites; the one site that consumes A (vy_step) is excluded by .xmatch. The parenthesised .if is needed: ca65 rejects the unparenthesised '.and .not .xmatch' form.

Review: Lines 32-34 match the file exactly. Checked each use site. At gravity 1270 the next instructions are sec / lda #0. At 2369 a bra (a jmp on the Model B) goes to @coll, whose add16 starts clc / lda. At 2599 a bra :++ goes to ldx / bgt16. At 2815 the next instructions are lda fa / cmp. So A and the flags are dead at all four sites. vy_step (dpx), which reads the high byte from A, is excluded by the .xmatch guard. ptr,640 keeps the long form. vy, fa, fb and fd are zero page, and the assert passes at link. No anonymous label is added. I built all four candidates together in a scratch copy: Master LOGIC and Model B LGCCODE each shrink by 18 bytes, of which this candidate is 4 sites x 2 bytes. Cycles go from 18 to 13 or 17.

## r027:1  src/logic.s:50-52  (window i0022)  M -6 B -6  [high]  review: accept

Cycles: per site (zp dst): original 18; new no-borrow 13, borrow 17. Same on both targets. 3 sites: sub16i fb,16 (2372), fc,16 (2595), fd,16 (2601)

Assumption: All sub16i uses: 2372 falls into @coll: add16 rx,fb (clc/lda: A,flags dead); 2595 next is lda fb/cmp #1 (dead); 2601 next is ':  ldx #54 / bgt16 ox,px' (ldx then lda/cmp/sbc: dead). fb/fc/fd = $AE/$B0/$B2 zero page, so dec is 2 bytes and bcs *+4 is exact (listing: B0 02 C6 xx). Values can go negative (e.g. 5-16): bcs/dec is exactly hi - 0 - borrow, so the 16-bit wrap is preserved.

```
        lda dst+1
        sbc #>(imm)
        sta dst+1
```
->
```
  .if >(imm) = 0
        .assert (dst) < $100, error, "sub16i: bcs *+4 skips a zero-page dec"
        bcs *+4                     ; no borrow: the high byte stands
        dec dst+1
  .else
        lda dst+1
        sbc #>(imm)
        sta dst+1
  .endif
```

dst+1 - 0 - borrow: borrow is !C after the low sbc; bcs skips the dec when no borrow, dec subtracts 1 when borrow. Memory identical; A ends as low result instead of high, flags differ -- dead at all three sites.

Review: Lines 50-52 match the file exactly. At 2372 the code falls into @coll, whose add16 starts clc / lda. At 2595 the next instructions are lda fb / cmp. At 2601 the next are ldx #54 and bgt16 (lda / cmp / sbc). A and the flags are dead at all three sites. fb, fc and fd are zero page, and the listing shows B0 02 C6 xx. The result is exactly hi - 0 - borrow, so negative values still wrap correctly. Cycles go from 18 to 13 or 17. Saves 3 sites x 2 bytes on each target.

## r027:2  src/logic.s:66-66  (window i0043)  M -2 B -2  [high]  review: accept

Cycles: cmp #imm 2 -> asl a 2 per site; identical on both targets. 2 sites (logic.s:2660, 2664)

Assumption: Both asr16 uses (logic.s:2660 asr16 t16 then add16 rx,t16; 2664 asr16 t16 then add16 ry,t16) are followed by add16, which starts clc / lda: A dead after the macro. Only flags after the macro come from 'ror var' (N,Z,C), unchanged.

```
        cmp #$80
```
->
```
        asl                         ; C = sign; A is not left holding var+1
```

cmp #$80 on A=var+1 sets C = (A >= $80) = bit 7; asl a sets C = bit 7 as well. The two rors that follow use only C and memory. A differs afterwards (var+1<<1 vs var+1), dead at both sites. Final N/Z/C set by ror var identical. Measured: -4 total with bgt16i below, both targets.

Review: Line 66 matches the file exactly. asl A sets C from bit 7, as cmp #$80 does. The two rors use only C and memory, so the final N/Z/C from ror var are unchanged. Both uses (2660 and 2664) are followed by add16 (clc / lda), so A is dead. Cycles stay at 2, and it saves 1 byte at each of the 2 sites.

## r027:3  src/logic.s:93-99  (window i0060)  M -2 B -2  [high]  review: accept

Cycles: one site (logic.s:2592, fc zp): original 2+3+2+3 + (bvc taken 3 | 2+eor 2) + bmi = 13/14 + branch; new 3+2+3+2+2 = 12 + branch. Faster on every path, both targets

Assumption: Only use: logic.s:2592 'bgt16i fc, -256, :+ / jmp @done / : sub16i fc,16': not-taken path jmps (no flags/A read), taken path starts sec/lda (A, flags dead). imm+1 must not overflow ($7FFF): the only imm is -256. fc=$B0 zero page.

```
        lda #<(imm)
        cmp var
        lda #>(imm)
        sbc var+1
        bvc :+
        eor #$80
:      bmi label
```
->
```
        lda var                     ; var > imm is var >= imm+1: ble16i's bias form
        cmp #<((imm)+1)
        lda var+1
        eor #$80
        sbc #(>((imm)+1) ^ $80)
:                                   ; placeholder - keeps the anonymous-label count
        bcs label
```

Signed var > imm <=> var >= imm+1 <=> (var ^ $8000) >= ((imm+1) ^ $8000) unsigned; lda/cmp/lda/eor/sbc computes that unsigned 16-bit compare, C=1 iff >=, exactly the ble16i form with the branch sense inverted (bcs vs bcc). Checked fc=$FF01 (-255): taken; $FF00 (-256): not; $7FFF: taken; $8000: not.

Review: Lines 93-99 match the file exactly. The only use is at 2592, bgt16i fc,-256,:+. Signed fc > -256 holds exactly when (fc ^ $8000) >= $7F01 unsigned, and lda / cmp #$01 / lda / eor / sbc #$7F leaves that result in C. The placeholder ':' keeps the anonymous-label count, and the listing shows B0 03 skipping jmp @done to the ':' before sub16i. On the not-taken path, jmp @done reaches an rts back to process_object's callers (1059 setbank, 1100 inc BINI), so A and the flags are dead. The taken path starts with sub16i's sec / lda. Size goes from 14 to 12 bytes, and cycles from 13 or 14 to 12, plus the branch.

## r028:0  src/logic.s:314-434  (window i0177)  M -16 B -16  [medium]  review: accept

Cycles: tilexy in-map 76->62 (x part 35->29, y part+rts 41->33); x-out 17->15; y-out 17->15 minus the x saving; getinfo/gettileattr branch polarity flipped at equal cost (bcc taken 3 = bcs taken 3). getaltitude: every path faster (prologue 30->18 before the branch; below path -2 more for the dropped clc; notbelow n4!=0 -7; notbelow second-lookup paths -6/-13). Model B identical (no macros involved).

Assumption: (1) COMPANION EDIT REQUIRED: tilexy's carry is inverted (now C=0 inside the map), so src/logic.s:1121 (DBGTILE-only debug block) `bcc @dbgt` must become `bcs @dbgt` -- the default builds and the gate do not assemble DBGTILE, so apply the separate 1121 candidate with this one. All other tilexy callers are inside this range (getinfo 351, gettileattr 431) or ignore C (logic.s:3077). (2) qx+1/qy+1 < mapw+1/maph+1 <= 8: mapw = 8<<maplw with lw<=8 (game.s:33-48; a map row is at most 256 tiles, maprow's 8-lw shift), so hi<=7, which the ror form needs (the asl form's comment already relies on it). (3) q2,q3,q4 are dead after tilexy/getinfo/getaltitude: q2..q5 appear only in logic.s, and every other read is preceded by a write in its own routine (object init writes q2/q3/q4 at 533/553/556 before reads at 573-785; logic.s:3078-3084 and 3120-3132 write before reading); getaltitude's callers (1324,1387,1585,1880) never read q2/q3/q4. (4) X is dead after getaltitude (new X = alt byte on the notbelow paths instead of the tile x): callers 1324->first X use is `tax` at 1442 (via gravity, vy_step, bar_touch, gettileattr, none reading X); 1387 same; 1585 loop -> `tax` at 1681, no X read or jsr between; 1880 -> m_addsprite does ldx NSPR, else rts from player_update, reached by jmp from game_frame, whose callers (game.s:251-303) do not read X. (5) Y behaviour unchanged. (6) nothing references getaltitude's internal labels from outside (cheap labels).

```
tilexy: lda qx+1
        cmp mapw+1
        bcs @out
        asl
        asl
        asl
        asl
        asl
        sta q2
        lda qx
        lsr
        lsr
        lsr
        ora q2
        tax
        lda qy+1
        cmp maph+1
        bcs @out
        asl
        asl
        asl
        asl
        asl
        sta q2
        lda qy
        lsr
        lsr
        lsr
        ora q2
        sec
        rts
@out:   clc
        rts

; getinfo: A = alt byte for pixel (qx, qy), 8 if outside the map
getinfo:
        jsr tilexy                  ; X = qx>>3, A = qy>>3
        bcs :+
        lda #8
        rts
:       jsr maptile
        ; alt class = LV_ALTCLS[tile id]
        tay
        lda LV_ALTCLS,y
        ; ALTTAB[cls*8 + (qx&7)]
        asl
        asl
        asl
        sta q2
        lda qx
        and #7
        ora q2
        tay
        lda LV_ALTTAB,y
        rts

; getaltitude: A = altitude (signed) at pixel (qx, qy)  [qy modified]
getaltitude:
        jsr getinfo
        sta q3                      ; n3
        lsr
        lsr
        lsr
        lsr
        sta q4                      ; n4
        lda qy
        and #7
        sta q5                      ; n5
        lda q3
        and #15
        cmp q5
        beq @below
        bcs @notbelow
@below: lda qy
        clc
        adc #8
        sta qy
        bcc @b1
        inc qy+1
@b1:    jsr getinfo
        lsr
        lsr
        lsr
        lsr
        clc
        adc #9                      ; the extra 1 pays the borrow: A <= 24 so adc leaves
        sbc q5                      ; C = 0, and (A+9) - q5 - 1 is the (A+8) - q5 wanted
        rts
@notbelow:
        lda q4
        bne @d2
        lda qy
        sec
        sbc #8
        sta qy
        bcs :+
        dec qy+1
:       jsr getinfo
        sta q3
        and #15
        cmp #8
        bne @done
        lda q3
        lsr
        lsr
        lsr
        lsr
        sbc #8
        sta q4
@done:  lda q4
@d2:    sec
        sbc q5
        rts

; gettileattr: A = attribute byte for the tile at (qx,qy): bits0-2 push+3, bit7 kill ; 3 if outside
gettileattr:
        jsr tilexy                  ; X = qx>>3, A = qy>>3
        bcs :+
        lda #3
        rts
:       jsr maptile
```
->
```
tilexy: lda qx+1
        cmp mapw+1
        bcs @out
        lsr                         ; hi <= 7: A = hi >> 1, C = hi & 1
        sta q2
        lda qx
        and #$F8
        ora q2                      ; lo7..lo3, 0, hi2, hi1 (C = hi0)
        ror
        ror
        ror                         ; hi2..hi0, lo7..lo3 (C = 0)
        tax
        lda qy+1
        cmp maph+1
        bcs @out
        lsr                         ; as for x: (hi << 5) | (lo >> 3) by rotation
        sta q2
        lda qy
        and #$F8
        ora q2
        ror
        ror
        ror
@out:   rts                         ; C = 0 inside (the rors), 1 outside (the bcs)

; getinfo: A = alt byte for pixel (qx, qy), 8 if outside the map
getinfo:
        jsr tilexy                  ; X = qx>>3, A = qy>>3
        bcc :+
        lda #8
        rts
:       jsr maptile
        ; alt class = LV_ALTCLS[tile id]
        tay
        lda LV_ALTCLS,y
        ; ALTTAB[cls*8 + (qx&7)]
        asl
        asl
        asl
        sta q2
        lda qx
        and #7
        ora q2
        tay
        lda LV_ALTTAB,y
        rts

; getaltitude: A = altitude (signed) at pixel (qx, qy)  [qy modified]
getaltitude:
        jsr getinfo
        tax                         ; X = n3 (the alt byte)
        and #15
        sta q3                      ; n3 & 15
        lda qy
        and #7
        sta q5                      ; n5
        cmp q3
        bcc @notbelow               ; n5 < (n3 & 15)
@below: lda qy                      ; C = 1 from the cmp: +7 is +8
        adc #7
        sta qy
        bcc @b1
        inc qy+1
@b1:    jsr getinfo
        lsr
        lsr
        lsr
        lsr
        clc
        adc #9                      ; the extra 1 pays the borrow: A <= 24 so adc leaves
        sbc q5                      ; C = 0, and (A+9) - q5 - 1 is the (A+8) - q5 wanted
        rts
@notbelow:
        txa
        lsr
        lsr
        lsr
        lsr                         ; n4
        bne @d2
        lda qy
        sec
        sbc #8
        sta qy
        bcs :+
        dec qy+1
:       jsr getinfo
        tax
        and #15
        cmp #8
        beq @eq
        lda #0                      ; n4, which was 0
        beq @d2
@eq:    txa
        lsr
        lsr
        lsr
        lsr                         ; C = 1: the low nibble is 8
        sbc #8
@d2:    sec
        sbc q5
        rts

; gettileattr: A = attribute byte for the tile at (qx,qy): bits0-2 push+3, bit7 kill ; 3 if outside
gettileattr:
        jsr tilexy                  ; X = qx>>3, A = qy>>3
        bcc :+
        lda #3
        rts
:       jsr maptile
```

tilexy: lsr/sta q2/lda lo/and #$F8/ora q2 builds A = lo7..lo3,0,hi2,hi1 with C = hi0; ror x3 rotates the 9-bit C:A ring to A = hi2 hi1 hi0 lo7..lo3 = (hi<<5)|(lo>>3), C = 0 (the zero bit), N/Z from the result as ora left them before. Brute-forced for all hi 0..7, lo 0..255. Y part ends with C=0 inside, so falls straight into @out: rts; both bcs @out arrive with C=1 and the same A/X as before (A = the high byte, X unchanged / x tile), so the only change is the carry sense; getinfo and gettileattr test bcc instead of bcs. getaltitude: keep the alt byte in X (tax/txa replace sta q3/lda q3); compare q5 against (alt&15) stored in q3: cmp q3 gives C=1 iff q5 >= alt&15, exactly the old beq-or-bcc 'below' condition, and the below path then has C=1 so clc/adc #8 becomes adc #7 (same sum and carry out). n4 is not stored: @notbelow recomputes it from X (txa/lsr x4, Z for the bne, A = n4 for @d2). The old @done path reloaded q4, which is provably 0 there (only reached after lda q4/bne fell through; getinfo does not write q4), so it becomes lda #0 / beq @d2 (always). The equal path: C=1 after four lsr because alt's low nibble is 8 (same as the old code's reliance), sbc #8, fall into @d2. Brute-force model of old vs new getaltitude over random getinfo tables and qy: identical A, C and qy. Measured by assembling both targets: Master LOGIC 9572->9556, Model B LGCCODE 10762->10746 (with the i0191 eor candidate on top: 9554 / 10744).

Review: Original matches logic.s 314..434 exactly. The rotation was checked for every hi 0..7 and lo 0..255: A=(hi<<5)|(lo>>3) and C=0. hi<=7 holds because mapshr=8-lw (engine.s:4513) forces lw<=8, so mapw+1<=8. The out paths return the same A and X with C=1, and the getinfo/gettileattr bcc tests are right. getinfo and gettileattr now return C=1 on the out path. For gettileattr in the map, C is now 0 on the Master, because the carry passes through maprow/pagelogic. No caller reads it: 1139 (bpl, then no carry read before clc/sec in player_hit, player_update or player_dead), 1419 (and, then sec) and 3077 (mark_dirty's cmp overwrites it). Old and new getaltitude were modelled over 20000 random tables: A and qy match, and N/Z/C/V come from the same final sbc. X changes only on the notbelow paths. The callers at 1324, 1387, 1585 and 1880 never read X before writing it (checked through @push/gettileattr, the friction tax, the @hl loop, m_addsprite and the rts to game.s). q2..q5 are used only in logic.s, and every other read comes after a write. Scratch build: Master LOGIC 9572->9556, Model B LGCCODE 10762->10746 (9554/10744 with index 5). No path is slower. It MUST be applied together with index 1 (DBGTILE line 1121). The header comment at logic.s:311 ('carry set if inside') goes stale and should say 'carry clear if inside'.

## r028:1  src/logic.s:1121-1121  (window i0177)  M -0 B -0  [high]  review: accept

Cycles: same (branch polarity only)

Assumption: ONLY together with the i0177 tilexy/getinfo/getaltitude/gettileattr candidate (lines 314-434), which makes tilexy return C=0 inside the map. DBGTILE-only code: not assembled by the default builds, so it measures 0 bytes; without it the DBGTILE build would call maptile outside the map and skip it inside.

```
        bcc @dbgt
```
->
```
        bcs @dbgt
```

tilexy's carry sense is inverted by the companion candidate; this debug caller skips when outside, i.e. on C=1 now.

Review: Line 1121 is 'bcc @dbgt' in the DBGTILE block after jsr tilexy. It is correct only together with index 0, which inverts tilexy's carry. The -D DBGTILE=1 build assembles with index 0 applied. It saves 0 bytes, so an applier that requires a shrink will refuse it on its own. Apply it in the same step as index 0, never alone: alone it breaks what the DBGTILE build does.

## r028:2  src/logic.s:371-426  (window i0226)  M -9 B -9  [high]  review: accept

Cycles: prologue after jsr getinfo 30->18 (+ branch: below-equal 33->20, below-less 34->20, notbelow 35->21); below path -2 more (adc #7 without clc); notbelow n4!=0: lda q4 (3) -> txa+lsr x4 (10), net -7 overall; second-lookup: sta q3->tax -1, equal path 21->15, not-equal 6->7, net -7 overall. Every path faster; Model B identical.

Assumption: q3/q4 dead after getaltitude and X dead after getaltitude (see the composite's items 3 and 4: callers logic.s 1324,1387,1585,1880; q3/q4 only read in logic.s after a write in the same routine at 553/556 and 3078/3084). getinfo does not read q3/q4/q5 or need X in. Superseded by the i0177 composite if that applies.

```
getaltitude:
        jsr getinfo
        sta q3                      ; n3
        lsr
        lsr
        lsr
        lsr
        sta q4                      ; n4
        lda qy
        and #7
        sta q5                      ; n5
        lda q3
        and #15
        cmp q5
        beq @below
        bcs @notbelow
@below: lda qy
        clc
        adc #8
        sta qy
        bcc @b1
        inc qy+1
@b1:    jsr getinfo
        lsr
        lsr
        lsr
        lsr
        clc
        adc #9                      ; the extra 1 pays the borrow: A <= 24 so adc leaves
        sbc q5                      ; C = 0, and (A+9) - q5 - 1 is the (A+8) - q5 wanted
        rts
@notbelow:
        lda q4
        bne @d2
        lda qy
        sec
        sbc #8
        sta qy
        bcs :+
        dec qy+1
:       jsr getinfo
        sta q3
        and #15
        cmp #8
        bne @done
        lda q3
        lsr
        lsr
        lsr
        lsr
        sbc #8
        sta q4
@done:  lda q4
@d2:    sec
        sbc q5
        rts
```
->
```
getaltitude:
        jsr getinfo
        tax                         ; X = n3 (the alt byte)
        and #15
        sta q3                      ; n3 & 15
        lda qy
        and #7
        sta q5                      ; n5
        cmp q3
        bcc @notbelow               ; n5 < (n3 & 15)
@below: lda qy                      ; C = 1 from the cmp: +7 is +8
        adc #7
        sta qy
        bcc @b1
        inc qy+1
@b1:    jsr getinfo
        lsr
        lsr
        lsr
        lsr
        clc
        adc #9                      ; the extra 1 pays the borrow: A <= 24 so adc leaves
        sbc q5                      ; C = 0, and (A+9) - q5 - 1 is the (A+8) - q5 wanted
        rts
@notbelow:
        txa
        lsr
        lsr
        lsr
        lsr                         ; n4
        bne @d2
        lda qy
        sec
        sbc #8
        sta qy
        bcs :+
        dec qy+1
:       jsr getinfo
        tax
        and #15
        cmp #8
        beq @eq
        lda #0                      ; n4, which was 0
        beq @d2
@eq:    txa
        lsr
        lsr
        lsr
        lsr                         ; C = 1: the low nibble is 8
        sbc #8
@d2:    sec
        sbc q5
        rts
```

As in the composite: X holds the alt byte (tax/txa for sta q3/lda q3); q3 = alt&15 and cmp q3 from A = q5 gives C=1 exactly when the old code went to @below (equal or less), so @below knows C=1 and adc #7 replaces clc/adc #8; q4 is replaced by recomputing n4 from X in @notbelow; the @done reload of q4 is 0 on that path (it fell through lda q4/bne) so lda #0/beq @d2; the equal path keeps its C=1 from the nibble-8 low bits after four lsr. Brute-force model of both versions agrees on A, C and qy. Measured: 9 bytes on both targets.

Review: Original matches 371..426 exactly. It is the same as the getaltitude part of index 0, so index 0 supersedes it if applied. Equivalence was checked with the random-table model. X, q3 and q4 are dead after getaltitude, as checked for index 0. Measured Master 9572->9563, Model B 10762->10753. Every path is faster.

## r028:3  src/logic.s:317-327  (window i0159)  M -2 B -2  [high]  review: accept

Cycles: 27->21 on the in-map path; out path unchanged. Model B same.

Assumption: qx+1 <= 7 after the cmp mapw+1/bcs (mapw = 8<<lw, lw<=8: game.s:33-40), which the existing code already relies on. q2 is overwritten with hi>>1 instead of hi<<5: its only reader is the ora in the same sequence (then overwritten by the y part). Carry after the sequence is 0 instead of qx bit 2: the next instructions are lda qy+1 / cmp, which overwrite it.

```
        asl
        asl
        asl
        asl
        asl
        sta q2
        lda qx
        lsr
        lsr
        lsr
        ora q2
```
->
```
        lsr                         ; hi <= 7: A = hi >> 1, C = hi & 1
        sta q2
        lda qx
        and #$F8
        ora q2                      ; lo7..lo3, 0, hi2, hi1 (C = hi0)
        ror
        ror
        ror                         ; hi2..hi0, lo7..lo3 (C = 0)
```

A=hi (00000h2h1h0). lsr: A=000000h2h1, C=h0. sta q2. lda qx; and #$F8 -> l7..l3 000; ora q2 -> l7 l6 l5 l4 l3 0 h2 h1 (C=h0 untouched). ror x3 on the 9-bit ring C:A gives A = h2 h1 h0 l7 l6 l5 l4 l3 = (hi<<5)|(lo>>3), C=0. N/Z as from the old ora (same value). Brute-forced hi 0..7 x lo 0..255. Superseded by the i0177 composite if that applies.

Review: Original matches 317..327 exactly. Rotation checked for hi 0..7. The C=0 it leaves is overwritten by cmp maph+1, and q2 is dead after tilexy. Measured 2/2 bytes, 25->19 cycles. Index 0 supersedes it.

## r028:4  src/logic.s:332-342  (window i0174)  M -2 B -2  [high]  review: accept

Cycles: 27->21 on the in-map path; out path unchanged. Model B same.

Assumption: qy+1 <= 7 after cmp maph+1/bcs (maph = 8<<lh, lh<=8: game.s:41-48). The following sec/rts fix the returned carry, so the rors' C=0 is irrelevant. q2 is dead after tilexy (see q2 notes: every later read is preceded by a write).

```
        asl
        asl
        asl
        asl
        asl
        sta q2
        lda qy
        lsr
        lsr
        lsr
        ora q2
```
->
```
        lsr                         ; as for x: (hi << 5) | (lo >> 3) by rotation
        sta q2
        lda qy
        and #$F8
        ora q2
        ror
        ror
        ror
```

Same rotation as the x part: A = (hi<<5)|(lo>>3) with N/Z from the result (rts follows sec, which does not touch N/Z). Superseded by the i0177 composite if that applies.

Review: Original matches 332..342 exactly. Same rotation as index 3. sec/rts follow, so the returned carry is unchanged and N/Z come from the same value. Measured 2/2 bytes. Index 0 supersedes it.

## r028:5  src/logic.s:362-365  (window i0191)  M -2 B -2  [high]  review: accept

Cycles: 11->8 (sta 3, lda 3, and 2, ora 3 -> eor 3, and 2, eor 3). Model B same.

Assumption: q2 dead after getinfo: getinfo's only caller is getaltitude, which never reads q2, and no caller of getaltitude reads q2 before writing it (q2 is read only in logic.s after writes at 533, 3082, 3129 in the same routines).

```
        sta q2
        lda qx
        and #7
        ora q2
```
->
```
        eor qx                      ; (A & $F8) | (qx & 7): A's low 3 bits are 0
        and #$F8
        eor qx
```

A = cls<<3 has bits 0-2 clear. eor qx; and #$F8; eor qx = ((A^qx)&$F8)^qx: high 5 bits A, low 3 bits qx&7 = A|(qx&7), same as sta q2/lda qx/and #7/ora q2. Carry untouched in both (from the last asl). N/Z are overwritten by tay/lda that follow. Brute-forced all A,qx. Independent of the composite (its lines are left verbatim there), so it still applies after it.

Review: Original matches 362..365 exactly, and these lines are also unchanged inside index 0's proposal, so it still applies after index 0. A=cls<<3 has its low 3 bits clear, so ((A^qx)&$F8)^qx = A|(qx&7). Neither version touches the carry, and tay/lda overwrite N/Z. Nothing reads q2 after getinfo: getaltitude's callers write it before reading. Measured 2/2 bytes alone and on top of index 0, 11->8 cycles.

## r029:0  src/logic.s:422-423  (window i0256)  M -1 B -1  [medium]  review: accept

Cycles: bne @done path: unchanged (lda q4 3). fall-through: sta q4 (3) + lda q4 (3) = 6 -> bit $C6A5 (4). Model B same.

Assumption: q4 ($C6) is dead after getaltitude returns: the only other readers of q4 are level_init (logic.s:618-783, after its own sta q4 at 556) and the tile-put code at logic.s:3078-3096 (stx q4 first); no other symbol aliases $C6 (grep). The skipped operand address is $C6A5 (lda zp = $A5, q4 = $C6): MOS ROM / HAZEL on the Master, OS ROM on the Model B -- not I/O, the read is harmless. Flags from bit are overwritten by the sec/sbc q5 at @d2 (V too: sbc sets it).

```
        sta q4
@done:  lda q4
```
->
```
        .byte $2C                   ; bit abs: skips the lda (A is already the result; q4 is dead after the rts)
@done:  lda q4
```

Fall-through: A = (q3>>4)-8 already; original stores it to q4 and reloads it (A unchanged). New: the $2C opcode makes 'lda q4' the operand of a bit abs, so A is kept, only N/V/Z change and the next sbc rewrites them. The bne @done path still lands on lda q4 and loads q4 as before. Result A and flags at rts identical; only difference is q4's final value on the fall-through path (dead).

Review: Original matches 422-423. q4=$C6 is zp on both (ZEROPAGE ends $A5), no alias of $C6 anywhere; q4 rewritten at logic.s:378 on every getaltitude call before the read at 403; other readers (level_init 618+, after sta at 556; ob_ code 3078/3120 write first). bit $C6A5 reads ROM/HAZEL, no side effects; A and C untouched, N/V/Z overwritten by sec/sbc q5. @done has one referrer (bne at 415). Assembled: -1 byte both. Fall-through 6->4 cycles, branch path unchanged.

## r029:1  src/logic.s:470-491  (window i0299)  M -1 B -11  [high]  review: accept

Cycles: Master: stz BINOK 4 + ldx# 2 + lda# 2 + stz stars 3 + stz bent 3 = 14 -> lda# 2 + tax 2 + sta BINOK 4 + sta stars 3 + sta bent 3 = 14. Model B: 2+4+2+2+12+12 = 34 -> 14. Loops unchanged.

Assumption: stars (.zeropage) and bent ($CF) are zero page on both targets (Model B listing: stza stars = 48 A9 00 85 rr 68) and lie outside both cleared areas (O_STAMP..+2559 at $B000/bank 7, LV_GRID) so zeroing them before the loops instead of after is the same final state; neither loop reads them. BINOK is absolute on both (engine.s:341/420). After the loops A=$FF, X=$FF exactly as before; the next flag consumer is 'ldy nobj / bne' which sets its own flags.

```
        stz BINOK                   ; the cached object list belongs to the old level
        ldx #0
        lda #0
:       sta O_STAMP,x
        sta O_STAMP+256,x
        sta O_STAMP+512,x
        sta O_STAMP+768,x
        sta O_STAMP+1024,x
        sta O_STAMP+1280,x
        sta O_STAMP+1536,x
        sta O_STAMP+1792,x
        sta O_STAMP+2048,x
        sta O_STAMP+2304,x
        inx
        bne :-
        ldx #127
        lda #$FF
:       sta LV_GRID,x
        dex
        bpl :-
        stza stars
        stza bent
```
->
```
        lda #0
        tax
        sta BINOK                   ; the cached object list belongs to the old level
        sta stars
        sta bent
:       sta O_STAMP,x
        sta O_STAMP+256,x
        sta O_STAMP+512,x
        sta O_STAMP+768,x
        sta O_STAMP+1024,x
        sta O_STAMP+1280,x
        sta O_STAMP+1536,x
        sta O_STAMP+1792,x
        sta O_STAMP+2048,x
        sta O_STAMP+2304,x
        inx
        bne :-
        ldx #127
        lda #$FF
:       sta LV_GRID,x
        dex
        bpl :-
```

lda #0/tax gives A=0,X=0 as ldx #0/lda #0 did; sta BINOK = stz BINOK; sta stars/sta bent store the same zero stz/stza stored, just earlier (A=0 then). The stza pair after the grid loop is deleted: on the 65C02 it was stz (no A/flag effect), on the 6502 pha/lda#0/sta/pla (A restored to $FF, N,Z from 0 but ldy nobj follows).

Review: Original matches 470-491. stars ($8A/$89) and bent ($CF) are zp on both, outside O_STAMP ($B000..$B9FF) and LV_GRID; BINOK absolute (engine.s:341/420); no IRQ path reads BINOK/stars/bent. A=0,X=0 into the stamp loop as before; A=$FF after grid loop as before (Model B stza restored it); next flag consumer is ldy nobj. Master 14->14 cycles, Model B 34->14. Assembled: Master LOGIC -1, Model B LGCCODE -11. Overlaps candidate 2 (lines 485-489 carried verbatim): the two compose (stamp loop still exits X=0); combined build verified.

## r029:2  src/logic.s:485-489  (window i0310)  M -2 B -2  [high]  review: accept

Cycles: setup ldx# 2 + lda# 2 = 4 -> dex 2 + txa 2 = 4; loop 128 iterations of sta abs,x 5 + dex 2 + branch 3 (last 2) both ways: identical (abs,x store is 5 cycles regardless of page). Model B same.

Assumption: X = 0 on entry: the preceding stamp loop exits on inx/bne with X = 0 (logic.s:483-484, independent of candidate i0299). After the loop X = $7F instead of $FF: X is dead -- next reads of X are after 'jsr @x8' (tax) at logic.s:526 on the @ol path, @objdone (logic.s:828-848) never reads X, and the caller (game.s:246 jsr t_level_init) does lda #1 next and never reads X. A = $FF afterwards as before. Flags after the loop: next consumer is ldy nobj/bne which sets its own.

```
        ldx #127
        lda #$FF
:       sta LV_GRID,x
        dex
        bpl :-
```
->
```
        dex                         ; X = $FF: the stamp loop left X = 0
        txa                         ; A = $FF
:       sta LV_GRID-128,x           ; X = $FF..$80: LV_GRID+127 down to +0
        dex
        bmi :-
```

Original stores $FF to LV_GRID+127..LV_GRID+0 with X 127..0, exit on X=$FF (bpl). New: X runs $FF..$80, address LV_GRID-128+X = LV_GRID+127..+0, exit when dex gives $7F (bmi not taken). Same 128 stores of the same value, same order.

Review: Original matches 485-489. X=0 on entry (inx/bne exit). LV_GRID-128+X for X=$FF..$80 covers LV_GRID+127..+0, same stores/order; sta abs,x always 5 cycles; setup 4->4. Exit X=$7F instead of $FF: X dead -- @ol path reaches jsr @x8 (tay..tax writes X first) without reading X; @objdone (mov16/stz/stza) never reads X; level_init returns straight to game.s:246 which does lda #1/sta/jsr t_game_frame (game_frame writes X before reading). Assembled -2 both. Overlaps candidate 1: apply merged/on top of it.

## r029:3  src/logic.s:500-519  (window i0324)  M -13 B -15  [high]  review: accept

Cycles: original 52. Master: no carry 2+3+2+2+5+3+3+2+3+5+5 = 35, carry 2+3+2+2+5+3+2+5+2+3+5+5 = 39. Model B (no final inc): 30 / 34. All <= 52.

Assumption: obj holds Y (sty obj at logic.s:498, Y untouched since). obj <= OBJN-1 = 148 (any 0..255 works: brute-forced all o in 0..255 for >LV_OBJS = $81 (Master engine.s:72) and $78 (Model B defs.inc:43): t16 = LV_OBJS + 6*o exactly, and neither rol carries out). <LV_OBJS = 0 on both. t16b is dead: next writes at logic.s:717/734 precede its reads (mod16), nothing else in level_init reads it; engine.s's use (4575+) writes first. C after the block is not consumed before jsr @x8 (ldazy/sta/ldy/lda/sta only). Assembled and checked with ca65 --cpu 6502 for both constants.

```
        tya
        asl
        sta t16
        lda #0
        rol
        sta t16+1
        lda t16
        asl
        sta t16b
        lda t16+1
        rol
        sta t16b+1
        clc
        lda t16
        adc t16b
        sta t16
        lda t16+1                   ; <LV_OBJS = 0: only the high byte moves
        adc t16b+1
        adc #>LV_OBJS               ; the high-byte add cannot carry: t16+1 <= 1, t16b+1 <= 3
        sta t16+1
```
->
```
        lda #(>LV_OBJS) >> 2        ; t16+1 seed: the two rols make it (>LV_OBJS) & $FC
        sta t16+1
        tya
        asl                         ; A = lo(2obj), C = obj.7
        rol t16+1                   ; C = 0: the seed is < $40
        adc obj                     ; A = lo(3obj), C = its carry (obj = Y, sty above)
        bcc @o3
        inc t16+1                   ; t16+1 = 2*seed + hi(3obj)
@o3:    asl                         ; A = lo(6obj), C = bit 8
        sta t16
        rol t16+1                   ; = 4*seed + hi(6obj); C = 0
  .assert ((>LV_OBJS) & 3) < 2, error, "LV_OBJS: the seed needs a second inc"
  .if (>LV_OBJS) & 1
        inc t16+1                   ; Master $81: the bit the seed's >> 2 dropped
  .endif
```

t16+1 := s = H>>2. rol with C=obj.7 gives 2s+o7 (C=0 since s<$40). adc obj: A = lo2 + o = lo(3o), carry k; inc on k gives 2s + hi(3o) (hi(3o) = o7+k). asl: lo(6o) = lo(3o)<<1, C = bit7 of lo(3o); rol gives 4s + 2*hi(3o) + b = 4s + hi(6o). 4s = H & $FC; the .if adds H & 1 (Master $81). Result t16 = lo(6o), t16+1 = H + hi(6o) as original.

Review: Original matches 500-519. Brute-forced o=0..255 for >LV_OBJS=$81 (Master) and $78 (Model B): t16 = LV_OBJS+6o exactly, both rols carry out 0, so C=0 at exit like the original. obj=Y (sty obj at 498, Y untouched). t16b's value is dead: only readers are mod16 (after writes at 717-737), 2708 (after 2701 write), engine.s 4588+ (after 4575-4587 writes); no zp alias of $D3/$D4. V differs but no bvc/bvs/php in level_init; N,Z reset by ldazy. @o3 unique in scope, no ':' change. Assembled: seed $20/$1E, Master -13, Model B -15. Cycles 52 -> Master 35/39, Model B 30/34.

## r029:4  src/logic.s:520-520  (window i0325)  M -0 B -6  [high]  review: accept

Cycles: Master unchanged (lda (t16) 5). Model B: ldazy = sty mtmp 3 + ldy# 2 + lda (zp),y 5 + ldy mtmp 3 + ora# 2 = 15 -> ldx# 2 + lda (zp,x) 6 = 8.

Assumption: X is dead from here to 'jsr @x8' at logic.s:526 (@x8 does tax; lines 521-525 are sta otype / sta O_TYPE,y / ldy #1 / lda (t16),y / sta q1, none reads X). mtmp ($DF) is only macro scratch, so not writing it is harmless. Flags: ldazy's ora #0 leaves N,Z from A; lda (t16,x) sets the same; neither consumed (sta, sta, ldy #1 follow).

```
        ldazy t16                   ; lda (t16), Y kept: it is still obj
```
->
```
  .if MODELB
        ldx #0                      ; X is free until jsr @x8: (t16,x) is (t16), Y kept
        lda (t16,x)
  .else
        ldazy t16                   ; lda (t16), Y kept: it is still obj
  .endif
```

With X=0, (t16,x) fetches the pointer from t16/t16+1 exactly as (t16),y with Y=0 did; A = same byte, Y still obj (never touched), N/Z from A as before.

Review: Original matches 520. Model B only; X dead from 520 to jsr @x8 (521-525 are sta/sta O_TYPE,y/ldy #1/lda (t16),y/sta q1; @x8 writes X via tax). With X=0, (t16,x) = (t16),y with Y=0; Y stays obj; N,Z from the load as ldazy's ora #0 gave. Assembled -6 on Model B, Master unchanged. 15->8 cycles.

## r029:5  src/logic.s:563-563  (window i0366)  M -0 B -3  [high]  review: accept

Cycles: Master unchanged. Model B: deca = sta mtmp 3 + dec mtmp 5 + lda mtmp 3 = 11 -> clc 2 + adc# 2 = 4.

Assumption: Carry is dead: the next instruction (the ':' target) is lsr, which overwrites C, on both paths. mtmp not read by anything here. No bvc/bvs/clv/php anywhere in level_init (grep of logic.s:442-850).

```
        deca
```
->
```
  .if MODELB
        clc                         ; A - 1: the carry dies at the lsr
        adc #$FF
  .else
        deca
  .endif
```

A + $FF with C=0 = A - 1 mod 256, same as deca's dec. N,Z,V,C differ but all are overwritten by lsr (C) and lsr (N,Z) before use; V is not read anywhere in level_init.

Review: Original matches 563. Reached only with A!=0 (beq skips it); clc/adc #$FF = A-1. Next instruction lsr (the ':' target) overwrites C, N, Z; V never consumed in level_init. Model B deca 6 bytes/11 cycles -> 3 bytes/4 cycles. Assembled -3 Model B, Master unchanged.

## r029:6  src/logic.s:579-579  (window i0381)  M -0 B -3  [high]  review: accept

Cycles: Master unchanged. Model B: inca = sta mtmp 3 + inc mtmp 5 + lda mtmp 3 = 11 -> clc 2 + adc# 2 = 4.

Assumption: Carry is dead: lsr follows immediately (logic.s:580). V from adc is never read: no bvc/bvs/clv/php anywhere in level_init (grep of logic.s:442-850).

```
        inca
```
->
```
  .if MODELB
        clc                         ; A + 1: the carry dies at the lsr
        adc #1
  .else
        inca
  .endif
```

A + 1 mod 256 either way (q2 = 255 wraps to 0 in both); flags differ only in C/V which lsr overwrites (C) or nothing reads (V); N,Z come from the lsr chain.

Review: Original matches 579. clc/adc #1 = A+1 mod 256; lsr follows immediately and sets C,N,Z; V never consumed in level_init. Model B 6 bytes/11 cycles -> 3 bytes/4 cycles. Assembled -3 Model B, Master unchanged.

## r030:0  src/logic.s:584-786  (window i0417)  M -40 B -58  [high]  review: accept

Cycles: Measured sizes: Master -40, Model B -58. Per type (both targets unless noted): 0: dispatch same; t0 same except Model B q2!=0 deca 11 -> sec/sbc 4. 1: clc (2) and always-taken bcc (3) gone; Model B inca 11 -> adc #0 2; Master same. 2/5/6: jmp @box 3 -> bpl @box taken 3. 3: jmp @t3 gone (-3). 4: each A+1/B+1 build 20 -> 8 (-24 total); Model B deca 11 -> 4; bra/jmp @box 3 -> bpl taken 3. 9: its bra @box gone, falls into @box (-3). 11: bne-nt 2 + lda 3 + sta 3 + bpl-taken 3 + jmp 3 = 14; original bne-nt 2 + jmp 3 + lda 3 + sta 3 + bra 3 = 14. 12: bne @n11 taken 3 (was bne :+ taken 3), cmp 2, bne-nt 2, body, jmp @box 3; original cmp 2, bne-nt 2, jmp @t12 3, body, fall through: equal. 7/10/other: cmp #12 + bne-taken 3 + jmp 3: equal.

Assumption: (1) O_XL,y = (q1<<3)&255 from @x8 (logic.s:537-541) and not written again before @t1, so bit 2 is clear: +4 never carried and ora #4 gives the same value. (2) O_AL = (q3<<4)&255 and O_BL = (q4<<4)&255 have a clear low nibble, so O_xL+1 = O_xL|1 with no carry (t16b+1 = O_xH). (3) m_rnd (rnd, or the Master's TOMAIN jsr rnd / jmp pagelogic with pha..pla) and mod16 never write t16b/t16b+1 or Y. mod16 only reads t16b. Nothing reads O_BL/O_BH between the old and new store points, so moving the q4 block after the first mod16 keeps every value and the rnd call order. (4) C = 1 at @t1: its only entry is beq @t1 after cmp #1 matched. ora/sta/lda keep C, so Model B adc #0 = q1+1. The carry is dead after (lsr). (5) gy = q2>>3 <= 31 when the dispatch reaches cmp #11 (set at logic.s:577; nothing on the dispatch path writes it), so lda gy gives N=0 and bpl is always taken. (6) N=0 after lsr/sta for the t4 and t256 bpl @box. (7) V is dead: there is no bvc/bvs in level_init (sbc/adc replace deca). (8) @t0/@t1/@t3/@t4/@t9/@t11/@t12/@t256 have no other references (grep logic.s). Branch ranges measured: beq @t1 +122 (B) / +120 (M). Verified in a scratch copy: both targets build. Master O_* + LV_GRID/BOBJ/BNEXT after level_init match base on all 16 levels, and all object types 0-7, 9-12 occur. Model B O_* + grid match the pristine Model B build on all levels. bdiff lock step OK at L0/3/5/12 for 200 frames. Gate is all green except 'B title'. Gate note: in the scratch copy EVERY Model B byte saving, even a 3-byte one, fails the gate's 'B title' check. btitlediff compares RAM $0300-$7FFF at fixed cycle counts, and the shorter bank-7 file changes the disc load timing. The pristine tree passes.

```
        ldy obj
        lda otype
        beq @t0
        cmp #1
        beq @t1
        cmp #2
        beq @t256a
        cmp #5
        beq @t256a
        cmp #6
        bne :+
@t256a: jmp @t256
:       cmp #3
        bne :+
        jmp @t3
:       cmp #4
        bne :+
        jmp @t4
:
        cmp #9
        bne :+
        jmp @t9
:
        cmp #11
        bne :+
        jmp @t11
:
        cmp #12
        bne :+
        jmp @t12
:
        jmp @box                    ; 7, 10: defaults
@t0:    lda q3
        sta O_EL,y                  ; e0 = box class from the converter (0 none/1 cyan/2 black)
        lda q4
        sta O_EH,y                  ; e1 = an enemy's range covers this one
        jsr m_rnd
        jsr mod12
        sta O_AL,y
        inc stars
        lda gy                      ; the prologue already left q2>>3 in gy
        sta gy1
        lda q2
        beq :+
        deca
:       lsr
        lsr
        lsr
        sta gy
        jmp @box
@t1:    lda O_XL,y
        clc
        adc #4
        sta O_XL,y
        bcc :+
        lda O_XH,y
        inca
        sta O_XH,y
:       lda q1
        submin0 2
        lsr
        lsr
        lsr
        sta gx0
        lda q1
        inca
        lsr
        lsr
        lsr
        sta gx1
        lda gy1
        sta gy
        jmp @box
@t256:  lda q3
        asl
        asl
        asl
        sta O_AL,y
        lda q3
        lsr
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        lda q1
        clc
        adc q3
        lsr
        lsr
        lsr
        sta gx1
        jmp @box
@t3:    lda q2
        submin0 4
        lsr
        lsr
        lsr
        sta gy
        jmp @box
@t4:    lda q3
        asl
        asl
        asl
        asl
        sta O_AL,y
        lda q3
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        lda q4
        asl
        asl
        asl
        asl
        sta O_BL,y
        lda q4
        lsr
        lsr
        lsr
        lsr
        sta O_BH,y
        ; C = rnd % (A+1) ; D = rnd % (B+1)  (A,B < 4096) -> use rnd16 & mask then reduce
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
        clc
        lda O_AL,y
        adc #1
        sta t16b
        lda O_AH,y
        adc #0
        sta t16b+1
        jsr mod16
        lda t16
        sta O_CL,y
        lda t16+1
        sta O_CH,y
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
        clc
        lda O_BL,y
        adc #1
        sta t16b
        lda O_BH,y
        adc #0
        sta t16b+1
:                                   ; placeholder - keeps the anonymous-label count
        jsr mod16
        lda t16
        sta O_DL,y
        lda t16+1
        sta O_DH,y
        lda q2
        beq :+                      ; submin0 1 specialised: max(q2-1, 0)
        deca
:       lsr
        lsr
        lsr
        sta gy
        lda q1
        clc
        adc q3
        lsr
        lsr
        lsr
        sta gx1
        lda q2
        clc
        adc q4
        lsr
        lsr
        lsr
        sta gy1
        bra @box
@t9:    lda q2
        submin0 2
        lsr
        lsr
        lsr
        sta gy
        lda q2
        lsr
        lsr
        lsr
        sta gy1
        bra @box
@t11:   lda gy
        sta gy1
        bra @box
@t12:   lda q3
        sta O_AL,y
        lda q4
        sta O_BL,y
        lda q5
        sta O_CL,y
```
->
```
        ldy obj
        lda otype
        beq @t0
        cmp #1
        beq @t1
        cmp #2
        beq @t256a
        cmp #5
        beq @t256a
        cmp #6
        bne :+
@t256a: jmp @t256
:       cmp #3
        bne @n3
        lda q2                      ; 3: y0 = max(y-4, 0) >> 3
        submin0 4
        lsr
        lsr
        lsr
        sta gy
        jmp @box
@n3:    cmp #4
        bne :+
        jmp @t4
:
        cmp #9
        bne :+
        jmp @t9
:
        cmp #11
        bne @n11
        lda gy                      ; 11: y1 = y0 (gy = q2>>3 < 32, so N = 0)
        sta gy1
        bpl @jbox
@n11:   cmp #12
        bne @jbox
        lda q3                      ; 12: e0, e1, e2 -> A, B, C
        sta O_AL,y
        lda q4
        sta O_BL,y
        lda q5
        sta O_CL,y
@jbox:  jmp @box                    ; 7, 10: defaults
@t0:    lda q3
        sta O_EL,y                  ; e0 = box class from the converter (0 none/1 cyan/2 black)
        lda q4
        sta O_EH,y                  ; e1 = an enemy's range covers this one
        jsr m_rnd
        jsr mod12
        sta O_AL,y
        inc stars
        lda gy                      ; the prologue already left q2>>3 in gy
        sta gy1
        lda q2
        beq :+
  .if ::MODELB
        sec                         ; A-1; the carry is dead (lsr follows)
        sbc #1
  .else
        deca
  .endif
:       lsr
        lsr
        lsr
        sta gy
        jmp @box
@t1:    lda O_XL,y                  ; C = 1: the dispatch's cmp #1 matched
        ora #4                      ; x*8 has bit 2 clear: +4 cannot carry into O_XH
        sta O_XL,y
        lda q1
  .if ::MODELB
        adc #0                      ; x+1, from the carry
  .else
        inca
  .endif
        lsr
        lsr
        lsr
        sta gx1
        lda q1
        submin0 2
        lsr
        lsr
        lsr
        sta gx0
        lda gy1
        sta gy
        jmp @box
@t4:    lda q3
        asl
        asl
        asl
        asl
        sta O_AL,y
        ora #1                      ; A+1 for mod16: the low nibble is clear, so no carry
        sta t16b
        lda q3
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        sta t16b+1
        ; C = rnd % (A+1) ; D = rnd % (B+1)  (A,B < 4096) -> use rnd16 & mask then reduce
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
        jsr mod16
        lda t16
        sta O_CL,y
        lda t16+1
        sta O_CH,y
        lda q4
        asl
        asl
        asl
        asl
        sta O_BL,y
        ora #1                      ; B+1, likewise
        sta t16b
        lda q4
        lsr
        lsr
        lsr
        lsr
        sta O_BH,y
        sta t16b+1
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
:                                   ; placeholder - keeps the anonymous-label count
        jsr mod16
        lda t16
        sta O_DL,y
        lda t16+1
        sta O_DH,y
        lda q2
        beq :+                      ; submin0 1 specialised: max(q2-1, 0)
  .if ::MODELB
        sec
        sbc #1
  .else
        deca
  .endif
:       lsr
        lsr
        lsr
        sta gy
        lda q1
        clc
        adc q3
        lsr
        lsr
        lsr
        sta gx1
        lda q2
        clc
        adc q4
        lsr
        lsr
        lsr
        sta gy1
        bpl @box                    ; N = 0 from the lsr
@t256:  lda q3
        asl
        asl
        asl
        sta O_AL,y
        lda q3
        lsr
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        lda q1
        clc
        adc q3
        lsr
        lsr
        lsr
        sta gx1
        bpl @box                    ; N = 0 from the lsr
@t9:    lda q2
        submin0 2
        lsr
        lsr
        lsr
        sta gy
        lda q2
        lsr
        lsr
        lsr
        sta gy1                     ; (falls into @box)
```

t3, t11 and t12 are opened in the dispatch, which saves each one's jmp @tN. t12 comes last so it shares the default's jmp @box (@jbox), and t11 reaches that by bpl (N=0). t9 becomes the last handler before @box and falls in. t256 moves after t4, so both reach @box by bpl (2 bytes, 3 cycles taken). t1: the O_XH carry path is dead (assumption 1), so clc/adc #4 becomes ora #4. gx1 is computed before gx0 (independent stores), so the dispatch's C=1 gives q1+1 with adc #0 on the Model B. t4: t16b/t16b+1 are stored straight from A while O_A/O_B are built (ora #1 is the +1), which removes both clc/lda/adc #1/sta/lda/adc #0/sta sequences. The O_B build moves after the first mod16. Model B deca in t0/t4 becomes sec/sbc #1 (carry dead: lsr follows). Every other instruction is verbatim.

Review: The original matches logic.s lines 584..786 exactly. I built it in a scratch copy: Master LOGIC is 40 bytes smaller and Model B LGCCODE 58 bytes smaller. Six facts make it correct. (1) C=1 at @t1, because its only entry is the beq after cmp #1 matched. (2) O_XL=(q1<<3)&255 comes from @x8 (logic.s:851, tya/asl x3) and nothing writes it again before @t1, so +4 never carries and ora #4 gives the same value. (3) O_AL and O_BL have their low nibble clear, so |1 equals +1 with no carry into t16b+1. (4) rnd (Model B m_rnd=rnd; Master TOMAIN jsr rnd/jmp pagelogic, which saves A) and mod16 touch neither t16b ($D3) nor Y. Nothing reads O_B between its old and new store points, and the rnd call order is unchanged. (5) gy=q2>>3<=31 on the dispatch path, so bpl after lda gy is always taken. N=0 after lsr for the t4 and t256 bpl @box. (6) V is dead: level_init has no bvc/bvs and a later adc always overwrites V. I checked the anonymous-label targets in the Model B listing and every one is correct. Cycles per type are equal or fewer on both targets: 1: -5; 3: -3; 4: -24; 9: -3; 11 and 12 equal; the default is equal.

## r030:1  src/logic.s:563-563  (window i0366)  M -0 B -3  [high]  review: accept

Cycles: Model B: q1=0 path unchanged (beq taken). q1!=0: deca 11 -> sec 2 + sbc 2 = 4. Master unchanged (same deca).

Assumption: The carry and V are dead after: the next instruction is lsr (logic.s:564), and level_init has no bvc/bvs. A != 0 here (beq :+ above). mtmp is scratch and not read after. Verified with the full set in a scratch build: Model B O_* + grid identical on all levels. Gate note: every Model B saving fails the 'B title' gate check in a scratch copy because of load timing; the pristine tree passes.

```
        deca
```
->
```
  .if ::MODELB
        sec                         ; A-1; the carry is dead (lsr follows)
        sbc #1
  .else
        deca
  .endif
```

On the 6502, deca is sta mtmp/dec mtmp/lda mtmp (6 bytes) so that it keeps the carry. The carry is dead here, so sec/sbc #1 gives the same A and N,Z.

Review: Line 563 matches. A is not 0 here (beq above). The carry is dead (lsr follows), V is dead, and mtmp ($DF) is scratch that nothing reads. Model B saves 3 bytes and goes from 11 to 4 cycles. Master is unchanged.

## r030:2  src/logic.s:579-579  (window i0381)  M -0 B -3  [high]  review: accept

Cycles: Model B: inca 11 -> clc 2 + adc 2 = 4. Master unchanged.

Assumption: The carry and V are dead after: lsr follows (logic.s:580). 255+1 = 0 either way (8-bit). Gate note: every Model B saving fails the 'B title' gate check in a scratch copy because of load timing.

```
        inca
```
->
```
  .if ::MODELB
        clc                         ; A+1; the carry is dead (lsr follows)
        adc #1
  .else
        inca
  .endif
```

inca (6 bytes on the 6502) exists only to keep the carry. The carry is dead here, so clc/adc #1 gives the same A and N,Z.

Review: Line 579 matches. The carry is dead: lsr follows, then the dispatch's cmp sets it again. A, N and Z come out the same, and 255+1 wraps the same way. Model B saves 3 bytes and goes from 11 to 4 cycles.

## r030:3  src/logic.s:628-628  (window i0424)  M -0 B -3  [high]  review: accept

Cycles: Model B: q2!=0 path deca 11 -> 4. q2=0 path unchanged. Master unchanged.

Assumption: Fallback if the i0417 restructure is not applied (it contains this change). Carry and V are dead: lsr follows (logic.s:629). A != 0 (beq :+ above).

```
        deca
```
->
```
  .if ::MODELB
        sec                         ; A-1; the carry is dead (lsr follows)
        sbc #1
  .else
        deca
  .endif
```

Same as the prologue's deca: on the Model B only, the carry-preserving 6502 deca becomes sec/sbc #1.

Review: Fallback, subsumed by index 0. Line 628 matches. A is not 0 and the carry is dead (lsr follows). Model B saves 3 bytes.

## r030:4  src/logic.s:634-642  (window i0436)  M -10 B -15  [high]  review: accept

Cycles: Both targets: clc (2) and the always-taken bcc (3) are gone, -5. ora #4 and adc #4 are both 2 cycles.

Assumption: Fallback if the i0417 restructure is not applied (it contains this change). O_XL,y = (q1<<3)&255 is stored by @x8 at logic.s:537-541 (tya/asl/asl/asl) and not written again before @t1 (the zeroing at 543-550 skips O_X*). Bits 0-2 are clear, so +4 never carries: the bcc was always taken and the O_XH increment is dead. The carry after is dead: submin0 2 starts with sec.

```
@t1:    lda O_XL,y
        clc
        adc #4
        sta O_XL,y
        bcc :+
        lda O_XH,y
        inca
        sta O_XH,y
:       lda q1
```
->
```
@t1:    lda O_XL,y
        ora #4                      ; x*8 has bit 2 clear: +4 cannot carry into O_XH
        sta O_XL,y
        lda q1
```

adc #4 with bit 2 clear and C=0 equals ora #4 (no carry out), so the carry-propagation block never ran.

Review: Fallback, subsumed by index 0. O_XL bits 0-2 are clear from @x8, so the bcc was always taken and the O_XH increment never ran. submin0 starts with sec, so the carry is dead. Built: 10 bytes smaller on the Master and 15 on the Model B, and 5 cycles faster.

## r030:5  src/logic.s:649-649  (window i0443)  M -0 B -3  [high]  review: accept

Cycles: Model B: inca 11 -> clc/adc #1 4. Master unchanged.

Assumption: Fallback if the i0417 restructure is not applied (that one contains a form 1 byte smaller). Carry and V are dead: lsr follows (logic.s:650).

```
        inca
```
->
```
  .if ::MODELB
        clc                         ; A+1; the carry is dead (lsr follows)
        adc #1
  .else
        inca
  .endif
```

The carry-preserving 6502 inca becomes clc/adc #1 where the carry is dead.

Review: Fallback; index 0 has the 1-byte-smaller adc #0 form. Line 649 matches and the carry is dead (lsr follows). Model B saves 3 bytes.

## r030:6  src/logic.s:689-737  (window i0514)  M -18 B -18  [high]  review: accept

Cycles: Both targets: each divisor build (clc 2 + lda abs,y 4 + adc 2 + sta 3 + lda abs,y 4 + adc 2 + sta 3 = 20) becomes ora 2 + sta 3 + sta 3 = 8, -24 in total. The moved O_B block costs the same.

Assumption: Fallback if the i0417 restructure is not applied (it contains this change). Spans 48 instructions (i0514-i0561), more than one window. O_AL = (q3<<4)&255 and O_BL = (q4<<4)&255 have a clear low nibble, so O_xL+1 = O_xL|1 and never carries into t16b+1 (= O_xH). m_rnd (rnd; the Master's TOMAIN is jsr rnd / jmp pagelogic with pha..pla) touches only seed, A and flags. mod16 reads t16b and writes only t16, X and A. Neither touches Y. Nothing reads O_BL/O_BH between their old and new store points. The rnd call order (two, mod16, two, mod16) is unchanged. The placeholder ':' at line 738 stays in front of the second jsr mod16.

```
        sta O_AL,y
        lda q3
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        lda q4
        asl
        asl
        asl
        asl
        sta O_BL,y
        lda q4
        lsr
        lsr
        lsr
        lsr
        sta O_BH,y
        ; C = rnd % (A+1) ; D = rnd % (B+1)  (A,B < 4096) -> use rnd16 & mask then reduce
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
        clc
        lda O_AL,y
        adc #1
        sta t16b
        lda O_AH,y
        adc #0
        sta t16b+1
        jsr mod16
        lda t16
        sta O_CL,y
        lda t16+1
        sta O_CH,y
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
        clc
        lda O_BL,y
        adc #1
        sta t16b
        lda O_BH,y
        adc #0
        sta t16b+1
```
->
```
        sta O_AL,y
        ora #1                      ; A+1 for mod16: the low nibble is clear, so no carry
        sta t16b
        lda q3
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        sta t16b+1
        ; C = rnd % (A+1) ; D = rnd % (B+1)  (A,B < 4096) -> use rnd16 & mask then reduce
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
        jsr mod16
        lda t16
        sta O_CL,y
        lda t16+1
        sta O_CH,y
        lda q4
        asl
        asl
        asl
        asl
        sta O_BL,y
        ora #1                      ; B+1, likewise
        sta t16b
        lda q4
        lsr
        lsr
        lsr
        lsr
        sta O_BH,y
        sta t16b+1
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
```

t16b/t16b+1 are stored from A as O_xL/O_xH are made (ora #1 is the +1). Moving the q4 block after the first mod16 lets the second divisor be stored the same way, so it no longer has to be reloaded and incremented.

Review: Fallback, subsumed by index 0. The low nibbles are clear, rnd and mod16 do not write t16b, nothing reads O_B between the old and new store points, and the rnd/mod16 order is unchanged. Built: 18 bytes smaller on both targets and 24 cycles faster.

## r030:7  src/logic.s:689-720  (window i0513)  M -9 B -9  [high]  review: accept

Cycles: Both targets: the first divisor build goes from 20 to 8 cycles.

Assumption: Weaker fallback, superseded by the lo=689..737 candidate and by the i0417 restructure. O_AL = (q3<<4)&255 has a clear low nibble, so +1 cannot carry. m_rnd and the O_B stores do not touch t16b/t16b+1.

```
        sta O_AL,y
        lda q3
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        lda q4
        asl
        asl
        asl
        asl
        sta O_BL,y
        lda q4
        lsr
        lsr
        lsr
        lsr
        sta O_BH,y
        ; C = rnd % (A+1) ; D = rnd % (B+1)  (A,B < 4096) -> use rnd16 & mask then reduce
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
        clc
        lda O_AL,y
        adc #1
        sta t16b
        lda O_AH,y
        adc #0
        sta t16b+1
```
->
```
        sta O_AL,y
        ora #1                      ; A+1 for mod16: the low nibble is clear, so no carry
        sta t16b
        lda q3
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        sta t16b+1
        lda q4
        asl
        asl
        asl
        asl
        sta O_BL,y
        lda q4
        lsr
        lsr
        lsr
        lsr
        sta O_BH,y
        ; C = rnd % (A+1) ; D = rnd % (B+1)  (A,B < 4096) -> use rnd16 & mask then reduce
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
```

t16b = O_AL|1 and t16b+1 = O_AH are stored from A as they are made, so the clc/lda/adc #1/sta/lda/adc #0/sta block goes.

Review: Weaker fallback, subsumed by indexes 6 and 0. It is correct for the same reasons: O_AL's low nibble is clear, and neither rnd nor the O_B stores touch t16b. Built: 9 bytes smaller on both targets.

## r031:0  src/logic.s:689-720  (window i0513)  M -9 B -9  [high]  review: accept

Cycles: One path. Removed clc/lda abs,y/adc/sta zp/lda abs,y/adc/sta zp = 20 cy; added ora #/sta zp/sta zp = 8 cy (-12). Model B the same.

Assumption: t16b/t16b+1 ($D3/$D4) are not written between line 689 and the mod16 call at 721. In between there are only lda/asl/lsr/sta O_*,y, jsr m_rnd, sta t16, and #, sta t16+1. m_rnd is rnd (engine.s:4879), which touches only seed. On the Master m_rnd is TOMAIN = jsr rnd / jmp pagelogic (engine.s:4734), which touches only ROMSEL, its copy and the stack. No other symbol aliases $D3/$D4 (grep). O_AL,y/O_AH,y are not rewritten in between, and m_rnd preserves Y (= obj). The carry into mod16 does not matter: mod16 starts lda t16 / sec.

```
        sta O_AL,y
        lda q3
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        lda q4
        asl
        asl
        asl
        asl
        sta O_BL,y
        lda q4
        lsr
        lsr
        lsr
        lsr
        sta O_BH,y
        ; C = rnd % (A+1) ; D = rnd % (B+1)  (A,B < 4096) -> use rnd16 & mask then reduce
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
        clc
        lda O_AL,y
        adc #1
        sta t16b
        lda O_AH,y
        adc #0
        sta t16b+1
```
->
```
        sta O_AL,y
        ora #1                      ; A+1: A = q3<<4 has a clear low nibble, so no carry
        sta t16b
        lda q3
        lsr
        lsr
        lsr
        lsr
        sta O_AH,y
        sta t16b+1                  ; (A+1) hi = A hi
        lda q4
        asl
        asl
        asl
        asl
        sta O_BL,y
        lda q4
        lsr
        lsr
        lsr
        lsr
        sta O_BH,y
        ; C = rnd % (A+1) ; D = rnd % (B+1)  (A,B < 4096) -> use rnd16 & mask then reduce
        jsr m_rnd
        sta t16
        jsr m_rnd
        and #$0F
        sta t16+1
```

Original: t16b = O_AL,y + 1 = (q3<<4) + 1, and t16b+1 = O_AH,y + carry. Bits 0-3 of q3<<4 are clear, so +1 never carries: t16b = (q3<<4)|1 and t16b+1 = q3>>4. A holds exactly q3<<4 at the line-689 sta and exactly q3>>4 at line 695. Storing ora #1 and the high byte there gives the same t16b pair at jsr mod16. The flags left by the new ora/sta and sta are overwritten by the following lda q3 and lda q4. The O_AL/O_AH stores are unchanged.

Review: Original matches lines 689-720. q3<<4 has bits 0-3 clear, so +1 = ora #1 with no carry, and the high byte = q3>>4. t16b ($D3/$D4) is not written before mod16 at 721: m_rnd = rnd (seed at $66/$67) on the Model B, and TOMAIN rnd+pagelogic on the Master. Y is preserved. mod16 opens with lda/sec, so A and C at entry are dead. Test assembly on both targets: all 10 applied together give Master LOGIC -21 and Model B LGCCODE -64, the sum of the claims.

## r031:1  src/logic.s:731-737  (window i0555)  M -3 B -3  [high]  review: accept

Cycles: One path: 2+4+2+3+4+2+3 = 20 -> 4+2+3+4+3 = 16. Model B the same.

Assumption: O_BL,y was stored as q4<<4 at line 701 (low nibble 0) and O_BH,y as q4>>4 at line 707. Neither is rewritten since: in between are only m_rnd, mod16 (writes t16, X and A only) and the stores to O_CL/O_CH. Y = obj throughout (m_rnd and mod16 preserve Y). The carry after line 737 is unused: mod16 begins lda t16 / sec.

```
        clc
        lda O_BL,y
        adc #1
        sta t16b
        lda O_BH,y
        adc #0
        sta t16b+1
```
->
```
        lda O_BL,y                  ; B+1: O_BL = q4<<4 has a clear low nibble, no carry
        ora #1
        sta t16b
        lda O_BH,y
        sta t16b+1
```

clc/adc #1 on a value with bits 0-3 clear equals ora #1 with no carry out, so the adc #0 on the high byte adds 0 and t16b+1 = O_BH,y. The t16b pair is the same. A ends as O_BH, as in the original (O_BH+0). Only C and V differ, and mod16 does not read them before its own sec/sbc.

Review: Original matches. O_BL,y = q4<<4 (low nibble 0) and O_BH,y = q4>>4 are not rewritten between 701 and 732: only m_rnd, mod16 (X/A/t16 only) and the O_CL/O_CH stores run, and those are different arrays. Y is preserved. mod16 starts lda/sec. 15 -> 12 bytes, 20 -> 16 cycles.

## r031:2  src/logic.s:746-746  (window i0569)  M -0 B -4  [high]  review: accept

Cycles: Master: unchanged (dec a). Model B: deca = sta mtmp/dec mtmp/lda mtmp = 3+5+3 = 11 cy -> sbc #0 = 2 cy. Reached only when q2 != 0.

Assumption: C = 0 at line 746. mod16 leaves only through `bcc :+` -> rts, so C = 0 on return from the jsr mod16 at line 739. Lines 740-745 (lda t16/sta O_DL,y/lda t16+1/sta O_DH,y/lda q2/beq) do not touch C, and line 744 is not a branch target. The D flag is clear: there is no sed in src or modelb/src. mtmp ($DF) is macro scratch that is always written before it is read; nothing in src or modelb/src reads it directly. The V flag set by sbc is overwritten by adc q3 at line 753 before any use.

```
        deca
```
->
```
.if MODELB
        sbc #0                      ; C = 0 from mod16's exit (bcc -> rts): A-1
.else
        deca
.endif
```

A != 0 here (beq above). With C = 0, sbc #0 computes A - 0 - 1 = A-1, as deca does. The next instruction (lsr) overwrites C, N and Z. mtmp is no longer written, and nothing observes that.

Review: The only exit from mod16 is bcc -> rts, so C = 0 after the jsr at 739. Lines 740-745 are lda/sta/beq, which leave C alone, and line 746 is not a branch target. No sed exists, so D is clear. With A != 0 and C = 0, sbc #0 = A-1. lsr then rewrites C/N/Z, and adc q3 rewrites V. mtmp is only macro scratch. Model B deca (sta/dec/lda zp, 6 bytes) -> 2 bytes. The Master keeps dec a.

## r031:3  src/logic.s:765-765  (window i0587)  M -0 B -1  [high]  review: accept

Cycles: 3 cy taken on both targets (bra 3, Model B jmp 3 -> bpl taken 3), ignoring page crossing

Assumption: None outside the window. The preceding instructions are lsr / sta gy1; sta leaves N alone, and lsr always clears N. @box is about 40 bytes ahead, in range.

```
        bra @box
```
->
```
        bpl @box                    ; N = 0 after lsr
```

lsr puts 0 into bit 7, so N = 0 and bpl is always taken; the target is unchanged. Master: 2 bytes either way.

Review: lsr/lsr/lsr/sta gy1 leaves N = 0, so bpl is always taken. In the test listing it assembles as 10 29 to @box, in range.

## r031:4  src/logic.s:766-776  (window i0588)  M -3 B -3  [high]  review: accept

Cycles: One path. The gy1 part, lda q2/lsr x3/sta gy1 = 3+6+3 = 12, becomes lda gy/sta gy1 = 6; the rest is identical. Model B the same.

Assumption: gy = q2>>3 at @t9. The prologue sets it (logic.s:573-577: lda q2/lsr/lsr/lsr/sta gy). The only way to @t9 is `jmp @t9` in the otype dispatch (logic.s:605), which writes only A and flags (ldy obj, lda otype, cmp/beq/bne/jmp) and does not touch gy or q2. Nothing else refers to @t9 (grep).

```
@t9:    lda q2
        submin0 2
        lsr
        lsr
        lsr
        sta gy
        lda q2
        lsr
        lsr
        lsr
        sta gy1
```
->
```
@t9:    lda gy                      ; the prologue left q2>>3 in gy: that is gy1
        sta gy1
        lda q2
        submin0 2
        lsr
        lsr
        lsr
        sta gy
```

Original: gy = max(q2-2,0)>>3 and gy1 = q2>>3. New: gy1 = the old gy = q2>>3 is written first, then gy = max(q2-2,0)>>3 as before. A ends as the new gy, with N = 0 from lsr/sta gy, as the original's lsr/sta gy1 left N = 0. The following bra @box does not read A, and Y is unchanged.

Review: gy = q2>>3 from the prologue (lines 573-577). The dispatch before jmp @t9 (line 605) writes only A, Y and flags. @t9 has no other referrer. New order: gy1 = old gy (= q2>>3), then gy = max(q2-2,0)>>3, the same as the original. The exit leaves N = 0 either way. The submin0 ':' count is unchanged. 7 -> 4 bytes.

## r031:5  src/logic.s:777-777  (window i0599)  M -0 B -1  [high]  review: accept

Cycles: 3 cy taken on both targets (bra/jmp 3 -> bpl taken 3)

Assumption: The instructions before are lsr then sta, both in the original lines 775-776 and in the lines 766-776 rewrite (lsr/sta gy), so N = 0. @box is about 20 bytes ahead.

```
        bra @box
```
->
```
        bpl @box                    ; N = 0 after lsr
```

lsr clears N, so bpl is always taken. Master bytes are equal.

Review: The preceding lsr/sta gy (after candidate 4) or lsr/sta gy1 (original) leaves N = 0, so bpl is always taken. Verified in the listing (10 15).

## r031:6  src/logic.s:780-780  (window i0602)  M -0 B -1  [high]  review: accept

Cycles: 3 cy taken on both targets

Assumption: gy at @t11 = q2>>3 (<= 31) from the prologue (logic.s:573-577). The only way in is `jmp @t11` (logic.s:609) from the dispatch, which does not write gy. So lda gy at line 778 leaves N = 0, and sta gy1 keeps it.

```
        bra @box
```
->
```
        bpl @box                    ; gy = q2>>3 < 32: N = 0
```

N = bit 7 of gy = 0, so bpl is always taken to @box. Master: 2 bytes either way.

Review: @t11 is reached only through jmp @t11 (line 609), and gy = q2>>3 <= 31, so lda gy gives N = 0 and sta keeps it. bpl is always taken.

## r031:7  src/logic.s:790-823  (window i0608)  M -6 B -7  [high]  review: accept

Cycles: @box entry: unchanged (a label emits no code). Row loop, gy <= gy1: lda 3 + cmp 3 + bcc not taken 2 + lda gx0 3 + sta 3 + bra/jmp 3 = 17 -> lda 3 + cmp 3 + bcs taken 3 + lda gx0 3 + sta 3 = 15 to the same lda gy. Exit, gy > gy1: lda + cmp + bcc taken = 9 -> lda + cmp + bcs not taken = 8. Model B the same.

Assumption: @bx is referenced only by `bra @bx` at line 823 (grep @bx\b: lines 793 and 823), which this removes. The ':' at line 821 has no referrer: its own comment says so, there is no ':-' between line 824 and the next ':', and no ':+'/':++' from above reaches it (beq :+ at line 813 targets line 817).

```
        bcc @nextobj
        lda gx0
        sta gx
@bx:    ; cell = gx + (gy << gridsh), built once per grid row: the gx loop below
        lda gy                      ; steps the cell index with inx instead of reshifting
        ldx gridsh
        beq :++
:       asl
        dex
        bne :-
:       clc
        adc gx
        tax
@cell:  ldy bent
        lda obj
        sta LV_BOBJ,y
        lda LV_GRID,x
        sta LV_BNEXT,y
        tya
        sta LV_GRID,x
        inc bent
        lda gx
        cmp gx1
        beq :+
        inc gx
        inx
        bra @cell
:       inc gy
        lda gy1                     ; C clear <=> gy1 < gy <=> gy > gy1
        cmp gy
        bcc @nextobj
:       lda gx0                     ; KEPT: this ':' now has no reference, but removing
        sta gx                      ; the line would retarget the anonymous labels
        bra @bx
```
->
```
        bcc @nextobj
@bx:    lda gx0                     ; per grid row: gx = gx0, then
        sta gx                      ; cell = gx + (gy << gridsh): the gx loop below
        lda gy                      ; steps the cell index with inx instead of reshifting
        ldx gridsh
        beq :++
:       asl
        dex
        bne :-
:       clc
        adc gx
        tax
@cell:  ldy bent
        lda obj
        sta LV_BOBJ,y
        lda LV_GRID,x
        sta LV_BNEXT,y
        tya
        sta LV_GRID,x
        inc bent
        lda gx
        cmp gx1
        beq :+
        inc gx
        inx
        bra @cell
:       inc gy
        lda gy1                     ; C set <=> gy <= gy1: another grid row
        cmp gy
        bcs @bx
:                                   ; placeholder - keeps the anonymous-label count
```

Lines 818-823 duplicated the tail of @box (lda gy1/cmp gy/bcc @nextobj/lda gx0/sta gx) and then jumped to @bx. Moving @bx up two instructions, to the lda gx0 inside @box, lets the loop tail test with bcs @bx (the complement of bcc @nextobj) and fall through to @nextobj. Every path executes the same stores. At @bx, C = 1 in both versions (bcc not taken / bcs taken), and A, N, Z come from lda gx0 and then lda gy. X is reloaded by ldx gridsh. The exit path leaves A = gy1 and C = 0, as before. The backward bcs reaches about 57 bytes.

Review: @bx is referenced only by bra @bx (line 823). The ':' at 821 has no referrer: beq :+ at 813 targets 817, no :- lies between 821 and the next ':' at 866, and there are no macros in between. The placeholder keeps the ordinal count. The loop path matches the original: C = 1 at @bx either way, and it is not consumed before asl/clc. The exit leaves A = gy1 and C = 0. The listing confirms beq :++ / bne :- / beq :+ land as before and bcs @bx (B0 C8) is in range. Master 12 -> 6 bytes, Model B 13 -> 6.

## r031:8  src/logic.s:816-816  (window i0634)  M -0 B -1  [medium]  review: accept

Cycles: 3 cy taken on both targets (bra/jmp 3 -> bne taken 3)

Assumption: At the inx, X is the grid cell index gx + (gy << gridsh), an index into LV_GRID. LV_GRID has 128 cells (the clear at logic.s:~484 is ldx #127 ... bpl). So X <= 127 before the inx and 1..128 after, and Z = 0. Only a level whose object box runs off the grid could differ: X would reach $FF, and the original already writes past LV_GRID in that case.

```
        bra @cell
```
->
```
        bne @cell                   ; X = cell+1 in 1..128: never 0
```

inx sets Z from X. X != 0, so bne is always taken to @cell, exactly as the bra is. Master: 2 bytes either way. This is independent of the lines 790-823 candidate, which leaves this line verbatim.

Review: I simulated every object box in all 16 level packs through level_init's per-type box arithmetic with 8-bit wrap. The maximum cell index is 127, there is no gx0 > gx1 wrap, and X is never 255 before the inx. So inx always leaves Z = 0 and bne is always taken. LV_GRID is 128 heads. Any data that reached X = 255 would already write past LV_GRID into LV_BOBJ in the original. It composes with candidate 7, whose text keeps bra @cell; the applier must find that line by text, because it moves from 816 to 815 inside candidate 7's replacement.

## r031:9  src/logic.s:832-848  (window i0645)  M -0 B -34  [high]  review: accept

Cycles: Master: identical code. Model B, excluding the mov16 (unchanged): 8 stz (lda #0/sta zp, 5 cy each) = 40, plus lda #1/sta/sta = 8, plus 5 stza (pha/lda #0/sta zp/pla, 12 cy each) = 60, total 108 -> lda #0 = 2, plus 13 sta zp = 39, plus 8, total 49.

Assumption: All the targets are distinct zero-page bytes (the .res list at logic.s:217-243); none is aliased. evframe copies frame before frame is cleared in both orders. The only caller is game.s:246, jsr t_level_init (= level_init on the Model B, engine.s:4806), which does lda #1 straight after, so A and the flags on return are dead anyway. Even so, the rewrite returns A = 1 with N = Z = 0 (the original on the Model B ends with pla of 1), and C, X and Y are untouched as before. No interrupt handler reads these variables (grep of engine.s and modelb src), and level_init runs with the game loop stopped.

```
        stz vx
        stz vx+1
        stz vy
        stz vy+1
        stz anim
        mov16 evframe, frame
        stz facing
        stz running
        stz firing
        lda #1
        sta hurt
        sta control
        stza bactive
        stza pausing
        stza exiting
        stza frame
        stza frame+1
```
->
```
.if MODELB
        mov16 evframe, frame        ; evframe takes frame before it is cleared below
        lda #0                      ; one zero for every store (stz is lda #0/sta here)
        sta vx
        sta vx+1
        sta vy
        sta vy+1
        sta anim
        sta facing
        sta running
        sta firing
        sta bactive
        sta pausing
        sta exiting
        sta frame
        sta frame+1
        lda #1
        sta hurt
        sta control                 ; A = 1, N = Z = 0 at the rts, as before
.else
        stz vx
        stz vx+1
        stz vy
        stz vy+1
        stz anim
        mov16 evframe, frame
        stz facing
        stz running
        stz firing
        lda #1
        sta hurt
        sta control
        stza bactive
        stza pausing
        stza exiting
        stza frame
        stza frame+1
.endif
```

Model B only. Each stz and stza reloads 0 into A, and stza also saves and restores A = 1 around the store. mov16 evframe, frame moves ahead of the zero stores: it reads frame, which is cleared only later, and none of the cleared bytes overlap evframe. That lets one lda #0 serve all 13 stores. lda #1/sta hurt/sta control move after them, so A = 1 at the rts. The final memory is identical: vx..firing, bactive, pausing, exiting and frame = 0; hurt = control = 1; evframe = the old frame. The Master keeps the original text under .else.

Review: All the variables are distinct .zeropage .res bytes (logic.s:217-243), and evframe does not overlap any cleared byte. frame is changed only by game_frame, with no IRQ writer, so copying evframe before the zeros gives the same value. The only caller is game.s:246, which does lda #1 next. The exit state matches anyway: A = 1, N = Z = 0, and C/X/Y are untouched. Model B count: 76 bytes -> 42 bytes, saving 34, and faster. The Master text is unchanged under .else.

## r032:0  src/logic.s:832-848  (window i0640)  M -1 B -37  [high]  review: accept

Cycles: Master: 8x stz zp(3)->sty zp(3), 5x stza=stz zp(3)->sty(3), lda #1(2)->iny(2): equal. Model B: 8x stz (lda#0/sta zp=5)->3, 5x stza (pha/lda/sta/pla=12)->3, lda #1->iny equal: -106 cycles.

Assumption: Y = 0 at @objdone: the only two ways in are logic.s:493-495 (ldy nobj / bne :+ / jmp @objdone, falls to jmp only when Y=0) and logic.s:825-826 (ldy obj / beq @objdone); line 827 is jmp so no fall-through; mov16 touches only A. All targets are zero page on both (listings t_m/t_b: 64 rr / 85 rr), and sty zp exists. At return A and Y are dead: the only caller is game.s:246 (jsr t_level_init; Model B t_level_init = level_init, Master TOBANK jsr pagelogic / jmp level_init), followed by lda #1 / sta / sta / jsr t_game_frame; game_frame writes Y before any read (tay at @walk, ldy at @runlist; clamp_window uses no Y). hurt/control are not read by any interrupt code (grep engine.s, modelb/src), so moving their stores after the zero stores is invisible.

```
        stz vx
        stz vx+1
        stz vy
        stz vy+1
        stz anim
        mov16 evframe, frame
        stz facing
        stz running
        stz firing
        lda #1
        sta hurt
        sta control
        stza bactive
        stza pausing
        stza exiting
        stza frame
        stza frame+1
```
->
```
        sty vx                      ; Y = 0 on both ways in (ldy nobj / ldy obj)
        sty vx+1
        sty vy
        sty vy+1
        sty anim
        mov16 evframe, frame
        sty facing
        sty running
        sty firing
        sty bactive
        sty pausing
        sty exiting
        sty frame
        sty frame+1
        iny                         ; Y = 1
        sty hurt
        sty control
```

Every stz/stza stores zero; Y holds zero, so sty stores the same byte to the same zp address. mov16 evframe, frame still copies frame before frame is zeroed (order kept). lda #1 / sta hurt / sta control becomes iny / sty hurt / sty control at the end (after the last zero store): hurt=control=1 as before. Flags at rts: original Master N=0,Z=0 from lda #1 (stz leaves flags), Model B N=0,Z=0 from the last stza's pla of A=1; new: iny 0->1 gives N=0,Z=0; C and V untouched throughout in all versions. Registers at rts change (A = frame+1's old value instead of 1, Y = 1 instead of 0) but both are dead at the caller. If the iny part is unwanted, keeping lda #1 / sta hurt / sta control in place and only using sty for the 13 zero stores is still +36 on Model B, 0 Master.

Review: Lines 832-848 match the file. Only two paths reach @objdone: logic.s:493-495 (ldy nobj / bne / jmp, so Y=0) and 825-826 (ldy obj / beq, so Y=0). mov16 changes only A. Every target is zero page (.zeropage at logic.s:198). The only caller is game.s:246, which reloads A with lda #1. game_frame writes Y before it reads it (tay at @walk, ldy at @runlist); clamp_window, setbank and TOBANKI do not use Y. No IRQ code reads hurt or control. From the t_b listing, Model B goes from 68 to 31 bytes (37 saved); Master goes from 32 to 31 (1 saved). Cycles are the same on Master and far fewer on Model B.

## r032:1  src/logic.s:869-869  (window i0647)  M -0 B -1  [high]  review: accept

Cycles: loop-back 3->3 (jmp/bra 3, bcs taken 3) on both; other paths unchanged

Assumption: none outside the window

```
        bra :-
```
->
```
        bcs :-                      ; C = 1: cmp found A >= 12, so the sbc does not borrow
```

bcc :+ not taken means C=1 from cmp #12 with A>=12; sbc #12 then leaves A>=0 with no borrow, C=1. So bcs :- is always taken, to the same ':' (cmp #12) as bra :-. The anonymous label count is unchanged (no ':' added or removed).

Review: If bcc is not taken, C=1 and A>=12, so sbc #12 leaves C=1. bcs is therefore always taken, to the same ':' as before. Model B: jmp (3 bytes) becomes bcs (2). Master: bra (2) becomes bcs (2). 3 cycles on both.

## r032:2  src/logic.s:882-882  (window i0657)  M -0 B -1  [high]  review: accept

Cycles: loop-back 3->3 on both targets (Model B jmp 3 / Master bra 3 vs bcs taken 3)

Assumption: none outside the window

```
        bra mod16
```
->
```
        bcs mod16                   ; C = 1: the bcc above was not taken
```

The bcc :+ was not taken, so C=1; sta t16+1 and stx t16 do not alter flags, so bcs mod16 is always taken. mod16 is 16 bytes back: in range.

Review: If bcc :+ is not taken, C=1. sta and stx keep C, so bcs mod16 is always taken, and the target is in range. Saves 0 bytes on Master and 1 on Model B; 3 cycles on both.

## r032:3  src/logic.s:892-892  (window i0661)  M -0 B -2  [high]  review: accept

Cycles: Master identical (stz zp both); Model B 12 -> 5

Assumption: none outside the window: the next instruction (logic.s:894) is lda health, which overwrites A and N,Z

```
:       stza bounce
```
->
```
:       stz bounce                  ; A is dead: lda health follows
```

The ':' is kept, so no anonymous-label change. stza and stz both store 0 to bounce; the only difference on Model B is the pha/pla keeping A (and the flags from pla), and A/N/Z are all overwritten by lda health; C and V are untouched by either form.

Review: The next instruction, lda health, overwrites A, N and Z. Neither form changes C or V, and the ':' is kept. Model B goes from 6 to 4 bytes and from 12 to 5 cycles. Master is identical.

## r032:4  src/logic.s:908-908  (window i0670)  M -0 B -1  [high]  review: accept

Cycles: down path: jmp/bra 3 -> bcc taken 3 on both; other paths unchanged

Assumption: none outside the window

```
        bra @offok
```
->
```
        bcc @offok                  ; C = 0: the bcs above was not taken
```

Reached only by falling through bcs @offup (not taken => C=0 from cpx camoff); dec camoff does not touch C, so bcc @offok is always taken. @offok is 2 bytes ahead.

Review: The only way here is falling past bcs @offup, so C=0 from cpx, and dec camoff keeps C. On Model B, bcc replaces the jmp (4C at B63 in the listing) and saves 1 byte; 3 cycles either way.

## r032:5  src/logic.s:928-938  (window i0698)  M -4 B -4  [high]  review: accept

Cycles: 24 -> 20 on both targets (lda 3, asl 2, sta zp 3, lda 3, rol 2, asl zp 5, rol 2)

Assumption: t16 ($D1) is zero page on both targets (listings: 85 D1 / 85 D2). t16 and t16+1 are dead at entry (the original writes t16+1 before reading it). t16+1 is no longer written here: its next use in the block writes it before reading (original gx1/gy/gy1), or is not written at all by the rewritten gx1/gy/gy1 -- t16/t16+1 are never read before written after this block: every read of t16 in logic.s/engine.s/menu.s is preceded by a write in its own routine (checked all ~180 uses; shr6, which reads t16 on entry, has no callers).

```
        lda wx+1                    ; shr6 inlined: the t16 staging and the jsr/rts
        asl                         ; were most of its cost, and wx/wy can be read
        asl                         ; directly (t16 < 16384 still holds)
        sta t16+1
        lda wx
        rol
        rol
        rol
        and #3
        ora t16+1
        sta gx0
```
->
```
        lda wx                      ; gx0 = wx >> 6 (wx < 16384): shift the top
        asl                         ; two bits of the low byte into the high byte
        sta t16
        lda wx+1
        rol
        asl t16
        rol
        sta gx0
```

Original: ((wx+1)<<2 & $FF) | (wx>>6 & 3). New: asl -> C=wx.7, t16=wx<<1; rol wx+1 -> A=(hi<<1)|wx.7; asl t16 -> C=wx.6; rol -> A=(hi<<2)|(wx.7<<1)|wx.6 = same value (high bits of hi fall out either way). C after differs, but the next code (gx1) starts with clc; N,Z are recomputed by later loads. Independent of the gx1/gy/gy1 candidates.

Review: The result is identical: (hi<<2)|wx.7<<1|wx.6. t16 ($D1) is zero page. I checked every other use of t16/t16+1 in logic, engine and menu: each routine writes it before reading it (square, ob_rsnake via square, ob_bat, player_update, draw_score, init_maprows, winlose). shr6 is never called, and no IRQ code uses t16. Nothing reads the carry on exit (gx1 starts with clc). 17 to 13 bytes on both; 27 to 23 cycles.

## r032:6  src/logic.s:939-954  (window i0714)  M -7 B -7  [high]  review: accept

Cycles: 36 -> 29 on both targets

Assumption: t16 zero page on both (listings). t16/t16+1 dead after this computation: every later read of t16 in the code base is preceded by a write in its routine (grep of all uses; shr6 is uncalled); within the block, gy/gy1 write before read. The final values of t16 (and t16+1, no longer written here) change.

```
        lda wx
        clc
        adc #159
        sta t16
        lda wx+1
        adc #0
        asl
        asl
        sta t16+1
        lda t16
        rol
        rol
        rol
        and #3
        ora t16+1
        sta gx1
```
->
```
        lda wx                      ; gx1 = (wx + 159) >> 6
        clc
        adc #159
        sta t16
        lda wx+1
        adc #0
        asl t16
        rol
        asl t16
        rol
        sta gx1
```

Original: s = wx+159 (16-bit), result = (s.hi<<2 & $FF) | (s.lo>>6). New: s.lo in t16, A = s.hi (adc #0 with the carry from the low add); asl t16 -> C = s.lo.7; rol -> (s.hi<<1)|s.lo.7; asl t16 -> C = s.lo.6; rol -> (s.hi<<2)|(s.lo.7<<1)|s.lo.6. Same byte. Carry on exit is not consumed (gy begins with a load then asl/rol from the load, gy1 with clc; my gy rewrite starts with lda/asl).

Review: t16 holds s.lo and A holds s.hi after adc #0. The two asl t16/rol pairs give (s.hi<<2)|(s.lo>>6), the same as the original. t16 is dead afterwards, as in index 5. 26 to 19 bytes on both, with fewer cycles.

## r032:7  src/logic.s:955-965  (window i0725)  M -4 B -4  [high]  review: accept

Cycles: 24 -> 20 on both targets

Assumption: as the gx0 candidate: t16 zp, t16/t16+1 dead in and out (every read of t16 elsewhere follows a write in its routine; gy1 writes t16/t16+1 before reading).

```
        lda wy+1
        asl
        asl
        sta t16+1
        lda wy
        rol
        rol
        rol
        and #3
        ora t16+1
        sta gy
```
->
```
        lda wy                      ; gy = wy >> 6
        asl
        sta t16
        lda wy+1
        rol
        asl t16
        rol
        sta gy
```

Same transform as gx0: A=(wy+1<<2)|(wy.7<<1)|wy.6 = original ((wy+1<<2)&$FF)|((wy>>6)&3). Note the original's first rol takes in the C left by the asl of wy+1, masked off by and #3; the new form needs no mask. gy1 starts with clc, so the exit carry is irrelevant.

Review: This is the gx0 transform applied to wy, and the result is identical. The gy1 code that follows does a load and then clc. 17 to 13 bytes on both.

## r032:8  src/logic.s:966-981  (window i0741)  M -7 B -7  [high]  review: accept

Cycles: 36 -> 29 on both targets

Assumption: t16 zp on both. The values left in t16 (now s.lo<<2) and t16+1 (no longer written here) at the end of the block are dead: next is lda gx0 / cmp BINR ..., the rebuild walk (no t16), process_object/po_star handlers and the rest of the code, where every read of t16/t16+1 is preceded by a write in the same routine (all logic.s uses checked; engine.s:4572 and menu.s:578/589 write first; shr6 reads t16 on entry but has no callers). Interrupt code does not use t16.

```
        lda wy
        clc
        adc #VISLINES/2-1
        sta t16
        lda wy+1
        adc #0
        asl
        asl
        sta t16+1
        lda t16
        rol
        rol
        rol
        and #3
        ora t16+1
        sta gy1
```
->
```
        lda wy                      ; gy1 = (wy + VISLINES/2-1) >> 6
        clc
        adc #VISLINES/2-1
        sta t16
        lda wy+1
        adc #0
        asl t16
        rol
        asl t16
        rol
        sta gy1
```

Same transform as gx1 with wy and VISLINES/2-1: result byte identical. Exit C differs but lda gx0 / cmp BINR follows, which sets N,Z,C.

Review: This is the gx1 transform applied to wy. lda gx0 / cmp BINR follows and resets the flags. The final values of t16/t16+1 are dead, because every later read writes first. 26 to 19 bytes on both.

## r032:9  src/logic.s:1013-1014  (window i0766)  M -0 B -2  [high]  review: accept

Cycles: Master unchanged (same stz instructions). Model B: 2x(lda #0 2 + sta abs 4)=12 -> 2+4+4=10

Assumption: A dead: the next instruction (logic.s:1015) is lda #1. NSTARL/NOTHL are absolute (8D rr rr in t_b.lst), unchanged by this. The stz macro on Model B already left A=0 and N,Z from 0; the new form leaves the same.

```
        stz NSTARL
        stz NOTHL
```
->
```
  .if MODELB
        lda #0                      ; one zero for both
        sta NSTARL
        sta NOTHL
  .else
        stz NSTARL
        stz NOTHL
  .endif
```

Model B: the stz macro expands to lda #0 / sta, twice; the second lda #0 is redundant since A still holds 0. Master assembles the identical instructions as before. No labels involved.

Review: The next instruction is lda #1, so A is dead and the flags are reset. On Model B the second lda #0 is redundant: 10 bytes become 8, and 12 cycles become 10. Master assembles the same stz abs instructions as before.

## r033:0  src/logic.s:1013-1014  (window i0768)  M -0 B -2  [high]  review: accept

Cycles: Master identical (stz, stz). Model B: lda #0 2 + sta 4 + lda #0 2 + sta 4 = 12 -> lda #0 2 + sta 4 + sta 4 = 10.

Assumption: The Model B stz macro is lda #0 / sta (cpu.inc), so A = 0 after the first one. A is dead afterwards on both targets (next is lda #1). Flags: the removed lda #0 set Z=1,N=0; the following lda #1 overwrites them before any branch. NSTARL/NOTHL are abs (LGCBSS), listing t_b.lst 0C1D/0C22.

```
        stz NSTARL
        stz NOTHL
```
->
```
        stz NSTARL
  .if MODELB
        sta NOTHL                   ; A = 0: the stz expansion's
  .else
        stz NOTHL
  .endif
```

On the Model B the pair expands to lda #0 / sta NSTARL / lda #0 / sta NOTHL; the second lda #0 reloads the value A already holds. The Master keeps its two stz. Same stores, same values.

Review: Text matches. On the Model B, stz expands to lda #0/sta abs, so A is already 0 after the first store. A is dead (lda #1 comes next) and no flag is read in between. Model B goes from 10 to 8 bytes and 12 to 10 cycles; the Master is unchanged. The comment is cut short, which is cosmetic only.

## r033:1  src/logic.s:1020-1021  (window i0773)  M -2 B -2  [high]  review: accept

Cycles: Every reachable path (gridsh >= 1): ldx 3 + beq not-taken 2 -> ldx 3, so 2 cycles saved per row on both targets. The gridsh = 0 path cannot occur.

Assumption: gridsh = maplw - 3 (logic.s level_init ~line 459: lda maplw / sec / sbc #3 / sta gridsh). maplw = LV_HDR byte 0 = the level's lw, written by tools/convert.py line 755 from ../v500/<lv>. All 16 level pieces have lw in 5..8 (checked by parsing v500/0..7 sub 1 and 2: lw 5,8,5,7,5,8,5,6,5,8,5,7,5,8,5,6), so gridsh is 2..5. tilexy (logic.s ~313) already relies on map width >= 256 px (it compares only the high byte against mapw+1). game_frame only runs after level_init.

```
        ldx gridsh
        beq :++
```
->
```
        ldx gridsh                  ; >= 2: every map is >= 256 px wide (maplw >= 5)
```

beq :++ only skips the shift loop when gridsh = 0. With gridsh >= 1 it never branches, so deleting it leaves ldx gridsh -> asl/dex/bne loop -> sta grow exactly as executed. The loop body sets flags itself; nothing reads the flags of ldx.

Review: gridsh = maplw-3, and logic.s:466 is its only writer. maplw is LV_HDR byte 0 (game.s:30). I rechecked the v500 lw values for all 16 level pieces: they are 5..8, so gridsh is 2..5 and the beq is never taken. The ':' labels stay, so the anonymous-label counts do not change. Saves 2 bytes and 2 cycles on both targets.

## r033:2  src/logic.s:1048-1049  (window i0799)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3. Model B: jmp 3 -> bne taken 3. Same.

Assumption: X = NSTARL and cpx #BINMAX / bcs @full just above mean NSTARL < BINMAX <= 255 here, so inc NSTARL gives 1..BINMAX (never 0) and Z = 0. @skip is +$26 bytes away on Model B (t_b.lst 0C6D -> 0C95), in branch range.

```
        inc NSTARL
        bra @skip
```
->
```
        inc NSTARL
        bne @skip                   ; NSTARL was < BINMAX: never 0
```

bra is unconditional; after inc NSTARL, Z is provably clear, so bne is always taken to the same label. Flags at @skip: ldx bent rewrites N,Z; C is unchanged by both.

Review: After bcs @full, X = NSTARL < BINMAX, so inc gives a nonzero result and bne is always taken. Model B distance is about +$25. On the Model B the 3-byte jmp becomes a 2-byte bne (3 cycles to 3); on the Master bra and bne are the same size.

## r033:3  src/logic.s:1055-1056  (window i0806)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3. Model B: jmp 3 -> bne taken 3. Same.

Assumption: X = NOTHL and cpx #BINMAX / bcs @full just above: NOTHL < BINMAX <= 255, so inc gives nonzero and Z = 0. @skip is within +$17 bytes (t_b.lst 0C7E -> 0C95).

```
        inc NOTHL
        bra @skip
```
->
```
        inc NOTHL
        bne @skip                   ; NOTHL was < BINMAX: never 0
```

Same as the star-list case: the inc result is 1..BINMAX, Z is clear, and bne is always taken to @skip.

Review: Same argument as index 2, for NOTHL. Branch distance is about +$15.

## r033:4  src/logic.s:1068-1069  (window i0818)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3. Model B: jmp 3 -> bne taken 3. Same.

Assumption: The inc is reached only when gx != gx1. gx starts at gx0 (@rows) and steps by 1, so gx < gx1 <= 255 whenever gx0 <= gx1, and gx+1 is nonzero. gx0 <= gx1 holds because gx0 = wx>>6 and gx1 = (wx+159)>>6, with wx clamped to [0, maxwx] by clamp_window (game.s 164; maxwx = mapw - WINPX, maps >= 256 px) or set to 0 (menu.s 204, main.s 102), so wx+159 < 16384 and the shift is monotone. Model B branch distance: t_b.lst bra at 0CA5, next PC 0CA7, @cells at 0C3C gives -107, in range; any other shrink in between only shortens it.

```
        inc gx
        bra @cells
```
->
```
        inc gx
        bne @cells                  ; gx < gx1 <= 255: never 0
```

bra -> bne on a provably nonzero inc result: always taken to @cells, which begins lda grow / clc (no flag read).

Review: wx is always kept in [0, maxwx]: clamp_window clamps it, the health==0 path keeps the previous clamped value, and main.s/menu.s set it to 0. With wx+159 < 16384, gx0 <= gx1, so gx < gx1 <= 34 at the inc and the result is nonzero. On the Model B, bne at CA5 to C3C is -107, in range.

## r033:5  src/logic.s:1090-1091  (window i0833)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3. Model B: jmp 3 -> bne taken 3. Same.

Assumption: BINI starts at 0 (stz BINI at 1081) and the loop exits at BINI = NSTARL, so at the inc BINI < NSTARL <= 255 and BINI+1 is nonzero. BINI is written only at logic.s 1081/1090/1092/1101 (grep src, modelb/src) and NSTARL only at 1013/1048, so po_star cannot change either. @rls is -$1A away.

```
        inc BINI
        bra @rls
```
->
```
        inc BINI
        bne @rls                    ; BINI < NSTARL <= 255: never 0
```

bra -> bne on a nonzero inc result; @rls starts with ldx BINI (flags rewritten).

Review: BINI and NSTARL are written only at the sites listed (grep of src and modelb/src), so at the inc BINI < NSTARL <= 255. Branch distance is -$1A.

## r033:6  src/logic.s:1101-1102  (window i0844)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3. Model B: jmp 3 -> bne taken 3. Same.

Assumption: BINI starts at 0 (stz BINI at 1092) and the loop exits at BINI = NOTHL. BINI and NOTHL are written nowhere else (grep: logic.s 1014/1055 for NOTHL, 1081/1090/1092/1101 for BINI), so process_object cannot change them; BINI+1 <= NOTHL <= 255 is nonzero. @rl2 is -$1A away.

```
        inc BINI
        bra @rl2
```
->
```
        inc BINI
        bne @rl2                    ; BINI < NOTHL <= 255: never 0
```

bra -> bne on a nonzero inc result; @rl2 starts with ldx BINI.

Review: Same argument as index 5, for NOTHL and @rl2.

## r033:7  src/logic.s:1142-1157  (window i0887)  M -5 B -5  [high]  review: accept

Cycles: Both targets identical. Kill path, facing=0: facing code 9 (lda3 beq3 sta3) -> 10 (lda3 lsr2 ror2 sta3); tail alive 9 (lda3 bne3 jmp3) -> 8 (lda3 beq2 jmp3); tail dead 8 (lda3 bne2 jmp3) -> 6 (lda3 beq3). Totals 18->18 alive, 17->16 dead. Kill path, facing=1: 10->10, then 9->8 / 8->6. Non-kill paths (bpl @nokill, hurt): tail only, 9->8 alive, 8->6 dead. No path is slower. This supersedes the next candidate; the two are mutually exclusive.

Assumption: facing is only ever 0 or 1: its writers are stz facing (logic.s 838, 1256, 1547) and lda #1 / sta facing (1544). hx+1 is read only by player_hit's 'bit hx+1 / bmi' (logic.s 1210); every other hx use is 'mov16 hx, rx' (a write) in the object handlers, and player_update/player_dead do not read hx. player_hit (including bar_touch) never reads C, N or Z on entry. player_dead is in branch range: Model B t_b.lst, after this edit beq at 0D2E -> player_dead 0D7D is +77 (about +35 more under DBGHIT, so about 112; less if shr6 is also deleted); Master is about +70. @alive is referenced nowhere else (grep).

```
        lda facing
        beq :+
        lda #$FF
:       sta hx+1                    ; facing left -> hx negative -> vx = +768
  .ifdef DBGHIT
        lda #250                    ; the readout shows 25009 for a kill tile (obj and
        sta obj                     ; otype are free here: the walk is over for this step)
        lda #9
        sta otype
  .endif
        jsr player_hit
@nokill:
        lda health
        bne @alive
        jmp player_dead
@alive: jmp player_update
```
->
```
        lda facing                  ; 0 or 1 (1 = left)
        lsr                         ; C = facing
        ror                         ; A = facing << 7: player_hit tests only hx+1's sign
:       sta hx+1                    ; facing left -> hx negative -> vx = +768
  .ifdef DBGHIT
        lda #250                    ; the readout shows 25009 for a kill tile (obj and
        sta obj                     ; otype are free here: the walk is over for this step)
        lda #9
        sta otype
  .endif
        jsr player_hit
@nokill:
        lda health
        beq player_dead             ; in range (player_hit is ~60 bytes)
        jmp player_update
```

lsr gives A = 0, C = facing (0/1); ror gives A = C<<7 = $80 (left) or $00 (right). That has the same sign as the original $FF / $00, and the sign is all player_hit reads. Tail: 'bne @alive / jmp player_dead / @alive: jmp player_update' equals 'beq player_dead / jmp player_update' (same targets for health == 0 / != 0), with player_dead reachable by a branch. C out of the facing code differs (0 instead of gettileattr's), but nothing reads it before a clc/sec (player_hit none; player_update clc; player_dead via gravity's add16i).

Review: facing is only ever 0 or 1 (writers at 838, 1256, 1544, 1547). A = $80/$00 gives the same N and Z as $FF/$00. The only reader of hx+1 ($D8, no alias) is bit hx+1 at 1210, and every other player_hit caller sets hx with mov16 first. C is now 0, but player_hit (mov16, dec, bar_touch's lda/sta) never reads carry. Cycles per path: facing=0 costs +1 in the head and saves 1 or 2 in the tail, so alive 18->18 and dead 17->16; facing=1 has an equal head. player_dead is in the same segment. The beq reaches about +75 bytes on the Model B (about +117 in a DBGHIT build, which adds about 42 bytes, still in range) and about +68 on the Master. This candidate includes index 8.

## r033:8  src/logic.s:1153-1157  (window i0887)  M -3 B -3  [high]  review: accept

Cycles: Both targets: alive lda3 bne3 jmp3 = 9 -> lda3 beq2 jmp3 = 8; dead lda3 bne2 jmp3 = 8 -> lda3 beq3 = 6. Apply only if the previous combined candidate was not applied, since it contains this change.

Assumption: @alive is referenced only here (grep logic.s). player_dead is in branch range: Model B (t_b.lst) beq at 0D2E, next PC 0D30, player_dead after this edit at 0D7D gives +77 (a DBGHIT build adds about 35 bytes to player_hit, giving about 112, still in range; deleting shr6 takes 16 off). Master (t_m.lst) is about +70.

```
@nokill:
        lda health
        bne @alive
        jmp player_dead
@alive: jmp player_update
```
->
```
@nokill:
        lda health
        beq player_dead             ; in range (player_hit is ~60 bytes)
        jmp player_update
```

The same Z flag from lda health selects the same two destinations: zero -> player_dead, nonzero -> player_update.

Review: Correct on its own: @alive is referenced nowhere else and the beq is in range on both targets. Index 7 already contains this change, so apply this only if index 7 is not applied.

## r033:9  src/logic.s:1159-1171  (window i0895)  M -16 B -16  [high]  review: accept

Cycles: Dead code: no path executes it.

Assumption: shr6 has no caller. grep -rn shr6 over the whole repo finds only its definition in src/logic.s and stale copies in profile/logic.s and opt/proposals.json. The latter is the inlining proposal that removed all four 'jsr shr6'; it notes 'shr6 itself is left in place (now dead, 16 bytes)'. No .export, far table or TOMAIN entry names it. No cheap (@) labels lie between shr6 and player_hit, so removing the normal label changes no @ scope.

```
; A = t16 >> 6 (t16 < 16384)
shr6:   lda t16+1                   ; t16 < 16384: result = (hi<<2) | (lo>>6)
        asl
        asl
        sta t16+1
        lda t16
        rol                         ; three ROLs bring bits 7,6 down to bits 1,0
        rol
        rol
        and #3
        ora t16+1
        rts

```
->
```

```

Deletes the unreferenced routine (its comment, 11 instructions, 16 bytes on both targets) and the blank line after it. player_hit's header comment then follows the @nokill tail directly.

Review: Nothing in src, modelb or tools references shr6; only its definition and stale listings/labels remain, and it is inlined at logic.s:928. It contains no anonymous or cheap labels, and there are no @ labels between shr6 and player_hit, so removing the normal label changes no @ scope. Saves 16 bytes on both targets.

## r034:0  src/logic.s:1154-1157  (window i0896)  M -3 B -3  [high]  review: accept

Cycles: health!=0: 3+3+3=9 -> 3+2+3=8; health=0: 3+2+3=8 -> 3+3=6 (+1 only if page crossed). Model B same.

Assumption: @alive is referenced only at logic.s:1155 (grep). player_dead is within branch range: listing t_b.lst beq at $D2E -> player_dead $D80 (+$50), t_m.lst $888 -> $8D3 (+$49); every other candidate here only shrinks the gap.

```
        lda health
        bne @alive
        jmp player_dead
@alive: jmp player_update
```
->
```
        lda health
        beq player_dead
        jmp player_update
```

lda health sets Z; beq to player_dead reproduces the health==0 jmp, fall-through jmp player_update reproduces the @alive jmp. Both targets are tail jumps (no stack use), so the transfer is identical.

Review: Original matches 1154-1157. @alive has no other reference. Assembled: beq player_dead offset $32 (B) in the combined build, and with -D DBGHIT=1 alone it is still in range (Master $69, Model B $70). Saves 3/3; both paths faster.

## r034:1  src/logic.s:1159-1171  (window i0896)  M -16 B -16  [high]  review: accept

Cycles: never executed: no path

Assumption: shr6 has no caller: grep of src/, modelb/src/, tools/ finds only its definition and the comment at logic.s:928 saying it was inlined. No .export, no label+n reference.

```
; A = t16 >> 6 (t16 < 16384)
shr6:   lda t16+1                   ; t16 < 16384: result = (hi<<2) | (lo>>6)
        asl
        asl
        sta t16+1
        lda t16
        rol                         ; three ROLs bring bits 7,6 down to bits 1,0
        rol
        rol
        and #3
        ora t16+1
        rts

```
->
```

```

Dead routine deleted (comment line, 16 bytes of code on both targets -- verified by assembling: player_update moves by 16 -- and the blank line after). No cheap labels live between the game_frame tail jumps and player_hit, so no @ scope changes.

Review: shr6 has no reference in src, modelb/src, tools or modelb/tools (only its definition and a comment). Assembled: player_update moves 16 on both targets; later code byte-identical.

## r034:2  src/logic.s:1200-1216  (window i0949)  M -4 B -11  [high]  review: accept

Cycles: Master: jsr bar_touch/lda #1/sta hurt = 6+(2+4+6)+2+3 = 23 -> lda #1/sta abs/sta zp = 2+4+3 = 9. Knockback block: health=0 23 -> 23; health!=0,hx+1<0 34 -> 30; hx+1>=0 37 -> 34. Model B: bar_touch 23 -> 2+3+3 = 8; four stz (4x5=20) -> lda #0 + 3 sta = 11; knockback paths as Master minus the removed sta vx+1 / stz vx+1.

Assumption: bar_touch (logic.s:3223) is exactly lda #1/sta BARDIRTY/rts and touches nothing else; BARDIRTY is zp $A4 on Model B (defs.inc:51), absolute on Master (engine.s:336) so jsr(3)=sta abs(3) there. player_hit callers (1152,2412,2569,2736,2897,2986) see A=SFX_HIT and X,Y untouched exactly as before.

```
        jsr bar_touch
        lda #1
        sta hurt
        stz control
        stz vx
        stz vx+1
        lda health
        beq :+
        lda #>768                   ; vx low is already 0 from the stz above, and both
        sta vx+1                    ; knockback speeds have a zero low byte
        bit hx+1
        bmi :+
        lda #>(-768)                ; -768 is $FD00: the HIGH byte is the non-zero one
        sta vx+1
:       stz vy                      ; -1280 = $FB00: the low byte is zero
        lda #>(-1280)
        sta vy+1
```
->
```
        lda #1                      ; bar_touch, inlined
        sta BARDIRTY
        sta hurt
  .if MODELB
        lda #0                      ; one zero for three stores
        sta control
        sta vx
        sta vy
  .else
        stz control
        stz vx                      ; both knockback speeds and -1280 have
        stz vy                      ; a zero low byte
  .endif
        lda health
        beq :+                      ; A = 0: no knockback, vx+1 = 0
        lda #>768
        bit hx+1
        bmi :+
        lda #>(-768)                ; -768 is $FD00: the HIGH byte is the non-zero one
:       sta vx+1
        lda #>(-1280)
        sta vy+1
```

bar_touch inlined: A=1 is stored to BARDIRTY then hurt (same values, BARDIRTY still written after dec health). vx+1 was stz'd then overwritten when health!=0; now A holds the final byte: 0 when health==0 (beq taken with A=0 from lda health), #>768 when hx+1 negative, #>(-768) otherwise, stored once at the label. stz vy moved before the health test (vy independent of it). Model B: the three stz share one lda #0. Final A = SFX_HIT and flags from that load, unchanged. Verified by assembly in a scratch copy: Master -4, Model B -11, object code after player_update byte-identical.

Review: bar_touch is lda #1/sta BARDIRTY/rts; A=1 goes to BARDIRTY then hurt in the same order as before. vx+1 ends 0 (A=0 from lda health on beq), 3 or $FD as before; vy low 0 on every path. At return A=SFX_HIT, N/Z from that load, C untouched, V as before, X/Y untouched for all six player_hit callers. Assembled: -4 Master, -11 Model B; anonymous-label count unchanged; tail byte-identical. Every path is no slower on either target.

## r034:3  src/logic.s:1233-1238  (window i0973)  M -3 B -3  [high]  review: accept

Cycles: sta t16 3->tax 2, lda t16+cmp 5 -> cpx 2: every path faster by 1 or 4. Model B same.

Assumption: X dead: @draw path ends in jmp m_addsprite (addsprite engine.s:1381 loads X from NSPR, Master TOMAIN wrapper jsr's it); @respawn path uses no X (mov16/stz/dec/jsr bar_touch), and its lives==0 rts returns to game.s:251/254/299/303 which never read X (lda exiting/render_frame). t16 is a transient temp not read after this on either path (m_addsprite, bar_touch do not read it; the next reader in player_update writes it first at logic.s:1398).

```
        sta t16
        lda frame+1
        sbc evframe+1
        bne @respawn
        lda t16
        cmp #61
```
->
```
        tax
        lda frame+1
        sbc evframe+1
        bne @respawn
        cpx #61
```

tax keeps the low difference in X without disturbing C for the high sbc; cpx #61 sets C exactly as cmp #61 on the same value, so bcc @draw is unchanged.

Review: tax leaves C alone for the sbc. X is dead: @draw goes to addsprite, which starts with ldx NSPR (on the Master pagelogic keeps X). The lives==0 rts goes back through game_frame to game.s 251/254/299/303, and none of those read X; game_frame already trashes X itself. t16 is written before any later read (engine.s:4572, menu, and the logic uses). Saves 3/3.

## r034:4  src/logic.s:1243-1247  (window i0983)  M -0 B -8  [high]  review: accept

Cycles: Model B: 5x(2+3)=25 -> 2+5x3=17. Master identical code.

Assumption: A dead at each stz already (macro contract); after the block mov16 evframe,frame reloads A. No flags consumed.

```
        stz vx
        stz vx+1
        stz vy
        stz vy+1
        stz anim
```
->
```
  .if MODELB
        lda #0
        sta vx
        sta vx+1
        sta vy
        sta vy+1
        sta anim
  .else
        stz vx
        stz vx+1
        stz vy
        stz vy+1
        stz anim
  .endif
```

Model B expansion of stz is lda #0/sta; the five loads of the same #0 collapse into one. Master unchanged.

Review: Model B only: five lda #0 collapse to one; A is dead because mov16 reloads it. Master code is unchanged. Assembled: 0/8.

## r034:5  src/logic.s:1256-1262  (window i0997)  M -0 B -5  [high]  review: accept

Cycles: Model B: 3x5=15 -> 2+9=11; jsr bar_touch (6+2+3+6=17) -> sta zp 3. Master: jsr bar_touch 17 -> sta abs 4; stz unchanged.

Assumption: bar_touch = lda #1/sta BARDIRTY/rts (logic.s:3223): A is already 1 here, so the store alone matches it, and A=1 on exit as before (@draw then reloads A). BARDIRTY zp $A4 on Model B, absolute on Master (3 bytes = jsr).

```
:       stz facing
        stz running
        stz firing
        lda #1
        sta hurt
        sta control
        jsr bar_touch
```
->
```
:
  .if MODELB
        lda #0
        sta facing
        sta running
        sta firing
  .else
        stz facing
        stz running
        stz firing
  .endif
        lda #1
        sta hurt
        sta control
        sta BARDIRTY                ; bar_touch, inlined
```

Model B stz's share one lda #0; bar_touch body inlined with A=1 already loaded. Flags after: bar_touch left N=0,Z=0 from lda #1; now left from lda #1 too (sta does not change them); @draw's mov16 reloads anyway.

Review: The ':' that 1252 targets stays in place. A=1 already, so sta BARDIRTY is exactly bar_touch's effect, and @draw reloads A. Master: jsr (3 bytes) becomes sta abs (3 bytes), 0 bytes saved and 13 cycles faster. Model B: sta zp. Assembled: 0/5.

## r034:6  src/logic.s:1282-1283  (window i1008)  M -2 B -2  [high]  review: accept

Cycles: sta t16+1 3 + add16 (2+3+3+3+3+3+3=20) = 23 -> 2+2+3+3+3+2+3+3 = 21. Model B same.

Assumption: X dead: gravity's only callers are logic.s:1226 and 1364, each followed by jsr vy_step and then code that sets X before reading it (player_dead: see the tax candidate; player_update: nothing between 1366 and gettileattr's tilexy reads X, and getaltitude on the @up path sets it). t16+1 is not read after gravity before being rewritten (player_dead writes only t16; player_update rewrites t16+1 at logic.s:1400 before bge16).

```
        sta t16+1
        add16 vy, t16
```
->
```
        tax                         ; the step's high byte
        clc
        lda vy
        adc t16
        sta vy
        txa
        adc vy+1
        sta vy+1
```

High byte of the shifted step held in X instead of t16+1; add is commutative so txa/adc vy+1 = vy+1 + hi + C where C is the carry out of the low add, as add16 computed.

Review: txa/adc vy+1 gives the same sum and carry as add16. X is dead after gravity: vy_step follows, then player_dead (as idx 3) or player_update. In player_update every X read (txa at @acc, cpx, txa at @drawp) comes after a tax on the same path, and tilexy/getinfo/gettileattr only write X. t16+1 is left stale, but it is next written before any read (logic.s:1401, 1452, 1493, 1535, engine.s:4574). 23->21 cycles. Assembled 2/2.

## r034:7  src/logic.s:1287-1309  (window i1021)  M -27 B -29  [high]  review: accept

Cycles: Master original: prefix mov16 12 + add16i 18 + sta 3 + and/beq/(lda) 5..7 + sta 3 = 41..43, then positive-no-clamp +12, positive-clamp +20, negative-no-clamp +11, negative-clamp +14 (+6 rts). New: 12 prefix + positive no clamp 3+2+3+3+3=14 / clamp 3+2+2+2+3+3=15 / negative no clamp 2+2+2+3+3+3=15 / clamp 2+2+2+2+2+3+3+3=19 (+6 rts). Every path ~30 cycles faster. Model B original stz is 5 not 3: still faster.

Assumption: X dead on return from vy_step at both callers (logic.s:1227 player_dead, where the next X read follows an ldx/tax of its own or addsprite; logic.s:1365 player_update, where nothing reads X before gettileattr's tilexy sets it at the @push path that every route reaches, or getaltitude on @up). Callers read nothing of A or flags on return: player_dead's add16 starts with clc; player_update's bmi16 dpx is bit dpx+1. Original vy_step left X untouched, so this relies on X being dead, verified above. MAXDWY = 8 (engine.s:102/118).

```
vy_step:
        mov16 dpx, vy
        add16i dpx, 128             ; leaves the high byte in A
        sta dpx
        and #$80
        beq :+
        lda #$FF
:       sta dpx+1
        bmi @up                     ; A already holds dpx+1, with matching N/Z
        bne @cdn                    ; >= 256
        lda dpx
        cmp #MAXDWY+1
        bcc @cdone
@cdn:   lda #MAXDWY
        sta dpx
        stz dpx+1
        rts
@up:    lda dpx                     ; dpx+1 is $FF on this arm, so neither the
        cmp #<-MAXDWY               ; <= -256 test nor the clamp's rewrite of the
        bcs @cdone                  ; high byte can do anything
@cup:   lda #<-MAXDWY
        sta dpx
@cdone: rts
```
->
```
vy_step:
        ldx #0                      ; X = dpx+1, the sign extension
        lda vy                      ; C = the carry out of vy low + 128
        cmp #$80
        lda vy+1
        adc #0                      ; A = (vy + 128) >> 8
        bmi @up
        cmp #MAXDWY+1
        bcc @st
        lda #MAXDWY
@st:    sta dpx
        stx dpx+1
        rts
@up:    dex                         ; dpx+1 = $FF
        cmp #<-MAXDWY
        bcs @st
        lda #<-MAXDWY
        bcc @st                     ; C = 0 from the compare
```

Original: dpx = hi(vy+128) (the 16-bit add's high byte stored to dpx), dpx+1 = $FF if its bit 7 else 0; the 'bne @cdn' was dead (dpx+1 is 0 on that arm). Positive: if dpx >= MAXDWY+1 then dpx=MAXDWY, dpx+1=0. Negative: if dpx < <-MAXDWY (unsigned) then dpx=<-MAXDWY; dpx+1=$FF. New: cmp #$80 on vy low gives C = carry of lo+$80, so lda vy+1/adc #0 is hi(vy+128) directly; N from adc is its bit 7 (ldx #0 comes first so it does not disturb the flags); the same unsigned compares clamp; X carries dpx+1 (0, or $FF after dex) to one shared store. dpx and dpx+1 end identical on every path. Brute-forced over all 65536 vy: identical dpx/dpx+1.

Review: Brute-forced every vy in 0..65535 for MAXDWY 1..127 (it is 8 on both targets): dpx and dpx+1 are identical. Callers ignore A and flags on return: add16 starts with clc, bmi16 is bit dpx+1. X is dead at both callers (see idx 3 and 6). The removed ':' at 1294 was referenced only by the removed beq; the combined assembly's code after player_update is byte-identical. Every path is about 25-30 cycles faster. Assembled: 27/29.

## r035a:0  src/logic.s:1291-1309  (window i1024)  M -10 B -10  [high]  review: accept

Cycles: incl. rts: h 0..8: 26->16 (MB 26->18); h 9..127: 33->20 (MB 35->22); h $F8..$FF: 26->19; h $80..$F7: 30->23; same on both targets except where stz noted

Assumption: On entry at 1291 A = h (high byte of vy+128) and N reflects h: lines 1289-1290 are add16i dpx,128 (macros.txt: ...; adc #>(128) = adc #0; sta dpx+1) then sta dpx, and sta sets no flags. If the adjacent region rewrites 1288-1290 it must still end with the high byte's adc/lda so N is its sign. A, C and other flags dead at rts: both callers, logic.s:1227 (jsr vy_step / add16 py,dpx = clc; lda) and logic.s:1365 (jsr vy_step / bmi16 dpx = bit dpx+1, then lda alt or add16's clc; lda). X, Y untouched as before. @cdn/@cup/@cdone referenced only inside vy_step (grep). MAXDWY = 8 on both targets (engine.s:102,118).

```
        and #$80
        beq :+
        lda #$FF
:       sta dpx+1
        bmi @up                     ; A already holds dpx+1, with matching N/Z
        bne @cdn                    ; >= 256
        lda dpx
        cmp #MAXDWY+1
        bcc @cdone
@cdn:   lda #MAXDWY
        sta dpx
        stz dpx+1
        rts
@up:    lda dpx                     ; dpx+1 is $FF on this arm, so neither the
        cmp #<-MAXDWY               ; <= -256 test nor the clamp's rewrite of the
        bcs @cdone                  ; high byte can do anything
@cup:   lda #<-MAXDWY
        sta dpx
@cdone: rts
```
->
```
        bmi @up                     ; N from add16i's adc #0: the sign of the high byte
        cmp #MAXDWY+1
        bcc @dn
        lda #MAXDWY
        sta dpx
@dn:    stz dpx+1
        rts
@up:    cmp #<-MAXDWY
        bcs @upd
        lda #<-MAXDWY
        sta dpx
@upd:   lda #$FF
        sta dpx+1
        rts
```

Original: dpx+1 = (h & $80) ? $FF : 0, then the N-flag test of that value (bmi @up) is exactly the sign of h, which the new bmi reads from the adc. bne @cdn is dead (Z=1 on the positive arm: flags from and #$80 = 0), so it goes. Positive arm: lda dpx is redundant (A still = h, dpx = h); cmp #MAXDWY+1 / bcc skip clamp; clamp writes dpx=MAXDWY; both outcomes then need dpx+1 = 0, written by the shared stz at @dn (original wrote 0 at 1294 and again at 1302). Negative arm: lda dpx redundant (A = h); cmp #<-MAXDWY / bcs skip clamp as before; clamp writes dpx=<-MAXDWY; dpx+1 = $FF written at @upd (original wrote it at 1294). Final dpx/dpx+1 identical for every h; only A/flags at rts differ, and both callers overwrite them.

Review: Original matches lines 1291-1309 verbatim. add16i has one definition (both targets): its last flag-setter is adc #0, then sta dpx+1 / sta dpx, so N at 1291 = sign of h and A = h = dpx. Old bmi on the $00/$FF value is the same sign test; bne @cdn was dead. Final dpx/dpx+1 match for every h: positive arm dpx = h or 8 with dpx+1 = 0; negative arm dpx = h or $F8 with dpx+1 = $FF. The only callers are 1227 (then add16 = clc/lda) and 1365 (then bmi16 = bit dpx+1), so A and flags are dead. @cdn/@cup/@cdone are used only inside vy_step. Removing the ':' at 1294 retargets nothing: 1252 goes to 1256 and 1344 to 1346, with no :- or :++ nearby and no anonymous labels emitted by these macros. dpx = $D5 is zero page. Bytes: Master 36->26, MB 38->28, so 10 saved on each. Every path is faster: 26->16/18, 33->20/22, 26->19, 30->23.

## r035a:1  src/logic.s:1337-1337  (window i1031)  M -0 B -2  [high]  review: accept

Cycles: Master 3->3; Model B 5 (lda #0/sta zp) -> 3

Assumption: Only reachable by falling through lda bactive / bne @nothrow at 1335-1336, so A = 0 here (no label on 1337). A overwritten by lda #1 at 1338 on both targets.

```
        stz anim
```
->
```
        sta anim                    ; A = 0: bactive, just tested
```

stz anim stores 0; A holds 0 after lda bactive with bne not taken, so sta anim stores the same 0. Master: stz zp and sta zp are both 2 bytes/3 cycles; flags unchanged by either (Z=1 from lda bactive; MB stz's lda #0 also Z=1). lda #1 follows and resets A and flags.

Review: Line 1337 matches and has no label. The only way in is falling through lda bactive / bne not taken, so A = 0. anim is zero page. Master: stz zp and sta zp are both 2 bytes and 3 cycles. MB: lda #0/sta zp (4 bytes, 5 cycles) becomes sta zp (2 bytes, 3 cycles). Flags: Z=1 either way, and lda #1 comes next.

## r035a:2  src/logic.s:1346-1363  (window i1054)  M -2 B -10  [high]  review: accept

Cycles: jump arm: Master stz 3 + bra 3 -> stz 3 + bne 3 (equal); MB stz 5 + jmp 3 -> 5 + bne 3 (equal). stand arm (any of the 3 entries): Master lda2 sta3 stz3 stz3 bra3 = 14 -> hoisted stz3 + stz3 lda2 sta3 bne3 = 14; MB lda2 sta3 stza12 stza12 jmp3 = 32 -> 5+5+2+3+3 = 18. The test chain in between is unchanged.

Assumption: The ':' at 1346 is reached only from beq :+ at 1344 and fall-through from bpl @grav at 1345, and every path from it goes to the jump arm or @stand (both zero vy low); vy is not read in between (firing, control, keys only). A dead at 1346 (lda firing follows; MB stz clobbers A). SFX_JUMP = 1 (logic.s:3308) so Z=0 after lda #SFX_JUMP; sta sets no flags. At @move (logic.s:1365) jsr vy_step begins mov16 dpx,vy = lda vy, so A and flags are dead there (the stza's preservation of A=1 was not needed). @stand is referenced only by the three branches in this block.

```
:       lda firing
        bne @stand
        lda control
        beq @stand
        lda keys
        and #(K_UP|K_FIRE)
        beq @stand
        stz vy                      ; -1280 = $FB00: the low byte is zero
        lda #>(-1280)
        sta vy+1
        lda #SFX_JUMP
        sta SFXREQ
        bra @move
@stand: lda #1
        sta control
        stza vy
        stza vy+1
        bra @move
```
->
```
:       stz vy                      ; both arms zero it: -1280 = $FB00, low byte zero
        lda firing
        bne @stand
        lda control
        beq @stand
        lda keys
        and #(K_UP|K_FIRE)
        beq @stand
        lda #>(-1280)
        sta vy+1
        lda #SFX_JUMP
        sta SFXREQ
        bne @move                   ; always: SFX_JUMP = 1
@stand: stz vy+1
        lda #1
        sta control
        bne @move                   ; always: A = 1
```

Both arms set vy low = 0 (jump arm stz vy; stand arm stza vy), so a single stz vy before the tests is equivalent. Jump arm then writes vy+1=$FB and SFXREQ as before; bra -> bne on Z=0 from lda #SFX_JUMP (=1). Stand arm: vy+1 = 0 via stz (A dead at @move, so stza's pha/pla is unnecessary), control = 1, then bne on Z=0 from lda #1 replaces bra. Final memory state of vy, control, SFXREQ identical on every path.

Review: Lines 1346-1363 match. The ':' stays on the first line, and the existing ': stz vy' at 1214 shows a label on a macro line assembles on MB. The ':' is reached only by beq :+ at 1344 and fall-through from 1345, and nothing between it and the stores reads vy. Both arms zero vy, so hoisting stz vy is exact. On MB the hoisted stz clobbers A, but lda firing follows. SFX_JUMP = 1 (logic.s:3308), so bne after lda #SFX_JUMP / sta is always taken; the same holds for lda #1 / sta control. At @move, jsr vy_step starts with lda vy, so A and flags are dead. @stand is used only by the three branches here. vy, control and firing are zero page. Bytes: Master extras 10 -> 8 (2 saved); MB 22 -> 12 (10 saved). Cycles: jump arm equal on both; stand arm Master 14 -> 14, MB 32 -> 18.

## r035b:0  src/logic.s:1366-1394  (window i1082)  M -16 B -21  [high]  review: accept

Cycles: Master: down path alt>=dpx 45->41 (py carry) / 37 (no carry); down alt<dpx 50->43/39; up entry 6+20=26 -> 5+14/18; each loop iteration 12 fewer (mov16 gone). Model B: down alt<dpx 59->43/39 (stza 12 cy gone), down alt>=dpx 45->41/37, up entry and loop as Master. Loop exit bpl @vdone unchanged.

Assumption: (1) vy_step (logic.s:1287-1309) is the only thing between the branch and its result: it leaves dpx+1 in {0,$FF} (and/beq/lda #$FF/sta dpx+1; @cdn stores 0; @up arm leaves $FF) so bpl16 dpx means dpx+1 = 0 and bmi means $FF. (2) qx = px on entry to @up: set by mov16 qx,px at logic.s:1316; the only calls between are getaltitude (1324), gravity, vy_step; getaltitude/getinfo/tilexy/maptile (logic.s:305-426) only READ qx (writes go to q2..q5, qy), maprow/mapbyte/pagelogic (engine.s:4513-4550, 4734) write only mapptr and ROMSEL; gravity writes vy,t16; vy_step writes dpx. No alias of $BE/$BF in src or modelb/src (grep). px unchanged in player_update before @vdone. Line 1566 already relies on qx surviving gettileattr. (3) At @vdone (logic.s:1395) sec/lda maph: A, flags, X, Y dead; py/alt/dpx values identical on every path. (4) @up/@upl referenced only at 1366,1391,1393 (grep).

```
        bmi16 dpx, @up
        ; down: dy = min(dy, alt)
        lda alt
        cmp dpx
        bcs :+
        sta dpx
        stza dpx+1                  ; A (alt) is live: the sbc below reads it
:       sec
        sbc dpx
        sta alt
        add16 py, dpx
        bra @vdone
@up:    add16 py, dpx
@upl:   mov16 qx, px
        clc
        lda py
        adc #16
        sta qy
        lda py+1
        adc #0
        sta qy+1
        jsr getaltitude
        sta alt
        bpl @vdone
        inc py
        bne @upl
        inc py+1
        bra @upl
@vdone:
```
->
```
        bpl16 dpx, @down            ; dpx+1 is 0 or $FF (vy_step)
        clc                         ; up: py += dpx, dpx+1 = $FF
        lda py
        adc dpx
        sta py
        bcs @upl
        dec py+1
@upl:   clc                         ; qx = px still: getaltitude keeps it
        lda py
        adc #16
        sta qy
        lda py+1
        adc #0
        sta qy+1
        jsr getaltitude
        sta alt
        bpl @vdone
        inc py
        bne @upl
        inc py+1
        bra @upl
@down:  lda alt                     ; dy = min(dy, alt); dpx+1 = 0 here
        cmp dpx
        bcs :+
        sta dpx
:       sec
        sbc dpx
        sta alt
        clc                         ; py += dpx
        lda py
        adc dpx
        sta py
        bcc @vdone
        inc py+1
@vdone:
```

Blocks swapped: bmi16 dpx,@up -> bpl16 dpx,@down with the up block inline and the down block falling into @vdone, killing the down path's bra @vdone. Up: add16 py,dpx with dpx+1=$FF is py+1 += $FF + C, i.e. unchanged if C else decremented -> clc/lda/adc/sta/bcs @upl/dec py+1 (bcs target is @upl, the next instruction, so no new label). Down: dpx+1 = 0 already, so stza dpx+1 is a no-op store and goes; add16 py,dpx with dpx+1=0 is py+1 += C -> bcc @vdone/inc py+1, falling into @vdone. mov16 qx,px at @upl removed: qx already equals px (assumption 2) and nothing in the loop writes qx. Verified by assembling both targets in a scratch copy: this candidate alone LOGIC -16 (Master), LGCCODE -21 (Model B).

Review: Original matches logic.s:1366-1394. vy_step leaves dpx+1 in {0,$FF} on every exit (sta of 0/$FF, @cdn stz, @up arm keeps $FF), so bpl16/bmi16 are exact complements and the 16-bit adds reduce correctly (down: py+1 += C; up: py+1 += $FF+C). qx = px holds at @upl: set at 1316, only getaltitude/getinfo/tilexy/maptile (read qx), maprow/mapbyte/pagelogic (mapptr/ROMSEL), gravity (vy,t16), vy_step (dpx) run in between, and $BE/$BF have no other symbol on either target (cleo.dbg, modelb labels.txt; Model B ZEROPAGE ends $A2). @up/@upl/@down have no other references in player_update's scope (to 1932). A/flags dead at @vdone (sec/lda maph). anoncheck: 0 retargeted. Every path no slower. Measured in a scratch build: LOGIC -16, LGCCODE -21.

## r035b:1  src/logic.s:1359-1363  (window i1056)  M -0 B -7  [high]  review: accept

Cycles: Master: identical code. Model B: 2+3+12+12+3 = 32 -> 2+3+2+3+3+3 = 16.

Assumption: A dead after the stores: @move is jsr vy_step, whose first instruction (mov16 dpx,vy: lda vy, logic.s:1288) loads A; vy_step does not read flags on entry. vy, vy+1 zero page (logic.s:221).

```
@stand: lda #1
        sta control
        stza vy
        stza vy+1
        bra @move
```
->
```
@stand: lda #1
        sta control
  .if ::MODELB
        lda #0
        sta vy
        sta vy+1
        beq @move                   ; always: A = 0
  .else
        stz vy
        stz vy+1
        bra @move
  .endif
```

Model B stza is pha/lda #0/sta/pla; A is dead so the preserve is unnecessary and one lda #0 serves both stores; after it Z=1 so beq @move is always taken (2 bytes vs jmp 3, 3 cycles both). Master branch keeps the exact original bytes (stza = stz there). Measured: Model B LGCCODE -7, Master unchanged.

Review: Original matches 1359-1363. A dead after: @move is jsr vy_step, which starts lda vy; flags not read on entry. After lda #0 Z=1 so beq always taken. Master text unchanged (stza==stz, bra) -> identical bytes. Model B 32->16 cycles. Measured: LOGIC 0, LGCCODE -7.

## r035b:2  src/logic.s:1337-1339  (window i1056)  M -2 B -4  [high]  review: accept

Cycles: Master: stz zp 3 + lda 2 + sta zp 3 = 8 -> sta zp 3 + inc zp 5 = 8. Model B: 2+3+2+3 = 10 -> 8.

Assumption: A = 0 here on both targets: reached only by falling through lda bactive/bne @nothrow (logic.s:1335-1336). firing = 0: lda firing/bne @nothrow at 1328-1329 and nothing between writes it (control, keys, bactive only read). After: @nothrow's bit vy+1 overwrites N,Z,V (C untouched by both forms); A dead -- the next reads are bit vy+1 then lda alt, or jsr gravity whose add16i starts clc/lda vy (logic.s:1270). anim, firing zero page (logic.s:222,226).

```
        stz anim
        lda #1
        sta firing
```
->
```
        sta anim                    ; A = 0: bactive, just tested
        inc firing                  ; 0 -> 1: firing was tested zero above
```

stz anim stores 0; A already 0 so sta anim stores the same. lda #1/sta firing stores 1 into a byte known to be 0; inc firing does the same. Only differences: final A (1 vs 0, dead) and N,Z (overwritten by bit at @nothrow). Measured: Master LOGIC -2, Model B LGCCODE -4.

Review: Original matches 1337-1339. Reached only by fall-through of lda bactive/bne, so A=0 on both targets; firing tested zero at 1328 and nothing between writes it. After @nothrow: bit vy+1 overwrites N/Z/V, C untouched by both forms, A reloaded on both continuations (lda alt / gravity's clc lda vy). Cycles Master 8->8, Model B 10->8. Measured: LOGIC -2, LGCCODE -4.

## r035b:3  src/logic.s:1358-1358  (window i1056)  M -0 B -1  [high]  review: accept

Cycles: Master bra 3 -> bne taken 3; Model B jmp 3 -> bne taken 3.

Assumption: SFX_JUMP = 1 (logic.s:3308), so lda #SFX_JUMP at 1356 leaves Z=0 and sta SFXREQ keeps it. @move within branch range (~15-20 bytes ahead).

```
        bra @move
```
->
```
        bne @move                   ; always: A = SFX_JUMP (1)
```

The preceding lda #SFX_JUMP sets Z from a nonzero constant; sta does not alter flags, so bne is always taken; jmp is 3 bytes on the Model B, bne 2. Independent of the @stand candidate (range stays short either way). Measured: Model B LGCCODE -1.

Review: Original matches 1358. lda #SFX_JUMP (=1, logic.s:3308) sets Z=0; sta SFXREQ (abs on Master, zp $A6 on Model B) leaves flags; bne always taken, 3 cycles like bra/jmp; in range. Measured: LOGIC 0, LGCCODE -1. All four together: LOGIC -18, LGCCODE -33 (additive).

## r035c:0  src/logic.s:1372-1372  (window i1088)  M -2 B -6  [high]  review: accept

Cycles: clamp arm: Master 3->0, Model B 12->0; other arm unchanged

Assumption: dpx+1 is 0 here. vy_step (logic.s:1287-1310) is called at 1365 with nothing in between, and it always leaves dpx+1 = 0 or $FF (and #$80 / lda #$FF / sta dpx+1; @cdn stores 0). bmi16 dpx,@up (bit dpx+1) was not taken, so dpx+1 = 0.

```
        stza dpx+1                  ; A (alt) is live: the sbc below reads it
```
->
```

```

The store writes 0 to a byte that already holds 0. On the Model B, stza's pla restores A = alt and sets N,Z, but the sec/sbc that follows overwrites every flag, so nothing downstream can see the difference.

Review: dpx+1 is 0 or $FF after vy_step (sign extension, or @cdn's stz); bmi16 not taken means it is 0, and nothing between 1366 and 1372 writes it. On the Model B, the flags pla sets are overwritten by sec/sbc. No ':' is removed. Verified by assembling both targets.

## r035c:1  src/logic.s:1376-1377  (window i1088)  M -2 B -3  [high]  review: accept

Cycles: no carry 23->14, carry 23->21; the same on both targets (bra/jmp are 3 cycles)

Assumption: dpx+1 = 0 on the down arm (same proof as the line-1372 candidate). At @vdone the code starts with sec / lda maph, so A, N, Z, C and V are all dead, and X and Y are not touched on either path. @vdone is +43 bytes away (checked by assembling).

```
        add16 py, dpx
        bra @vdone
```
->
```
        clc                         ; dpx+1 = 0 on this arm
        lda py
        adc dpx
        sta py
        bcc @vdone
        inc py+1
        bcs @vdone                  ; C = 1: the adc carried
```

py += dpx when dpx+1 = 0 is the low-byte add plus a carry into py+1. On the carry path, bcc falls through only when C = 1, and inc leaves C alone, so bcs is always taken. That turns the 3-byte bra (Model B jmp) into a 2-byte branch.

Review: dpx+1 = 0 on the down arm, so py += dpx is the low add plus a carry. inc keeps C = 1, so bcs is always taken. @vdone starts with sec/lda, and A and the flags are dead there. Assembled: bcc +$2B, bcs +$27.

## r035c:2  src/logic.s:1378-1378  (window i1088)  M -2 B -2  [high]  review: accept

Cycles: C=1 path 20->14, C=0 path 20->18; same on both targets

Assumption: dpx+1 = $FF on the @up arm: vy_step leaves dpx+1 as 0 or $FF, and bmi16 took this arm on bit 7. @upl follows immediately and starts with clc / lda py, so the flags and A are dead there.

```
@up:    add16 py, dpx
```
->
```
@up:    clc                         ; dpx+1 = $FF on this arm
        lda py
        adc dpx
        sta py
        bcs @upl
        dec py+1
```

py+1 + $FF + C = py+1 - 1 + C. When C = 1, py+1 is unchanged, so the branch skips the dec. When C = 0, dec py+1 does the -1.

Review: dpx+1 = $FF on the @up arm, so the high byte becomes py+1-1+C, which is the bcs/dec pair. The flags and A are dead at @upl (clc/lda py). @upl is only reached by fall-through and by the loop's own branches.

## r035c:3  src/logic.s:1379-1380  (window i1088)  M -8 B -8  [high]  review: accept

Cycles: 12 cycles fewer on every pass through the loop, on both targets

Assumption: qx = px holds at @upl. player_update sets it at 1316, and nothing between there and here writes qx or px: getaltitude, getinfo, tilexy, maptile, maprow (both variants, engine.s:4511-4541), mapbyte and pagelogic only read qx; gravity writes vy and t16; vy_step writes dpx; the throw code touches anim and firing. px is first written in the horizontal step, after 1562. qx = $BE is a fixed logic temp, above both targets' ZEROPAGE segments (Model B ends at $A2, Master's ZP area ends at $A8), so no interrupt code uses it.

```
@upl:   mov16 qx, px
        clc
```
->
```
@upl:   clc                         ; qx = px still, from the entry
```

The copy stores into qx the value qx already holds, on the first entry and on every later iteration. The @upl label stays on the clc.

Review: qx = px from 1316. getaltitude, getinfo, tilexy and maptile (Model B maprow, mapbyte, pagelogic) only read qx; getaltitude writes qy. gravity, vy_step and the throw code do not touch qx or px, and no interrupt code uses $BE/$BF. The loop never writes qx or px.

## r035c:4  src/logic.s:1393-1393  (window i1088)  M -0 B -1  [high]  review: accept

Cycles: 3->3 on both targets (taken)

Assumption: Before the inc, py+1 is never in $7F..$FE. py is Cleo's y position: 0 <= maph <= $800 (tilexy's one-byte tile coordinate, game.s:40-48), player_update runs only while health != 0 (logic.s:1154-1157), and falling past maph-24 kills her at @fell. Above the top of the map py is a small negative, where py+1 = $FF and the inc gives 0.

```
        bra @upl
```
->
```
        bpl @upl                    ; py+1 < $7F
```

inc py+1 sets N from the new high byte. For every reachable value the result is 0..$7F, so N = 0 and bpl is always taken, like the bra. Z cannot be used instead, because $FF -> 0 does happen.

Review: py stays within about [-24, maph+8], with maph < $800: above the map getaltitude returns negative, which pushes her down. So after the inc, py+1 is 0..8 and N = 0, and bpl is always taken. Same bytes on the Master, 1 byte saved on the Model B, taken branch 3 cycles on both.

## r035c:5  src/logic.s:1395-1402  (window i1090)  M -11 B -11  [high]  review: accept

Cycles: push 36->23, fell 35->22 (original with no overflow, so bvc taken); same on both targets

Assumption: (1) Y is dead at @vdone. Nothing in player_update after this point (1402-1926) reads Y before writing it: tay/tya at 1445/1450; gettileattr and getaltitude either leave Y alone or write it before reading (maptile, getinfo); m_addsprite writes Y with ldy, or returns at @full without reading it. player_update returns to frame_loop/game.s, which does lda exiting / jsr render_frame. (2) t16 is dead after the compare: every later reader writes it first (1449/1452, 1480+sx16, 1490/1493, 1534/1535, 1538, 1813/1816), and so do engine.s:4572 and menu.s:578/589. (3) maph - 24 - py fits in int16: maph <= $800 and py stays within a few hundred of [0, maph], so the bvc/eor overflow fix never fires.

```
        sec
        lda maph
        sbc #24
        sta t16
        lda maph+1
        sbc #0
        sta t16+1
        bge16 t16, py, @push
```
->
```
        sec                         ; fell: maph - py - 24 < 0
        lda maph
        sbc py
        tay
        lda maph+1
        sbc py+1
        cpy #24
        sbc #0
:                                   ; placeholder - keeps the anonymous-label count
        bpl @push
```

Original: push iff (maph-24) >= py (signed). New: Y:A = maph - py, 16-bit. cpy #24 sets C = no borrow of lo-24, and sbc #0 gives the high byte of (maph - py - 24). bpl pushes iff that is >= 0, which is the same condition when there is no overflow. @fell and @push both reload A and do not read flags. X is not changed.

Review: Y:A = maph-py, cpy #24 gives the borrow, and sbc #0 gives the high byte of maph-py-24. There is no int16 overflow in range, so it is the same signed test. Y is dead: gettileattr and getaltitude write Y before reading it (maptile tay), the ground path does tay before tya, addsprite does ldy, and game.s callers never read Y. t16 is dead: every later use writes it first. The anonymous-label count is kept. Assembled OK.

## r035c:6  src/logic.s:1404-1408  (window i1096)  M -0 B -8  [high]  review: accept

Cycles: Model B 44->31; Master 29->29 (same instructions, reordered)

Assumption: bar_touch (logic.s:3223) is lda #1 / sta BARDIRTY / rts: it reads nothing and writes only A and BARDIRTY, so storing control and vx before it instead of after changes nothing. A after the mov16 is dead because bar_touch overwrites it and lda #SFX_DIE follows, so stza need not save it. All four targets are logic zero page (logic.s:217-246). .if MODELB is used inside routines in this file already (3217).

```
        stza health
        jsr bar_touch
        stz control
        stz vx
        stz vx+1
```
->
```
  .if MODELB
        lda #0
        sta health
        sta control
        sta vx
        sta vx+1
  .else
        stz health
        stz control
        stz vx
        stz vx+1
  .endif
        jsr bar_touch
```

The same four zero stores and the same bar_touch call, with the call moved after the stores. On the Model B, one lda #0 serves four sta, replacing pha/lda/sta/pla plus three lda #0/sta. The flags after the jsr come from bar_touch in both versions. Fallback if conditionals are unwanted: stza health -> stz health alone saves 2 bytes on the Model B.

Review: bar_touch is lda #1/sta BARDIRTY/rts and reads none of these. A (frame+1) is dead: bar_touch and lda #SFX_DIE reload it. The flags after the jsr come from bar_touch on both versions. MODELB is defined as 0 on the Master (cpu.inc:22). Model B 21->13 bytes; Master 11->11.

## r035c:7  src/logic.s:1411-1412  (window i1100)  M -8 B -8  [high]  review: accept

Cycles: 12 fewer on both targets

Assumption: qx = px on every path to @push. The down arm and the @upl exit carry it from the entry copy at 1316 (see the @upl candidate): getaltitude's callees only read qx, and px is unchanged. @fell's mov16 evframe, bar_touch and stores do not touch qx. The later comment at 1565 ('qx = px ... already, set at @push') stays true.

```
@push:  mov16 qx, px
        clc                         ; qy = py + 16 in one pass
```
->
```
@push:  clc                         ; qx = px still; qy = py + 16 in one pass
```

The copy stores into qx the value it already holds. The @push label stays on the clc.

Review: Every path to @push carries qx = px: the down arm from 1316, the @up exit from the loop, and @fell (mov16 evframe, bar_touch and zero stores do not touch qx). @push is referenced only by the bge16.

## r035c:8  src/logic.s:1425-1434  (window i1118)  M -5 B -5  [high]  review: accept

Cycles: control=0: 6->6; alt>0: 16->14; alt=0: 16->13; alt<0: 17->14; same on both targets

Assumption: (1) Y is dead here: the ground path writes it (tay at 1445) before tya; @air never reads Y; @nofric onward reads Y only after writing it (see the @vdone candidate). A at @nofric and @air is dead: both start with a load (lda control, lda vx). (2) @air is in branch range: as assembled with all candidates, bpl @air is +120 (Master) and +121 (Model B) bytes. Candidates from other regions can only shrink this. (3) A = q6 after sta q6 at 1423.

```
        lda control
        bne :+
        jmp @nofric
:
        lda alt
        beq :+
        bmi :+
        jmp @air
:
:       lda q6
```
->
```
        ldy control                 ; (A = q6 kept for the cmp below)
        bne :+
        jmp @nofric
:
        ldy alt
        beq :+
        bpl @air
:
:
```

ldy sets N,Z exactly as lda did, so the bne, beq and bpl test the same values, and A keeps q6 for the cmp #3 at 1435. 'bmi :+ / jmp @air' after a failed beq means alt > 0 goes to @air, which is bpl @air, since @air is in range. (2 bytes come from dropping lda q6, 3 from the branch.)

Review: ldy sets N/Z like lda, and A keeps q6 from the sta at 1423 for the cmp #3. alt>0 -> bpl @air; alt<=0 -> ground. Line 1434's ':' is the target of no branch (no :- or :++ reaches it), and both ':' are kept. Y is dead as for index 5. @air is 121 bytes (Model B) / 120 (Master) from the bpl, with only this change and none of the others. Assembled OK.

## r035d:0  src/logic.s:1398-1402  (window i1117)  M -7 B -7  [high]  review: accept

Cycles: All paths: 23 -> 13 cycles for lines 1398-1402 up to the bvc (sta t16 3, lda maph+1 3, sbc 2, sta t16+1 3, lda t16 3, cmp py 3, lda t16+1 3, sbc py+1 3 -> tax 2, lda maph+1 3, sbc 2, cpx py 3, sbc py+1 3). bvc/eor/bpl unchanged. Master and Model B identical (no macros differ).

Assumption: (1) t16 is dead after the bge16: on every path from @push/@fell it is written before it is read. bar_touch (logic.s:3223) only writes BARDIRTY. gettileattr/tilexy/maptile/maprow/mapbyte/pagelogic (logic.s:305-440, engine.s:4511-4552, 4734) never touch t16. After that, the ground path writes t16 first (sta t16 at 1449), @air writes it first (sta t16), and @nofric's run path writes it (lda #36/sta t16 or mov16i) before sub16/add16. The @hmove/@hl loop does not read it, and the boomerang block and @bdone write it first. (2) X is dead at this point. gettileattr may return X unchanged (tilexy @out), but after that X is written before any read on every path: ground (tax at 1442 originally), @nofric run path tax before @acc's txa, and after @hdone every path writes X first (tax at the @animdone hurt test, tax at @afterhurt, ldx at @noctl, tax at @spr). getaltitude in the @hl loop reads X only inside maptile, after tilexy has set it. (3) cpx zp exists on both CPUs, and the C out of cpx equals the C out of cmp with the same value in A.

```
        sta t16
        lda maph+1
        sbc #0
        sta t16+1
        bge16 t16, py, @push
```
->
```
        tax                         ; X = t16 low (maph - 24)
        lda maph+1
        sbc #0                      ; A = t16 high
        cpx py                      ; bge16 t16, py with t16 in X:A
        sbc py+1
        bvc :+
        eor #$80
:       bpl @push
```

t16 = maph - 24 is kept in X (low) and A (high) instead of being stored and reloaded. tax replaces sta t16/lda t16, and A already holds the high byte from sbc #0, so sta t16+1/lda t16+1 go. cpx py produces the same carry as lda t16/cmp py and does not disturb A. sbc py+1 then computes t16hi - py+1 - borrow exactly as the macro's lda t16+1/sbc py+1 did, so N, V and the bvc/eor/bpl outcome are identical. The macro's one anonymous label is spelled inline (bvc :+ / : bpl), so the count is unchanged. The change starts at line 1398 (i1117), before this region's first window, and overlaps i1120 (line 1401).

Review: Original matches 1398-1402. cpx py gives the same C as lda t16/cmp py; A already holds maph+1-borrow, so sbc py+1 gives the same N/V. The inline bvc :+/: bpl keeps the macro's one anon label. X is dead: every path onward writes X before reading it (tax at 1442/1521, tilexy before maptile, tax/ldx at 1681/1700/1712/1767). t16 lo is written before it is read on every path, at the latest at 1906. t16+1 is left stale on some paths, but game_frame writes it at 931 before any read, and no other symbol aliases $D1/$D2. Bytes 22->15 on both targets, cycles 23->13.

## r035d:1  src/logic.s:1404-1408  (window i1120)  M -0 B -8  [high]  review: accept

Cycles: Model B: stza health 12 (pha 3, lda 2, sta 3, pla 4) + 3x stz 5 = 27 -> lda #0 2 + 4x sta zp 3 = 14. The jsr bar_touch (moved after the stores) costs the same. Master: unchanged code (stz health = the old stza health, same order).

Assumption: (1) A is dead at stza health: bar_touch (logic.s:3223) starts with lda #1, and after it the code reloads A (lda #SFX_DIE). (2) bar_touch reads none of health/control/vx (it is lda #1 / sta BARDIRTY / rts), so moving the jsr after the stores changes nothing. (3) health, control and vx are zero page on the Model B (listing: 85 rr). (4) The flags after the block are not consumed: lda #SFX_DIE follows.

```
        stza health
        jsr bar_touch
        stz control
        stz vx
        stz vx+1
```
->
```
  .if MODELB
        lda #0                      ; one zero for the four stores (A dead:
        sta health                  ; bar_touch loads A itself)
        sta control
        sta vx
        sta vx+1
        jsr bar_touch
  .else
        stz health
        jsr bar_touch
        stz control
        stz vx
        stz vx+1
  .endif
```

Model B only: the original emits pha/lda #0/sta/pla plus three lda #0/sta pairs. One lda #0 serves all four stores because sta does not change A. bar_touch only sets BARDIRTY=1 and returns A=1, and lda #SFX_DIE follows, so the final A and flags do not matter. The Master branch of the .if is the original sequence (stza = stz on the 65C02) and assembles to identical bytes.

Review: Original matches. Model B: stza (6 bytes) + 3x stz (4 each) + jsr = 21 -> lda #0 + 4 sta zp + jsr = 13. A is clobbered by bar_touch (lda #1) anyway, and lda #SFX_DIE follows. bar_touch touches only BARDIRTY, so it can move. Master: the .else branch assembles to identical bytes (stza = stz). Model B 27->14 cycles.

## r035d:2  src/logic.s:1411-1411  (window i1130)  M -8 B -8  [high]  review: accept

Cycles: All paths through @push: 12 fewer cycles (lda 3, sta 3, lda 3, sta 3 removed). Same on both targets.

Assumption: qx == px on both paths into @push. player_update sets qx = px at logic.s:1316, and the @up loop sets it again at 1380 (@upl) before each getaltitude. Since then nothing writes px or qx: getaltitude/getinfo/tilexy (logic.s:314-420) write only qy and q2-q5 and read qx; maptile/maprow/mapbyte/pagelogic write only mapptr/ROMSEL; gravity (1269) and vy_step (1287) write vy, t16 and dpx; the down path writes alt, dpx and py; @fell's bar_touch writes BARDIRTY. qx = $BE (logic.s:268) is logic-only zero page (no other symbol at $BE/$BF). The code downstream already relies on qx surviving gettileattr (comment at logic.s:1566).

```
@push:  mov16 qx, px
```
->
```
@push:                              ; qx = px already: set before getaltitude, untouched since
```

mov16 qx, px rewrites qx with the value it already holds on every path that reaches @push (bge16 taken from @vdone, or fall-through from @fell), so removing it changes no memory. A is reloaded immediately (lda py after clc), and no flags from the mov16 are consumed (clc then lda).

Review: @push is reached only from the bge16 at 1402 or by falling through from @fell. qx = px is set at 1316 or at @upl (1379) and nothing writes px or qx after that: getaltitude/getinfo/tilexy only read qx, maprow/mapbyte/pagelogic write mapptr/ROMSEL, and gravity/vy_step/the down path/@fell/bar_touch write other things. Existing code already relies on qx surviving across getinfo/maptile calls. clc and lda py follow, so no flag or A dependence. mov16 zp = 8 bytes, 12 cycles saved.

## r035d:3  src/logic.s:1425-1437  (window i1143)  M -8 B -8  [high]  review: accept

Cycles: control=0: ldx 3 + bne 2 + jmp 3 = 8 -> 8. alt>0: 16 -> 14. alt=0, q6!=3: 20 -> 16. alt=0, q6=3: 22 -> 17. alt<0: 1-3 fewer on each path. Same on both targets. Branch ranges: in the original layout, bpl @air lands 123 bytes on (Model B) and 122 (Master), and beq @air 117/116. All are within 127, and every other candidate only shrinks them.

Assumption: (1) A = q6 on entry (sta q6 at line 1423 just before), and nothing in between reads A except the new cmp. (2) X is dead from here. gettileattr may even leave the caller's X, but on every path onward X is written before it is read: the @nofric run path does tax before @acc's txa; after @hdone X is written at @animdone/@afterhurt/@noctl/@spr before any read; @hl's getaltitude reads X only in maptile, after tilexy wrote it. (3) A is dead at @nofric (lda control first), @air (lda vx first) and the ground code (lda vx first). (4) @air is within branch range: Model B listing @air - $FBE = $7E before this change, Master $7D, and this change removes 3 more bytes before @air.

```
        lda control
        bne :+
        jmp @nofric
:
        lda alt
        beq :+
        bmi :+
        jmp @air
:
:       lda q6
        cmp #3
        bne :+
        jmp @air
```
->
```
        ldx control                 ; X, not A: A keeps q6 for the push test
        bne :+
        jmp @nofric
:
        ldx alt
        beq :+
        bpl @air
:
:       cmp #3                      ; A = q6 (push), still
        beq @air
```

ldx control and ldx alt set N,Z exactly as lda did, so bne/beq/bpl behave identically, while A keeps q6 from the sta q6 just above. The later lda q6 is therefore redundant, and cmp #3 alone gives the same Z. beq :+ / bmi :+ / jmp @air equals beq :+ / bpl @air (alt>0 -> @air, alt<=0 -> the q6 test). bne :+ / jmp @air / : equals beq @air. The control=0 path now reaches @nofric with A = q6 and X = control, instead of A = control and X = gettileattr's X. @nofric reloads A, and X is dead.

Review: Original matches 1425-1437. A = q6 from the sta q6 at 1423, and ldx sets N/Z as lda did. The anon count is 3 -> 3, the 1438 ':' is kept, and no remaining reference crosses the changed labels. X is dead, as in candidate 0, and A is reloaded at @nofric, @air and ground. Branch range, Model B listing: bpl next = $FBE and @air moves $103C -> $1034, offset 118. beq next = $FC2, offset 114. Master: 117/113. Every path is equal or faster.

## r035d:4  src/logic.s:1440-1453  (window i1153)  M -8 B -8  [high]  review: accept

Cycles: Ground path only: 50 -> 40 cycles. Original: 32 for the 3vx code + 18 for neg16. New: lda 3, asl 2, sta 3, lda 3, rol 2, asl zp 5, rol 2, sta 3, sec 2, lda 3, sbc 3, sta 3, lda 3, sbc 3 = 40. Same on both targets.

Assumption: (1) t16+1 is not read between this block and the 'sta t16+1' after the asr (line 1473): the asr at 1455-1472 uses only A and t16 (and the i1167 candidate begins with sta t16+1). (2) X and Y are dead after this block (the original left 2vx in them). X: proven as in the i1117 candidate. Y: nothing in the rest of player_update (logic.s:1440-1925) reads Y (checked: no tya/sty/cpy/,y use), addsprite writes Y before using it, and player_update returns into game.s:251-305, which reloads its registers. (3) The flags at the end are not consumed: the next instruction is cmp #$80.

```
        lda vx
        asl
        tax                         ; X = 2*vx low, C untouched by tax
        lda vx+1
        rol                         ; A = 2*vx high
        tay
        txa
        clc
        adc vx
        sta t16
        tya
        adc vx+1
        sta t16+1                   ; t16 = 3vx
        neg16 t16                   ; leaves A = t16+1
```
->
```
        lda vx
        asl
        sta t16
        lda vx+1
        rol
        asl t16
        rol
        sta t16+1                   ; t16 = 4vx
        sec
        lda vx
        sbc t16
        sta t16
        lda vx+1
        sbc t16+1                   ; A:t16 = vx - 4vx = -3vx
```

-3vx = vx - 4vx (mod 2^16). 4vx: asl lo/rol hi twice (the first shift into t16 with hi in A, then asl t16/rol A), and the high byte is stored to t16+1. sec and a 16-bit vx - t16 give A = high byte of -3vx and t16 = low byte. That is exactly the state neg16 left (A = high, t16 = low). The only difference is that t16+1 holds 4vx high instead of the -3vx high, and it is overwritten before it is read. Verified exhaustively over all 65536 vx together with the i1167 candidate.

Review: Original matches 1440-1453. vx - 4vx = -3vx mod 2^16, giving A = hi and t16 = lo, the same state neg16 left. Checked exhaustively over all 65536 vx. t16+1 is not read before the store at 1473 (or before the sta t16+1 that opens candidate 5). X/Y dead: X as above, and Y is not read in the rest of player_update (addsprite does ldy before use), and the Y at return already varies by path. 32->24 bytes, 50->40 cycles, both targets.

## r035d:5  src/logic.s:1454-1474  (window i1167)  M -12 B -12  [high]  review: accept

Cycles: Ground path only: asr x6 54 + sta t16+1 3 + add16 20 = 77 -> 55 (56 when -3vx is negative). Same on both targets.

Assumption: (1) On entry A = high byte and t16 = low byte of -3vx (both the original neg16 and the i1153 candidate leave that state). (2) t16 is dead after the add: the next code (lda q6 ... sta t16, sx16 t16) writes t16 and t16+1 before reading them. (3) Nothing refers to the anonymous placeholder at 1454, and no other anonymous label or reference lies between it and the new ':' (the next one is inside sx16, after the add16), so moving it keeps every count.

```
:                                   ; placeholder - keeps the anonymous-label count
        cmp #$80                    ; asr16 x6, high byte held in A
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        sta t16+1
        add16 vx, t16
```
->
```
        sta t16+1                   ; asr16 x6 as asl16 x2 into a sign byte in A
        and #$80
        beq :+
        lda #$FF
:       asl t16
        rol t16+1
        rol
        asl t16
        rol t16+1
        rol                         ; A:t16+1 = asr6(-3vx)
        sta t16
        clc                         ; add16 vx, (t16+1, t16)
        lda vx
        adc t16+1
        sta vx
        lda vx+1
        adc t16
        sta vx+1
```

The 16-bit asr6 result is the top two bytes of the 24-bit sign-extended value shifted left 2. sta t16+1 stores the high byte, and and #$80/beq/lda #$FF makes A the sign byte. Two rounds of asl t16/rol t16+1/rol A shift the 24-bit A:t16+1:t16 left by 2. That leaves A = high byte and t16+1 = low byte of asr6(-3vx), the value the six cmp/ror/ror rounds left in t16+1:t16. The add16 is written with those two locations (t16 reused for the high byte), so it produces the same vx and the same final C/N/Z/V (same additions). Checked exhaustively over all 65536 vx together with the i1153 candidate.

Review: Original matches 1454-1474. Checked exhaustively over every (A, t16) entry state: the sign byte plus a 24-bit shift left by 2 gives asr6 exactly, and the add16 gives the same vx and the same C/N/Z/V. Nothing references the placeholder ':' (listing decoded: the branches at 1426/1430/1431/1436 target 1428/1433/1438), and there is no anon label or reference between the old and new positions, so its move is safe. t16 is dead (sta t16 at 1480, sx16 writes both bytes). 45->33 bytes, 77->55/56 cycles.

## r036a:0  src/logic.s:1440-1485  (window i1152)  M -37 B -37  [high]  review: accept

Cycles: ground block (1440-1474) worst 127 -> 84; 24p block (1476-1485) 50/51 -> 38/39 (bpl taken/not); every path faster; Model B identical (no macros in either new block; orig neg16/add16/sx16 are target-independent)

Assumption: (1) vx != $8000: at vx=$8000 3vx=$8000 and neg16 wraps back to $8000, so the original and the ceil form differ there only; exhaustive simulation of all 65536 vx x q6 in {-3..2,4} x C-in matches for every vx except $8000. vx is only written in logic.s (stz, knockback +-768 at 1204-1213, this friction/accel code, @wall stz); friction bounds |vx| to ~2900, and the air path at 1487 already assumes |5vx| < 32768. (2) X and Y dead at the end: original also clobbers X,Y (2vx) on this path only, and @nofric (1512) is reached from 1427 and @air with other X/Y values; after @nofric X is written (tax at 1522/@spr/tilexy) before any read, no Y read before getinfo/ldy. (3) t16/t16+1 dead after: next reads of t16 after @nofric are all preceded by writes (1534/1538, 1810, 1905). (4) q6 in -3..4 so 24p fits a signed byte (same assumption as the original sx16 comment).

```
        lda vx
        asl
        tax                         ; X = 2*vx low, C untouched by tax
        lda vx+1
        rol                         ; A = 2*vx high
        tay
        txa
        clc
        adc vx
        sta t16
        tya
        adc vx+1
        sta t16+1                   ; t16 = 3vx
        neg16 t16                   ; leaves A = t16+1
:                                   ; placeholder - keeps the anonymous-label count
        cmp #$80                    ; asr16 x6, high byte held in A
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        cmp #$80
        ror
        ror t16
        sta t16+1
        add16 vx, t16
        ; * 24: |24*q6| <= 96, so it fits in 8 bits and needs one sign extension
        lda q6
        asl
        asl
        asl
        sta t16                     ; 8p
        asl                         ; 16p
        clc
        adc t16                     ; 24p
        sx16 t16
        add16 vx, t16
```
->
```
        lda vx
        asl
        tax                         ; X = 2*vx low, C untouched by tax
        lda vx+1
        rol                         ; A = 2*vx high
        tay
        txa
        clc
        adc vx
        sta t16                     ; t16 = 3vx low
        ldx #0
        tya
        adc vx+1                    ; A = 3vx high, N = its sign
        bpl :+
        dex
:       stx t16+1                   ; t16+1:A:t16 = 3vx sign-extended to 24 bits
        asl t16                     ; two left shifts: t16+1:A = floor(3vx/64)
        rol
        rol t16+1
        asl t16
        rol
        rol t16+1                   ; t16 = the dropped bits (3vx & 63) << 2
        ldy #0
        cpy t16                     ; C = nothing dropped: floor == ceil
        eor #$FF
        adc vx                      ; vx + ~floor + C = vx - ceil(3vx/64)
        sta vx
        lda vx+1
        sbc t16+1
        sta vx+1
        ; * 24: |24*q6| <= 96, so it fits in 8 bits and needs one sign extension
        lda q6
        asl
        asl
        asl
        sta t16                     ; 8p
        asl                         ; 16p
        clc
        adc t16                     ; 24p, N = its sign
        bpl :+
        dey                         ; Y = 0 from above: now the sign fill
:       clc
        adc vx
        sta vx
        tya
        adc vx+1
        sta vx+1
```

Original: vx += asr6(neg16(3vx)) then vx += sx16(24p). For 16-bit z=3vx (same wrapping computation, lines unchanged up to adc vx+1), asr6(-z) = floor(-z/64) = -ceil(z/64) (z != -32768). floor(z/64) is computed exactly as the top 16 bits of the 24-bit (sign:hi:lo) shifted left twice (X=0/$FF sign fill via N from adc vx+1; ldx #0 placed before tya so it does not disturb C or N). ceil = floor + [z&63 != 0]; after the shifts t16 = (z_lo<<2)&255 which is zero iff z&63==0, so ldy #0/cpy t16 gives C = [dropped==0]. eor #$FF/adc vx with that C computes vx_lo + ~r_lo + C, and sbc t16+1 continues the same 16-bit add of ~r (adc ~m == sbc m incl. carry), giving vx + ~r + C = vx - r - 1 + C = vx - ceil(z/64). 24p: ldy #0 left Y=0; N after adc t16 is the sign of 24p (same bit sx16 tested with and #$80), so Y becomes the sign byte; clc/adc vx/sta vx/tya/adc vx+1/sta vx+1 is add16 with the sign extension from Y instead of t16+1. Verified by exhaustive simulation (all vx, q6 in -3..2,4, both carry-in), byte counts by ca65: 111 -> 74.

Review: The original matches lines 1440-1485 exactly. I simulated every case: all 65536 vx values, q6 in -3..2 and 4, and both carry-in states. The only mismatch is at vx=$8000, which cannot occur. vx is written only by stz, the knockback of +-768, this friction/accel code (steady state about 2816 on the ground and about 768 in the air) and @wall. t16, X and Y are dead afterwards. Every later read of t16 comes after a write (1534/1538, 1813, 1906), and the t16 users in engine.s and menu.s write it first. On the original ground path X and Y already hold junk (2vx), and they are rewritten (tax, getinfo's tay, addsprite's ldy) before anything reads them. The anonymous-label count stays at 2 and no :+/:- reference crosses the region. ca65 gives 111 -> 74 bytes on both CPUs. Cycles fall from 127 to 83/84 and from 50/51 to 38/39. The macros involved expand the same on both targets, and the flags left behind are dead (bra @nofric, then lda control).

## r036a:1  src/logic.s:1488-1497  (window i1183)  M -4 B -4  [high]  review: accept

Cycles: 31 -> 25 on the single path; Model B same

Assumption: t16 (low byte) dead after @air: it is no longer written here (original left 4vx low in it); every later read of t16 is preceded by a write (1534/1538 accel, 1810, 1905) and the other paths into @nofric leave unrelated values in t16. t16+1 still ends = 4vx high as before.

```
        lda vx
        asl
        sta t16
        lda vx+1
        rol
        sta t16+1                   ; t16 = 2vx
        asl t16
        rol t16+1                   ; t16 = 4vx
        clc
        lda t16
```
->
```
        lda vx+1
        sta t16+1
        lda vx
        asl
        rol t16+1
        asl
        rol t16+1                   ; A:t16+1 = 4vx (low in A, t16 not written)
        clc
```

Original builds 4vx in t16:t16+1 then reloads the low byte into A. New keeps the low byte in A: copy vx+1 to t16+1, asl A / rol t16+1 twice gives the same 4vx (low in A, high in t16+1) with the same carry chain; clc then adc vx as before, and line 1500 lda t16+1 reads the same 4vx high. Exhaustively simulated for all 65536 vx and both carry-ins: identical vx; ca65 42 -> 38 bytes for 1488-1511.

Review: The original matches lines 1488-1497 exactly. I simulated lines 1488-1511 for every vx and both carry-ins: vx, t16+1, A and C all come out identical. The only difference is that t16 low is no longer written, and it is dead. Every later read comes after a write (1534/1538, 1813, 1906), and the control==0 path already reaches @nofric with an unrelated value in t16. ca65 gives 17 -> 13 bytes on both targets, and cycles fall from 31 to 25.

## r036a:2  src/logic.s:1436-1437  (window i1121)  M -3 B -3  [high]  review: accept

Cycles: q6!=3 (ground): 3 -> 2; q6==3: 5 -> 3; Model B same

Assumption: @air (1487) is within branch range: bytes from 1438 to 1486 are 113 (Master) / 114 (Model B, bra=jmp) in the current source, and shrink further with the other candidates; Z comes from cmp #3 at 1435.

```
        bne :+
        jmp @air
```
->
```
        beq @air
```

bne :+ skips the jmp when q6!=3, else jmp @air; beq @air takes the same branch on the same Z. Spans i1151 (bne) and my i1152 (jmp).

Review: The original matches lines 1436-1437 exactly. Z comes from the cmp #3 at 1435, so beq @air branches in the same cases as before. Branch distance from the listings: on the Master the jmp is at $AEC and @air at $B60, so the offset is 113; on the Model B the jmp is at $FC7 and @air at $103C, so the offset is 114. Both are within 127 and only shrink if the other candidates are applied. The ':' at 1438 stays, so no anonymous branch is retargeted. 5 -> 2 bytes. Cycles: 3 -> 2 when q6 != 3, and 5 -> 3 when q6 == 3.

## r036a:3  src/logic.s:1430-1432  (window i1117)  M -3 B -3  [high]  review: accept

Cycles: alt==0: 3 -> 3; alt<0: 5 -> 4; alt>0: 7 -> 5; Model B same

Assumption: N,Z from lda alt at 1429; @air within +127 from after the bpl: lines 1433-1486 are 122 (Master) / 123 (Model B) bytes now and only shrink with the other candidates. Outside my window range (found while reading), reported for completeness.

```
        beq :+
        bmi :+
        jmp @air
```
->
```
        beq :+
        bpl @air
```

alt==0 -> beq to the ":" (same); alt!=0 and N clear (alt>0) -> original bmi falls to jmp @air, new bpl @air; alt<0 -> original bmi :+, new bpl not taken falls into the ":" at 1433.

Review: The original matches lines 1430-1432 exactly. N and Z come from the lda alt at 1429. If alt == 0, beq still reaches the ':' at 1433. If alt > 0, bpl @air replaces bmi-not-taken followed by jmp. If alt < 0, bpl falls through into the same ':' that bmi :+ used to reach. The ':' is kept and beq :+ still targets it. Branch distance from the end of the bpl to @air is 122 on the Master and 123 on the Model B, both within 127. 7 -> 4 bytes. Cycles: 3 -> 3, 5 -> 4 and 7 -> 5.

## r036b:0  src/logic.s:1473-1486  (window i1184)  M -20 B -20  [high]  review: accept

Cycles: Original (sta t16+1 .. bra @nofric): 76 cy (24p >= 0) / 77 (24p < 0). New: 24p>=0 no carry 46, 24p>=0 carry 50, 24p<0 no carry 47, 24p<0 carry 50. Model B counts are the same (bra = jmp = 3). Faster on every path. This is not timing-critical code.

Assumption: Y dead at the site: the ground path already destroys Y (tay at logic.s:1445, 'A = 2*vx high'), and nothing after @nofric reads Y before writing it (checked through @hmove, getaltitude/getinfo/gettileattr at logic.s:371-437, to the end of player_update). t16 and t16+1 are dead after @nofric: the run path writes both at 1533-1538 before sub16/add16 reads them, and every later t16 use (logic.s:1810+, 1906+) writes first. t16, q6 and vx are zero page (t16=$D1, q6=$C8, vx .zeropage at logic.s:220). No anonymous branch crosses lines 1455-1523.

```
        sta t16+1
        add16 vx, t16
        ; * 24: |24*q6| <= 96, so it fits in 8 bits and needs one sign extension
        lda q6
        asl
        asl
        asl
        sta t16                     ; 8p
        asl                         ; 16p
        clc
        adc t16                     ; 24p
        sx16 t16
        add16 vx, t16
        bra @nofric
```
->
```
        tay                         ; Y = asr6(-3vx) high; A = t16 low below
        lda q6                      ; * 24 = (2p + p) << 3, the same byte as 16p + 8p
        asl
        clc
        adc q6
        asl
        asl
        asl                         ; A = 24p, N = its bit 7
        bpl :+
        dey                         ; sign-extend 24p into the high byte
:       clc
        adc t16                     ; A:Y = asr6(-3vx) + sx(24p)
        bcc :+
        iny
        clc
:       adc vx
        sta vx
        tya
        adc vx+1
        sta vx+1
        bra @nofric
```

Original: vx += (H:T), where H = A (asr6 high) and T = t16 (after ror); then P = (16p+8p) mod 256, sx16, and vx += sx(P). The new code fuses both 16-bit adds into one. Y = H. P is computed as ((2q6 + q6) << 3) mod 256, which is the same byte as 16p+8p (the clc before adc q6 kills the asl carry). N after the last asl is bit 7 of P, exactly what sx16 tested (and #$80), so dey = adding the $FF high byte of sx(P). clc (both paths) / adc t16 gives the low byte of T+P. bcc/iny carries into Y, and the clc on the iny path restores C=0 (on the bcc-taken path C is already 0). A:Y is then (H:T)+sx(P) mod 2^16, added to vx with one add (adc vx / tya / adc vx+1). Two mod-2^16 adds equal one add of the mod-2^16 sum. Checked in Python: all 256 q6 bytes with 300k random vx/T/H. t16 low is left = T instead of 24p and t16+1 is not written (both dead, see assumption). X is unchanged compared with the original. Assembled on both targets with zp symbols: 51->31 bytes (Master), 52->32 (Model B). Follow-on: the ground block shrinks by 20 bytes, so @air may come within branch range of the dispatch at 1435-1437 (bne :+ / jmp @air / : -> beq @air). That dispatch belongs to an earlier window.

Review: The original text matches lines 1473-1486. I simulated all 256 q6 values against random vx/T/H: the fused A:Y sum equals the two add16s done one after the other. Y is dead: the original already clobbers it (2vx high on this path, the tile id on the others), getaltitude/getinfo do tay before any read, and player_update is reached by a tail jmp. t16 is dead after @nofric: lines 1533-1538 write it before 1542/1546 read it, and later uses write first. vx, q6 and t16 are zero page. The anonymous-label count goes up by 1 (sx16's one becomes two). No :+/:- crosses 1473-1486: the references at 1426-1436 end at 1438 and the next is at 1524. Bytes: 51->31 on the Master, 52->32 on the Model B. Every path is faster: 46-50 cycles against 76-77.

## r036b:1  src/logic.s:1488-1501  (window i1184)  M -4 B -4  [high]  review: accept

Cycles: Original 43 cy, new 37 cy (straight line, both targets).

Assumption: t16 low is dead after the air path: the original stored 4vx low there and the new code does not. The only reads after @nofric come after the run path writes t16 (logic.s:1533-1538), and every later use writes first (1810+, 1906+). vx and t16 are zero page. No register other than A is touched, as in the original.

```
        lda vx
        asl
        sta t16
        lda vx+1
        rol
        sta t16+1                   ; t16 = 2vx
        asl t16
        rol t16+1                   ; t16 = 4vx
        clc
        lda t16
        adc vx
        sta vx                      ; 5vx low, straight into vx: no add16 at the end
        lda t16+1
        adc vx+1                    ; A = 5vx high (vx+1 not written yet)
```
->
```
        lda vx+1
        sta t16+1
        lda vx
        asl
        rol t16+1
        asl
        rol t16+1                   ; A:t16+1 = 4vx
        clc
        adc vx
        sta vx                      ; 5vx low, straight into vx: no add16 at the end
        lda t16+1
        adc vx+1                    ; A = 5vx high (vx+1 not written yet)
```

The original builds t16 = 4vx (16-bit) in memory and then adds vx. The new code keeps the low byte in A: t16+1 = vx+1; A = vx; asl/rol t16+1 twice gives A:t16+1 = 4vx mod 2^16 with the same carries. clc; adc vx; sta vx gives the 5vx low byte as before. lda t16+1; adc vx+1 gives the 5vx high byte in A with the same carry, feeding the unchanged asr3 at 1502. Checked exhaustively over all 65536 vx. Assembled: 25 -> 21 bytes on both targets.

Review: The original text matches lines 1488-1501. Checked exhaustively over all 65536 vx: after the two asl/rol pairs, A:t16+1 = 4vx, and adding vx stores the same 5vx low byte and leaves the same high byte in A, with the same carry into the asr3 at 1502. The t16 low byte is no longer written, and it is dead after @nofric. 25->21 bytes on both targets, 43->37 cycles.

## r036b:2  src/logic.s:1533-1536  (window i1211)  M -0 B -3  [high]  review: accept

Cycles: Master: 2+3+3+3 = 11 -> 3+2+3+3 = 11 (same). Model B: 2+3+12 (pha/lda/sta/pla)+3 (jmp) = 20 -> 2+3+2+3+3 = 13.

Assumption: A is dead at the stz: lda #36 overwrites it immediately, and @acc starts with txa (logic.s:1539). The only other entry, @acc288, loads A itself. stz leaves X (keys) alone on both targets. @acc is 8 bytes past the bne (mov16i sits between), so it is in range.

```
        lda #36
        sta t16
        stza t16+1                  ; was lda #0 / sta t16+1
        bra @acc
```
->
```
        stz t16+1                   ; A dead: reloaded below and at @acc (txa)
        lda #36
        sta t16
        bne @acc                    ; Z = 0 from lda #36: always
```

stza (Model B pha/lda #0/sta/pla, 6 bytes) exists only to preserve A, but A is dead here, so the plain stz macro does the job (Model B lda #0/sta, 4 bytes; Master stz, 2 bytes as before). Moving it before lda #36 makes lda #36 (Z=0) the last flag setter on both targets: the Master's stz sets no flags, and the Model B's lda #0 is followed by lda #36. So bra @acc (Model B jmp, 3 bytes) becomes bne @acc (2 bytes, 3 cycles taken, always taken). t16 = 36 and t16+1 = 0 as before. Assembled over the span including @acc288: Master 17->17, Model B 22->19.

Review: The original text matches lines 1533-1536. A is dead at the stz: lda #36 follows it and @acc starts with txa. lda #36 is the last instruction to set flags before the bne, and it leaves Z=0, so the bne is always taken. @acc is 8 bytes ahead, within range. Master: 8->8 bytes, 11->11 cycles. Model B: 13->10 bytes, 20->13 cycles. There are no labels inside the span, and the beq @acc288 at 1532 is unaffected.

## r036c:0  src/logic.s:1525-1525  (window i1216)  M -0 B -2  [high]  review: accept

Cycles: Master: stz zp 3 cycles becomes sta zp 3 (the same). Model B: lda #0 / sta, 5 cycles, becomes sta, 3.

Assumption: A = 0 here. The only way in is fall-through from `lda running / ora firing / bne :+` with bne not taken, so A = 0. A and the flags are dead afterwards: the next instruction (after the ':' label) is `lda alt`.

```
        stz anim
```
->
```
        sta anim                    ; A = running|firing = 0
```

The only entry is bne not taken, so A = running|firing = 0. Storing A writes the same 0 that stz writes. The line has no label.

Review: Original text matches line 1525. The only way in is fall-through from bne at 1524, so A = running|firing = 0. The line has no label and the anonymous-label count is unchanged. anim is zp. The next instruction, lda alt, sets A and the flags. Model B: lda #0/sta zp (4 bytes, 5 cycles) becomes sta zp (2 bytes, 3 cycles). Master: stz zp becomes sta zp, 2 bytes and 3 cycles either way.

## r036c:1  src/logic.s:1533-1538  (window i1216)  M -2 B -5  [high]  review: accept

Cycles: 36 path: Master lda2+sta3+stz3+bra3 = 11 becomes stz3+lda2+bne3+sta3 = 11. Model B 2+3+12+3 = 20 becomes 5+2+3+3 = 13. 288 path: lda2 sta3 lda2 sta3 = 10, unchanged on both.

Assumption: A is dead at `stz t16+1` (the Model B expansion loads 0 into A): the next instruction reloads A with #36. A is dead after the block because @acc starts with `txa`. X (the key bits from `tax`) is not touched. t16 is plain zp scratch ($D1); only this code writes it here, and the order of its two stores does not matter.

```
        lda #36
        sta t16
        stza t16+1                  ; was lda #0 / sta t16+1
        bra @acc
@acc288:
        mov16i t16, 288
```
->
```
        stz t16+1                   ; A dead: loaded next
        lda #36
        bne @acclo                  ; Z = 0 from the load
@acc288:
        lda #>288
        sta t16+1
        lda #<288
@acclo: sta t16
```

Both paths give the same final values: t16 = $0024 or $0120. The store order is swapped, which is safe for zp scratch. `bne @acclo` is always taken because lda #36 sets Z=0 on both targets (on the Master stz does not touch flags; on the Model B the expansion's lda #0 comes before lda #36). The 288 path shares the low-byte store through @acclo. The new cheap label @acclo does not end the @ scope. @acc stays in place: it is now unreferenced, which is harmless. A scratch copy assembles for both targets with the sizes stated.

Review: Original text matches lines 1533-1538. A is dead after stz t16+1 because lda #36 follows, and the original stza only kept A alive over an unneeded stretch: @acc begins with txa. lda #36 gives Z=0 on both targets, so bne @acclo is always taken. The 288 path gives t16=$0120 with the two stores in the other order; t16 is zp scratch ($D1). bpl/beq @acc288 still reach it. @acc is left unreferenced, which is harmless. X is not touched. Cycles: 36 path Master 11->11, Model B 20->13; 288 path 10->10 on both. Assembled in a scratch copy.

## r036c:2  src/logic.s:1540-1551  (window i1225)  M -4 B -9  [high]  review: accept

Cycles: Left path: Master 2+2+2+20+2+3+3+2+3+3 = 42 becomes txa2 and2 sta3 beq2 sub20 bra3 lda2 sta3 bne3 = 40. Model B 42 becomes 40. Right path: Master txa2 and2 beq3 add20 stz3 lda2 sta3 bra3 = 38 becomes 2+2+3+3+20+2+3+3 = 38. Model B 40 becomes 38. Norun path: Master stz 3, unchanged. Model B stza 12 becomes stz 5.

Assumption: X = keys & 3 is 1 or 2 at the tax on line 1521, because the 0 and 3 cases have already branched to @norun. So A & K_LEFT is 1 for left and 0 for right, the same values the old code stored to facing. facing must be 0 or 1 (it is read by `ora facing` at 1766 and by lda/beq at 901, 1142 and 1644), and it still is. A is dead at @norun: all three entries are `beq @norun`, and @hmove begins with `lda vx`. I grepped @run, @right, @hmove and @norun: they are referenced only at lines 1515-1552.

```
        and #K_LEFT
        beq @right
        sub16 vx, t16
        lda #1
        sta facing
        bra @run
@right: add16 vx, t16
        stz facing
@run:   lda #1
        sta running
        bra @hmove
@norun: stza running
```
->
```
        and #K_LEFT                 ; A = 1 left, 0 right: the facing flag
        sta facing
        beq @right
        sub16 vx, t16
        bra @run
@right: add16 vx, t16
@run:   lda #1
        sta running
        bne @hmove                  ; Z = 0 from the load
@norun: stz running                 ; A dead: @hmove reloads it
```

facing is now stored from the and result (1 or 0) before the split. sta keeps the Z flag from the and, so beq still works. The left path no longer reloads #1 for facing and still jumps to @run. The right path no longer needs stz facing and falls into @run. At @run, lda #1 sets Z=0, so bne @hmove is always taken; it replaces bra and is 2 bytes on the Model B instead of jmp's 3. The stza at @norun (which keeps A) becomes stz because A is dead. running and facing get the same values; only the order of those stores relative to the vx update changes, and they are zp game state that the ISR does not read. Checked by assembling a scratch copy with all five candidates: on the Model B @hl moves from $10F5 to $10E2, on the Master from $0C07 to $0C01.

Review: Original text matches lines 1540-1551. K_LEFT = 1 (engine.s:190), so and #K_LEFT gives 1 or 0, the same values as before, and facing (zp) holds 1 = left. sta does not change flags, so beq still tests the and. lda #1 sets Z=0, so bne @hmove is always taken. @norun is reached only by the three beq at 1515/1518/1520, and @hmove starts with lda vx, so A is dead and stz can replace stza. facing and running are not read in any ISR, and sub16/add16 do not touch them. Cycles: left 40->38 on both; right Master 36->36, Model B 38->36; norun Model B 12->5. Assembled in a scratch copy.

## r036c:3  src/logic.s:1560-1560  (window i1232)  M -0 B -2  [high]  review: accept

Cycles: Master: stz zp 3 becomes sta zp 3. Model B: 5 becomes 3. jmp @hdone follows in both versions.

Assumption: The only way in is `sta dpx / bne :+` with bne not taken, so A = 0. sta leaves Z as the adc set it. @hdone begins with `inc anim / lda firing`, so neither A nor the flags are used.

```
        stz dpx+1                   ; dpx = 0, so its sign extension is 0 too
```
->
```
        sta dpx+1                   ; A = dpx = 0
```

A = 0 whenever this line runs, so sta A stores the same 0 as stz.

Review: Original text matches line 1560. The only entry is bne not taken after sta dpx, so A = 0. On the Model B the flags are the same as the stz expansion's (Z=1, N=0, C not touched). jmp @hdone follows, and @hdone begins with inc anim / lda firing. Model B saves 2 bytes and 2 cycles; the Master is unchanged.

## r036c:4  src/logic.s:1566-1566  (window i1236)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 becomes bcc taken 3. Model B: jmp 3 becomes bcc taken 3.

Assumption: C = 0 here. C comes from `adc #0` at 1557, which carries out only when vx+1 = $FF and C = 1. That makes A = 0, and A = 0 takes the other path (bne :+ not taken, then jmp @hdone). On this path the and/beq/lda/sta in between leave C unchanged. @hstep is 23 bytes ahead, so it is in branch range.

```
        bra @hstep                  ; qx = px and qy = py+16 already, set at @push
```
->
```
        bcc @hstep                  ; C = 0: adc #0 carried out only when A became 0
```

The branch is always taken, so it behaves exactly like bra. bcc does not change flags, so the bmi at @hstep still sees N from and #$80 / lda #$FF as before.

Review: Original text matches line 1566. adc #0 at 1557 carries out only when A becomes 0, and A = 0 takes the jmp @hdone path. On the A != 0 path, sta/bne/and/beq/lda/sta leave C = 0, so bcc is always taken. No label sits on the line. The listing shows 90 17 in range. bmi at @hstep still sees N from the and/lda. Model B jmp 3 bytes / 3 cycles becomes bcc 2 bytes / 3 cycles; the Master is unchanged.

## r036d:0  src/logic.s:1559-1627  (window i1262)  M -31 B -37  [medium]  review: accept

Cycles: dpx = 0: bne 2 + stz 3/5 + jmp 3 = 8/10 -> beq 3. First step: bne taken 3 ... bra @hstep 3 -> beq not taken 2 ... fall through (4 fewer). Loop back: beq nt 2 + jmp 3 + mov16 12 + qy 18 + lda 3 = 38 -> beq nt 2 + qy 18 + lda 3 + bra 3 = 26 (Model B: jmp for bra, same). @hget..@hmoved: >= 19 -> 14 (see E2). alt = 0 tail on the Model B 15 -> 8. Exits (beq @hdone taken, bne @wall) unchanged. No path slower on either target.

Assumption: The union of dpx1, hlmov, E2 and alt (see those: dpx dead after @hdone; qx == px at @hl; X dead after getaltitude; q1 dead after the loop; A dead at @hnext), plus: the beq @hdone reaches (assembled: offset $6C Master, $76 Model B, only because the rest of this rewrite shrinks the loop), and bra @hstep backwards is in range on the Master (assembled offset $A0 = -96; checked by building both targets with ca65 in a scratch copy: M -31, B -37 bytes). Spans more than one 32-instruction window (i1262..i1321); submitted as one edit because the move and the beq only work together with the rest. If it fails review, dpx1, hlmov, E2/E1/E and alt are the separate fallbacks.

```
        bne :+
        stz dpx+1                   ; dpx = 0, so its sign extension is 0 too
        jmp @hdone
:       and #$80
        beq :+
        lda #$FF
:       sta dpx+1
        bra @hstep                  ; qx = px and qy = py+16 already, set at @push
@hl:    mov16 qx, px
        clc
        lda py
        adc #16
        sta qy
        lda py+1
        adc #0
        sta qy+1
        lda dpx+1
@hstep: bmi @hneg
        inc qx
        bne @hget
        inc qx+1
        bra @hget
@hneg:  lda qx
        bne :+
        dec qx+1
:       dec qx
@hget:  jsr getaltitude
        sta q1
        bpl @hok
        cmp #$FF
        bne @wall
@hok:   ; px += dir
        lda dpx+1
        bmi @hdn
        inc px
        bne @hmoved
        inc px+1
        bra @hmoved
@hdn:   lda px
        bne :+
        dec px+1
:       dec px
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        lda q1
        bmi @stepn
        cmp #2
        bcs @alt
        adc py                      ; C is clear from the cmp: 0 or 1, addend high byte 0
        sta py
        bcc :+
        inc py+1
:       stza alt
        bra @hnext
@stepn: clc                         ; negative: high byte of the addend is $FF
        adc py
        sta py
        bcs :-                      ; no borrow (255 times in 256): py+1 is unchanged,
        dec py+1                    ; so join the 0/1 case's tail instead of adding $FF
        bra :-
@alt:   sta alt
@hnext: ; steps -= dir
        lda dpx+1
        bmi :+
        dec dpx
        bra :++
:       inc dpx
:       beq @hdone      ; Z survives from the inc/dec
        jmp @hl
```
->
```
        beq @hdone                  ; dpx = 0: dpx+1 is dead after @hdone (vy_step rewrites dpx)
        and #$80
        beq :+
        lda #$FF
:       sta dpx+1                   ; qx = px and qy = py+16 already, set at @push
@hstep: bmi @hneg
        inc qx
        bne @hget
        inc qx+1
        bra @hget
@hneg:  lda qx
        bne :+
        dec qx+1
:       dec qx
@hget:  jsr getaltitude
        bpl @hok                    ; N from getaltitude's closing sbc
        cmp #$FF
        bne @wall
@hok:   ldx qx                      ; px += dir: qx is px + dir already
        stx px                      ; (X is free: A still holds the altitude)
        ldx qx+1
        stx px+1
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        tax                         ; N from the altitude
        bmi @stepn
        cmp #2
        bcs @alt
        adc py                      ; C is clear from the cmp: 0 or 1, addend high byte 0
        sta py
        bcc :+
        inc py+1
:       stz alt                     ; A is dead: @hnext reloads it
  .if ::MODELB
        beq @hnext                  ; the 6502 stz is lda #0 / sta: Z = 1
  .else
        bra @hnext
  .endif
@stepn: clc                         ; negative: high byte of the addend is $FF
        adc py
        sta py
        bcs :-                      ; no borrow (255 times in 256): py+1 is unchanged,
        dec py+1                    ; so join the 0/1 case's tail instead of adding $FF
        bra :-
@alt:   sta alt
@hnext: ; steps -= dir
        lda dpx+1
        bmi :+
        dec dpx
        bra :++
:       inc dpx
:       beq @hdone      ; Z survives from the inc/dec
@hl:    clc                         ; qx = px already: @hok set px to it
        lda py
        adc #16
        sta qy
        lda py+1
        adc #0
        sta qy+1
        lda dpx+1
        bra @hstep
```

(1) bne :+ / stz dpx+1 / jmp @hdone -> beq @hdone: Z is from adc #0 (sta leaves it), dpx+1 is dead on that path (dpx1). (2) The @hl block, minus its redundant mov16 (hlmov), moves from between 'bra @hstep' and @hstep to after ': beq @hdone', replacing 'jmp @hl' and ending in 'bra @hstep': the first entry now falls into @hstep with the same N (from and #$80 / lda #$FF), and the loop path runs the same instructions as before (qy = py + 16, lda dpx+1, then @hstep). (3) sta q1 dropped, px = qx via X, lda q1 -> tax (E2). (4) stza alt / bra -> stz alt with beq on the Model B (alt). Every branch target other than those named is textually unchanged.

Review: Verified by assembling both targets in a scratch copy: -31 Master, -37 Model B, and every branch is in range (beq @hdone +$76 on the Model B; bcs :- still lands on the stz alt line). Z at the beq comes from adc #0. dpx is dead after @hdone: vy_step does mov16 dpx, vy before any read, and bdiff does not compare dpx. N reaches @hstep correctly on both ways in (from and/lda before the fall-through, and from lda dpx+1 before bra @hstep). qx == px at @hl because getaltitude/getinfo/tilexy/maptile/maprow/mapbyte/pagelogic never write qx. X is dead from @hok: the loop reads no X before jsr getaltitude, and after @hdone X is written (ldx vx+1 at @noctl, tax at @spr) before any read. q1 is dead: every later reader writes it first. A is dead at @hnext. The Model B stz leaves Z=1, so beq @hnext is always taken. No path is slower. Overlaps 1-4, 7 and 8: apply this one OR those.

## r036d:1  src/logic.s:1586-1603  (window i1268)  M -15 B -16  [medium]  review: accept

Cycles: as E1, and the sta q1 (3) goes too: old >= 19, new 14 on every path, both targets.

Assumption: as E1, plus q1 ($C3) is dead after this loop: in player_update the only reader is the lda q1 this removes (grep q1 in logic.s: 1586, 1603 only between 1300 and 2200); player_update is the tail of the logic step (jmp player_update, logic.s:1157), and every other routine that reads q1 writes it first (star_safe/tramp_safe via their callers 2207/2296, div10/div10_16 write it, 525, 2579, 2606, 2629, 2749, 2864, 3073; menu.s:676 after div10_16). getaltitude ends every path with sbc q5 / rts (logic.s:397-399, 424-426), so N at the bpl reflects A without the sta (which never changed flags anyway).

```
        sta q1
        bpl @hok
        cmp #$FF
        bne @wall
@hok:   ; px += dir
        lda dpx+1
        bmi @hdn
        inc px
        bne @hmoved
        inc px+1
        bra @hmoved
@hdn:   lda px
        bne :+
        dec px+1
:       dec px
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        lda q1
```
->
```
        bpl @hok                    ; N from getaltitude's closing sbc
        cmp #$FF
        bne @wall
@hok:   ldx qx                      ; px += dir: qx is px + dir already
        stx px                      ; (X is free: A still holds the altitude)
        ldx qx+1
        stx px+1
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        tax                         ; N from the altitude
```

sta q1 only kept A for the lda q1 at @hmoved; A itself now survives to there (X does the copy), so the store is dead. bpl @hok / cmp #$FF / bne @wall see the same flags and A. Then as E1.

Review: Measured 15/16. sta q1 never changed flags, so bpl/cmp/bne see the same N and A. q1 ($C3) is written before it is read by every other reader (525, 2207/2296 before star_safe/tramp_safe, 2579/2606/2629/2749/2864/3073, div10/div10_16), getaltitude does not use q1, and bdiff does not compare it. X is dead, as in candidate 2. Subsumes candidates 2 and 3.

## r036d:2  src/logic.s:1590-1603  (window i1268)  M -13 B -14  [high]  review: accept

Cycles: old: @hok 13/17/20/21 (see E) + lda q1 3 = 16..24; new: ldx/stx/ldx/stx 12 + tax 2 = 14, both targets.

Assumption: as E (qx = px + dir at @hok), plus X is dead from @hok on: getaltitude has just returned it scrambled; nothing up to the next jsr getaltitude or up to @hdone reads X (@hmoved..@hnext, @hl, @hstep use A and memory only; tilexy writes X before any read and its @out path reads none), and after @hdone X is written before it is read on every path (tax at logic.s:1681/1698, ldx vx+1 at 1710, tax at @spr 1767; @ctl..@sprf and @bdir/@animdone use no X). A = q1 on arrival at @hok: both ways in are sta q1 / bpl, or sta q1 / cmp #$FF / bne not taken.

```
@hok:   ; px += dir
        lda dpx+1
        bmi @hdn
        inc px
        bne @hmoved
        inc px+1
        bra @hmoved
@hdn:   lda px
        bne :+
        dec px+1
:       dec px
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        lda q1
```
->
```
@hok:   ldx qx                      ; px += dir: qx is px + dir already
        stx px                      ; (X is free: A still holds the altitude)
        ldx qx+1
        stx px+1
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        tax                         ; N from the altitude
```

px = qx as in E, but through X so A still holds the altitude (sta q1 left it in A; cmp does not change A). tax then sets N and Z from A exactly as lda q1 did (same value), and bmi @stepn / cmp #2 / adc py / @alt: sta alt all use A as before. X is left holding the altitude instead of garbage; nobody reads it. q1 is still stored.

Review: Measured 13/14. On both ways into @hok, A equals q1. getaltitude does not write qx (only qy), and qx = px + dir because it was px at @push or @hl (gettileattr does not touch it) and @hstep applied the same 16-bit inc/dec. X is dead: nothing reads X before the next jsr getaltitude or after @hdone before ldx/tax writes it (the bmi16/ble16i/bge16i macros do not read X). tax sets N and Z as lda q1 did. The removed ':' at 1600 had only the bne :+ at 1598 referring to it.

## r036d:3  src/logic.s:1590-1600  (window i1267)  M -12 B -13  [high]  review: accept

Cycles: old: pos 13 (lda 3, bmi 2, inc 5, bne 3), pos with carry 20, neg 17 (lda 3, bmi 3, lda px 3, bne 3, dec 5), neg with borrow 21; Model B the same (the bra is a 3-cycle jmp). new: 12 on every path, both targets.

Assumption: qx = px + dir (16-bit) on arrival at @hok: qx equalled px before @hstep (set at @push, logic.s:1411, or at @hl), @hstep added dir to qx with the same 16-bit inc/dec the old code applies to px, and getaltitude does not write qx (it only reads it: logic.s:311-427; its helpers maprow/mapbyte/pagelogic, engine.s:4513-4560 and 4734, touch mapptr, ROMSEL and the stack). The flags and A after @hok are dead: @hmoved begins lda q1. X and Y untouched.

```
@hok:   ; px += dir
        lda dpx+1
        bmi @hdn
        inc px
        bne @hmoved
        inc px+1
        bra @hmoved
@hdn:   lda px
        bne :+
        dec px+1
:       dec px
```
->
```
@hok:   mov16 px, qx                ; px += dir: qx is px + dir already
```

The old block adds dir (sign of dpx+1) to px as a 16-bit inc or dec; the new one copies qx, which is px + dir computed by the identical inc/dec at @hstep, so px ends with the same value. @hdn and the anonymous label go with the dec path; @hmoved is kept as the fall-through target (no other references to @hdn).

Review: Measured 12/13. qx == px + dir at @hok (getaltitude and its helpers only read qx; pagelogic keeps A/X/Y/C), and @hmoved reloads A with lda q1, so A and the flags after the copy are dead. Every path now takes 12 cycles, against 13-21. The anonymous-label removal is self-contained.

## r036d:4  src/logic.s:1567-1567  (window i1237)  M -8 B -8  [high]  review: accept

Cycles: every loop iteration after the first: 12 cycles fewer (the mov16), both targets; nothing else changes.

Assumption: At every jmp @hl, qx == px already: the step just taken set qx = px +/- 1 at @hstep (qx was px at loop entry: @push sets it, logic.s:1411, and nothing between @push and @hmove writes qx or px; or @hl set it), getaltitude/getinfo/tilexy/maptile/mapbyte/pagelogic only read qx (logic.s:311-427, engine.s:4513-4560, 4734), then @hok moves px by the same +/- 1, and @hmoved..@hnext write only py, alt, dpx. Nothing else writes $BE/$BF (grep: the only other writers are mov16 qx at logic.s:1112/1131/1316/1379/1411/1878/3075, none reachable inside the loop). Lowest window is i1237 (region r036c): may duplicate.

```
@hl:    mov16 qx, px
```
->
```
@hl:                                ; qx = px already: @hok set px to it
```

mov16 qx, px stores values qx already holds, so removing it leaves memory unchanged; its A is overwritten by lda py two instructions later and its flags by the lda/adc that follow. The label @hl stays on the next instruction (clc). No anonymous labels involved.

Review: Measured 8/8. When jmp @hl runs, qx == px: both started equal (set at @push; gettileattr does not write qx) and were moved by the same +/-1, and nothing else in the loop writes either. The mov16's A and flags are overwritten by clc/lda py.

## r036d:5  src/logic.s:1539-1547  (window i1221)  M -4 B -6  [high]  review: accept

Cycles: left path: lda #1 / sta facing 5 -> sta facing 3; right path: stz facing 3 (Master) / 5 (Model B) -> 3; the beq and everything else unchanged. Faster on both targets, every path.

Assumption: K_LEFT = 1 (src/engine.s:190), so A & K_LEFT is exactly the 1/0 facing wants (logic.s:224 '1 = left'); every reader of facing (logic.s:901, 1142, 1644, 1766 ora facing) sees the same value as before. Lowest window is i1221 (region r036c): may duplicate a candidate from there.

```
@acc:   txa
        and #K_LEFT
        beq @right
        sub16 vx, t16
        lda #1
        sta facing
        bra @run
@right: add16 vx, t16
        stz facing
```
->
```
@acc:   txa
        and #K_LEFT
        sta facing                  ; K_LEFT = 1: facing = 1 left, 0 right
        beq @right
        sub16 vx, t16
        bra @run
@right: add16 vx, t16
```

After and #K_LEFT, A is 1 when left is pressed and 0 otherwise, and Z is set from it. sta facing stores exactly the value the left path stored (lda #1) and the right path stored (stz), and sta does not touch Z, so beq @right still branches on the same flag. The two stores in the arms are removed. sub16/add16 do not read facing or A on entry (they load vx). No labels change.

Review: Measured 4/6. K_LEFT = 1 (engine.s:190), so after and #K_LEFT, A is exactly 1 (left) or 0 (right), the values facing held before. sta does not touch Z, so beq @right is unchanged. sub16/add16 do not read facing. The left path is 2 cycles faster, and the right path is the same on the Master and 2 faster on the Model B. bdiff compares facing, and the values are the same.

## r036d:6  src/logic.s:1548-1551  (window i1225)  M -0 B -5  [high]  review: accept

Cycles: Model B: run path lda #1 2 + sta 3 + jmp 3 = 8 -> lda #1 2 + bne taken 3 + sta 3 = 8; norun path stza (pha 3, lda 2, sta 3, pla 4) = 12 -> lda #0 2 + sta 3 = 5. Master: the .else arm is the original code (stza on the Master is stz), identical bytes and cycles.

Assumption: A and the flags are dead at @hmove: it starts with lda vx (logic.s:1554), and every path to @norun (the three beq @norun after @nofric) and to @run ends in @hmove. Lowest window is i1225 (region r036c): may duplicate.

```
@run:   lda #1
        sta running
        bra @hmove
@norun: stza running
```
->
```
@run:   lda #1
  .if ::MODELB
        bne @strun                  ; A = 1
@norun: lda #0                      ; A is dead: @hmove reloads it
@strun: sta running
  .else
        sta running
        bra @hmove
@norun: stz running                 ; A is dead: @hmove reloads it
  .endif
```

Model B arm: on the run path A = 1 so bne is always taken, landing on the shared sta running (stores 1, as before), then falls into @hmove as the jmp did. On the norun path lda #0 / sta running stores the 0 stza stored; stza only preserved A, which @hmove overwrites at once. @strun is a new cheap label inside player_update's scope, not a normal label, so no @ scope ends. Master arm is textually the original apart from stza -> stz, which assemble identically on the 65C02.

Review: Measured 0/5 (the Master arm assembles to the original bytes). On the Model B the run path is 8 cycles either way, and norun drops from 12 to 5. @hmove begins lda vx, so A and the flags are dead. @norun has only the three beq @norun references. ::MODELB is the spelling cpu.inc uses.

## r036d:7  src/logic.s:1560-1560  (window i1232)  M -2 B -4  [high]  review: accept

Cycles: dpx = 0 path: stz dpx+1 3 (Master) / 5 (Model B) removed; nothing else changes.

Assumption: dpx ($D5/$D6, logic.s:288) is named only in logic.s (grep over src/ and modelb/src: no other file names dpx, $D5 or $D6). After @hdone nothing in player_update reads dpx (grep: no dpx reference after logic.s:1627), and every other reader writes it first: vy_step starts with mov16 dpx, vy (logic.s:1288), player_dead and player_update read dpx only after jsr vy_step (logic.s:1227-1228, 1365-1378). Lowest window is i1232 (region r036c): may duplicate.

```
        stz dpx+1                   ; dpx = 0, so its sign extension is 0 too
```
->
```
                                    ; (dpx is dead after @hdone: vy_step rewrites it)
```

On the dpx = 0 path the code jumps straight to @hdone, so the loop that reads dpx+1 (@hstep via the flags, @hok, @hnext, @hl) never runs; the store is dead. The flags are not consumed (jmp @hdone, then inc anim / lda firing). A is not changed on the Master (stz); on the Model B the lda #0 of the stz expansion is dropped, and A (already 0 here, since bne was not taken) is not read before lda firing.

Review: Measured 2/4. On the dpx = 0 path the code jumps to @hdone, and nothing after @hdone reads dpx. Every other reader runs after vy_step, which writes dpx first (player_dead 1227-1228, player_update 1365-1378). A and the flags are not consumed: inc anim / lda firing come next. This overlaps candidate 0.

## r036d:8  src/logic.s:1611-1612  (window i1276)  M -0 B -3  [high]  review: accept

Cycles: Model B: stza 12 + jmp 3 = 15 -> lda #0 2 + sta 3 + beq taken 3 = 8. Master: stz 3 + bra 3, unchanged (stza is stz on the 65C02).

Assumption: A is dead at @hnext (it begins lda dpx+1, logic.s:1621), so stza's preservation of A is not needed; the Model B stz macro is lda #0 / sta (cpu.inc), which leaves Z = 1.

```
:       stza alt
        bra @hnext
```
->
```
:       stz alt                     ; A is dead: @hnext reloads it
  .if ::MODELB
        beq @hnext                  ; the 6502 stz is lda #0 / sta: Z = 1
  .else
        bra @hnext
  .endif
```

The ':' line keeps its label (bcc :+ at 1609 and bcs :- / bra :- at 1616/1618 still reach it). stz stores the same 0. On the Model B Z = 1 after lda #0 (sta does not change it), so beq @hnext is always taken, like the jmp. On the Master both arms assemble to the original bytes.

Review: Measured 0/3. A is dead at @hnext (lda dpx+1). On the Model B, stz is lda #0 / sta, which leaves Z=1, so beq is always taken (8 cycles, against 15). The ':' stays on the stz line, and bcs :- / bra :- still reach it (checked in the listing). The Master bytes are unchanged.

## r037:0  src/logic.s:1567-1603  (window i1280)  M -22 B -23  [high]  review: accept

Cycles: non-wall step: old sta q1 3 + bpl + px step 13..22 + lda q1 3 -> new ldy/sty x2 12 + ora #0 2 (>= 4 cycles faster); wall path 10->7; each loop pass also loses @hl's mov16 (12 cycles). Model B identical (no macros involved).

Assumption: (1) getaltitude (logic.s:371-426) and everything it calls (getinfo, tilexy, maptile, maprow, mapbyte, pagelogic) never write qx: grep shows qx written only at 1112(DBGTILE),1131,1316,1379,1411,1567,1577-1584,1878,3075; so at @hok qx = px + dir exactly (qx = px at @hl and at the first entry via bra @hstep, set at @push 1411 with nothing writing qx/px between). (2) @hl is reached only from the loop end (jmp @hl, 1627), which is reached only through @hok, and nothing between @hok and it writes px or qx (py, alt, dpx only) -> qx = px at @hl, so its mov16 qx,px is redundant. (3) Y dead: after the loop player_update (logic.s:1628-1927) never reads Y before writing it (no ,y / tya / sty / cpy); it returns to game_frame's tail and frame_top (game.s:296-303), which do not read Y; getaltitude already leaves Y garbage here. (4) q1 ($C3) has no reader after this: every other q1 read (logic.s 561..751, 2209, 2237, 2258, 2298, 2584, 2614, 2796, 2869, 2880, 3080, 3250, 3281, 3291; menu.s 676) follows a write in its own routine; no other symbol aliases $C3. (5) getaltitude returns with N,Z from A (both exits end in sbc q5), which the bpl already relied on.

```
@hl:    mov16 qx, px
        clc
        lda py
        adc #16
        sta qy
        lda py+1
        adc #0
        sta qy+1
        lda dpx+1
@hstep: bmi @hneg
        inc qx
        bne @hget
        inc qx+1
        bra @hget
@hneg:  lda qx
        bne :+
        dec qx+1
:       dec qx
@hget:  jsr getaltitude
        sta q1
        bpl @hok
        cmp #$FF
        bne @wall
@hok:   ; px += dir
        lda dpx+1
        bmi @hdn
        inc px
        bne @hmoved
        inc px+1
        bra @hmoved
@hdn:   lda px
        bne :+
        dec px+1
:       dec px
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        lda q1
```
->
```
@hl:                                ; qx = px already: @hok set px = qx
        clc
        lda py
        adc #16
        sta qy
        lda py+1
        adc #0
        sta qy+1
        lda dpx+1
@hstep: bmi @hneg
        inc qx
        bne @hget
        inc qx+1
        bra @hget
@hneg:  lda qx
        bne :+
        dec qx+1
:       dec qx
@hget:  jsr getaltitude
        bpl @hok
        cmp #$FF
        bne @wall
@hok:   ldy qx                      ; px += dir: qx is px + dir already (getaltitude
        sty px                      ; leaves qx alone); Y is dead, A is the altitude
        ldy qx+1
        sty px+1
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        ora #0                      ; N from the altitude (was lda q1)
```

@hl: mov16 qx,px deleted (qx = px on every entry, see assumption 2). sta q1 deleted: A keeps the altitude through bpl/cmp/bne/ldy/sty (none writes A), and bpl sees the same flags (sta never set them). The 10-instruction px +/- 1 (inc/dec with 16-bit carry) is replaced by px = qx, which is the same 16-bit value px + dir because qx was formed from px by exactly that inc/dec pair and nothing wrote it since. lda q1 -> ora #0: same A, same N/Z; C is untouched and first consumed after cmp #2 / clc as before.

Review: The original text matches lines 1567-1603. Nothing in getaltitude, getinfo, tilexy, maptile, maprow, mapbyte or pagelogic writes qx ($BE), and no other symbol is an alias of $BE. On first entry qx = px (set at @push 1411, with only gettileattr between). At every jmp @hl, qx = px too, because @hok now sets px = qx. So the mov16 at @hl is redundant, and px = qx gives exactly px+/-1. A survives bpl/cmp/bne/ldy/sty, and ora #0 gives the same N and Z as lda q1 with C untouched. Y is not an input to getaltitude, and after the loop it is written before it is read (addsprite's ldy; no Y reads through player_update's rts or frame_top). Every later reader of q1 is preceded by a write in its own routine. Only the ':' at 1600 is removed, together with its only reference. Bytes: 32/33 removed, 10 added, so 22/23 saved. Every path is faster.

## r037:1  src/logic.s:1586-1603  (window i1280)  M -14 B -15  [high]  review: accept

Cycles: non-wall: old 3 (sta) + 13..22 (step) + 3 (lda) -> 12 + 2; wall path 10 -> 7; Model B identical

Assumption: FALLBACK for P2b (same lines 1586-1603 without deleting @hl's mov16); goes stale once P2b applies. (1) getaltitude never writes qx and qx = px at @hl and at the first entry (@push, 1411) -> qx = px + dir at @hok. (2) Y dead: after the loop player_update (logic.s:1628-1927) never reads Y before writing it (no ,y / tya / sty / cpy); it returns to game_frame's tail and frame_top (game.s:296-303), which do not read Y; getaltitude already leaves Y garbage here. (3) q1 has no later reader (see P2b). (4) getaltitude's exit flags are from A.

```
        sta q1
        bpl @hok
        cmp #$FF
        bne @wall
@hok:   ; px += dir
        lda dpx+1
        bmi @hdn
        inc px
        bne @hmoved
        inc px+1
        bra @hmoved
@hdn:   lda px
        bne :+
        dec px+1
:       dec px
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        lda q1
```
->
```
        bpl @hok
        cmp #$FF
        bne @wall
@hok:   ldy qx                      ; px += dir: qx is px + dir already (getaltitude
        sty px                      ; leaves qx alone); Y is dead, A is the altitude
        ldy qx+1
        sty px+1
@hmoved:
        ; if a <= 1: py += a ; alt = 0 else alt = a   (a may be -1: step up a slope)
        ora #0                      ; N from the altitude (was lda q1)
```

as P2b without the @hl change: sta q1 dropped (A survives, flags unchanged), px = qx replaces the +/-1 step, lda q1 -> ora #0 (same A, N, Z; C untouched).

Review: Fallback for #0; the two cannot both apply. Same argument without the @hl change: 24/25 bytes removed, 10 added.

## r037:2  src/logic.s:1590-1601  (window i1280)  M -12 B -13  [high]  review: accept

Cycles: old: 13 (inc, no carry) / 21 (inc with carry) / 17 or 22 (dec) -> mov16 12 on every path; Model B identical (bra was a 3-cycle jmp)

Assumption: FALLBACK for P2b/P2 (A-only, keeps q1). getaltitude and its callees never write qx (grep: qx written only at 1112,1131,1316,1379,1411,1567,1577-1584,1878,3075) and qx = px at @hl and at the first entry via bra @hstep (@push 1411), so qx = px + dir here. A is dead: @hmoved reloads q1.

```
@hok:   ; px += dir
        lda dpx+1
        bmi @hdn
        inc px
        bne @hmoved
        inc px+1
        bra @hmoved
@hdn:   lda px
        bne :+
        dec px+1
:       dec px
@hmoved:
```
->
```
@hok:   mov16 px, qx                ; px += dir: qx is px + dir already (getaltitude
@hmoved:                            ; leaves qx alone)
```

px += dir with dir = sign(dpx+1) as inc/dec 16-bit == qx, which was computed from px by the same inc/dec pair at @hstep/@hneg and not written since. Flags after are not consumed (lda q1 follows).

Review: Fallback for #0 and #1; only one of the three can apply. mov16 px,qx (8 bytes, 12 cycles) replaces the step of 20/21 bytes and 13..22 cycles. qx = px+dir as argued in #0. A is dead because lda q1 follows.

## r037:3  src/logic.s:1579-1580  (window i1280)  M -0 B -1  [medium]  review: accept

Cycles: taken 3 -> 3 (Master bra / Model B jmp both 3); never falls through

Assumption: qx = px here (@hl / @push) and px is inside the map: px only changes in this loop (grep: writes at 1593-1600 and mov16 px,startx at 830/1241) and a probe outside the map is always a wall (tilexy @out -> getinfo returns 8 -> getaltitude returns $F8-(qy&7) in $F1..$F8, negative and not $FF -> bne @wall); maps are < 2048 px (tilexy: 'byte < 8'), so px+1 <= 7. startx is inside the map (level data).

```
        inc qx+1
        bra @hget
```
->
```
        inc qx+1
        bne @hget                   ; always: qx+1 <= 8 (px is inside the map, < 2048)
```

inc qx+1 runs when qx low wrapped; qx+1 <= 7 before, so the result is 1..8: Z = 0 and bne is always taken, exactly like bra/jmp.

Review: px stays in [0, mapw-1]: it changes only through the wall-checked step or from startx. A probe outside the map makes getaltitude return $F1..$F8, never $FF, so it is always a wall. After the inc, qx+1 is therefore 1..8 and never 0, so bne is always taken. Compatible with #0.

## r037:4  src/logic.s:1609-1627  (window i1280)  M -2 B -8  [high]  review: accept

Cycles: 0/1 path: after bcc/inc, Master stz 3 + bra 3 -> lda #0 2 + sta 3 (6->5), Model B stza 12 + jmp 3 -> 5; negative path: bcs 3 -> bcs 3 then 6->5 (Master) / 15->5 (B); dec py+1 path: bra/jmp 3 -> bcc 3; a>=2 (@alt) path unchanged; bmi @stepn stays a taken branch (3) and beq @hdone gets longer (still in range, assembled)

Assumption: A is dead after alt is written: @hnext starts with lda dpx+1. The relocated @stepn block sits after jmp @hl (unconditional), so nothing falls into it.

```
        bcc :+
        inc py+1
:       stza alt
        bra @hnext
@stepn: clc                         ; negative: high byte of the addend is $FF
        adc py
        sta py
        bcs :-                      ; no borrow (255 times in 256): py+1 is unchanged,
        dec py+1                    ; so join the 0/1 case's tail instead of adding $FF
        bra :-
@alt:   sta alt
@hnext: ; steps -= dir
        lda dpx+1
        bmi :+
        dec dpx
        bra :++
:       inc dpx
:       beq @hdone      ; Z survives from the inc/dec
        jmp @hl
```
->
```
        bcc @alt0
        inc py+1
@alt0:  lda #0                      ; alt = 0 (A is dead: @hnext reloads it)
@alt:   sta alt
@hnext: ; steps -= dir
        lda dpx+1
        bmi :+
        dec dpx
        bra :++
:       inc dpx
:       beq @hdone      ; Z survives from the inc/dec
        jmp @hl
@stepn: clc                         ; negative: high byte of the addend is $FF
        adc py
        sta py
        bcs @alt0                   ; no borrow (255 times in 256): py+1 is unchanged,
        dec py+1                    ; so join the 0/1 case's tail instead of adding $FF
        bcc @alt0                   ; always: C is clear, from the adc
```

The stz alt / bra @hnext pair becomes lda #0 falling into @alt: sta alt, which is alt = 0 and then @hnext. The negative-step block moves below jmp @hl; its two exits go to @alt0; dec py+1 does not touch C, which is clear because the bcs above was not taken, so bcc is the old bra.

Review: A is dead once alt is written (@hnext starts with lda dpx+1). The moved @stepn block sits after the unconditional jmp @hl, so nothing falls into it. dec py+1 is reached only when bcs was not taken, so C is clear and bcc behaves like the old bra. The ':' at 1611 becomes @alt0 and all three of its references are renamed; no other anonymous reference counts across it. bmi @stepn, beq @hdone and bcs @alt all stay in range.

## r037:5  src/logic.s:1623-1624  (window i1280)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both (bra/jmp vs bpl); never falls through

Assumption: on this path dpx+1 = 0, which @hmove sets only when dpx bit 7 is clear (logic.s:1562-1565) and dpx != 0 (1559); the loop exits at dpx = 0, so dpx is 1..$7F before this dec.

```
        dec dpx
        bra :++
```
->
```
        dec dpx
        bpl :++                     ; always: dpx was 1..$7F, so N is clear
```

dec dpx leaves 0..$7E, N = 0, so bpl is always taken to the same ':' (beq @hdone), and Z from the dec survives the branch as before.

Review: dpx+1 is 0 only when dpx has bit 7 clear and is nonzero at entry. The loop exits at 0 and nothing else writes $D5 inside it, so dpx is 1..$7F before the dec. N is then clear, bpl is always taken, and Z survives to the beq.

## r037:6  src/logic.s:1626-1627  (window i1280)  M -1 B -1  [high]  review: accept

Cycles: 3 -> 3 on both (jmp vs taken bne)

Assumption: @hl within branch range: assembled on both targets from the unmodified tree (and every other candidate only shrinks the loop).

```
:       beq @hdone      ; Z survives from the inc/dec
        jmp @hl
```
->
```
:       beq @hdone      ; Z survives from the inc/dec
        bne @hl                     ; always: Z is clear
```

reached only when beq @hdone was not taken, so Z = 0 and bne @hl is always taken.

Review: Z = 0 whenever beq @hdone falls through. Range checked on the unmodified listings: the Model B offset is exactly -128 ($1175 to $10F5) and the Master offset is -119. #0, #2 and #4 only shrink the loop.

## r037:7  src/logic.s:1628-1629  (window i1290)  M -0 B -2  [high]  review: accept

Cycles: Master unchanged; Model B 2+3+2+3 -> 2+3+3 (10 -> 8)

Assumption: A dead at @hdone: inc anim / lda firing follow; flags reset by inc anim.

```
@wall:  stz vx
        stz vx+1
```
->
```
@wall:
  .if MODELB
        lda #0                      ; one zero for both (A is dead: @hdone reloads)
        sta vx
        sta vx+1
  .else
        stz vx
        stz vx+1
  .endif
```

Model B's two stz expansions each load 0; one lda #0 serves both stores. Master keeps the stz pair.

Review: A is dead at @hdone (inc anim, then lda firing), and inc anim rewrites the flags. Model B goes from 8 bytes to 6; the Master is unchanged.

## r037:8  src/logic.s:1641-1650  (window i1308)  M -2 B -8  [high]  review: accept

Cycles: facing=0: Master 23 -> 23, Model B 31 -> 25; facing=1: Master 27 -> 24, Model B 35 -> 27

Assumption: X is dead: from here every path writes X before reading it (@animdone -> 1681 tax / @noctl ldx / @spr tax / CT's ldx running; nothing between reads X). facing is a zero-page byte (flags only used by the beq).

```
        stz bvx
        lda #>3584
        sta bvx+1
        lda facing
        beq @bdir
        lda #>(-3584)
        sta bvx+1
@bdir:  stz bvy
        stz bvy+1
        stz bcnt
```
->
```
  .if MODELB
        lda #0                      ; one zero for the four clears
        sta bvx
        sta bvy
        sta bvy+1
        sta bcnt
  .else
        stz bvx
        stz bvy
        stz bvy+1
        stz bcnt
  .endif
        lda #>3584
        ldx facing                  ; X is dead (written before any read below)
        beq @bdir
        lda #>(-3584)
@bdir:  sta bvx+1
```

bvx, bvy, bvy+1, bcnt get 0 (Model B from one lda #0; Master four stz). bvx+1 = facing ? >(-3584) : >3584, now selected in A with the test on X (ldx facing sets Z like lda facing) and stored once. Store order changes; nothing reads these in between.

Review: X is dead: every path from here writes X before reading it (tax at 1681 or 1700, ldx at @noctl, tax at @spr), and nothing in @ctl or @nofire reads X. facing and bvx..bcnt are zero page. The order of the stores does not matter. Master 20 to 18 bytes, Model B 28 to 20. Cycles never go up.

## r037:9  src/logic.s:1651-1652  (window i1310)  M -2 B -2  [medium]  review: accept

Cycles: 5 -> 5 on both (lda #/sta zp vs inc zp)

Assumption: bactive = 0 at launch: firing is set only at logic.s:1339, after lda bactive / bne @nothrow (1335) and with stz anim (1337); while firing, anim is never reset (1525 needs running|firing = 0; 1666/1671 are in @notfiring; 836/1247 also clear firing at 840/1258), so anim = 4 happens once per throw; bactive becomes nonzero only here (other writes 844, 1802, 1891 are zeroes).

```
        lda #1
        sta bactive
```
->
```
        inc bactive                 ; 0 here: a throw starts only with bactive clear
```

inc of 0 gives 1, as lda #1 / sta. A is reloaded by lda #SFX_THROW next; the inc's flags are overwritten by it.

Review: This is the only place bactive is set nonzero. firing is set (together with stz anim) only when bactive = 0. anim cannot be reset while firing (1525 needs running|firing = 0, and a respawn clears firing as well), so anim reaches 4 once per throw, with bactive still 0. inc turns 0 into 1, and the next lda overwrites the flags.

## r037:10  src/logic.s:1654-1655  (window i1313)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both

Assumption: SFX_THROW = 3 (logic.s:3310); sta leaves the flags of lda #SFX_THROW.

```
        sta SFXREQ
        bra @animdone
```
->
```
        sta SFXREQ
        bne @animdone               ; always: SFX_THROW <> 0
```

Z = 0 from lda #3, so bne is always taken.

Review: SFX_THROW = 3 (logic.s:3310), and sta keeps the Z = 0 from lda #3.

## r037:11  src/logic.s:1658-1659  (window i1316)  M -0 B -3  [high]  review: accept

Cycles: Master stz 3 + bra 3 -> stz 3 + beq 3; Model B stza 12 + jmp 3 -> 5 + 3

Assumption: A is dead: @animdone begins lda hurt. NOTE: branches on the flags across stz, which cpu.inc's contract says no site does -- correct here on both targets, but tools/flagscan.py may list it.

```
        stza firing
        bra @animdone
```
->
```
        stz firing                  ; A dead; Z = 1 after it on both (cmp #12 equal /
        beq @animdone               ; Model B's lda #0)
```

Master: cmp #12 was equal (bne not taken) so Z = 1 and stz leaves flags alone; Model B: stz is lda #0 / sta so Z = 1. beq always taken. stza's A-preservation was unnecessary.

Review: Z = 1 on both targets: on the Master, cmp #12 was equal and stz leaves the flags alone; on the Model B, stz expands to lda #0. A is dead at @animdone. This branches on the flags across stz, which cpu.inc's contract says no site does; flagscan is not part of the gate. Superseded by #13.

## r037:12  src/logic.s:1666-1667  (window i1323)  M -0 B -3  [high]  review: accept

Cycles: Master stz 3 + bra 3 -> 3 + 3; Model B 12 + 3 -> 5 + 3

Assumption: A dead at @animdone (lda hurt). Branches on flags across stz (see C10 note).

```
        stza anim
        bra @animdone
```
->
```
        stz anim                    ; A dead; Z = 1 after it on both (cmp #16 equal /
        beq @animdone               ; Model B's lda #0)
```

cmp #16 equal -> Z = 1 on Master (stz keeps flags); Model B lda #0 -> Z = 1; beq always taken.

Review: Z = 1 on both targets (cmp #16 was equal on the Master; lda #0 on the Model B). A is dead at @animdone. Same flagscan caveat as #11. Superseded by #13.

## r037:13  src/logic.s:1655-1671  (window i1326)  M -9 B -18  [medium]  review: accept

Cycles: running, no wrap: 3+2+3+2+3 = 13 -> ldx 3 + lda 3 + cmp abs,x 4 + bne 3 = 13; running, wrap: 16 + stza/bra (Master 6, B 15) -> 12 + stz (Master 3, B 5); standing, no wrap: 14 -> 13; standing, wrap: 13+stz -> 12+stz; firing branch as C9/C10 (3 -> 3; B 15 -> 8). Page-crossing of cmp abs,x ignored.

Assumption: running is 0 or 1 (writes: stz at 839/1257, stza at 1551, sta after lda #1 at 1548-1549). X dead here (see C7). SFX_THROW = 3 so the bne before the 2-byte table is always taken (the table is never executed). Supersedes C9, C10, C11 (they go stale when this applies).

```
        bra @animdone
:       cmp #12
        bne @animdone
        stza firing
        bra @animdone
@notfiring:
        lda running
        beq :+
        lda anim
        cmp #16
        bne @animdone
        stza anim
        bra @animdone
:       lda anim
        cmp #128
        bne @animdone
        stz anim
```
->
```
        bne @animdone               ; always: SFX_THROW <> 0
@awrap: .byte 128, 16               ; anim wraps at: standing, running (running is 0/1)
:       cmp #12
        bne @animdone
        stz firing                  ; A dead; Z = 1 after it on both (cmp #12 equal /
        beq @animdone               ; Model B's lda #0)
@notfiring:
        ldx running                 ; 0 or 1; X is dead here
        lda anim
        cmp @awrap,x
        bne @animdone
        stz anim
```

@notfiring: anim wraps to 0 when it equals 16 (running) or 128 (standing); a two-byte table indexed by running (0 -> 128, 1 -> 16) makes it one compare. The firing tail: bra -> bne (Z = 0 from lda #SFX_THROW), stza -> stz + beq (Z = 1 on both, see C10).

Review: running is only ever 0 or 1 (written at 839, 1257, 1548-1549 and 1551), so the table (index 0 gives 128, index 1 gives 16) matches the old branches. X is dead, as shown in #8. The table sits after a bne that is always taken (Z = 0 from lda #3), so it never executes. The ':' at 1668 is removed with its only reference; the ':' at 1656 stays. Cycles 13 to 13 and 14 to 13; the wrap paths are faster. Master 32 to 23 bytes, Model B 45 to 27. Supersedes #10, #11 and #12.

## r037:14  src/logic.s:1690-1691  (window i1342)  M -0 B -1  [high]  review: accept

Cycles: 3 -> 3 on both

Assumption: 

```
        bcc @noctl
        bra @setctl
```
->
```
        bcc @noctl
        bcs @setctl                 ; always
```

bcc @noctl not taken means C = 1, so bcs @setctl is always taken.

Review: When bcc is not taken, C = 1, so bcs is always taken.

## r037:15  src/logic.s:1707-1709  (window i1357)  M -2 B -3  [high]  review: accept

Cycles: Master lda# 2 + sta 3 + bra 3 = 8 -> inc 5 + bne 3 = 8; Model B 8 -> 8

Assumption: control = 0 on both ways into @setctl: 1691 follows lda control / bne @ctl (1687-1688) not taken; 1703/1705 follow lda control / bne @ctl (1695-1696) not taken; nothing writes control between. A dead at @ctl (lda firing).

```
        lda #1
        sta control
        bra @ctl
```
->
```
        inc control                 ; control is 0 on both ways in
        bne @ctl                    ; always: it is 1 now
```

inc of 0 stores 1 like lda #1 / sta, and leaves Z = 0 so bne @ctl is always taken.

Review: control = 0 on both ways into @setctl: bne @ctl was not taken at 1687-1688 and at 1695-1696, and nothing writes control in between. inc leaves 1 with Z = 0. A is dead at @ctl (lda firing).

## r037:16  src/logic.s:1711-1717  (window i1364)  M -2 B -1  [medium]  review: accept

Cycles: every path 2 cycles faster on both (tax skipped; ldx#/lda vs lda#/ldx equal; inx 2 = lda# 2; Master bra 3 = jmp 3)

Assumption: @spr's first instruction is the 1-byte tax (logic.s:1767 '@spr:   tax'), so @spr+1 is its lda hurt; A is dead there (lda hurt). @spr within Master bra range: assembled.

```
        lda #22
        ldx vx+1
        bmi @spr2
        ldx vx
        beq @spr2
        lda #23
@spr2:  jmp @spr
```
->
```
        ldx #22
        lda vx+1
        bmi @spr2
        lda vx
        beq @spr2
        inx                         ; 23
@spr2:  bra @spr+1                  ; past @spr's tax: the frame is in X already
```

The frame number is built in X (22, inx -> 23) instead of A; the vx+1 / vx tests move from ldx to lda (same N / Z). Entering @spr+1 skips only tax, whose job (X = frame) is already done; A is reloaded by lda hurt.

Review: The first instruction at @spr is the 1-byte tax (listing $D96 / $12B2). X already holds 22 or 23, and lda hurt reloads A. lda vx+1 and lda vx (zero page) give the same N and Z as the old ldx. The Master bra reaches: $D24 to $D97 is +115. Every path is 2 cycles faster.

## r037:17  src/logic.s:1727-1728  (window i1375)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both (Master bra / Model B jmp vs bne)

Assumption: @sprf within branch range on both: assembled.

```
@f16:   lda #16
        bra @sprf
```
->
```
@f16:   lda #16
        bne @sprf
```

lda #16 sets Z = 0, so bne @sprf is always taken.

Review: lda #16 leaves Z = 0, and @sprf is forward and in range on both targets.

## r037:18  src/logic.s:1729-1730  (window i1377)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both (Master bra / Model B jmp vs bne)

Assumption: @sprf within branch range on both: assembled.

```
@f14:   lda #14
        bra @sprf
```
->
```
@f14:   lda #14
        bne @sprf
```

lda #14 sets Z = 0, so bne @sprf is always taken.

Review: lda #14 leaves Z = 0; @sprf is in range.

## r037:19  src/logic.s:1731-1732  (window i1379)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both (Master bra / Model B jmp vs bne)

Assumption: @sprf within branch range on both: assembled.

```
@f18:   lda #18
        bra @sprf
```
->
```
@f18:   lda #18
        bne @sprf
```

lda #18 sets Z = 0, so bne @sprf is always taken.

Review: lda #18 leaves Z = 0; @sprf is in range.

## r037:20  src/logic.s:1743-1744  (window i1389)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both

Assumption: @sprf in range: assembled.

```
        and #$FE                    ; (anim >> 2) << 1 with one shift, not three
        bra @sprf
```
->
```
        and #$FE                    ; (anim >> 2) << 1 with one shift, not three
        bpl @sprf                   ; always: the lsr cleared bit 7
```

lsr clears bit 7 and and #$FE keeps it clear: N = 0, so bpl @sprf is always taken (Z may be 1, so not bne).

Review: lsr clears bit 7 and and #$FE keeps it clear, so N = 0 and bpl is always taken. Z may be 1, which is why bne would be wrong here.

## r037:21  src/logic.s:1753-1754  (window i1398)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both (Master bra / Model B jmp vs bne)

Assumption: @sprf within branch range on both: assembled.

```
@s10:   lda #10
        bra @sprf
```
->
```
@s10:   lda #10
        bne @sprf
```

lda #10 sets Z = 0, so bne @sprf is always taken.

Review: lda #10 leaves Z = 0; @sprf is in range.

## r037:22  src/logic.s:1755-1756  (window i1400)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both (Master bra / Model B jmp vs bne)

Assumption: @sprf within branch range on both: assembled.

```
@s8:    lda #8
        bra @sprf
```
->
```
@s8:    lda #8
        bne @sprf
```

lda #8 sets Z = 0, so bne @sprf is always taken.

Review: lda #8 leaves Z = 0; @sprf is in range.

## r037:23  src/logic.s:1757-1758  (window i1402)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both (Master bra / Model B jmp vs bne)

Assumption: @sprf within branch range on both: assembled.

```
@s12:   lda #12
        bra @sprf
```
->
```
@s12:   lda #12
        bne @sprf
```

lda #12 sets Z = 0, so bne @sprf is always taken.

Review: lda #12 leaves Z = 0; @sprf is in range.

## r037:24  src/logic.s:1761-1762  (window i1406)  M -0 B -1  [high]  review: accept

Cycles: taken 3 -> 3 on both (Master bra / Model B jmp vs bne)

Assumption: @sprf within branch range on both: assembled.

```
        lda #22
        bra @sprf
```
->
```
        lda #22
        bne @sprf
```

lda #22 sets Z = 0, so bne @sprf is always taken.

Review: lda #22 leaves Z = 0; @sprf is in range.

## r038a:0  src/logic.s:1730-1730  (window i1408)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3; Model B: jmp 3 -> bne taken 3 (ignoring page cross)

Assumption: The line is reached only by falling through from `@f14: lda #14` (line 1729) and is not itself a branch target. @sprf ($12B0 on Model B) is within +127 of every site (at most ~98 bytes) and only gets closer as the other bra->bne changes land.

```
        bra @sprf
```
->
```
        bne @sprf                   ; A = 14, Z = 0
```

`lda #14` just before this line sets Z=0 (non-zero immediate), so bne is always taken, to the same target as bra. A and all flags at @sprf are identical. Master: 2 -> 2 bytes. Model B: the bra macro expands to a 3-byte jmp, which becomes a 2-byte bne.

Review: Line 1730 is exactly '        bra @sprf'. It has no label and is reached only by falling through from 'lda #14'. That value is non-zero, so Z=0 and bne is always taken. A and the flags at @sprf are the same as before. On the Model B the branch sits at $1251/$1253 and @sprf is at $12B0, 93 bytes away, which is in range. Master: bra 2 bytes/3 cycles becomes bne 2/3. Model B: jmp 3/3 becomes bne 2/3.

## r038a:1  src/logic.s:1732-1732  (window i1408)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3; Model B: jmp 3 -> bne taken 3 (ignoring page cross)

Assumption: The line is reached only by falling through from `@f18: lda #18` (line 1731) and is not a branch target. @sprf is within branch range on both targets.

```
        bra @sprf
```
->
```
        bne @sprf                   ; A = 18, Z = 0
```

`lda #18` sets Z=0, so bne is always taken to @sprf. A and flags are unchanged. Model B: 3-byte jmp -> 2-byte bne.

Review: Line 1732 is exactly '        bra @sprf'. It has no label and is reached only by falling through from 'lda #18'. That value is non-zero, so bne is always taken. On the Model B @sprf is 88 bytes away, in range. Model B: jmp 3 bytes becomes bne 2; cycles stay at 3.

## r038a:2  src/logic.s:1739-1746  (window i1408)  M -2 B -3  [high]  review: accept

Cycles: running path: 3+2+3+2+2+3=15 -> 3+3+2+2+2+3=15 (Master bra 3 = bpl taken 3; Model B jmp 3 = bpl taken 3). Standing path up to cmp #93: 3+3+3=9 -> 3+3+3=9. Same on both targets.

Assumption: X is dead from @ground to @sprf. Both exits (running -> @sprf, standing -> @s8/@s10/@s12 -> @sprf) reach `@sprf: ora facing / @spr: tax`, which overwrites X before anything reads it. Nothing in lines 1738-1766 reads X. @standing is referenced only from line 1740 (grep). running and anim are zero page on both targets (listing t_b/t_m: A5 rr), so ldx running is 2 bytes, the same as lda running.

```
        lda running
        beq @standing
        lda anim
        lsr
        and #$FE                    ; (anim >> 2) << 1 with one shift, not three
        bra @sprf
@standing:
        lda anim
```
->
```
        lda anim
        ldx running                 ; X dead: @sprf ends in tax
        beq @standing
        lsr
        and #$FE                    ; (anim >> 2) << 1 with one shift, not three
        bpl @sprf                   ; N=0: lsr cleared bit 7
@standing:
```

Old: A=running, Z from running, branch, then A=anim on both paths. New: A=anim first, then ldx running sets Z from running (the same test). beq @standing therefore arrives with A=anim already, so the standing path's `lda anim` (line 1746) is dropped and the running path's `lda anim` is hoisted. After lsr, bit 7 is 0 and and #$FE keeps it 0, so N=0 and bpl is always taken; it replaces bra (Master 2->2 bytes, Model B jmp 3->2). A at @sprf is identical. X is clobbered but dead.

Review: Lines 1739-1746 match the file exactly. running and anim are zero page on both targets (A5 in t_b/t_m, running at $7B), so ldx running is 2 bytes. X is dead: every path from @ground reaches '@sprf: ora facing / @spr: tax', and nothing on the way reads X (only cmp, bcc, lda and bra). @standing is referenced only from line 1740 and @ground only from 1736. At @standing, A = anim as before, and cmp #93 overwrites the flags. On the running path, lsr clears bit 7 and and #$FE keeps it clear, so bpl is always taken. Cycles: running path 15 -> 15, standing path up to the cmp 9 -> 9, on both targets. Bytes: Master 13 -> 11, Model B 14 -> 11.

## r038a:3  src/logic.s:1754-1754  (window i1408)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3; Model B: jmp 3 -> bne taken 3

Assumption: The line is reached only by falling through from `@s10: lda #10` (line 1753), which is entered by fall-through from bcc @s12 and by bcc @s10. The bra line itself is unlabelled. @sprf is in range.

```
        bra @sprf
```
->
```
        bne @sprf                   ; A = 10, Z = 0
```

`lda #10` sets Z=0, so bne is always taken. Model B: 3-byte jmp -> 2-byte bne.

Review: Line 1754 is exactly '        bra @sprf' with no label. It is reached only by falling through from '@s10: lda #10', which is non-zero, so bne is always taken. @sprf is in range. Model B: jmp 3 bytes becomes bne 2; cycles stay at 3.

## r038a:4  src/logic.s:1756-1756  (window i1408)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3; Model B: jmp 3 -> bne taken 3

Assumption: The line is reached only by falling through from `@s8: lda #8` (line 1755) and is not a branch target. @sprf is in range.

```
        bra @sprf
```
->
```
        bne @sprf                   ; A = 8, Z = 0
```

`lda #8` sets Z=0, so bne is always taken. Model B: 3-byte jmp -> 2-byte bne.

Review: Line 1756 is exactly '        bra @sprf' with no label. It is reached only by falling through from '@s8: lda #8', which is non-zero, so bne is always taken. @sprf is in range. Model B: jmp 3 bytes becomes bne 2; cycles stay at 3.

## r038a:5  src/logic.s:1758-1758  (window i1408)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3; Model B: jmp 3 -> bne taken 3

Assumption: The line is reached only by falling through from `@s12: lda #12` (line 1757) and is not a branch target. @sprf is in range.

```
        bra @sprf
```
->
```
        bne @sprf                   ; A = 12, Z = 0
```

`lda #12` sets Z=0, so bne is always taken. Model B: 3-byte jmp -> 2-byte bne.

Review: Line 1758 is exactly '        bra @sprf' with no label. It is reached only by falling through from '@s12: lda #12', which is non-zero, so bne is always taken. @sprf is in range. Model B: jmp 3 bytes becomes bne 2; cycles stay at 3.

## r038a:6  src/logic.s:1762-1762  (window i1408)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3; Model B: jmp 3 -> bne taken 3

Assumption: The line is reached only by falling through from `lda #22` (line 1761) and is not a branch target. @sprf is in range (offset 8 on Model B).

```
        bra @sprf
```
->
```
        bne @sprf                   ; A = 22, Z = 0
```

`lda #22` sets Z=0, so bne is always taken. Model B: 3-byte jmp -> 2-byte bne.

Review: Line 1762 is exactly '        bra @sprf' with no label. It is reached only by falling through from 'lda #22', since bge16i branches away to @j24. 22 is non-zero, so bne is always taken. @sprf is in range. Model B: jmp 3 bytes becomes bne 2; cycles stay at 3.

## r038a:7  src/logic.s:1764-1764  (window i1408)  M -0 B -1  [high]  review: accept

Cycles: Master: bra 3 -> bne taken 3; Model B: jmp 3 -> bne taken 3

Assumption: The line is reached only by falling through from `@j20: lda #20` (line 1763) and is not a branch target. @sprf is in range.

```
        bra @sprf
```
->
```
        bne @sprf                   ; A = 20, Z = 0
```

`lda #20` sets Z=0, so bne is always taken. Model B: 3-byte jmp -> 2-byte bne.

Review: Line 1764 is exactly '        bra @sprf' with no label. It is reached only by falling through from '@j20: lda #20', which is non-zero, so bne is always taken. @sprf is 3 bytes away. Model B: jmp 3 bytes becomes bne 2; cycles stay at 3.

## r038b:0  src/logic.s:1798-1801  (window i1469)  M -16 B -16  [high]  review: accept

Cycles: The instructions are the same on both targets (no macros, and every operand is zp, as the Model B listing t_b.lst shows): 4 x 12 = 48 bytes -> 18+14 = 32. rx<=-8: 15 -> 13 (always rejected at the first bne: rx+7 unsigned >= $8007, or rx+1=$FF with low<$F9 gives A=$FF). rx>=8: 29 -> 13 (bne) or 22 (rx 8..248: 3+2+3+2+2+2+3+2+3). rx in range: 28 -> 15 (rx+1=$FF) or 21 (rx+1=0). The ry cost adds to the rx pass cost in both versions, and that cost only fell. ry<=-8: 15 -> 13 (ry+7 unsigned >= $8007, so the high byte is nonzero). ry>=8: 29 -> 13 or 17. ry in range: 28 -> 16, then stz bactive is unchanged.

Assumption: X = low byte of py-by (the tax at logic.s:1786) and it survives to the ry test: the only instructions in between are sbc/sta/txa/clc/adc/bcc/inc (1787-1795) and the new rx test, which touches only A. ry = (py-by)+8 exactly as 16 bits (1784-1795), so ry+7 = (py-by)+15 and low(ry+7) = X+15 mod 256. A and the flags are dead at @nocatch (1804 lda bcnt) and at stz bactive (Model B lda #0, as before). X on exit is unchanged from the original (still low(py-by)). @xin is a new cheap label; grep shows it is not used in player_update's scope. This is not timing-critical code. A brute-force check of both predicates over all 65536 values matches the original signed tests.

```
        ble16i rx, -8, @nocatch
        bge16i rx, 8, @nocatch
        ble16i ry, -8, @nocatch
        bge16i ry, 8, @nocatch
```
->
```
        lda rx                      ; rx in -7..7 iff rx+7 is 0..14 unsigned
        cmp #$F9                    ; C = carry out of rx+7's low byte
        lda rx+1
        adc #0                      ; A = high byte of rx+7
        bne @nocatch
        bcs @xin                    ; rx+1 was $FF: rx = -7..-1
        lda rx                      ; rx+1 was 0: rx = 0..248
        cmp #8
        bcs @nocatch
@xin:   lda ry                      ; the same for ry, whose low byte less 8 is still in X
        cmp #$F9
        lda ry+1
        adc #0                      ; A = high byte of ry+7
        bne @nocatch
        cpx #$F1                    ; low(ry+7) = X+15 < 15 iff X >= $F1
        bcc @nocatch
```

Signed 16-bit v is in [-7,7] iff v+7 mod 65536 is in [0,14], i.e. high(v+7)=0 and low(v+7)<15. cmp #$F9 sets C = (low >= $F9), which is the carry out of low+7; adc #0 then gives high(v+7) = v+1 + C. For rx, A=0 in only two cases. (rx+1=$FF, C=1): rx is -7..-1, in range, carry out of adc = 1, so bcs @xin. (rx+1=0, C=0): rx is 0..$F8, carry out 0, in range iff low<8 (cmp #8/bcs). Every other high byte is out of range and takes bne @nocatch. For ry, once high(ry+7)=0, low(ry+7) = (X+15) mod 256, which is < 15 iff X+15 wraps, iff X >= $F1 (cpx #$F1/bcc). This matches the four ble16i/bge16i tests exactly (they are exact signed compares via the $80 bias).

Review: Original matches logic.s:1798-1801 verbatim. rx,ry are zp ($B6,$B8), so the original is 4x12=48 bytes and the proposal 18+14=32 on both targets (no macros in it). X=low(py-by) from the tax at 1786 survives: 1787-1795 and the new rx test do not touch X. A brute force over all 65536 values of both the rx and the ry test (the ry test with X=(ry_lo-8)&255) matches signed -7..7 exactly. Every path is faster on both targets: rx 15/29/28 -> 13/13-22/15-21, ry 15/29/28 -> 13/13-17/16. A and the flags are dead at @nocatch (lda bcnt) and at stz bactive. The 4 macro placeholder ':' labels go, and no anonymous reference crosses them: 1779 bne :+ ->1781, 1806 bcc :+ ->1808, 1829 bne :- ->1825, and the only :++ in 1590-1960 (1624) resolves long before. @xin is new and unique in player_update's scope (1314-1931).

## r038b:1  src/logic.s:1810-1823  (window i1471)  M -3 B -3  [high]  review: accept

Cycles: Straight line on both targets: 45 -> 40. It drops sta t16+1 (3), replaces rol zp (5) with rol a (2) + tax (2), and replaces lda t16+1 (3) with txa (2).

Assumption: X is dead: ldx #6 follows at logic.s:1824 and nothing in between reads X. t16+1 is not read before the sta t16+1 at 1830: the loop reads only A and t16, and the comment at 1823 already calls the store dead.

```
        sec                         ; t16 = -bvx: negate first, then double in place
        lda #0
        sbc bvx
        sta t16
        lda #0
        sbc bvx+1
        sta t16+1
        asl16 t16                   ; t16 = -2*bvx
        sec
        lda t16
        sbc bvx
        sta t16
        lda t16+1
        sbc bvx+1                   ; A = high byte of -3*bvx (t16+1 store is dead)
```
->
```
        sec                         ; t16 = -bvx: negate first, then double in place
        lda #0
        sbc bvx
        sta t16
        lda #0
        sbc bvx+1                   ; A = high byte of -bvx
        asl t16
        rol                         ; A = high byte of -2*bvx
        tax
        sec
        lda t16
        sbc bvx
        sta t16
        txa
        sbc bvx+1                   ; A = high byte of -3*bvx
```

The high byte of -bvx stays in A instead of going to t16+1. asl t16 leaves A alone and puts t16's bit 7 in C, so rol a does the work of rol t16+1. tax parks the byte across the low-byte subtract without touching C, and txa brings it back for sbc bvx+1 with the borrow from sbc bvx intact. A on exit is the same high byte of -3*bvx and t16 low is identical. t16+1 is no longer written here, but it is dead until 1830.

Review: Original matches logic.s:1810-1823 verbatim. t16 and bvx are zp. Bytes go 28 -> 25 on both targets (sta t16+1 -2, rol zp->rol a -1, tax +1, lda t16+1->txa -1) and cycles 45 -> 40. X is dead because ldx #6 at 1824 overwrites it. t16+1 is not read before sta t16+1 at 1830 (the 1825-1829 loop uses A, t16 and X). tax/txa leave C alone, so the borrow from sbc bvx reaches sbc bvx+1. Bare 'rol' is accumulator mode in ca65 on both CPUs (bare 'ror' is already used at 1826). No labels or branch-ins. The window tag is bookkeeping only: the lines are i1476+.

## r038b:2  src/logic.s:1833-1837  (window i1473)  M -3 B -3  [high]  review: accept

Cycles: Straight line on both targets: 30 -> 26. lda bvx (3) + cmp (2) + lda bvx+1 (3) become tax (2) + cpx (2).

Assumption: X is dead: after this, sx16 t16 and add16 bx,t16 use only A, and the bvy block (1847-1857) sets X with ldx #6 before any read. The change runs past this region's last window (i1502), so the lowest window holding all of it is i1473. If the next region also finds it, report it once, there.

```
        add16 bvx, rx
        lda bvx
        cmp #$80                    ; C = bit 7 of bvx = carry out of (bvx + 128)
        lda bvx+1
        adc #0                      ; A = high byte of bvx + 128
```
->
```
        clc                         ; add16 bvx, rx, keeping the new low byte in X
        lda bvx
        adc rx
        sta bvx
        tax
        lda bvx+1
        adc rx+1
        sta bvx+1
        cpx #$80                    ; C = bit 7 of bvx = carry out of (bvx + 128)
        adc #0                      ; A = high byte of bvx + 128
```

add16 is expanded in place, and the tax between the two adc's does not touch C. After sta bvx+1, A already holds bvx+1, so the reload goes. cpx #$80 on the new low byte sets C exactly as lda bvx/cmp #$80 did, and adc #0 then gives the same A. bvx in memory is identical.

Review: Original matches logic.s:1833-1837 verbatim. bvx and rx are zp. Bytes go 21 -> 18 and cycles 30 -> 26 on both targets. After sta bvx+1, A already holds bvx+1. tax between the adcs keeps C, and cpx #$80 on the new low byte gives the same C as lda bvx/cmp #$80. X is dead: sx16 t16, add16 bx,t16 and the bvy block 1840-1853 use only A until ldx #6 at 1854. No labels or branch-ins. The change is independent of candidates 0 and 1 (candidate 0 needs X only up to 1801). Lines 1836-1837 fall past this region's last window (i1471 covers to i1502), so it may be found twice. Dedupe it against the next region.

## r038c:0  src/logic.s:1814-1823  (window i1472)  M -3 B -3  [high]  review: accept

Cycles: straight line 35->30 on both targets (sta t16+1 3 + asl/rol mem 10 + lda t16+1 3 = 16 -> asl mem 5 + rol A 2 + tax 2 + txa 2 = 11)

Assumption: X dead: overwritten by ldx #6 at logic.s:1824 immediately after; t16+1 not read before the sta t16+1 at 1830 (loop only touches A and t16); t16,bvx zp (listing modelb/build/main.lst 0x1338..)

```
        lda #0
        sbc bvx+1
        sta t16+1
        asl16 t16                   ; t16 = -2*bvx
        sec
        lda t16
        sbc bvx
        sta t16
        lda t16+1
        sbc bvx+1                   ; A = high byte of -3*bvx (t16+1 store is dead)
```
->
```
        lda #0
        sbc bvx+1                   ; A = high byte of -bvx
        asl t16
        rol                         ; A:t16 = -2*bvx
        tax
        sec
        lda t16
        sbc bvx
        sta t16
        txa
        sbc bvx+1                   ; A = high byte of -3*bvx
```

Original stores hi(-v) to t16+1, shifts t16:t16+1 left in memory, then reloads t16+1 as the minuend high byte. New keeps hi(-v) in A: asl t16 puts bit 7 of the low byte in C and rol shifts it into A, so A:t16 = -2v exactly as t16+1:t16 was. tax parks the high byte (txa does not touch C), sec/lda t16/sbc v/sta t16 is unchanged, txa/sbc v+1 gives the same A and C and N/Z as lda t16+1/sbc v+1. t16+1 is no longer written here but the original comment already notes that store is dead: the next write to t16+1 (sta after the ror loop) precedes every read. Bytes (all zp): 21 -> 18.

Review: Original matches 1814-1823. A:t16 after asl t16/rol A equals t16+1:t16 after the in-memory asl16; tax/txa leave C from sbc bvx intact, so final A/C/N/Z match. t16+1 is not read by the ror loop (1825-1829) and is rewritten at 1830 (or never read, with the other candidates). X is overwritten by ldx #6 at 1824. All operands are zp (t16=$D1, bvx in .zeropage at logic.s:216): 21->18 bytes on both targets, 35->30 cycles.

## r038c:1  src/logic.s:1844-1853  (window i1489)  M -3 B -3  [high]  review: accept

Cycles: straight line 35->30 on both targets

Assumption: X dead: overwritten by ldx #6 at logic.s:1854; t16+1 not read before sta t16+1 at 1860; all zp

```
        lda #0
        sbc bvy+1
        sta t16+1
        asl16 t16                   ; t16 = -2*bvy
        sec
        lda t16
        sbc bvy
        sta t16
        lda t16+1
        sbc bvy+1                   ; A = high byte of -3*bvy (t16+1 store is dead)
```
->
```
        lda #0
        sbc bvy+1                   ; A = high byte of -bvy
        asl t16
        rol                         ; A:t16 = -2*bvy
        tax
        sec
        lda t16
        sbc bvy
        sta t16
        txa
        sbc bvy+1                   ; A = high byte of -3*bvy
```

Same as the bvx block: hi(-v) kept in A, asl t16/rol A doubles A:t16 exactly as asl16 did in memory, tax/txa replaces the t16+1 store/reload (txa leaves C from the low sbc intact), final A/C/N/Z identical. Bytes 21 -> 18.

Review: Same as index 0 for bvy: lines match, t16+1 not read before 1860, X dead (ldx #6 at 1854), zp operands. 21->18 bytes, 35->30 cycles.

## r038c:2  src/logic.s:1830-1831  (window i1472)  M -2 B -2  [high]  review: accept

Cycles: straight line 23->21 on both targets (sta zp 3 + lda zp 3 -> tax 2 + txa 2)

Assumption: X dead: X is 0 after the loop and nothing reads X until it is reloaded (ldx #6 at 1854, or ldx bvx if that candidate is taken; sx16/add16/asl16 at 1832-1839 don't use X). t16+1 dead: next access is sx16 t16's store at 1838 (write) before add16 bx,t16 reads it

```
        sta t16+1
        add16 bvx, t16
```
->
```
        tax
        clc
        lda bvx
        adc t16
        sta bvx
        txa
        adc bvx+1
        sta bvx+1
```

After the ror loop A holds the high byte of the shifted delta. Original stores it to t16+1 then add16 reloads it as the addend. New parks it in X (tax), does the same low-byte add, then txa/adc bvx+1 -- addition is commutative so A, C, N, Z, V after adc are identical and bvx+1 gets the same value. t16+1 is dead: sx16 t16 at 1838 rewrites it before add16 bx,t16 reads it (or, with the sx16 candidate, it is never read again).

Review: tax/txa replaces sta t16+1 and the reload in add16. The add is commutative, so A, flags and bvx+1 are unchanged. X is not read before ldx #6 at 1854 (or before a write from the other candidates). t16+1 is written by sx16 at 1838 before it is read at 1839, and with index 6 it is never read. 15->13 bytes (zp), 23->21 cycles.

## r038c:3  src/logic.s:1860-1861  (window i1496)  M -2 B -2  [high]  review: accept

Cycles: straight line 23->21 on both targets

Assumption: X dead: nothing reads X until ldx #0 at logic.s:1868 (asl16 ry/add16 bvy,ry/lda/cmp/adc only). t16+1 dead after this: grep of t16 in logic.s after 1861 shows only writes-before-reads at @bdone (1906.., only t16 lo, written first); getaltitude/getinfo/tilexy/maptile/addsprite do not touch t16 (engine.s t16 uses are only in init_maprows 4574-4601); player_update ends at @noexit rts

```
        sta t16+1
        add16 bvy, t16
```
->
```
        tax
        clc
        lda bvy
        adc t16
        sta bvy
        txa
        adc bvy+1
        sta bvy+1
```

After the ror loop A holds the high byte of the shifted delta. Original stores it to t16+1 then add16 reloads it as the addend. New parks it in X, same low add, then txa/adc bvy+1 -- commutative, so A, C, N, Z, V identical and bvy+1 same. t16+1 is never read again in player_update or its callees.

Review: Same transform for bvy. X is not read before ldx #0 at 1868. t16+1 is not read again: @bdone uses only t16 low, getaltitude/getinfo/tilexy/maptile (logic.s:305-430) do not touch t16, and player_update ends in rts. Outside the boomerang path t16+1 already holds a stale value, so no caller can depend on it. 15->13 bytes, 23->21 cycles.

## r038c:4  src/logic.s:1834-1837  (window i1473)  M -2 B -2  [high]  review: accept

Cycles: straight line 10->7 on both targets

Assumption: A = bvx+1 on entry: add16 bvx,rx (macro, macros.txt:1069) ends with sta bvx+1. X dead: next X access is ldx #0 (sx16 candidate) or tax/ldx #6 in block 2; sx16/add16 bx do not read X

```
        lda bvx
        cmp #$80                    ; C = bit 7 of bvx = carry out of (bvx + 128)
        lda bvx+1
        adc #0                      ; A = high byte of bvx + 128
```
->
```
        ldx bvx
        cpx #$80                    ; C = bit 7 of bvx = carry out of (bvx + 128)
        adc #0                      ; A (= bvx+1 from add16) = high byte of bvx + 128
```

After add16 bvx,rx the last instruction is sta bvx+1, so A already equals bvx+1: the reload is redundant. ldx bvx / cpx #$80 sets C = bit 7 of bvx lo, exactly as lda bvx / cmp #$80 did; adc #0 then gives the same A and flags. Bytes 8 -> 6.

Review: add16 bvx,rx (1833) ends with sta bvx+1, so A already equals bvx+1. cpx #$80 sets C exactly as cmp #$80 did, and adc #0 gives the same A and flags. X is dead: sx16/add16 do not use it, and the next X use is a write (ldx #0 in index 6, tax in index 0/1, or ldx #6). 8->6 bytes (zp), 10->7 cycles.

## r038c:5  src/logic.s:1864-1867  (window i1502)  M -2 B -2  [high]  review: accept

Cycles: straight line 10->7 on both targets

Assumption: A = bvy+1 on entry: add16 bvy,ry ends with sta bvy+1. X dead: ldx #0 at logic.s:1868 follows directly

```
        lda bvy                     ; only the high byte of bvy+128 is ever used
        cmp #128
        lda bvy+1
        adc #0
```
->
```
        ldx bvy                     ; only the high byte of bvy+128 is ever used
        cpx #128
        adc #0                      ; A was bvy+1 from add16
```

After add16 bvy,ry the last instruction is sta bvy+1, so A already equals bvy+1. ldx bvy / cpx #128 sets C = bit 7 of bvy lo as lda/cmp did; adc #0 gives the same A and flags. Bytes 8 -> 6.

Review: add16 bvy,ry at 1863 ends with sta bvy+1, so A = bvy+1. ldx bvy/cpx #128 sets the same C, and ldx #0 at 1868 immediately overwrites X. 8->6 bytes, 10->7 cycles.

## r038c:6  src/logic.s:1838-1839  (window i1475)  M -6 B -6  [high]  review: accept

Cycles: A>=0: 31->23 (sx16 11 + add16 20 -> ldx 2 cmp 2 bcc 3 + 16); A<0: 32->24 (sx16 12 -> ldx/cmp/bcc/dex 8); both targets

Assumption: t16 dead after: block 2 writes t16 at 1843 and t16+1 at 1846/1860 before any read (and t16 is not read downstream). X dead: next X use in block 2 is a write (ldx #6 / tax). Carry: the add starts with clc

```
        sx16 t16
        add16 bx, t16
```
->
```
        ldx #0                      ; sign-extend the delta into X and add it straight
        cmp #$80                    ; to bx, as the bvy block does for by
        bcc :+
        dex
:       clc
        adc bx
        sta bx
        txa
        adc bx+1
        sta bx+1
```

sx16 t16 stores A to t16 and $00/$FF (by bit 7 of A) to t16+1; add16 bx,t16 adds that. New keeps the sign extension in X (0 or $FF by cmp #$80, same test as and #$80) and adds A and X directly: identical arithmetic. Same code shape already used for by at 1868-1875. Bytes 23 -> 17.

Review: The sign extension goes into X (cmp #$80 is the same test as the and #$80) and is added straight to bx. The add is commutative, so bx and the final A/flags are identical. t16/t16+1 are dead: block 2 writes t16 at 1843 and t16+1 at 1846 (or never reads t16+1 under index 1) before any read. X is dead (next X use is a write). The anonymous label count is unchanged: sx16 emits one ':' and the replacement has one in the same place, and bne :- at 1859 and bcc :+ at 1806 still resolve correctly. 23->17 bytes (zp). Cycles: A>=0 31->23, A<0 32->24.

## r038c:7  src/logic.s:1802-1802  (window i1472)  M -0 B -2  [medium]  review: accept

Cycles: Master unchanged (stz zp 3); Model B lda #0/sta zp 5 -> dec zp 5

Assumption: bactive is only ever 0 or 1: writes at logic.s:844 (stza, 0), 1652 (lda #1/sta), 1802 (stz), 1891 (stza, 0), no indexed/other writes (grep). This point is reached only via lda bactive / bne :+ at 1778-1779 with no intervening write, so bactive==1. A after: original leaves A=0 on Model B, but @nocatch (1804) reloads A with lda bcnt and nothing reads A/flags first

```
        stz bactive
```
->
```
.if MODELB
        dec bactive                 ; bactive is 1 here (0/1 flag, nonzero on entry)
.else
        stz bactive
.endif
```

Model B stz macro = lda #0 / sta bactive (4 bytes, 5 cycles). With bactive==1, dec bactive (2 bytes, 5 cycles) stores the same 0. A and flags are dead (lda bcnt next).

Review: bactive is written only at logic.s:844 (stza 0, level init), 1652 (lda #1/sta), 1802 and 1891 (0), so it is always 0 or 1. Line 1802 has no label and is reached only by falling through from lda bactive/bne at 1778-1779, with no intervening write, so bactive==1 there and dec gives 0. A and Z are dead: @nocatch does lda bcnt next. Model B: lda #0/sta zp (4 bytes, 5 cycles) -> dec zp (2 bytes, 5 cycles). Master is unchanged.

## r038d:0  src/logic.s:1837-1879  (window i1504)  M -24 B -24  [high]  review: accept

Cycles: bx part: pos 33->29, neg 34->30 (plus mov16 qx 12 cycles gone); -3*bvy part 45->40; lda bvy/cmp->asl 2->2; sign-ext pos ldx+cmp+bcc 7->bpl 3, neg 8->4; qy: mov16 qy 12 -> two sta 6. Every path faster; identical on Model B (no target-dependent macros in the range: sx16/add16/asl16/mov16 are plain). Byte counts confirmed by assembling a scratch copy of both targets (Master @noexit $F4E->$F36; Model B $1474->$1458 with all three candidates = 24+2+2).

Assumption: All operands zero page (bx $7F, by $81, bvx $83, bvy $85 in both label files; t16=$D1, ry=$B8, qx=$BE, qy=$C0): no aliasing between qx/qy and bvy/by/t16/ry, so storing qx early is safe. X is not read between line 1837 and the ldx #6 at 1854 (only A/zp arithmetic in between). X = 0 on falling out of the dex/bne loop at 1858-1859 and nothing between 1860 and 1868 writes X (sta, clc/lda/adc, asl/rol zp, lda/asl/lda/adc). t16+1 is not read between 1846 and 1860 (the comment at 1853 says the store is dead; the loop rors only t16), and 1860 still stores it, so t16/t16+1 end with the same values. bx is not written between 1839 and 1878, so qx = bx is the same value stored earlier. No label other than anonymous ones inside 1837-1879; the only entry is the fall-through (the jmp @bfly at 1807 lands after 1879). getaltitude reads qx/qy only.

```
        adc #0                      ; A = high byte of bvx + 128
        sx16 t16
        add16 bx, t16
        sec                         ; t16 = -bvy, same shape as the bvx block above
        lda #0
        sbc bvy
        sta t16
        lda #0
        sbc bvy+1
        sta t16+1
        asl16 t16                   ; t16 = -2*bvy
        sec
        lda t16
        sbc bvy
        sta t16
        lda t16+1
        sbc bvy+1                   ; A = high byte of -3*bvy (t16+1 store is dead)
        ldx #6
:       cmp #$80
        ror
        ror t16
        dex
        bne :-
        sta t16+1
        add16 bvy, t16
        asl16 ry
        add16 bvy, ry
        lda bvy                     ; only the high byte of bvy+128 is ever used
        cmp #128
        lda bvy+1
        adc #0
        ldx #0                      ; sign-extend the delta into X and add it straight
        cmp #$80                    ; to by, instead of staging it through t16
        bcc :+
        dex
:       clc
        adc by
        sta by
        txa
        adc by+1
        sta by+1
        mov16 qx, bx
        mov16 qy, by
```
->
```
        ldx #0                      ; X = sign of the delta (N from the adc)
        adc #0                      ; A = high byte of bvx + 128
        bpl :+
        dex
:       clc                         ; bx += the delta, and qx = bx for getaltitude
        adc bx
        sta bx
        sta qx
        txa
        adc bx+1
        sta bx+1
        sta qx+1
        sec                         ; t16 = -bvy, same shape as the bvx block above
        lda #0
        sbc bvy
        sta t16
        lda #0
        sbc bvy+1                   ; A = high byte of -bvy
        asl t16                     ; -2*bvy: high byte in A, then X
        rol
        tax
        sec
        lda t16
        sbc bvy
        sta t16
        txa
        sbc bvy+1                   ; A = high byte of -3*bvy
        ldx #6
:       cmp #$80
        ror
        ror t16
        dex
        bne :-
        sta t16+1
        add16 bvy, t16
        asl16 ry
        add16 bvy, ry
        lda bvy                     ; only the high byte of bvy+128 is ever used
        asl                         ; C = bit 7 of bvy
        lda bvy+1
        adc #0
        bpl :+                      ; X = 0 from the loop: sign-extend the delta into it
        dex
:       clc
        adc by
        sta by
        sta qy
        txa
        adc by+1
        sta by+1
        sta qy+1
```

(1) bx: ldx #0 does not touch C, so adc #0 still adds the carry from cmp at 1835 and its N is bit 7 of the delta; bpl/dex leaves X = sx16's t16+1 value (0/$FF). clc/adc bx/sta bx/txa/adc bx+1/sta bx+1 is add16 bx,t16 with the operands commuted, same carry chain; t16 (sx16's write) is overwritten at 1843 before any read, so dropping it is safe. sta qx/qx+1 replace the mov16 qx,bx at 1878 (bx unchanged in between). (2) -3*bvy: instead of sta t16+1 / rol t16+1 / lda t16+1 the high byte stays in A: asl t16 then rol A gives hi(-2*bvy) with the same carry-in; tax holds it across the low-byte subtract; txa does not touch C so sbc bvy+1 gets the same borrow. (3) lda bvy / asl sets C = bit 7 of bvy exactly as cmp #128 does; N,Z are overwritten by lda bvy+1. (4) X = 0 from the loop replaces ldx #0; adc #0 sets N = bit 7 of the delta, so bpl/dex is the cmp #$80/bcc/dex. (5) qy: by's two stores are followed by sta qy / sta qy+1 of the same values, replacing mov16 qy,by; A = by+1 and C are as after the original mov16 (lda by+1 sets N,Z same as the adc result), and getaltitude does not consume flags on entry.

Review: The original matches lines 1837-1879 exactly. Every operand is zero page (bx $7F, by $81, bvy $85, t16 $D1, ry $B8, qx $BE, qy $C0), so nothing aliases, and storing qx early is safe because nothing between writes bx or qx. ldx #0 leaves C alone for adc #0, and the N flag from that adc is the same bit sx16's and #$80 tests. The commuted add16 has the same carry chain, and t16/t16+1 from sx16 are overwritten before anything reads them. For -3*bvy, asl t16 / rol A / tax replaces sta t16+1 / asl16 / lda t16+1 with the same carry in, and nothing reads t16+1 before the sta at 1860. lda bvy / asl sets C to bit 7, as cmp #128 does. X is 0 after the dex/bne loop and nothing writes X before the bpl, so bpl does what cmp #$80 / bcc did. sta qy/qy+1 replace mov16 qy,by: A, N, Z, C, V and X are all the same at jsr getaltitude, which starts with jsr getinfo and does not read flags. The anonymous labels stay at 3, in the same order. The assembled listing differs only inside the range, and the new branch offsets resolve correctly. Every path is faster on both targets. Scratch build: Master LOGIC $2564 -> $254C (24 bytes); Model B LGCCODE $2A0A -> $29EE (28 bytes with the two stz candidates, i.e. 24+2+2).

## r038d:1  src/logic.s:1888-1888  (window i1535)  M -0 B -2  [high]  review: accept

Cycles: bcnt = 8 path: Model B stza 12 (pha 3, lda 2, sta 3, pla 4) -> stz 5; Master stz 3 -> 3.

Assumption: The only consumer of A after the store is `cmp #14 / bne :+` at 1889-1890 whose target (1892) reloads A with lda bactive; on this path A was 8 and becomes 0 on the Model B, and both fail the compare with 14 (Z=0, C=0 in both cases) so bne is taken either way. Note tools/flagscan.py will list the A use as a hazard: it is benign.

```
        stza bcnt
```
->
```
        stz bcnt                    ; (Model B: A = 0, and 0 fails cmp #14 as 8 does)
```

stza = pha/lda #0/sta/pla keeps A = 8; stz = lda #0/sta leaves A = 0. The next instruction cmp #14 gives Z = 0 and C = 0 for both, so bne :+ goes to lda bactive either way, which overwrites A, N and Z. Master: stza and stz are both `stz`.

Review: Line 1888 matches. It is reached only with A = 8 (lda bcnt / cmp #8 / bne). On the Model B, stz leaves A = 0. The cmp #14 that follows gives Z=0 and C=0 for both 0 and 8, and cmp does not touch V, so bne :+ is taken to 1892 lda bactive, which reloads A. Neither later path uses C (beq @bdone goes to lda px/sec; the fall-through does lsr/clc). On the Master, stza and stz both expand to stz. Model B goes from 6 to 4 bytes and from 12 to 5 cycles. Confirmed in a scratch build.

## r038d:2  src/logic.s:1891-1891  (window i1535)  M -0 B -2  [high]  review: accept

Cycles: bcnt = 14 path: Model B 12 -> 5; Master 3 -> 3.

Assumption: The next instruction (line 1892, `: lda bactive`) reloads A and redefines N,Z; C is not consumed afterwards on either branch (beq @bdone -> 1903 lda/sec; fall-through -> clc before adc #27).

```
        stza bactive
```
->
```
        stz bactive
```

A is dead after the store (lda bactive follows immediately), so the Model B's stza (pha/lda #0/sta/pla) can be stz (lda #0/sta); the stored value is 0 in both. Master: identical instruction.

Review: Line 1891 matches. The next instruction, 1892 ': lda bactive', reloads A and sets N/Z, and nothing afterwards uses C. On the Model B, stza (pha/lda #0/sta/pla, 6 bytes, 12 cycles) becomes lda #0/sta (4 bytes, 5 cycles). On the Master both are the same stz. Confirmed in a scratch build.

## r039:0  src/logic.s:1872-1879  (window i1536)  M -4 B -4  [high]  review: accept

Cycles: One path: 6 cycles fewer on both targets (two lda zp removed).

Assumption: A is dead at jsr getaltitude: getaltitude -> getinfo -> tilexy loads A (lda qx+1) and X before reading them (logic.s:314,349,371). A was by+1 at the jsr and is now bx+1. qx, qy, by and bx are all zero page (logic.s:227-228,268-269).

```
:       clc
        adc by
        sta by
        txa
        adc by+1
        sta by+1
        mov16 qx, bx
        mov16 qy, by
```
->
```
:       clc
        adc by
        sta by
        sta qy                      ; qy = by as it is made: no reload
        txa
        adc by+1
        sta by+1
        sta qy+1
        mov16 qx, bx
```

mov16 qy,by expands to lda by/sta qy/lda by+1/sta qy+1. A already holds by right after sta by, and by+1 right after sta by+1, so storing qy/qy+1 at those points writes the same memory. sta leaves the flags alone, so the adc carry chain is unchanged. mov16 qx,bx then leaves A = bx+1 instead of by+1, which getaltitude does not read.

Review: Original matches logic.s:1872-1879. by, bx, qx($BE), qy($C0) are all zero page; A holds by then by+1 at the new stores; sta leaves the adc carry chain alone; getaltitude->getinfo->tilexy loads A (lda qx+1) before reading it, and bpl @bfly tests getaltitude's own flags. Build-checked: jsr getaltitude moves 4 bytes on both targets.

## r039:1  src/logic.s:1888-1888  (window i1536)  M -0 B -2  [high]  review: accept

Cycles: bcnt==8 path: Model B 7 cycles fewer (pha/pla gone). Master identical (stza = stz there).

Assumption: None outside the window. A is read only by the cmp #14 at 1889 and is then reloaded by lda bactive at 1892.

```
        stza bcnt
```
->
```
        stz bcnt                    ; A = 8 or (Model B) 0: cmp #14 fails either way
```

On this path A = 8. Master: stz leaves A = 8, so nothing changes. Model B: A becomes 0 instead of 8. cmp #14 gives Z=0 for both 0 and 8, so bne :+ is taken as before, and the target reloads A with lda bactive. The carry from cmp #14 is never read: beq @bdone follows, and the exit test starts with sec.

Review: On this path A=8. Model B stz leaves A=0; cmp #14 still gives Z=0 (bne taken) and C=0 either way, and the target reloads A with lda bactive; no carry consumer before sec/lsr-clc. Master: stza is stz already.

## r039:2  src/logic.s:1891-1891  (window i1536)  M -0 B -2  [high]  review: accept

Cycles: bcnt==14 path: Model B 7 cycles fewer. Master identical.

Assumption: None outside the window. The next instruction (1892 lda bactive) reloads A and sets N,Z.

```
        stza bactive
```
->
```
        stz bactive
```

stza keeps A (=14) across the store, but the very next instruction is lda bactive. So A, and the flags the 6502 stz sets, are never read. The memory written is the same.

Review: The next instruction, lda bactive, overwrites A and N,Z, so the Model B's A=0 and flags are dead. Master unchanged.

## r039:3  src/logic.s:1906-1912  (window i1541)  M -3 B -3  [high]  review: accept

Cycles: Every path, both targets: tax 2 vs sta zp 3 (-1); cpx # 2 vs lda zp + cmp # 5 (-3).

Assumption: X is dead from here to the rts at 1926. player_update is reached only by the tail jump jmp player_update in game_frame (logic.s:1157). game_frame is called from game.s:251,254,299,303 via t_game_frame (a TOBANKI jmp on the Master, an alias on the Model B), and every caller goes on to lda exiting or jsr render_frame without reading X. t16 ($D1) is scratch: these writes are read back only here, and every other use of t16 writes it first.

```
        sta t16                     ; and low byte under the width
        lda px+1
        sbc exitx+1
        bne @noexit
        lda t16
        cmp #16
        bcs @noexit
```
->
```
        tax                         ; and low byte under the width
        lda px+1
        sbc exitx+1
        bne @noexit
        cpx #16
        bcs @noexit
```

tax does not affect C, so the borrow into sbc exitx+1 is unchanged. X holds the same low difference that t16 held, so cpx #16 sets C exactly as lda t16/cmp #16 did. The flags at bne and bcs are identical. Only X changes, and t16 is no longer written.

Review: tax keeps C for sbc exitx+1, and cpx #16 sets C as lda t16/cmp #16 did. X is dead: player_update is reached only by jmp player_update (logic.s:1157), and every game_frame caller (game.s:251-303) goes on to lda exiting or render_frame, which starts with wait_flip (lda flipreq). t16 is scratch.

## r039:4  src/logic.s:1916-1922  (window i1551)  M -3 B -3  [high]  review: accept

Cycles: Every path, both targets: -1 (tax) and -3 (cpx).

Assumption: As for the px block: X is dead to the rts (player_update is tail-jumped from game_frame, and the callers at game.s:251-303 do not read X); t16 is scratch.

```
        sta t16
        lda py+1
        sbc exity+1
        bne @noexit
        lda t16
        cmp #24
        bcs @noexit
```
->
```
        tax
        lda py+1
        sbc exity+1
        bne @noexit
        cpx #24
        bcs @noexit
```

Same as the px block: tax keeps C for the high-byte sbc, and cpx #24 on the same value sets C as lda t16/cmp #24 did.

Review: The same argument as the px block. tax keeps C for sbc exity+1, cpx #24 gives the same C, and X is dead to the callers.

## r039:5  src/logic.s:1923-1924  (window i1554)  M -2 B -2  [medium]  review: accept

Cycles: Exit path, both targets: lda #/sta zp (2+3 = 5) vs inc zp (5). Equal.

Assumption: exiting is only ever tested for zero or nonzero: game.s:300,304 (lda exiting / bne fl_over), and the tools bench.mjs and bdiff.mjs test truthiness. level_init clears it (logic.s:846). It is set only here and at 1254 (to 1, on the player_dead path, which never runs in the same call as player_update). frame_loop tests it after every game_frame, so it is 0 on entry. The one exception is the two unchecked game_frames at the start of a level (game.s:251,254), where it could reach 2 instead of 1. That is still nonzero, and it can never wrap to 0.

```
        lda #1
        sta exiting
```
->
```
        inc exiting                 ; 0 here in play: the loop leaves on any nonzero
```

With exiting = 0 on entry, inc gives 1, the same as lda #1/sta. Otherwise the result is still nonzero, and every reader tests only zero/nonzero. A is dead afterwards (rts to the caller, which reloads A).

Review: exiting is zero page and is read only as zero/nonzero (game.s:300,304; the tools only test truthiness). level_init clears it, and frame_loop leaves on any nonzero value, so it is 0 on entry except in the two unchecked start frames, where it can only reach 2, never wrap. Master and Model B both take 5 cycles; A is dead at the rts.

## r039:6  src/logic.s:1932-2037  (window i1587)  M -9 B -9  [high]  review: accept

Cycles: Gen path (type >= 2): asl/tax +4, bcs still taken (3), lda otype/asl/tax removed -7, net -3. Lean path (type 0/1): +4, bcs still not taken (2), jmp @lean removed -3, lda otype/asl/tax removed -7, net -6. The Model B deltas are the same, because the jmpx expansion is unchanged.

Assumption: (1) otype < 128 (the table has 13 entries, types 0..12), so asl carries out 0, and otype*2 >= NLEAN*2 (=4) exactly when otype >= NLEAN (=2, logic.s:191). (2) Neither prologue touches X: both use only lda/sta/sbc with ,y or zero page. (3) The handlers now start with a different A and C. A was otype*2 and is now ry+1 (lean) or fe+1 (gen); C was 0 and is now the borrow from the ry high byte; N,Z follow A (on the Model B, jmpx ends with pla). Every handler loads A, or sets C, before using it on every path: ob_star (already entered from po_star in exactly this state), ob_tramp lda O_AL,y then cmp/clc, ob_snake lda fc/cmp, ob_rsnake lda fb/ora then cmp #17 or the cmp in bgt16i, ob_bat lda fe/cmp, ob_walker inc fe/lda/cmp, ob_spike inc fa/lda/cmp, ob_flame lda frame then clc, ob_powerup lda fa then cmp #6 or lda fa/lsr, ob_vanish lda fe then either bitimm (C kept) and rts or inrange (cmp #2 before any C use), ob_switch lda fd then clc or inrange, ob_none rts. Where C reaches the rts (ob_none, ob_vanish @done), neither caller of process_object reads A, C or X: logic.s:1059 goes on to setbank/ldx, and 1100 to inc BINI/bra/ldx. V is unchanged: the last write to V is the sbc py+1, in both old and new. (4) @lean, @gen and @call are referenced only inside process_object (grep). (5) bcs @gen jumps over the lean prologue and jmpx, under 60 bytes on the Model B; the build confirms it assembles.

```
process_object:
        lda O_TYPE,y
        sta otype
        cmp #NLEAN                  ; types below NLEAN read and write the arrays in
        bcs @gen                    ; place; the rest are still staged through zero page
        jmp @lean
@gen:   lda O_XL,y
        sta ox
        sta spx
        sec
        sbc px
        sta rx
        lda O_XH,y
        sta ox+1
        sta spx+1
        sbc px+1
        sta rx+1
        lda O_YL,y
        sta oy
        sta spy
        sec
        sbc py
        sta ry
        lda O_YH,y
        sta oy+1
        sta spy+1
        sbc py+1
        sta ry+1
        lda O_AL,y
        sta fa
        lda O_AH,y
        sta fa+1
        lda O_BL,y
        sta fb
        lda O_BH,y
        sta fb+1
        lda O_CL,y
        sta fc
        lda O_CH,y
        sta fc+1
        lda O_DL,y
        sta fd
        lda O_DH,y
        sta fd+1
        lda O_EL,y
        sta fe
        lda O_EH,y
        sta fe+1
        lda otype
        asl
        tax
        jsr @call
        ; store back
        setbank BANK_LVL, BANK_LVL
        ldy obj
        lda fa
        sta O_AL,y
        lda fa+1
        sta O_AH,y
        lda fb
        sta O_BL,y
        lda fb+1
        sta O_BH,y
        lda fc
        sta O_CL,y
        lda fc+1
        sta O_CH,y
        lda fd
        sta O_DL,y
        lda fd+1
        sta O_DH,y
        lda fe
        sta O_EL,y
        lda fe+1
        sta O_EH,y
        rts
@lean:  ; Nothing staged.  Y is the object index throughout -- none of these handlers,
        ; nor anything they call, touches it -- so each reads and writes its own fields
        ; where they live: one cycle over a zero-page access, against seven to fetch a
        ; field and eight to put it back.  Every object field is 8-bit (tools/objaudit.py
        ; and tools/objfields.mjs are how that was established per handler).
        lda O_XL,y                  ; rx/ry = object relative to the player
        sta spx                     ; spx/spy = where it draws: sta touches no flags,
        sec                         ; so the source byte can be banked on the way past
        sbc px
        sta rx
        lda O_XH,y
        sta spx+1                   ; sta touches no flags, so C survives for the sbc
        sbc px+1
        sta rx+1
        lda O_YL,y
        sta spy
        sec
        sbc py
        sta ry
        lda O_YH,y
        sta spy+1
        sbc py+1
        sta ry+1
        lda otype
        asl
        tax
@call:  jmpx @tab                   ; tail dispatch: the handler returns to our caller
        ; (@call is the jsr entry for the staged path)
@tab:   .word ob_star, ob_tramp, ob_snake, ob_rsnake, ob_bat, ob_walker, ob_walker
        .word ob_spike, ob_none, ob_flame, ob_powerup, ob_vanish, ob_switch
```
->
```
process_object:
        lda O_TYPE,y
        sta otype
        asl                         ; X = otype*2, the dispatch index, for both paths:
        tax                         ; neither prologue below touches X
        cmp #NLEAN*2                ; types below NLEAN read and write the arrays in
        bcs @gen                    ; place; the rest are still staged through zero page
@lean:  ; Nothing staged.  Y is the object index throughout -- none of these handlers,
        ; nor anything they call, touches it -- so each reads and writes its own fields
        ; where they live: one cycle over a zero-page access, against seven to fetch a
        ; field and eight to put it back.  Every object field is 8-bit (tools/objaudit.py
        ; and tools/objfields.mjs are how that was established per handler).
        lda O_XL,y                  ; rx/ry = object relative to the player
        sta spx                     ; spx/spy = where it draws: sta touches no flags,
        sec                         ; so the source byte can be banked on the way past
        sbc px
        sta rx
        lda O_XH,y
        sta spx+1                   ; sta touches no flags, so C survives for the sbc
        sbc px+1
        sta rx+1
        lda O_YL,y
        sta spy
        sec
        sbc py
        sta ry
        lda O_YH,y
        sta spy+1
        sbc py+1
        sta ry+1
@call:  jmpx @tab                   ; tail dispatch: the handler returns to our caller
        ; (@call is the jsr entry for the staged path)
@gen:   lda O_XL,y
        sta ox
        sta spx
        sec
        sbc px
        sta rx
        lda O_XH,y
        sta ox+1
        sta spx+1
        sbc px+1
        sta rx+1
        lda O_YL,y
        sta oy
        sta spy
        sec
        sbc py
        sta ry
        lda O_YH,y
        sta oy+1
        sta spy+1
        sbc py+1
        sta ry+1
        lda O_AL,y
        sta fa
        lda O_AH,y
        sta fa+1
        lda O_BL,y
        sta fb
        lda O_BH,y
        sta fb+1
        lda O_CL,y
        sta fc
        lda O_CH,y
        sta fc+1
        lda O_DL,y
        sta fd
        lda O_DH,y
        sta fd+1
        lda O_EL,y
        sta fe
        lda O_EH,y
        sta fe+1
        jsr @call
        ; store back
        setbank BANK_LVL, BANK_LVL
        ldy obj
        lda fa
        sta O_AL,y
        lda fa+1
        sta O_AH,y
        lda fb
        sta O_BL,y
        lda fb+1
        sta O_BH,y
        lda fc
        sta O_CL,y
        lda fc+1
        sta O_CH,y
        lda fd
        sta O_DL,y
        lda fd+1
        sta O_DH,y
        lda fe
        sta O_EL,y
        lda fe+1
        sta O_EH,y
        rts
@tab:   .word ob_star, ob_tramp, ob_snake, ob_rsnake, ob_bat, ob_walker, ob_walker
        .word ob_spike, ob_none, ob_flame, ob_powerup, ob_vanish, ob_switch
```

The lean block moves from after the store-back to straight after the type test. It becomes the bcs fall-through, so jmp @lean goes. X = otype*2 is computed once, from A, which still holds otype after sta otype, and X survives both prologues, so each path's lda otype/asl/tax is dropped. cmp #NLEAN*2 on otype*2 splits the types exactly as cmp #NLEAN on otype did. @call (jmpx @tab) now ends the lean block. The gen path still reaches it with jsr @call, and its store-back and rts are unchanged. @tab stays where it was: after the rts, before po_star.

Review: Original matches 1932-2037. O_TYPE comes from level data, types 0..12, so asl carries out 0 and cmp #4 splits the types as cmp #2 did. Both prologues leave X alone. At handler entry A, C and N,Z now differ, but V is the same (last set by sbc py+1 in both old and new). Every handler loads A or sets C before using it: checked ob_tramp, snake, rsnake (@knocked starts with bgt16i: lda/cmp), bat (@dead: lda), walker, spike, flame, powerup (@adv cmp), vanish (@count keeps entry C only as far as rts), switch. The callers at 1059 (setbank) and 1100 (inc/bra/ldx) read no A or C. The Model B bcs @gen span is 53 bytes. Built on both targets.

## r039:7  src/logic.s:2038-2153  (window i1670)  M -3 B -3  [high]  review: accept

Cycles: Star-list path: jmp ob_star (3) removed, -3 on both targets. No other executed instruction changes.

Assumption: There is no PLACE or .segment between logic.s:1932 and ob_star (grep), so all of it is in LOGIC (Master) / LGCCODE (Model B). addscore ends in jmp bar_touch, so nothing falls through into the moved block. po_star has no cheap or anonymous labels. The branches in inrange, boomrel, boomready and addscore are all internal. po_star is reached only by jsr po_star (logic.s:1089). RNGTAB is data but is only moved, not changed: its contents stay the same, and it moves to a lower address by the size of po_star.

```
po_star:                            ; the lean prologue, then straight in: the star list
        lda O_XL,y
        sta spx
        sec
        sbc px
        sta rx
        lda O_XH,y
        sta spx+1
        sbc px+1
        sta rx+1
        lda O_YL,y
        sta spy
        sec
        sbc py
        sta ry
        lda O_YH,y
        sta spy+1
        sbc py+1
        sta ry+1
        jmp ob_star
ob_none:
        rts

; range check: rx > lo && rx < hi && ry > lo2 && ry < hi2 ; X = offset of the limit
; quad in RNGTAB (limits stored +128 so the test is an unsigned byte compare on r^$80,
; after checking r fits in -128..127 - anything wider fails every limit anyway).
; Returns carry set if inside. Clobbers A, X.
inrange:
        lda rx+1
        inca                         ; $FF -> 0, 0 -> 1, anything else >= 2
        cmp #2
        bcs @no
        lda rx
        eor rx+1                    ; low byte sign must agree with the high byte
        bmi @no
        lda rx
        eor #$80
        cmp RNGTAB,x
        bcc @no
        beq @no                     ; rx > lo
        cmp RNGTAB+1,x
        bcs @no                     ; rx < hi
        lda ry+1
        inca
        cmp #2
        bcs @no
        lda ry
        eor ry+1
        bmi @no
        lda ry
        eor #$80
        cmp RNGTAB+2,x
        bcc @no
        beq @no
        cmp RNGTAB+3,x
        bcs @no
        sec
        rts
@no:    clc
        rts
RNGTAB:                             ; inrange limit quads: lo, hi, lo2, hi2, each +128
        .byte 112, 144, 112, 148        ; 0: <-16, 16, <-16, 20
        .byte 120, 136, 120, 136        ; 4: <-8, 8, <-8, 8
        .byte 119, 145, 128, 136        ; 8: <-9, 17, 0, 8
        .byte 112, 144, 104, 140        ; 12: <-16, 16, <-24, 12
        .byte 120, 136, 112, 132        ; 16: <-8, 8, <-16, 4
        .byte 116, 140, 110, 130        ; 20: <-12, 12, <-18, 2
        .byte 118, 138, 120, 144        ; 24: <-10, 10, <-8, 16
        .byte 116, 140, 116, 140        ; 28: <-12, 12, <-12, 12
        .byte 112, 144, 116, 144        ; 32: <-16, 16, <-12, 16
        .byte 116, 140, 0, 255          ; 36: <-12, 12, <-128, 127
        .byte 112, 144, 104, 148        ; 40: <-16, 16, <-24, 20
        .byte 118, 138, 112, 136        ; 44: <-10, 10, <-16, 8
        .byte 120, 128, 104, 136        ; 48: <-8, 0, <-24, 8
        .byte 112, 144, 104, 140        ; 52: <-16, 16, <-24, 12
        .byte 112, 129, 143, 145        ; 56: <-16, 1, 15, 17
        .byte 112, 144, 104, 140        ; 60: <-16, 16, <-24, 12
        ; Guard bands: not "close enough to collect" but "the drawn rectangles touch".
        ; (These sat at 32/36 for a while, which pushed every quad after them along by
        ; eight without moving their callers: the bat read Cleo's band, the vanishing
        ; platforms never saw her feet, and the spike and powerup boxes were wrong.)
        .byte 105, 147, 113, 152        ; 64: Cleo      <-23, 19, <-15, 24
        .byte 111, 144, 118, 143        ; 68: boomerang <-17, 16, <-10, 15
        ; trampoline guard bands (its box (-16..8, 8..16) grown by the disturber's box,
        ; which the star bands imply is Cleo x(-15,13) y(-11,16), boomerang x(-9,10) y(-6,7))
        .byte 105, 157, 101, 136        ; 72: Cleo      <-23, 29, <-27, 8
        .byte 111, 154, 106, 127        ; 76: boomerang <-17, 26, <-22, -1

; boomerang-relative position: sx = spx - bx ; sy = spy - by  (uses spx/spy as the object's draw pos)
boomrel:
        dif16 sx, spx, bx
        dif16 sy, spy, by
        rts
; boomerang hit test helper: bactive && bcnt < 8 -> carry set
boomready:
        lda bactive
        beq @no
        lda bcnt
        cmp #8
        bcs @no
        sec
        rts
@no:    clc
        rts
addscore:                           ; A = points
  .if .defined(DBGHIT) .or .defined(DBGTILE)
        rts                         ; (debug: the score is a readout)
  .endif
        clc
        adc score
        sta score
        bcc :+
        inc score+1
:       jmp bar_touch

; ---------------------------------------------------------------- STAR (0)
```
->
```
ob_none:
        rts

; range check: rx > lo && rx < hi && ry > lo2 && ry < hi2 ; X = offset of the limit
; quad in RNGTAB (limits stored +128 so the test is an unsigned byte compare on r^$80,
; after checking r fits in -128..127 - anything wider fails every limit anyway).
; Returns carry set if inside. Clobbers A, X.
inrange:
        lda rx+1
        inca                         ; $FF -> 0, 0 -> 1, anything else >= 2
        cmp #2
        bcs @no
        lda rx
        eor rx+1                    ; low byte sign must agree with the high byte
        bmi @no
        lda rx
        eor #$80
        cmp RNGTAB,x
        bcc @no
        beq @no                     ; rx > lo
        cmp RNGTAB+1,x
        bcs @no                     ; rx < hi
        lda ry+1
        inca
        cmp #2
        bcs @no
        lda ry
        eor ry+1
        bmi @no
        lda ry
        eor #$80
        cmp RNGTAB+2,x
        bcc @no
        beq @no
        cmp RNGTAB+3,x
        bcs @no
        sec
        rts
@no:    clc
        rts
RNGTAB:                             ; inrange limit quads: lo, hi, lo2, hi2, each +128
        .byte 112, 144, 112, 148        ; 0: <-16, 16, <-16, 20
        .byte 120, 136, 120, 136        ; 4: <-8, 8, <-8, 8
        .byte 119, 145, 128, 136        ; 8: <-9, 17, 0, 8
        .byte 112, 144, 104, 140        ; 12: <-16, 16, <-24, 12
        .byte 120, 136, 112, 132        ; 16: <-8, 8, <-16, 4
        .byte 116, 140, 110, 130        ; 20: <-12, 12, <-18, 2
        .byte 118, 138, 120, 144        ; 24: <-10, 10, <-8, 16
        .byte 116, 140, 116, 140        ; 28: <-12, 12, <-12, 12
        .byte 112, 144, 116, 144        ; 32: <-16, 16, <-12, 16
        .byte 116, 140, 0, 255          ; 36: <-12, 12, <-128, 127
        .byte 112, 144, 104, 148        ; 40: <-16, 16, <-24, 20
        .byte 118, 138, 112, 136        ; 44: <-10, 10, <-16, 8
        .byte 120, 128, 104, 136        ; 48: <-8, 0, <-24, 8
        .byte 112, 144, 104, 140        ; 52: <-16, 16, <-24, 12
        .byte 112, 129, 143, 145        ; 56: <-16, 1, 15, 17
        .byte 112, 144, 104, 140        ; 60: <-16, 16, <-24, 12
        ; Guard bands: not "close enough to collect" but "the drawn rectangles touch".
        ; (These sat at 32/36 for a while, which pushed every quad after them along by
        ; eight without moving their callers: the bat read Cleo's band, the vanishing
        ; platforms never saw her feet, and the spike and powerup boxes were wrong.)
        .byte 105, 147, 113, 152        ; 64: Cleo      <-23, 19, <-15, 24
        .byte 111, 144, 118, 143        ; 68: boomerang <-17, 16, <-10, 15
        ; trampoline guard bands (its box (-16..8, 8..16) grown by the disturber's box,
        ; which the star bands imply is Cleo x(-15,13) y(-11,16), boomerang x(-9,10) y(-6,7))
        .byte 105, 157, 101, 136        ; 72: Cleo      <-23, 29, <-27, 8
        .byte 111, 154, 106, 127        ; 76: boomerang <-17, 26, <-22, -1

; boomerang-relative position: sx = spx - bx ; sy = spy - by  (uses spx/spy as the object's draw pos)
boomrel:
        dif16 sx, spx, bx
        dif16 sy, spy, by
        rts
; boomerang hit test helper: bactive && bcnt < 8 -> carry set
boomready:
        lda bactive
        beq @no
        lda bcnt
        cmp #8
        bcs @no
        sec
        rts
@no:    clc
        rts
addscore:                           ; A = points
  .if .defined(DBGHIT) .or .defined(DBGTILE)
        rts                         ; (debug: the score is a readout)
  .endif
        clc
        adc score
        sta score
        bcc :+
        inc score+1
:       jmp bar_touch

po_star:                            ; the lean prologue, then straight in: the star list
        lda O_XL,y
        sta spx
        sec
        sbc px
        sta rx
        lda O_XH,y
        sta spx+1
        sbc px+1
        sta rx+1
        lda O_YL,y
        sta spy
        sec
        sbc py
        sta ry
        lda O_YH,y
        sta spy+1
        sbc py+1
        sta ry+1
; ---------------------------------------------------------------- STAR (0)
```

po_star moves, unchanged, to immediately before ob_star, so its final jmp ob_star becomes a fall-through. ob_none, inrange, RNGTAB, boomrel, boomready and addscore move up by po_star's size, unchanged. It is a pure relocation.

Review: A pure move of po_star, which has no anonymous or cheap labels, to just before ob_star. addscore ends in jmp bar_touch and the @tab .word data comes before ob_none, so nothing falls into the moved code. The order of the ':' labels does not change. There is no segment change in the range. po_star is referenced only by jsr po_star. Build-checked: all eight candidates applied together save 24 bytes on the Master (LOGIC $2564->$254C) and 28 on the Model B (LGCCODE $2A0A->$29EE), exactly the claimed sums, and both assemble clean.

## r040:0  src/logic.s:2066-2086  (window i1692)  M -0 B -18  [high]  review: accept

Cycles: Model B, per axis: fits 25->12, hi byte out of range 18->13, sign mismatch 26->13 (inca costs 11 cycles there). Master: the original code, unchanged.

Assumption: Nothing outside the window: inrange's contract (C out, A/X clobbered) is unchanged. mtmp, which the Model B inca wrote, is expansion scratch that nothing reads afterwards. TEXT OVERLAP: r039's po_star relocation candidate (logic.s 2038-2153) quotes these lines verbatim, so apply that first or re-derive it after this one.

```
        lda rx+1
        inca                         ; $FF -> 0, 0 -> 1, anything else >= 2
        cmp #2
        bcs @no
        lda rx
        eor rx+1                    ; low byte sign must agree with the high byte
        bmi @no
        lda rx
        eor #$80
        cmp RNGTAB,x
        bcc @no
        beq @no                     ; rx > lo
        cmp RNGTAB+1,x
        bcs @no                     ; rx < hi
        lda ry+1
        inca
        cmp #2
        bcs @no
        lda ry
        eor ry+1
        bmi @no
```
->
```
  .if ::MODELB                      ; r fits -128..127 iff hi + (lo's sign) = 0 mod 256
        lda rx
        asl                         ; C = the low byte's sign
        lda rx+1
        adc #0                      ; $FF+1 and 0+0 are 0; nothing else is
        bne @no
  .else
        lda rx+1
        inca                         ; $FF -> 0, 0 -> 1, anything else >= 2
        cmp #2
        bcs @no
        lda rx
        eor rx+1                    ; low byte sign must agree with the high byte
        bmi @no
  .endif
        lda rx
        eor #$80
        cmp RNGTAB,x
        bcc @no
        beq @no                     ; rx > lo
        cmp RNGTAB+1,x
        bcs @no                     ; rx < hi
  .if ::MODELB
        lda ry
        asl
        lda ry+1
        adc #0
        bne @no
  .else
        lda ry+1
        inca
        cmp #2
        bcs @no
        lda ry
        eor ry+1
        bmi @no
  .endif
```

Model B only (.if ::MODELB). The old test passes iff hi is $FF or 0 and lo's sign agrees with hi. lda lo / asl puts lo's bit 7 in C. lda hi / adc #0 gives hi + sign mod 256, which is 0 exactly for (hi=0,sign=0) and (hi=$FF,sign=1); brute-forced over all 65536 values, identical. bne @no takes the fail exits (@no does clc/rts as before). Then the unchanged lda rx / eor #$80 compare. Same for ry. Master text unchanged. Checked by assembling a scratch copy: LGCCODE 10762 -> 10744, LOGIC unchanged.

Review: Original matches 2066-2086 verbatim. Model B only; Master text untouched. Test (hi + lo.bit7) mod 256 == 0 accepts exactly hi=0/sign 0 and hi=$FF/sign 1, the same as inca/cmp #2 plus the eor/bmi sign test. The carry out of adc is dead: the following lda/eor/cmp rewrites C. inrange callers see only C/A/X, and those are unchanged. mtmp is scratch that each expansion writes before it reads. Measured by assembly: Master 0, Model B LGCCODE -18. Faster on every path.

## r040:1  src/logic.s:2155-2172  (window i1737)  M -6 B -10  [high]  review: accept

Cycles: To @nostep: odd frame 12->12 (lda3 and2 bne3 + reload4 vs lda3 lsr2 lda4 bcs3); even capped 20->16; even step Master 26->22, Model B 35->22 (inca 11 -> adc 2). After @nostep: AL!=12 same; AL=12,CL!=0 18->11; AL=12,CL=0 23->21. Same shape on Model B.

Assumption: frame is zero page (logic.s:217), and bit 0 of its low byte is what and #1 tested. @nostep is referenced only by the two branches here (grep). The : at the old 2171 stays, so the first bne :+ keeps its target. @anim is within branch range (assembled). At @anim the state (A = O_CL,y nonzero, X, Y) is the same as on the old route through : lda O_CL,y / bne @anim.

```
        lda frame
        and #1
        bne @nostep
        lda O_AL,y
        cmp #18                     ; cap: a collected star's A must not wrap 8-bit
        bcs @nostep                 ; (it would make the star reappear ~every 20s).
        inca                        ; A still holds it: there is no inc abs,y
        sta O_AL,y
@nostep:
        lda O_AL,y
        cmp #12
        bne :+
        lda O_CL,y
        bne :+
        lda #0                      ; (no stz abs,y either)
        sta O_AL,y
:       lda O_CL,y
        bne @anim
```
->
```
        lda frame
        lsr                         ; C = frame bit 0: odd frames do not step
        lda O_AL,y                  ; A = the star's A on every way to @nostep
        bcs @nostep
        cmp #18                     ; cap: a collected star's A must not wrap 8-bit
        bcs @nostep                 ; (it would make the star reappear ~every 20s).
  .if ::MODELB
        adc #1                      ; C = 0: the bcs was not taken
  .else
        inca                        ; A still holds it: there is no inc abs,y
  .endif
        sta O_AL,y
@nostep:
        cmp #12
        bne :+
        lda O_CL,y
        bne @anim                   ; (the reload at : would take the same branch)
        sta O_AL,y                  ; A = 0 (no stz abs,y either)
:       lda O_CL,y
        bne @anim
```

lsr puts frame bit 0 in C (and #1 / bne tested the same bit). lda O_AL,y moves above the branch (loads do not touch C), so A = O_AL,y on the odd and capped exits, and on the step path A = the value just stored. The @nostep reload is redundant on all three. Model B: after bcs not taken C = 0, so adc #1 = inca (no carry-out, A < 18); the carry afterwards is not read (sta, then cmp #12). Master keeps inc a. Second part: if AL = 12 and CL != 0, the old code fell to : which reloaded CL and took bne @anim, so branch there at once. If CL = 0, A = 0 already, so lda #0 goes; sta O_AL,y then falls into the unchanged : lda O_CL,y / bne @anim. Checked by assembly: Master 6, Model B 10.

Review: Original matches 2155-2172. lsr A puts frame bit 0 in C, the same bit and #1 tested. lda O_AL,y leaves C alone. A = O_AL,y on every way into @nostep, so the reload is redundant. Model B adc #1 runs with C=0 known and A<18. @nostep is referenced only here. The ':' count is unchanged. bne @anim takes the same route the reload through ':' would have taken, and @anim reloads A. When CL=0, A=0 already, so the lda #0 can go. Odd path 12->12; every other path is faster. Measured: Master -6, Model B -10.

## r040:2  src/logic.s:2190-2195  (window i1758)  M -5 B -5  [high]  review: accept

Cycles: Collect path about 20 cycles faster (jsr bar_touch 6 + lda #1 2 + sta BARDIRTY 3/4 + rts 6, plus the lda #1 2). Other paths untouched. Both targets.

Assumption: bar_touch (logic.s:3223) is lda #1 / sta BARDIRTY / rts, and addscore (2142) ends in jmp bar_touch in the non-debug build. Nothing between reads BARDIRTY (its readers are render/menu code, engine.s:3088, not interrupts), so setting it once after the score add leaves the same state. A = 1 from lda #1 survives sta/dec to addscore. In DBGHIT/DBGTILE builds addscore is a bare rts, so the jsr bar_touch is kept there under the same .if as addscore's (both variants assemble).

```
        lda #1
        sta O_CL,y
        dec stars
        jsr bar_touch
        lda #1
        jsr addscore
```
->
```
        lda #1
        sta O_CL,y
        dec stars
  .if .defined(DBGHIT) .or .defined(DBGTILE)
        jsr bar_touch               ; (debug: addscore stops short of it)
  .endif
        jsr addscore                ; A = 1 still; it ends in jmp bar_touch
```

Old: dec stars; BARDIRTY=1 (A=1); A=1; score+=1; BARDIRTY=1 (A=1 on return). New: dec stars; score+=1 (A=1 still from lda #1 / sta O_CL,y, and dec zp does not touch A); BARDIRTY=1, A=1 on return. Final memory, A, Y (bar_touch/addscore do not touch Y) and the flags on return (from bar_touch's lda #1) are identical, and the following lda #SFX_STAR reloads A anyway. Checked by assembly: 5 bytes on each target; DBGHIT assembles.

Review: Original matches 2190-2195. bar_touch (3223) is lda #1/sta BARDIRTY/rts. Non-debug addscore ends in jmp bar_touch, so the earlier call only duplicates the flag set. BARDIRTY is read by the render loop (engine.s:3088), not asynchronously, and the end state is identical. A=1 survives sta abs,y/dec zp into addscore. The debug variant keeps the jsr under the same .if as addscore's rts. Measured: Master -5, Model B -5. Master DBGHIT builds -2. The Model B DBGHIT build already overflows B7 in the base; the candidate reduces the overflow.

## r040:3  src/logic.s:2133-2141  (window i1715)  M -3 B -3  [high]  review: accept

Cycles: Inactive 14->14 (incl rts). Active: bcnt<8 20->16, bcnt>=8 21->16. Same on both targets.

Assumption: The callers (logic.s:2413, 2527, 2698, 2898) read only C (bcc), and A/N/Z are rewritten before any use. The fall-through is jsr boomrel (sec/lda) or mov16 (lda). The bcc targets are @draw (2436, ble16i: lda), @hitp (2549, dif16: sec/lda), @player (2714: lda health) and @draw (2921, bmi16: bit). A on return changes (bcnt -> 7), as does Z on the active path; neither is read. bcnt is zero page (logic.s:233). TEXT OVERLAP: r039's po_star relocation (2038-2153) quotes these lines.

```
        lda bactive
        beq @no
        lda bcnt
        cmp #8
        bcs @no
        sec
        rts
@no:    clc
        rts
```
->
```
        lda bactive
        beq @no
        lda #7
        cmp bcnt                    ; C = 7 >= bcnt = bcnt < 8
        rts
@no:    clc
        rts
```

C = bactive != 0 && bcnt < 8. Active path: lda #7 / cmp bcnt gives C = (7 >= bcnt) = (bcnt < 8) for unsigned bcnt, returned directly instead of bcs @no / sec / rts. Inactive path unchanged. V untouched in both. Checked by assembly: 3 bytes on each target.

Review: Original matches 2133-2141. cmp bcnt with A=7 gives C = bcnt<=7 = bcnt<8, unsigned. All four callers (2413, 2527, 2698, 2898) do bcc. The fall-through (jsr boomrel / mov16) and the bcc targets (@draw ble16i lda, @hitp dif16 sec/lda, @player lda health, @draw bmi16 bit) rewrite A/N/Z before any read. Measured: -3 on both targets. Active paths are 4-5 cycles faster; the inactive path is unchanged.

## r040:4  src/logic.s:2034-2034  (window i1664)  M -0 B -2  [high]  review: accept

Cycles: Model B: every dispatch 7 cycles faster (pha 3 + pla 4 gone). Master unchanged (same jmp (abs,x)).

Assumption: A and N/Z at handler entry are dead: every @tab handler writes A or the flags before reading them. ob_star lda frame (logic.s:2155), ob_tramp lda O_AL,y (2265), ob_snake lda fc (2305), ob_rsnake lda fb (2474), ob_bat lda fe (2648), ob_walker inc fe/lda fe (2856), ob_spike inc fa/lda fa (2958), ob_flame lda frame (3002), ob_powerup lda fa (3016), ob_vanish lda fe (3046), ob_switch lda fd (3108). ob_none (rts, type 8) is reached only by the staged path (NLEAN=2, logic.s:191), which returns into the store-back whose first instruction is setbank's lda (logic.s:1985). C is untouched either way (pha/pla do not affect it). @call is a cheap label, so no self-modification can reach it. jv is the macro's own vector (engine.s:199).

```
@call:  jmpx @tab                   ; tail dispatch: the handler returns to our caller
```
->
```
@call:                              ; tail dispatch: the handler returns to our caller
  .if ::MODELB                      ; jmpx less its pha/pla: every handler loads A before
        lda @tab,x                  ; reading it (ob_none returns to a setbank or to
        sta jv                      ; the lean path's caller, which only gets types 0/1)
        lda @tab+1,x
        sta jv+1
        jmp (jv)
  .else
        jmpx @tab
  .endif
```

The Model B jmpx expansion is pha / lda tab,x / sta jv / lda tab+1,x / sta jv+1 / pla / jmp (jv). The pha/pla only preserve A (and set N,Z from it) across the vector load. With A and N/Z dead at every handler entry, the two are dropped. Master keeps jmp (@tab,x). @call still labels the first instruction, so jsr @call (logic.s:1983) enters at the same place. Checked by assembling a scratch copy: LGCCODE 10762 -> 10760, LOGIC unchanged.

Review: Original matches 2034. Every handler's first instruction writes A or N/Z (lda or inc zp): ob_star, tramp, snake, rsnake, bat, walker, spike, flame, powerup, vanish and switch were all checked. ob_none (type 8, staged only because NLEAN=2) returns into the store-back, which begins with setbank's lda. C and V are untouched either way, and X is unchanged. jsr @call still enters at the first instruction. Master is unchanged. Measured: Model B LGCCODE -2, 7 cycles faster per dispatch.

## r040:5  src/logic.s:2193-2195  (window i1758)  M -2 B -2  [high]  review: accept

Cycles: Collect path 2 cycles faster, both targets.

Assumption: FALLBACK for the 5-byte collect candidate at 2190-2195 (same lines, so it goes stale if that one applies). bar_touch (logic.s:3223-3226) is lda #1 / sta BARDIRTY / rts, so A = 1 on return.

```
        jsr bar_touch
        lda #1
        jsr addscore
```
->
```
        jsr bar_touch               ; returns A = 1 (lda #1 / sta BARDIRTY)
        jsr addscore
```

jsr bar_touch returns A = 1, and addscore wants A = 1 (the points), so the lda #1 is redundant. addscore's clc/adc set the flags it uses. Checked by assembly.

Review: Correct on its own: bar_touch returns A=1, which is the points addscore wants. Measured: -2 on both targets. It is the fallback to index 2 on the same lines, so it goes stale if index 2 is applied first.

## r041:0  src/logic.s:2207-2301  (window i1792)  M -57 B -57  [high]  review: accept

Cycles: Star box arm (EL!=0, frame<6), from the adc: old sta q1 3 + jsr 6 + [lda O_EH,bne] + ldx#64 2 + ... + rts 6 + lda q1 3 + jmp 3; new ldx#64 2 + bne 3 + sta q1 3 + [same body without the ldx] + lda q1 3 + jmp 3. Per exit: EH!=0 21->14; Cleo inside 23->14; bactive=0 (@yes) 31->22; boomerang tested: extra ldx#68 2 -> txa/ora/tax 6, @no 25->20, @yes 33->26. Trampoline: EL=0 arm beq taken 3 -> bne not taken 2; box arm beq not taken 2 -> bne taken 3, sta/jsr/rts/lda/jmp 21 -> ldx#72 2 + (sta 3 + lda q1 3 + jmp 3) with no jmp to reach box_safe (fall through): every path at least 7 cycles faster. Identical on both targets (no macros whose expansion changes, jmp/bne same on both).

Assumption: (1) star_safe and tramp_safe are called only from these two sites (grep over src/ and modelb/src: only logic.s:2208 and 2297; other hits are historical notes in opt/). (2) inrange (logic.s:2065-2100) never writes X -- it only indexes RNGTAB,x; its inca is inc a / sta-inc-lda mtmp -- and boomrel (dif16 x2, logic.s:2127) and mov16 touch only A: so X survives to the second inrange and X|4 gives 68/76 (64|4, 72|4). (3) Nothing inspects the stack (inrange, boomrel, addsprite are plain), so removing the jsr/rts pair is safe. (4) addsprite (engine.s:1381) reads only A, spx, spy and loads X/Y itself; X at entry differs on the EH!=0 path (was O_EL, now 64/72) and is ignored. q1 is still left holding the final frame. (5) The C=0 facts at @yes are the same as before (both ways in are a not-taken bcs after inrange). (6) ldx #64 sets Z=0, so the bne is always taken; target is 88 (Master) / 90 (Model B) bytes ahead, measured from the listing.

```
        sta q1
        jsr star_safe
        lda q1
        jmp m_addsprite
@regc:  sec
@reg:   adc #33                     ; +33 with C=1 is the old +34 with C=0
        jmp m_addsprite
@done:  rts
boxbase: .byte 103, 109

; A box star is an opaque rectangle, so if the same frame is already on screen in the
; same place its pixels are still right -- unless something has been drawn through
; them.  The converter marks the stars an enemy's range covers; the rest can still be
; walked through by Cleo or the boomerang, which the collect check tests for with a
; band widened from "close enough to pick up" to "the rectangles touch".  Safe ones
; are drawn under an alias id BOXN above the real one.
star_safe:
        lda O_EH,y
        bne @no
        ldx #64
        jsr inrange
        bcs @no
        lda bactive
        beq @yes
        jsr boomrel
        mov16 rx, sx
        mov16 ry, sy
        ldx #68
        jsr inrange
        bcs @no
@yes:   lda q1                      ; both ways in are a bcs that was not taken,
        adc #BOXN                   ; so the carry is already clear
        sta q1
@no:    rts

; The trampoline box is opaque the same way; nothing may draw through it if it is to be
; left alone.  rx/ry are still Cleo-relative here (ob_tramp does not call boomrel).
tramp_safe:
        lda O_EH,y                    ; an enemy's range covers it
        bne @no
        ldx #72
        jsr inrange                 ; Cleo overlaps its rectangle
        bcs @no
        lda bactive
        beq @yes
        jsr boomrel
        mov16 rx, sx
        mov16 ry, sy
        ldx #76
        jsr inrange                 ; the boomerang overlaps it
        bcs @no
@yes:   lda q1                      ; both ways in fall through a 'bcs @no' that was not
        adc #BOXN                   ; taken, so C is already clear here
        sta q1
@no:    rts

; ---------------------------------------------------------------- TRAMPOLINE (1)
ob_tramp:
        lda O_AL,y
        beq :+
        inca                        ; there is no inc abs,y, and A holds it anyway --
        sta O_AL,y                  ; which also saves the reload the old code did
        cmp #10
        bne :+
        lda #0                      ; nor stz abs,y
        sta O_AL,y
:       lda health
        beq @draw
        ldx #8
        jsr inrange
        bcc @draw
        lda vy+1                    ; was bmi16 vy / beq16 vy: one load does both tests
        bmi @draw
        ora vy
        beq @draw
        lda #1
        sta O_AL,y
        mov16i vy, -2048
        lda #SFX_JUMP
        sta SFXREQ
@draw:  lda O_AL,y
        clc
        adc #2
        lsr
        lsr                         ; bounce frame 0..2
        clc                         ; hoisted: serves both arms
        ldx O_EL,y
        beq @reg
        adc #115
        sta q1
        jsr tramp_safe
        lda q1
        jmp m_addsprite
@reg:   adc #43
        jmp m_addsprite
```
->
```
        ldx #64                     ; box_safe: Cleo's RNGTAB quad (the boomerang's is +4)
        bne box_safe                ; always: Z = 0 from the ldx
@regc:  sec
@reg:   adc #33                     ; +33 with C=1 is the old +34 with C=0
        jmp m_addsprite
@done:  rts
boxbase: .byte 103, 109

; A box star is an opaque rectangle, so if the same frame is already on screen in the
; same place its pixels are still right -- unless something has been drawn through
; them.  The converter marks the stars an enemy's range covers; the rest can still be
; walked through by Cleo or the boomerang, which the collect check tests for with a
; band widened from "close enough to pick up" to "the rectangles touch".  Safe ones
; are drawn under an alias id BOXN above the real one.  The trampoline box is opaque
; the same way; both go through box_safe (at the end of ob_tramp).

; ---------------------------------------------------------------- TRAMPOLINE (1)
ob_tramp:
        lda O_AL,y
        beq :+
        inca                        ; there is no inc abs,y, and A holds it anyway --
        sta O_AL,y                  ; which also saves the reload the old code did
        cmp #10
        bne :+
        lda #0                      ; nor stz abs,y
        sta O_AL,y
:       lda health
        beq @draw
        ldx #8
        jsr inrange
        bcc @draw
        lda vy+1                    ; was bmi16 vy / beq16 vy: one load does both tests
        bmi @draw
        ora vy
        beq @draw
        lda #1
        sta O_AL,y
        mov16i vy, -2048
        lda #SFX_JUMP
        sta SFXREQ
@draw:  lda O_AL,y
        clc
        adc #2
        lsr
        lsr                         ; bounce frame 0..2
        clc                         ; hoisted: serves both arms
        ldx O_EL,y
        bne @box
        adc #43
        jmp m_addsprite
@box:   adc #115
        ldx #72                     ; box_safe: Cleo's RNGTAB quad (the boomerang's is +4)
        ; fall through
; A = frame, X = the RNGTAB quad for Cleo (64 star, 72 trampoline; the boomerang's is
; X+4 -- inrange and boomrel leave X alone), C = 0.  Adds BOXN if nothing can draw
; through the box, then tail-calls m_addsprite.  (For the trampoline rx/ry are still
; Cleo-relative: ob_tramp does not call boomrel.)
box_safe:
        sta q1
        lda O_EH,y                  ; an enemy's range covers it
        bne @no
        jsr inrange                 ; Cleo overlaps its rectangle
        bcs @no
        lda bactive
        beq @yes
        jsr boomrel
        mov16 rx, sx
        mov16 ry, sy
        txa
        ora #4
        tax
        jsr inrange                 ; the boomerang overlaps it
        bcs @no
@yes:   lda q1                      ; both ways in fall through a 'bcs @no' that was not
        adc #BOXN                   ; taken, so C is already clear here
        sta q1
@no:    lda q1
        jmp m_addsprite
```

star_safe and tramp_safe were the same 49 bytes but for the two RNGTAB offsets (64/68 vs 72/76) and both callers did sta q1 / jsr / lda q1 / jmp m_addsprite. Merged into one box_safe that takes the Cleo quad in X (callers load 64 or 72 where the old routine did), derives the boomerang quad as X|4 (txa/ora #4/tax, A dead: next instruction is jsr inrange which loads A), takes the sta q1 inside, and tail-exits through @no: lda q1 / jmp m_addsprite (the @yes path's sta q1 falls into that reload). The trampoline arm is reordered (bne @box instead of beq @reg) so its box arm falls straight into box_safe; the star arm reaches it with bne after ldx #64. Carry: the hoisted clc still covers both trampoline arms; adc #115 / adc boxbase-1,x cannot overflow (A <= 117), and C is not relied on at entry anyway. Measured with ca65 + od65: LOGIC -57 (Master), LGCCODE -57 (Model B).

Review: Original matches 2207..2301. X survives inrange (only indexes RNGTAB,x; Model B inca uses mtmp $DF) and boomrel/mov16 (A only), so X|4 gives 68/76. star_safe/tramp_safe have no other references (src, modelb/src, tools). addsprite loads X/Y itself; q1 ends with the final frame on every exit; C=0 at @yes as before. bne box_safe assembles in range on both. Every path is faster (jsr/rts saved outweighs ldx+bne and txa/ora/tax). Assembled: LOGIC -57, LGCCODE -57.

## r041:1  src/logic.s:2267-2268  (window i1845)  M -0 B -3  [high]  review: accept

Cycles: Model B: inca (sta mtmp 3 + inc mtmp 5 + lda mtmp 3 = 11) -> clc 2 + adc #1 2 = 4. Master: unchanged (inc a).

Assumption: A = O_AL in 1..9 here (the beq :+ above skips 0; the counter resets at 10), so +1 never carries out. The carry inca preserves is dead: the cmp #10 two instructions later rewrites C, N, Z; V (which adc sets and inca did not) is never tested in ob_tramp or in what it tail-calls (no bvc/bvs; checked through @draw and m_addsprite). mtmp is expansion scratch nobody reads back. The line is unchanged verbatim in the r041 box_safe candidate, so this applies before or after it.

```
        inca                        ; there is no inc abs,y, and A holds it anyway --
        sta O_AL,y                  ; which also saves the reload the old code did
```
->
```
  .if MODELB
        clc                         ; the carry is dead: cmp #10 below sets it
        adc #1                      ; (inca would keep it, through mtmp, at 6 bytes)
  .else
        inca                        ; there is no inc abs,y, and A holds it anyway --
  .endif
        sta O_AL,y                  ; which also saves the reload the old code did
```

On the 6502 the inca macro spends 6 bytes to keep the carry; nothing here needs it, so clc/adc #1 gives the same A and N/Z. The Master keeps its 1-byte inc a via .if MODELB (the file already uses .if .not MODELB at 210/3172). Measured: LGCCODE -3, LOGIC 0.

Review: Original matches. A=O_AL is 1..9 so +1 never carries. The carry inca keeps is dead because cmp #10 rewrites it, V is never tested, and mtmp is scratch. Master is unchanged under .if MODELB. Also valid after candidate 0, which keeps the line verbatim, but lo/hi move. Assembled: LGCCODE -3.

## r041:2  src/logic.s:2305-2321  (window i1879)  M -7 B -12  [high]  review: accept

Cycles: fc<2, fe!=12: old lda fc 3 cmp 2 bcs 2 inc 5 lda 3 cmp 2 bne 3 = 20; new inc 5 lda 3 ldx 3 cpx 2 bcs 2 cmp 2 bne 3 = 20. fc<2, fe=12: Master 25->25 (stz 3 + bra 3 -> beq 3 + stz 3); Model B 34->27 (stza 12 + jmp 3 -> beq 3 + lda#0/sta 5). fc>=2, fe!=64: 21->21. fc>=2, fe=64: lda fc 3 -> txa 2, so 1 faster on both. All paths then reach @st: lda fc unchanged.

Assumption: X is free in ob_snake: it is reached through the staged path (type 2 >= NLEAN=2, logic.s:191) via jsr @call (jmpx, X=otype*2); nothing in ob_snake reads X before loading it (@c4 does tax, @coll does ldx #12, @boom ldx #16, @draw ldx fe, m_addsprite ldx NSPR); the store-back after jsr @call (setbank = bankimm lda/sta/wrsel, ldy obj, lda/sta) and the callers at logic.s:1059/1100 do not read X. fc ($B0) and fe ($B4) are distinct, so hoisting inc fe above the fc test changes nothing. A is dead at @z: @st reloads it with lda fc (so the Model B stz expansion is fine, and stza was never needed). @z is a new cheap label; no @z exists in ob_snake's scope (checked to ob_rsnake).

```
        lda fc
        cmp #2
        bcs @s23
        inc fe
        lda fe
        cmp #12
        bne @st
        stza fe
        bra @st
@s23:   inc fe
        lda fe
        cmp #64
        bne @st
        lda fc
        and #1
        sta fc
        stz fe
```
->
```
        inc fe                      ; both arms step the counter
        lda fe
        ldx fc                      ; X is free here (the handler never reads it before a load)
        cpx #2
        bcs @s23
        cmp #12
        bne @st
        beq @z                      ; fe = 12: back to 0
@s23:   cmp #64
        bne @st
        txa                         ; fc
        and #1
        sta fc
@z:     stz fe
```

Both arms began inc fe / lda fe: hoisted once, with the fc<2 test moved to X (ldx/cpx: same C as lda/cmp, same cycles). The fe=12 reset reuses the fe=64 arm's stz fe via beq @z (Z=1 from the cmp #12 that matched, and bne @st not taken), replacing stza fe + bra @st; on the Master stz does not touch flags and on the Model B it leaves A=0, both harmless since @st reloads A. In the fe=64 arm lda fc becomes txa (X = fc, unchanged since the ldx). Measured: LOGIC -7, LGCCODE -12.

Review: Original matches. fc/fe are distinct zp ($B0/$B4). X is dead in ob_snake until written: @c4 tax, ldx #12/#16, ldx fe, m_addsprite. The process_object store-back (setbank lda/sta, ldy obj) and both callers reload X. A is dead at @st (lda fc), so the Model B stz is fine. @z is unique in scope. Paths are equal or faster on both targets. Assembled: LOGIC -7, LGCCODE -12.

## r041:3  src/logic.s:2330-2330  (window i1904)  M -1 B -1  [high]  review: accept

Cycles: fc in {2,3,...}: jmp 3 -> bne taken 3, both targets.

Assumption: Z = 0 at this point: the only way here is beq @c5 not taken after cmp #5. @coll is 118 bytes ahead on the Model B in the current layout (fewer on the Master), within reach (all other candidates here only shrink the gap).

```
        jmp @coll
```
->
```
        bne @coll                   ; Z = 0: the cmp #5 did not match
```

jmp @coll after a cmp #5 / beq that fell through is an always-taken bne. Measured -1 on both.

Review: Only reachable through beq @c5 not taken after cmp #5, so Z=0 and bne is always taken. It is in range, and the other candidates only shrink the gap. Assembled -1/-1.

## r041:4  src/logic.s:2343-2345  (window i1915)  M -2 B -3  [high]  review: accept

Cycles: lda #1 2 + sta fc 3 = 5 -> inc fc 5; bra/jmp 3 -> bne taken 3. Same on the Master; Model B same.

Assumption: fc = 0 on this path: @c0 is entered only by @st's beq @c0 after lda fc; @pausef (logic.s ~2447) only loads fe; inc/lda/cmp on fb/fa do not touch fc. A is dead after: @coll begins add16 (clc / lda rx). A was 1 before, now fb+1; nothing reads it.

```
        lda #1
        sta fc
        bra @coll
```
->
```
        inc fc                      ; fc = 0 here (we came by beq @c0): 0 -> 1, Z = 0
        bne @coll
```

fc 0 -> 1 by inc leaves fc = 1 as lda #1/sta did and sets Z = 0, so the unconditional bra becomes bne. Measured Master -2, Model B -3.

Review: @c0 is reached only by @st's beq @c0 after lda fc, so fc=0. @pausef and the fb/fa code do not write fc. inc gives fc=1 with Z=0. A and flags are dead at @coll (add16: clc/lda). 5+3 cycles become 5+3. Assembled -2/-3.

## r041:5  src/logic.s:2355-2356  (window i1926)  M -0 B -5  [high]  review: accept

Cycles: Master: stz fc 3 + bra 3 -> sta fc 3 + beq taken 3. Model B: stza (pha 3 lda 2 sta 3 pla 4 = 12) + jmp 3 -> sta 3 + beq 3.

Assumption: A = 0 and Z = 1 here: the previous instruction pair is lda fb+1 / bne @coll, not taken. sta does not change flags. A after is 0 in both versions.

```
        stza fc
        bra @coll
```
->
```
        sta fc                      ; A = fb+1 = 0 (the bne above was not taken)
        beq @coll                   ; and Z = 1 from that load
```

Storing the zero that A already holds replaces stza (6 bytes on the 6502), and the Z=1 from lda fb+1 makes beq @coll unconditional. Master byte count unchanged (stz zp 2 = sta zp 2, bra 2 = beq 2). Measured LGCCODE -5.

Review: lda fb+1 / bne @coll not taken means A=0 and Z=1. sta keeps the flags, so beq is always taken. Master is the same size and cycles; Model B drops the 6-byte stza and the jmp. Assembled LGCCODE -5.

## r041:6  src/logic.s:2369-2370  (window i1938)  M -3 B -4  [high]  review: accept

Cycles: old clc 2 + lda 3 + adc 2 + sta 3 + lda 3 + adc 2 + sta 3 + bra/jmp 3 = 21; new no carry: lda 3 adc 2 sta 3 bcc 3 = 11; carry: 3+2+3+2 + inc 5 + bcs 3 = 18. Both targets.

Assumption: C = 0 on entry: the preceding bcs @coll (line 2368) was not taken. After the edit A and flags at @coll are dead: @coll's add16 starts clc / lda rx. fb is zero page ($AE).

```
        add16i fb, 16
        bra @coll
```
->
```
        lda fb                      ; C = 0: the bcs @coll above was not taken
        adc #16
        sta fb
        bcc @coll
        inc fb+1
        bcs @coll                   ; C = 1 still: inc leaves it
```

16-bit fb += 16 with the carry already clear: low byte add, and increment the high byte only on carry-out; inc leaves C = 1, so bcs @coll replaces the bra. fb ends identical. Measured Master -3, Model B -4.

Review: The bcs @coll just before this (after the placeholder ':') was not taken, so C=0. The low add followed by an inc of the high byte on carry gives the same fb. inc keeps C=1, so bcs is always taken. A and flags are dead at @coll. No anonymous labels change. Faster on every path. Assembled -3/-4.

## r041:7  src/logic.s:2372-2372  (window i1941)  M -3 B -3  [high]  review: accept

Cycles: old sec 2 + lda 3 + sbc 2 + sta 3 + lda 3 + sbc 2 + sta 3 = 18; new no borrow: lda 3 sbc 2 sta 3 bcs 3 = 11; borrow: 3+2+3+2 + dec 5 = 15. Both targets.

Assumption: C = 1 on entry: ble16i's last instruction is bcc @coll (macros.txt:1186), not taken. @coll follows immediately and begins add16 (clc / lda), so A and flags after are dead. The ble16i placeholder ':' is untouched.

```
        sub16i fb, 16
```
->
```
        lda fb                      ; C = 1: ble16i's bcc was not taken
        sbc #16
        sta fb
        bcs @coll
        dec fb+1
```

fb -= 16 with the carry already set: low byte subtract, decrement the high byte only on borrow, fall into @coll. Measured -3 on both.

Review: ble16i ends in ':' then bcc @coll, not taken, so C=1. The low subtract followed by a dec of the high byte on borrow gives the same fb. It falls into @coll, whose add16 starts with clc/lda. The ble16i placeholder is untouched. Faster. Assembled -3/-3.

## r042:0  src/logic.s:2355-2356  (window i1920)  M -0 B -5  [high]  review: accept

Cycles: Master: stz zp 3 + bra 3 -> sta zp 3 + beq taken 3 (6->6). Model B: stza 12 + jmp 3 -> 3 + 3 (15->6).

Assumption: The only way here is fall-through of `lda fb+1 / bne @coll` (logic.s:2353-2354), so A = 0 and Z = 1. sta does not touch flags. @coll (2373) starts add16 with clc, so no flag or A value is consumed there. @coll is about 60 bytes ahead on Model B, so it is in branch range.

```
        stza fc
        bra @coll
```
->
```
        sta fc                      ; A = 0: lda fb+1 / bne fell through
        beq @coll                   ; always: Z = 1 from that load
```

stza fc stored 0 and kept A (= 0). sta fc with A = 0 stores the same value and keeps A. bra becomes beq because Z is provably set. The Model B stza expansion (pha/lda #0/sta/pla) also left A = 0 and Z = 1, so the state afterwards is identical.

Review: Only entry is fall-through of lda fb+1/bne @coll (2353-2354): A=0, Z=1. sta keeps flags, beq always taken, @coll (add16 starts clc) reads neither A nor flags; @coll within range. fc=$B0 zp. Master stz zp2+bra2 -> sta2+beq2 (0); Model B stza 6 + jmp 3 -> 4 (5).

## r042:1  src/logic.s:2358-2358  (window i1920)  M -1 B -1  [high]  review: accept

Cycles: Both targets: -2 cycles (sec removed).

Assumption: @c4 is reached only by `cmp #4 / beq @c4` (logic.s:2326-2327). A grep of @c4 in the ob_snake scope finds no other reference, so C = 1 on entry from the equal compare. The instruction before @c4 (2356, bra or beq @coll) never falls through.

```
        sec                         ; B - A against the immediate 128, the way @c5
```
->
```
                                    ; C = 1 here (cmp #4 / beq @c4). B - A against the immediate 128, the way @c5
```

sec set C = 1, which the equal compare already guarantees. lda fb does not affect C, so the sbc fa sees C = 1 either way.

Review: @c4 referenced only by cmp #4/beq @c4 (2326-2327), so C=1; line before @c4 is an unconditional bra/beq @coll. sec redundant.

## r042:2  src/logic.s:2369-2369  (window i1920)  M -1 B -1  [high]  review: accept

Cycles: Both targets: 20 -> 18 cycles (clc dropped).

Assumption: Reached only by fall-through of `bcs @coll` (2368), so C = 0.

```
        add16i fb, 16
```
->
```
        lda fb                      ; C = 0: the bcs @coll above was not taken
        adc #<16
        sta fb
        lda fb+1
        adc #>16
        sta fb+1
```

add16i fb,16 expands to clc/lda/adc/sta/lda/adc/sta. The clc is redundant because C is already clear. Results and final flags are identical.

Review: Line 2369 has no label, reached only by fall-through of bcs @coll: C=0, clc redundant.

## r042:3  src/logic.s:2372-2372  (window i1920)  M -1 B -1  [high]  review: accept

Cycles: Both targets: 20 -> 18 cycles (sec dropped).

Assumption: Reached only by fall-through of `ble16i fb, -128, @coll` (2371). The macro's last instruction is `bcc @coll` (see macros.txt, ble16i), so C = 1.

```
        sub16i fb, 16
```
->
```
        lda fb                      ; C = 1: ble16i fell through its bcc
        sbc #<16
        sta fb
        lda fb+1
        sbc #>16
        sta fb+1
```

sub16i fb,16 expands to sec/lda/sbc/sta/lda/sbc/sta. The sec is redundant because C = 1 already. Results and flags are identical.

Review: Line 2372 unlabelled, reached by fall-through of ble16i's bcc @coll: C=1, sec redundant.

## r042:4  src/logic.s:2380-2408  (window i1943)  M -6 B -7  [high]  review: accept

Cycles: Stomp path: lda fc/cmp #2 (5 cycles) becomes one hoisted cmp #2 (2 cycles), so -3. bra -> bne is 3 -> 3 (Model B: jmp 3 -> bne taken 3). No-stomp path: 5 -> 2, so -3. The fc >= 4 exit and earlier exits are unchanged. Same on both targets.

Assumption: A = fc after the `lda fc / cmp #4 / bcs @boom` fall-through. Between the hoisted cmp #2 and both consumers (the stomp `bcc :+` and the @nostomp `bcs @boom`) only lda/ora/bmi/beq run (2383-2390), and none of them touches C. @nostomp is reached only from the four bmi/beq at 2384-2390; the @nostomp at 2720-2729 is in another routine's scope. SFX_KILL = 5 (logic.s:3312), so Z = 0 at the bra. fc is not modified before either consumer.

```
        lda fc
        cmp #4
        bcs @boom
        lda ry+1
        bmi @nostomp
        ora ry
        beq @nostomp
        lda vy+1
        bmi @nostomp
        ora vy
        beq @nostomp
        ; stomped
        lda fc
        cmp #2
        bcc :+
        lda #5
        jsr addscore
:       inc fc
        inc fc
        stz fe
        lda #1
        sta bounce
        lda #SFX_KILL
        sta SFXREQ
        bra @boom
@nostomp:
        lda fc
        cmp #2
        bcs @boom
```
->
```
        lda fc
        cmp #4
        bcs @boom
        cmp #2                      ; C = fc >= 2 for both arms: no op below touches C
        lda ry+1
        bmi @nostomp
        ora ry
        beq @nostomp
        lda vy+1
        bmi @nostomp
        ora vy
        beq @nostomp
        ; stomped
        bcc :+
        lda #5
        jsr addscore
:       inc fc
        inc fc
        stz fe
        lda #1
        sta bounce
        lda #SFX_KILL
        sta SFXREQ
        bne @boom                   ; always: A = SFX_KILL (5), Z = 0
@nostomp:
                                    ; C = fc >= 2 still: the cmp #2 above the stomp tests
        bcs @boom
```

One cmp #2 of fc replaces two identical `lda fc / cmp #2` pairs. A is dead at both sites: on the stomp arm A is reloaded by lda #5 or not read before inc fc/stz, and on the no-stomp arm the next load is lda hurt. bra @boom becomes bne @boom because Z is provably clear. Lines 2399-2401 are unchanged.

Review: A=fc after bcs @boom fall-through; lda/ora/bmi/beq between don't touch C; @nostomp referenced only by the four branches in ob_snake scope; fc unchanged before both consumers; A dead on both arms; SFX_KILL=5 so Z=0 at bne. fc zp: removes 2x(lda zp+cmp#)=8, adds 2 -> 6; Model B jmp->bne +1. Every path 5->2 cycles.

## r042:5  src/logic.s:2399-2401  (window i1937)  M -0 B -1  [medium]  review: accept

Cycles: Master unchanged. Model B: lda #0 2 + sta zp 3 + lda #1 2 + sta zp 3 = 10 -> 2+3+2+3 = 10.

Assumption: bounce and fe are zero page (logic.s:235, 263). The next instruction is lda #SFX_KILL (2402), which resets N and Z. C (now 1) is not read before @boom, and boomready (logic.s:2132) sets C itself. These three lines appear unchanged, word for word, inside the 2380-2408 candidate. If that one is applied first, find this one by its text. The .if block adds no anonymous labels.

```
        stz fe
        lda #1
        sta bounce
```
->
```
  .if MODELB
        lda #1                      ; 6502: one byte under stz fe / lda #1
        sta bounce
        lsr                         ; A = 0
        sta fe
  .else
        stz fe
        lda #1
        sta bounce
  .endif
```

On Model B, stz fe expands to lda #0/sta fe. The new order stores bounce = 1 first, then lsr makes A = 0 and stores fe = 0. The memory end state is identical, and A is reloaded next.

Review: Model B: lda#0/sta fe/lda#1/sta bounce (8) -> lda#1/sta bounce/lsr/sta fe (7), 10->10 cycles. C=1 afterwards but lda #SFX_KILL/sta/bra-or-bne @boom -> boomready (lda bactive, sets C itself) never reads it. MODELB is defined on both targets (logic.s uses .if .not MODELB at top level). Master untouched.

## r042:6  src/logic.s:2437-2444  (window i1978)  M -10 B -10  [high]  review: accept

Cycles: Both targets: the original costs 18 cycles to build t16 = fa+128 plus 17-19 for bge16, so 35-37 in total. The rewrite costs 22 when it falls through and 23 when it branches.

Assumption: C = 1 after the ble16i fall-through, because its last instruction is bcc. Value ranges: a snake's fa = q3*8 with q3 a byte (logic.s @t256, around lines 650-661), so fa <= 2040, and ob_snake never writes fa. fb starts at 0 and @c0/@c1/@c4/@c5 keep it roughly in [-144, fa+144]. So neither fa+128 nor fb-fa overflows 16 bits, which is the same assumption @c4 (2357-2368) already makes. t16 is dead afterwards: the bge16 being replaced is its only reader, and every other t16 user in logic.s, engine.s and menu.s writes it first. X is dead: the next use is ldx fe, or @f6 -> m_addsprite, and addsprite starts with ldx NSPR (engine.s:1381). The process_object callers also reload X (logic.s:1061 ldx bent, 1102 ldx BINI).

```
        clc
        lda fa
        adc #128
        sta t16
        lda fa+1
        adc #0
        sta t16+1
        bge16 fb, t16, @done
```
->
```
        lda fb                      ; B - A against 128, as @c4 (C = 1: ble16i fell through)
        sbc fa
        tax
        lda fb+1
        sbc fa+1
        eor #$80
        cpx #<128
        sbc #(>128 ^ $80)
:                                   ; placeholder - keeps the anonymous-label count
        bcs @done
```

Original: T = fa+128, then branch to @done if fb >= T (signed, with the V correction). New: D = fb-fa, then the biased compare D^$8000 >= $8080 (unsigned), which holds exactly when D >= 128 signed, i.e. when fb >= fa+128 as long as nothing overflows. bcs @done branches on the same condition.

Review: Line 2437 unlabelled, C=1 from ble16i fall-through. fa for type 2 = q3*8 (logic.s:657-668) <=2040, never written in ob_snake; fb bounded to about [-143, fa+143] by @c0/@c1/@c4/@c5, so neither fa+128 nor fb-fa overflows and D>=128 signed <=> fb>=fa+128. Biased compare verified. X clobber dead: fall-through reaches ldx fe or addsprite (ldx NSPR, engine.s:1382); @done returns to process_object callers which reload X (1061 ldx bent, 1093 ldx BINI) after setbank (A only). t16 has no other reader here. One ':' kept. 27 -> 17 bytes, 35-37 -> 22-23 cycles.

## r042:7  src/logic.s:2445-2453  (window i1987)  M -2 B -2  [high]  review: accept

Cycles: fc < 2 path: 3+2+2+2+3+4+3 = 19 -> 19 (and moved before bcs). fc >= 2 path: 3+2+3+2+2+3 = 15 -> 3+2+2+3+2+3 = 15.

Assumption: Nothing outside the window: fc is loaded at 2445, and ldx fe does not touch A.

```
        lda fc
        cmp #2
        bcs @f6
        ldx fe                      ; fe is 0..11 whenever fc < 2; the 0..63 ladder @s23
        and #1                      ; runs only while fc >= 2, and that takes @f6.  A is
        ora @ftab,x                 ; still fc: cmp/bcs/ldx do not touch it
        jmp m_addsprite
@f6:    and #1                      ; base is even, so ora == the old clc/adc
        ora #46+6
```
->
```
        lda fc
        cmp #2
        and #1                      ; and leaves C from the cmp: both arms want fc & 1
        bcs @f6
        ldx fe                      ; fe is 0..11 whenever fc < 2; the 0..63 ladder @s23
        ora @ftab,x                 ; runs only while fc >= 2, and that takes @f6.  A is
        jmp m_addsprite             ; fc & 1: cmp/and/bcs/ldx leave it
@f6:    ora #46+6                   ; base is even, so ora == the old clc/adc
```

`and #1` sets only N and Z, so bcs still tests the carry from cmp #2. Both arms applied `and #1` to the same A = fc before anything else. Hoisting one copy above the branch and deleting both originals gives the same A at each ora.

Review: and #1 affects only N/Z so bcs still tests cmp #2's C; both arms applied and #1 to A=fc first; @f6 referenced only from 2447. One and #1 removed, cycles equal on both paths.

## r042:8  src/logic.s:2497-2497  (window i2016)  M -2 B -2  [high]  review: accept

Cycles: Both targets: -3 cycles on both paths into @norst.

Assumption: @norst has two ways in: `bne @norst` (2484) after `lda fa / cmp #17`, where A = fa, and fall-through after `sta fa` (2490), where A = fa. A grep finds no other @norst in this scope (only 2484 and 2491).

```
        lda fa
```
->
```
                                    ; A = fa on both ways in (lda fa / cmp #17, or sta fa)
```

The reload is redundant because A already holds fa on both entries. The next instruction, eor #$80, sets its own flags.

Review: @norst referenced only by bne @norst (A=fa from lda fa/cmp #17) and fall-through after sta fa; eor #$80 sets its own flags. fa zp.

## r042:9  src/logic.s:2505-2505  (window i2023)  M -0 B -4  [high]  review: accept

Cycles: Master unchanged. Model B: inca (sta/inc/lda mtmp) 3+5+3 = 11 -> adc #0 2.

Assumption: @calc (2502) is reached only by fall-through of `cmp #113 / bcc @nowarm` (2499-2500); a grep finds no other reference to @calc. So C = 1, and lda fa, bpl and eor #$FF keep it. fa is -15..-1 here, so ~fa+1 is 1..15 and does not wrap. The carry afterwards (0) is not consumed: square starts with sta/sta/lda.

```
        inca
```
->
```
  .if MODELB
        adc #0                      ; C = 1: the bcc @nowarm was not taken
  .else
        inca
  .endif
```

With C = 1, adc #0 adds 1 exactly as inca does. The N and Z results may differ, but the next instruction is jsr square, which does not read flags.

Review: @calc unreferenced, reached by fall-through of bcc @nowarm: C=1, kept through lda/bpl/eor. fa in -15..-1 so result 1..15, no wrap. square (2629) starts sta q1, reads no flags; C was not otherwise consumed. Model B inca = 6 bytes (mtmp zp) -> 2; 11 -> 2 cycles.

## r042:10  src/logic.s:2507-2521  (window i2038)  M -9 B -9  [high]  review: accept

Cycles: Both targets: 3*(5+5) + 24 = 54 -> 3*(5+2) + 15 = 36.

Assumption: square (logic.s:2629-2645) ends with `sta t16 / rts`, so A = t16 low on return. t16 is dead afterwards. ob_rsnake does not read it again (2521-2626). boomready, boomrel, inrange, addscore, bar_touch, player_hit and addsprite do not read it either. Every other t16 user writes it first: logic.s 1906, 2659, 2700, 2777, 2807, 2820-2825 and 3265, engine.s 4572, and draw_number in menu.s.

```
        lsr t16+1
        ror t16
        lsr t16+1
        ror t16
        lsr t16+1
        ror t16
        sec
        lda t16
        sbc #28
        sta t16
        sta rise
        lda t16+1
        sbc #0
        sta t16+1
        sta rise+1
```
->
```
        lsr t16+1                   ; square left A = t16 low: shift it in A
        ror a
        lsr t16+1
        ror a
        lsr t16+1
        ror a
        sec
        sbc #28
        sta rise
        lda t16+1
        sbc #0
        sta rise+1
```

This is the same 16-bit shift right by 3, with the low byte held in A instead of memory (lsr hi / ror A), followed by rise = value - 28. The only difference is that t16 is left holding the unshifted low byte and the shifted high byte instead of the final value, and t16 is dead.

Review: square ends sta t16/rts so A=t16 low; lsr t16+1/ror a is the same 16-bit shift. t16 has no reader later in ob_rsnake, boomready/boomrel/inrange/player_hit/addscore/bar_touch/addsprite, or process_object's store-back; all other users write first. ror a syntax used elsewhere. 9 bytes, 54->36 cycles.

## r042:11  src/logic.s:2522-2524  (window i2041)  M -1 B -2  [high]  review: accept

Cycles: Master: lda # 2 + sta 3 + bra 3 = 8 -> inx 2 + stx 3 + bne 3 = 8. Model B: 2 + 3 + jmp 3 -> 8.

Assumption: square exits its loop with X = 0 (ldx #8 ... dex / bne @l, then sta t16 / rts; logic.s:2633-2645). Nothing between it and here touches X, whether lines 2507-2521 are the original or the rewrite above. X is dead afterwards: boomready and boomrel do not use X, ldx #20 comes before inrange, the rest of the routine loads X before using it (ldx #1, #24, #54 and so on), and the process_object callers reload X.

```
        lda #1
        sta q6                      ; snake visible
        bra @boom
```
->
```
        inx                         ; X = 0 from square (dex/bne): q6 = 1
        stx q6                      ; snake visible
        bne @boom                   ; always: inx left Z = 0
```

q6 = 1 either way. bra becomes a branch on Z = 0, which inx guarantees (X = 1).

Review: square exits dex/bne with X=0 and nothing up to 2522 touches X (original or candidate 10). inx->Z=0 so bne always. X dead: @boom path writes X (ldx #20) before inrange; @hitp path ldx #24 or @draw ldx #54 before any read. Master 6->5, Model B 7->5; 8->8 cycles.

## r042:12  src/logic.s:2526-2526  (window i2042)  M -0 B -2  [high]  review: accept

Cycles: Master stz 3 -> 3. Model B stza 12 -> stz 5.

Assumption: @boom calls boomready (logic.s:2132), which starts with lda bactive and reads no incoming flags. So A, and the N and Z that the 6502 stz sets, are dead.

```
        stza q6
```
->
```
        stz q6                      ; A dead: boomready loads it
```

stza keeps A across the store. A is not needed afterwards, so the cheaper stz (whose contract is that A is dead) is valid.

Review: @nowarm entered only by bcc @nowarm; falls into @boom: jsr boomready which begins lda bactive and reads no incoming flags, so A dead. Model B stza 6 bytes/12cy -> stz 4 bytes/5cy; Master identical.

## r043:0  src/logic.s:2502-2521  (window i2048)  M -9 B -13  [high]  review: accept

Cycles: fa>=0: 3x(ror t16 5 -> ror a 2) = -9, lda t16 -3, the two sta t16 stores -6: 18 fewer on both targets. fa<0: the same, and Model B also has inca 11 -> adc #0 2 (9 fewer).

Assumption: @calc is referenced nowhere: it is reached only when bcc @nowarm at 2500 falls through, so C=1 there, and lda/bpl/eor keep C. square (logic.s:2629-2645) ends 'sta t16 / rts', so A = t16 low on return. t16 is dead afterwards: nothing in ob_rsnake (2473-2626) reads it, nor do boomready, boomrel, inrange, addscore, bar_touch, player_hit or addsprite (logic.s:2132,2127,2065,2142,3223,1175; engine.s:1381), and process_object's store-back (logic.s:1985) does not read it.

```
@calc:  lda fa
        bpl :+
        eor #$FF
        inca
:       jsr square
        lsr t16+1
        ror t16
        lsr t16+1
        ror t16
        lsr t16+1
        ror t16
        sec
        lda t16
        sbc #28
        sta t16
        sta rise
        lda t16+1
        sbc #0
        sta t16+1
        sta rise+1
```
->
```
@calc:  lda fa
        bpl :+
        eor #$FF
  .if MODELB
        adc #0                      ; C = 1: the cmp #113 fell through (inca is 6 bytes)
  .else
        inca
  .endif
:       jsr square                  ; returns A = t16, the low byte
        lsr t16+1                   ; rise = (A*A >> 3) - 28, the low byte shifted in A;
        ror a                       ; t16 itself is dead after this
        lsr t16+1
        ror a
        lsr t16+1
        ror a
        sec
        sbc #28
        sta rise
        lda t16+1
        sbc #0
        sta rise+1
```

A = t16 on return, so lsr t16+1/ror a x3 is the same 16-bit shift with the low byte held in A. sec/sbc #28/sta rise then lda t16+1/sbc #0/sta rise+1 give an identical rise. The only effect lost is t16 holding the result, and nothing reads it. Model B: eor #$FF then adc #0 with C=1 adds 1, the same as inca. Its carry out is 0 (A<=$7F), and square's first asl overwrites C anyway. The Master keeps inc a (1 byte). Checked by assembling: LOGIC -9, LGCCODE -13.

Review: Original matches 2502-2521. @calc is reached only by the fall-through of bcc @nowarm, so C=1, and lda/bpl/eor keep it. fa is -15..16 here, so adc #0 adds exactly 1, like inca. square ends sta t16/rts, so A = t16 low. t16 is not read afterwards in ob_rsnake, boomready, boomrel, inrange, addscore/bar_touch, player_hit (incl. DBGHIT), addsprite or process_object's store-back, and the @nowarm/@knocked paths never wrote it anyway. Assembled: LOGIC -9, LGCCODE -13.

## r043:1  src/logic.s:2522-2526  (window i2048)  M -0 B -3  [high]  review: accept

Cycles: Warm path: bra (jmp, 3) -> bne taken (3), no change. Nowarm path on Model B: stza 12 -> stz 5. Master identical (bne = bra, stz = stza).

Assumption: A=1 at the bne (lda #1 just above). @nowarm is reached only by the bcc at 2500. A is dead at @boom: boomready (logic.s:2132) starts with lda bactive and sets C itself with sec/clc; nothing reads N/Z.

```
        lda #1
        sta q6                      ; snake visible
        bra @boom
@nowarm:
        stza q6
```
->
```
        lda #1
        sta q6                      ; snake visible
        bne @boom                   ; always: A = 1
@nowarm:
        stz q6                      ; A is dead: boomready loads it
```

After lda #1, Z=0, so bne @boom is always taken, like bra. On Model B, stz is lda #0/sta q6: it destroys A, which is dead, and leaves C alone.

Review: A=1 so bne is always taken (3 cycles, the same as the Model B jmp). boomready starts lda bactive, so A is dead at @boom on the @nowarm path, and no flag is consumed. Assembled: 0/-3.

## r043:2  src/logic.s:2555-2562  (window i2075)  M -4 B -4  [high]  review: accept

Cycles: 40 -> 36 cycles on both targets (one path).

Assumption: X and Y are dead here. X is reloaded by ldx #24 at 2563. Nothing reads Y: not ob_rsnake, not inrange (logic.s:2065, which uses only ,x), not player_hit (1175-1219), and addsprite writes Y before reading it. process_object reloads Y with ldy obj after the handler (logic.s:1985). inrange ignores incoming C/V: its first flag use follows cmp #2, and it returns C with sec/clc.

```
        clc                         ; ry = oy + rise, in one pass
        lda oy
        adc rise
        sta ry
        lda oy+1
        adc rise+1
        sta ry+1
        sub16 ry, py
```
->
```
        sec                         ; ry = oy - py + rise, in one pass: the
        lda oy                      ; difference waits in X (low) and Y (high)
        sbc py
        tax
        lda oy+1
        sbc py+1
        tay
        clc
        txa
        adc rise
        sta ry
        tya
        adc rise+1
        sta ry+1
```

ry = (oy + rise) - py mod 2^16 = (oy - py) + rise. The 16-bit difference is kept in X/Y, then rise is added. The final ry is identical. Afterwards only C/V differ, and neither ldx #24 nor inrange reads them.

Review: ry = oy - py + rise mod 2^16, the same value. X is reloaded by ldx #24. Y is never read before a write on any path: inrange, player_hit (the DBGHIT variant uses X), addsprite (ldy sprmul5,x), then process_object's ldy obj. The staged path is taken for types >= NLEAN=2, and rsnake is type 3. inrange consumes no incoming flag before cmp #2. 40->36 cycles. Assembled: -4/-4.

## r043:3  src/logic.s:2579-2585  (window i2097)  M -12 B -12  [high]  review: accept

Cycles: stx q1 3 + inc q1 5 + mov16/add16 32 + lda q1 3 -> inx 2 + 20 + txa 2. Faster on both paths and both targets.

Assumption: bge16 (macros.txt) touches only A. addsprite (engine.s:1381) takes A, spx and spy and starts with ldx NSPR, so X on entry does not matter; it does not read q1. Nothing reads q1 after @fr (the @basket tail loads A #60).

```
@fr:    stx q1
        bge16 px, ox, :+
        inc q1
:       mov16 spy, oy
        add16 spy, rise             ; snake Y = oy + parabola (was t16b: wrong)
        lda q1
        jsr m_addsprite
```
->
```
@fr:    bge16 px, ox, :+
        inx                         ; the frame stays in X: nothing below touches it
:       clc                         ; snake Y = oy + parabola, in one pass
        lda oy
        adc rise
        sta spy
        lda oy+1
        adc rise+1
        sta spy+1
        txa
        jsr m_addsprite
```

X holds the frame, so the conditional increment is done in X, and txa replaces lda q1 (same value, same N/Z). spy = oy + rise in one pass gives the same final value and the same C/V/N/Z from the last adc.

Review: X = 54/56/58 on all three ways into @fr. bge16 touches only A, and inx replaces inc q1. addsprite ignores X and q1. q1 is not read after ob_rsnake: every other reader writes it first, and the @basket-only path never wrote it anyway. spy is computed in one pass, with the same value. Assembled: -12/-12.

## r043:4  src/logic.s:2593-2593  (window i2103)  M -2 B -2  [high]  review: accept

Cycles: jmp @done (3) + rts (6) = 9 -> rts 6.

Assumption: @done (2626) is 'rts', and the stack is the same at both points.

```
        jmp @done
```
->
```
        rts
```

A jmp to an rts is equivalent to an rts.

Review: @done is an rts and the stack is the same at both points. Assembled: -2/-2.

## r043:5  src/logic.s:2599-2599  (window i2108)  M -1 B -1  [high]  review: accept

Cycles: fb=1 path: clc removed, 2 fewer cycles. Other path unchanged.

Assumption: Line 2599 has no label and is reached only when 'cmp #1 / bne :+' at 2597-2598 falls through, so Z=1 and C=1 there.

```
        add16i fd, 16
```
->
```
        lda fd                      ; fd += 16: C = 1 from the cmp #1 (equal), so 15
        adc #15
        sta fd
        lda fd+1
        adc #0
        sta fd+1
```

add16i fd,16 is clc/lda fd/adc #16/... With C=1, adc #15 gives the same sum, carry out and V (15 and 16 are both positive and the result is the same). The high-byte adc #0 is unchanged.

Review: It falls through bne after cmp #1 only when they are equal, so C=1. adc #15 with C=1 gives the same sum, carry and V as clc/adc #16. Assembled: -1/-1.

## r043:6  src/logic.s:2602-2615  (window i2120)  M -5 B -6  [high]  review: accept

Cycles: ox>px: ldx 2 + T + ldx 2 + stx 3 + lda q1 3 -> ldx 2 + T + txa 2 (6 fewer). Otherwise: 2 + NT + bra 3 + stx 3 + lda 3 -> 2 + NT + ldx 2 + txa 2 (5 fewer; Model B's jmp is also 3 cycles).

Assumption: Nothing else reads q1 (addsprite does not). The clc/lda/adc/sta block leaves X alone.

```
:       ldx #54
        bgt16 ox, px, :+
        bra :++
:       ldx #55
:       stx q1
        clc                         ; spy = oy + fc in one pass, the way the ox + fd
        lda oy                      ; code twelve lines below already does it
        adc fc
        sta spy
        lda oy+1
        adc fc+1
        sta spy+1
        lda q1
        jsr m_addsprite
```
->
```
:       ldx #55
        bgt16 ox, px, :+
        ldx #54
:       clc                         ; spy = oy + fc in one pass, the way the ox + fd
        lda oy                      ; code twelve lines below already does it
        adc fc
        sta spy
        lda oy+1
        adc fc+1
        sta spy+1
        txa                         ; the frame is still in X
        jsr m_addsprite
```

X = 55 if ox>px, else 54, as before: bgt16 branches over ldx #54. txa is equivalent to lda q1.

Review: X=55 when ox>px, else 54. bgt16's bmi now reaches ': clc', and bra :++ at 2600 still reaches the ldx line, as the listing checks (bra 80 0D -> ldx #55). There are no :- refs in 2400-2900. q1 is dead afterwards. Assembled: -5/-6.

## r043:7  src/logic.s:2626-2626  (window i2131)  M -1 B -1  [high]  review: accept

Cycles: None: the line is dead code.

Assumption: REQUIRES C5 (line 2593) to be applied first. After C5, nothing references @done: its only use was jmp @done at 2593, and its @ scope runs from ob_rsnake to square. Line 2625 is jmp m_addsprite, so the line cannot be reached by falling through.

```
@done:  rts
```
->
```

```

Deletes an unreachable rts.

Review: Valid only with C4. Then @done has no reference: the grep's only use was 2593, and each other @done is in a different scope. It follows jmp m_addsprite, so it is unreachable. Assembled with C4: -3/-3, i.e. -1/-1 for this one.

## r043:8  src/logic.s:2631-2632  (window i2135)  M -0 B -4  [high]  review: accept

Cycles: Master: stz zp 3 -> sta zp 3. Model B: stza (pha/lda/sta/pla) 12 -> sta 3.

Assumption: A = 0 from the lda #0 on the line above.

```
        lda #0                      ; accumulator low byte lives in A
        stza t16+1                  ; stz never touches A on a 65C02
```
->
```
        lda #0                      ; accumulator low byte lives in A
        sta t16+1                   ; and the high byte starts at the same zero
```

t16+1 becomes 0 either way and A stays 0. ldx #8 then sets the flags.

Review: A=0 from lda #0, so sta t16+1 stores the same zero. The next flag consumer comes after ldx #8/asl. Assembled: 0/-4.

## r043:9  src/logic.s:2653-2657  (window i2156)  M -4 B -8  [high]  review: accept

Cycles: No wrap: 13 -> 7. Wrap: Master 15 -> 7, Model B 17 -> 7 (stza 12).

Assumption: The ':' at 2652 is reached only by bcc :+ at 2650, after 'lda fe / cmp #8', so A = fe < 8 and C = 0. After this block, mov16/asr16 load A and set C with cmp #$80 before anything uses it. Nothing reads V before add16 sets it again.

```
        inc fe
        lda fe
        cmp #8
        bne :+
        stza fe
```
->
```
        adc #1                      ; A = fe < 8 and C = 0 from the bcc:
        and #7                      ; fe = (fe + 1) & 7
        sta fe
```

fe = (fe+1) & 7 is inc fe with the wrap from 8 to 0, since fe < 8. A ends as the new fe (it was fe, or 0/8), and A is dead.

Review: The ':' at 2652 is reached only from bcc :+ at 2650, with A=fe<8 and C=0, and no :- ref exists. (fe+1)&7 is the inc with its wrap at 8. A and N/Z/V differ afterwards, but mov16 loads A, asr16's cmp sets C, and add16 sets V before any use. Assembled: -4/-8.

## r043:10  src/logic.s:2659-2666  (window i2164)  M -22 B -22  [high]  review: accept

Cycles: Per half: mov16 12 + asr16 15 + add16 20 + add16 20 = 67 -> 50 cycles. Same for both halves.

Assumption: X and Y are dead. ob_bat reads X only after a tax (the @draw wobble) and never reads Y (grep of ob_bat 2647-2855); process_object reloads Y. This block no longer writes t16, but every later read of t16 in ob_bat comes after a write (@boom mov16 t16,rx; @wx2 sta t16; @dead sta t16).

```
        mov16 t16, fc
        asr16 t16
        add16 rx, t16
        add16 spx, t16
        mov16 t16, fd
        asr16 t16
        add16 ry, t16
        add16 spy, t16
```
->
```
        lda fc+1                    ; X:Y = fc >> 1 (arithmetic); rx += it, spx += it
        cmp #$80
        ror a
        tax
        lda fc
        ror a
        tay
        clc
        adc rx
        sta rx
        txa
        adc rx+1
        sta rx+1
        clc
        tya
        adc spx
        sta spx
        txa
        adc spx+1
        sta spx+1
        lda fd+1                    ; and fd >> 1 into ry, spy
        cmp #$80
        ror a
        tax
        lda fd
        ror a
        tay
        clc
        adc ry
        sta ry
        txa
        adc ry+1
        sta ry+1
        clc
        tya
        adc spy
        sta spy
        txa
        adc spy+1
        sta spy+1
```

X:Y = fc >> 1 (arithmetic), built exactly as asr16 builds t16: cmp #$80 sets C to the sign, then ror hi, ror lo. rx += X:Y swaps the addends, which is commutative and gives the same sum, C and V. spx += X:Y as add16 did. The final rx/spx/ry/spy are identical, and so are the final flags, because the last operation is the same add.

Review: This builds the same arithmetic >>1 as asr16, in X:Y. The adds are commutative, so rx/spx/ry/spy and the final flags come out identical. X is next read after a tax or ldx, Y is never read in ob_bat, and process_object reloads Y. Every later t16 read in ob_bat (@boom, @wx2, @dead) follows a write. Assembled: -22/-22.

## r043:11  src/logic.s:2669-2671  (window i2167)  M -2 B -3  [high]  review: accept

Cycles: fc<fa: bmi taken 3 -> bpl not taken 2. fc>=fa: bmi not taken 2 + bra 3 -> bpl taken 3.

Assumption: bge16 and blt16 build the same signed compare (macros.txt) and differ only in bpl vs bmi. @ydir is 32 bytes ahead, so bpl reaches it (listing: 10 20).

```
        blt16 fc, fa, :+
        bra @ydir
:       inc fc
```
->
```
        bge16 fc, fa, @ydir
:       inc fc                      ; (label kept: the anonymous count is unchanged)
```

If fc >= fa, skip to @ydir; otherwise fall into the increment. This is the same as blt16 to the increment followed by bra @ydir.

Review: This is the same signed compare, with bpl to @ydir replacing bmi-over-bra, and bpl reaches @ydir. The ': inc fc' is kept, so the anonymous count is unchanged. Assembled: -2/-3.

## r043:12  src/logic.s:2502-2645  (window i2048)  M -40 B -52  [medium]  review: accept

Cycles: The savings of C1..C9 plus jsr square 6 + rts 6 + sta t16 3 removed (15 more fewer). No path is slower.

Assumption: ALTERNATIVE to applying C1..C9 separately (same range; it supersedes them). It is C1..C9 applied, then square inlined at its call site and deleted. square has one caller (logic.s:2506); grep finds no other reference in src, modelb or tools. The inlined loop's cheap label is @sq, which is unused in ob_rsnake's scope. All the C1..C9 assumptions apply.

```
@calc:  lda fa
        bpl :+
        eor #$FF
        inca
:       jsr square
        lsr t16+1
        ror t16
        lsr t16+1
        ror t16
        lsr t16+1
        ror t16
        sec
        lda t16
        sbc #28
        sta t16
        sta rise
        lda t16+1
        sbc #0
        sta t16+1
        sta rise+1
        lda #1
        sta q6                      ; snake visible
        bra @boom
@nowarm:
        stza q6
@boom:  jsr boomready
        bcc @hitp
        jsr boomrel
        mov16 rx, sx
        mov16 ry, sy
        ldx #20
        jsr inrange
        bcc @hitp
        lda #4
        jsr addscore
        ldx #1
        bgt16 ox, px, :+
        ldx #2
:       stx fb
        stz fb+1
        lda q6
        beq :+
        mov16 fc, rise
:       lda #8
        sta bcnt
        lda #SFX_KILL
        sta SFXREQ
@hitp:  dif16 rx, ox, px            ; the boomerang test above left rx boomerang-
        lda q6                      ; relative; on a miss it fell through here with
        beq @draw                   ; that x, so a boomerang passing the snake while
                                    ; Cleo stood at its height read as Cleo touching it
        lda health
        beq @draw
        clc                         ; ry = oy + rise, in one pass
        lda oy
        adc rise
        sta ry
        lda oy+1
        adc rise+1
        sta ry+1
        sub16 ry, py
        ldx #24
        jsr inrange
        bcc @draw
        lda hurt
        bne @draw
        mov16 hx, rx
        jsr player_hit
@draw:  lda q6
        beq @basket
        ldx #54
        lda fa
        bmi @fr
        ldx #56
        cmp #0                      ; A still holds fa; ldx did not touch it
        beq @fr
        ldx #58
@fr:    stx q1
        bge16 px, ox, :+
        inc q1
:       mov16 spy, oy
        add16 spy, rise             ; snake Y = oy + parabola (was t16b: wrong)
        lda q1
        jsr m_addsprite
@basket:
        mov16 spx, ox
        mov16 spy, oy
        lda #60
        jmp m_addsprite
@knocked:
        bgt16i fc, -256, :+
        jmp @done
:
        sub16i fc, 16
        lda fb
        cmp #1
        bne :+
        add16i fd, 16
        bra :++
:       sub16i fd, 16
:       ldx #54
        bgt16 ox, px, :+
        bra :++
:       ldx #55
:       stx q1
        clc                         ; spy = oy + fc in one pass, the way the ox + fd
        lda oy                      ; code twelve lines below already does it
        adc fc
        sta spy
        lda oy+1
        adc fc+1
        sta spy+1
        lda q1
        jsr m_addsprite
        clc
        lda ox
        adc fd
        sta spx
        lda ox+1
        adc fd+1
        sta spx+1
        mov16 spy, oy
        lda #60
        jmp m_addsprite
@done:  rts

; t16 = A * A (A unsigned 0..128)
square: sta q1
        sta q1x
        lda #0                      ; accumulator low byte lives in A
        stza t16+1                  ; stz never touches A on a 65C02
        ldx #8
@l:     asl
        rol t16+1
        asl q1
        bcc :+
        clc
        adc q1x
        bcc :+
        inc t16+1
:       dex
        bne @l
        sta t16
        rts
```
->
```
@calc:  lda fa
        bpl :+
        eor #$FF
  .if MODELB
        adc #0                      ; C = 1: the cmp #113 fell through (inca is 6 bytes)
  .else
        inca
  .endif
:       sta q1                      ; A*A inline (square had one caller): the low
        sta q1x                     ; byte ends in A, the high byte in t16+1
        lda #0
        sta t16+1
        ldx #8
@sq:    asl
        rol t16+1
        asl q1
        bcc :+
        clc
        adc q1x
        bcc :+
        inc t16+1
:       dex
        bne @sq
        lsr t16+1                   ; rise = (A*A >> 3) - 28, the low byte shifted in A;
        ror a                       ; t16 itself is dead after this
        lsr t16+1
        ror a
        lsr t16+1
        ror a
        sec
        sbc #28
        sta rise
        lda t16+1
        sbc #0
        sta rise+1
        lda #1
        sta q6                      ; snake visible
        bne @boom                   ; always: A = 1
@nowarm:
        stz q6                      ; A is dead: boomready loads it
@boom:  jsr boomready
        bcc @hitp
        jsr boomrel
        mov16 rx, sx
        mov16 ry, sy
        ldx #20
        jsr inrange
        bcc @hitp
        lda #4
        jsr addscore
        ldx #1
        bgt16 ox, px, :+
        ldx #2
:       stx fb
        stz fb+1
        lda q6
        beq :+
        mov16 fc, rise
:       lda #8
        sta bcnt
        lda #SFX_KILL
        sta SFXREQ
@hitp:  dif16 rx, ox, px            ; the boomerang test above left rx boomerang-
        lda q6                      ; relative; on a miss it fell through here with
        beq @draw                   ; that x, so a boomerang passing the snake while
                                    ; Cleo stood at its height read as Cleo touching it
        lda health
        beq @draw
        sec                         ; ry = oy - py + rise, in one pass: the
        lda oy                      ; difference waits in X (low) and Y (high)
        sbc py
        tax
        lda oy+1
        sbc py+1
        tay
        clc
        txa
        adc rise
        sta ry
        tya
        adc rise+1
        sta ry+1
        ldx #24
        jsr inrange
        bcc @draw
        lda hurt
        bne @draw
        mov16 hx, rx
        jsr player_hit
@draw:  lda q6
        beq @basket
        ldx #54
        lda fa
        bmi @fr
        ldx #56
        cmp #0                      ; A still holds fa; ldx did not touch it
        beq @fr
        ldx #58
@fr:    bge16 px, ox, :+
        inx                         ; the frame stays in X: nothing below touches it
:       clc                         ; snake Y = oy + parabola, in one pass
        lda oy
        adc rise
        sta spy
        lda oy+1
        adc rise+1
        sta spy+1
        txa
        jsr m_addsprite
@basket:
        mov16 spx, ox
        mov16 spy, oy
        lda #60
        jmp m_addsprite
@knocked:
        bgt16i fc, -256, :+
        rts
:
        sub16i fc, 16
        lda fb
        cmp #1
        bne :+
        lda fd                      ; fd += 16: C = 1 from the cmp #1 (equal), so 15
        adc #15
        sta fd
        lda fd+1
        adc #0
        sta fd+1
        bra :++
:       sub16i fd, 16
:       ldx #55
        bgt16 ox, px, :+
        ldx #54
:       clc                         ; spy = oy + fc in one pass, the way the ox + fd
        lda oy                      ; code twelve lines below already does it
        adc fc
        sta spy
        lda oy+1
        adc fc+1
        sta spy+1
        txa                         ; the frame is still in X
        jsr m_addsprite
        clc
        lda ox
        adc fd
        sta spx
        lda ox+1
        adc fd+1
        sta spx+1
        mov16 spy, oy
        lda #60
        jmp m_addsprite
```

Inlines a leaf routine that has one caller. The body ends with A = low byte and t16+1 = high byte, which is exactly what C1 uses. The removed 'sta t16' only fed t16, which is dead. Assembled: LOGIC -40, LGCCODE -52 vs pristine; branch targets checked in the listing.

Review: This is an alternative to C0-C8: apply either it or them, not both. square's only caller is 2506, and grep finds no other use in src, modelb or tools. The inlined loop leaves A = low byte and t16+1 = high byte, as square did, so it is correct under the C0-C8 arguments. Removing the square: label only widens ob_rsnake's @ scope, and @sq is unique there. The moved ':' is crossed by no ref, as the listing checks (bpl +3 -> sta q1, bcc -> dex, bra :++ -> ldx #55, bgt16 -> clc). Assembled: -40/-52 (with C9-C11 as well: -68/-85).

## r044:0  src/logic.s:2710-2805  (window i2234)  M -22 B -27  [high]  review: accept

Cycles: kill via boomerang: jsr 6 + rts 6 + bra/jmp 3 (15) -> bne @draw 3, -12; kill via stomp: 15 -> bne @kill 3 + bne @draw 3, -9; every other path through the range as in the stand-alone @draw/q1/wobble candidates (never slower); both targets

Assumption: @kill is referenced only by the two jsr at 2710/2725 (grep of ob_bat scope). addscore (logic.s:2142, -> bar_touch 3223) reads neither bcnt nor bounce, so storing them before the kill body instead of after is equivalent; nothing between inspects the stack. SFX_KILL = 5 (logic.s:3312). Branch ranges on the Model B layout: bne @draw ~+59, bne @kill ~-60, bcc @player at 2699 grows to ~+85, all shorter on the Master.

```
        jsr @kill
        lda #8
        sta bcnt
        bra @draw
@player:
        lda health
        beq @draw
        ldx #32
        jsr inrange
        bcc @draw
        ble16i ry, 4, @nostomp
        lda vy+1                    ; N from the high byte, exactly what bmi16's bit did
        bmi @nostomp
        ora vy                      ; then Z from vy|vy+1, exactly what beq16 built
        beq @nostomp
        jsr @kill
        lda #1
        sta bounce
        bra @draw
@nostomp:
        lda hurt
        bne @draw
        ldx #36
        jsr inrange
        bcc @draw
        mov16 hx, rx
        jsr player_hit
@draw:  lda fe
        cmp #2
        bcc @f0
        cmp #4
        bcc @f2
        cmp #6
        bcc @f0
        lda #65
        bra @fr
@f0:    lda #61
        bra @fr
@f2:    lda #63
@fr:    sta q1
        bgt16 spx, px, :+
        bra :++
:       inc q1
:       ; wobble: x += BAT_OFFSET[(frame + obj*5) & 15] ; y += BAT_OFFSET[((5*frame>>2) + obj*7) & 15]
        lda obj
        asl
        asl
        clc
        adc obj
        adc frame
        and #15
        tax
        lda batoff,x
        bpl @wx1                    ; spx += a signed byte, without building the word:
        dec spx+1                   ; pre-borrow the high byte when it is negative
@wx1:   clc
        adc spx
        sta spx
        bcc @wx2
        inc spx+1
@wx2:   lda frame
        asl
        asl                         ; A = 5*frame, low byte only: the high byte of
        clc                         ; 5*frame is dead (the >>2 below shifts the low
        adc frame                   ; byte alone, and only t16's low byte is used)
        lsr
        lsr
        sta t16
        lda obj
        asl
        asl
        asl
        sec
        sbc obj
        clc
        adc t16
        and #15
        tax
        lda batoff,x
        bpl @wy1
        dec spy+1
@wy1:   clc
        adc spy
        sta spy
        bcc @wy2
        inc spy+1
@wy2:   lda q1
        jmp m_addsprite
@kill:  lda #6
        jsr addscore
        mov16i fa, -640
        lda #8
        sta fe
        lda #SFX_KILL
        sta SFXREQ
        rts
```
->
```
        lda #8                      ; bcnt first: the kill does not read it
        sta bcnt
@kill:  lda #6
        jsr addscore
        mov16i fa, -640
        lda #8
        sta fe
        lda #SFX_KILL
        sta SFXREQ
        bne @draw                   ; SFX_KILL <> 0
@player:
        lda health
        beq @draw
        ldx #32
        jsr inrange
        bcc @draw
        ble16i ry, 4, @nostomp
        lda vy+1                    ; N from the high byte, exactly what bmi16's bit did
        bmi @nostomp
        ora vy                      ; then Z from vy|vy+1, exactly what beq16 built
        beq @nostomp
        lda #1                      ; bounce first: the kill does not read it
        sta bounce
        bne @kill                   ; A = 1
@nostomp:
        lda hurt
        bne @draw
        ldx #36
        jsr inrange
        bcc @draw
        mov16 hx, rx
        jsr player_hit
@draw:  lda fe
        cmp #6                      ; C: fe >= 6 draws 65
        and #2                      ; else 61, or 63 for fe 2..3 (61 has bit 1 clear)
        ora #61
        bcc @fr
        lda #65
@fr:    sta q1
        bge16 px, spx, :++          ; spx > px: one frame on
:       inc q1
:       ; wobble: x += BAT_OFFSET[(frame + obj*5) & 15] ; y += BAT_OFFSET[((5*frame>>2) + obj*7) & 15]
        lda obj
        asl
        asl
        clc
        adc obj
        adc frame
        and #15
        tax
        lda batoff,x
        bpl @wx1                    ; spx += a signed byte, without building the word:
        dec spx+1                   ; pre-borrow the high byte when it is negative
@wx1:   clc
        adc spx
        sta spx
        bcc @wx2
        inc spx+1
@wx2:   lda frame
        asl
        asl                         ; A = 5*frame, low byte only: the high byte of
        clc                         ; 5*frame is dead (the >>2 below shifts the low
        adc frame                   ; byte alone, and only t16's low byte is used)
        lsr
        lsr
        sta t16
        lda obj
        asl
        asl
        asl                         ; 8*obj: mod 16 only bit 3 is left, so eor adds it
        eor t16
        sec
        sbc obj                     ; q + 8*obj - obj = q + 7*obj (mod 16)
        and #15
        tax
        lda batoff,x
        bpl @wy1
        dec spy+1
@wy1:   clc
        adc spy
        sta spy
        bcc @wy2
        inc spy+1
@wy2:   lda q1
        jmp m_addsprite
```

@kill moves from a subroutine at the end to straight-line code after the boomerang hit: that path now does bcnt=8, then the kill body, then bne @draw (Z=0 from lda #SFX_KILL). The stomp path sets bounce=1 and bne @kill (Z=0) into the same body. The old @kill ... rts is deleted. Includes the stand-alone @draw (2737), q1 (2750) and wobble (2777) candidates verbatim (the rest of 2737-2797 unchanged), so it supersedes them and the two bra->bne candidates (2711, 2726).

Review: Verified: original matches 2710-2805; @kill referenced only by the two jsr; addscore/bar_touch read neither bcnt nor bounce, so moving those stores before the kill body is equivalent; Z=0 from lda #SFX_KILL (5) and lda #1 makes bne @draw / bne @kill always taken; bge16 px,spx,:++ is bgt16 spx,px with inverted branch and :++ lands on the wobble ':' (anon count 4->4); @draw and eor-wobble parts checked separately (see 1, 14). Assembled both targets in scratch together with 2,3,4,5,9,10,13: no range errors (bcc @player at 2699 now +$55 on Model B, bne @kill -$3C), ob_bat shrinks exactly by the claimed amounts. Every path no slower (kill paths -12/-9). Supersedes 1, 11, 14, 15, 16.

## r044:1  src/logic.s:2737-2749  (window i2240)  M -12 B -14  [high]  review: accept

Cycles: fe 0-1: 13->12; fe 2-3: 14->12; fe 4-5: 21->12; fe>=6: 20->13; same on both targets

Assumption: @f0 and @f2 referenced only inside this block (grep of ob_bat scope 2647-2849).

```
@draw:  lda fe
        cmp #2
        bcc @f0
        cmp #4
        bcc @f2
        cmp #6
        bcc @f0
        lda #65
        bra @fr
@f0:    lda #61
        bra @fr
@f2:    lda #63
@fr:    sta q1
```
->
```
@draw:  lda fe
        cmp #6                      ; C: fe >= 6 draws 65
        and #2                      ; else 61, or 63 for fe 2..3 (61 has bit 1 clear)
        ora #61
        bcc @fr
        lda #65
@fr:    sta q1
```

Original maps fe<2 ->61, <4 ->63, <6 ->61, else 65, for every byte value. New: C = (fe>=6) from cmp; and/ora leave C; for fe<6, fe&2 is 2 exactly for 2,3 (and 0 for 0,1,4,5), and 61|2 = 63, 61|0 = 61; fe>=6 takes lda #65. Checked exhaustively for fe 0..255. Flags after sta q1 unused (next is bgt16's lda). (Also contained in the 2710-2805 kill-restructure candidate; this is its stand-alone form.)

Review: Mapping checked for all fe: C=(fe>=6) survives and/ora; fe&2 is 2 only for 2,3 below 6; 61|2=63. @f0/@f2/@fr referenced only here. Flags after sta q1 unused (next is lda). Every path faster. Assembled: Master -12, Model B -14. Subsumed by candidate 0.

## r044:2  src/logic.s:2807-2814  (window i2301)  M -7 B -12  [medium]  review: accept

Cycles: drops lda fb/sta t16 (-6) and, on Model B, inca (-9); branch as the 2812-2814 candidate; both targets never slower

Assumption: As the stand-alone candidates on 2809-2811 (X dead) and 2812-2814 (@done in bpl range), plus: t16 low byte is not read after this point before being written -- continuation writes it in sx16 t16 (2820) before any read; the @done path returns to process_object, which does not read t16, and every later reader of t16 in the object loop/player code writes it first (not exhaustively proven: that is why medium).

```
        lda fb                      ; t16 = fb + 256: only the high byte changes
        sta t16
        lda fb+1
        inca
        sta t16+1
        blt16 fd, t16, :+
        jmp @done
:
```
->
```
        ldx fb+1                    ; t16+1 = fb+1 + 1; the low byte is fb's own
        inx
        stx t16+1
        lda fd
        cmp fb
        lda fd+1
        sbc t16+1
        bvc :+
        eor #$80
:       bpl @done
:
```

blt16 fd,t16 with t16 = fb + $100 compares fd lo with t16 lo = fb lo: cmp fb gives the identical C. High byte subtracts t16+1 as before; V fixup and the inverted branch as bge16.

Review: cmp fb gives the same C as cmp t16 (t16 low = fb). t16 low no longer written on the @dead path: the continuation writes t16 in sx16 before any read, and on the @done exit t16 is dead -- every routine touching t16 writes it before reading (first-reference scan of logic/engine/menu; mod16/shr6/div10_16 take it as an argument from callers that write it). X dead (process_object callers reload X; addsprite starts ldx NSPR). Anon count 2->2. @done range +$6E on Model B. Assembled: -7 Master / -12 Model B. Supersedes 8 and 12.

## r044:3  src/logic.s:2653-2657  (window i2176)  M -4 B -8  [high]  review: accept

Cycles: fe<7: 13->7; fe=7: Master 15->7, Model B 24->7 (stza is pha/lda/sta/pla there)

Assumption: Line 2653 is reached only by `bcc :+` at 2650 (C=0, A=fe from `lda fe` at 2648; `cmp` does not change A); no :- reference anywhere in ob_bat (grep 2600-2900). Flags/A after: next instruction (mov16 t16, fc at 2659) begins with lda. D flag clear (decimal mode never used).

```
        inc fe
        lda fe
        cmp #8
        bne :+
        stza fe
```
->
```
        adc #1                      ; C = 0 (bcc above), A = fe < 8: fe+1 is 1..8
        and #7                      ; and 8 wraps to 0
        sta fe
```

A=fe in 0..7, C=0: adc #1 -> fe+1 in 1..8; and #7 maps 8->0 and leaves 1..7; sta fe. Original: inc fe; if fe==8 then fe=0 -- same value in fe. fe+1 untouched in both. A differs (fe vs 8/0) but is reloaded by the next lda.

Review: 2653 reached only by bcc :+ at 2650 with C=0 and A=fe<8 (cmp keeps A); no ':-' in ob_bat; D never set (no sed anywhere). adc #1/and #7 gives fe+1 with 8->0, same as inc/cmp/stza. A/flags reloaded by the following lda. The 2658 ':' kept, count unchanged. Assembled: -4 Master / -8 Model B.

## r044:4  src/logic.s:2659-2662  (window i2176)  M -8 B -8  [high]  review: accept

Cycles: single path 67->53 on both targets

Assumption: X dead: nothing reads X before `ldx #28`/`ldx #32`/`tax` (2705, 2717, 2761) on every path, and ob_bat's caller does not use X (process_object writeback uses Y; callers at logic.s:1059/1100 reload X). t16 low byte dead from here until it is next written: FD block/`mov16 t16, rx` (2700)/`sta t16` (2777); between, only boomready, inrange, player_hit, addscore/bar_touch, boomrel run and none reads t16 (checked logic.s:1175, 2065, 2127-2152, 3223). t16+1 still stored with the same value.

```
        mov16 t16, fc
        asr16 t16
        add16 rx, t16
        add16 spx, t16
```
->
```
        lda fc+1                    ; X:t16+1 = fc >> 1 (arith); t16 low byte dead
        cmp #$80
        ror a
        sta t16+1
        lda fc
        ror a
        tax
        clc
        adc rx
        sta rx
        lda rx+1
        adc t16+1
        sta rx+1
        txa
        clc
        adc spx
        sta spx
        lda spx+1
        adc t16+1
        sta spx+1
```

mov16 t16,fc + asr16 t16 computes t16 = fc asr 1: lda fc+1 / cmp #$80 (C = sign) / ror a gives the high byte, then lda fc / ror a (C = old bit 0 of high) gives the low byte -- asr16's redundant `lda t16+1` gone. The low byte is kept in X instead of t16: add16 rx,t16 = clc / (A = low) adc rx / sta / lda rx+1 / adc t16+1 / sta -- addition commutes, same carry chain; add16 spx,t16 = txa / clc / adc spx / sta / lda spx+1 / adc t16+1 / sta. Final rx,spx,t16+1 identical; flags after are from the last adc/sta as before (next code begins with its own lda/bit).

Review: Values identical to mov16+asr16. X free: nothing reads X until ldx/tax on every path and ob_bat's callers reload X. t16 low not read before next written: 2700 mov16 t16,rx on the boomerang path, and every path reaches sta t16 at 2777 before the only other read (2785); boomready/inrange/boomrel/player_hit/addscore/bar_touch do not read t16. Flags after: next is lda. 67->53 cycles. Assembled: 8/8.

## r044:5  src/logic.s:2663-2666  (window i2176)  M -8 B -8  [high]  review: accept

Cycles: single path 67->53 on both targets

Assumption: X dead: nothing reads X before `ldx #28`/`ldx #32`/`tax` (2705, 2717, 2761) on every path, and ob_bat's caller does not use X (process_object writeback uses Y; callers at logic.s:1059/1100 reload X). t16 low byte dead from here until it is next written: `mov16 t16, rx` (2700)/`sta t16` (2777); between, only boomready, inrange, player_hit, addscore/bar_touch, boomrel run and none reads t16 (checked logic.s:1175, 2065, 2127-2152, 3223). t16+1 still stored with the same value.

```
        mov16 t16, fd
        asr16 t16
        add16 ry, t16
        add16 spy, t16
```
->
```
        lda fd+1                    ; X:t16+1 = fd >> 1 (arith); t16 low byte dead
        cmp #$80
        ror a
        sta t16+1
        lda fd
        ror a
        tax
        clc
        adc ry
        sta ry
        lda ry+1
        adc t16+1
        sta ry+1
        txa
        clc
        adc spy
        sta spy
        lda spy+1
        adc t16+1
        sta spy+1
```

mov16 t16,fd + asr16 t16 computes t16 = fd asr 1: lda fd+1 / cmp #$80 (C = sign) / ror a gives the high byte, then lda fd / ror a (C = old bit 0 of high) gives the low byte -- asr16's redundant `lda t16+1` gone. The low byte is kept in X instead of t16: add16 ry,t16 = clc / (A = low) adc ry / sta / lda ry+1 / adc t16+1 / sta -- addition commutes, same carry chain; add16 spy,t16 = txa / clc / adc spy / sta / lda spy+1 / adc t16+1 / sta. Final ry,spy,t16+1 identical; flags after are from the last adc/sta as before (next code begins with bit rx+1).

Review: Same argument as the fc X variant; next instruction is bit rx+1 so flags/A dead; X not read by chase code or boomready before ldx #28/#32 or tax at 2761. t16 low dead as above. Assembled: 8/8.

## r044:6  src/logic.s:2659-2662  (window i2176)  M -6 B -6  [high]  review: accept

Cycles: single path 67->55 on both targets

Assumption: none beyond the window (fallback for the X variant: t16 both bytes still written with the same values)

```
        mov16 t16, fc
        asr16 t16
        add16 rx, t16
        add16 spx, t16
```
->
```
        lda fc+1                    ; t16 = fc >> 1 (arith)
        cmp #$80
        ror a
        sta t16+1
        lda fc
        ror a
        sta t16
        clc
        adc rx
        sta rx
        lda rx+1
        adc t16+1
        sta rx+1
        add16 spx, t16
```

as the X variant, but the low byte is stored to t16 and add16 spx,t16 is kept; the lda rx of add16 rx is folded by adding rx to A (= t16 low) instead -- commutative, same carry chain. (Fallback for the X variant on the same lines.)

Review: Fallback for 4: t16 both bytes written with the same values; adc rx commutes with the original lda rx/adc t16, same carry chain. 67->55. Correct.

## r044:7  src/logic.s:2663-2666  (window i2176)  M -6 B -6  [high]  review: accept

Cycles: single path 67->55 on both targets

Assumption: none beyond the window (fallback for the X variant: t16 both bytes still written with the same values)

```
        mov16 t16, fd
        asr16 t16
        add16 ry, t16
        add16 spy, t16
```
->
```
        lda fd+1                    ; t16 = fd >> 1 (arith)
        cmp #$80
        ror a
        sta t16+1
        lda fd
        ror a
        sta t16
        clc
        adc ry
        sta ry
        lda ry+1
        adc t16+1
        sta ry+1
        add16 spy, t16
```

as the X variant, but the low byte is stored to t16 and add16 spy,t16 is kept; the lda ry of add16 ry is folded by adding ry to A (= t16 low) instead -- commutative, same carry chain. (Fallback for the X variant on the same lines.)

Review: Fallback for 5: same as 6 with fd/ry/spy. Correct.

## r044:8  src/logic.s:2809-2811  (window i2299)  M -0 B -5  [high]  review: accept

Cycles: Master 8->8; Model B 17->8 (inca is sta/inc/lda mtmp there)

Assumption: X dead: the @dead path to @done (rts) or to jmp m_addsprite never reads X (addsprite starts ldx NSPR, engine.s:1382); process_object's writeback uses Y and its callers reload X (logic.s:1059-1062, 1100-1102). A after is reloaded by blt16's lda fd. Carry not consumed (blt16 starts lda/cmp).

```
        lda fb+1
        inca
        sta t16+1
```
->
```
        ldx fb+1                    ; X is free here
        inx
        stx t16+1
```

t16+1 = fb+1 + 1 mod 256 either way; inx wraps like inc a. (Fallback if the 2807-2814 candidate is rejected.)

Review: X dead on the @dead path (@done rts, or jmp m_addsprite which starts ldx NSPR; callers reload X). C not consumed (blt16 starts lda/cmp). inx wraps as inca. Master 5->5, Model B 10->5. Fallback for 2.

## r044:9  src/logic.s:2669-2671  (window i2176)  M -2 B -3  [high]  review: accept

Cycles: fc<fa: bmi taken 3 -> bpl not taken 2; else bmi 2 + bra/jmp 3 -> bpl taken 3; both targets

Assumption: @ydir within branch range of the bpl (about 34 bytes on Model B listing: $1BE7 -> $1C0D).

```
        blt16 fc, fa, :+
        bra @ydir
:       inc fc
```
->
```
        bge16 fc, fa, @ydir
:       inc fc
```

blt16 fc,fa ends `bmi :+` (to inc) after an identical lda/cmp/lda/sbc/bvc/eor; bge16 fc,fa has the same instructions ending `bpl @ydir`. So branch-over-bra becomes one inverted branch.

Review: blt16 and bge16 share lda/cmp/lda/sbc/bvc/eor; bmi-to-inc + bra @ydir becomes bpl @ydir. Anon count unchanged. Range fine (assembled). Faster on both paths.

## r044:10  src/logic.s:2683-2685  (window i2179)  M -2 B -3  [high]  review: accept

Cycles: fd<fb: 3 -> 2; else 5 -> 3; both targets

Assumption: @boom within range of the bpl (~34 bytes, listing $1C1D -> $1C41).

```
        blt16 fd, fb, :+
        bra @boom
:       inc fd
```
->
```
        bge16 fd, fb, @boom
:       inc fd
```

same as the fc case: blt16 + bra over the inc becomes bge16 straight to @boom.

Review: As 9 for fd/fb/@boom; range verified by assembly.

## r044:11  src/logic.s:2750-2752  (window i2242)  M -2 B -3  [high]  review: accept

Cycles: spx>px: 3 -> 2; else 2+3 -> 3; both targets

Assumption: none (target is the `:` on 2753, 3 bytes on; verified in the Model B listing: bpl +2)

```
        bgt16 spx, px, :+
        bra :++
:       inc q1
```
->
```
        bge16 px, spx, :++          ; spx > px: one frame on
:       inc q1
```

bgt16 spx,px = lda px/cmp spx/lda px+1/sbc spx+1/bvc/eor/bmi; bge16 px,spx is the same instructions with bpl. Skip the inc when N=0, i.e. exactly when the original bmi fell into bra :++. (Also contained in the 2710-2805 kill-restructure candidate; this is its stand-alone form.)

Review: bge16 px,spx = same instructions as bgt16 spx,px ending bpl; :++ from the bpl counts the kept 'inc q1' ':' then the wobble ':' (2753), as bra :++ did. Subsumed by 0.

## r044:12  src/logic.s:2812-2814  (window i2301)  M -3 B -3  [high]  review: accept

Cycles: fd<t16: bmi taken 3 -> bpl not taken 2; else 2+3 -> 3; both targets

Assumption: @done within range of the bpl: Model B listing puts it $6E-$71 bytes on (bpl at $1D72, @done $1DE5 before the jmp is removed), Master closer.

```
        blt16 fd, t16, :+
        jmp @done
:
```
->
```
        bge16 fd, t16, @done
:
```

blt16 + jmp over the continuation becomes bge16 (same lda/cmp/lda/sbc/bvc/eor, bpl) straight to @done. (Fallback if the 2807-2814 candidate is rejected.)

Review: blt16 + jmp @done -> bge16 ... @done; @done within range (+$6E/$71). Fallback for 2.

## r044:13  src/logic.s:2816-2818  (window i2306)  M -2 B -2  [high]  review: accept

Cycles: lda 3 + cmp 2 + lda 3 -> ldx 3 + cpx 2: 8->5 both targets

Assumption: A = fa+1 already: add16i fa,120 on 2815 ends `sta fa+1`. X dead on the @dead path (see the 2809-2811 candidate).

```
        lda fa                      ; want only the high byte of fa + 128
        cmp #$80                    ; C = carry out of fa_lo + 128
        lda fa+1
```
->
```
        ldx fa                      ; want only the high byte of fa + 128
        cpx #$80                    ; C = carry out of fa_lo + 128
```

add16i leaves A = fa+1; ldx/cpx set C from fa lo exactly as lda/cmp did without touching A, so the `lda fa+1` goes; adc #0 follows unchanged.

Review: add16i fa,120 ends sta fa+1 so A = fa+1 already; ldx/cpx sets the same C without touching A; X dead on this path. adc #0 follows unchanged.

## r044:14  src/logic.s:2777-2785  (window i2274)  M -1 B -1  [high]  review: accept

Cycles: 22 -> 20 both targets

Assumption: Only A & 15 is used: `and #15` follows at 2786, then tax/lda batoff,x; C after and #15 not consumed (clc at @wy1, and the bpl path has no carry use). t16 still = q.

```
        sta t16
        lda obj
        asl
        asl
        asl
        sec
        sbc obj
        clc
        adc t16
```
->
```
        sta t16
        lda obj
        asl
        asl
        asl                         ; 8*obj: mod 16 only bit 3 is left, so eor adds it
        eor t16
        sec
        sbc obj                     ; q + 8*obj - obj = q + 7*obj (mod 16)
```

Let a = (8*obj) mod 256: its low nibble is 8*(obj&1). q XOR a agrees with q + a in bits 0-3 (flipping bit 3 = adding 8 mod 16). Subtraction's low nibble depends only on the operands' low nibbles, so ((q^a) - obj) & 15 = (a - obj + q) & 15, the original value (checked exhaustively over q, obj 0..255). sec/sbc obj keeps the exact -obj. (Also contained in the 2710-2805 kill-restructure candidate; this is its stand-alone form.)

Review: Low nibble of (8*obj) xor q equals that of q + 8*obj (bits 0-2 of 8*obj clear); subtraction's low nibble depends only on low nibbles; only A&15 used, C/V after not consumed (clc at @wy1). Subsumed by 0.

## r044:15  src/logic.s:2711-2713  (window i2206)  M -0 B -1  [high]  review: accept

Cycles: bra 3 (Master) / jmp 3 (Model B) -> bne taken 3

Assumption: @draw within range (listing: $1C87 -> $1CC9, 64 bytes).

```
        lda #8
        sta bcnt
        bra @draw
```
->
```
        lda #8
        sta bcnt
        bne @draw                   ; A = 8
```

lda #8 sets Z=0 and sta keeps it, so bne is always taken. (Also contained in the 2710-2805 kill-restructure candidate; this is its stand-alone form.)

Review: lda #8 sets Z=0, bne always taken; range fine. Subsumed by 0.

## r044:16  src/logic.s:2726-2728  (window i2220)  M -0 B -1  [high]  review: accept

Cycles: 3 -> 3 both targets

Assumption: @draw within range (listing: $1CB0 -> $1CC9).

```
        lda #1
        sta bounce
        bra @draw
```
->
```
        lda #1
        sta bounce
        bne @draw                   ; A = 1
```

lda #1 sets Z=0; sta keeps it; bne always taken. (Also contained in the 2710-2805 kill-restructure candidate; this is its stand-alone form.)

Review: lda #1 sets Z=0, bne always taken; range fine. Subsumed by 0.

## r045:0  src/logic.s:2798-2805  (window i2287)  M -1 B -1  [high]  review: accept

Cycles: jsr addscore(6) + return to @kill(6) + rts(6) -> jmp addscore(3) + bar_touch's rts straight to @kill's caller(6): 9 fewer, both targets

Assumption: addscore (logic.s:2142) and bar_touch (logic.s:3223) touch only A, score, BARDIRTY and the flags, not fa, fe or SFXREQ, so the stores can go in any order. No stack inspection: plain jsr/jmp in the same segment. @kill's two callers (logic.s:2710, 2726) reload A after jsr @kill, so returning A=1 instead of SFX_KILL does not matter. DBGHIT/DBGTILE builds return at addscore's rts, with the same effect

```
@kill:  lda #6
        jsr addscore
        mov16i fa, -640
        lda #8
        sta fe
        lda #SFX_KILL
        sta SFXREQ
        rts
```
->
```
@kill:  mov16i fa, -640
        lda #8
        sta fe
        lda #SFX_KILL
        sta SFXREQ
        lda #6
        jmp addscore                ; tail call: addscore touches only score, BARDIRTY
```

Do the stores first, then lda #6 / jmp addscore. addscore's rts (through jmp bar_touch) returns to @kill's caller. X and Y are untouched either way. py65 equivalence-tested on both CPUs.

Review: Tail call is sound: addscore/bar_touch touch only A, score, BARDIRTY and flags; callers 2710/2725 reload A; no stack inspection, same segment, rts not a branch target. DBGHIT/DBGTILE early rts returns equally.

## r045:1  src/logic.s:2807-2814  (window i2295)  M -9 B -14  [high]  review: accept

Cycles: Before the high-byte sbc: Master 20 -> 15, Model B 29 -> 15 (inca is 11 there). Continue path: bmi taken 3 -> bmi not taken 2. Exit path: bmi not taken 2 + jmp 3 -> bmi taken 3. Every path faster on both

Assumption: X dead through @dead: the path ends in jmp m_addsprite (addsprite does ldx NSPR, engine.s:1382) or rts to process_object. Its continuation (logic.s:1985 setbank/ldy obj) and callers (logic.s:1059-1062 ldx bent; 1100-1102 inc BINI/bra/ldx BINI) reload X. t16 is a transient temp: the original fall-through overwrote it (sx16) before any read, and nothing after process_object reads t16 before writing it

```
        lda fb                      ; t16 = fb + 256: only the high byte changes
        sta t16
        lda fb+1
        inca
        sta t16+1
        blt16 fd, t16, :+
        jmp @done
:
```
->
```
        lda fb                      ; exit unless fd < t16 = fb + 256 (signed), tested
        clc                         ; as t16 - fd - 1 >= 0, so t16 is never stored
        sbc fd                      ; (t16's low byte is fb's own)
        ldx fb+1                    ; t16+1 = fb+1 + 1: ldx/inx/txa keep the carry
        inx
        txa
        sbc fd+1
        bvc :+
        eor #$80
:       bmi @done
```

blt16 fd, t16 tests fd < t16 (signed), with t16 = fb + 256 and t16+1 = fb+1 + 1 mod 256. The new code computes t16 - fd - 1 as a 16-bit subtract: clc/sbc fd on the low byte (t16's low byte is fb), then (fb+1 + 1) - fd+1 - borrow. N^V (bvc/eor #$80) is the true sign of t16 - fd - 1, which is negative exactly when fd >= t16. So bmi @done exits exactly where the original reached jmp @done, and every other case falls into add16i, as the original's taken bmi :+ did. py65-verified on both CPUs, including fd = fb+256+{-2..2} and fb+1 = $7F/$FF wraps.

Review: Computes t16-fd-1 as a 16-bit subtract (fb low, fb+1 +1 high); N^V negative exactly when fd >= t16. Spot-checked 200k cases incl. wraps in Python. X dead: process_object's staged path does not read X after @call, callers reload X, addsprite loads X first. t16 never read afterwards on either path. Anonymous -1 crosses no reference (no ':-' in logic.s 2800-3160). bmi @done in range (listing offset $5C).

## r045:2  src/logic.s:2815-2818  (window i2302)  M -3 B -3  [high]  review: accept

Cycles: add16i 20 + tax 2 + cpx 2 = 24, vs add16i 20 + lda 3 + cmp 2 + lda 3 = 28; both targets

Assumption: X dead: jmp m_addsprite follows and nothing between reads X. The adc #0 kept at line 2819 consumes A = new fa+1 and C

```
        add16i fa, 120
        lda fa                      ; want only the high byte of fa + 128
        cmp #$80                    ; C = carry out of fa_lo + 128
        lda fa+1
```
->
```
        clc                         ; add16i fa, 120, keeping the new low byte in X
        lda fa
        adc #120
        sta fa
        tax
        lda fa+1
        adc #0
        sta fa+1                    ; want only the high byte of fa + 128:
        cpx #$80                    ; C = carry out of fa_lo + 128
```

add16i is expanded in place with tax after the low store, so X = new fa_lo. After sta fa+1, A = new fa+1, exactly as the original's lda fa+1 reloaded it. cpx #$80 gives C = (fa_lo >= $80), as cmp #$80 on A = fa_lo did. N and Z differ before adc #0, but adc rewrites them.

Review: add16i expanded with tax after the low store; cpx #$80 gives the same C; A = new fa+1 for the kept adc #0. X dead (only later X use is candidate 4's own tax).

## r045:3  src/logic.s:2820-2821  (window i2307)  M -10 B -10  [high]  review: accept

Cycles: Original 31/32. New, A positive: 14 (18 with a carry). A negative: 21 (25 with a carry). Both targets

Assumption: N at line 2820 is A's sign, set by the adc #0 at line 2819. t16 and t16+1 are dead after: t16+1 is rewritten at line 2825 before any read, and t16 is never read again

```
        sx16 t16
        add16 fd, t16
```
->
```
        bpl :+                      ; fd += A sign-extended (N from the adc): pre-borrow
        dec fd+1                    ; the high byte when A is negative
:       clc
        adc fd
        sta fd
        bcc @fdok
        inc fd+1
@fdok:
```

fd += sign-extend(A). The low byte adds A; the high byte adds $FF when A is negative (the dec) plus the carry (bcc/inc). Identical to sx16 t16 / add16 fd, t16 for every A and fd (py65, both CPUs). The flags after differ, but lda fc+1 follows.

Review: bpl on N from adc #0; dec/adc/bcc/inc is fd += sext(A) exactly; t16 no longer written but never read afterwards. @fdok unique in ob_bat scope; anon count unchanged, no crossing refs.

## r045:4  src/logic.s:2825-2832  (window i2312)  M -2 B -2  [high]  review: accept

Cycles: sta 3 -> tax 2; lda 3 -> txa 2: 2 fewer, both targets

Assumption: X dead on this path: it ends in jmp m_addsprite and nothing between reads X. t16+1 dead: only these lines read it

```
        sta t16+1
        lda fc
        ror a
        clc
        adc spx
        sta spx
        lda spx+1
        adc t16+1
```
->
```
        tax                         ; the high half waits in X (dead: addsprite loads it)
        lda fc
        ror a
        clc
        adc spx
        sta spx
        txa
        adc spx+1
```

The high half of fc>>1 waits in X instead of t16+1. tax and txa leave C, so ror a still takes C from the first ror. spx+1 + hi + C = hi + spx+1 + C: same result and same carry.

Review: tax/txa preserve C for the second ror and the adc; addition commutes. X dead through jmp m_addsprite.

## r045:5  src/logic.s:2837-2844  (window i2324)  M -2 B -2  [high]  review: accept

Cycles: 2 fewer, both targets

Assumption: X dead: bgt16 and jmp m_addsprite follow, and neither reads X. t16+1 dead after

```
        sta t16+1
        lda fd
        ror a
        clc
        adc spy
        sta spy
        lda spy+1
        adc t16+1
```
->
```
        tax                         ; the high half waits in X, as above
        lda fd
        ror a
        clc
        adc spy
        sta spy
        txa
        adc spy+1
```

Same as the fc block, for fd into spy.

Review: Same as index 4 for fd/spy; bgt16 and jmp m_addsprite do not read X.

## r045:6  src/logic.s:2848-2848  (window i2335)  M -0 B -1  [high]  review: accept

Cycles: Master bra 3 -> bne 3 at the same size; Model B jmp 3 -> bne 3 (page crossing aside)

Assumption: None outside the window: Z = 0 from lda #65 on the line before

```
        bra :++
```
->
```
        bne :++                     ; always: Z = 0 from the lda #65
```

lda #65 clears Z, so the branch is always taken to the same ':'. Anonymous count unchanged.

Review: lda #65 sets Z=0 and the line is reached only by fall-through. Model B jmp->bne.

## r045:7  src/logic.s:2860-2860  (window i2341)  M -0 B -2  [high]  review: accept

Cycles: Model B 12 -> 5; Master identical

Assumption: A dead: the next line (the ':' at 2861, the only other way in) is lda frame, which reloads A and the flags

```
        stza fe
```
->
```
        stz fe                      ; A is dead: lda frame follows
```

stza only keeps A for a later use, and there is none. On the Master it is the same stz.

Review: A dead: the next line (also the bne :+ target) is lda frame. Master identical; Model B stza 6 -> stz 4.

## r045:8  src/logic.s:2863-2880  (window i2343)  M -3 B -9  [high]  review: accept

Cycles: Master: inc a (2) gone; lda #1/sta (5) -> inc fc (5); bra 3 -> bne 3. Model B: inca (11) gone; jmp 3 -> bne 3. Every path faster or equal

Assumption: q1 dead after ob_walker: it is read only here, and every other reader writes it first (logic.s 1586, 2207, 2296, 2579, 2629, 2749, 3073, 3261/3289; inrange, player_hit, boomready and boomrel never read it). fc's low byte = 0 on the right path, since bne @left fell through and nothing writes fc before

```
        inca
        sta q1                      ; step 1 or 2
        lda fc
        bne @left
        lda fb                      ; walking right.  fb can be -1 coming in (the left
        clc                         ; path rests one past the near end), and the carry
        adc q1                      ; out of the add is what clears its sign byte -- drop
        sta fb                      ; that and -1 + 2 becomes -255, not 1.
        bcc :+
        inc fb+1
:       cmp fa                      ; past here fb+1 is 0 and fb is 0..194, so the
        bcc @coll                   ; unsigned compare says what the signed 16-bit did
        lda #1
        sta fc
        bra @coll
@left:  lda fb
        sec
        sbc q1
```
->
```
        sta q1                      ; frame & 1: the step is q1 + 1, the + 1 the carry's
        lda fc
        bne @left
        lda fb                      ; walking right.  fb can be -1 coming in (the left
        sec                         ; path rests one past the near end), and the carry
        adc q1                      ; out of the add is what clears its sign byte -- drop
        sta fb                      ; that and -1 + 2 becomes -255, not 1.  (sec: the + 1)
        bcc :+
        inc fb+1
:       cmp fa                      ; past here fb+1 is 0 and fb is 0..194, so the
        bcc @coll                   ; unsigned compare says what the signed 16-bit did
        inc fc                      ; fc = 0 here (bne @left fell through): now 1
        bne @coll                   ; always
@left:  lda fb
        clc                         ; fb - q1 - 1: the step (C = 0 is the - 1)
        sbc q1
```

q1 = frame&1 instead of (frame&1)+1. Right path: sec/adc q1 = fb + old q1, so A, carry-out (the sign-byte fix-up), N and Z are the same. Left path: clc/sbc q1 = fb - old q1, with C = 1 exactly when fb >= old q1, so the borrow, Z and N are the same. inc fc takes fc from 0 to 1 and clears Z, so bne always branches.

Review: q1 only read within ob_walker here; every other q1 reader writes it first (mapbyte, 2207/2296 before star_safe/tramp_safe, 3073, div10, level init). sec/adc q1 and clc/sbc q1 give identical A, C, N, Z to the old step. fc low is 0 on the right path so inc fc = 1 with Z=0.

## r045:9  src/logic.s:2882-2886  (window i2361)  M -2 B -3  [high]  review: accept

Cycles: Borrow 10 -> 9; became zero 5 -> 3; otherwise 6 -> 5. Both targets

Assumption: The step is 1 or 2, so a borrow leaves fb >= 254: C = 0 implies Z = 0. Z and C come from the sbc (sta fb keeps them). @stop has no other referrer

```
        bcs :+                      ; borrow == fb went negative: that IS the bmi16 test
        dec fb+1
        bra @stop
:       bne @coll                   ; Z still from the sbc, so fb's own load is not needed
@stop:  stz fc
```
->
```
        beq @stop                   ; fb reached 0 (a borrow never leaves zero: >= 254)
        bcs @coll                   ; no borrow, not zero: still walking
        dec fb+1                    ; borrow == fb went negative: that IS the bmi16 test
@stop:  stz fc
```

The sbc has three outcomes. Borrow: dec fb+1, then @stop. No borrow and zero: @stop. No borrow and non-zero: @coll. The original reaches the same targets; beq only ever catches the zero case.

Review: Borrow with step 1..2 leaves 254/255, so C=0 implies Z=0; all three outcomes map to the same targets. @stop has no other referrer; removed ':' crosses no reference.

## r045:10  src/logic.s:2901-2902  (window i2379)  M -2 B -2  [high]  review: accept

Cycles: lda sy+1 removed: 3 fewer, both targets

Assumption: boomrel (logic.s:2127) ends dif16 sy, spy, by (lda spy+1 / sbc by+1 / sta sy+1) and then rts, so A = sy+1 on return on both targets

```
        mov16 rx, sx
        mov16 ry, sy
```
->
```
        sta ry+1                    ; A = sy+1: boomrel's last store
        lda sy
        sta ry
        mov16 rx, sx
```

The same four bytes are written, ry+1 first from A. Nothing reads them in between.

Review: boomrel ends with sta sy+1 (A = sy+1) then rts, on both targets (direct jsr, same file). Flags after are overwritten by ldx #44.

## r045:11  src/logic.s:2909-2912  (window i2386)  M -2 B -4  [high]  review: accept

Cycles: fb >= fa: 5 -> 3. fb < fa: bmi 3 -> bpl 2, then the same stz and a 3-cycle branch

Assumption: A dead after: @bset does lda #8. On the Model B, Z = 1 from the lda #0

```
        blt16 fb, fa, :+
        bra @bset
:       stz fc
        bra @bset
```
->
```
        bge16 fb, fa, @bset
  .if ::MODELB
        lda #0                      ; (stz's own expansion, spelled out for its Z)
        sta fc
        beq @bset                   ; always
  .else
        stz fc
        bra @bset
  .endif
```

bge16 is the same compare as blt16, with bpl in place of bmi, so it folds the bra. On the Model B, stz is spelled out so the lda #0 sets Z for a 2-byte beq in place of the 3-byte jmp.

Review: bge16 is blt16 with bpl: fb>=fa goes straight to @bset; else fc=0 then @bset. Model B lda #0 sets Z for beq. A dead at @bset (lda #8). Macro-internal ':' retained; removed ':' crosses nothing.

## r045:12  src/logic.s:2933-2941  (window i2408)  M -1 B -5  [high]  review: accept

Cycles: fe >= 9: 7 -> 5. @f0/@f2/@f8: 3 -> 3. Page crossing aside

Assumption: @f0 and @f2 are reached only by bcc (lines 2928, 2930), so C = 0 there. The lda #6 arm is reached only when cmp #9's bcc falls through, so C = 1. @f8 loads 8, so Z = 0. The carry at @sp is dead: cpx or clc rewrites it

```
        lda #6
        clc                         ; only this arm reaches @fr with C set (cmp #9
        bra @fr                     ; fell through); @f2 and @f4 got here on a bcc
@f0:    lda fc
        bra @sp
@f2:    lda #2
        bra @fr
@f8:    lda #8
        bra @sp
```
->
```
        lda #5                      ; C = 1 (cmp #9 fell through): 5 + fc + 1 at @fr
        bcs @fr                     ; always; @f2 and @f4 reach @fr on a bcc, C = 0
@f0:    lda fc
        bcc @sp                     ; always: @f0 is reached on a bcc
@f2:    lda #2
        bcc @fr                     ; always: @f2 is reached on a bcc (C = 0 for the adc)
@f8:    lda #8
        bne @sp                     ; always
```

lda #5 with C = 1 gives adc fc = 6 + fc, the same sum, carry, N and Z as lda #6 / clc / adc fc. Each bra becomes a branch on a flag that is proven at that point.

Review: C at each site proven: lda #6 arm reached only on cmp #9 fall-through (C=1), @f0/@f2 only via bcc (C=0), @f8 loads 8 (Z=0). 5+fc+1 == 6+fc+0 in result and C.

## r045:13  src/logic.s:2944-2951  (window i2419)  M -5 B -6  [medium]  review: accept

Cycles: 12 on both otypes -> 9 (+1 if the table straddles a page). Both targets

Assumption: otype is 5 or 6: ob_walker is reached only through @tab entries 5 and 6 (logic.s:2036), with otype = O_TYPE,y (logic.s:1933). The flags after are dead

```
@sp:    ldx otype                   ; the frame stays in A: no round trip through q1
        cpx #5
        bne :+
        adc #66                     ; C = 1 from the equal cpx, so this is A + 67
        bra :++
:       clc
        adc #76
:       jmp m_addsprite
```
->
```
@sp:    ldx otype                   ; the frame stays in A: no round trip through q1
        clc                         ; otype is 5 or 6 (the dispatch table's two
        adc @wbase-5,x              ; ob_walker entries): + 67 for 5, + 76 for 6
        jmp m_addsprite
@wbase: .byte 67, 76
```

Adds 67 (otype 5) or 76 (otype 6) through a 2-byte table in the code segment. The byte savings are net of the table.

Review: otype is only 5 or 6 in ob_walker (dispatch @tab entries 5/6; otype written only by process_object, level init, and the DBGHIT kill-tile path outside the walk). Table is read from the executing segment. Assembled both targets with all 14 applied: every byte outside the region identical; Master LOGIC 9572->9530 (-42), Model B LGCCODE 10762->10698 (-64), matching the claimed sums.

## r046:0  src/logic.s:2926-2951  (window i2432)  M -6 B -11  [high]  review: accept

Cycles: fe<3: 14->14; 3..5: 20->20; 6..8: 21->21; >=9: 25->22; @f8: 5->5 (lda 2 + bne 3 = lda 2 + bra/jmp 3). Tail otype 5: 12->10, otype 6: 12->11. Model B identical (bra=jmp is 3 cycles, same as the taken branches).

Assumption: otype is 5 or 6 here: ob_walker is entries 5 and 6 of process_object's @tab (logic.s:2036) and nothing in ob_walker writes otype (inrange, player_hit, boomready, boomrel checked; only DBGHIT's tile-kill path writes otype and it does not reach here). fc+8 < 247 (A+76 is a sprite number, so A < 180), so adc #8 with C=1 cannot carry. X dead on entry to the chain: @sp reloads it with ldx otype on every path. addsprite (engine.s:1381) reads neither X nor C nor V on entry (ldx NSPR first), and the handler's return goes to process_object's setbank (A,flags dead). @f8 is reached only from the bmi16/beq/bcs above (lines 2920-2924); its new place after the jmp is ~50 bytes away, in branch range.

```
        lda fe
        cmp #3
        bcc @f0
        cmp #6
        bcc @f2
        cmp #9
        bcc @f4
        lda #6
        clc                         ; only this arm reaches @fr with C set (cmp #9
        bra @fr                     ; fell through); @f2 and @f4 got here on a bcc
@f0:    lda fc
        bra @sp
@f2:    lda #2
        bra @fr
@f8:    lda #8
        bra @sp
@f4:    lda #4
@fr:    adc fc
@sp:    ldx otype                   ; the frame stays in A: no round trip through q1
        cpx #5
        bne :+
        adc #66                     ; C = 1 from the equal cpx, so this is A + 67
        bra :++
:       clc
        adc #76
:       jmp m_addsprite
```
->
```
        ldx fe                      ; fe in X, so each arm can load its frame early
        cpx #3
        bcc @f0
        cpx #6
        bcc @f2
        cpx #9
        lda #4
        bcc @fr                     ; C = 0: fc + 4
        lda #5                      ; C = 1 (cpx #9 fell through): fc + 6
@fr:    adc fc
@sp:    ldx otype                   ; the frame stays in A: no round trip through q1
        cpx #6                      ; otype is 5 or 6 (the walker's two table entries)
        bne :+                      ; 5: C = 0 from the cpx
        adc #8                      ; 6: C = 1, so A + 9, and C = 0 again
:       adc #67
        jmp m_addsprite
@f0:    lda fc                      ; C = 0: the bcc
        bcc @sp
@f2:    lda #2                      ; C = 0: the bcc
        bcc @fr
@f8:    lda #8
        bne @sp
```

Frame chain: X=fe; cpx sets C exactly as cmp did. fe<3 -> @f0: A=fc, C=0 -> bcc @sp (was bra). 3..5 -> @f2: A=2, C=0 -> bcc @fr: fc+2. 6..8: lda #4 (C kept from cpx #9 = 0) -> bcc @fr: fc+4 (was bcc @f4 / lda #4). >=9: C=1, lda #5 falls into @fr: 5+fc+1 = fc+6 (was lda #6 / clc / bra @fr). @f8: lda #8 leaves Z=0 so bne = bra. Tail: original otype 5: cpx #5 equal -> adc #66 with C=1 = A+67; otype 6: clc/adc #76 = A+76. New: cpx #6: otype 5 -> C=0, Z=0 -> adc #67 = A+67; otype 6 -> C=1, Z=1 -> adc #8 (+9, C=0) then adc #67 = A+76. A, N, Z identical at m_addsprite; X = otype as before. Verified in a scratch tree: all 12 candidates applied together assemble on both targets (measured savings 35 Master / 59 Model B LGCCODE), tools/gate.sh passes everything except 'B title', which fails identically for a trivial 1-byte shrink alone (a disc-size timing artifact, not this code); bdiff lock-step OK on all 16 levels.

Review: Original matches. otype is 5/6 only (ob_walker is @tab entries 5,6; nothing in the handler writes otype outside DBGHIT's non-reaching path). Frame chain: cpx sets C as cmp did; 6..8: lda #4 keeps C=0 -> fc+4; >=9: C=1, lda #5 -> fc+6; @f0/@f2 reached on bcc so C=0; @f8 lda #8 Z=0 so bne always. Tail: cpx #6: otype 5 -> C=0 -> adc #67; otype 6 -> C=1, adc #8 (+9, no carry since fc is 0/1) then adc #67 = +76. A/N/Z/C/V at m_addsprite identical, X = otype. @f8 within range. Anonymous labels: 2949/2951 only referenced within block; blt16 at 2909 targets 2911, ob_spike's bcc :+ forward, no :- before 3202. Cycles no worse on any path. Scratch build: all 12 applied assemble on both; Master LOGIC -35, Model B LGCCODE -59, matching the claimed totals.

## r046:1  src/logic.s:2969-2974  (window i2437)  M -4 B -4  [high]  review: accept

Cycles: health=0: 6->6; health!=0 & fa<0: 11->10; health!=0 & 8<=fa<128: 15->10; health!=0 & fa<8: 14->9. Same on Model B.

Assumption: A = fa at @nowrap on both ways in: the fall-through from 'lda fa / cmp #24 / bne @nowrap' (cmp does not write A) and the wrap path ending 'sta fa'. X dead: the proceed path does ldx #48 before inrange; the @draw paths end in m_addsprite (addsprite does ldx NSPR first) or @done rts, and process_object's callers never read X after the handler (logic.s:1983 setbank/ldy obj; 1059 setbank; 1100 inc BINI/ldx BINI).

```
        lda health
        beq @draw
        lda fa
        bmi @draw                   ; still dormant
        cmp #8
        bcs @draw
```
->
```
        ldx health                  ; A = fa on both ways in, and ldx keeps it
        beq @draw
        cmp #8                      ; unsigned: a dormant (negative) fa is >= 8 too
        bcs @draw
```

lda health/beq -> ldx health/beq: same Z, A kept = fa. 'lda fa / bmi @draw / cmp #8 / bcs @draw' -> 'cmp #8 / bcs @draw': the unsigned compare sends 128..255 (negative) and 8..127 to @draw alike, so the bmi was only a fast path; it is now faster still since the reload is gone. Fall-through: A = fa in 0..7, C = 0, exactly as before.

Review: A = fa at @nowrap on both entries (fall-through after cmp #24, and after sta fa). ldx health keeps A and gives same Z. Unsigned cmp #8 sends negatives (>=128) to @draw exactly like bmi did. Fall-through A=fa 0..7, C=0 as before. X changed to health on the @draw path, but X is dead: @draw reloads A, ends in addsprite (ldx NSPR) or rts to process_object staged path (setbank/ldy/lda) whose callers (logic.s:1059 ldx bent, 1100 ldx BINI) reload X. Cycles 6->6, 11->10, 15->10, 14->9.

## r046:2  src/logic.s:2976-2978  (window i2441)  M -1 B -6  [high]  review: accept

Cycles: Master 6->4; Model B 15 (inca 11 + asl 2 + eor 2) -> 4.

Assumption: C = 0 and A = fa in 0..7 on arrival: the preceding 'cmp #8 / bcs @draw' fell through (true with or without the ldx health candidate). V after is not read: next is sta RNGTAB+49 / ldx #48 / jsr inrange, which starts with lda rx+1 and never reads V or C.

```
        inca                        ; A is still fa: cmp does not write A
        asl
        eor #$80                    ; biased limit
```
->
```
        asl                         ; fa < 8 and C = 0 (bcs fell through): 2fa, C = 0
        adc #130                    ; (fa+1)*2 biased by $80
```

Original: A=fa (0..7) -> inc a -> fa+1 -> asl -> 2fa+2 (C=0) -> eor #$80 -> 2fa+130. New: asl -> 2fa, C=0 (bit 7 of fa is 0) -> adc #130 -> 2fa+130, no carry (<=144). N=1, Z=0, C=0 as before. On the Model B inca also wrote mtmp, scratch owned by the expansions (always written before read).

Review: C=0, A=fa in 0..7 after the cmp #8/bcs fall-through. asl -> 2fa, C=0; adc #130 -> 2fa+130 (<=144), C=0, N=1, V=0: same A/N/Z/C as inca/asl/eor #$80. inrange starts lda rx+1/inca/cmp and never reads incoming C or V. Master 4->3 bytes, Model B 9->3 (inca is 6).

## r046:3  src/logic.s:2991-2993  (window i2459)  M -1 B -1  [high]  review: accept

Cycles: Master and Model B: 7 -> 4 on the fa>=8 path.

Assumption: A = fa at this point (lda fa / bmi / cmp #8 / bcc: cmp does not write A) and C = 1 (bcc not taken). fa <= 23 at @draw: fa is incremented to 24 only to be replaced by a negative value in the same call (lines 2958-2967), and negatives leave at bmi @done.

```
        lda #23
        sec
        sbc fa
```
->
```
        eor #$FF                    ; A = fa (8..23), C = 1: 255-fa+23+1 = 23-fa, C = 1
        adc #23
```

lda #23 / sec / sbc fa = 23-fa with C=1 (no borrow). eor #$FF gives 255-fa; adc #23 with C=1 gives 279-fa = 256 + (23-fa): A = 23-fa and C=1 since fa<=23. Same A, N, Z, C, and V irrelevant (lsr/clc/adc #85 follow; adc overwrites V).

Review: A = fa (lda fa/bmi/cmp #8), C=1 on fall-through. eor #$FF/adc #23 with C=1 computes 23 + ~fa + 1, the exact operation sbc does for 23-fa: identical A, N, Z, C and V for every fa. 5->4 bytes, 7->4 cycles both targets.

## r046:4  src/logic.s:3009-3011  (window i2470)  M -1 B -1  [medium]  review: accept

Cycles: Both paths 2 cycles faster on both targets.

Assumption: C = 0 at ob_flame's entry: it is reached only through process_object's @tab (logic.s:2036-2037; grep finds no other reference), and type 9 >= NLEAN(2) goes through the @gen path, which ends 'lda otype / asl / tax / jsr @call' (logic.s:1980-1983): asl of 9 clears C. @call is 'jmpx @tab': jmp (abs,x) on the Master, and on the Model B pha/lda/sta/lda/sta/pla/jmp (jv), none of which writes C. Nothing in ob_flame before here writes C (lda, and, bne, inc, sta).

```
:       lda fe
        clc
        adc #93
```
->
```
:       lda fe
        adc #93                     ; C = 0: process_object's asl of otype (9) before the dispatch
```

The clc was redundant: C is provably 0 at the adc #93 on both paths (bne-taken and inc path).

Review: ob_flame is referenced only from process_object @tab; type 9 >= NLEAN goes through the staged path 'lda otype/asl/tax/jsr @call': asl of 9 leaves C=0; jmpx (jmp (abs,x) on Master; pha/lda/sta/lda/sta/pla/jmp (jv) on Model B) and lda/and/bne/inc/sta in ob_flame do not touch C. clc is redundant on both paths.

## r046:5  src/logic.s:3025-3026  (window i2482)  M -2 B -2  [high]  review: accept

Cycles: 5 -> 5 on both (lda #imm 2 + sta zp 3 = inc zp 5).

Assumption: fa = 0 here: 'lda fa / bne @adv' fell through, and neither the health checks nor inrange (logic.s:2065, reads rx/ry/RNGTAB only) write fa. fa is zero page ($AC). A and flags are overwritten by the following lda #3.

```
        lda #1
        sta fa
```
->
```
        inc fa                      ; fa was 0: bne @adv fell through
```

inc of 0 stores 1, as lda #1 / sta fa did.

Review: fa = 0 on this path (lda fa/bne @adv fell through); lda health/cmp and inrange (reads rx/ry/RNGTAB only) do not write fa; fa is zp $AC. inc fa stores 1; A and N/Z are overwritten by lda #3, C untouched by either form. 4->2 bytes, 5->5 cycles.

## r046:6  src/logic.s:3032-3032  (window i2489)  M -0 B -1  [high]  review: accept

Cycles: 3 -> 3 (taken branch vs bra/jmp).

Assumption: SFX_POWER = 6 (logic.s:3313), so the preceding lda #SFX_POWER leaves Z = 0; sta does not change flags.

```
        bra @draw
```
->
```
        bne @draw                   ; Z = 0: SFX_POWER is 6
```

Branch always taken, same target.

Review: SFX_POWER = 6 (logic.s:3313): lda #SFX_POWER leaves Z=0 and sta SFXREQ keeps it, so bne is always taken to the nearby @draw. Master 2->2, Model B jmp 3->2; 3 cycles either way.

## r046:7  src/logic.s:3055-3056  (window i2508)  M -2 B -2  [high]  review: accept

Cycles: 5 -> 5 on both.

Assumption: fe = 0 here: 'lda fe / bne @count' fell through and inrange does not write fe. fe is zero page ($B4). A and flags dead: @ret rts returns to process_object, whose next instruction is setbank's lda (logic.s:1984).

```
        lda #1
        sta fe
```
->
```
        inc fe                      ; fe was 0: bne @count fell through
```

inc of 0 stores 1, as lda #1 / sta fe did. A is left 0 (from the lda vy / ora vy+1 that fell through) instead of 1; A and flags are dead at the rts.

Review: fe = 0 here (lda fe/bne @count fell through; inrange does not write fe); fe is zp $B4. inc fe stores 1 and leaves N=0,Z=0 as lda #1 did; only A differs (0 vs 1), and A is dead: rts to process_object staged path whose next instruction is setbank's lda #.

## r046:8  src/logic.s:3060-3061  (window i2513)  M -0 B -4  [high]  review: accept

Cycles: Master unchanged. Model B: taken 15+3 -> 2+3; fall through 15+2 -> 2+2+3.

Assumption: A = fe before (lda fe on the line above); the next instruction after the fall-through is cmp #12, which sets N, Z and C, so the flags the bitimm left are not consumed (bitimm left C from before, also unused since cmp overwrites it). @done is rts (A dead). mtmp, which bitimm wrote, is scratch owned by the expansions.

```
        bitimm 3
        bne @done
```
->
```
  .if ::MODELB
        and #3                      ; bitimm's save and restore of A are not needed:
        bne @done                   ; A is reloaded
        lda fe
  .else
        bitimm 3
        bne @done
  .endif
```

On the Model B: and #3 gives Z = (fe & 3) == 0 exactly as bitimm's php/plp did; lda fe restores A = fe for the chain below. Master keeps bit #3.

Review: Model B only. A = fe (lda fe). and #3 gives Z exactly as bitimm's php/plp did; V untouched in both; bne @done (rts, A dead); fall-through reloads A=fe for cmp #12, which overwrites N/Z/C. mtmp is expansion scratch (written before read by every expansion). Master keeps bit #3. Model B 10->6 bytes; taken 18->5, fall-through 17->7 cycles.

## r046:9  src/logic.s:3062-3072  (window i2515)  M -5 B -7  [high]  review: accept

Cycles: fe<12: 9->7; 12..36: 12->11; >=37: 19->17. Model B the same (bra = jmp, 3 cycles).

Assumption: A = fe on arrival (lda fe; bitimm/and-reload keeps it). @six has no other referrer (grep: only bcc @six); @set's other referrers are within this block.

```
        cmp #12
        bcs :+
        lsr
        bra @set
:       cmp #37
        bcc @six
        lda #48
        sbc fe                      ; carry is already set: bcc fell through
        lsr
        bra @set
@six:   lda #6
```
->
```
        cmp #12
        bcc @half                   ; fe < 12: fe >> 1
        cmp #37
        lda #6                      ; lda keeps C from the cmp
        bcc @set
        lda #48
        sbc fe                      ; carry is already set: bcc fell through
@half:  lsr
```

fe<12: bcc @half -> lsr -> fe>>1 (was lsr / bra @set). 12..36: cmp #37 C=0, lda #6 keeps C, bcc @set: q1=6 (was bcc @six / lda #6). >=37: C=1, lda #48 / sbc fe / lsr falls into @set (was the same then bra @set). A at @set identical on every path; flags at @set are dead (sta q1 then mov16's lda).

Review: A = fe on arrival. fe<12: bcc @half -> lsr -> fe>>1; 12..36: cmp #37 C=0, lda #6 keeps C, bcc @set -> 6; >=37: C=1, lda #48/sbc fe/lsr falls into @set. A at @set identical on every path; flags dead (sta q1, mov16, tilexy starts with lda/cmp). @six has no other reference. Removed ':' at 3066 was referenced only by 3063's bcs :+; 3004 bne :+ targets 3009, next :- is at 3205 targeting 3202. Cycles 9->7, 12->11, 19->17.

## r046:10  src/logic.s:3073-3099  (window i2525)  M -13 B -18  [medium]  review: accept

Cycles: Straight line; non-call instructions 45 -> 26 cycles on the Master, 54 -> 26 on the Model B (inca was 11).

Assumption: (1) maptile keeps X: maptile = jsr maprow / txa / tay / jmp mapbyte (logic.s:305); Master maprow (engine.s:4533) uses A,Y only and ends jmp pagelogic; Model B maprow (engine.s:4513) saves and restores X explicitly; mapbyte and mapput (engine.s:4545,4553) touch A only; pagelogic (engine.s:4734) keeps A, X, Y and C. So X = tile x at maptile (as ldx q4 gave) and X = q1 across both mapputs, Y = tx then tx+1 across mapput. (2) The same bank is paged when LV_HDR is read: before, at handler entry (process_object has BANK_LVL in to read O_*); now, after maptile/mapput, whose pagelogic pages BANK_LVL on both targets. (3) q2, q3 and q4 are scratch: every read of them in src/ and modelb/src (logic.s 322-783 getinfo/getaltitude/object init, 3120-3147 ob_switch) is preceded by a write in the same routine, so their final values (q2 now tilexy's, q3 unchanged, q4 = tx+1 instead of tx) are never observed. q1 keeps its original final value. (4) mark_dirty clobbers Y (tay), so the tx+1 for the second call comes from memory (q4) as before, and the call order is unchanged.

```
@set:   sta q1
        ; tiles at (ox>>3, oy>>3) and +1 : codes from header per page
        mov16 qx, ox
        mov16 qy, oy
        jsr tilexy
        stx q4                      ; tile x (q4/q5: gx/gy are the live grid-walk cursor)
        sta q5                      ; tile y
        ldx q1
        lda LV_HDR+8,x
        sta q2
        lda LV_HDR+9,x
        sta q3
        ldx q4
        lda q5
        jsr maptile                 ; sets mapptr, Y = tx
        lda q2
        jsr mapput
        iny
        lda q3
        jsr mapput
        lda q4
        ldx q5
        jsr m_mark_dirty
        lda q4
        inca
        ldx q5
        jsr m_mark_dirty
```
->
```
@set:   sta q1
        ; tiles at (ox>>3, oy>>3) and +1 : codes from header per page
        mov16 qx, ox
        mov16 qy, oy
        jsr tilexy                  ; X = tile x, A = tile y
        sta q5                      ; tile y (q4/q5: gx/gy are the live grid-walk cursor)
        jsr maptile                 ; sets mapptr, Y = tx; X kept, bank 7 back
        ldx q1
        lda LV_HDR+8,x
        jsr mapput                  ; X and Y kept
        iny
        sty q4                      ; tile x + 1
        lda LV_HDR+9,x
        jsr mapput
        dey
        tya                         ; tile x
        ldx q5
        jsr m_mark_dirty
        lda q4
        ldx q5
        jsr m_mark_dirty
```

Original: stage LV_HDR+8/9,x into q2/q3 before maptile, reload X=q4/A=q5 for maptile, pass q2/q3 to mapput, then mark (q4, q5) and (q4+1, q5). New: tilexy's X/A go straight to maptile (A=ty still in A after sta q5); the header bytes are loaded (X=q1) straight into A for each mapput; after the iny, Y = tx+1 is kept in q4; dey/tya gives tx for the first mark_dirty and lda q4 gives tx+1 for the second (was lda q4 / inca). Map writes, their addresses and the mark_dirty arguments and order are identical. C into the second m_mark_dirty is whatever the first left, as with inca (which preserves C).

Review: tilexy returns X = tx (tax) and A = ty on every exit that the original also consumed unchecked; maptile (maprow keeps X on both targets, txa/tay, mapbyte) keeps X and Y; mapput keeps X and Y; pagelogic keeps A/X/Y/C and leaves bank 7 (the bank the code executes from, so LV_HDR is read in the same bank as before). Map writes, addresses, mark_dirty arguments and order identical; C/N/Z into second m_mark_dirty identical (inca preserved C; lda q4 = tx+1). q2/q3/q4 final values differ but every reader (tilexy, getaltitude, level_init, ob_switch) writes them first; q1 and q5 unchanged. mark_dirty does not read Y. Bytes 52->39 Master, 57->39 Model B.

## r046:11  src/logic.s:3103-3103  (window i2554)  M -0 B -2  [high]  review: accept

Cycles: Master: stz either way. Model B: pha/lda/sta/pla 3+2+3+4=12 -> lda/sta 5.

Assumption: A dead after @done rts: process_object @gen (logic.s:1983-1984) follows jsr @call with setbank BANK_LVL (lda #). Flags dead likewise (stz on the Model B leaves Z=1 where stza left Z from pla of 48; nothing reads them).

```
        stza fe
```
->
```
        stz fe                      ; A dead: rts to process_object's setbank
```

stza preserves A around the store; A is not used after the rts, so stz (the A-dead spelling in cpu.inc) is equivalent.

Review: @done is rts to process_object staged path (setbank's lda # next), so A and flags are dead; stza (pha/lda/sta/pla, 6 bytes) -> stz (lda/sta, 4 bytes) on Model B, identical stz on Master. 12->5 cycles on Model B.

## r047:0  src/logic.s:3078-3092  (window i2560)  M -12 B -12  [high]  review: accept

Cycles: Straight line: 55 -> 37 cycles on both targets. Removed: the q2/q3 staging, ldx q4, lda q5, lda q2 and lda q3. The maptile/mapput bodies are unchanged.

Assumption: (1) tilexy (logic.s:314) returns X = qx>>3 and A = qy>>3, exactly what lines 3078-3079 store and lines 3085-3086 reload into X/A for maptile. (2) maptile (logic.s:305) does jsr maprow / txa / tay / jmp mapbyte. maprow preserves X on both targets: on the Master (engine.s ~4532) it uses only Y; on the Model B (engine.s:4512) it saves and restores X with pha/pla. mapbyte and mapput (engine.s:4545,4553) touch only A and the bank, and both end in jmp pagelogic, which preserves A,X,Y,C (engine.s:4734). So X = q1 survives the first mapput and indexes the second LV_HDR load. (3) None of maprow/mapbyte/mapput/pagelogic writes q1; the 'q1 = page' in the comment at logic.s:301 is stale. (4) Every map call ends in pagelogic (BANK_LVL). That is the bank LV_HDR ($8000) was read in originally, so LV_HDR+8/9 read the same bytes after the calls. (5) q2/q3 are transient temps ($C4/$C5) that nothing reads afterwards: not ob_vanish (lines 3093-3104 use q4, q5, fe), not the process_object store-back (logic.s:1985-2006), and not its callers (logic.s:1059-1063, 1100-1102). (6) maptile ignores Y and C on entry: on the Master maprow starts with tay; on the Model B maprow never reads Y, and its first use of C is set by an lsr.

```
        stx q4                      ; tile x (q4/q5: gx/gy are the live grid-walk cursor)
        sta q5                      ; tile y
        ldx q1
        lda LV_HDR+8,x
        sta q2
        lda LV_HDR+9,x
        sta q3
        ldx q4
        lda q5
        jsr maptile                 ; sets mapptr, Y = tx
        lda q2
        jsr mapput
        iny
        lda q3
        jsr mapput
```
->
```
        stx q4                      ; tile x (q4/q5: gx/gy are the live grid-walk cursor)
        sta q5                      ; tile y
        jsr maptile                 ; X = tx, A = ty (tilexy's): sets mapptr, Y = tx
        ldx q1                      ; the map calls keep X, and leave BANK_LVL selected
        lda LV_HDR+8,x
        jsr mapput
        iny
        lda LV_HDR+9,x
        jsr mapput
```

Original: store tx,ty; X=q1; q2=HDR[8+q1]; q3=HDR[9+q1]; X=tx; A=ty; maptile; mapput(q2) at tx; iny; mapput(q3) at tx+1. New: X and A still hold tx and ty from tilexy, so maptile is called first with identical inputs. Then X=q1, which mapput preserves, feeds HDR[8+q1] and HDR[9+q1] straight to the two mapputs at Y=tx and tx+1. The map writes, q4/q5 and the exit bank are all the same. A after the second mapput equals the old q3 and is dead anyway (next is lda q4).

Review: Original matches 3078-3092. X/A from tilexy are stored and reloaded unchanged, so maptile gets the same inputs. X survives maptile/mapput on both targets: Master maprow uses Y only, Model B maprow does pha/pla X, and mapbyte/mapput/pagelogic leave X alone. LV_HDR ($8000, bank 7 LGCLVL) is read after pagelogic has selected BANK_LVL. q2/q3 are scratch that is written before it is read everywhere (grep). Assembled: Master LOGIC and Model B LGCCODE shrink as claimed (34->22 bytes).

## r047:1  src/logic.s:3096-3098  (window i2560)  M -0 B -4  [high]  review: accept

Cycles: Master: text identical (5 bytes, 8 cy). Model B: lda q4 + inca (sta/inc/lda mtmp) is 8 bytes and 14 cy; ldx q4/inx/txa is 4 bytes and 7 cy. The ldx q5 is unchanged.

Assumption: inx wraps exactly as inc mtmp does, so no assumption about q4's value is needed. Both forms preserve C: inca is documented to keep carry, and ldx/inx/txa do not touch it. X is reloaded by the following ldx q5, so its intermediate value is dead. mtmp ($DF) is scratch only (cpu.inc contract).

```
        lda q4
        inca
        ldx q5
```
->
```
  .if MODELB
        ldx q4                      ; inca is 6 bytes here
        inx
        txa
  .else
        lda q4
        inca
  .endif
        ldx q5
```

Model B: A = q4+1 mod 256 either way and C is unchanged; N/Z are overwritten by ldx q5. The Master path is unchanged.

Review: Model B: lda q4 + inca (sta/inc/lda mtmp) is 8 bytes and 14 cy; ldx q4/inx/txa is 4 bytes and 7 cy. A is the same, C is untouched, and X is dead (ldx q5 follows). The Master text is unchanged.

## r047:2  src/logic.s:3100-3103  (window i2560)  M -0 B -4  [high]  review: accept

Cycles: Master: cmp and eor are both 2 cy, and stz fe = sta fe = 3 cy, so no change. Model B: stza (pha/lda#0/sta/pla, 12 cy) becomes sta fe (3 cy). The not-equal path is identical.

Assumption: ob_vanish is only reached through the jmpx table (logic.s:2037), on the staged path (type 11 >= NLEAN=2). process_object then does setbank BANK_LVL (lda #) and an lda/sta store-back before returning (logic.s:1985-2006), so A is dead. That store-back does not touch C, and C is dead at both callers: logic.s:1059 goes setbank / ldx bent / lda LV_BNEXT,x / bra @walk -> cmp #$FF; logic.s:1100 goes inc BINI / bra @rl2 -> cpx NOTHL. Z/N are overwritten the same way. The two targets already disagree on Z at this rts (Master Z=1, Model B Z=0), which fits flags being dead. fe = $B4, zero page.

```
        lda fe
        cmp #48
        bne @done
        stza fe
```
->
```
        lda fe
        eor #48                     ; A = 0 when fe = 48 (A and C dead on return)
        bne @done
        sta fe
```

eor #48 sets Z exactly when fe = 48, as cmp #48 does. On the equal path A = 0, so sta fe writes 0, the same as stz fe. At rts only A and C differ: A = fe^48 instead of fe or 48, and C is not set by eor. Both are dead. @done and the earlier bne @done are unchanged.

Review: eor #48 gives Z iff fe=48, and then A=0 so sta fe stores 0. A, C and N/Z are dead at return: ob_vanish is reached only via the jmpx table on the staged path, the store-back reloads A, and the callers at 1059 (setbank, ldx/lda, @walk cmp) and 1100 (inc BINI, cpx) overwrite the flags. The Model B stza (6 bytes) becomes sta zp (2 bytes).

## r047:3  src/logic.s:3126-3138  (window i2585)  M -6 B -6  [high]  review: accept

Cycles: One loop iteration: 46 -> 38 cycles on both targets, plus the unchanged mapbyte/mapput bodies.

Assumption: maptile returns Y = X = fa (logic.s:305-309; maprow keeps X). mapbyte and mapput preserve Y (engine.s:4544,4552), and both end in pagelogic, which keeps A,X,Y,C. q2/q3 ($C4/$C5) are transient temps that nothing reads afterwards: the rest of the loop and @set/@draw use fa, q5, q4 and fd; the process_object store-back and its callers do not read them; m_addsprite is engine code with its own zero page. Y is dead after the second mapput: lines 3139-3148 do not read it, m_mark_dirty clobbers it, and the loop restarts at ldx fa / lda q5 / jsr maptile, which sets Y.

```
        dey
        dey
        jsr mapbyte
        sta q2
        iny
        jsr mapbyte
        sta q3
        iny
        lda q2
        jsr mapput
        iny
        lda q3
        jsr mapput
```
->
```
        dey
        dey
        jsr mapbyte                 ; A = (row),fa-2
        iny
        iny
        jsr mapput                  ; -> (row),fa
        dey
        jsr mapbyte                 ; A = (row),fa-1
        iny
        iny
        jsr mapput                  ; -> (row),fa+1
```

Original: read m[fa-2] -> q2 and m[fa-1] -> q3, then write m[fa]=q2 and m[fa+1]=q3. New: read m[fa-2] and write it straight to m[fa], then read m[fa-1] and write it to m[fa+1]. The only reordering is that m[fa-1] is now read after m[fa] is written. They are different cells (fa-1 and fa in the same row, distinct mod 256), so every read sees the same value and the final map is identical. The write order (fa then fa+1) is unchanged, and Y still ends at fa+1.

Review: The four cells fa-2, fa-1, fa and fa+1 are distinct mod 256, so reading fa-1 after writing fa gives the same value. At exit Y=fa+1 and A=m[fa-1], as before. q2/q3 are dead. Each mapput has its own wrsel, so interleaving it with mapbyte is safe on the write-select boards. 25->19 bytes, 22->14 cy of overhead.

## r047:4  src/logic.s:3142-3144  (window i2591)  M -0 B -4  [high]  review: accept

Cycles: Master: text identical. Model B: lda fa + inca is 8 bytes and 14 cy; ldx fa/inx/txa is 4 bytes and 7 cy.

Assumption: Same as the ob_vanish instance: both forms preserve C, X is reloaded immediately by ldx q5, and mtmp is scratch only.

```
        lda fa
        inca
        ldx q5
```
->
```
  .if MODELB
        ldx fa                      ; inca is 6 bytes here
        inx
        txa
  .else
        lda fa
        inca
  .endif
        ldx q5
```

Model B: A = fa+1 mod 256 and C is unchanged; the flags are overwritten by ldx q5.

Review: Same as index 1: X is reloaded by ldx q5 and C is preserved. Model B saves 4 bytes and 7 cy.

## r047:5  src/logic.s:3149-3150  (window i2597)  M -2 B -2  [high]  review: accept

Cycles: lda #1 (2) + sta zp (3) = 5 cy; inc zp = 5 cy. Same on both targets. A differs, but @draw reloads it.

Assumption: fd ($B2, zero page) is 0 on every path to @set. ob_switch starts with lda fd / bne @draw. Between there and @set the only things that run are inrange (A,X), addscore (score, BARDIRTY), the SFXREQ store, q4/q5, the map helpers and m_mark_dirty. None of them writes fd: it is a fixed logic-owned zp address, and the original already relies on it surviving m_addsprite for the store-back. @set is reached by beq @set and by the loop fall-through; A is not used before @draw reloads it with lda fd.

```
@set:   lda #1
        sta fd
```
->
```
@set:   inc fd                      ; fd = 0 here (bne @draw fell through): now 1
```

fd goes 0 -> 1 in both versions. At @draw, lda fd loads 1. inc sets N=0,Z=0 as lda #1 does, and lda fd overwrites them anyway.

Review: fd ($B2) is 0 on both paths to @set (bne @draw fell through). inrange, addscore/bar_touch, the map helpers, m_mark_dirty and farcall never write $B2: engine ZEROPAGE ends below $A3 on both maps, and the Model B fixed zp equates are $A3-$A7 and $DF-$EF. inc zp is 5 cy, the same as lda#/sta zp. A is dead because @draw reloads it.

## r047:6  src/logic.s:3172-3178  (window i2610)  M -2 B -0  [high]  review: accept

Cycles: Master: pha (3) + pla (4) removed, -7 cy. Model B text unchanged.

Assumption: On the Master, setbank HUD_BANK is bankimm lda #n / sta ROMSEL_CPY / sta ROMSEL / wrsel; BANKREF and wrsel are empty on the Master, so no recorded site moves. None of these touches C. w16 is a zero-page .res (engine.s:208), not sideways RAM, so it does not matter which bank is selected at sta w16+1. This code runs from main RAM (segment CODE on the Master).

```
  .if .not MODELB
        pha
        setbank HUD_BANK
        pla
  .endif
        lsr                         ; C = d bit0, A = d >> 1
        sta w16+1
```
->
```
        lsr                         ; C = d bit0, A = d >> 1
        sta w16+1
  .if .not MODELB
        setbank HUD_BANK            ; (lda #/sta/sta: C survives)
  .endif
```

Master: the original saves A around setbank so it can do lsr/sta afterwards. Doing lsr and sta w16+1 first leaves C = d bit0 intact through setbank (lda/sta only). lda #0 follows, so A after setbank is dead. The skip path (beq bd_same) is before this, so it still does not touch the bank.

Review: On the Master, setbank HUD_BANK is lda #/sta/sta (BANKREF and wrsel are empty), so C from lsr survives it. w16 is zero page, so the selected bank does not matter for the store. A is dead after setbank (lda #0 follows). Saves pha/pla: 2 bytes, 7 cy. The Model B text is unchanged.

## r047:7  src/logic.s:3179-3191  (window i2623)  M -3 B -7  [high]  review: accept

Cycles: Master: +sta ptr+1 (3) -clc (2) -and (2) -stz ptr+1 (3) = -4 cy. Model B: +3 -2 -2 -12 (stza) = -13 cy.

Assumption: Every caller of bar_digit passes an even X: draw_lives 18, draw_health 46, draw_stars 74/82, draw_score (X+4)*8+76 (logic.s:3236-3279). grep of src and modelb/src finds no other reference. ptr (engine.s:200) and w16 (engine.s:208) are distinct zp words, and nothing reads or writes ptr+1 between line 3179 and the old stza.

```
        lda #0
        ror                         ; A = d0 << 7
        lsr w16+1                   ; C = d bit1, w16+1 = d >> 2
        ror                         ; A = d1 << 7 | d0 << 6
        clc
        adc #<SPR_DIGITS
        sta w16
        lda w16+1
        adc #>SPR_DIGITS
        sta w16+1                   ; digit tile
        txa
        and #$FE                    ; char = x/2, then *8 -- i.e. (x & $FE) * 4
        stza ptr+1
```
->
```
        lda #0
        sta ptr+1                   ; for the x * 4 below (sta keeps C)
        ror                         ; A = d0 << 7
        lsr w16+1                   ; C = d bit1, w16+1 = d >> 2
        ror                         ; A = d1 << 7 | d0 << 6; C = 0 (A bit 0 was 0)
        adc #<SPR_DIGITS
        sta w16
        lda w16+1
        adc #>SPR_DIGITS
        sta w16+1                   ; digit tile
        txa                         ; x is even at every call: x * 4 = char * 8
```

(1) clc: before the second ror, A = d0<<7 ($00 or $80) with bit 0 = 0, so ror leaves C = 0 and the clc was a no-op. (2) ptr+1 := 0 moves earlier, to where A already holds 0; sta keeps C for the ror, and nothing in between touches ptr+1. (3) and #$FE on an even X is the identity, and bit 7 is kept either way, so the asl/rol carries are the same. The Model B also loses the stza pha/pla. A, C and ptr+1 at the first asl are identical to the original.

Review: The clc is redundant: before the second ror, A is $00 or $80, so ror shifts out bit 0 = 0. ptr+1 is disjoint from w16, and sta keeps C for the ror. Every bar_digit caller passes an even X (18, 46, 74, 82, and (X+4)*8+76 in draw_score, where the adc has C=0 and even operands), and grep finds no other callers, so and #$FE is the identity. Assembled byte deltas confirmed.

## r047:8  src/logic.s:3196-3197  (window i2628)  M -2 B -2  [high]  review: accept

Cycles: -2 cy on both targets

Assumption: BARADDR = $0300 / $2B00 (engine.s:137,172), so its low byte is 0 on both targets; the .assert guards this. C = 0 here: the second rol ptr+1 shifts out bit 7 of ptr+1, which is 0 because ptr+1 <= 1 before it (as the existing comment at line 3198 says).

```
        adc #<BARADDR               ; straight into the back buffer's bar row (no offscreen
        sta ptr                     ; buffer): bank 4 glyph at $8xxx, screen bar at $7Bxx
```
->
```
  .assert <BARADDR = 0, error, "bar_digit: the low-byte add was dropped"
        sta ptr                     ; (<BARADDR = 0 and C = 0: nothing to add)
```

adc #0 with C = 0 leaves A unchanged and C = 0. The following sta/lda do not touch C, so adc #>BARADDR sees the same C. V is not used.

Review: After the second rol ptr+1, C is the old bit 7 of ptr+1 (which was at most 1), so C=0. <BARADDR = 0 on both targets ($0300 and $2B00), so adc #0 is a no-op and C stays 0 for adc #>BARADDR. The .assert assembles on both.

## r047:9  src/logic.s:3228-3263  (window i2679)  M -7 B -8  [high]  review: accept

Cycles: redraw_hud: -3 cy (the jmp draw_score is gone). draw_stars: -12 cy (no jsr/rts). div10 loop: bra (Master, 3 cy) or jmp (Model B, 3 cy) becomes bcs taken (3 cy), so each iteration costs the same.

Assumption: draw_score is the next line (3264) after the replaced range, so redraw_hud falls into it. div10 has exactly one caller (logic.s:3246); grep of src and modelb/src finds no other div10 reference apart from div10_16, which is a different label. bar_digit does not touch q1 (it uses w16, ptr and BARCACHE). The labels redraw_hud, draw_lives, draw_health and draw_stars all remain: the engine.s TOBANK t_ wrappers and t_redraw_hud = redraw_hud resolve them by name, and none is at a fixed address.

```
redraw_hud:
        jsr draw_lives
        jsr draw_health
        jsr draw_stars
        jmp draw_score

draw_lives:
        lda lives
        ldx #18
        ldy #0
        jmp bar_digit
draw_health:
        lda health
        ldx #46
        ldy #1
        jmp bar_digit
draw_stars:                         ; stars remaining: 2 digits at 74, 82
        lda stars
        jsr div10
        ldx #74
        ldy #2
        jsr bar_digit
        lda q1
        ldx #82
        ldy #3
        jmp bar_digit
; A = A / 10 ; q1 = A mod 10
div10:  ldx #0
:       cmp #10
        bcc :+
        sbc #10
        inx
        bra :-
:       sta q1
        txa
        rts
```
->
```
draw_lives:
        lda lives
        ldx #18
        ldy #0
        jmp bar_digit
draw_health:
        lda health
        ldx #46
        ldy #1
        jmp bar_digit
draw_stars:                         ; stars remaining: 2 digits at 74, 82
        lda stars
        ldx #0                      ; X = A / 10, q1 = A mod 10 (div10, inlined)
:       cmp #10
        bcc :+
        sbc #10
        inx
        bcs :-                      ; always: A >= 10 went in, so C = 1
:       sta q1
        txa
        ldx #74
        ldy #2
        jsr bar_digit
        lda q1
        ldx #82
        ldy #3
        jmp bar_digit
redraw_hud:
        jsr draw_lives
        jsr draw_health
        jsr draw_stars              ; and falls into draw_score
```

The HUD call order is unchanged (lives, health, stars, score); the jmp draw_score becomes a fall-through. div10's body is inlined into its only caller and gives the same A, X and q1. bra :- becomes bcs :-. That branch is only reached after cmp #10 did not branch (A >= 10, C = 1) and sbc #10 (no borrow, so C = 1); inx does not touch C, so bcs is always taken.

Review: draw_score immediately follows line 3263, so the fall-through is valid. div10 has one caller, and its inlined body gives the same A and q1 (X is overwritten by ldx #74). bcs :- is always taken (C=1 after sbc on A>=10). The anonymous labels move together with their only references, and no ':' lies between the old and new positions. The engine reaches every moved routine by name (TOBANK, t_redraw_hud).

## r047:10  src/logic.s:3270-3272  (window i2686)  M -0 B -2  [high]  review: accept

Cycles: Master: plx 4 + phx 3 + txa 2 = 9 cy and pla 4 + pha 3 + tax 2 = 9 cy, 3 bytes each. Model B: pla/tax/txa/pha/txa is 5 bytes and 13 cy; the new form is 3 bytes and 9 cy.

Assumption: None beyond the three lines: the following clc/adc #4 uses A, and X holds the same value as before.

```
        plx
        phx
        txa
```
->
```
        pla                         ; = plx / phx / txa: A = X = the count, still stacked
        pha
        tax
```

Original: pull the count into X, push it back, A = X. New: pull it into A, push it back, X = A. The stack is the same, A = X = count, and N/Z come from tax on the same value that txa loaded. C is untouched in both (clc follows).

Review: A=X=count, and the stack is unchanged. N/Z come from tax on the same value, and C is untouched (clc follows). Master: same size and cycles. Model B: plx/phx/txa is 5 bytes and 13 cy; the new form is 3 bytes and 9 cy.

## r047:11  src/logic.s:3168-3263  (window i2633)  M -17 B -20  [high]  review: accept

Cycles: The sum of the four candidates it contains (setbank move, bar_digit head, adc #<BARADDR, HUD reorder), plus draw_health falling into bar_digit (-3 cy, the jmp is gone). Every path is no slower on either target.

Assumption: This is a SUPERSET of the four candidates on lines 3172-3178, 3179-3191, 3196-3197 and 3228-3263: apply it INSTEAD of them. It stops matching once any of them is applied, and they stop matching once it is. One extra fact: draw_health is reached only by name (jsr from redraw_hud, and the TOBANK wrapper at engine.s:4774), so it can sit anywhere in the segment. It and bar_digit are under the same PLACE (line 3159 governs lines 3159-3297).

```
bar_digit:
        cmp BARCACHE,y              ; one bar, so one cache: no curbuf in the index
        beq bd_same
        sta BARCACHE,y
  .if .not MODELB
        pha
        setbank HUD_BANK
        pla
  .endif
        lsr                         ; C = d bit0, A = d >> 1
        sta w16+1
        lda #0
        ror                         ; A = d0 << 7
        lsr w16+1                   ; C = d bit1, w16+1 = d >> 2
        ror                         ; A = d1 << 7 | d0 << 6
        clc
        adc #<SPR_DIGITS
        sta w16
        lda w16+1
        adc #>SPR_DIGITS
        sta w16+1                   ; digit tile
        txa
        and #$FE                    ; char = x/2, then *8 -- i.e. (x & $FE) * 4
        stza ptr+1
        asl
        rol ptr+1
        asl
        rol ptr+1
        adc #<BARADDR               ; straight into the back buffer's bar row (no offscreen
        sta ptr                     ; buffer): bank 4 glyph at $8xxx, screen bar at $7Bxx
        lda ptr+1                   ; C is already clear: ptr+1 was 0 or 1 before the second
        adc #>BARADDR               ; rol, so that rol shifted a 0 out
        sta ptr+1
        ldy #31
:       lda (w16),y
        sta (ptr),y
        dey
        bpl :-
        lda w16
        adc #32                     ; C still clear: ptr+1 <= 3 and >BARADDR = $7B, so the
        sta w16                     ; adc above cannot carry, and the copy loop leaves C
        bcc @nc
        inc w16+1
@nc:    add16i ptr, 640
        ldy #31
:       lda (w16),y
        sta (ptr),y
        dey
        bpl :-
  .if .not MODELB
        setbank BANK_LVL, BANK_LVL
  .endif
bd_same:
        rts

bar_touch:
        lda #1
        sta BARDIRTY
        rts

redraw_hud:
        jsr draw_lives
        jsr draw_health
        jsr draw_stars
        jmp draw_score

draw_lives:
        lda lives
        ldx #18
        ldy #0
        jmp bar_digit
draw_health:
        lda health
        ldx #46
        ldy #1
        jmp bar_digit
draw_stars:                         ; stars remaining: 2 digits at 74, 82
        lda stars
        jsr div10
        ldx #74
        ldy #2
        jsr bar_digit
        lda q1
        ldx #82
        ldy #3
        jmp bar_digit
; A = A / 10 ; q1 = A mod 10
div10:  ldx #0
:       cmp #10
        bcc :+
        sbc #10
        inx
        bra :-
:       sta q1
        txa
        rts
```
->
```
draw_health:                        ; falls into bar_digit
        lda health
        ldx #46
        ldy #1
bar_digit:
        cmp BARCACHE,y              ; one bar, so one cache: no curbuf in the index
        beq bd_same
        sta BARCACHE,y
        lsr                         ; C = d bit0, A = d >> 1
        sta w16+1
  .if .not MODELB
        setbank HUD_BANK            ; (lda #/sta/sta: C survives)
  .endif
        lda #0
        sta ptr+1                   ; for the x * 4 below (sta keeps C)
        ror                         ; A = d0 << 7
        lsr w16+1                   ; C = d bit1, w16+1 = d >> 2
        ror                         ; A = d1 << 7 | d0 << 6; C = 0 (A bit 0 was 0)
        adc #<SPR_DIGITS
        sta w16
        lda w16+1
        adc #>SPR_DIGITS
        sta w16+1                   ; digit tile
        txa                         ; x is even at every call: x * 4 = char * 8
        asl
        rol ptr+1
        asl
        rol ptr+1
  .assert <BARADDR = 0, error, "bar_digit: the low-byte add was dropped"
        sta ptr                     ; (<BARADDR = 0 and C = 0: nothing to add)
        lda ptr+1                   ; C is already clear: ptr+1 was 0 or 1 before the second
        adc #>BARADDR               ; rol, so that rol shifted a 0 out
        sta ptr+1
        ldy #31
:       lda (w16),y
        sta (ptr),y
        dey
        bpl :-
        lda w16
        adc #32                     ; C still clear: ptr+1 <= 3 and >BARADDR = $7B, so the
        sta w16                     ; adc above cannot carry, and the copy loop leaves C
        bcc @nc
        inc w16+1
@nc:    add16i ptr, 640
        ldy #31
:       lda (w16),y
        sta (ptr),y
        dey
        bpl :-
  .if .not MODELB
        setbank BANK_LVL, BANK_LVL
  .endif
bd_same:
        rts

bar_touch:
        lda #1
        sta BARDIRTY
        rts


draw_lives:
        lda lives
        ldx #18
        ldy #0
        jmp bar_digit
draw_stars:                         ; stars remaining: 2 digits at 74, 82
        lda stars
        ldx #0                      ; X = A / 10, q1 = A mod 10 (div10, inlined)
:       cmp #10
        bcc :+
        sbc #10
        inx
        bcs :-                      ; always: A >= 10 went in, so C = 1
:       sta q1
        txa
        ldx #74
        ldy #2
        jsr bar_digit
        lda q1
        ldx #82
        ldy #3
        jmp bar_digit
redraw_hud:
        jsr draw_lives
        jsr draw_health
        jsr draw_stars              ; and falls into draw_score
```

Each part is argued in its own candidate. The new part: draw_health moves to just before bar_digit, so its jmp bar_digit becomes a fall-through with the same A, X and Y.

Review: This is an exact superset of indices 6, 7, 8 and 9 (apply it instead of them), plus draw_health moved to just before bar_digit so it falls into it with A/X/Y the same: 3 bytes and 3 cy each. Everything is in the same PLACE (CODE/LGCCODE) with no intervening PLACE, bar_digit's @nc scope is intact, and draw_health is referenced only by name. Applied together with 0-5 and 10 it assembles on both targets: Master CODE -17, Model B LGCCODE -54 in total (= 32+20+2).

## r048:0  src/logic.s:3244-3263  (window i2688)  M -5 B -6  [high]  review: accept

Cycles: div10 inlined: jsr (6) + rts (6) + txa (2) removed = -14 on both targets; loop: bra (Master 3) / jmp (Model B 3) -> bcs taken 3, equal; sta q1 -> stx q1 equal; every other instruction unchanged. Every stars value: 14 cycles faster on both

Assumption: div10 has no other caller (grep 'div10' across src/ and modelb/src: only draw_stars' jsr; div10_16/m_div10_16 are different labels). draw_stars' only caller is redraw_hud (logic.s:3231; the TOBANK m_draw_stars wrapper at engine.s:4778 has no caller), which next calls draw_score, whose div10_16 overwrites q1 before any read, so q1 = tens (not units) after draw_stars is unobserved. bar_digit preserves q1 (the original already reloads q1 after it). The two digits go to independent BARCACHE slots/bar columns, so drawing slot 3 before slot 2 gives the same bar.

```
draw_stars:                         ; stars remaining: 2 digits at 74, 82
        lda stars
        jsr div10
        ldx #74
        ldy #2
        jsr bar_digit
        lda q1
        ldx #82
        ldy #3
        jmp bar_digit
; A = A / 10 ; q1 = A mod 10
div10:  ldx #0
:       cmp #10
        bcc :+
        sbc #10
        inx
        bra :-
:       sta q1
        txa
        rts
```
->
```
draw_stars:                         ; stars remaining: 2 digits at 74, 82
        lda stars
        ldx #0                      ; X = stars / 10, A = stars mod 10
:       cmp #10
        bcc :+
        sbc #10
        inx
        bcs :-                      ; C = 1: the sbc did not borrow
:       stx q1                      ; tens digit, drawn second
        ldx #82
        ldy #3
        jsr bar_digit
        lda q1
        ldx #74
        ldy #2
        jmp bar_digit
```

Inlining div10 removes jsr/rts. After the loop A = stars mod 10 and X = stars/10 exactly as div10 produced them (div10 then did sta q1 (units) / txa (tens in A)). Instead store the tens digit to q1 (stx) and draw the units digit (already in A) at slot 3 col 82 first, then reload q1 and draw the tens at slot 2 col 74: same (digit,col,slot) pairs. bra -> bcs: the bcs follows sbc #10 executed only when cmp #10 left C=1 (A>=10), so sbc cannot borrow and C=1; inx does not affect C; the branch is always taken, 3 cycles, same as bra/jmp.

Review: Original matches lines 3244-3263. div10's only caller is draw_stars; draw_stars' only live caller is redraw_hud (t_draw_stars TOBANK wrapper uncalled). After the loop A=units, X=tens as before; bcs after sbc #10 (taken only when cmp left C=1) is always taken, 3 cycles like bra/jmp. Digits go to independent BARCACHE slots/columns, bar_digit does not read q1, so drawing slot 3 before slot 2 gives the same bar. q1 now ends as tens, but draw_score's div10_16 overwrites q1 before any read (and still follows draw_stars if candidate 3 is applied). Anonymous label count/order unchanged. -14 cycles on both. Bytes confirmed by assembly (the four candidates together: Master CODE -13 / LOGIC -4, Model B LGCCODE -21). Overlaps r047's candidates on lines 3228-3263 / 3168-3263, so apply only one of them.

## r048:1  src/logic.s:3265-3300  (window i2711)  M -6 B -13  [high]  review: accept

Cycles: draw_score per digit (5 iterations): Master removes phx3+plx4+clc2+adc#4 2 and adds cpx2 (phx/txa -> txa/pha 5->5) = -9/iter; Model B removes phx5+plx6+clc2+adc2, phx+txa 7 -> txa+pha 5, adds cpx2 = -15/iter; loop exit bpl 2 -> bcs 2 equal. div10_16 entry: Master stz3+ldx2+lda3=8 -> lda2+ldy2=4; Model B stza(pha3 lda2 sta3 pla4)+ldx2+lda3=17 -> 4; ldx/dex -> ldy/dey equal. All paths faster or equal on both.

Assumption: div10_16 callers: draw_score (here), menu.s:673 draw_number (Master, direct jsr) and Model B menu.s:671 via m_div10_16 far call (engine.s:4867, FARTAB entry banks.s:1269 points at the label, unchanged). draw_number wraps the call in phx/plx and never uses Y: after the loop it does ldx #0 ... and ends 'jmp drawtext', which starts with ldy #0 (menu.s:16). farcall (modelb/src/low.s:22-50) passes Y through and clobbers X regardless. So Y = 0 instead of preserved, and X preserved instead of 0, on div10_16 return are both unobserved by existing callers. q1's value on entry to div10_16 is never read (it is written by the final sta q1 at logic.s:3301, kept). bar_digit (logic.s:3168) does not rely on C on entry (it starts with cmp). The slot*8 math: slot<=8 so asl x3 shifts out zeros, C=0 before adc #76.

```
        mov16 t16, score
        ldx #4
@d:     phx
        ; t16 /= 10 -> remainder
        jsr div10_16
        plx
        phx
        txa
        clc
        adc #4                      ; score digits are slots 4..8
        tay
        asl
        asl
        asl
        adc #76                     ; (X+4)*8 + 76 = X*8 + 108
        tax
        lda q1
        jsr bar_digit
        plx
        dex
        bpl @d
        rts
; t16 = t16 / 10 ; q1 = remainder
div10_16:
        stza q1
        ldx #16
        lda q1                  ; remainder lives in A for the whole loop
@l:     asl t16
        rol t16+1
        rol a                   ; was rol q1 / lda q1
        cmp #10
        bcc :+
        sbc #10                 ; was sbc #10 / sta q1
        inc t16
:       dex
        bne @l
```
->
```
        mov16 t16, score
        ldx #8                      ; X = digit slot 8..4 (div10_16 keeps X)
@d:     jsr div10_16                ; t16 /= 10 -> remainder
        txa                         ; not phx/txa: on a 6502 that is txa/pha/txa
        pha
        tay                         ; score digits are slots 4..8
        asl
        asl
        asl                         ; C = 0: slot*4 < 128
        adc #76                     ; slot*8 + 76 = 108..140
        tax
        lda q1
        jsr bar_digit
        plx
        dex
        cpx #4
        bcs @d
        rts
; t16 = t16 / 10 ; q1 = remainder
div10_16:
        lda #0                      ; remainder lives in A for the whole loop
        ldy #16                     ; Y, not X: draw_score keeps its slot in X
@l:     asl t16
        rol t16+1
        rol a                       ; was rol q1 / lda q1
        cmp #10
        bcc :+
        sbc #10                     ; was sbc #10 / sta q1
        inc t16
:       dey
        bne @l
```

div10_16: stza q1 / ldx #16 / lda q1 only establishes A=0 and a count of 16 (q1 is overwritten at exit; flags and C are reset by the first asl) -> lda #0 / ldy #16; dex -> dey with identical Z semantics. Since div10_16 no longer touches X, draw_score keeps the counter in X across the call, dropping the plx/phx pair. Counting X as the slot itself (8 down to 4) instead of 4..0 removes clc/adc #4; the loop test becomes dex/cpx #4/bcs (continue while X>=4): iterations X=8,7,6,5,4, same order as the old X=4..0 (first remainder -> slot 8, col 140). Column: old (X+4)*8+76 == slot*8+76. txa/pha replaces phx/txa: A = X afterwards either way, same stack contents. bar_digit gets A=q1 digit, X=col, Y=slot as before; plx restores the slot.

Review: Correct, but the byte counts are overstated by one on each target. Hand count: draw_score goes 29->26 on Master (33->27 on Model B, where the phx/plx macros are 2 bytes) and div10_16's head goes 6->4 (Model B 10->4), giving 5/12, not 6/13. Assembly confirms it: Master CODE -13 = 5 (cand 0) + 5 + 3 (cand 3); Model B LGCCODE -21 = 6 + 12 + 3. Correctness: div10_16 now preserves X and returns Y=0; its exit flags are the same (dey/bne gives Z=1 like dex). Callers: draw_score; draw_number (Master direct jsr, Model B m_div10_16 farcall F_DIV10). draw_number wraps the call in phx/plx and never reads Y before 'jmp drawtext', which does ldy #0. farcall passes Y through and reloads X. q1 is never read on entry. The slot loop X=8..4 (dex/cpx #4/bcs) gives the same slot, column and remainder order as the old X=4..0. asl x3 of a slot <= 8 leaves C=0 before adc #76. txa/pha leaves the same stack and A as phx/txa, and plx restores the slot. Every path is faster on both targets: -9/-15 cycles per digit, and div10_16's entry drops 8->4 (Master) and 17->4 (Model B).

## r048:2  src/menu.s:669-676  (window i2711)  M -4 B -0  [high]  review: accept

Cycles: Master: phx3 + plx4 + lda q1 3 removed = -10 per digit; Model B: identical instruction stream

Assumption: DEPENDS ON the i2711 logic.s candidate (div10_16 counting in Y so X survives it) -- apply only after it. div10_16 ends 'sta q1 / rts', so A = q1 on return (Master direct jsr). Y not used by draw_number (it ends in jmp drawtext which does ldy #0). The next instruction 'ora #'0'' sets its own flags. Model B path unchanged (farcall clobbers X; its plx clobbers A).

```
@d:     phx
  .if MODELB
        jsr m_div10_16
  .else
        jsr div10_16
  .endif
        plx
        lda q1
```
->
```
@d:
  .if MODELB
        phx
        jsr m_div10_16
        plx
        lda q1
  .else
        jsr div10_16                ; X survives (Y counts), A = q1 on return
  .endif
```

Master: with div10_16 no longer touching X, phx/plx around it are redundant; and since plx no longer intervenes, A still holds the remainder div10_16 stored to q1, so lda q1 is redundant. Model B keeps phx/jsr m_div10_16/plx/lda q1 exactly.

Review: Original matches menu.s 669-676. It must be applied after candidate 1, which makes div10_16 count in Y so X survives. With that, on the Master X survives the direct jsr, and A equals q1 on return because div10_16 ends 'sta q1 / rts'. The next instruction, ora #'0', sets its own flags. Y is not read before drawtext's ldy #0. The Model B instruction stream is unchanged. Master saves 4 bytes and 10 cycles per digit (confirmed by assembly: Master LOGIC -4).

## r048:3  src/logic.s:3228-3232  (window i2688)  M -3 B -3  [medium]  review: accept

Cycles: old overhead: jsr draw_lives 6 + its return rts 6 + jmp draw_score 3 = 15; new: jsr draw_score 6 + its rts 6 = 12, then fall into draw_lives whose jmp bar_digit returns to the caller as draw_score's did. -3 cycles on both targets

Assumption: NOTE: these lines precede i2688 (redraw_hud is instructions ~i2683-i2686, region r047) -- may duplicate an r047 candidate. redraw_hud is directly followed by draw_lives (logic.s:3234, only a blank line between). Callers: engine.s:3090 'jsr t_redraw_hud' then stz BARDIRTY (A/X/Y/flags dead; ptr/w16 scratch that already differ between bar_digit's skip and draw paths); the TOBANK m_redraw_hud wrapper has no other caller. q1 and t16 at exit are identical (draw_lives touches neither; draw_score's are the last written). Digits go to independent BARCACHE slots and bar columns, so drawing lives last gives the same bar.

```
redraw_hud:
        jsr draw_lives
        jsr draw_health
        jsr draw_stars
        jmp draw_score
```
->
```
redraw_hud:                         ; falls into draw_lives
        jsr draw_health
        jsr draw_stars
        jsr draw_score
```

Reordering the four independent HUD draws so draw_lives is last lets redraw_hud fall through into it: jsr draw_lives + jmp draw_score (6 bytes) become jsr draw_score (3 bytes).

Review: Original matches 3228-3232, and draw_lives follows at 3234 with only a blank line between. The only caller is t_redraw_hud (engine.s:3090: Master TOBANK jsr pagelogic/jmp, Model B an alias). What follows is stz BARDIRTY then lda wx..., so A/X/Y/flags are dead. X on exit (18 vs $FF) cannot be relied on, since that point is also reached when BARDIRTY=0. q1/t16 at exit are unchanged, because draw_lives and bar_digit touch neither. w16/ptr are scratch that were already data-dependent. The digits are independent cache slots, so the final bar is the same; saves 3 bytes and 3 cycles. One caveat: the lives digit is now drawn last instead of first, so in a rare frame where 3+ digits change and the ~2560-cycle bar deadline is overrun, a one-frame tear would land on a different digit. The total finish time is earlier, so this is not a correctness failure. Conflicts with r047's candidate over lines 3228-3263 (same lines), so apply only one.

## r049:0  src/game.s:17-19  (window i0000)  M -1 B -0  [high]  review: accept

Cycles: Master: 2 cycles faster (clc gone); Model B: code not assembled

Assumption: C = 0 after asl: A = X eor 1 with X = level (0..15, the only caller game.s:241 does ldx level), so bit 7 is clear and asl shifts out 0

```
        asl                         ; two pieces: map, bank-7 tables
        clc
        adc #FI_L0A
```
->
```
        asl                         ; two pieces: map, bank-7 tables (C = 0: X < 128)
        adc #FI_L0A
```

txa/eor #1 give 0..15; asl moves bit 7 (=0) into C, so C is already clear and clc is a no-op; adc #FI_L0A then computes the same A and flags

Review: Master-only code. Only caller is game.s:241 ldx level; level is 0..15 at every load (level_loop is reached only with level <16: fl_over sends 16 to game_won; level select gives asl of 0..7). eor #1 keeps it <128, so asl shifts 0 into C and clc is redundant. Assembled: Master CODE -1, Model B unchanged.

## r049:1  src/game.s:29-49  (window i0000)  M -6 B -12  [high]  review: accept

Cycles: Master 36->28 cycles before the loops (loops identical); Model B 54->30 (two stza at 12 each gone)

Assumption: A is not read before line 50's lda mapw (only X/memory in the loops); flags after the second loop come from the same dex/bne and the same last rol maph+1; LV_HDR is absolute on both (ldx abs exists). Anonymous label count unchanged (both ':' kept, no anon branch crosses the block)

```
        lda LV_HDR
        sta maplw
        lda LV_HDR+1
        sta maplh
        ; mapw = 8 << lw ; maph = 8 << lh
        lda #8
        sta mapw
        stza mapw+1
        ldx maplw
:       asl mapw
        rol mapw+1
        dex
        bne :-
        lda #8
        sta maph
        stza maph+1
        ldx maplh
:       asl maph
        rol maph+1
        dex
        bne :-
```
->
```
        lda #8                      ; mapw = 8 << lw ; maph = 8 << lh
        sta mapw
        sta maph
  .if MODELB
        lda #0
        sta mapw+1
        sta maph+1
  .else
        stz mapw+1
        stz maph+1
  .endif
        ldx LV_HDR
        stx maplw
:       asl mapw
        rol mapw+1
        dex
        bne :-
        ldx LV_HDR+1
        stx maplh
:       asl maph
        rol maph+1
        dex
        bne :-
```

maplw/maplh get the same header bytes (via X instead of A; stx instead of sta); mapw/maph low bytes = 8 and high bytes = 0 before each loop as before (all stores happen before either loop, and neither loop touches the other's variables); X enters each loop with maplw/maplh as before and leaves 0; the last rol maph+1 sets C identically. A differs (0 on B, 8 on Master, was 8) but is dead: reloaded by lda mapw. On Model B lda #0 + two sta replace two 6-byte stza; on the Master stz is kept. Byte savings confirmed by assembling both targets (CODE -6, LGCCODE -12)

Review: Original is lines 29-49 verbatim. All vars zero page; LV_HDR absolute, and ldx abs/stx zp exist on both. maplw/maplh get the same bytes, mapw/maph are 8/0 before their loops (neither loop touches the other's variables), X enters each loop with lw/lh. A is dead (line 50 lda mapw); flags and C at exit come from the same last rol/dex/bne. Two ':' kept, so the anon count is unchanged. Assembled: Master CODE -6, Model B LGCCODE -12. Cycles are lower on both.

## r049:2  src/game.s:50-52  (window i0002)  M -1 B -1  [medium]  review: accept

Cycles: 2 cycles faster on both (sec gone)

Assumption: C = 0 on entry: the preceding instruction path ends with rol maph+1 (dex/bne leave C alone), which shifts out bit 15 of 8<<(maplh-1); that is 0 for maplh <= 12 (every level header has lh 5..7: build/L?? headers; a map of 2^(lw+lh) bytes could not fit otherwise). Holds with or without the i0000 geometry candidate

```
        lda mapw
        sec
        sbc #WINPX
```
->
```
        lda mapw                    ; C = 0: the last rol shifted out maph's bit 15
        sbc #WINPX-1
```

SBC with C=0 and operand WINPX-1 computes A-(WINPX-1)-1 = A-WINPX, and C out = (A >= WINPX), identical to sec/sbc #WINPX; V identical too (159 and 160 both have bit 7 set, same result). maxwx+1's sbc #0 then sees the same carry

Review: C on entry is the carry out of the last rol maph+1, which is bit 15 of 8<<(lh-1). That bit is 0 for lh<=12. Every level's lh is 5..7 (read from the build/L?? headers; Model B packs the same lw/lh via convert.py). SBC with C=0 and #WINPX-1 gives the same A, C, N, Z and V as sec/sbc #WINPX, because A+~159+0 == A+~160+1. Also holds with candidate 1 applied. Assembled: -1 on both.

## r049:3  src/game.s:57-59  (window i0010)  M -1 B -1  [medium]  review: accept

Cycles: 2 cycles faster on both

Assumption: C = 1 after line 55's sbc #0: no 16-bit borrow because mapw >= WINPX (160). Every level's lw is 5..8 (mapw 256..2048; headers in build/L0A..L7B at offset LM_*256), which is also what clamp_window's maxwx >= 0 relies on

```
        lda maph
        sec
        sbc #VISLINES/2
```
->
```
        lda maph                    ; C = 1: mapw >= WINPX
        sbc #VISLINES/2
```

sec was setting a carry already set; sbc #VISLINES/2 computes the same value and flags

Review: C after line 55's sbc #0 is 1 when mapw >= 160. mapw = 8<<lw with lw 5..8 on every level, so mapw is 256..2048. Unaffected by candidates 1 and 2, whose carry is identical at line 55. Assembled: -1 on both.

## r049:4  src/game.s:64-75  (window i0022)  M -0 B -4  [high]  review: accept

Cycles: Model B: stza NSPR (12) -> sta NSPR (3); Master identical code

Assumption: Model B: lvreset (modelb/src/banks.s:175) ends with stz DIRTYCNT+1, whose 6502 expansion is lda #0/sta, so A = 0 on return. Master: code unchanged (stza NSPR moved inside the .else, same bytes)

```
  .if MODELB
        jsr lvreset                 ; the records (bank 7) and the buffers' state (main RAM)
  .else
        stza BUF_VALID
        stza BUF_VALID+1
        stza RECCNT
        stza RECCNT+1
        stza DIRTYCNT
        stza DIRTYCNT+1
  .endif
        stza NSPR
        rts
```
->
```
  .if MODELB
        jsr lvreset                 ; the records (bank 7) and the buffers' state (main RAM)
        sta NSPR                    ; A = 0: lvreset ends with a stz
  .else
        stza BUF_VALID
        stza BUF_VALID+1
        stza RECCNT
        stza RECCNT+1
        stza DIRTYCNT
        stza DIRTYCNT+1
        stza NSPR
  .endif
        rts
```

NSPR gets 0 either way. Original stza preserved A (=0 anyway) and ended with pla setting N=0,Z=1; the new sta leaves A=0 and the Z=1/N=0 from lvreset's lda #0, so even A and flags on return are identical

Review: lvreset (modelb/src/banks.s:175-182) ends stz DIRTYCNT+1, which the 6502 macro expands to lda #0/sta, so A=0, Z=1, N=0 at return. NSPR is zp $A3 on Model B, so sta NSPR is 2 bytes against stza's 6. Master text is unchanged; stza NSPR simply moves inside the .else. Assembled: Model B -4, Master 0. Caveat: this relies on lvreset keeping A=0 at exit, which any lda #0/sta rewrite of it preserves.

## r049:5  src/game.s:172-187  (window i0104)  M -0 B -5  [medium]  review: accept

Cycles: Model B: over-max path 23->17 cycles, zero path 18->13, in-range path unchanged (8); Master identical code

Assumption: On the over-max path C = 1: wx+1 >= 0 (bmi not taken) and maxwx >= 0 (every level's mapw >= 256 so maxwx = mapw-160 >= 96), so the 16-bit signed test N=0 coincides with no unsigned borrow. ALTERNATIVE to the next candidate (same lines, jmp instead of bcs, no data assumption); apply one

```
        lda maxwx
        sta wx
        lda maxwx+1
        sta wx+1
        bra @wxok
@wx0:   stz wx
        stz wx+1
@wxok:
  .if MODELB
        lda wx
        and #$FE
        sta wx
  .else
        lda #1
        trb wx                      ; wx &= ~1 : the 65C02 does this in one RMW
  .endif
```
->
```
  .if MODELB
        lda maxwx+1
        sta wx+1
        lda maxwx
        bcs @wxev                   ; C = 1: wx >= maxwx >= 0, no borrow
@wx0:   lda #0
        sta wx+1
        beq @wxev                   ; A = 0
@wxok:  lda wx
@wxev:  and #$FE
        sta wx
  .else
        lda maxwx
        sta wx
        lda maxwx+1
        sta wx+1
        bra @wxok
@wx0:   stz wx
        stz wx+1
@wxok:
        lda #1
        trb wx                      ; wx &= ~1 : the 65C02 does this in one RMW
  .endif
```

Model B over path: wx+1=maxwx+1, then A=maxwx and-ed with $FE into wx (the original stored maxwx then re-loaded, and-ed and stored: same result, A = wx at exit). Zero path: wx+1=0, A=0 and-ed -> 0 into wx (same as stz/stz then and). In-range path: lda wx/and/sta unchanged. Master branch is the original text verbatim. X,Y untouched; exit A = wx on all paths as before

Review: On the over-max path, bmi is not taken after the 16-bit wx - maxwx. wx+1 >= 0 and maxwx = mapw-160 is 96..1888 (lw 5..8), so the difference cannot overflow and N=0 implies no unsigned borrow, i.e. C=1, and bcs is always taken. The zero path and the in-range path give the same wx/wx+1 and the same exit A (=wx) and flags (from and). Master text is verbatim. Assembled: Model B -5. Over path 23->17 cycles, zero path 18->13. ALTERNATIVE to index 6 on the same lines; apply only one (this one saves more).

## r049:6  src/game.s:172-187  (window i0104)  M -0 B -4  [high]  review: accept

Cycles: Model B: over-max path 23->17 cycles, zero path 18->13, in-range path unchanged; Master identical code

Assumption: None beyond the window (no carry assumption: unconditional jmp). Alternative to the bcs candidate above on the same lines

```
        lda maxwx
        sta wx
        lda maxwx+1
        sta wx+1
        bra @wxok
@wx0:   stz wx
        stz wx+1
@wxok:
  .if MODELB
        lda wx
        and #$FE
        sta wx
  .else
        lda #1
        trb wx                      ; wx &= ~1 : the 65C02 does this in one RMW
  .endif
```
->
```
  .if MODELB
        lda maxwx+1
        sta wx+1
        lda maxwx
        jmp @wxev
@wx0:   lda #0
        sta wx+1
        beq @wxev                   ; A = 0
@wxok:  lda wx
@wxev:  and #$FE
        sta wx
  .else
        lda maxwx
        sta wx
        lda maxwx+1
        sta wx+1
        bra @wxok
@wx0:   stz wx
        stz wx+1
@wxok:
        lda #1
        trb wx                      ; wx &= ~1 : the 65C02 does this in one RMW
  .endif
```

As the bcs version: over path stores maxwx+1 to wx+1 and maxwx&$FE to wx; zero path stores 0 to both; in-range path unchanged; A at exit = wx on every path, X/Y untouched. The later lda wy+1 does not read flags

Review: Same as index 5 but with an unconditional jmp, so it needs no data assumption. Equivalence checked path by path. Assembled: Model B -4, Master 0. Mutually exclusive with index 5: it goes stale once 5 is applied.

## r049:7  src/game.s:200-201  (window i0118)  M -0 B -6  [high]  review: accept

Cycles: Model B zero path 24->8 cycles; Master identical code

Assumption: A dead after clamp_window returns: its only caller is m_clamp_window at logic.s:924 (Model B: m_clamp_window = clamp_window, engine.s:4809), followed at @cam by setbank BANK_LVL (bankimm lda first); flags also dead (lda then asl)

```
@wy0:   stza wy
        stza wy+1
```
->
```
  .if MODELB
@wy0:   lda #0                      ; A dead: the caller's setbank reloads it
        sta wy
        sta wy+1
  .else
@wy0:   stza wy
        stza wy+1
  .endif
```

wy and wy+1 both get 0 as before; only A (was preserved by stza, now 0) and N/Z differ, both dead at return

Review: The only Model B caller is logic.s:924 jsr m_clamp_window (= clamp_window, engine.s:4809), followed at @cam by setbank, whose bankimm lda reloads A. Flags are dead too. wy and wy+1 are both still zeroed; @wyok: rts is untouched. Assembled: Model B -6 (stza zp 6 bytes each). Master unchanged.

## r049:8  src/game.s:206-209  (window i0123)  M -0 B -14  [high]  review: accept

Cycles: Model B 49->15 cycles; Master identical code

Assumption: A dead at game_main: entered by jmp game_main (modelb/src/init.s:95); falls into title_loop whose first instruction on the Model B is jsr ensure_menu, which starts lda title_res (game.s:352). hiscore/hiscore+1/maxlevel zero page, title_res absolute (low.s LOWBSS)

```
        stza hiscore
        stza hiscore+1
        stza maxlevel
        stza title_res
```
->
```
  .if MODELB
        lda #0                      ; A dead: ensure_menu loads title_res first
        sta hiscore
        sta hiscore+1
        sta maxlevel
        sta title_res
  .else
        stza hiscore
        stza hiscore+1
        stza maxlevel
        stza title_res
  .endif
```

All four get 0 as before; the original stza preserved A and ended with pla; A and flags are dead (ensure_menu reloads A, and flags from lda title_res)

Review: game_main is entered only by jmp (modelb/src/init.s:95) and falls into title_loop. On Model B that runs jsr ensure_menu, which starts lda title_res (game.s:352), so A and flags are dead. All four locations still get 0. Assembled: Model B -14 (title_res absolute), Master unchanged.

## r050:0  src/game.s:176-178  (window i0097)  M -0 B -3  [high]  review: accept

Cycles: clamp path: bra/jmp 3 -> bpl taken 3 (both); @wx0 path Master stz,stz unchanged, Model B 5+5=10 -> 2+3+3=8

Assumption: N=0 after `lda maxwx+1 / sta wx+1` (lines 174-175): maxwx = mapw - WINPX (game.s:50-56), mapw = 8<<maplw, and every level header has maplw 5..8 (checked from v500 data via tools/convert.py parse_level: lw in {5,6,7,8}), so maxwx = 96..1888 and maxwx+1 = 0..7. A is dead at @wx0 on MB: @wxok's Model B code starts `lda wx` (line 181).

```
        bra @wxok
@wx0:   stz wx
        stz wx+1
```
->
```
        bpl @wxok                   ; N = 0: maxwx+1 is 0..7 (maps 256..2048 px wide)
  .if MODELB
@wx0:   lda #0
        sta wx
        sta wx+1
  .else
@wx0:   stz wx
        stz wx+1
  .endif
```

bra @wxok -> bpl @wxok: the only way here is the store of maxwx+1 whose bit 7 is clear, so the branch is always taken (3 cycles, 2 bytes; MB saves the jmp's third byte). MB @wx0: the two stz macros are lda #0/sta, lda #0/sta; the second lda #0 is redundant since A still holds 0. Flags after @wx0: both leave Z=1,N=0 and @wxok reloads A. Master text unchanged in effect.

Review: Text matches 176-178. N after sta wx+1 comes from lda maxwx+1; maxwx = (8<<maplw)-160 and every level header (convert.parse_level, all 16) has lw 5..8, so maxwx+1 = 0..7 and bpl is always taken (3cy = bra/jmp). clamp_window only runs from game_frame after load_level set maxwx. MB @wx0: A dead (@wxok MB starts lda wx). MB 11->8 bytes.

## r050:1  src/game.s:200-202  (window i0117)  M -0 B -6  [high]  review: accept

Cycles: @wy0 path MB: 2x(pha3+lda2+sta3+pla4)=24 -> 2+3+3=8 (+rts both); Master unchanged

Assumption: clamp_window's only caller is logic.s:924 (`jsr m_clamp_window`; MB m_clamp_window = clamp_window at engine.s:4809, Master TOMAIN at engine.s:4788 whose pagelogic preserves A); the next instruction there is `@cam: setbank BANK_LVL,...` = `lda #BANK_LVL`, so A and N/Z at return are dead. On other return paths A already differs (maxwy+1 or the sbc result), so no caller can depend on it.

```
@wy0:   stza wy
        stza wy+1
@wyok:  rts
```
->
```
  .if MODELB
@wy0:   lda #0                      ; (A is dead: the caller's setbank reloads it)
        sta wy
        sta wy+1
  .else
@wy0:   stza wy
        stza wy+1
  .endif
@wyok:  rts
```

stza keeps A = wy+1 across the two zero stores; since A is dead at return, one lda #0 and two stores write the same memory. Master keeps stza (= stz) so its bytes are unchanged.

Review: Only caller logic.s:924 jsr m_clamp_window (MB = clamp_window direct), followed by @cam: setbank = lda #BANK_LVL; A and flags dead. MB 12->6 bytes, faster. Master unchanged.

## r050:2  src/game.s:206-209  (window i0120)  M -0 B -14  [high]  review: accept

Cycles: MB: 3x12+13=49 -> 2+3+3+3+4=15; Master stza=stz identical

Assumption: game_main is entered only by `jmp game_main` (src/main.s:119, modelb/src/init.s:95) and falls into title_loop, whose first instruction on MB is `jsr ensure_menu` which starts `lda title_res` (game.s:352); on the Master t_title_menu -> pagelogic -> title_menu which starts `jsr load_title` (menu.s:405, no A input). So A and flags are dead.

```
        stza hiscore
        stza hiscore+1
        stza maxlevel
        stza title_res
```
->
```
  .if MODELB
        lda #0                      ; (A is free: ensure_menu reloads it)
        sta hiscore
        sta hiscore+1
        sta maxlevel
        sta title_res
  .else
        stz hiscore
        stz hiscore+1
        stz maxlevel
        stz title_res
  .endif
```

Four stza (pha/lda #0/sta/pla) store 0 to hiscore, hiscore+1, maxlevel (zp) and title_res (abs); one lda #0 followed by four stores does the same. Master spells stz as before (stza on the 65C02 is stz).

Review: game_main entered only by jmp (main.s:119, init.s:95); falls into title_loop whose MB first act is jsr ensure_menu -> lda title_res. Listing: 3x6 + 7 = 25 -> 11 bytes. Master .else spells stz (=stza on 65C02).

## r050:3  src/game.s:220-225  (window i0130)  M -0 B -4  [high]  review: accept

Cycles: MB 5+2+3+3+5+5=23 -> 5+3+3+2+3+3=19; Master unchanged (stz reordered, same instructions)

Assumption: MB stz macro is `lda #0 / sta` (cpu.inc) so A = 0 after `stz level`. Nothing reads score/lives/health between these stores (straight-line code). The following `lda maxlevel` (line 226) sets the flags the beq uses.

```
        stz level
        lda #3
        sta lives
        sta health
        stz score
        stz score+1
```
->
```
        stz level
  .if MODELB
        sta score                   ; A = 0 (the stz)
        sta score+1
  .else
        stz score
        stz score+1
  .endif
        lda #3
        sta lives
        sta health
```

The two score stz on MB are lda #0/sta each; hoisting them ahead of `lda #3` lets them reuse the zero already in A. Same final memory, A = 3 after in both versions.

Review: MB stz level = lda #0/sta, so A=0 for sta score/score+1; straight line, lda maxlevel sets the flags for beq. Listing: 8 -> 4 bytes. Master identical instructions reordered.

## r050:4  src/game.s:236-236  (window i0143)  M -0 B -2  [high]  review: accept

Cycles: MB pha3+lda2+sta4+pla4=13 -> lda2+sta4=6; Master stza==stz (9C) identical

Assumption: A dead at this point on MB: next instructions are `ldx level / jsr load_level` (lines 241-242); load_level on MB begins `jsr load_level_b` (game.s:11) which does `stx ld_level / jsr music_stop` and music_stop begins `stz MUSON` (engine.s:4199, lda #0) -- A never read. On the Master `lda #FI_BOX` follows. N/Z: ldx overwrites.

```
        stza title_res               ; the level's map replaces the title pack
```
->
```
        stz title_res               ; the level's map replaces the title pack
```

stza preserves A across the store; A is dead, so the plain stz (lda #0/sta on MB) is equivalent. Master: both spell `stz title_res`.

Review: MB: next is ldx level / jsr load_level -> jsr load_level_b (disc.s:289 stx, jsr music_stop -> stz MUSON = lda #0) -- A never read. Tools' search for ldx level within 24 bytes of level_loop still succeeds (+8). 7->5 bytes.

## r050:5  src/game.s:253-253  (window i0155)  M -3 B -6  [high]  review: accept

Cycles: Master -4 cycles, MB -12 cycles on the level-start path

Assumption: render_frame always ends with `stza NSPR` (engine.s:3130, unconditional, no early exit; only build_sections and the curbuf/flipreq stores follow, none touches NSPR). NSPR is written only by engine.s:1395 (addsprite, called from logic.s), engine.s:3130, engine.s:3591 (init) and game.s -- grep NSPR over src and modelb/src; no ISR writes it.

```
        stza NSPR
```
->
```

```

Line 253 immediately follows `jsr render_frame`, which has just set NSPR = 0, so the store is redundant. A/flags: the next instruction is `jsr t_game_frame`; game_frame (logic.s:888) starts inc frame / stza bounce / lda health -- no A or flag input (Master TOBANKI preserves A anyway).

Review: render_frame (engine.s:3075-3150) has no early exit and runs stza NSPR at 3130 unconditionally; nothing after it writes NSPR; only writers are addsprite (logic.s game_frame paths), engine.s:3130/3591, game.s. game_frame starts inc frame (A dead). Master abs stz 3 bytes, MB 6. Depends on engine.s:3130 staying (do not also accept a candidate removing that one).

## r050:6  src/game.s:257-257  (window i0159)  M -3 B -4  [high]  review: accept

Cycles: Master -4, MB -5 cycles

Assumption: NSPR = 0 after render_frame (engine.s:3130); set_palette does not touch NSPR (grep). The next instruction is `lda vsyncs`, so the MB stz's A = 0 is not used.

```
        stz NSPR
```
->
```

```

The store is redundant: render_frame (line 255) left NSPR = 0 and set_palette leaves it alone.

Review: NSPR = 0 from render_frame at 255; set_palette never writes NSPR; next is lda vsyncs. Master 3, MB 4 (A9 00 85 A3). Depends on engine.s:3130.

## r050:7  src/game.s:260-260  (window i0162)  M -2 B -4  [high]  review: accept

Cycles: Master -3, MB -5 cycles

Assumption: t_level_init (line 246) = level_init (logic.s:442) whose single exit path runs `stza pausing` (logic.s:845, before its only rts at logic.s:849); pausing is otherwise written only in game.s (grep pausing: game.s, logic.s:242 decl, logic.s:845). Lines 247-259 (t_game_frame x2, render_frame x2, set_palette) never write pausing. Next instruction after deletion is frame_loop's `lda keys`.

```
        stz pausing
```
->
```

```

pausing is already 0 when control reaches line 260, so `stz pausing` is a no-op; frame_loop reloads A.

Review: level_init (logic.s:442-849) has a single rts at 849 preceded by stza pausing (845); its only jmps go to its own @ labels. pausing written only there and in game.s; lines 247-259 do not touch it. Master stz zp 2, MB 4.

## r050:8  src/game.s:268-276  (window i0168)  M -7 B -8  [high]  review: accept

Cycles: exit: cmp2+beq2+jmp3=7 -> bne taken 3; resume: cmp2+beq3+lda2+sta3+lda3+sta3+bra3=19 -> bne2+lda3+sta3+inc5+bne3=16 (both targets)

Assumption: (1) t_pause_menu returns Z from A: Master TOBANK t_pause_menu = jsr pagelogic / jmp pause_menu, and pause_menu ends `pla / rts` (menu.s:519-520); MB t_pause_menu ends `lda #1 / rts` or `lda #0 / rts` (game.s:376-380). (2) pausing = 0 at the call: `lda pausing / bne fl_nopause` (266-267) just fell through, and neither pause_menu nor t_pause_menu writes pausing (grep). pausing is zp on both (85 rr in both listings) so inc zp 5 = lda #imm 2 + sta zp 3. (3) C from cmp #0 is not consumed: resume path reaches frame_loop -> lda/and/beq/lda/bne and `sec` before the sbc (288); exit path goes to title_loop which is also entered with arbitrary C (from game_main, line 218 and 339). (4) Range: bne title_loop is -116 bytes on MB ($22B6->$2244) and -109 on Master before any other shrink.

```
        jsr t_pause_menu
        cmp #0
        beq :+
        jmp title_loop
:       lda #1
        sta pausing
        lda vsyncs
        sta logicvs
        bra frame_loop
```
->
```
        jsr t_pause_menu            ; Z from A: 0 resume, else exit
        bne title_loop
        lda vsyncs
        sta logicvs
        inc pausing                 ; 0 -> 1 (it was 0 to get here): Z = 0
        bne frame_loop
```

cmp #0 is redundant because Z already reflects A. beq :+ / jmp title_loop -> bne title_loop (in range). pausing 0 -> 1 by inc instead of lda #1/sta; the order of the pausing and logicvs stores does not matter (no interrupt reads either). inc leaves Z = 0, so the unconditional bra/jmp becomes bne. A at frame_loop is dead (lda keys).

Review: Z reflects A on return: Master TOBANK t_pause_menu = jsr pagelogic/jmp pause_menu and pause_menu ends pla/rts (menu.s:519); MB t_pause_menu ends lda #imm/rts (or, with cand 14, and #K_FIRE with Z=0). pausing = 0 at the call (lda pausing/bne fell through) and no pause code writes it, so inc -> 1, Z=0, bne always taken. C from cmp #0 dead (frame_loop reaches sec before sbc; title_loop entered with arbitrary C elsewhere). Anon removal: no ref counts across 272 (refs 270->272, 280->282, 311->310). Ranges: MB -116, Master -109. Bytes MB 18->10, Master 17->10. Cycles exit 7->3, resume 19->16.

## r050:9  src/game.s:277-282  (window i0176)  M -6 B -8  [medium]  review: accept

Cycles: key up: beq3 then lda3+and2+bne2+stz(M3/B5)=10/12 -> sta 3; key held while paused: bne3 then lda3+and2+bne3=8 -> sta 3 (both targets)

Assumption: fl_nopause has exactly two references, both in frame_loop (lines 265, 267; grep over src, modelb/src and tools): from 265, A = keys & K_MENU = 0; from 267, keys&K_MENU != 0 and A = pausing (non-zero). The fall-in from above is impossible (line 276 is an unconditional bra). `keys` is written only by scan_keys (engine.s:3936), which runs in the vsync interrupt: the original re-reads keys ~5 cycles after the first read, the rewrite reuses the first read -- the two differ only if that interrupt lands between the reads, and then the only effect is that pausing is updated one frame_loop pass later (the next pass re-reads keys).

```
fl_nopause:
        lda keys
        and #K_MENU
        bne :+
        stz pausing
:       ; The peg is three vsyncs -- 16.7Hz of render -- and the logic takes two
```
->
```
fl_nopause:                         ; A = 0 (menu key up: clear) or pausing (held: kept)
        sta pausing
        ; The peg is three vsyncs -- 16.7Hz of render -- and the logic takes two
```

Original: re-read keys & K_MENU; if 0 clear pausing, else leave it. Arriving from line 265 the (first) read was 0 and A = 0, so `sta pausing` clears it. Arriving from line 267 the read was non-zero, so pausing must be left alone; A = pausing, so `sta pausing` writes back the same value. The next instruction `lda vsyncs` reloads A; the stored value's flags are not used. Only difference is the single keys read instead of two (see assumption).

Review: fl_nopause only reached from 265 (A = keys&K_MENU = 0 -> clears pausing) and 267 (A = pausing != 0 -> rewrites same value); 276 is unconditional. Only difference: one keys read instead of two ~5cy apart (keys is written by scan_keys in the interrupt); either outcome of that race is valid latch behaviour, settled on the next pass. Anon label 282 removal safe. MB 10->2, Master 8->2.

## r050:10  src/game.s:298-299  (window i0187)  M -3 B -6  [medium]  review: accept

Cycles: Master -4, MB -12 cycles per rendered frame

Assumption: NSPR == 0 on every arrival at frame_top: (a) from frame_loop after `jsr render_frame / bra frame_loop` (render_frame ends `stza NSPR`, engine.s:3130); (b) from the level start (lines 251-260: render_frame last touched it); (c) fl_wait and the pause-resume path go round frame_loop without touching NSPR (neither pause_menu/menu code nor MB t_pause_menu references NSPR -- grep); (d) after fl_over the next level runs load_level which ends `stza NSPR` (game.s:74) before any game_frame. NSPR writers are only addsprite (engine.s:1395, from game_frame), engine.s:3130, 3591, game.s. frame_top stays at the same place for tools/harness.mjs (it now labels the jsr); the scene fingerprint's NSPR read at frame_top is 0 in both versions.

```
        stza NSPR                   ; the list is rebuilt by each step; only the
        jsr t_game_frame            ; second one's survives to be drawn
```
->
```
        jsr t_game_frame            ; (NSPR is 0 here: render_frame and load_level clear it)
```

The store writes 0 over a value that is already 0 on every path. A (vsyncs) and flags: game_frame starts `inc frame` and never reads A before `lda health` (logic.s:888-894); Master TOBANKI preserves A regardless.

Review: Invariant NSPR=0 at frame_loop entry: from 260 (render_frame at 255 cleared it), 307 (after render_frame), 276 (pause: menus never call addsprite; only logic.s game paths do), 312 (fl_wait). Nothing between frame_loop and frame_top writes NSPR. frame_top label stays (now on the jsr); harness/bdiff read NSPR = 0 either way. game_frame starts inc frame so A dead. Depends on engine.s:3130 staying.

## r050:11  src/game.s:302-302  (window i0191)  M -0 B -4  [high]  review: accept

Cycles: MB pha3+lda2+sta3+pla4=12 -> 3; Master stz abs 4 -> sta abs 4

Assumption: A = exiting = 0 here: `lda exiting / bne fl_over` (300-301) fell through. NSPR is abs on the Master (9C rr rr: sta abs also 3 bytes), zp $A3 on MB.

```
        stza NSPR
```
->
```
        sta NSPR                    ; A = 0: exiting, just tested
```

Storing A (= 0) writes the same zero. Flags: MB stza ended with pla (Z=1 from A=0), the rewrite leaves Z=1 from lda exiting; the next instruction is jsr t_game_frame anyway. A = 0 afterwards in both.

Review: A = exiting = 0 after lda exiting/bne fl_over fell through. Master NSPR abs: stz abs 9C (3B/4cy) -> sta abs (3B/4cy). MB stza zp 6 -> sta zp 2. Next is jsr t_game_frame.

## r050:12  src/game.s:312-312  (window i0199)  M -0 B -1  [high]  review: accept

Cycles: bra/jmp 3 -> bne taken 3 (both targets)

Assumption: The only way to line 312 is the fall-through of `beq :-` at line 311, so Z = 0. frame_loop is -94 bytes back on MB ($2305 -> $22A9), in range.

```
        bra frame_loop
```
->
```
        bne frame_loop              ; Z = 0: the vsync ticked
```

An unconditional jump executed only when Z is known clear becomes bne. Master: bra and bne both 2 bytes / 3 cycles.

Review: 312 has no label and is reached only by fall-through of beq :- so Z=0. MB bne offset -94 in range. jmp 3 -> bne 2 bytes, 3cy both.

## r050:13  src/game.s:315-347  (window i0200)  M -13 B -1  [high]  review: accept

Cycles: ldx lives 3 = lda lives 3. stars=0: 14->14; stars & odd level: lda3 beq2 lda3 lsr2 bcs3 inc5 lda3=21 -> lda3 beq2 lda3 ora2 sta3 inc5 lda3=21; stars & even: 25->21. Master game_over/game_won: lda #imm 2 -> txa 2. MB game blocks unchanged.

Assumption: (1) winlose uses its A only via `sta mtop` then `lda mtop / beq @lose` (menu.s:525,530) and `lda mtop / beq @lframe` (menu.s:602); nothing in winlose rewrites mtop (menu_list/item_y/clear_items are the other mtop users and winlose calls none of them), so any non-zero A means win. (2) update_hiscore (game.s:383-389) uses only A; Master t_winlose (TOBANK) goes through pagelogic which preserves A. (3) X is dead on the other exits of fl_over: level_loop starts `jsr blank_palette` which does `ldx #15` first (engine.s:4297); MB game_over/game_won reach t_winlose = FARSUB (ldx #F_WINLOSE). (4) game_over/game_won/fl_over are referenced only inside game.s (grep src, modelb/src, tools). lives, stars, level zp on both.

```
        lda lives
        beq game_over
        lda stars
        beq @next1
        lda level
        lsr                         ; C = bit 0
        bcs @next1
        inc level
@next1: inc level
        lda level
        cmp #16
        beq game_won
        lsr
        cmp maxlevel
        bcc :+
        sta maxlevel
:       jmp level_loop
game_over:
        jsr update_hiscore
  .if MODELB
        jsr ensure_menu
  .endif
        lda #0
        jsr t_winlose
        jmp title_loop
game_won:
        jsr update_hiscore
  .if MODELB
        jsr ensure_menu
  .endif
        lda #1
        jsr t_winlose
        jmp title_loop
```
->
```
        ldx lives                   ; (X = lives: the Master's winlose flag below)
        beq game_over
        lda stars
        beq @next1
        lda level
        ora #1                      ; a star: on to the next odd level (+2 from even)
        sta level
@next1: inc level
        lda level
        cmp #16
        beq game_won
        lsr
        cmp maxlevel
        bcc :+
        sta maxlevel
:       jmp level_loop
  .if MODELB
game_over:
        jsr update_hiscore
        jsr ensure_menu
        lda #0
        jsr t_winlose
        jmp title_loop
game_won:
        jsr update_hiscore
        jsr ensure_menu
        lda #1
        jsr t_winlose
        jmp title_loop
  .else
game_over:                          ; X = lives: 0 lost, else won (winlose tests its flag
game_won:                           ; only for zero; update_hiscore keeps X)
        jsr update_hiscore
        txa
        jsr t_winlose
        jmp title_loop
  .endif
```

lda lives -> ldx lives: same Z for beq game_over, and A is reloaded (lda stars) or dead (game_over: jsr update_hiscore). Stars: original adds 2 to an even level, 1 to an odd one when stars != 0, else 1; (level | 1) + 1 gives e+2 for even e and o+1 for odd o; C from lsr was dead (cmp #16 sets C). Master: at game_over X = lives = 0, at game_won X = lives != 0 (X untouched between ldx and beq game_won); txa after update_hiscore gives winlose 0 / non-zero, which is all it tests, so both labels share one tail and the 11-byte game_won copy goes. MB keeps both copies because ensure_menu destroys X.

Review: ldx lives same size/cycles, same Z. (level|1)+1 equals original +2/+1 when stars; lsr's C dead (cmp #16). Master: X untouched from ldx to beq game_won, update_hiscore (lda/cmp/sbc/mov16) keeps X, txa gives 0 lose / nonzero win; winlose only tests mtop for zero (menu.s:530,602) and its callees never write mtop (only menu_list at 285). X dead at level_loop (blank_palette ldx #15; X arbitrary there on other paths too). MB game blocks byte-identical. Master saves 1 + (22-10) = 13, MB 1.

## r050:14  src/game.s:376-380  (window i0241)  M -0 B -2  [high]  review: accept

Cycles: exit path lda#1 2 + rts -> rts (-2); resume unchanged

Assumption: t_pause_menu (MB only) has one caller, game.s:268 (grep), which tests the result only for zero (`cmp #0 / beq`, or with the i0168 candidate Z from A via `bne title_loop`); title_loop's first instruction (jsr ensure_menu -> lda title_res) discards A. At line 376, `and #K_FIRE / beq @w` fell through, so A = K_FIRE = 16 and Z = 0. The header comment at line 362 ('1 exit') should read 'non-zero exit'.

```
        lda #1
        rts
@resume:
        lda #0
        rts
```
->
```
        rts                         ; A = K_FIRE: exit (non-zero is all the caller tests)
@resume:
        lda #0
        rts
```

Returning 16 instead of 1 with Z = 0 is indistinguishable to the only caller. Resume path is untouched.

Review: MB-only; A = K_FIRE (16), Z=0 after the and/beq fall-through; the only caller game.s:268 tests only for zero, and title_loop discards A. Saves lda #1 (2 bytes). Header comment at line 362 ('1 exit') becomes stale, which is cosmetic.

## r051:0  src/menu.s:24-31  (window i0000)  M -0 B -14  [high]  review: accept

Cycles: Model B: 8 x (lda #0 2 + sta abs 4) = 48 -> 2 + 8x4 = 34; Master unchanged (same stz text)

Assumption: A is dead after the stores: the next instruction is jsr draw_glyph_rows, whose first instruction is lda tx (menu.s:89). The flags are not consumed (draw_glyph_rows sets them). GLYPHBUF is absolute (MNUBSS) on the Model B, so the stz macro is A9 00 8D lo hi, 5 bytes (t_b.lst 00014B..).

```
        stz GLYPHBUF+0              ; '_' = erase: draw an all-zero glyph
        stz GLYPHBUF+1
        stz GLYPHBUF+2
        stz GLYPHBUF+3
        stz GLYPHBUF+4
        stz GLYPHBUF+5
        stz GLYPHBUF+6
        stz GLYPHBUF+7
```
->
```
  .if MODELB
        lda #0                      ; '_' = erase: draw an all-zero glyph (A is dead:
        sta GLYPHBUF+0              ;  draw_glyph_rows starts with lda tx)
        sta GLYPHBUF+1
        sta GLYPHBUF+2
        sta GLYPHBUF+3
        sta GLYPHBUF+4
        sta GLYPHBUF+5
        sta GLYPHBUF+6
        sta GLYPHBUF+7
  .else
        stz GLYPHBUF+0              ; '_' = erase: draw an all-zero glyph
        stz GLYPHBUF+1
        stz GLYPHBUF+2
        stz GLYPHBUF+3
        stz GLYPHBUF+4
        stz GLYPHBUF+5
        stz GLYPHBUF+6
        stz GLYPHBUF+7
  .endif
```

The Model B stz macro is lda #0/sta m, so the eight of them reload the same zero. One lda #0 and then eight sta leave the same 8 bytes zero, with A = 0 and N,Z from the zero as before. The Master arm is the original text.

Review: The original lines match the file. On the Model B, stz is lda #0 / sta abs (GLYPHBUF is in MNUBSS). One lda #0 followed by 8 sta leaves the same memory, with A=0 and Z=1. A is dead: the next instruction is jsr draw_glyph_rows, which starts with lda tx. The Master arm is the original text. Built: MNUCODE -14, LOGIC unchanged.

## r051:1  src/menu.s:33-33  (window i0000)  M -0 B -1  [high]  review: accept

Cycles: Model B jmp 3 -> beq taken 3; Master bra 3 -> beq taken 3

Assumption: draw_glyph_rows (menu.s:88-143) has a single exit, inx / cpx #8 / beq :+ / : rts, so it always returns with Z = 1 (and C = 1). Nothing runs between the jsr and this branch. @space is 6 bytes on, in range on both targets.

```
        bra @space
```
->
```
        beq @space                  ; Z = 1: draw_glyph_rows returns from its cpx #8
```

bra is jmp (3 bytes) on the Model B. Z is provably 1 after the call, so beq is always taken: same target, same cycles, 2 bytes.

Review: draw_glyph_rows has a single exit (menu.s:139-143: cpx #8 equal, then rts), so it returns with Z=1 and C=1. This still holds after candidate 8 (bne not taken, or beq taken). So beq is always taken, with the same cycles on both targets. Built: Model B -1, Master 0.

## r051:2  src/menu.s:37-38  (window i0000)  M -1 B -1  [high]  review: accept

Cycles: clc removed: -2 cycles on every path, both targets

Assumption: @space is reached three ways: (a) beq @space after cmp #' ' matched (C = 1); (b) the '_' path, jsr draw_glyph_rows then bra/beq @space; (c) jsr glyph_index / jsr draw_glyph, which falls into draw_glyph_rows. draw_glyph_rows's only exit follows cpx #8 with X = 8 (C = 1). grep finds no other reference to @space.

```
        clc
        adc #8
```
->
```
        adc #7                      ; C = 1 on every way in: cmp #' ' equal, or the cpx #8
                                    ; draw_glyph_rows returns from
```

A + 8 + 0 = A + 7 + 1: same result and same carry out. V/N/Z are recomputed by later instructions (ldy tchar/iny, then lda/cmp at @ch) before any use, and C is the same.

Review: @space is reached in only three ways, all with C=1: beq @space after cmp #' ' matched; the '_' path after draw_glyph_rows; and falling in after jsr draw_glyph, which falls into draw_glyph_rows. tx+7+1 gives the same A, C and V as tx+8+0 (both operands are positive). Built: -1 on both targets.

## r051:3  src/menu.s:42-42  (window i0000)  M -0 B -1  [high]  review: accept

Cycles: Model B jmp 3 -> bne taken 3; Master bra 3 -> bne 3

Assumption: Y = index+1 after iny. Every string drawn is at most 19 chars (menu.s:363-364, 706-727; numbuf is 8 bytes, menu.s:738), so Y never wraps to 0. The backward distance is about 80 bytes on the Model B: in range, and it shrinks with the other candidates.

```
        bra @ch
```
->
```
        bne @ch                     ; Y > 0: no string is 256 characters
```

bra is jmp on the Model B. Z from iny is 0 for every reachable string, so bne is always taken.

Review: Every string drawn is at most 19 characters plus the 0 (menu strings, cursor/blank strings, numbuf of 8). So Y is never 0 after iny and bne is always taken. The branch is in range after assembly on both targets. Model B -1.

## r051:4  src/menu.s:62-72  (window i0015)  M -8 B -8  [high]  review: accept

Cycles: from this point: '/' cmp2+bne2+lda2 = 6 -> cmp2+bcs2+eor2 = 6; '.' 11 -> 6; digits 5+5+sec2+sbc2 = 14 -> cmp2+bcs3+sbc2 = 7; same on both

Assumption: glyph_index is only called from drawtext (menu.s:34), and never for ' ' or '_'. Every drawn string holds only A-Z, space, '_', '<', '>' and digits (menu.s:363-364, 706-727), or is numbuf (digits and spaces, draw_number menu.s:664-694). So the only chars that reach this tail are '/', '.' and '0'-'9'. Checked by a Python equivalence check over all valid chars.

```
:       cmp #'/'
        bne :+
        lda #28
        rts
:       cmp #'.'
        bne :+
        lda #29
        rts
:       sec
        sbc #('0'-30)               ; -'0'+30 folded into one subtraction
        rts
```
->
```
:       cmp #'0'                    ; past '>' and '<' only '/', '.' and digits come here
        bcs :+
        eor #$33                    ; '/' -> 28, '.' -> 29
        rts
:       sbc #('0'-30)               ; C = 1 from the bcs: -'0'+30 in one subtraction
        rts
```

'/' ($2F) eor $33 = $1C = 28, and '.' ($2E) eor $33 = $1D = 29. Both are below '0', so bcs falls through. Digits >= '0' take bcs with C = 1, so the sec was redundant and the result is A - $12 as before.

Review: The only strings that reach glyph_index (menu1/menu2, helptab, levelnames, the score strings, cursor_str/blank_str, numbuf) hold only A-Z, space, _, <, > and digits, and space and _ never reach it. / and . are unused but still map correctly: $2F^$33=28 and $2E^$33=29, and both are below '0', so bcs falls through. Digits take bcs with C=1, so sbc #18 gives the same result. C on return differs for / and ., but draw_glyph begins with stz/stza then asl, so C is dead. The anonymous labels inside the range go from 3 to 2. The external bne :+ at line 59 still reaches the first of them, and there is no :- between lines 73 and 108. Built: -8 on both targets.

## r051:5  src/menu.s:76-86  (window i0023)  M -6 B -10  [high]  review: fix

Cycles: Master 3+6+5+2+2+3+3+2+3 = 29 -> 2+2+2+2+3+2+2+3 = 18; Model B (stza 12) 38 -> 18

Assumption: A = glyph index 0..39. draw_glyph's only caller is drawtext (menu.s:35), after glyph_index, and glyph_index only ever sees A-Z < > / . 0-9 (strings at menu.s:363-364 and 706-727, plus numbuf digits). SPR_FONT = $B8A0 on the Master (build/assets.inc) and $8000 on the Model B. Both bytes are even in both, and (<SPR_FONT)/2 + 156 is 236 and 156 respectively, both < 256 (asserted). X and Y are untouched as before, and getglyph reads only (w16b),y. A Python check confirms identical addresses for every valid char on both SPR_FONT values.

```
        stza w16b+1
        asl
        asl
        asl
        rol w16b+1
        clc
        adc #<SPR_FONT
        sta w16b
        lda w16b+1
        adc #>SPR_FONT
        sta w16b+1                  ; glyph rows
```
->
```
        .assert (SPR_FONT & $0101) = 0 && (<SPR_FONT) / 2 + 39*4 < 256, error, "draw_glyph folds the font base into its shifts"
        asl
        asl                         ; glyph (0..39) * 4, C = 0
        adc #(<SPR_FONT) / 2        ; no carry out: the assert
        asl                         ; glyph*8 + <SPR_FONT, C = its bit 8
        sta w16b
        lda #(>SPR_FONT) / 2
        rol                         ; >SPR_FONT (even) + C
        sta w16b+1                  ; glyph rows
```

idx < 64, so two asl leave idx*4 <= 156 with C = 0. Adding (<SPR_FONT)/2 stays below 256. The third asl gives the 9-bit value idx*8 + <SPR_FONT, with the low byte in A and bit 8 in C. (>SPR_FONT)/2 then rol gives >SPR_FONT + C, because >SPR_FONT is even. So w16b = SPR_FONT + idx*8, identical to the original 16-bit add. In neither version is w16b+1 read before it is written.

Review: Refuted on the Model B. There SPR_FONT = font_art (modelb/build/assets.inc:16), a label that follows bank-5 code (modelb/src/banks.s:215). In the current build it is at $907D, which is odd, not $8000 as the proposal assumes. Applied alone, the Model B link fails the proposal's own assert (ld65 Error at menu.s(76)). It passes only when earlier savings happen to leave font_art at an even address whose low byte is $C6 or less, so any later odd-sized saving in bank 5 (candidates 6 and 7 here) would break the build. On the Master, SPR_FONT = $B8A0 is a fixed constant and the fold is exact for idx 0..39. Fix: keep the fold on the Master only. On the Model B keep the original minus its clc, which is redundant because rol w16b+1 shifts out bit 7 of a byte just zeroed, so C=0. Built: Master -6, Model B -1. Cycles are equal or fewer on every path.

Fixed:
```
  .if MODELB                        ; (font_art moves with bank 5's code: no fold)
        stza w16b+1
        asl
        asl
        asl
        rol w16b+1                  ; C = 0: the byte it shifts out was 0
        adc #<SPR_FONT
        sta w16b
        lda w16b+1
        adc #>SPR_FONT
        sta w16b+1                  ; glyph rows
  .else
        .assert (SPR_FONT & $0101) = 0 && (<SPR_FONT) / 2 + 39*4 < 256, error, "draw_glyph folds the font base into its shifts"
        asl
        asl                         ; glyph (0..39) * 4, C = 0
        adc #(<SPR_FONT) / 2        ; no carry out: the assert
        asl                         ; glyph*8 + <SPR_FONT, C = its bit 8
        sta w16b
        lda #(>SPR_FONT) / 2
        rol                         ; >SPR_FONT (even) + C
        sta w16b+1                  ; glyph rows
  .endif
```

## r051:6  src/menu.s:100-102  (window i0047)  M -1 B -1  [high]  review: accept

Cycles: clc removed: -2 cycles on the X = 4 row, both targets

Assumption: This block is reached only by falling through `cpx #4 / bne :++` (menu.s:98-99) with X = 4, so C = 1. ROWBYTES = 640 ($280), so its low byte is $80.

```
        lda sp                      ; second char row: one row on
        clc
        adc #<ROWBYTES
```
->
```
        lda sp                      ; second char row: one row on
        adc #(<ROWBYTES) - 1        ; C = 1: cpx #4 found X = 4
        .assert (<ROWBYTES) <> 0, error, "the carry-in add needs a nonzero low byte"
```

sp + $7F + 1 = sp + $80 + 0: same A and the same carry into the adc #>ROWBYTES that follows. V may differ, but the next adc recomputes it and nothing reads it.

Review: Line 100 is reached only by falling through cpx #4 / bne :++, so X=4 and C=1. sp+$7F+1 gives the same A, and the same carry into adc #>ROWBYTES, as sp+$80+0. V differs, but the next adc recomputes it. The low byte of ROWBYTES is $80, and the proposal asserts it is nonzero. Built: -1 on both targets.

## r051:7  src/menu.s:132-132  (window i0077)  M -1 B -5  [high]  review: accept

Cycles: per @pair iteration: -2 (clc) both targets; Model B carry path inca 11 -> adc #0 2

Assumption: At this point C comes from the second `rol` (menu.s:125). A was set by lda #0 and the first rol put one bit into bit 0, so the second rol shifts out bit 7 = 0. tay, lda pairtab,y, ldy tmp2, sta (sp),y, iny and sta (sp),y do not touch C. ringup begins with cmp ringehi on the Model B (cmp #>RINGEND on the Master), so the carry left by adc #0 is never read. sp+1 < $80, so adc #0 never carries out.

```
        spnext
```
->
```
        lda sp                      ; spnext, less its clc: C = 0, the second rol
        adc #8                      ;  above shifted out bit 7 of a 0 or 1
        sta sp
        bcc :++                     ; past the fold's own anonymous label
        lda sp+1
  .if MODELB
        adc #0                      ; C = 1 (bcc not taken): inca's + 1, 4 bytes less
  .else
        inca
  .endif
        ringup sp
        sta sp+1
:
```

This is spnext's own expansion with its clc dropped, since C is already 0. On the Model B, inca is sta mtmp/inc mtmp/lda mtmp (+1, C kept). With C = 1 after the untaken bcc, adc #0 is the same +1 in 2 bytes, and ringup's cmp overwrites N/Z.

Review: C=0 at spnext, because the second rol shifts out bit 7 of a 0 or 1. tay, lda, ldy, sta (),y and iny do not change C. The inline text has the same anonymous labels as the macro, so bcc :++ still skips past ringup's label. Model B: with bcc not taken C=1, so adc #0 adds 1, and there is no carry out because sp+1 <= $7F. ringup starts with cmp ringehi, which overwrites C, N and Z. Built: Master -1, Model B -5.

## r051:8  src/menu.s:139-180  (window i0088)  M -6 B -4  [high]  review: accept

Cycles: row end: Master beq3+rts -> bne not taken 2+rts (exit), jmp3 -> bne taken 3 (loop); Model B beq taken 3 to the other rts, same; clear_ring per page: inc5+dex2+bne3 -> inc5+bpl3, ldx #n (2) gone; both targets

Assumption: Master: the bne sits 102 bytes after @row (t_m.lst $1B8B..$1BEF), in branch range. Model B: cr_rts is about 26 bytes ahead. RINGEND_B = $8000 (asserted at engine.s:135). Master RINGEND = $3000 + $5000 = $8000 (ringup compares #$80 in t_m.lst). BARADDR ($0300 / $2B00) is below $8000, so w16+1 counts $03..$7F / $2B..$7F with N = 0 and first sets N at $80: the same page count as the old X. X after clear_ring was 0 and is now unchanged. clear_ring's only caller is menu_begin (menu.s:215), which never reads X before its rts. Every menu_begin caller (menu.s:409,427,469,501,529) writes X before reading it: mov16i/draw_piece -> drawsprite ldx spbank; ldx #0; tax; ldx #40; the win/lose draw_piece chain. The lda #<menurec that follows overwrites the flags after clear_ring. Assembled and linked on both targets; asserts pass.

```
        inx
        cpx #8
        beq :+
        jmp @row                    ; (the 6502 spellings put @row out of a branch's reach)
:       rts
pairtab: .byte $00, $33, $CC, $FF    ; logical 3 (yellow) on both dots of a game px

; ---------------------------------------------------------------- menu screen helpers
; clear the current back buffer ring ($3000-$7FFF) to black
  .if MODELB
; Model B: the bar, both mirrors and both rings: all of main RAM from $0300
        .assert (<BARADDR) = 0 && (<RINGEND_B) = 0, error, "the clear is whole pages"
clear_ring:
        lda #>BARADDR               ; the bar, both mirrors and both rings: main RAM
        ldx #(>RINGEND_B - >BARADDR) ; from $0300 to the top, whole pages
        sta w16+1
        lda #0
        sta w16
        tay
@l:     sta (w16),y
        iny
        bne @l
        inc w16+1
        dex
        bne @l
        rts
  .else
clear_ring:
        stz w16
        lda #>BARADDR               ; from the bar, not the ring base: the bar sits below
        sta w16+1                   ; $3000 now and the menu still wants it black
        ldx #((RINGEND - BARADDR) >> 8)
        ldy #0
        tya
@l:     sta (w16),y
        iny
        bne @l
        inc w16+1
        dex
        bne @l
        rts
  .endif
```
->
```
        inx
        cpx #8
  .if MODELB
        beq cr_rts                  ; (Z = 1, C = 1 on return: drawtext relies on both)
        jmp @row                    ; (the 6502 spellings put @row out of a branch's reach)
  .else
        bne @row
        rts                         ; (Z = 1, C = 1 on return: drawtext relies on both)
  .endif
pairtab: .byte $00, $33, $CC, $FF    ; logical 3 (yellow) on both dots of a game px

; ---------------------------------------------------------------- menu screen helpers
; clear the current back buffer ring ($3000-$7FFF) to black
  .if MODELB
; Model B: the bar, both mirrors and both rings: all of main RAM from $0300
        .assert (<BARADDR) = 0 && RINGEND_B = $8000, error, "the clear is whole pages, up to $8000"
clear_ring:
        lda #>BARADDR               ; the bar, both mirrors and both rings: main RAM
        sta w16+1                   ; from $0300 to the top, whole pages
        lda #0
        sta w16
        tay
@l:     sta (w16),y
        iny
        bne @l
        inc w16+1
        bpl @l                      ; until $8000
cr_rts: rts                         ; (draw_glyph_rows's exit too)
  .else
        .assert RINGEND = $8000 && (<BARADDR) = 0, error, "the clear is whole pages, up to $8000"
clear_ring:
        stz w16
        lda #>BARADDR               ; from the bar, not the ring base: the bar sits below
        sta w16+1                   ; $3000 now and the menu still wants it black
        ldy #0
        tya
@l:     sta (w16),y
        iny
        bne @l
        inc w16+1
        bpl @l                      ; until $8000
        rts
  .endif
```

Row end: on the Master, bne @row does the same as beq over jmp @row and is now in range. The Model B branches to an identical rts. Z = 1, C = 1 and X = 8 at return in all versions. clear_ring: inc w16+1 sets N exactly when the page reaches $80, the end, so dex/bne and its counter are redundant.

Review: Master: bne @row assembles in range, and the exit still leaves Z=1, C=1, X=8. Model B: beq cr_rts reaches an identical rts. cr_rts is a normal label, but no cheap label follows it in its scope. The removed : had only one reference, the beq :+ that is rewritten, and no :- or :-- counts back to it. clear_ring: inc w16+1 first sets N at $80, which is RINGEND on both targets (asserted, and both links pass), so the same number of pages is cleared. X is no longer 0 on return. menu_begin, the only caller, returns without reading X, and all five of its callers (menu.s:409, 427, 469, 501, 529) write X before reading it. lda #<menurec overwrites the flags. Built: Master -6, Model B -4.

## r051:9  src/menu.s:204-212  (window i0124)  M -0 B -36  [high]  review: accept

Cycles: Model B 9 x stza (pha3 lda2 sta3 pla4 = 12) = 108 -> 9 x 3 = 27; Master unchanged

Assumption: On the Model B, m_wait_flip is the local loop `lda flipreq / bne m_wait_flip / rts` (engine.s:4860-4863, MNUCODE), so it returns with A = 0, N = 0 and Z = 1. The original leaves A = 0 with Z = 1, N = 0 from the pla, and the new code leaves the same A and flags. wx..curbuf are zero page (85 rr in t_b.lst). select_backbuf, called next, starts with ldx curbuf. On the Master, m_wait_flip goes through pagelogic, so the Master arm keeps stza.

```
        stza wx
        stza wx+1
        stza wy
        stza wy+1
        stza wcx
        stza wcx+1
        stza wcy
        stza wfine
        stza curbuf
```
->
```
  .if MODELB
        sta wx                      ; A = 0: m_wait_flip spun until flipreq was 0
        sta wx+1
        sta wy
        sta wy+1
        sta wcx
        sta wcx+1
        sta wcy
        sta wfine
        sta curbuf
  .else
        stza wx
        stza wx+1
        stza wy
        stza wy+1
        stza wcx
        stza wcx+1
        stza wcy
        stza wfine
        stza curbuf
  .endif
```

Storing the zero already in A does the same as stza's pha/lda #0/sta/pla. A and the flags end identical.

Review: On the Model B, m_wait_flip is the local loop lda flipreq / bne / rts (engine.s:4860, in the MODELB arm), so it returns with A=0, Z=1, N=0. The stza sequence left A=0 with Z=1, N=0 from its pla, and the sta sequence leaves exactly the same. wx..curbuf are zero page (built: -36). m_select_backbuf reloads from curbuf. The Master arm is unchanged.

## r052:0  src/menu.s:301-362  (window i0196)  M -6 B -14  [high]  review: accept

Cycles: @it loop: 5 cycles fewer per item on both targets (lda tmp3 3 + tax 2 removed; item_y's tax moves from entry to exit, same count). Initial cursor: 4 fewer (lda msel abs removed). K_UP path: dec + bpl 6+3 vs dec + bra 6+3 (Model B jmp 3): equal. K_DOWN path: Master ldx 4, inx 2, cpx 4 = lda 4, inc a 2, cmp 4; stx 4 = sta 4; bcc taken 3 = bra 3. Model B 4+2+4 vs 4+11+4 (faster); bcc 3 = jmp 3. @move: beq taken 3 = bra/jmp 3. @curstr: tax before the jsr instead of after it; item_y has a trailing tax instead of a leading one. Equal on both item_y paths.

Assumption: item_y's only callers are lines 302 and 350 (grep: no other reference and no item_y+N). drawtext (menu.s:13-41) has a single exit, `@done: rts`, reached only from `lda (ptr),y / beq @done`, so it returns A = 0 and Z = 1. text_centred and @cursor/@curstr both end in `jmp drawtext`. msel is 0 at line 309: `stz msel` at 286, and clear_items, item_y, text_centred and drawtext never write msel (its only writers are menu.s 286, dec/sta in this loop, and winlose's own stz/inc). msel < mcount <= 8 (title/pause lists have 2 items, the level list max+1 <= 8), so dec msel leaves N = 0. X is dead where ldx msel replaces lda msel/inca: @move reloads X, and @loop's menu_keys does tax. The fire exit returns X = msel+1 instead of keys, and no caller reads X after menu_list: game.s:214-229 (cmp / asl), game.s:268 (cmp #0), pause_menu's pha/pla, and the Master TOBANK t_* entries (jsr pagelogic / jmp). The Model B reaches them by farcall, which destroys X anyway. item_y does not touch Y.

```
        lda tmp3
        jsr item_y
        tax
        jsr text_centred
        ldx tmp3
        inx
        cpx mcount
        bne @it
        lda msel
        jsr @cursor
        jsr menu_show
@loop:  jsr menu_keys
        sta tmp
        and #K_UP
        beq :+
        lda msel
        beq :+
        dec msel
        bra @move
:       lda tmp
        and #K_DOWN
        beq :+
        lda msel
        inca
        cmp mcount
        bcs :+
        sta msel
        bra @move
:       lda tmp
        and #(K_FIRE|K_RIGHT)
        beq @loop
        lda msel
        rts
        ; cursor moved: the buffer is on display, so touch only the cursor rows (right after
        ; the vsync menu_keys waited for) rather than clearing and redrawing every item
@move:  lda mlast
        ldx #<blank_str
        ldy #>blank_str
        jsr @curstr
        lda msel
        jsr @cursor
        bra @loop
@cursor:
        sta mlast
        ldx #<cursor_str
        ldy #>cursor_str
@curstr:
        stx ptr
        sty ptr+1
        jsr item_y
        tax
        lda #8
        jmp drawtext
item_y: tax
        lda mtop
        cpx #0
        beq :++
:       clc
        adc mstep
        dex
        bne :-
:       rts
```
->
```
        jsr item_y                  ; X = the index still: A = X = its y
        jsr text_centred
        ldx tmp3
        inx
        cpx mcount
        bne @it
        jsr @cursor                 ; A = 0 = msel: drawtext returns A = 0
        jsr menu_show
@loop:  jsr menu_keys
        sta tmp
        and #K_UP
        beq :+
        lda msel
        beq :+
        dec msel
        bpl @move                   ; always: msel < mcount <= 8
:       lda tmp
        and #K_DOWN
        beq :+
        ldx msel
        inx
        cpx mcount
        bcs :+
        stx msel
        bcc @move                   ; always: bcs not taken
:       lda tmp
        and #(K_FIRE|K_RIGHT)
        beq @loop
        lda msel
        rts
        ; cursor moved: the buffer is on display, so touch only the cursor rows (right after
        ; the vsync menu_keys waited for) rather than clearing and redrawing every item
@move:  lda mlast
        ldx #<blank_str
        ldy #>blank_str
        jsr @curstr
        lda msel
        jsr @cursor
        beq @loop                   ; always: drawtext returns Z = 1
@cursor:
        sta mlast
        ldx #<cursor_str
        ldy #>cursor_str
@curstr:
        stx ptr
        sty ptr+1
        tax
        jsr item_y
        lda #8
        jmp drawtext
item_y: lda mtop                    ; X = item index -> A = X = its y
        cpx #0
        beq :++
:       clc
        adc mstep
        dex
        bne :-
:       tax
        rts
```

@it: X holds the loop index from stx tmp3 on (txa/asl/tay/lda/sta/iny do not write X), so item_y can take X directly. Drop `lda tmp3` and item_y's leading `tax`. item_y now ends with `tax`, so both callers' post-call `tax` go; the @curstr path does `tax` before the call instead (A = the index there). The flags at return come from item_y's tax, which are what the callers' tax set. Line 309 `lda msel` is redundant: A = 0 (from drawtext) = msel. `bra @move` after `dec msel` becomes bpl (result is 0..6, so N = 0). Down key: ldx/inx/cpx/stx computes and compares msel+1 exactly as lda/inca/cmp/sta did (values are small, no wrap). C = 0 when bcs falls through, so `bcc @move` is always taken. `bra @loop` after `jsr @cursor` becomes beq: @cursor ends in `jmp drawtext`, which returns Z = 1. Checked in an assembled listing: @loop is about $2D bytes back, @move about $1E forward.

Review: Original matches lines 301-362. X = tmp3 at 301 (only txa/asl/tay/loads/stores since stx tmp3). text_centred -> jmp drawtext, whose only exit is lda (ptr),y / beq @done, so A=0,Z=1 on return; msel=0 from stz msel (286), nothing in clear_items/drawtext/engine writes msel. dec msel leaves 0..6 so bpl always taken; bcc after an untaken bcs always taken; beq after jsr @cursor always taken (drawtext Z=1). X on the fire exit changes only on the down-at-bottom+fire path; no caller reads X (game.s 214-229, 268 path, pause_menu pha/pla, Model B farcall). item_y's second anon label moves rts->tax, count unchanged; no :-/:+ crosses it. Assembled both targets: branches in range, LOGIC -6, MNUCODE -14. Cycles equal or fewer on every path.

## r052:1  src/menu.s:204-212  (window i0132)  M -0 B -36  [high]  review: accept

Cycles: Master: stz zp 3 -> sta zp 3, x9: equal. Model B: stza (pha 3, lda # 2, sta 3, pla 4 = 12) -> sta zp 3, x9: 108 -> 27, faster.

Assumption: A = 0 at line 204. Model B: m_wait_flip is engine.s:4860 `lda flipreq / bne m_wait_flip / rts` (MNUCODE, in the .if MODELB branch), so it exits only with A = 0. Master: m_wait_flip is TOMAIN wait_flip = `jsr wait_flip / jmp pagelogic`. wait_flip (engine.s:3155) exits with A = 0, and pagelogic (engine.s:4734) is pha ... pla, so A = 0 survives. wx..curbuf are all zero page (engine.s:212-217; the listings show 85/64 zp opcodes). No flag is read afterwards: the next instruction is jsr m_select_backbuf (Model B select_backbuf starts with ldx curbuf; A is not an input).

```
        stza wx
        stza wx+1
        stza wy
        stza wy+1
        stza wcx
        stza wcx+1
        stza wcy
        stza wfine
        stza curbuf
```
->
```
        sta wx                      ; A = 0 here: m_wait_flip returns only once
        sta wx+1                    ; flipreq reads 0 (both targets)
        sta wy
        sta wy+1
        sta wcx
        sta wcx+1
        sta wcy
        sta wfine
        sta curbuf
```

Each stza stores 0 and leaves A unchanged (0). sta with A = 0 stores 0 and also leaves A = 0. The only difference is the Model B flags. stza's pla sets Z=1, N=0 from 0; with sta the flags are the ones m_wait_flip left, also Z=1, N=0 (lda flipreq = 0, or pagelogic's pla of 0). Nothing reads them before select_backbuf's ldx.

Review: A=0 at line 204 on both: Model B m_wait_flip is lda flipreq/bne/rts; Master TOMAIN wait_flip returns A=0 through pagelogic's pha/pla. wx..curbuf are zero page. Flags not read (select_backbuf starts ldx curbuf). Measured Master 0, Model B 36.

## r052:2  src/menu.s:226-226  (window i0143)  M -0 B -2  [high]  review: accept

Cycles: Master: identical (stza = stz). Model B: pha/lda/sta/pla 12 -> lda/sta 5.

Assumption: A is dead at line 226. Model B: the next instruction is jsr m_build_sections = FARSUB (ldx #F_BUILDSECT / jsr farcall). farcall passes A to build_sections (modelb/src/display.s:178), whose first A use is `lda @cbl,x` after ldx curbuf, so A is never read. Master: build_sections (engine.s:2757) starts with lda curbuf. menu_show's exit A comes from m_set_palette, not from here. Note: the identical text `stza curbuf` also appears at line 212; the intended site is line 226.

```
        stza curbuf
```
->
```
        stz curbuf                  ; (A is dead: build_sections loads it)
```

stz stores 0 like stza. Only A (dead) and the Model B flags (overwritten by farcall/build_sections) differ.

Review: A dead: Model B next is FARSUB m_build_sections (ldx/jsr farcall), build_sections (display.s:178) starts ldx curbuf / lda @cbl,x and never reads the incoming A. Master stza==stz. Measured Model B -2.

## r052:3  src/menu.s:234-235  (window i0149)  M -2 B -2  [medium]  review: accept

Cycles: lda # 2 + sta zp 3 = 5 -> inc zp 5, on both targets.

Assumption: flipreq = 0 on entry to line 234. flipreq is set to 1 only by render_frame (engine.s:3145, not called from the menus) and by this line. It is cleared by the vsync ISR (engine.s:3873, modelb display.s:102) and init (engine.s:4238). Every path into menu_show comes either through menu_begin's m_wait_flip (title_menu, help_screen, level_select, pause_menu -> menu_list, winlose) or through the previous menu_show, whose loop exits only on flipreq = 0 (winlose @loop). Nothing in between (draw_piece, drawtext, clear_items, load_title, music_stop) writes flipreq. The ISR only tests it for zero (beq @noflip).

```
        lda #1
        sta flipreq
```
->
```
        inc flipreq                 ; 0 -> 1: every way in has waited for it to clear
```

With flipreq = 0, inc leaves 1, just as lda #1/sta did. The next instruction reloads A and the flags from flipreq, so the A = 1 and flags the old code left do not matter. inc is a single instruction, so the ISR cannot see a torn value.

Review: flipreq writers: render_frame (not reachable from menus), ISRs (clear only), init, and this line. Every path into menu_show passes menu_begin's m_wait_flip or a previous menu_show's wait loop, so flipreq=0 and inc gives 1. Loop reloads A/flags. inc zp 5 cycles = lda#/sta zp. Measured -2/-2.

## r052:4  src/menu.s:238-239  (window i0152)  M -2 B -2  [high]  review: accept

Cycles: lda # 2 + sta zp 3 = 5 -> inc zp 5 (curbuf is zero page), on both targets.

Assumption: curbuf = 0 here: line 226 stored 0. Its only other writers are render_frame (engine.s:3151), init/reset (engine.s:3596, main.s:110-115, modelb init.s:90-92) and menu_begin, and none of these can run from build_sections or the vsync ISR. A is dead: the next instruction is jmp m_set_palette, and set_palette (engine.s:4268) starts with ldx #15 / txa. Model B farcall returns the target's A (fcA), with flags from lda fcA. Master TOMAIN returns set_palette's A through pagelogic's pha/pla. So menu_show's return A and flags are unchanged.

```
        lda #1
        sta curbuf                  ; next game frame renders into the other buffer
```
->
```
        inc curbuf                  ; 0 -> 1: next game frame renders into the other buffer
```

curbuf goes 0 -> 1 as before. A differs (0 instead of 1) on the way into set_palette, which never reads it.

Review: curbuf=0 from line 226; build_sections and the ISR only read it. A is dead: set_palette starts ldx #15/txa, and menu_show's return A comes from set_palette via pagelogic/fcA. Measured -2/-2, cycles equal.

## r052:5  src/menu.s:256-259  (window i0165)  M -2 B -2  [high]  review: accept

Cycles: 7 cycles fewer on both targets: pha 3 + pla 4 removed; ldx #/abs costs the same as lda #/abs, and stx the same as sta.

Assumption: X is not an input of drawsprite: engine.s:1476-1490 is stza, cmp, sbc, sta, stza, asl/rol, then ldx spbank before any X read. draw_piece does not preserve X today either (Master drawsprite reloads X; Model B farjsr does ldx #idx and farcall does tsx). ldpbank (cpu.inc) records no bank site: on the Model B it is `op PBANK + (n - 4)` (ldx abs exists and is 3 bytes like lda abs), on the Master `op #n`. Y is untouched.

```
        pha
        ldpbank lda, BANK_MAP       ; the title pack sits where the map goes (the
        sta spbank                  ; overlay comes off the disc: the loader cannot
        pla                         ; patch it, so the physical bank is read)
```
->
```
        ldpbank ldx, BANK_MAP       ; the title pack sits where the map goes (the
        stx spbank                  ; overlay comes off the disc: the loader cannot
                                    ; patch it, so the physical bank is read)
```

The bank byte goes through X instead of A, so A (the piece index) no longer needs saving and restoring around it. spbank receives the same byte, and A reaches m_drawsprite unchanged.

Review: drawsprite does not read X or flags before ldx spbank (engine.s 1476-1490); Model B farjsr loads X anyway. ldpbank records no bank site; ldx abs/imm same size as lda. X at draw_piece exit unchanged (comes from drawsprite). Measured -2/-2, 7 cycles faster.

## r052:6  src/menu.s:267-271  (window i0173)  M -2 B -3  [high]  review: accept

Cycles: String length n >= 4 (the shortest is 'HELP'). Old: 2 + 12n + 8 = 10 + 12n (Model B bra = jmp 3, same). New: 2 + 10n + 9 = 11 + 10n. Faster for every n >= 1 on both targets.

Assumption: Every string text_centred sees is non-empty and shorter than 255 characters. Its callers are menu_list (menu1, menu2, levelnames) and help_screen (helptab h1..h6); the strings are at menu.s:706-725 and are all 4..14 characters.

```
        ldy #0
:       lda (ptr),y
        beq :+
        iny
        bra :-
```
->
```
        ldy #$FF
:       iny
        lda (ptr),y
        bne :-
```

Both versions end with Y = the index of the terminator (= len) and A = 0, and tya follows. Pre-incrementing from $FF gives Y = 0 on the first load. bne :- targets the iny line.

Review: All text_centred strings (menu1, menu2, levelnames, helptab) are 4..14 chars, so the n=0 path (1 cycle slower) never runs. Exit state Y=len, A=0, Z=1, C unchanged, same as before. Anon label count unchanged, no crossing references. Measured -2/-3.

## r052:7  src/menu.s:276-277  (window i0178)  M -1 B -1  [high]  review: accept

Cycles: 2 cycles fewer on both targets (sec removed).

Assumption: len (Y) < 64: the strings are at most 14 characters (menu.s:706-725). The carry into line 276 comes from the second asl, i.e. bit 6 of Y, which is 0. drawtext starts with sta tx / stx ty and ignores the flags.

```
        sec
        adc #(WINPX/2)
```
->
```
        adc #(WINPX/2)+1            ; C = 0 from the second asl: len < 64
```

~A + (K+1) + 0 = ~A + K + 1, so A is identical. WINPX/2 = 80 and K+1 = 81 have the same sign, so even V matches; the flags are unused anyway.

Review: C after the second asl is bit 6 of len (<64), so 0; adc #81 with C=0 == sec/adc #80. WINPX=160 on both. drawtext sets C before reading it. Measured -1/-1.

## r052:8  src/menu.s:170-178  (window i0111)  M -3 B -0  [high]  review: accept

Cycles: Master: 2 fewer once (ldx) and 2 fewer per page (dex); bpl taken 3 = bne taken 3.

Assumption: Master RINGEND = $3000 + 32*640 = $8000 (engine.s:153; the old ldx loaded #$55 = ($8000-$2B00)>>8). BARADDR is $2B00, below $8000, so N stays 0 until the $7F -> $80 increment. X is not read after clear_ring: its only caller, menu_begin (menu.s:215), returns straight after. menu_begin's callers (title_menu, help_screen, level_select, pause_menu, winlose) either load X before using it (ldx #48/#0/#40, tax) or never read it (the draw_piece and drawtext paths reload X).

```
        ldx #((RINGEND - BARADDR) >> 8)
        ldy #0
        tya
@l:     sta (w16),y
        iny
        bne @l
        inc w16+1
        dex
        bne @l
```
->
```
        ldy #0
        tya
@l:     sta (w16),y
        iny
        bne @l
        inc w16+1
        bpl @l                      ; to $8000: RINGEND (asserted below)
        .assert RINGEND = $8000, error, "clear_ring stops at $8000"
```

The same pages $2B..$7F are cleared; the loop now stops when the high byte reaches $80 instead of counting X down from $55. A = 0 and Y = 0 at exit as before. X now keeps the caller's value instead of 0, and it is dead. Lowest window is i0111, in the previous region; reported here in case it is not reported there.

Review: Master RINGEND = $3000+$5000 = $8000, assert added; pages $2B..$7F cleared, bpl exits on $80. X dead after menu_begin in every caller (ldx #48/#0/#40, tax, or drawsprite/drawtext reload). Measured Master -3. Possibly a duplicate of a r051 report (window i0111).

## r052:9  src/menu.s:152-163  (window i0098)  M -0 B -3  [high]  review: accept

Cycles: Model B: 2 fewer once (ldx) and 2 fewer per page (dex); bpl taken 3 = bne taken 3.

Assumption: engine.s:135 asserts RINGEND_B = $8000. BARADDR = $0300, so N is first set by the $7F -> $80 increment. X is dead after clear_ring (see the Master clear_ring candidate: menu_begin's callers never read X before loading it).

```
        lda #>BARADDR               ; the bar, both mirrors and both rings: main RAM
        ldx #(>RINGEND_B - >BARADDR) ; from $0300 to the top, whole pages
        sta w16+1
        lda #0
        sta w16
        tay
@l:     sta (w16),y
        iny
        bne @l
        inc w16+1
        dex
        bne @l
```
->
```
        lda #>BARADDR               ; the bar, both mirrors and both rings: main RAM
        sta w16+1                   ; from $0300 to the top ($8000), whole pages
        lda #0
        sta w16
        tay
@l:     sta (w16),y
        iny
        bne @l
        inc w16+1
        bpl @l                      ; RINGEND_B = $8000 (engine.s asserts it)
```

Pages $03..$7F ($7D pages) are cleared either way. A = 0 and Y = 0 at exit; X keeps its entry value instead of 0, and it is dead. Lowest window is i0098, in the previous region; reported here in case it is not reported there.

Review: RINGEND_B = $8000 asserted at engine.s:135; pages $03..$7F cleared, bpl exits on $80. X dead as for index 8. Measured Model B -3. Possibly a duplicate of a r051 report (window i0098).

## r053:0  src/menu.s:301-362  (window i0227)  M -4 B -5  [high]  review: accept

Cycles: @it path (per item): orig lda tmp3 3 + [item_y tax 2] + tax 2 after = 7 -> new 0 + tax inside 2 = 2 (5 faster); item_y index 0: cpx2+beq3 -> dex2+bmi3; index n>0: cpx2+beq2 -> dex2+bmi2, loop body identical (bpl=bne); @curstr path: tax moved from after the jsr to before the rts, same count; line 342 Master bra 3 -> beq taken 3, Model B jmp 3 -> beq taken 3.

Assumption: X = tmp3 at line 301: menu.s:292 stx tmp3, and 293-300 (txa/asl/tay/lda (menuptr),y/sta/iny/lda/sta) never write X. item_y has exactly two callers (menu.s:302,350; grep), both did `tax` immediately after, and text_centred (menu.s:260) / drawtext (menu.s:13) take the y in X and overwrite A/flags first. index < 128: index < mcount (<= 8 levels, 2 items). drawtext's only rts is @done, reached only via `beq @done` after `lda (ptr),y`, so Z=1 on return from jsr @cursor (which jmps drawtext). Label item_yx is new (unused name, grep); item_y has no @-labels, the next @-scope user is clear_items (normal label) so no cheap-label scope is broken. beq range to @loop ~ -70 bytes on both. Assembled on both targets in a scratch copy: sizes as stated.

```
        lda tmp3
        jsr item_y
        tax
        jsr text_centred
        ldx tmp3
        inx
        cpx mcount
        bne @it
        lda msel
        jsr @cursor
        jsr menu_show
@loop:  jsr menu_keys
        sta tmp
        and #K_UP
        beq :+
        lda msel
        beq :+
        dec msel
        bra @move
:       lda tmp
        and #K_DOWN
        beq :+
        lda msel
        inca
        cmp mcount
        bcs :+
        sta msel
        bra @move
:       lda tmp
        and #(K_FIRE|K_RIGHT)
        beq @loop
        lda msel
        rts
        ; cursor moved: the buffer is on display, so touch only the cursor rows (right after
        ; the vsync menu_keys waited for) rather than clearing and redrawing every item
@move:  lda mlast
        ldx #<blank_str
        ldy #>blank_str
        jsr @curstr
        lda msel
        jsr @cursor
        bra @loop
@cursor:
        sta mlast
        ldx #<cursor_str
        ldy #>cursor_str
@curstr:
        stx ptr
        sty ptr+1
        jsr item_y
        tax
        lda #8
        jmp drawtext
item_y: tax
        lda mtop
        cpx #0
        beq :++
:       clc
        adc mstep
        dex
        bne :-
:       rts
```
->
```
        jsr item_yx                 ; X = tmp3 still: returns A = X = y
        jsr text_centred
        ldx tmp3
        inx
        cpx mcount
        bne @it
        lda msel
        jsr @cursor
        jsr menu_show
@loop:  jsr menu_keys
        sta tmp
        and #K_UP
        beq :+
        lda msel
        beq :+
        dec msel
        bra @move
:       lda tmp
        and #K_DOWN
        beq :+
        lda msel
        inca
        cmp mcount
        bcs :+
        sta msel
        bra @move
:       lda tmp
        and #(K_FIRE|K_RIGHT)
        beq @loop
        lda msel
        rts
        ; cursor moved: the buffer is on display, so touch only the cursor rows (right after
        ; the vsync menu_keys waited for) rather than clearing and redrawing every item
@move:  lda mlast
        ldx #<blank_str
        ldy #>blank_str
        jsr @curstr
        lda msel
        jsr @cursor
        beq @loop                   ; Z=1: drawtext only returns via beq @done
@cursor:
        sta mlast
        ldx #<cursor_str
        ldy #>cursor_str
@curstr:
        stx ptr
        sty ptr+1
        jsr item_y
        lda #8
        jmp drawtext
item_y: tax                         ; A = index -> A = X = mtop + index*mstep
item_yx:                            ; (or X = index)
        lda mtop
        dex
        bmi :++                     ; index 0 (index < 128)
:       clc
        adc mstep
        dex
        bpl :-
:       tax
        rts
```

item_y: dex/bmi replaces cpx #0/beq (same test for 0<=X<128, 1 byte less); loop iterates X-1..0 = X times as before (bpl instead of bne because X was pre-decremented). The trailing tax gives X=A=y for both callers, so each caller's `tax` goes (net -1). @it enters at item_yx with X = index already, dropping lda tmp3 (2 bytes). Exit flags (N,Z of y) and X=y instead of X=0 are consumed by nobody. Line 342 bra->beq on known Z=1 saves the Model B jmp byte. Spans 54 instructions (more than one window). Supersedes the smaller candidates for lines 342 and 354-362; apply this one first (they then fail to match).

Review: The original text matches lines 301-362. X holds the index at line 301: stx tmp3 is at 292, and lines 293-300 never write X. In item_y/item_yx the dex/bmi entry and the bpl loop make exactly 'index' passes. The trailing tax gives A=X=y to both callers, which are lines 302 and 350 only (checked by grep). text_centred and drawtext both take y in X. drawtext returns only through beq @done, so Z=1 after jsr @cursor and beq @loop is always taken. The new item_yx label follows item_y, which is already a normal label, so no @ scope breaks. The anonymous label count in item_y is unchanged. Assembled on both targets: LOGIC shrinks by 4 bytes, MNUCODE by 5. Every path is the same speed or faster.

## r053:1  src/menu.s:342-342  (window i0233)  M -0 B -1  [high]  review: accept

Cycles: Master bra (3 taken) -> beq taken 3; Model B jmp 3 -> beq taken 3.

Assumption: The preceding jsr @cursor ends in jmp drawtext (menu.s:353); drawtext (menu.s:13-43) returns only through @done, reached only by `beq @done` after `lda (ptr),y`, so Z=1 always. Branch distance to @loop (menu.s:312) about -74 bytes on Model B, -67 on Master: in range.

```
        bra @loop
```
->
```
        beq @loop                   ; Z=1: drawtext only returns via beq @done
```

Z known set, so beq is always taken: identical control flow. Alternative to (subsumed by) the lines 301-362 candidate.

Review: Correct on its own: Z=1 comes from drawtext's @done. Candidate 0 includes this change, so it will not match once 0 is applied. Assembled savings: 0 Master, 1 Model B.

## r053:2  src/menu.s:354-362  (window i0249)  M -1 B -1  [high]  review: accept

Cycles: index 0: cpx2+beq3=5 -> dex2+bmi3=5; index n: cpx2+beq2=4 -> dex2+bmi2=4, n loop passes identical (bne->bpl both 3/2). Both targets.

Assumption: Both callers (menu.s:302,350) do `tax` straight after, so X on exit ($FF instead of 0) and the exit flags are dead. index < 128 (index < mcount <= 8).

```
item_y: tax
        lda mtop
        cpx #0
        beq :++
:       clc
        adc mstep
        dex
        bne :-
:       rts
```
->
```
item_y: tax
        lda mtop
        dex
        bmi :++                     ; index 0 (index < 128)
:       clc
        adc mstep
        dex
        bpl :-
:       rts
```

X pre-decremented: X=0 exits via bmi, else the loop runs for X-1..0 (n passes) exactly as the dex/bne loop did. Alternative to (subsumed by) the lines 301-362 candidate.

Review: Correct on its own: both callers do tax immediately after, and index < 8. Candidate 0 includes this change. Assembled savings: 1 Master, 1 Model B.

## r053:3  src/menu.s:379-380  (window i0261)  M -1 B -1  [high]  review: accept

Cycles: ldy #0 (2) + lda #0 (2) -> lda #0 (2) + tay (2): same per row, both targets.

Assumption: none beyond the window

```
        ldy #0
        lda #0
```
->
```
        lda #0
        tay
```

A=0, Y=0 and flags (Z=1,N=0) identical after either pair; C untouched by both.

Review: A=0 and Y=0 either way, N and Z are identical, and C is untouched. Same cycles. Assembled savings: 1 Master, 1 Model B.

## r053:4  src/menu.s:410-412  (window i0282)  M -4 B -4  [high]  review: accept

Cycles: orig 4x(lda 2 + sta zp 3) + lda 2 = 22 -> 3 lda (6) + 4 sta zp (12) = 18, both targets.

Assumption: TP_LOGO = 0 (build/assets.inc:32, listing shows A9 00; the .assert guards it). spx/spy are zero page on both (listings: 85 rr). draw_piece (menu.s:245) reads only A and spx/spy via m_drawsprite; store order is irrelevant (no interrupt reads spx/spy).

```
        mov16i spx, 40
        mov16i spy, 4
        lda #TP_LOGO
```
->
```
        lda #40
        sta spx
        lda #4
        sta spy
        lda #TP_LOGO                ; = 0: both high bytes
        .assert TP_LOGO = 0, error, "title_menu: TP_LOGO doubles as the zero high bytes"
        sta spx+1
        sta spy+1
```

mov16i spx,40 / mov16i spy,4 store 40,0,4,0; the high bytes are both 0, which is also TP_LOGO, so one lda #0 serves both high bytes and the draw_piece argument. Same memory, same A (0) and same N,Z (lda #0 then sta) at the jsr.

Review: TP_LOGO=0 (assets.inc:32, now guarded by the assert), and spx/spy are in zero page. Memory gets the same values, and A=0 with the same N,Z at jsr draw_piece. 4 cycles faster. Assembled savings: 4 Master, 4 Model B.

## r053:5  src/menu.s:420-422  (window i0292)  M -1 B -1  [high]  review: accept

Cycles: lda #2 (2) -> asl (2), both targets.

Assumption: Carry dead: menu_list (menu.s:283) runs sta/stx/stz(lda #0/sta)/lda #$FF/sta then jsr clear_items, which (mclear=1 here) does lda mtop / lsr, overwriting C before any use; after clear_items the @it loop asl's before any adc.

```
        lda #1
        sta mclear
        lda #2
```
->
```
        lda #1
        sta mclear
        asl                         ; A = 2 items
```

A=1 -> asl -> A=2, N=0, Z=0 as lda #2; only C differs (cleared) and C is dead.

Review: A=1, then asl gives 2 with the same N,Z as lda #2. C ends up cleared, but menu_list calls clear_items (mclear=1), whose lda mtop/lsr overwrites C before anything uses the carry. Assembled savings: 1 Master, 1 Model B.

## r053:6  src/menu.s:438-446  (window i0314)  M -2 B -2  [high]  review: accept

Cycles: Model B: lda tmp3 3 + asl 2 + asl 2 -> tya 2 + asl 2 (3 faster). Master: lda tmp3 3 + asl 2 -> tya 2 (3 faster).

Assumption: Y = 2i: menu.s:430-432 txa/asl/tay, then lda helptab,y / sta ptr / lda helptab+1,y / sta ptr+1 leave Y and C alone. C=0 after that asl since i < 6 (cpx #6 loop). tmp3 = i (stx tmp3 at 429) still read by adc.

```
        lda tmp3
        asl
        asl
        adc tmp3                    ; i*5
        adc #8
        asl                         ; (i*5+8)*2 = i*10+16
  .else
        lda tmp3
        asl
```
->
```
        tya                         ; Y = 2i (C=0 from the asl above)
        asl
        adc tmp3                    ; i*5
        adc #8
        asl                         ; (i*5+8)*2 = i*10+16
  .else
        tya                         ; Y = 2i = i*2 (C=0 from the asl above)
```

Model B: orig A=i, asl, asl -> 4i with C=bit7(2i)=0; new tya -> 2i, asl -> 4i, C=bit7(2i)=0: identical A and C into adc tmp3. Master: orig A=i, asl -> 2i, C=bit7(i)=0; new tya -> 2i, C still 0 from line 431's asl of i: identical A and C into adc tmp3 (line 447, untouched). The .if/.else structure (lines 437,444,451) is unchanged; the range covers 438-446 including the .else line 444.

Review: Y=2i from lines 430-432, and the loads and stores that follow leave Y and C alone. C=0 from the asl of i < 6. Model B: tya/asl gives 4i with C=0, as before. Master: tya gives 2i with C=0, as before. adc still reads tmp3. The .else line is kept. Faster on both targets. Assembled savings: 2 Master, 2 Model B.

## r053:7  src/menu.s:478-482  (window i0342)  M -0 B -10  [high]  review: accept

Cycles: Master: inc a 2 + sta zp 3 + dec a 2 = 7 -> tax 2 + inx 2 + stx zp 3 = 7 (equal). Model B: inca 11 + sta 3 + deca 11 = 25 -> 7.

Assumption: X dead: overwritten by tax at menu.s:495 before menu_list; lines 483-494 never read X. Carry dead: the inca/deca macros preserve C, but the next instruction is asl (483) which overwrites it. mtmp (the macro scratch) no longer written: it carries no value between expansions (cpu.inc contract). tmp is zp on both (listings 85 rr / A5 rr).

```
        pla
        inca
        sta tmp
        ; top y = (108 - (n-1)*12 - 8)/2 rounded to multiple of 4
        deca
```
->
```
        pla                         ; max = n-1
        tax
        inx
        stx tmp                     ; n
        ; top y = (108 - (n-1)*12 - 8)/2 rounded to multiple of 4
```

A still = max after (pla, tax/inx/stx do not touch A); tmp = max+1 as before; flags into the asl irrelevant (asl sets its own). Master same bytes and cycles; Model B drops two 6-byte inca/deca expansions.

Review: X is dead until the tax at line 495. A keeps max and tmp gets max+1. The asl at line 483 overwrites C. Master is equal in bytes and cycles. Model B drops two 6-byte inca/deca expansions. Assembled savings: 0 Master, 10 Model B.

## r053:8  src/menu.s:486-488  (window i0348)  M -1 B -1  [high]  review: accept

Cycles: clc 2 cycles removed, both targets.

Assumption: max = n-1 <= 7: maxlevel is set only at game.s:326-330 from level/2 with level < 16 (game.s:325 cmp #16 / beq game_won), and passed by game.s:226-228. So A = 4*max <= 28 before this asl and the asl shifts out 0: C=0.

```
        asl
        clc
        adc tmp2                    ; (n-1)*12
```
->
```
        asl
        adc tmp2                    ; (n-1)*12 (C=0: (n-1)*8 <= 56)
```

asl already clears C for A < 128; clc redundant.

Review: maxlevel is written only at game.s:208 (zero) and game.s:330 (level/2 with level < 16), so max <= 7. Then 4*max < 128 and the asl leaves C=0. A reaches level_select intact through pagelogic on the Master and through the FAR 'in' entry on the Model B. Assembled savings: 1 Master, 1 Model B.

## r053:9  src/menu.s:489-492  (window i0352)  M -3 B -3  [high]  review: accept

Cycles: sta tmp2 3 + lda # 2 + sec 2 + sbc zp 3 = 10 -> eor 2 + adc 2 = 4, both targets.

Assumption: C=0 after `adc tmp2` at 488: 4*max + 8*max = 12*max <= 84 < 256 (max <= 7, see game.s:325-330). tmp2 dead after: level_select jmps menu_list, and every tmp2 reader in menu.s (line 128, after its write at 114) and the engine routines writes it first (menu.s:597 notes menu_show's callees clobber it). K+1 = 113 (Master) / 77 (Model B) fits in a byte.

```
        sta tmp2
        lda #(VISLINES/2 - 8)       ; (108 on the Master, 84 here)
        sec
        sbc tmp2
```
->
```
        eor #$FF
        adc #(VISLINES/2 - 8 + 1)   ; K - (n-1)*12 (C=0 from the adc)
```

K + (255 - v) + 1 = K - v + 256: A identical mod 256 to K - v from sec/sbc, and the carry out (K >= v) identical too, though lsr overwrites it anyway. N,Z identical since A identical.

Review: C=0 after adc tmp2, since 12*max <= 84. ~v + (K+1) equals K-v mod 256, with the same carry out and the same N,Z, and the lsr comes next anyway. tmp2 is left holding 4*max instead of 12*max. Every later reader writes it first (menu.s:114 before 128, and the engine's scratch uses). K+1 is 113 on the Master and 77 on the Model B (VISROWS 30 and 21), so it fits in a byte. Assembled savings: 3 Master, 3 Model B.

## r053:10  src/menu.s:500-521  (window i0377)  M -1 B -55  [medium]  review: accept

Cycles: Master: pha 3 + 4 stz abs 16 + lda#1 2 + sta abs 4 + pla 4 = 29 -> ldx 2 + 4 stx abs 16 + inx 2 + stx abs 4 = 24. Model B: code not assembled.

Assumption: pause_menu has no Model B caller: game.s:363 defines the Model B's own t_pause_menu stub, engine.s TOBANK "pause_menu" sits inside `.if .not MODELB` (engine.s:4742), and modelb/src/banks.s's far table has no pause entry (grep pause_menu over src/ and modelb/src). Master: the only caller is t_pause_menu = jsr pagelogic / jmp pause_menu, returning to game.s:268 which does `cmp #0` itself (return flags dead) and never reads X (beq/jmp title_loop or lda #1...). BUF_VALID/RECCNT/BARDIRTY writes and their order are unchanged.

```
pause_menu:
        jsr menu_begin
        lda #<menu2
        sta menuptr
        lda #>menu2
        sta menuptr+1
        lda #14
        sta mstep
        stz mclear
        lda #2
        ldx #40
        jsr menu_list
        pha
        ; invalidate game buffers
        stz BUF_VALID
        stz BUF_VALID+1
        stz RECCNT
        stz RECCNT+1
        lda #1
        sta BARDIRTY
        pla
        rts
```
->
```
  .if .not MODELB                  ; (the Model B's t_pause_menu is its own stub, game.s)
pause_menu:
        jsr menu_begin
        lda #<menu2
        sta menuptr
        lda #>menu2
        sta menuptr+1
        lda #14
        sta mstep
        stz mclear
        lda #2
        ldx #40
        jsr menu_list
        ldx #0                      ; invalidate game buffers (A = the result survives)
        stx BUF_VALID
        stx BUF_VALID+1
        stx RECCNT
        stx RECCNT+1
        inx
        stx BARDIRTY
        rts
  .endif
```

Model B: the whole routine is dead code (the menu2 data stays, untouched). Master: A (menu_list's result) is never disturbed, so pha/pla go; X carries the zero and then 1 for BARDIRTY. Exit flags now come from inx rather than pla, but the caller does its own cmp #0.

Review: The Model B build assembles and links with pause_menu removed. The TOBANK in engine.s is inside .if .not MODELB, game.s:363 defines the Model B's own stub, and banks.s has no FAR entry, so the routine is dead there. On the Master, A survives without pha/pla. Nothing reads X after the return. The caller at game.s:268 does its own cmp #0, so the exit flags are dead. The same stores happen in the same order, 5 cycles faster. Assembled savings: 1 Master, 55 Model B.

## r053:11  src/menu.s:512-520  (window i0376)  M -1 B -9  [medium]  review: accept

Cycles: Master 29 -> 24 cycles; Model B: pha 3 + 4x(lda#0 2 + sta abs 4) 24 + lda#1 2 + sta zp 3 + pla 4 = 36 -> ldx 2 + 16 + inx 2 + stx zp 3 = 23.

Assumption: Fallback if the .if .not MODELB candidate for 500-521 is not taken. Return flags dead: the Master caller (game.s:268) does cmp #0 itself; X unread after (game.s:268-276); on the Model B the routine is never called. BARDIRTY zp on Model B (defs.inc:51), abs on Master.

```
        pha
        ; invalidate game buffers
        stz BUF_VALID
        stz BUF_VALID+1
        stz RECCNT
        stz RECCNT+1
        lda #1
        sta BARDIRTY
        pla
```
->
```
        ldx #0                      ; invalidate game buffers (A = the result survives)
        stx BUF_VALID
        stx BUF_VALID+1
        stx RECCNT
        stx RECCNT+1
        inx
        stx BARDIRTY
```

A keeps menu_list's result throughout (no pha/pla needed); the four zero stores and the BARDIRTY=1 store happen in the same order with the same values.

Review: Fallback if candidate 10 is not taken, and correct for the same reasons. On the Model B each stz macro is 5 bytes and BARDIRTY is in zero page. Assembled savings: 1 Master, 9 Model B.

## r054:0  src/menu.s:499-521  (window i0384)  M -0 B -55  [high]  review: accept

Cycles: Model B: the code is never executed (no caller), so no path changes; Master: assembled identically

Assumption: pause_menu has no Model B caller: game.s:268 calls t_pause_menu, which on the Model B is game.s:363's own routine (.if MODELB); the only other reference, engine.s:4782 TOBANK "pause_menu", sits inside engine.s:4742 `.if .not MODELB` (the Model B listing shows it emitting nothing). grep of src, modelb/src, tools, modelb/tools finds no other reference. Measured by assembling both targets: MNUCODE $791 -> $75A, Master LOGIC unchanged.

```
; pause menu: returns 0 resume, 1 exit
pause_menu:
        jsr menu_begin
        lda #<menu2
        sta menuptr
        lda #>menu2
        sta menuptr+1
        lda #14
        sta mstep
        stz mclear
        lda #2
        ldx #40
        jsr menu_list
        pha
        ; invalidate game buffers
        stz BUF_VALID
        stz BUF_VALID+1
        stz RECCNT
        stz RECCNT+1
        lda #1
        sta BARDIRTY
        pla
        rts
```
->
```
; pause menu: returns 0 resume, 1 exit
  .if .not MODELB                   ; (Model B: t_pause_menu is game.s's own; this is dead)
pause_menu:
        jsr menu_begin
        lda #<menu2
        sta menuptr
        lda #>menu2
        sta menuptr+1
        lda #14
        sta mstep
        stz mclear
        lda #2
        ldx #40
        jsr menu_list
        pha
        ; invalidate game buffers
        stz BUF_VALID
        stz BUF_VALID+1
        stz RECCNT
        stz RECCNT+1
        lda #1
        sta BARDIRTY
        pla
        rts
  .endif
```

Dead code removal on the Model B only: the whole of pause_menu (55 bytes of MNUCODE, $4F2..$528 in the listing) is wrapped in .if .not MODELB. The Master assembles the identical bytes. (Not included, being data outside this region: menu2 and s_resume at menu.s:705/709 are then unreferenced on the Model B too -- another 16 bytes.) Lines 503-505 and 512-520 are carried verbatim, so the separate candidates on them (z:menuptr, the X-store tail) still apply after this one.

Review: Original matches 499-521. pause_menu has no Model B caller: game.s:268 t_pause_menu is game.s:363's own MODELB routine; TOBANK "pause_menu" (engine.s:4782) is inside .if .not MODELB; no F_ far-table entry (banks.s COMMON_TABLES) and no tool/harness reference. No anon or cheap labels inside. Assembled: Master LOGIC unchanged, Model B MNUCODE $791->$75A (-55), links clean.

## r054:1  src/menu.s:503-505  (window i0384)  M -2 B -0  [high]  review: accept

Cycles: Master: sta abs 4 -> sta zp 3, twice (-2); Model B: identical bytes (menuptr is already zp there, defs.inc)

Assumption: menuptr is a .zeropage symbol on the Master (menu.s:723-725, `.zeropage` / menuptr: .res 2) but is defined AFTER this use, so ca65 assembles the forward reference as absolute (Master listing: 8D rr rr); z: forces the zp form. Verified: Master listing now 85 rr, ld65 links without a range error; Model B 85 EE unchanged.

```
        sta menuptr
        lda #>menu2
        sta menuptr+1
```
->
```
        sta z:menuptr
        lda #>menu2
        sta z:menuptr+1
```

Same store to the same address; only the addressing mode is shortened. Lines are inside pause_menu, carried verbatim by the dead-code candidate for 499-521, so this applies before or after it.

Review: Original matches. Master menuptr is .zeropage (menu.s:729-731), forward ref assembled abs; z: gives 85 zp, links. Model B menuptr=$EE, bytes unchanged. Measured Master LOGIC -2. Applies inside #0's verbatim copy too.

## r054:2  src/menu.s:512-520  (window i0384)  M -1 B -9  [high]  review: accept

Cycles: Master: pha3 + 4 stz abs 16 + lda#2 + sta abs4 + pla4 = 29 -> ldx#2 + 4 stx abs 16 + inx2 + stx abs4 = 24. Model B (only if pause_menu is still assembled there): 47 -> 24

Assumption: A (menu_list's choice) survives because stx never touches it. Return flags and X change (N,Z from inx = clear; X = 1): the only caller, game.s:268 `jsr t_pause_menu`, does `cmp #0` at once (flags recomputed from A), then either `jmp title_loop` (title_loop -> t_title_menu -> title_menu, which writes X before reading it) or `lda #1/sta pausing/.../bra frame_loop`, where frame_loop..frame_top read no X or Y before t_game_frame (game.s:261-307), which is entered each frame with whatever render_frame left in X, so reads no incoming X. On the Master t_pause_menu is TOBANK (jsr pagelogic / jmp pause_menu): the rts goes straight back to game.s. Model B bytes (9) count only if the dead-code candidate for 499-521 is NOT applied; after it they are 0.

```
        pha
        ; invalidate game buffers
        stz BUF_VALID
        stz BUF_VALID+1
        stz RECCNT
        stz RECCNT+1
        lda #1
        sta BARDIRTY
        pla
```
->
```
        ldx #0                      ; A = the choice survives (the caller cmp #0s it)
        ; invalidate game buffers
        stx BUF_VALID
        stx BUF_VALID+1
        stx RECCNT
        stx RECCNT+1
        inx
        stx BARDIRTY
```

The four zero stores use X = 0 instead of stz (A dead in the Model B expansion, so it had to be saved around them with pha/pla); inx makes X = 1 for BARDIRTY. A is never touched, so pha/pla go. Measured: Master LOGIC -1, Model B MNUCODE -9 (standalone).

Review: Original matches. A untouched so pha/pla go; caller game.s:268 does cmp #0 at once; X not read before written on either exit (title_loop->title_menu, or frame_loop..t_game_frame whose X is arbitrary every frame); Master TOBANK returns directly. Measured Master -1, Model B -9 standalone (0 once #0 is applied, since it then lies in the Master-only block; combined build confirmed). Cycles 29->24 Master.

## r054:3  src/menu.s:530-561  (window i0413)  M -41 B -54  [high]  review: accept

Cycles: Excluding the two jsr draw_piece (unchanged). Model B: win 53 -> 43, lose 51 -> 43. Master: win 47 -> 40, lose 43 -> 40 (lda zp3+clc2+adc2 replaces lda abs4+asl2+adc2 for the second x).

Assumption: mtop = 0 or 1 exactly: winlose stores its A there (menu.s:525) and the only callers are game.s:338 `lda #0` / game.s:346 `lda #1` then jsr t_winlose, whose bridges preserve A (Master TOBANK -> pagelogic, engine.s:4734 'A, X, Y and the carry all come through intact'; Model B FARSUB -> farcall, low.s:22 'A goes in and comes back'); load_title/m_music_stop/menu_begin do not write mtop (only menu_list does: stx mtop, not called here). draw_piece (menu.s:255) -> drawsprite (engine.s:1476) only READS spx/spy: the only writers of spx/spy in the code base are menu.s's own stores, logic.s's object draws and engine.s draw_sprites (engine.s:1433-1445, called only from render_frame/render_core), none reachable from draw_piece. TP_WIN = 2, TP_LOSE = 3 (build/assets.inc, modelb/build/assets.inc) -- asserted. @lose and @scores lose their only references (@scores label left in place, zero bytes).

```
        lda mtop
        beq @lose
        mov16i spx, 36
        lda #4
        sta spy
        stz spy+1
        lda #TP_YOU
        jsr draw_piece
        lda #82
        sta spx
        stz spx+1
        lda #4
        sta spy
        stz spy+1
        lda #TP_WIN
        jsr draw_piece
        bra @scores
@lose:  lda #34
        sta spx
        stz spx+1
        lda #4
        sta spy
        stz spy+1
        lda #TP_YOU
        jsr draw_piece
        lda #80
        sta spx
        stz spx+1
        lda #4
        sta spy
        stz spy+1
        lda #TP_LOSE
```
->
```
        .assert TP_WIN = TP_LOSE - 1, error, "winlose picks the piece as TP_LOSE - mtop"
  .if MODELB
        lda #0
        sta spx+1
        sta spy+1
  .else
        stz spx+1
        stz spy+1
  .endif
        lda #4
        sta spy
        lda mtop                    ; 1 win, 0 lose
        asl                         ; (C = 0)
        adc #34                     ; YOU at 36 / 34
        sta spx
        lda #TP_YOU
        jsr draw_piece              ; (leaves spx, spx+1, spy, spy+1 alone)
        lda spx
        clc
        adc #80-34                  ; WIN at 82 / LOSE at 80; C = 0
        sta spx
        lda #TP_LOSE+1
        sbc mtop                    ; C = 0: TP_LOSE - mtop = TP_WIN / TP_LOSE
```

Both arms drew TP_YOU at (34+2*mtop, 4) then the win/lose piece at (80+2*mtop, 4); hi bytes 0 in all four stores. New: hi bytes zeroed once (Model B lda #0 shared by both stores; Master stz), spy = 4 once (draw_piece leaves it), x = mtop*2+34 (asl leaves C = 0 as mtop < $80). Second x = spx + 46 (spx untouched by draw_piece) -> 82/80, and that adc leaves C = 0 (A <= 82), so `lda #TP_LOSE+1 / sbc mtop` = TP_LOSE - mtop = TP_WIN (win) / TP_LOSE (lose). Same pieces, same positions, same order. The branch and the Model B 3-byte `bra` go. Measured by assembly: Master LOGIC -41, Model B MNUCODE -54.

Review: Original matches. mtop is exactly 0/1 (game.s:337/345 lda #0/#1 through A-preserving pagelogic / farcall; menu_begin, load_title, m_music_stop don't write mtop). draw_piece/drawsprite only read spx/spy (only writers: draw_sprites via render_*, logic.s objects, menu.s) and ignore carry. Arithmetic: x=34+2m, second x=spx+46 = 82/80, adc leaves C=0 so TP_LOSE+1-m-1 = TP_WIN/TP_LOSE (asserted 2/3). Cycles Master 47/43 -> 40, Model B 53/51 -> ~42. Measured Master -41, Model B -54.

## r054:4  src/menu.s:593-661  (window i0472)  M -50 B -55  [medium]  review: accept

Cycles: Per pass, frame choice: Model B/Master original >= 51 (shift loops of up to 30 iterations of 14-15 cy, plus clc/adc 4) -> new 25 lose / 27 win. Draw pass: the 6 stores + lda mcount (Model B 24, Master 20) go; the once-only 10 cy setup before @loop is paid for by the first pass, which ALWAYS draws (mcount = $FF, frames are 4..11). Loop back: bne nt 2 + jmp 3 -> beq taken 3; exit: bne taken 3 -> beq nt 2.

Assumption: (1) spx/spy are not written by anything the loop calls (draw_piece -> drawsprite reads them; menu_show -> build_sections, menu_keys, m_set_palette don't touch them; grep: only writers are menu.s's stores, logic.s object draws, engine.s draw_sprites from render_frame/render_core) and spx+1 = spy+1 = 0 after the TP_YOU/TP_WIN/TP_LOSE draws above (original or rewritten form) and across drawtext/draw_number. (2) shr16x and shr24x have no other callers (grep src, modelb/src: only menu.s:609/621). (3) w16/w16b are scratch no longer written here: drawsprite writes w16 before reading it (engine.s:1625), draw_glyph zeroes w16b+1 first. (4) X is dead after the frame choice (original left X = 0 from the shift; nothing after reads X: draw_piece takes A, menu_show/menu_keys set X). (5) TP_CLEO0 = 4 (assets.inc) so TP_CLEO0+7 fits 5 bits -- asserted. (6) msel wraps mod 256 exactly as before; (msel>>1)&15 = (msel&30)/2.

```
        lda #$FF
        sta lastkeys
        sta mcount                  ; last drawn frame   (menu_list's variables: this
        stz msel                    ; animation counter   screen never runs a list, and
@loop:  ; big cleo frame            ; tmp2/tmp3 are clobbered by menu_show's callees
        lda msel                    ; every pass, which froze big Cleo on one frame)
                                    ; the shift count is the same on both arms
        and #30
        tax
        lda mtop
        beq @lframe
        ; win: 4 + ((441 >> (n & 30)) & 3)
        lda #<441
        sta w16
        lda #>441
        sta w16+1
        jsr shr16x
        lda w16
        and #3
        ora #4
        bra @drawc
@lframe:
        lda #$79
        sta w16
        lda #$9E
        sta w16+1
        lda #$E7
        sta w16b
        jsr shr24x
        lda w16
        and #3
@drawc: clc
        adc #TP_CLEO0
        cmp mcount
        beq @same
        sta mcount
        lda #66
        sta spx
        stz spx+1
        lda #28
        sta spy
        stz spy+1
        lda mcount
        jsr draw_piece
@same:  jsr menu_show
        inc msel
        jsr menu_keys
        and #(K_FIRE|K_RIGHT)
        bne :+
        jmp @loop
:       rts

; w16 >>= X
shr16x: cpx #0
        beq :++
:       lsr w16+1
        ror w16
        dex
        bne :-
:       rts
; w16b:w16 (24 bit) >>= X
shr24x: cpx #0
        beq :++
:       lsr w16b
        ror w16+1
        ror w16
        dex
        bne :-
:       rts
```
->
```
        lda #$FF
        sta lastkeys
        sta mcount                  ; last drawn frame   (menu_list's variables: this
        stz msel                    ; animation counter   screen never runs a list)
        lda #66                     ; big Cleo's place, once: nothing in the loop moves
        sta spx                     ; spx/spy (spx+1, spy+1 are 0 from the pieces above)
        lda #28
        sta spy
        .assert TP_CLEO0 <= 4, error, "cleo_fr packs TP_CLEO0+7 into five bits"
@loop:  ; big cleo frame, k = (msel & 30) / 2
        lda msel
        lsr
        and #15
        tax
        lda cleo_fr,x               ; the lose piece in bits 0-2, the win piece in 3-7
        ldx mtop
        bne @win
        and #7
        bpl @drawc                  ; (always)
@win:   lsr
        lsr
        lsr
@drawc: cmp mcount
        beq @same
        sta mcount
        jsr draw_piece
@same:  jsr menu_show
        inc msel
        jsr menu_keys
        and #(K_FIRE|K_RIGHT)
        beq @loop
:       rts

; big Cleo's pieces by k: lose TP_CLEO0 + (($E79E79 >> 2k) & 3), win TP_CLEO0 + 4 +
; ((441 >> 2k) & 3) -- the frame sequences the shifts used to compute
cleo_fr:
  .repeat 16, k
        .byte (TP_CLEO0 + (($E79E79 >> (2*k)) & 3)) | ((TP_CLEO0 + 4 + ((441 >> (2*k)) & 3)) << 3)
  .endrepeat
```

The frame was TP_CLEO0 + (mtop ? 4 + ((441 >> 2k) & 3) : (($E79E79 >> 2k) & 3)), k = (msel&30)/2 in 0..15 -- a pure function of (k, mtop). cleo_fr[k] holds the lose piece in bits 0-2 and the win piece in bits 3-7, generated by .repeat from the SAME expressions (so no hand transcription); checked exhaustively for all 256 msel x 2 mtop in Python: 0 mismatches; bytes 4D 56 5F 55 4E 47 45 46 47 45 46 47 44 44 44 44. and #7 leaves N = 0 so bpl always goes. The position stores are hoisted (never change inside the loop), which also makes `lda mcount` redundant (A still holds what sta mcount stored). `bne :+ / jmp @loop` -> `beq @loop` (in range: the loop is ~50 bytes). NOTE: this adds a 16-byte lookup table in the code segment in place of the two shift routines -- if tables are out of bounds for the grind, apply the next candidate (593-643) instead; the two are alternatives.

Review: Original matches 593-661. Table verified in the listing (4D 56 5F 55 4E 47 45 46 47 45 46 47 44 44 44 44, same on both targets, TP_CLEO0=4) and exhaustively against the shift formulas for 256 msel x 2 mtop: 0 mismatches. shr16x/shr24x/cleo_fr have no other references; removed anon labels were referenced only internally; kept ':' rts unreferenced, so the count is unchanged; draw_number's :-/:+ are unaffected. spx/spy invariant in the loop and hi bytes 0 from the pieces above (original or #3). X is dead. Every pass is faster, and the first pass always draws, so it absorbs the 10-cycle hoist. Measured Master -50, Model B -55. Caveat: it adds a 16-byte generated table in the code segment. It is mutually exclusive with #5.

## r054:5  src/menu.s:593-643  (window i0472)  M -10 B -15  [high]  review: accept

Cycles: ALTERNATIVE to the 593-661 candidate. Win arm: bra (Master 3 / Model B jmp 3) -> bne 3. Draw pass: 6 stores + lda mcount (Model B 24, Master 20) removed; once-only 10 cy setup paid by the first pass, which always draws (mcount = $FF). Loop back 5 -> 3, exit 3 -> 2.

Assumption: As for the 593-661 candidate items (1) and (4): spx/spy untouched inside the loop, spx+1 = spy+1 = 0 from the pieces drawn above. `ora #4` leaves Z = 0 (A >= 4), so bne always goes.

```
        lda #$FF
        sta lastkeys
        sta mcount                  ; last drawn frame   (menu_list's variables: this
        stz msel                    ; animation counter   screen never runs a list, and
@loop:  ; big cleo frame            ; tmp2/tmp3 are clobbered by menu_show's callees
        lda msel                    ; every pass, which froze big Cleo on one frame)
                                    ; the shift count is the same on both arms
        and #30
        tax
        lda mtop
        beq @lframe
        ; win: 4 + ((441 >> (n & 30)) & 3)
        lda #<441
        sta w16
        lda #>441
        sta w16+1
        jsr shr16x
        lda w16
        and #3
        ora #4
        bra @drawc
@lframe:
        lda #$79
        sta w16
        lda #$9E
        sta w16+1
        lda #$E7
        sta w16b
        jsr shr24x
        lda w16
        and #3
@drawc: clc
        adc #TP_CLEO0
        cmp mcount
        beq @same
        sta mcount
        lda #66
        sta spx
        stz spx+1
        lda #28
        sta spy
        stz spy+1
        lda mcount
        jsr draw_piece
@same:  jsr menu_show
        inc msel
        jsr menu_keys
        and #(K_FIRE|K_RIGHT)
        bne :+
        jmp @loop
:       rts
```
->
```
        lda #$FF
        sta lastkeys
        sta mcount                  ; last drawn frame   (menu_list's variables: this
        stz msel                    ; animation counter   screen never runs a list, and
        lda #66                     ; big Cleo's place, once: nothing in the loop moves
        sta spx                     ; spx/spy (spx+1, spy+1 are 0 from the pieces above)
        lda #28
        sta spy
@loop:  ; big cleo frame            ; tmp2/tmp3 are clobbered by menu_show's callees
        lda msel                    ; every pass, which froze big Cleo on one frame)
                                    ; the shift count is the same on both arms
        and #30
        tax
        lda mtop
        beq @lframe
        ; win: 4 + ((441 >> (n & 30)) & 3)
        lda #<441
        sta w16
        lda #>441
        sta w16+1
        jsr shr16x
        lda w16
        and #3
        ora #4
        bne @drawc                  ; (always)
@lframe:
        lda #$79
        sta w16
        lda #$9E
        sta w16+1
        lda #$E7
        sta w16b
        jsr shr24x
        lda w16
        and #3
@drawc: clc
        adc #TP_CLEO0
        cmp mcount
        beq @same
        sta mcount
        jsr draw_piece
@same:  jsr menu_show
        inc msel
        jsr menu_keys
        and #(K_FIRE|K_RIGHT)
        beq @loop
:       rts
```

Position stores hoisted out of the loop (nothing in the loop changes spx/spy; hi bytes already 0), lda mcount dropped (A = what sta mcount stored), bra -> bne on a known-nonzero result (saves the Model B jmp byte), bne :+/jmp @loop -> beq @loop. Measured: Master -10, Model B -15.

Review: ALTERNATIVE to #4 (overlapping lines 593-643): apply only if #4 is not applied. It is correct on its own: the hoisted spx/spy stores are safe (as in #4), A = mcount after sta, so lda mcount goes, ora #4 gives Z=0 so bne always takes it (Model B jmp->bne saves 1), and beq @loop is in range (assembled clean). The kept ':' leaves the anon count unchanged. Measured Master -10, Model B -15.

## r054:6  src/menu.s:668-680  (window i0509)  M -2 B -4  [high]  review: accept

Cycles: Per digit (x5): Master phx3 + plx4 = 7 saved; Model B txa/pha 5 + pla/tax 6 = 11 saved; sta abs,y = sta abs,x (5), ldy# = ldx#, dey = dex.

Assumption: Y survives the division: div10_16 (logic.s:3288-3302) uses only A, X, q1, t16 (stza is pha/lda/sta/pla on the Model B); on the Model B m_div10_16 is FARSUB -> farcall (low.s:22-50), documented and coded to leave Y alone (fcret: 'Y must survive'); interrupts save/restore Y (irq_y). Y is dead after the loop: the zero-suppress loop uses X (ldx #0), and the routine ends `jmp drawtext`, which does ldy #0; callers (menu.s:581, 592) never pass or expect Y. numbuf is absolute on both targets (Master TABLES, Model B MNUBSS), so sta numbuf,y is 3 bytes like sta numbuf,x.

```
        ldx #4
@d:     phx
  .if MODELB
        jsr m_div10_16
  .else
        jsr div10_16
  .endif
        plx
        lda q1
        ora #'0'
        sta numbuf,x
        dex
        bpl @d
```
->
```
        ldy #4                      ; Y: div10_16 and farcall leave it alone
@d:
  .if MODELB
        jsr m_div10_16
  .else
        jsr div10_16
  .endif
        lda q1
        ora #'0'
        sta numbuf,y
        dey
        bpl @d
```

The digit counter moves from X (which div10_16/farcall destroy, hence the phx/plx) to Y (which they preserve). Same five digits stored at numbuf+4..numbuf+0 in the same order; loop ends on the same bpl. Measured: Master -2, Model B -4.

Review: Original matches. div10_16 (logic.s:3288) uses only A, X, q1, t16. farcall (low.s) leaves Y untouched ('Y must survive'), and the IRQ saves Y. Y is dead afterwards: the zero-suppress loop uses X, and drawtext does ldy #0. numbuf is abs on both, so sta abs,y is the same size and cycles as abs,x. Measured Master -2, Model B -4. Combined build of #0,1,2,3,4,6: Master LOGIC -96, Model B MNUCODE -168, links clean.

## r055:0  src/menu.s:641-643  (window i0512)  M -3 B -3  [high]  review: accept

Cycles: loop path: bne not taken 2 + jmp 3 = 5 -> beq taken 3; exit path: bne taken 3 + rts -> beq not taken 2 + rts; both targets same

Assumption: beq @loop is in range: Model B 0x637 -> 0x5D9 (-96), Master 0x1F72 -> 0x1F19 (-91), from ca65 listings of both targets; @loop is a cheap label in winlose's scope (no normal label between @loop and here); the rts at 643 is referenced only by the bne :+ at 641 (no other anonymous refs in winlose, menu.s:524-643)

```
        bne :+
        jmp @loop
:       rts
```
->
```
        beq @loop
        rts
```

and #(K_FIRE|K_RIGHT) sets Z; original loops (jmp @loop) when Z=1 and returns when Z=0; beq @loop / rts does the same. No flags or registers consumed differently.

Review: Lines 641-643 match the file. I checked the branch range in the current listings. On the Master the bne at $1F72 reaches @loop at $1F19 (-91). On the Model B it runs from $0637 to $05D9 (-96). Other candidates only shorten this distance. menu.s 470-640 has no anonymous labels and no anonymous references, and the mov16, mov16i, stz and bra expansions emit none. shr16x's :++/:- and draw_number's :+/:- all resolve inside their own code. The Z flag from and #(K_FIRE|K_RIGHT) is used the same way. The loop path goes from 5 to 3 cycles and the exit path from 3 to 2. Saves 3 bytes on both targets: bne plus jmp (5) becomes beq (2).

## r055:1  src/menu.s:646-646  (window i0512)  M -1 B -1  [high]  review: accept

Cycles: txa 2 = cpx #0 2 on both targets; all paths unchanged

Assumption: only caller menu.s:609 (grep src/ and modelb/src): A dead there -- lda #>441/sta w16+1 before, lda w16 reloads A right after the jsr; C not consumed after return on the X=0 path: caller does lda w16/and/ora/bra @drawc whose first instruction is clc

```
shr16x: cpx #0
```
->
```
shr16x: txa                         ; Z from X (A dead at both callers)
```

cpx #0 sets Z=(X==0) (plus C=1, N from X); txa sets Z=(X==0), N from X, leaves C. beq consumes only Z. Loop path: lsr/ror overwrite C, identical after. X=0 path returns with C=caller's C instead of 1 and A=0 instead of 1: both dead (clc at @drawc; A reloaded).

Review: The only caller is menu.s:609 (grep of src and modelb/src). txa sets Z and N the same way cpx #0 does. On the X=0 exit, A is now 0 (was 1) and C is left unchanged (was 1). Both are dead: the caller does lda w16 / and / ora / bra @drawc, and @drawc starts with clc. On the loop path lsr overwrites C. Both instructions take 2 cycles. The listings show cpx #0 as E0 00 on both targets, so this saves 1 byte on each.

## r055:2  src/menu.s:654-654  (window i0518)  M -1 B -1  [high]  review: accept

Cycles: txa 2 = cpx #0 2 on both targets; all paths unchanged

Assumption: only caller menu.s:621 (grep src/ and modelb/src): A dead there -- lda #$E7/sta w16b before, lda w16 reloads A right after the jsr; C not consumed after return on the X=0 path: caller does lda w16/and #3 then @drawc: clc

```
shr24x: cpx #0
```
->
```
shr24x: txa                         ; Z from X (A dead at the caller)
```

As shr16x: txa gives the same Z as cpx #0; C/A differences on the X=0 exit are dead (clc at @drawc, A reloaded). Loop path overwrites C with lsr/ror.

Review: The only caller is menu.s:621. After the call, A ($E7) and C are dead: the caller does lda w16 / and #3, then @drawc: clc. N and Z are the same as before. Saves 1 byte on both targets.

## r055:3  src/menu.s:668-690  (window i0527)  M -6 B -8  [high]  review: accept

Cycles: per digit (x5): Master -phx 3 -plx 4 -lda q1 3 = -10; Model B -txa/pha 5 -pla/tax 6 -lda q1 3 = -14; sta abs,y = sta abs,x (5). Suppress loop: exit at nonzero index j: orig ldx#0 2 + j*(full iter incl inx) + lda/cmp/bne; new j*(full iter incl iny) + iny/lda/cmp/bne -> equal; all four zero: new saves the ldx #0 (-2). Both targets.

Assumption: div10_16 (logic.s:3288-3302) never touches Y and returns A = remainder (ends sta q1/rts); Model B m_div10_16 = FARSUB (engine.s:4847: ldx #idx/jsr farcall/rts) and farcall (modelb/src/low.s:22-50) leaves Y untouched and returns the target's A (fcret: sta fcA ... lda fcA); the IRQ handler saves/restores Y; X after draw_number's loops is dead (tail reloads X from ty, drawtext does ldy #0); numbuf+5..7 not touched by the loops

```
        ldx #4
@d:     phx
  .if MODELB
        jsr m_div10_16
  .else
        jsr div10_16
  .endif
        plx
        lda q1
        ora #'0'
        sta numbuf,x
        dex
        bpl @d
        ; suppress leading zeros (keep at least one)
        ldx #0
:       lda numbuf,x
        cmp #'0'
        bne :+
        lda #' '
        sta numbuf,x
        inx
        cpx #4
        bne :-
```
->
```
        ldy #4
@d:
  .if MODELB
        jsr m_div10_16
  .else
        jsr div10_16
  .endif
        ora #'0'                    ; A = the remainder (= q1); Y survives the call
        sta numbuf,y
        dey
        bpl @d
        ; suppress leading zeros (keep at least one)
:       iny                         ; (Y = $FF from the loop above)
        lda numbuf,y
        cmp #'0'
        bne :+
        lda #' '
        sta numbuf,y
        cpy #3
        bne :-
```

Digit loop: Y replaces X as counter (4..0), so no push/pull around the call; the call returns the remainder in A on both targets, so lda q1 is redundant (ora sets the same flags anyway). Suppress loop: starts Y=$FF, iny first gives indices 0..3; exits on first non-'0' (same index checked as orig), after replacing index 3 exits (cpy #3 == orig inx/cpx #4). Final state of numbuf identical.

Review: The original matches lines 668-690.

Registers: div10_16 (logic.s:3288) never touches Y and ends with sta q1/rts, so A = q1 on return. On the Model B, stza keeps A. m_div10_16 is ldx #F_DIV10 / jsr farcall / rts. farcall and fcret leave Y alone (wrselx is only sta ROMSEL) and return the target's A through lda fcA. The IRQ stub saves and restores Y (irq_y).

Loop logic: the suppress loop starts with Y=$FF and increments first, so it checks indices 0..3. It exits on the first non-'0' to the same ':' at line 691. It also exits after replacing index 3, since cpy #3 matches the old inx/cpx #4.

Cycles: the suppress loop costs the same on an early exit and 2 fewer when all four digits are zero. The digit loop saves 10 cycles per digit on the Master and 14 on the Model B.

Anonymous labels: the ':' count is unchanged. X is dead at the end: the tail reloads X from ty, and drawtext only stores X.

Bytes: numbuf is absolute (listing shows 9D rr rr), so the abs,y forms cost no extra bytes. From the listings, the Master saves phx 1 + plx 1 + lda q1 (zp) 2 + ldx #0 2 = 6, and the Model B saves 2+2+2+2 = 8.

Conflicts: index 4 is a subset of this one, and index 5 overlaps it on lines 682-690.

## r055:4  src/menu.s:668-680  (window i0527)  M -4 B -6  [high]  review: accept

Cycles: per digit (x5): Master -10 cycles, Model B -14 cycles; sta abs,y = sta abs,x

Assumption: ALTERNATIVE to the 668-690 candidate (use if that one fails). div10_16 never touches Y, returns A=remainder; Model B farcall leaves Y, returns target's A (modelb/src/low.s:11-50); X after the loop is dead (ldx #0 follows at 682)

```
        ldx #4
@d:     phx
  .if MODELB
        jsr m_div10_16
  .else
        jsr div10_16
  .endif
        plx
        lda q1
        ora #'0'
        sta numbuf,x
        dex
        bpl @d
```
->
```
        ldy #4
@d:
  .if MODELB
        jsr m_div10_16
  .else
        jsr div10_16
  .endif
        ora #'0'                    ; A = the remainder (= q1); Y survives the call
        sta numbuf,y
        dey
        bpl @d
```

Y as counter removes phx/plx; A on return = remainder = q1 on both targets, so lda q1 is dropped; ora #'0' sets flags as before; next instruction ldx #0 unaffected by Y=$FF.

Review: This is a subset of index 3 and an alternative to it; the same reasoning holds. Y survives div10_16 and farcall, and A holds the remainder on return. ldx #0 follows, so Y=$FF and the leftover X do no harm. Saves 4 bytes on the Master and 6 on the Model B.

## r055:5  src/menu.s:682-694  (window i0527)  M -0 B -2  [high]  review: accept

Cycles: Master stz abs 4 -> stx abs 4; Model B lda #0/sta abs 6 -> stx abs 4; rest unchanged

Assumption: ALTERNATIVE, overlaps the 668-690 candidate (stale if that applies). numbuf+7 is not read or written by the suppress loop (X in 0..3) or the '0' stores; stx sets no flags and lda numbuf,x follows

```
        ldx #0
:       lda numbuf,x
        cmp #'0'
        bne :+
        lda #' '
        sta numbuf,x
        inx
        cpx #4
        bne :-
:       lda #'0'
        sta numbuf+5
        sta numbuf+6
        stz numbuf+7
```
->
```
        ldx #0
        stx numbuf+7                ; the terminator (X = 0)
:       lda numbuf,x
        cmp #'0'
        bne :+
        lda #' '
        sta numbuf,x
        inx
        cpx #4
        bne :-
:       lda #'0'
        sta numbuf+5
        sta numbuf+6
```

The zero terminator is stored from X (=0, just loaded) before the suppress loop instead of by stz after it. A after the change: the Model B stz left A=0 but lda #<numbuf reloads A immediately after (line 695); flags: lda #<numbuf sets them afresh.

Review: Correct on its own. X is 0 at the store. numbuf+7 is not touched by the suppress loop (X runs 0..3) or by the '0' stores. stx sets no flags. lda #<numbuf follows, so the A=0 and flags left by the Model B stz expansion were dead. On the Master, stz abs (3 bytes) becomes stx abs (3), saving nothing. On the Model B, lda #0 / sta abs (5) becomes stx abs (3), saving 2. It overlaps index 3 on lines 682-690: index 3 removes ldx #0, so no register holds zero. Apply this one only if index 3 is not applied.

## r055:6  src/menu.s:699-701  (window i0527)  M -6 B -6  [medium]  review: accept

Cycles: saves lda abs 4 + ldx abs 4 + sta abs 4 + stx abs 4 = 16 cycles on both targets

Assumption: drawtext (menu.s:13-15) begins sta tx (3 bytes) / stx ty (3 bytes) then ldy #0 on both targets (tx, ty are absolute: Master TABLES, Model B MNUBSS; listings show 8D rr rr / 8E rr rr on both, ldy #0 at +6); tx/ty are written only in menu.s (drawtext, draw_number); div10_16/m_div10_16 do not touch them; drawtext uses A/X only via those stores (next is ldy #0)

```
        lda tx
        ldx ty
        jmp drawtext
```
->
```
        jmp drawtext+6              ; past its sta tx/stx ty: tx, ty hold A, X already
```

draw_number stored A->tx, X->ty on entry; reloading them and re-storing them in drawtext's prologue writes the same values back. Entering at drawtext+6 skips the reload and the re-store. Cleaner equivalent: put a label (e.g. drawtext_xy:) on drawtext's ldy #0 at menu.s:16 (scoping @ch/@space/@done under it is fine, all their refs follow it) and jmp to that.

Review: The listings show drawtext starting with 8D rr rr (sta tx) and 8E rr rr (stx ty), then A0 00 (ldy #0) at +6, on both targets. tx and ty are absolute (TABLES on the Master, MNUBSS on the Model B). Only drawtext and draw_number in menu.s write tx and ty; div10_16 and farcall do not touch them. drawtext never reads X after its stx: the body uses tx, ty, tchar and Y. Both callers of draw_number reload X and A afterwards (menu.s:582-588 and 593-600). Saves 6 bytes and 16 cycles on both targets. The +6 offset would break if a later edit changes drawtext's prologue. A safer equivalent is a label drawtext_xy: on menu.s:16. It does not break the @ scope, because every @ch/@space/@done reference comes after it. Use the label if the applier can edit line 16.

## r056:0  src/main.s:27-52  (window i0009)  M -22 B -0  [high]  review: accept

Cycles: Master only (startup, sei). LOW copy: setup 10+2 -> 2; per byte lda (zp),y 5(+1)/sta 5/iny 2/cpy 2/bne 3 = 17 -> lda abs,y 4(+1)/sta 5/dey 2/bne 3 = 14. LOW2 staging: setup 10+2 (+5 for inc ptr+1 mid) -> 0; per 2 bytes 2x15=30 -> 23. Page-cross +1s occur on the loads in both forms (non-aligned source). Faster on every path; not timing-critical.

Assumption: __MAIN_LAST__/__LOW_START__/__LOW_LAST__ are link-time constants imported absolute, so abs,y works (verified in listing: B9/99 rr rr). LOW size is $B6 (map.txt; area max $ED), nonzero and <=255 so ldy #size / dey / bne copies exactly bytes 0..size-1. ptr is not read between line 52 and line 69 where it is re-set (OSWRCH only between; ptr is user zp $02). Source ($28CD..) and destinations ($0D03.., SPRREC=$0910..$0B0F) do not overlap, so copy order is irrelevant. Y leaves the LOW loop at 0 (dey/bne), replacing the ldy #0 at line 43; Y leaves the staging loop at 0 as before (line 59 relies on it). X untouched. A ends holding the last byte copied instead of the same -- A is reloaded (lda #22) at line 54.

```
        lda #<__MAIN_LAST__
        sta ptr
        lda #>__MAIN_LAST__
        sta ptr+1
        ldy #0
:       lda (ptr),y
        sta __LOW_START__,y
        iny
        cpy #<(__LOW_LAST__ - __LOW_START__)
        bne :-
        ; LOW2 follows LOW in the file; its home ($0300) is VDU workspace until MODE 1 has
        ; been selected and the screen clear wipes the file image, so stage it in SPRREC
        lda #<(__MAIN_LAST__ + __LOW_LAST__ - __LOW_START__)
        sta ptr
        lda #>(__MAIN_LAST__ + __LOW_LAST__ - __LOW_START__)
        sta ptr+1
        ldy #0                      ; two whole pages (SPRREC is 640 bytes; the overrun
:       lda (ptr),y                 ; past LOW2's end lands in MASKTAB, rebuilt below)
        sta SPRREC,y
        iny
        bne :-
        inc ptr+1
:       lda (ptr),y
        sta SPRREC+256,y
        iny
        bne :-
```
->
```
        ldy #<(__LOW_LAST__ - __LOW_START__)
:       lda __MAIN_LAST__-1,y       ; top down, so Y leaves at 0
        sta __LOW_START__-1,y
        dey
        bne :-
        ; LOW2 follows LOW in the file; its home ($0300) is VDU workspace until MODE 1 has
        ; been selected and the screen clear wipes the file image, so stage it in SPRREC:
        ; two whole pages (SPRREC is 640 bytes; the overrun past LOW2's end lands in
        ; MASKTAB, rebuilt below)
:       lda __MAIN_LAST__ + __LOW_LAST__ - __LOW_START__,y
        sta SPRREC,y
        lda __MAIN_LAST__ + __LOW_LAST__ - __LOW_START__ + 256,y
        sta SPRREC+256,y
        iny
        bne :-
```

LOW copy: original copies MAIN_LAST[y] -> LOW_START[y] for y=0..size-1 via (ptr),y with ptr=MAIN_LAST; new copies MAIN_LAST-1+y -> LOW_START-1+y for y=size..1, i.e. the same bytes, top down, with absolute,y addressing so the ptr setup goes. LOW2 staging: original ptr=MAIN_LAST+size, copies page 0 then (inc ptr+1) page 1 into SPRREC/SPRREC+256; new copies both pages in one loop with abs,y from the constant source, same 512 bytes to the same places. Y=0 on entry from the first loop replaces ldy #0. Test-assembled: CODE shrinks by 22 bytes.

Review: The original text matches src/main.s lines 27-52. src/main.s is Master-only: modelb/src/main.s does not include it. LOW is $B6 bytes (map: $0D03..$0DB9), so ldy #$B6/dey/bne copies exactly bytes 0..$B5. The abs,y forms assemble absolute on the link-time imports. The source ($28CD..) does not overlap $0D03.. or SPRREC $0910..$0B0F, so copy order does not matter. ptr is set again at stz ptr before its next use, and Y leaves both loops at 0. Test-assembled with all three candidates in a scratch copy: CODE went from $1ACD to $1AB3, which is 26 = 22+2+2 bytes. Master statediff on L0 was identical over 100 frames, and titlediff was pixel-identical. Every byte copies faster; this is startup code only.

## r056:1  src/main.s:59-59  (window i0036)  M -2 B -0  [high]  review: accept

Cycles: 2 -> 0 on the single path (Master only, startup).

Assumption: Y = 0 at line 53: the staging loop ends with iny/bne falling through (original line 52, and still so with the i0009 candidate). OSWRCH (lines 55,57; VDU 22,1) preserves A, X and Y per the MOS contract (A,X,Y preserved on exit), and the interrupt handlers enabled by cli restore registers. Nothing else between (cli, lda #imm, sei) touches Y.

```
        ldy #0
```
->
```

```

The ldy #0 reloads a value Y already holds; removing it leaves Y = 0 into the copy loop at line 60, and flags are set by the loop's own lda before any branch.

Review: Y is 0 when the iny/bne staging loop exits (the original loop and the i0009 form both end this way). cli, lda #imm, OSWRCH and sei all leave Y unchanged: the MOS contract says OSWRCH preserves A, X and Y. Checked by the scratch build plus statediff/titlediff.

## r056:2  src/main.s:69-79  (window i0042)  M -2 B -0  [high]  review: accept

Cycles: lda #0 2 -> ldx #9 2 + tya 2 (+2 once); per page inc 5/ldx zp 3/cpx 2/bne 3 = 13 -> inc 5/dex 2/bne 3 = 10, x9 pages = -27. Net faster (Master only, startup).

Assumption: Y = 0 at line 69 (loop at 60-65 exits on iny/bne). X dead at exit: original leaves X=$0D, new leaves X=0; the next code is jsr blank_palette (engine.s:4296), which starts ldx #15 and never reads X first. Exit A=0, Y=0, Z=1 identical to the original.

```
        stz ptr                     ; <$0400 is 0
        lda #>$0400
        sta ptr+1
        lda #0
:       sta (ptr),y
        iny
        bne :-
        inc ptr+1
        ldx ptr+1
        cpx #$0D
        bne :-
```
->
```
        stz ptr                     ; <$0400 is 0
        lda #>$0400
        sta ptr+1
        ldx #$0D - >$0400           ; pages $04..$0C
        tya                         ; Y = 0 from the copy above
:       sta (ptr),y
        iny
        bne :-
        inc ptr+1
        dex
        bne :-
```

tya gives A=0 as lda #0 did (Y=0). The page loop ran for ptr+1 = $04..$0C (9 pages) ending when inc made it $0D; a down-counter X=9 with dex/bne runs the same 9 pages, and ptr+1 still ends at $0D. Test-assembled: 2 bytes smaller.

Review: Y is 0 on entry because the copy loop at lines 60-65 exits on iny/bne, so tya does what lda #0 did. X counts 9 pages, $04..$0C, and ptr+1 still ends at $0D. X is dead afterwards: blank_palette starts with ldx #15. The carry left by cpx is never read, because the asl in blank_palette overwrites it. Each page takes 3 fewer cycles. Checked by the scratch build plus statediff/titlediff.

## r057:0  modelb/src/display.s:91-107  (window i0070)  M -0 B -2  [high]  review: accept

Cycles: Every path runs exactly one ldx #0 (2 cycles), before and after the change, so the no-flip paths (flipreq=0: 13; flipreq!=0 but <2 vsyncs: 25) are unchanged. Flip path: stz flipreq = lda #0/sta zp (5) becomes stx zp (3), 2 cycles fewer. Master: this file is not assembled.

Assumption: X is free in vsync_tick: it is reached by a jmp from isr_body, and its caller (low.s irq_handler) saved X/Y and reloads them. Nothing between the hoisted ldx and @noflip reads or writes X (lines 92-101 use only A: lda/sec/sbc/cmp/bcc/sta). @noflip is branched to only by the beq/bcc at lines 92/97, both after the hoisted ldx. flipreq is zp, and stx zp exists on the 6502. The Q-time R12/R13/T1-latch writes after @noflip are not cycle-critical: their timing already differs by about 30 cycles across the three existing paths (13/25/43 cycles to @noflip), and they latch at the bar restart about 16 rows later. The ldx goes after the last re-phase CRTC write (line 89), so nothing before that write moves.

```
        lda flipreq
        beq @noflip
        lda vsyncs
        sec
        sbc flipvs
        cmp #2
        bcc @noflip
        lda vsyncs
        sta flipvs
        lda NEXTSECT
        sta DISPSECT
        stz flipreq
@noflip:
        ldx #0                      ; section 0 is the bar: its address and length come
        ldy DISPSECT                ; from the buffer about to be displayed
        beq :+
        ldx #2
```
->
```
        ldx #0                      ; X = 0: the bar's pair below, and flipreq's clear
        lda flipreq
        beq @noflip
        lda vsyncs
        sec
        sbc flipvs
        cmp #2
        bcc @noflip
        lda vsyncs
        sta flipvs
        lda NEXTSECT
        sta DISPSECT
        stx flipreq
@noflip:
        ldy DISPSECT                ; section 0 is the bar: its address and length come
        beq :+                      ; from the buffer about to be displayed
        ldx #2
```

ldx #0 moves from @noflip to before lda flipreq, so X=0 on arrival at @noflip on all three paths, as before (ldx sets N,Z, but lda flipreq overwrites them at once). stz flipreq (6502: lda #0 / sta flipreq, with A dead because the path after @noflip reloads A at lda BUF_SEC0T1,x) becomes stx flipreq with X=0: the same memory effect. Its flags were never read, because ldy DISPSECT sets Z for the beq. The DISPSECT store and reload are unchanged.

Review: Original matches lines 91-107. X is written at 104 anyway, and nothing in 91-102 reads or writes X. The only branches to @noflip (92, 97) come after the hoisted ldx, so X=0 at @noflip as before. stx flipreq (zp, 86 xx) stores the same 0. A is dead because it is reloaded at lda BUF_SEC0T1,x. Z at @noflip comes from ldy DISPSECT. Cycles before the last re-phase CRTC write (89) do not change. The no-flip paths do not change. The flip path is 2 cycles earlier to the T1-latch and R12/R13 latch writes, which already vary by 30+ cycles across the three paths and latch rows later, so this is not phase-sensitive. The scratch build shows vsync_tick is 2 bytes shorter and @noflip is at the same address.

## r057:1  modelb/src/display.s:188-190  (window i0120)  M -0 B -1  [high]  review: accept

Cycles: lda zp 3 -> txa 2 (one path). Master: this file is not assembled.

Assumption: X = curbuf from ldx curbuf at line 179; lines 180-187 are only lda abs,x / sta abs, which leave X unchanged.

```
        lda curbuf
        asl
        tax
```
->
```
        txa
        asl
        tax
```

txa gives A = curbuf and sets N,Z from it, exactly as lda curbuf does. asl/tax are unchanged (C = bit 7 of curbuf = 0, as before).

Review: X = curbuf from line 179 and is unchanged by 180-187 (lda abs,x / sta abs). txa gives the same A and N,Z as lda curbuf (zp). Built: 8A 0A AA, 1 byte saved, 3->2 cycles.

## r057:2  modelb/src/display.s:199-201  (window i0120)  M -0 B -1  [high]  review: accept

Cycles: ldx zp 3 -> txa 2 on both branch outcomes. Master: this file is not assembled.

Assumption: X = curbuf*2 from lda curbuf/asl/tax at 188-190 (the sta abs,x at 192-198 leave X alone). curbuf is 0 or 1 (engine.s:3149-3151 eor #1 toggle, init.s stz/inc, engine.s:3596 stz). A is dead: the next instruction on both paths is lda #BARROWS-1 at the ':' on line 202.

```
        ldx curbuf
        beq :+
        ldx #48
```
->
```
        txa                         ; X = 2*curbuf: 0 iff curbuf = 0
        beq :+
        ldx #48
```

curbuf=0: originally X=0 and Z=1, so beq is taken with X=0; now A=X=0 and Z=1, so beq is taken with X=0. curbuf=1: originally ldx gives X=1, Z=0, then ldx #48; now A=2, Z=0, then ldx #48. Neither version touches carry (C=0 from the asl stays for the eor/adc below).

Review: X = 2*curbuf (curbuf is 0/1: @cbl has two entries). Z is the same for the beq, and X is 0 on the taken path and 48 on the other. A is dead (lda #BARROWS-1 at the ':' on both paths). C (0 from the asl) is untouched either way. ldx zp (2) -> txa (1), 3->2 cycles.

## r057:3  modelb/src/display.s:213-214  (window i0123)  M -0 B -4  [high]  review: accept

Cycles: inca (sta zp 3 + inc zp 5 + lda zp 3 = 11) -> adc # 2 on the single path. Master: this file is not assembled.

Assumption: C = 0 here: the asl at line 189 sets it (curbuf is 0 or 1, so bit 7 = 0), and nothing between 189 and 213 writes C (tax, lda, sta abs,x, ldx/txa, beq, ldx #, lda wfine, beq, eor). @dur (line 437) starts with sta tmp3 / lda #0 / lsr tmp3, so it ignores C and V on entry and overwrites both (lsr, sbc). mtmp is expansion scratch that nothing reads after inca.

```
        eor #7
        inca                        ; 8-f lines of it
```
->
```
        eor #7
        adc #1                      ; 8-f lines of it (C = 0 from the asl of curbuf)
```

inca computes A+1, keeps C (0 here) and sets N,Z from the result. adc #1 with C=0 also gives A+1 (A=7-f <= 7, so there is no carry out and C stays 0) and sets N,Z from the result. adc sets V to 0 where inca left it alone, but nothing reads V before @dur's sbc rewrites it.

Review: C = 0 at 213: asl at 189 of curbuf (0/1), and nothing in 190-213 writes C. wfine = (wy&3)*2 is in {2,4,6} here, so 7^f+1 <= 8, with no carry out and the same N,Z. @dur ignores incoming C/V (sta/lda#0/lsr) and sbc overwrites V. mtmp is scratch. No decimal mode anywhere (no sed). The Model B inca is sta/inc/lda zp (6 bytes, 11 cycles) -> adc # (2 bytes, 2 cycles).

## r057:4  modelb/src/display.s:215-446  (window i0123)  M -0 B -16  [high]  review: accept

Cycles: Each of the three call sites runs exactly the same instructions (sta abs,x 5 / lda zp 3 / sta abs,x 5), now just before @dur's rts instead of just after the call. The jsr/rts counts are unchanged, so every path takes the same time. Master: this file is not assembled (engine.s has its own copy).

Assumption: @dur's only callers are jsr @dur at 215 and 297 and jmp @dur at 395 (@lines), and @lines' only caller is jsr @lines at 350 (@emit) (grep '@dur\|@lines' display.s). Each call is followed at once by sta SECTAB+6,x / lda tmp3 / sta SECTAB+7,x, with X the same entry index @dur was entered with (@dur/@lines never touch X). The range is large only because the edit has four parts that must land together; nothing else in lines 219-436 changes, so applying it will leave any other candidate that overlaps 219-436 stale.

```
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        lda ringS                   ; the composed row is the 80 chars above the window
        sec
        sbc #<ROWCHARS
        sta w16
        lda ringS+1
        sbc #0
        sta w16+1
        bpl :+
        lda w16
        clc
        adc #<RINGCHARS
        sta w16
        lda w16+1
        adc #>RINGCHARS
        sta w16+1
:       jsr @addr
        txa
        clc
        adc #8
        tax                         ; A's entry
        stz SECTAB+2,x
        lda wfine
        eor #7                      ; 7 - f
        sta SECTAB+3,x
        lda #2
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda ringS                   ; the run starts one row into the window
        clc
        adc #<ROWCHARS
        sta w16
        lda ringS+1
        adc #0
        sta w16+1
        jsr @wrap
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        ldy barq
        iny
        cpy #RINGROWS
        bcc :+
        ldy #0
:       sty tmp3
        bra @run
@coarse:                            ; ---- f = 0: T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda #VISROWS
        sta tmp4
        lda barq
        sta tmp3
@run:   ; w16 = the run's ring offset, tmp4 = its rows, X = the entry before it
        jsr @nfull                  ; rows that end before the ring end
        cmp tmp4
        bcs @one                    ; the whole run fits
        cmp #0
        beq @one                    ; it starts inside the straddling row: all of it folds
        sta tmp2
        jsr @emit                   ; up to the ring end
        lda tmp2
        jsr @advance
        lda tmp4
        sec
        sbc tmp2
        sta tmp4
@one:   lda tmp4
        sta tmp2
        jsr @emit
        lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2: the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2, x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        rts
@cbl:   .byte <CRTCB_A, <CRTCB_B
@cbh:   .byte >CRTCB_A, >CRTCB_B
@cml:   .byte <(CRTCB_A-RINGCHARS), <(CRTCB_B-RINGCHARS)
@cmh:   .byte >(CRTCB_A-RINGCHARS), >(CRTCB_B-RINGCHARS)
; --- emit a run of tmp2 rows starting at ring offset w16, following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address of the row at ring offset w16.  A row starting
; past RINGCHARS-80 straddles the ring end and is read from the mirror below the base,
; which is exactly the address w16 - RINGCHARS names.
@addr:  lda w16
        ldy w16+1
        cpy #>(RINGCHARS-ROWCHARS+1)
        bcc :++
        bne :+
        cmp #<(RINGCHARS-ROWCHARS+1)
        bcc :++
:       clc
        adc crtcbm
        sta SECTAB+1,x
        tya
        adc crtcbm+1
        sta SECTAB,x
        rts
:       clc
        adc crtcb
        sta SECTAB+1,x
        tya
        adc crtcb+1
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
        ; --- rows of the run that finish before the ring end.  The run starts on ring
        ; row tmp3; when r = ringS mod 80 is non-zero the last ring row straddles and
        ; @addr sends it to the mirror, so RINGROWS-1-tmp3 rows come first.
@nfull: ldy barq
        lda ringS
        sec
        sbc mulrowlo,y              ; r
        cmp #1                      ; C = 1 iff r > 0
        lda #RINGROWS-1
        bcs :+
        adc #0                      ; r == 0: C is clear here, so this adds 1
        adc #1
:       sec
        sbc tmp3
        rts
        ; --- A = lines -> A/tmp3 = the T1 count that lasts that long
@dur:   sta tmp3                    ; n*64 == (n*256)>>2
        lda #0
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair: A - 2
        bcs :+
        dec tmp3
:       rts
```
->
```
        jsr @dur
        lda ringS                   ; the composed row is the 80 chars above the window
        sec
        sbc #<ROWCHARS
        sta w16
        lda ringS+1
        sbc #0
        sta w16+1
        bpl :+
        lda w16
        clc
        adc #<RINGCHARS
        sta w16
        lda w16+1
        adc #>RINGCHARS
        sta w16+1
:       jsr @addr
        txa
        clc
        adc #8
        tax                         ; A's entry
        stz SECTAB+2,x
        lda wfine
        eor #7                      ; 7 - f
        sta SECTAB+3,x
        lda #2
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda ringS                   ; the run starts one row into the window
        clc
        adc #<ROWCHARS
        sta w16
        lda ringS+1
        adc #0
        sta w16+1
        jsr @wrap
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        ldy barq
        iny
        cpy #RINGROWS
        bcc :+
        ldy #0
:       sty tmp3
        bra @run
@coarse:                            ; ---- f = 0: T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda #VISROWS
        sta tmp4
        lda barq
        sta tmp3
@run:   ; w16 = the run's ring offset, tmp4 = its rows, X = the entry before it
        jsr @nfull                  ; rows that end before the ring end
        cmp tmp4
        bcs @one                    ; the whole run fits
        cmp #0
        beq @one                    ; it starts inside the straddling row: all of it folds
        sta tmp2
        jsr @emit                   ; up to the ring end
        lda tmp2
        jsr @advance
        lda tmp4
        sec
        sbc tmp2
        sta tmp4
@one:   lda tmp4
        sta tmp2
        jsr @emit
        lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2: the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        txa
        clc
        adc #8
        tax
        stza SECTAB+2, x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        rts
@cbl:   .byte <CRTCB_A, <CRTCB_B
@cbh:   .byte >CRTCB_A, >CRTCB_B
@cml:   .byte <(CRTCB_A-RINGCHARS), <(CRTCB_B-RINGCHARS)
@cmh:   .byte >(CRTCB_A-RINGCHARS), >(CRTCB_B-RINGCHARS)
; --- emit a run of tmp2 rows starting at ring offset w16, following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address of the row at ring offset w16.  A row starting
; past RINGCHARS-80 straddles the ring end and is read from the mirror below the base,
; which is exactly the address w16 - RINGCHARS names.
@addr:  lda w16
        ldy w16+1
        cpy #>(RINGCHARS-ROWCHARS+1)
        bcc :++
        bne :+
        cmp #<(RINGCHARS-ROWCHARS+1)
        bcc :++
:       clc
        adc crtcbm
        sta SECTAB+1,x
        tya
        adc crtcbm+1
        sta SECTAB,x
        rts
:       clc
        adc crtcb
        sta SECTAB+1,x
        tya
        adc crtcb+1
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
        ; --- rows of the run that finish before the ring end.  The run starts on ring
        ; row tmp3; when r = ringS mod 80 is non-zero the last ring row straddles and
        ; @addr sends it to the mirror, so RINGROWS-1-tmp3 rows come first.
@nfull: ldy barq
        lda ringS
        sec
        sbc mulrowlo,y              ; r
        cmp #1                      ; C = 1 iff r > 0
        lda #RINGROWS-1
        bcs :+
        adc #0                      ; r == 0: C is clear here, so this adds 1
        adc #1
:       sec
        sbc tmp3
        rts
        ; --- A = lines -> A/tmp3 = the T1 count that lasts that long
@dur:   sta tmp3                    ; n*64 == (n*256)>>2
        lda #0
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair: A - 2
        bcs :+
        dec tmp3
:       sta SECTAB+6,x           ; every caller stores the count in its entry
        lda tmp3
        sta SECTAB+7,x
        rts
```

Shared tail: the three identical 8-byte sequences after the calls move to @dur's single exit (its ':' rts, which both the bcs-taken and the dec-tmp3 paths reach). On return A = tmp3, N,Z come from lda tmp3, C is as @dur's sbc left it (sta/lda do not touch C), X is unchanged and SECTAB+6/7,x are written. That is the same state the original had after the stores at all three sites. The ':' label stays on the first line of the tail, so bcs :+ still targets it, and the anonymous label count does not change. A scratch assembly of all five candidates together builds cleanly: isr_body..build_sections shrinks by 2 bytes and build_sections..mirror_copy by 22.

Review: Original matches 215-446 verbatim, and so does the rest of the proposal. @dur's only callers are jsr 215, jsr 297 and jmp 395 (@lines, whose only caller is jsr 350). Each is followed by the same sta SECTAB+6,x / lda tmp3 / sta SECTAB+7,x, and X is unchanged by @dur/@lines. Both @dur exits reach the relabelled ':' tail, so the anon label count and the bcs :+ target are kept. No later ':-' reaches back (mirror_copy defines its own ':' first). The exit state (A=tmp3, N,Z, C from sbc, X, stores) is the same. The cycles per path are the same. The scratch build assembles, and build_sections..mirror_copy shrinks by 22 bytes with 0-3 applied as well (16 from this one).

## r058:0  modelb/src/display.s:188-446  (window i0129)  M -0 B -65  [high]  review: accept

Cycles: Every path faster: py65 per-input cycle delta new-old in [-68,-28] over all 29440 inputs (Model B only file). No path slower.

Assumption: Union of C1-C8, C10, C11, M1 plus M3 (M2 extended: @dur also advances X by 8, and the A section calls @addr before @dur). A dead on return from build_sections: every caller overwrites it (engine.s:3133 lda curbuf; init.s:90 inc curbuf / 92 stz curbuf; menu.s:233 stz NEXTSECT after m_build_sections). curbuf is 0/1 (every writer: engine.s 3151/3596, init.s 90/92, menu.s 212/226/239, main.s 110-115). ringS in 0..RINGCHARS-1 (calc_ring folds it, engine.s ~3450). >BARCRTC = 0 (BARADDR = $0300, now .asserted). @nfull and @dur have no callers outside build_sections (grep). SECTAB is written for the back buffer (the ISR walks the displayed one), so reordering stores within an entry is safe; menu_show rebuilds buffer 0 in place but the original already writes entries non-atomically.

```
        lda curbuf
        asl
        tax
        lda #>BARCRTC               ; section 0 is the bar: fixed address and length
        sta BUF_SEC0,x
        lda #<BARCRTC
        sta BUF_SEC0+1,x
        lda #<(BARROWS*8*LINE-2)
        sta BUF_SEC0T1,x
        lda #>(BARROWS*8*LINE-2)
        sta BUF_SEC0T1+1,x
        ldx curbuf
        beq :+
        ldx #48
:       lda #BARROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #BARROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda wfine
        beq @coarse
        ; ---- f > 0: T -> A (the composed row) -> P.. -> P2 -> Q
        eor #7
        inca                        ; 8-f lines of it
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        lda ringS                   ; the composed row is the 80 chars above the window
        sec
        sbc #<ROWCHARS
        sta w16
        lda ringS+1
        sbc #0
        sta w16+1
        bpl :+
        lda w16
        clc
        adc #<RINGCHARS
        sta w16
        lda w16+1
        adc #>RINGCHARS
        sta w16+1
:       jsr @addr
        txa
        clc
        adc #8
        tax                         ; A's entry
        stz SECTAB+2,x
        lda wfine
        eor #7                      ; 7 - f
        sta SECTAB+3,x
        lda #2
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda ringS                   ; the run starts one row into the window
        clc
        adc #<ROWCHARS
        sta w16
        lda ringS+1
        adc #0
        sta w16+1
        jsr @wrap
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        ldy barq
        iny
        cpy #RINGROWS
        bcc :+
        ldy #0
:       sty tmp3
        bra @run
@coarse:                            ; ---- f = 0: T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda #VISROWS
        sta tmp4
        lda barq
        sta tmp3
@run:   ; w16 = the run's ring offset, tmp4 = its rows, X = the entry before it
        jsr @nfull                  ; rows that end before the ring end
        cmp tmp4
        bcs @one                    ; the whole run fits
        cmp #0
        beq @one                    ; it starts inside the straddling row: all of it folds
        sta tmp2
        jsr @emit                   ; up to the ring end
        lda tmp2
        jsr @advance
        lda tmp4
        sec
        sbc tmp2
        sta tmp4
@one:   lda tmp4
        sta tmp2
        jsr @emit
        lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2: the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2, x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        rts
@cbl:   .byte <CRTCB_A, <CRTCB_B
@cbh:   .byte >CRTCB_A, >CRTCB_B
@cml:   .byte <(CRTCB_A-RINGCHARS), <(CRTCB_B-RINGCHARS)
@cmh:   .byte >(CRTCB_A-RINGCHARS), >(CRTCB_B-RINGCHARS)
; --- emit a run of tmp2 rows starting at ring offset w16, following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address of the row at ring offset w16.  A row starting
; past RINGCHARS-80 straddles the ring end and is read from the mirror below the base,
; which is exactly the address w16 - RINGCHARS names.
@addr:  lda w16
        ldy w16+1
        cpy #>(RINGCHARS-ROWCHARS+1)
        bcc :++
        bne :+
        cmp #<(RINGCHARS-ROWCHARS+1)
        bcc :++
:       clc
        adc crtcbm
        sta SECTAB+1,x
        tya
        adc crtcbm+1
        sta SECTAB,x
        rts
:       clc
        adc crtcb
        sta SECTAB+1,x
        tya
        adc crtcb+1
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
        ; --- rows of the run that finish before the ring end.  The run starts on ring
        ; row tmp3; when r = ringS mod 80 is non-zero the last ring row straddles and
        ; @addr sends it to the mirror, so RINGROWS-1-tmp3 rows come first.
@nfull: ldy barq
        lda ringS
        sec
        sbc mulrowlo,y              ; r
        cmp #1                      ; C = 1 iff r > 0
        lda #RINGROWS-1
        bcs :+
        adc #0                      ; r == 0: C is clear here, so this adds 1
        adc #1
:       sec
        sbc tmp3
        rts
        ; --- A = lines -> A/tmp3 = the T1 count that lasts that long
@dur:   sta tmp3                    ; n*64 == (n*256)>>2
        lda #0
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair: A - 2
        bcs :+
        dec tmp3
:       rts
```
->
```
        txa                         ; X = curbuf still (ldx curbuf above)
        asl
        tax
        lda #>BARCRTC               ; section 0 is the bar: fixed address and length
        sta BUF_SEC0,x
        lda #<BARCRTC
        sta BUF_SEC0+1,x
        lda #<(BARROWS*8*LINE-2)
        sta BUF_SEC0T1,x
        lda #>(BARROWS*8*LINE-2)
        sta BUF_SEC0T1+1,x
        txa                         ; Z from X = curbuf*2
        beq :+
        ldx #48
:       lda #BARROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #BARROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda wfine
        beq @coarse
        ; ---- f > 0: T -> A (the composed row) -> P.. -> P2 -> Q
        lda ringS                   ; the composed row is the 80 chars above the window
        sec
        sbc #<ROWCHARS
        sta w16
        lda ringS+1
        sbc #0
        bpl :+                      ; negative: C = 0 (the borrow), A = $FF
        lda w16                     ; + RINGCHARS
        adc #<RINGCHARS
        sta w16
        lda #>(RINGCHARS-$100)      ; $FF + >RINGCHARS + C
        adc #0
:       sta w16+1
        jsr @addr
        lda wfine
        eor #7                      ; 7 - f: A's R9
        sta SECTAB+8+3,x
        clc
        adc #1                      ; 8-f lines of it
        jsr @dur                    ; X = A's entry
        stz SECTAB+2,x
        lda #2
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda ringS                   ; the run starts one row into the window (C = 0: @dur's adc #8)
        adc #<ROWCHARS
        sta w16
        lda ringS+1
        adc #0
        sta w16+1
        jsr @wrap
        ldy barq
        iny
        cpy #RINGROWS
        bcc :+
        ldy #0
:       sty tmp3
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        bne @run                    ; always: A = VISROWS-1
@coarse:                            ; ---- f = 0: T -> P.. -> Q
        lda ringS
        sta w16
        lda ringS+1
        sta w16+1
        lda #VISROWS
        sta tmp4
        lda barq
        sta tmp3
@run:   ; w16 = the run's ring offset, tmp4 = its rows, X = the entry before it
        ; rows of the run that end before the ring end (was @nfull): the run starts on
        ; ring row tmp3; r = ringS mod 80 non-zero -> the last ring row straddles
        ldy barq
        lda ringS
        sec
        sbc mulrowlo,y              ; r
        cmp #1                      ; C = 1 iff r > 0
        lda #RINGROWS-1
        bcs :+
        adc #0                      ; r == 0: C is clear here, so this adds 1
        adc #1
:       sec
        sbc tmp3
        cmp tmp4
        bcs @one                    ; the whole run fits
        tay                         ; Z from A (Y is dead: @emit's @addr reloads it)
        beq @one                    ; it starts inside the straddling row: all of it folds
        sta tmp2
        jsr @emit                   ; up to the ring end
        lda tmp2
        jsr @advance
        lda tmp4
        sec
        sbc tmp2
        sta tmp4
@one:   lda tmp4
        sta tmp2
        jsr @emit
        lda tmp4
        jsr @advance                ; now the row below the playfield
        jsr @addr                   ; the row below the playfield: P2's start, or Q's
        lda wfine
        beq @sq2
        ; --- P2: the top f lines of that row (A = wfine)
        jsr @dur                    ; X = P2's entry
        stz SECTAB+2,x              ; A dead: reloaded next
        lda wfine
        sbc #0                      ; C = 0 from @dur's adc #8: f - 1
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
@sq2:   txa
        clc
        adc #8
        tax
        lda #<(40*LINE-2)           ; the previous section's T1 and Q's
        sta SECTAB-8+6,x
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB-8+7,x
        sta SECTAB+7,x
        .assert >BARCRTC = 0, error, "Q's R12 and R6 share the zero"
        lda #0                      ; >BARCRTC, and Q's R6
        sta SECTAB,x                ; hands the chain back to the bar
        sta SECTAB+4,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #QVSYNC
        sta SECTAB+5,x
        rts
@cbl:   .byte <CRTCB_A, <CRTCB_B
@cbh:   .byte >CRTCB_A, >CRTCB_B
@cml:   .byte <(CRTCB_A-RINGCHARS), <(CRTCB_B-RINGCHARS)
@cmh:   .byte >(CRTCB_A-RINGCHARS), >(CRTCB_B-RINGCHARS)
; --- emit a run of tmp2 rows starting at ring offset w16, following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines                  ; X = the run's entry
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address of the row at ring offset w16.  A row starting
; past RINGCHARS-80 straddles the ring end and is read from the mirror below the base,
; which is exactly the address w16 - RINGCHARS names.
@addr:  lda w16
        ldy w16+1
        cpy #>(RINGCHARS-ROWCHARS+1)
        bcc :++
        bne :+
        cmp #<(RINGCHARS-ROWCHARS+1)
        bcc :++
:       clc
        adc crtcbm
        sta SECTAB+1,x
        tya
        adc crtcbm+1
        sta SECTAB,x
        rts
:       clc
        adc crtcb
        sta SECTAB+1,x
        tya
        adc crtcb+1
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
        ; --- A = lines, X = an entry -> its T1lo/T1hi (SECTAB+6/7,x) = the T1 count
        ; that lasts that long (tmp3 = the high byte); then X = the next entry (C = 0)
@dur:   sta tmp3                    ; n*64 == (n*256)>>2
        lda #0
        lsr tmp3
        ror
        lsr tmp3
        ror
        sbc #1                      ; C = 0 out of the ror pair: A - 2
        bcs :+
        dec tmp3
:       sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        rts
```

[ALL] Apply first: it subsumes every other candidate here (C9 and M2 are alternatives it supersedes). Composite of the individual candidates in this file (see each) plus M3: @dur's tail becomes 'sta SECTAB+6,x / lda tmp3 / sta SECTAB+7,x / txa / clc / adc #8 / tax / rts', removing the three copies of the store triple and the txa/clc/adc #8/tax after each @dur call (P2, @emit via @lines). The A section is reordered: w16 = ringS-80 (wrapped) is computed first, jsr @addr fills T's address, then 'lda wfine / eor #7 / sta SECTAB+8+3,x (A's R9 = 7-f, stored before X moves) / clc / adc #1 / jsr @dur' fills T's duration and moves X to A's entry. @addr and @dur touch disjoint bytes of entry X so their order is free. C = 0 after @dur (its adc #8 cannot overflow: X <= 88) is what C5 (no clc before adc #<ROWCHARS) and C8 (sbc #0 = f-1) now rely on. Verified: assembled (LGCCODE size from ld65 map, $2A0A -> $29C9) and simulated with py65 against the original build_sections over all 29440 inputs (curbuf 0/1 x wfine 0..7 x ringS 0..1839, barq = ringS/80): SECTAB (both buffers), BUF_SEC0/T1, crtcb/crtcbm, w16/tmp2/tmp3/tmp4, X, Y, P identical; A on return differs ($08 vs $09), dead at every caller.

Review: Original matches lines 188-446 verbatim. I assembled it (ca65 6502 + cpu.inc macros) and ran it in py65 against the original over every curbuf 0/1 x wfine 0..7 x ringS 0..1839 (barq = ringS/80) with random zp, SECTAB and register contents: SECTAB (both buffers), BUF_SEC0/T1, crtcb/crtcbm, w16/tmp2..4, X, Y and P are identical. Only two things differ: A on return, which every caller overwrites (init.s:90/92, engine.s:3133 lda curbuf, menu.s:233 stz NEXTSECT after the far return), and mtmp ($DF). mtmp is cpu.inc's own scratch and nothing reads it outside a macro expansion. Cycle delta is -68..-28, no path slower. C = 0 after the new @dur tail holds because X <= 88. >BARCRTC = 0 on the Model B (BARADDR $0300) and the .assert guards it. The anonymous labels still resolve correctly, since the assembled result simulates identically. Reordering stores within an entry is safe: build_sections writes the back buffer, and menu_show's in-place rebuild of buffer 0 rewrites the same values.

## r058:1  modelb/src/display.s:188-188  (window i0098)  M -0 B -1  [high]  review: accept

Cycles: lda zp 3 -> txa 2: -1 cycle (only path).

Assumption: X = curbuf here: ldx curbuf at display.s:179, then only lda abs,x / sta abs at 180-187 (X untouched).

```
        lda curbuf
```
->
```
        txa                         ; X = curbuf still (ldx curbuf above)
```

[C1] lda curbuf -> txa: same A, same N/Z (curbuf 0/1). Verified by assembly and py65 simulation over all 29440 inputs: memory, X, Y, P, A identical.

Review: X = curbuf from ldx at 179 (lda abs,x / sta zp leave X alone), and asl/tax follow. Simulated identical, -1 cycle. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:2  modelb/src/display.s:199-199  (window i0109)  M -0 B -1  [high]  review: accept

Cycles: ldx zp 3 -> txa 2: -1 on both branch outcomes.

Assumption: X = curbuf*2 from asl/tax at 189-190, untouched by the sta abs,x at 192-198. A dead: the ':' target (202) and the fall-through ldx #48 are followed by lda #BARROWS-1.

```
        ldx curbuf
```
->
```
        txa                         ; Z from X = curbuf*2
```

[C2] ldx curbuf / beq: Z from curbuf. txa / beq: Z from curbuf*2 -- zero iff curbuf is. X keeps curbuf*2 = 0 on the taken path (it needs 0) and is reloaded with 48 otherwise. Verified by assembly and py65 simulation over all 29440 inputs.

Review: X = curbuf*2 after asl/tax, so Z is the same. A is dead (lda #BARROWS-1 follows on both paths). Simulated identical, -1 cycle. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:3  modelb/src/display.s:213-214  (window i0122)  M -0 B -4  [high]  review: accept

Cycles: inca (Model B: sta/inc/lda zp = 3+5+3=11) -> adc #1 (2): -9 on the only path.

Assumption: C = 0 at line 213: set by asl of curbuf at 189 (curbuf 0/1, bit 7 clear) and unchanged by tax/lda/sta/ldx/beq/txa up to 211. The carry out is dead: @dur (437) does sta/lda #0/lsr before its first carry use. mtmp's value is not read afterwards (macro scratch).

```
        eor #7
        inca                        ; 8-f lines of it
```
->
```
        eor #7
        adc #1                      ; C = 0 from the asl of curbuf: 8-f lines of it
```

[C3] A = f^7 in 0..6 (f = 1..7); inca gives A+1 with N,Z from the result; adc #1 with C=0 gives the same A and N,Z (no carry out, V clear -- neither read). Verified by assembly and py65 simulation over all 29440 inputs.

Review: C = 0 from the asl of curbuf (0/1), and nothing through 213 touches C. The Model B inca is sta/inc/lda mtmp (6 bytes), so adc #1 saves 4. Simulated identical; delta -11..0 (0 is the f=0 path, which does not run this code). Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:4  modelb/src/display.s:225-234  (window i0141)  M -0 B -3  [high]  review: accept

Cycles: Positive (no wrap) path: sta zp 3 + bpl taken 3 = 6 -> bpl taken 3 + sta 3 = 6. Negative path: 23 -> 17.

Assumption: ringS in 0..RINGCHARS-1 = 0..$72F (calc_ring, engine.s ~3450), so ringS+1 is 0..7: the sbc #0 result is negative only when it borrowed ($00-1 = $FF), i.e. C = 0 and A = $FF on the bmi path. RINGCHARS = $730 on the Model B.

```
        sta w16+1
        bpl :+
        lda w16
        clc
        adc #<RINGCHARS
        sta w16
        lda w16+1
        adc #>RINGCHARS
        sta w16+1
:       jsr @addr
```
->
```
        bpl :+                      ; negative: C = 0 (the borrow), A = $FF
        lda w16                     ; + RINGCHARS
        adc #<RINGCHARS
        sta w16
        lda #>(RINGCHARS-$100)      ; $FF + >RINGCHARS + C
        adc #0
:       sta w16+1
        jsr @addr
```

[C4] Negative path: w16+1 was $FF; new low byte = w16 + <RINGCHARS with C=0 (the dropped clc was redundant), carry c out; high = $FF + >RINGCHARS + c = >(RINGCHARS-$100) + c, computed as lda #6 / adc #0 (no carry out, not needed: @addr overwrites flags). Positive path stores A (the sbc #0 result) to w16+1 exactly as before. Verified by assembly and py65 simulation over all 29440 inputs.

Review: On the negative path ringS+1 is 0 and a borrow occurred, so A = $FF and C = 0. $FF + 7 + c = 6 + c, as the proposal says. The positive path stores the same A. The ':' moves but stays the one label between the bpl and the next ':'. Simulated identical, 3 bytes. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:5  modelb/src/display.s:247-248  (window i0155)  M -0 B -1  [high]  review: accept

Cycles: clc removed: -2 on the only path.

Assumption: C = 0 at 247: adc #8 at 237 on X = 0 or 48 (T's entry) cannot carry; stz (lda #0/sta), lda, eor, sta at 239-246 do not touch C.

```
        lda ringS                   ; the run starts one row into the window
        clc
```
->
```
        lda ringS                   ; the run starts one row into the window (C = 0: adc #8)
```

[C5] clc was a no-op. Verified by assembly and py65 simulation over all 29440 inputs.

Review: C = 0 after adc #8 on X = 0/48, and the stz expansion, lda, eor and sta do not touch C. Simulated identical. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:6  modelb/src/display.s:255-263  (window i0169)  M -0 B -1  [high]  review: accept

Cycles: bra (Model B jmp, 3) -> bne taken (3), same; lda/sta moved, same count.

Assumption: Y and tmp3 computation does not read A or tmp4; @run (jsr @nfull) reads neither A nor flags on entry; A = VISROWS-1 at @run in both orders.

```
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        ldy barq
        iny
        cpy #RINGROWS
        bcc :+
        ldy #0
:       sty tmp3
        bra @run
```
->
```
        ldy barq
        iny
        cpy #RINGROWS
        bcc :+
        ldy #0
:       sty tmp3
        lda #VISROWS-1
        sta tmp4                    ; rows in the run
        bne @run                    ; always: A = VISROWS-1
```

[C6] lda #VISROWS-1 / sta tmp4 moved after sty tmp3, so Z = 0 (A = 20) at the branch: bne always taken, same target, same A/X/Y/memory. Verified by assembly and py65 simulation over all 29440 inputs.

Review: A = VISROWS-1 = 20, which is non-zero, so bne is always taken. The Model B bra is a 3-byte jmp, so this saves 1. Simulated identical, same cycles. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:7  modelb/src/display.s:277-277  (window i0181)  M -0 B -1  [high]  review: accept

Cycles: cmp #0 (2) -> tay (2): same.

Assumption: Y dead: both successors reach jsr @emit, whose first act is jsr @addr -> ldy w16+1 (display.s:348,371); nothing between reads Y. @nfull's result is 0..23 (never used as signed).

```
        cmp #0
```
->
```
        tay                         ; Z from A (Y is dead: @emit's @addr reloads it)
```

[C7] Only Z is consumed (beq @one); tay sets Z from A like cmp #0. C (from cmp tmp4, =0 here) is not read afterwards. Verified by assembly and py65 simulation over all 29440 inputs.

Review: Y is dead: both successors reach @emit, then @addr, which does ldy w16+1. Only Z is consumed, and C is not read before @addr's cpy. Simulated identical. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:8  modelb/src/display.s:305-307  (window i0210)  M -0 B -6  [high]  review: accept

Cycles: stza (pha 3 + lda 2 + sta 5 + pla 4 = 14) -> stz (7); deca (11) -> sbc #0 (2): 28 -> 12 on the P2 path.

Assumption: A dead after the store: lda wfine follows. C = 0 at 305: adc #8 at 303 on X <= 80 cannot carry and stz/lda do not touch C. wfine is 1..7 on this path (beq @setq at 293). V is not read before the adc #8 at 325 rewrites it.

```
        stza SECTAB+2, x
        lda wfine
        deca
```
->
```
        stz SECTAB+2,x              ; A dead: reloaded next
        lda wfine
        sbc #0                      ; C = 0 from the adc #8: f - 1
```

[C8] stza preserved A only for it to be overwritten by lda wfine. deca = A-1 with C preserved; sbc #0 with C=0 = A-1, same N/Z; C comes out 1 instead of 0 but the next carry use is after clc at 324. Verified by assembly and py65 simulation over all 29440 inputs.

Review: A is dead after the store (lda wfine follows), and C = 0 from adc #8 with X <= 80. Saves 2 (stza pha/pla) plus 4 (deca), 6 in all. Simulated identical except mtmp, which is macro scratch. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:9  modelb/src/display.s:292-318  (window i0221)  M -0 B -8  [high]  review: accept

Cycles: f = 0 path: lda 3 + beq taken 3 + jsr @addr -> jsr @addr + lda 3 + beq 3, same. f > 0 path: -6 (one lda wfine and the jmp gone).

Assumption: Both paths called @addr with the same X and w16 (nothing between @advance's return and either call changes them); @addr does not read A or flags and wfine is reloaded after it. Supersedes C9 (contains its bra). Composes with C8 (lines 305-307 kept verbatim).

```
        lda wfine
        beq @setq
        ; --- P2: the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2, x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
```
->
```
        jsr @addr                   ; the row below the playfield: P2's start, or Q's
        lda wfine
        beq @sq2
        ; --- P2: the top f lines of that row (A = wfine)
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2, x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
```

[C11] jsr @addr hoisted above the wfine test, so P2 no longer needs its own call nor the lda wfine reload (A = wfine from the beq test), and @setq's jsr @addr goes; P2 then falls into @sq2, dropping the bra (3-byte jmp on the Model B). Verified by assembly and py65 simulation over all 29440 inputs.

Review: @addr gets the same X and w16 on both paths and does not depend on A. A = wfine is still live for @dur on the P2 path. Simulated identical, 8 bytes, -8..0 cycles. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:10  modelb/src/display.s:309-317  (window i0220)  M -0 B -1  [high]  review: accept

Cycles: bra (jmp 3) -> bne taken (3): same. SUPERSEDED by C11 (which removes the branch entirely); apply only if C11 is rejected.

Assumption: A and flags dead at @sq2 (lda #imm first). Stores within the back buffer's SECTAB entry reordered (see ALL).

```
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
```
->
```
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        bne @sq2                    ; always: A = 30
```

[C9] The address copy moved ahead of the VISROWS/30 stores so the last load is lda #30: Z = 0, bne always taken. Same bytes written. Verified by assembly and py65 simulation over all 29440 inputs.

Review: Correct (simulated identical, 1 byte). Also superseded by [9] C11, which removes the branch entirely. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:11  modelb/src/display.s:319-342  (window i0245)  M -0 B -8  [high]  review: accept

Cycles: Q tail 85 -> 72 cycles on both paths into @sq2 (py65: -13..-15 per input).

Assumption: A on return is dead at every caller (engine.s:3133 lda curbuf; init.s:90/92; menu.s:233 stz NEXTSECT): new returns A = QVSYNC instead of >(40*LINE-2). >BARCRTC = 0 (BARADDR = $0300, engine.s:137) -- .asserted. X <= 80 at @sq2 so adc #8 cannot carry.

```
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        rts
```
->
```
@sq2:   txa
        clc
        adc #8
        tax
        lda #<(40*LINE-2)           ; the previous section's T1 and Q's
        sta SECTAB-8+6,x
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB-8+7,x
        sta SECTAB+7,x
        .assert >BARCRTC = 0, error, "Q's R12 and R6 share the zero"
        lda #0                      ; >BARCRTC, and Q's R6
        sta SECTAB,x                ; hands the chain back to the bar
        sta SECTAB+4,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #QVSYNC
        sta SECTAB+5,x
        rts
```

[C10] Both entries' T1 words are the same constant, so each lda #imm feeds two stores: the previous entry's +6/+7 as SECTAB-8+6/7,x after X moves. Q's R12 (>BARCRTC) and R6 are both 0, so one lda #0 feeds both (replaces the Model B stza, 7 bytes). Same bytes land in the same places; only the order differs. Verified by assembly and py65 simulation over all 29440 inputs (only A on return differs).

Review: The same bytes land in the same places, and >BARCRTC = 0 on the Model B (asserted). A on return (QVSYNC instead of >(40*LINE-2)) is dead at every caller, and P is unchanged. Simulated identical except A, 8 bytes, -15..-13 cycles. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:12  modelb/src/display.s:274-435  (window i0209)  M -0 B -4  [high]  review: accept

Cycles: jsr/rts removed: -12 on every path (py65 -12..-14).

Assumption: @nfull is called only from display.s:274 (grep). Its body does not touch the stack.

```
        jsr @nfull                  ; rows that end before the ring end
        cmp tmp4
        bcs @one                    ; the whole run fits
        cmp #0
        beq @one                    ; it starts inside the straddling row: all of it folds
        sta tmp2
        jsr @emit                   ; up to the ring end
        lda tmp2
        jsr @advance
        lda tmp4
        sec
        sbc tmp2
        sta tmp4
@one:   lda tmp4
        sta tmp2
        jsr @emit
        lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2: the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2, x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        rts
@cbl:   .byte <CRTCB_A, <CRTCB_B
@cbh:   .byte >CRTCB_A, >CRTCB_B
@cml:   .byte <(CRTCB_A-RINGCHARS), <(CRTCB_B-RINGCHARS)
@cmh:   .byte >(CRTCB_A-RINGCHARS), >(CRTCB_B-RINGCHARS)
; --- emit a run of tmp2 rows starting at ring offset w16, following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address of the row at ring offset w16.  A row starting
; past RINGCHARS-80 straddles the ring end and is read from the mirror below the base,
; which is exactly the address w16 - RINGCHARS names.
@addr:  lda w16
        ldy w16+1
        cpy #>(RINGCHARS-ROWCHARS+1)
        bcc :++
        bne :+
        cmp #<(RINGCHARS-ROWCHARS+1)
        bcc :++
:       clc
        adc crtcbm
        sta SECTAB+1,x
        tya
        adc crtcbm+1
        sta SECTAB,x
        rts
:       clc
        adc crtcb
        sta SECTAB+1,x
        tya
        adc crtcb+1
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
        ; --- rows of the run that finish before the ring end.  The run starts on ring
        ; row tmp3; when r = ringS mod 80 is non-zero the last ring row straddles and
        ; @addr sends it to the mirror, so RINGROWS-1-tmp3 rows come first.
@nfull: ldy barq
        lda ringS
        sec
        sbc mulrowlo,y              ; r
        cmp #1                      ; C = 1 iff r > 0
        lda #RINGROWS-1
        bcs :+
        adc #0                      ; r == 0: C is clear here, so this adds 1
        adc #1
:       sec
        sbc tmp3
        rts
```
->
```
        ; rows of the run that end before the ring end (was @nfull): the run starts on
        ; ring row tmp3; r = ringS mod 80 non-zero -> the last ring row straddles
        ldy barq
        lda ringS
        sec
        sbc mulrowlo,y              ; r
        cmp #1                      ; C = 1 iff r > 0
        lda #RINGROWS-1
        bcs :+
        adc #0                      ; r == 0: C is clear here, so this adds 1
        adc #1
:       sec
        sbc tmp3
        cmp tmp4
        bcs @one                    ; the whole run fits
        cmp #0
        beq @one                    ; it starts inside the straddling row: all of it folds
        sta tmp2
        jsr @emit                   ; up to the ring end
        lda tmp2
        jsr @advance
        lda tmp4
        sec
        sbc tmp2
        sta tmp4
@one:   lda tmp4
        sta tmp2
        jsr @emit
        lda tmp4
        jsr @advance                ; now the row below the playfield
        lda wfine
        beq @setq
        ; --- P2: the top f lines of that row
        jsr @addr
        lda wfine
        jsr @dur
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        stza SECTAB+2, x
        lda wfine
        deca
        sta SECTAB+3,x
        lda #VISROWS
        sta SECTAB+4,x
        lda #30
        sta SECTAB+5,x
        lda SECTAB-8,x              ; w16 has not moved: the address the P2 @addr left
        sta SECTAB,x                ; in the previous entry is this one's too
        lda SECTAB-8+1,x
        sta SECTAB+1,x
        bra @sq2
@setq:  jsr @addr                   ; Q starts on the row below the playfield
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
        rts
@cbl:   .byte <CRTCB_A, <CRTCB_B
@cbh:   .byte >CRTCB_A, >CRTCB_B
@cml:   .byte <(CRTCB_A-RINGCHARS), <(CRTCB_B-RINGCHARS)
@cmh:   .byte >(CRTCB_A-RINGCHARS), >(CRTCB_B-RINGCHARS)
; --- emit a run of tmp2 rows starting at ring offset w16, following entry X
@emit:  jsr @addr
        lda tmp2
        jsr @lines
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address of the row at ring offset w16.  A row starting
; past RINGCHARS-80 straddles the ring end and is read from the mirror below the base,
; which is exactly the address w16 - RINGCHARS names.
@addr:  lda w16
        ldy w16+1
        cpy #>(RINGCHARS-ROWCHARS+1)
        bcc :++
        bne :+
        cmp #<(RINGCHARS-ROWCHARS+1)
        bcc :++
:       clc
        adc crtcbm
        sta SECTAB+1,x
        tya
        adc crtcbm+1
        sta SECTAB,x
        rts
:       clc
        adc crtcb
        sta SECTAB+1,x
        tya
        adc crtcb+1
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
        ; --- A = rows: advance w16 by that many rows, folding into 0..RINGCHARS
@advance:
        tay
        clc
        lda w16
        adc mulrowlo,y
        sta w16
        lda w16+1
        adc mulrowhi,y
        sta w16+1
@wrap:  lda w16+1
        cmp #>RINGCHARS
        bcc :++
        bne :+
        lda w16
        cmp #<RINGCHARS
        bcc :++
:       lda w16
        sec
        sbc #<RINGCHARS
        sta w16
        lda w16+1
        sbc #>RINGCHARS
        sta w16+1
:       rts
```

[M1] Multi-site: line 274 'jsr @nfull' is replaced by @nfull's body (without label and rts), and lines 421-435 (comment + @nfull) are deleted; lines between are verbatim. Subsumed by ALL. Verified by assembly and py65 simulation over all 29440 inputs.

Review: @nfull's only caller is line 274. The ':' move does not retarget any branch (simulated identical). Saves the jsr/rts: 4 bytes, -12..-14 cycles. Superseded by [0] (ALL): apply only if [0] is not applied.

## r058:13  modelb/src/display.s:215-446  (window i0155)  M -0 B -16  [high]  review: accept

Cycles: Same instructions executed in the same order: 0 (py65 0..-2, page effects).

Assumption: @dur's only callers (215, 297, and 350 via @lines' jmp) each follow it immediately with 'sta SECTAB+6,x / lda tmp3 / sta SECTAB+7,x' (grep @dur/@lines); X is the same entry in the callee.

```
(verbatim lines 215-446 of modelb/src/display.s; the exact text is in build/grind/out/r058.cand.json, candidate [M2])
```
->
```
(lines 215-446 with exactly these edits: delete lines 216-218, 298-300 and 351-353 -- each 'sta SECTAB+6,x / lda tmp3 / sta SECTAB+7,x' after jsr @dur / jsr @lines; replace comment line 436 with '        ; --- A = lines, X = an entry -> its T1lo/T1hi (SECTAB+6/7,x) = the T1 count' + '        ; that lasts that long; A = tmp3 = the high byte'; replace line 446 ':       rts' with ':       sta SECTAB+6,x' / '        lda tmp3' / '        sta SECTAB+7,x' / '        rts'. Full verbatim text in build/grind/out/r058.cand.json)
```

[M2] Multi-site: the store triple moves from the three call sites into @dur's tail ahead of its rts (the ':' target of bcs stays on the first moved instruction). A = tmp3 and flags on return are what the callers had after their own triple. Superseded by ALL, which also moves the X += 8 into @dur. Verified by assembly and py65 simulation over all 29440 inputs.

Review: I rebuilt this from build/grind/out/r058.cand.json: the original matches the file, and the proposal simulates identical. 16 bytes, cycles 0..-2. Superseded by [0] (ALL): apply only if [0] is not applied.

## r059:0  modelb/src/display.s:319-341  (window i0253)  M -0 B -6  [high]  review: accept

Cycles: @sq2..rts (both ways in: from P2's bra and @setq): the two immediate pairs 28->24, stza 14->7; every path 11 cycles faster. Model B only.

Assumption: X = the entry before Q on both entries (same as original); SECTAB is abs in LGCBSS ($B279), so SECTAB+8+6,x and SECTAB-8+7,x are abs,x; nothing between the new early store and Q's stores writes Q's +6/+7. The final A ($09) and N/Z (from lda #>(40*LINE-2)) are exactly what the original returned with.

```
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        lda #>(40*LINE-2)
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        stza SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #<(40*LINE-2)
        sta SECTAB+6, x
        lda #>(40*LINE-2)
        sta SECTAB+7, x
```
->
```
@sq2:   lda #<(40*LINE-2)
        sta SECTAB+6,x
        sta SECTAB+8+6,x            ; Q's own duration is the same 40 lines
        txa
        clc
        adc #8
        tax
        lda #>BARCRTC               ; and hands the chain back to the bar
        sta SECTAB,x
        lda #<BARCRTC
        sta SECTAB+1,x
        lda #QROWS-1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #0                      ; (A is reloaded next: no stza)
        sta SECTAB+4, x
        lda #QVSYNC
        sta SECTAB+5, x
        lda #>(40*LINE-2)
        sta SECTAB-8+7,x            ; the entry before Q
        sta SECTAB+7, x             ; and Q
```

Q's T1 count (+6/+7) is the same constant as the previous entry's, so each immediate is loaded once and stored to both entries: the low byte before the X step (to +6 and +8+6), the high byte after it (to -8+7 = the previous entry's +7, and +7). stza SECTAB+4,x (Model B: pha/lda #0/sta/pla, 7 bytes) becomes lda #0/sta because A is overwritten by lda #QVSYNC next. Starts at line 319 (i0253, just before this region): the lowest window holding it is i0253; the in-region fallback is the stza-only candidate at line 335. Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: The original text matches lines 319-341. Both ways into @sq2 (the bra from P2 at 317 and the fall-through from @setq) have X = the entry before Q. So +6,x and +8+6,x before the step, and -8+7,x and +7,x after it, write exactly the four bytes the original wrote. SECTAB is abs (LGCBSS). The stores go to the back buffer, so their order does not matter. stza becomes lda #0/sta: A is reloaded by lda #QVSYNC straight after, and that load also replaces the flags from pla. The final A and N/Z match. Builds; LGCCODE shrinks by 6 (measured).

## r059:1  modelb/src/display.s:349-395  (window i0277)  M -0 B -11  [high]  review: accept

Cycles: @emit's T1 count: lda+jsr @lines+3 asl+jmp @dur+@dur body+stores ~65 -> 19; the deca 14 -> 5 (lda+sbc); @addr's non-mirror tail 2 faster (no clc). Every path faster.

Assumption: tmp2 <= VISROWS (21) < 128 at every @emit (set at line 279 from A < tmp4 <= VISROWS, or line 287 from tmp4), so asl clears C and tmp2*8 fits a byte as the original's asl x3 needed. @lines has one caller (line 350, grep). LINE = 64 (defs.inc:72), asserted. X+8 never carries (X <= 88: two buffers of 6 x 8-byte entries), so C = 0 after the adc #8. tmp3 is no longer written by @emit: the only reads of tmp3 after an @emit are after a fresh @dur (line 297-299); @nfull (the other reader) runs before any @emit; mirror_copy/render_frame/menu/init callers of build_sections do not read tmp3. mtmp (deca's scratch) is not read by anyone. The second @addr tail (line 384) is entered only from the two bcc :++ (lines 373, 376): C = 0 there, and the first tail ends in rts.

```
        lda tmp2
        jsr @lines
        sta SECTAB+6,x
        lda tmp3
        sta SECTAB+7,x
        txa
        clc
        adc #8
        tax
        lda tmp2
        deca
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address of the row at ring offset w16.  A row starting
; past RINGCHARS-80 straddles the ring end and is read from the mirror below the base,
; which is exactly the address w16 - RINGCHARS names.
@addr:  lda w16
        ldy w16+1
        cpy #>(RINGCHARS-ROWCHARS+1)
        bcc :++
        bne :+
        cmp #<(RINGCHARS-ROWCHARS+1)
        bcc :++
:       clc
        adc crtcbm
        sta SECTAB+1,x
        tya
        adc crtcbm+1
        sta SECTAB,x
        rts
:       clc
        adc crtcb
        sta SECTAB+1,x
        tya
        adc crtcb+1
        sta SECTAB,x
        rts
        ; --- A = rows -> A/tmp3 = that many rows of lines, as a T1 count
@lines: asl
        asl
        asl
        jmp @dur
```
->
```
        lda tmp2                    ; tmp2 rows = tmp2*8*LINE ticks, less 2: $FE and
        asl                         ; 2*tmp2-1 (C = 0: tmp2 <= VISROWS)
        sbc #0
        sta SECTAB+7,x
        lda #<(8*LINE-2)
        sta SECTAB+6,x
        .assert 8*LINE = 512, error, "@emit's T1 count assumes 64-tick lines"
        txa
        clc
        adc #8
        tax
        lda tmp2
        sbc #0                      ; C = 0 out of the adc: tmp2 - 1
        sta SECTAB+2,x
        lda #7
        sta SECTAB+3,x
        lda #30
        sta SECTAB+4,x
        sta SECTAB+5,x
        rts
; --- SECTAB+0/1,x = the CRTC address of the row at ring offset w16.  A row starting
; past RINGCHARS-80 straddles the ring end and is read from the mirror below the base,
; which is exactly the address w16 - RINGCHARS names.
@addr:  lda w16
        ldy w16+1
        cpy #>(RINGCHARS-ROWCHARS+1)
        bcc :++
        bne :+
        cmp #<(RINGCHARS-ROWCHARS+1)
        bcc :++
:       clc
        adc crtcbm
        sta SECTAB+1,x
        tya
        adc crtcbm+1
        sta SECTAB,x
        rts
:       adc crtcb                   ; C = 0: both ways here are a bcc
        sta SECTAB+1,x
        tya
        adc crtcb+1
        sta SECTAB,x
        rts
```

(1) A run of tmp2 rows lasts tmp2*512 T1 ticks; @dur(n*8) returns lo = $FE and tmp3 = 2*tmp2-1 (checked exhaustively for tmp2 0..31), so asl/sbc #0 (C=0) gives the high byte directly and the low byte is the constant <(8*LINE-2) = $FE; the stores go to the same +6/+7. @lines then has no caller and is deleted (6 bytes). (2) deca (Model B: sta mtmp/dec mtmp/lda mtmp, 6 bytes) -> sbc #0 with C = 0 from the adc #8: A = tmp2-1; N/Z are overwritten by the lda #7 before any use. (3) the clc at the non-mirror @addr tail is dead (C = 0 on both ways in). Anonymous labels at 377 and 384 are kept. Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: Matches lines 349-395. tmp2 <= VISROWS=21 at both @emit calls, so asl clears C and asl/sbc #0 = 2*tmp2-1. That equals the tmp3 from the old @lines/@dur path (A=$FE, then dec tmp3), including tmp2=0. The 8*LINE=512 assumption is asserted. deca -> sbc #0: C=0 out of adc #8 (X<=88). The changed C out is never used: both @emit callers go on to @advance, which does clc. @emit no longer writes tmp3, but nothing reads tmp3 before it is written again (the P2 @dur, build_sections' own writes, and the render_frame/menu/init callers). @lines has no other reference. The second @addr tail is reached only by the two bcc :++ (C=0), and line 383 is rts. The anonymous labels at 377 and 384 stay. LGCCODE shrinks by 11 (measured).

## r059:2  modelb/src/display.s:406-406  (window i0327)  M -0 B -2  [high]  review: accept

Cycles: both entries 3 cycles faster

Assumption: @wrap has two ways in: fall-through from @advance (line 405 sta w16+1 immediately before) and jsr @wrap at line 254 (preceded by lda ringS+1/adc #0/sta w16+1 at 251-253). Both leave A = w16+1; cmp # comes next so flags on entry are irrelevant.

```
@wrap:  lda w16+1
```
->
```
@wrap:                              ; A = w16+1: both ways in have just stored it
```

lda w16+1 at @wrap reloads what A already holds on both entries; the label moves onto the cmp. Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: Both ways into @wrap leave A = w16+1: the fall-through after sta w16+1 at 405, and jsr @wrap at 254 after sta w16+1 at 253. cmp comes next, so the flags on entry do not matter. No other reference to @wrap. Saves 2 (measured).

## r059:3  modelb/src/display.s:413-414  (window i0333)  M -0 B -1  [high]  review: accept

Cycles: wrap path 2 cycles faster; others unchanged

Assumption: The ':' at line 413 is entered by bne :+ (line 409, after cmp #>RINGCHARS with bcc not taken: C = 1) and by fall-through from bcc :++ at 412 not taken (C = 1); no other reference counts to it.

```
:       lda w16
        sec
```
->
```
:       lda w16                     ; C = 1: both ways here
```

sec before the 16-bit subtract is redundant: C = 1 on both ways in; lda w16 does not touch C. Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: The ':' at 413 has two ways in, both with C=1: bne :+ at 409 (after cmp, with bcc not taken), and the fall-through when 412's bcc :++ is not taken. The :++ branches at 408 and 412 go to 420. lda does not change C. Saves 1 (measured).

## r059:4  modelb/src/display.s:424-435  (window i0340)  M -0 B -6  [high]  review: accept

Cycles: r>0: 24 -> 19; r=0: 27 -> 19

Assumption: Only caller: line 274, which follows with cmp tmp4 (only A consumed, C/N/Z/V overwritten). Y = barq on exit as before. RINGROWS = 23 on the Model B (engine.s:99).

```
@nfull: ldy barq
        lda ringS
        sec
        sbc mulrowlo,y              ; r
        cmp #1                      ; C = 1 iff r > 0
        lda #RINGROWS-1
        bcs :+
        adc #0                      ; r == 0: C is clear here, so this adds 1
        adc #1
:       sec
        sbc tmp3
        rts
```
->
```
@nfull: ldy barq
        lda ringS
        eor mulrowlo,y              ; 0 iff r = 0
        cmp #1                      ; C = 1 iff r > 0
        lda tmp3
        adc #$FF-RINGROWS           ; tmp3 - RINGROWS - 1 + C
        eor #$FF                    ; RINGROWS-1-tmp3, and one more when r = 0
        rts
```

r = 0 iff ringS_lo == mulrowlo[barq] iff their eor is 0, so eor replaces sec/sbc (C from cmp #1 is identical). Result: original A = RINGROWS-1-tmp3 when C, RINGROWS-tmp3 when not C. New: (tmp3 + ($FF-RINGROWS) + C) eor $FF = -(tmp3 - RINGROWS - 1 + C) - 1 = RINGROWS - tmp3 - C: same; checked exhaustively for all tmp3 and C. (The original adc #0 with C = 0 was a no-op.) Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: eor gives 0 exactly when the low bytes are equal, which is when the old sbc difference was 0, so cmp #1 sets the same C. (tmp3 + $FF-23 + C) eor $FF = 23 - tmp3 - C. That matches RINGROWS-1-tmp3 when C=1 and RINGROWS-tmp3 when C=0 (the old adc #0 did nothing). The only caller (274) uses just A, through cmp tmp4. Y = barq is kept. The ':' at 433 goes together with its only reference (bcs at 430), and no other branch counts across it: 408/412 stop at 420, 444 targets 446, and there is no :- after it. Saves 6 (measured).

## r059:5  modelb/src/display.s:437-442  (window i0351)  M -0 B -1  [high]  review: accept

Cycles: 19 -> 16 cycles for the shift part; rest unchanged

Assumption: @dur's callers (lines 215, 297; @lines at 395 if still present) pass n in A and read A/tmp3 only.

```
@dur:   sta tmp3                    ; n*64 == (n*256)>>2
        lda #0
        lsr tmp3
        ror
        lsr tmp3
        ror
```
->
```
@dur:   lsr                         ; n*64 == (n*256)>>2
        sta tmp3
        lda #0
        ror
        lsr tmp3
        ror
```

Original: tmp3=n, A=0, lsr tmp3/ror twice -> tmp3=n>>2, A=(n1<<7)|(n0<<6), C=0. New: lsr (A=n>>1, C=n0), sta tmp3, lda #0, ror (A=n0<<7, C=0), lsr tmp3 (tmp3=n>>2, C=n1), ror (A=n1<<7|n0<<6, C=0): identical A, tmp3, C; checked exhaustively for all 256 n. Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: Traced for every n: the new sequence leaves tmp3 = n>>2, A = n1<<7|n0<<6, C = 0 and the same N/Z as the old one. The callers at 215 and 297 (and @lines, if kept) pass n in A. 19 -> 16 cycles. Saves 1 (measured).

## r059:6  modelb/src/display.s:460-465  (window i0364)  M -0 B -2  [high]  review: accept

Cycles: reset path 2 cycles faster; other path unchanged

Assumption: mirdty is only ever tested for zero: its readers are display.s:466 (lda/bne); writers are banks.s:130 (#1), display.s:461/473. The value falls straight into lda mirdty,x/bne :+, which still takes.

```
        lda #1
        sta mirdty,x
        lda #0
        sta mirlo,x
        lda #ROWCHARS-1
        sta mirhi,x
```
->
```
        lda #0
        sta mirlo,x
        lda #ROWCHARS-1
        sta mirhi,x
        sta mirdty,x                ; non-zero: dirty
```

mirdty needs to be non-zero, not 1: storing the ROWCHARS-1 (79) already in A for mirhi saves the lda #1. Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: mirdty is only ever tested for zero (display.s:466, lda/bne). Its other writers are banks.s:130 (#1) and display.s:473 (#0), and nothing compares it with 1. 79 is non-zero, so the bne at 467 still takes. Saves 2 (measured).

## r059:7  modelb/src/display.s:472-489  (window i0373)  M -0 B -5  [high]  review: accept

Cycles: paths reaching it 6 cycles faster (ldy abs,x 4 + tya 2 vs lda abs,x 4 + sta zp 3 + lda #0 2 + lda zp 3)

Assumption: Y is free in mirror_copy up to ldy #0 at line 522; on the early rts at 491 Y now returns mirhi: mirror_copy is render_core's tail (engine.s:3354), render_frame (engine.s:3113) then does stza NSPR and jsr build_sections, which writes Y before reading it. tmp3 is not read after mirror_copy (build_sections writes it first: @coarse sta tmp3 / @dur). mirlo/mirhi/mirdty are LOWBSS abs (ldy abs,x exists on the 6502); only bank-5 blitters (main-line, not IRQ) write them.

```
:       lda #0
        sta mirdty,x
        lda mirlo,x                 ; the copy starts at the first written char, or at
        cmp wcxm                    ; wcxm if the writing started left of it
        bcs :+
        lda wcxm
:       sta tmp4                    ; and runs to the last written one
        lda mirhi,x
        sta tmp3
        lda #$FF                    ; the written range is empty again
        sta mirlo,x
        lda #0
        sta mirhi,x
        lda wcxm                    ; the chars left of wcxm are not copied
        sta mirwcx,x
        lda tmp3                    ; the chars from the first written to the last
        sec
        sbc tmp4
```
->
```
:       lda #0
        sta mirdty,x
        ldy mirhi,x                 ; the last written char
        sta mirhi,x                 ; the written range is empty again (mirlo below)
        lda mirlo,x                 ; the copy starts at the first written char, or at
        cmp wcxm                    ; wcxm if the writing started left of it
        bcs :+
        lda wcxm
:       sta tmp4                    ; and runs to the last written one
        lda #$FF
        sta mirlo,x
        lda wcxm                    ; the chars left of wcxm are not copied
        sta mirwcx,x
        tya                         ; the chars from the first written to the last
        sec
        sbc tmp4
```

mirhi is read into Y (instead of via tmp3) and zeroed at once with the A = 0 already there for mirdty (drops lda #0); tya replaces lda tmp3. The order of the mirlo/mirhi/mirwcx stores relative to each other does not matter (no interrupt reads them). Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: The anonymous label count does not change (472 and the one on sta tmp4), and bcs :+ still targets sta tmp4. mirhi is read into Y before it is zeroed, and the A=0 from the mirdty store is reused. Y is free up to ldy #0. On the early rts at 491, Y now returns mirhi. That is harmless: render_core's caller render_frame (engine.s:3113) goes on to stza NSPR and build_sections, which writes Y before reading it. build_sections also writes tmp3 before any read. This runs on the main line only: the bank-5 blitters run before it, not in an interrupt. Saves 5 (measured).

## r059:8  modelb/src/display.s:492-493  (window i0383)  M -0 B -1  [high]  review: accept

Cycles: 2 cycles faster

Assumption: The ':' at 492 is reached only by bcs :+ at 490 (C = 1); line 491 is rts.

```
:       clc
        adc #1
```
->
```
:       adc #0                      ; C = 1 from the bcs: +1
```

clc/adc #1 with known C = 1 -> adc #0 adds 1 the same; C out same (count <= 80, no carry). Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: The ':' at 492 is reached only by bcs :+ at 490 (C=1). Line 491 is rts, and no later :- can target it (533 comes first). adc #0 with C=1 adds 1, with the same C out (A<=79). Saves 1 (measured).

## r059:9  modelb/src/display.s:497-522  (window i0388)  M -0 B -11  [high]  review: accept

Cycles: shift/add 51 -> 34, sec removed -2, ldy #0 removed -2; single path

Assumption: C = 0 after line 513 (w16+1 <= $85, see above). Y is not touched between line 497 and the loop (adc/sta/lda/sbc only). tmp4*8 high part fits (tmp4 8-bit).

```
        lda tmp4
        sta w16
        lda #0
        sta w16+1
        asl w16
        rol w16+1
        asl w16
        rol w16+1
        asl w16
        rol w16+1
        lda w16
        clc
        adc #<(RING_A + (RINGROWS-1)*ROWBYTES)
        sta w16
        lda w16+1
        adc ringbhi
        adc #>((RINGROWS-1)*ROWBYTES)
        sta w16+1
        lda w16
        sec
        sbc #<RINGBYTES
        sta w16b
        lda w16+1
        sbc #>RINGBYTES
        sta w16b+1
        ldy #0
```
->
```
        ldy #0                      ; Y = 0 is the copy's index too
        sty w16+1
        lda tmp4
        asl
        rol w16+1
        asl
        rol w16+1
        asl
        rol w16+1                   ; C = 0: w16+1 was < 4
        adc #<(RING_A + (RINGROWS-1)*ROWBYTES)
        sta w16
        lda w16+1
        adc ringbhi
        adc #>((RINGROWS-1)*ROWBYTES)
        sta w16+1
        lda w16                     ; C = 0 from the adc above (no carry out of the high byte)
        sbc #<(RINGBYTES-1)         ; so this subtracts <RINGBYTES, borrow as before
        sta w16b
        lda w16+1
        sbc #>RINGBYTES
        sta w16b+1
```

Combined: (a) tmp4*8 shifted in A (asl a) with rol w16+1, dropping sta w16/lda w16 and the clc (C = 0 out of the third rol since w16+1 < 4 before it); (b) w16+1 zeroed with ldy #0/sty (same size/cycles as lda #0/sta), so Y = 0 reaches the copy loop and its ldy #0 (line 522) goes; (c) the sec before the low-byte subtract is dead with C = 0 known, compensated by subtracting <RINGBYTES-1. Spans i0394..i0419, so its lowest window is i0388 (region r060's); in-region fallbacks are 497-510 and 515-517. Verified in a scratch tree: Model B builds, bpixdiff+SECTAB/mirror-state compare identical over 300 frames on L0,1,3,6,9,12, bdiff lock step OK L0,5,12 (with the whole set applied).

Review: The third rol sets C to the old bit 7 of w16+1, which held tmp4>>6 <= 1, so C=0 and the clc can go. The high byte is at most 2 + 1 + ringbhi ($0A or $46, for RING_A/RING_B) + $37 = $80, so there is no carry. C is therefore 0 into the sbc, and sbc #<(RINGBYTES-1)=$7F equals the old sec/sbc #$80 with the same borrow (<RINGBYTES=$80, not 0). Y=0 from the top reaches @c unchanged: only sty/lda/asl/rol/adc/sta/sbc run in between. @c is entered only from above and by bne @c. Saves 11 (measured).

## r059:10  modelb/src/display.s:335-335  (window i0256)  M -0 B -2  [high]  review: accept

Cycles: 14->7 cycles; Model B only

Assumption: A dead: the next instruction is lda #QVSYNC (line 336). Flags: lda #QVSYNC resets N/Z; stza preserved C and so does lda #0/sta.

```
        stza SECTAB+4, x
```
->
```
        lda #0                      ; (A is reloaded next: no stza)
        sta SECTAB+4, x
```

FALLBACK, subsumed by the 319-341 candidate: Model B stza = pha/lda #0/sta/pla (7 bytes); A is reloaded straight after, so the pha/pla are dead.

Review: Correct on its own: A is reloaded by lda #QVSYNC next, which also replaces the flags from pla. It is subsumed by candidate 0, so apply it only if 0 is not applied. Saves 2 (measured).

## r059:11  modelb/src/display.s:358-359  (window i0284)  M -0 B -4  [high]  review: accept

Cycles: 14 -> 5 cycles

Assumption: C = 0 after txa/clc/adc #8/tax (X <= 88, no carry); mtmp not read afterwards; N/Z overwritten by lda #7 (line 361).

```
        lda tmp2
        deca
```
->
```
        lda tmp2
        sbc #0                      ; C = 0 out of the adc: tmp2 - 1
```

FALLBACK, subsumed by the 349-395 candidate: deca (sta mtmp/dec mtmp/lda mtmp) -> sbc #0 with borrow-in 1 = tmp2-1.

Review: Correct on its own: C=0 out of adc #8 (X<=88), so sbc #0 gives tmp2-1. The C out is never used (@advance does clc), and mtmp is only macro scratch. Subsumed by candidate 1. Saves 4 (measured).

## r059:12  modelb/src/display.s:384-385  (window i0296)  M -0 B -1  [high]  review: accept

Cycles: non-mirror @addr path 2 cycles faster; mirror path unchanged

Assumption: The ':' at line 384 is reached only by bcc :++ at 373 and 376 (C = 0); line 383 is rts.

```
:       clc
        adc crtcb
```
->
```
:       adc crtcb                   ; C = 0: both ways here are a bcc
```

FALLBACK, subsumed by the 349-395 candidate: clc before adc crtcb is redundant; the ':' label stays on the adc.

Review: Correct on its own: 384 is reached only by the two bcc :++ (C=0), and 383 is rts. Subsumed by candidate 1. Saves 1 (measured).

## r059:13  modelb/src/display.s:497-510  (window i0383)  M -0 B -8  [high]  review: accept

Cycles: 51 -> 34 cycles

Assumption: none beyond the window

```
        lda tmp4
        sta w16
        lda #0
        sta w16+1
        asl w16
        rol w16+1
        asl w16
        rol w16+1
        asl w16
        rol w16+1
        lda w16
        clc
        adc #<(RING_A + (RINGROWS-1)*ROWBYTES)
        sta w16
```
->
```
        lda #0
        sta w16+1
        lda tmp4
        asl
        rol w16+1
        asl
        rol w16+1
        asl
        rol w16+1                   ; C = 0: w16+1 was < 4
        adc #<(RING_A + (RINGROWS-1)*ROWBYTES)
        sta w16
```

FALLBACK, subsumed by the 497-522 candidate: build tmp4*8 with A as the low byte (asl a) instead of asl w16 in memory, which also removes sta w16/lda w16; after the third rol w16+1, C = old bit 7 of w16+1 = 0 (it held tmp4>>6 <= 3), so the clc goes too.

Review: Correct on its own: before the third rol, w16+1 held tmp4>>6 <= 1, so that rol leaves C=0 and the clc can go. w16 gets the same sum. Subsumed by candidate 9. Saves 8 (measured).

## r059:14  modelb/src/display.s:515-517  (window i0383)  M -0 B -1  [high]  review: accept

Cycles: 2 cycles faster

Assumption: C = 0 after line 513: w16+1 = (tmp4>>5 <= 7) + ringbhi ($0A or $46) + $37 + carry <= $85, no carry out.

```
        lda w16
        sec
        sbc #<RINGBYTES
```
->
```
        lda w16                     ; C = 0 from the adc above (no carry out of the high byte)
        sbc #<(RINGBYTES-1)         ; so this subtracts <RINGBYTES, borrow as before
```

FALLBACK, subsumed by the 497-522 candidate: with C = 0, sbc #<RINGBYTES-1 = A - $7F - 1 = A - $80 with the same carry out as sec/sbc #$80.

Review: Correct on its own: C=0 after the adc at 513 (the high byte is at most $80, so no carry). <RINGBYTES=$80 is not 0, so sbc #$7F with C=0 equals sec/sbc #$80 with the same borrow. Subsumed by candidate 9. Saves 1 (measured).

## r060:0  modelb/src/display.s:492-493  (window i0384)  M -0 B -1  [high]  review: accept

Cycles: continue path 4->2 (clc 2 + adc# 2 -> adc# 2); exit path (bcs not taken, rts) unchanged; Master: not assembled

Assumption: C = 1 on entry to the ':' line. Its only way in is the bcs :+ at line 490: the line before it is rts, so nothing falls through, and no :- from below reaches it before the next ':' at line 533. C after the adc is not read: sta tmp follows, then lda/lsr (proposal i0387) or asl (original) sets C.

```
:       clc
        adc #1
```
->
```
:       adc #0                      ; C = 1 from the bcs: +1
```

bcs taken => C=1. clc/adc #1 gives A+1, with C out = the carry of A+1. adc #0 with C=1 gives the same A+1, the same C out and the same N/Z/V. A = tmp3-tmp4 >= 0.

Review: Lines 492-493 match the file. Only the bcs :+ at line 490 reaches the ':' at 492. The line before it is rts, and no :- between 492 and the next ':' at 533 reaches it, so C=1 on entry. adc #0 with C=1 gives the same A, N, Z, V and carry out as clc/adc #1. Model B only (modelb/src). Bytes 3->2, cycles 4->2.

## r060:1  modelb/src/display.s:497-521  (window i0387)  M -0 B -18  [high]  review: accept

Cycles: single path 80->48 (Model B only). Original: 3+3+2+3+30+3+2+2+3+3+3+2+3+3+2+2+3+3+2+3 = 80; new: 3+10+2+3+3+2+3+2+3+3+6+3+2+3 = 48

Assumption: (1) ringe3 = ringbhi + $37 always. select_backbuf (src/engine.s:3031-3039) is the only writer of either, and it sets both from @bhi/@ehi for the same buffer (ringe3 = >RINGEND-3 = >RING+$3A-3). The proposal asserts this. (2) X is free: it is last read at line 486 (sta mirwcx,x) and next written by ldx #8 at @c (line 523). (3) A and the flags are dead at the end: ldy #0 follows, the loop reloads A, and the loop never reads C. (4) ringbhi is $0A or $46 (engine.s:3046 @bhi). So Thi+c+ringe3 <= 7+1+$7D < $100 and Thi+ringbhi < $100, and both adcs leave C=0 for ANY tmp4 0..255. (5) <RING_A = <RING_B = $80. The original already assumed this by adding #<(RING_A+...) for both buffers; the proposal asserts it. An exhaustive simulation of all tmp4 0..255 on both rings gives the same w16 and w16b as the original (0 mismatches). A scratch-copy build passes the asserts and LGCCODE shrinks by the expected amount.

```
        lda tmp4
        sta w16
        lda #0
        sta w16+1
        asl w16
        rol w16+1
        asl w16
        rol w16+1
        asl w16
        rol w16+1
        lda w16
        clc
        adc #<(RING_A + (RINGROWS-1)*ROWBYTES)
        sta w16
        lda w16+1
        adc ringbhi
        adc #>((RINGROWS-1)*ROWBYTES)
        sta w16+1
        lda w16
        sec
        sbc #<RINGBYTES
        sta w16b
        lda w16+1
        sbc #>RINGBYTES
        sta w16b+1
```
->
```
        .assert (RINGEND_A >> 8) - 3 = (RING_A >> 8) + (((RINGROWS-1)*ROWBYTES) >> 8) && (RINGEND_B >> 8) - 3 = (RING_B >> 8) + (((RINGROWS-1)*ROWBYTES) >> 8), error, "ringe3 is the last slot's page less the base's"
        .assert <(RING_A + (RINGROWS-1)*ROWBYTES) = $80 && <RING_A = <RING_B, error, "the last slot is at xx80"
        .assert (RING_A & $FF) + (RINGROWS-1)*ROWBYTES - RINGBYTES = -$200, error, "the mirror is 2 pages below the base page"
        lda tmp4                    ; T = tmp4*8: A = its high byte, C = bit 7 of its low
        lsr                         ; byte -- the carry out of the +$80 below
        lsr
        lsr
        lsr
        lsr
        tax
        adc ringe3                  ; ringbhi + >((RINGROWS-1)*ROWBYTES); C = 0 after
        sta w16+1
        txa
        adc ringbhi                 ; C = 0 in and out
        sbc #1                      ; C = 0: -2, the mirror's page
        sta w16b+1
        lda tmp4
        asl
        asl
        asl
        sta w16b
        eor #<(RING_A + (RINGROWS-1)*ROWBYTES)   ; +$80, its carry taken above
        sta w16
```

T = tmp4*8. Thi = tmp4>>5 (5 lsr), and the 5th lsr leaves C = tmp4 bit 4 = bit 7 of Tlo = the carry c of Tlo+$80. Original w16 = T + $80 + 256*(ringbhi+$37). Its lo = Tlo+$80 = Tlo EOR $80; its hi = Thi + ringbhi + $37 + c = Thi + ringe3 + c, which is adc ringe3 with C=c. Original w16b = w16 - RINGBYTES = T + 256*ringbhi + $80 + $3700 - $3980 = T + 256*(ringbhi-2). Its lo = Tlo; its hi = Thi + ringbhi - 2, which is txa, then adc ringbhi (C=0 from the previous adc), then sbc #1 (C=0, so it subtracts 2). Tlo = tmp4 shifted left 3 times (asl x3). tmp4 and tmp3 are unchanged, as before. The three .asserts cost no bytes and fix the constants' values; they pass in the scratch build.

Review: Lines 497-521 match the file exactly. Model B constants: RINGROWS=23, (RINGROWS-1)*ROWBYTES=$3700, RINGBYTES=$3980, RING_A/B=$0A80/$4680, RINGEND_A/B=$4400/$8000. All three asserts hold. Only select_backbuf (engine.s:3031-3039) writes ringbhi and ringe3 ($E0/$E2 zp), and it sets both from the same curbuf. render_core does farjsr F_SELBB before jmp mirror_copy, so ringe3 = ringbhi+$37 at run time. X is dead: the next write is ldx #8 at @c. Neither adc can carry (maximums $85 and $4D). An exhaustive simulation of tmp4 0..255 on both rings found 0 mismatches in w16 and w16b. All operands are zp: bytes 48->30, cycles 80->48.

## r060:2  modelb/src/display.s:529-529  (window i0395)  M -0 B -1  [high]  review: accept

Cycles: cpy #0 2 -> tya 2 on every iteration; same

Assumption: A is dead here and at the rts. Inside the loop, @b reloads A (lda (w16),y) before any use. After the final rts, mirror_copy returns through render_core's jmp (engine.s:3354) to render_frame at engine.s:3113. The next instruction there, stza NSPR, saves and restores A around its store, and jsr build_sections then loads A before reading it (display.s:178). grep finds no other caller of mirror_copy.

```
        cpy #0
```
->
```
        tya                         ; Z: Y = 0 (A is dead: reloaded at @b, and after the rts)
```

cpy #0 sets Z = (Y==0); its N and C are not read (bne, then dec tmp/bne/ldx). tya sets Z = (Y==0) and N, and leaves C alone. C is never read in the loop or after it (the build_sections path reloads). Only A changes, and A is dead.

Review: Line 529 matches the file. tya sets Z=(Y==0), as cpy #0 does. Nothing in the loop reads the N or C that cpy set. A is reloaded at @b. After the rts, control goes back through render_core, whose last step is jmp mirror_copy at engine.s:3354; render_core's only call is at engine.s:3113. Next comes stza NSPR (pha/lda/sta/pla), then build_sections, which loads A and X and runs asl before anything reads C. The early rts exits of mirror_copy already return with varying A and C. Bytes 2->1, cycles 2->2.

## r060:3  modelb/src/display.s:539-565  (window i0426)  M -0 B -26  [medium]  review: accept

Cycles: Whole routine 186 -> 175 cycles: 8 x (lda abs,x 4 + sta 4 + lda abs,x 4 + sta 4 + dex 2 + bpl 3) - 1 + ldx 2 + rts 6, ignoring page crossings of the two 8-byte tables. Each write is still a store to CRTC_IDX/CRTC_DAT, in the same order. Not timing-critical: it runs once at init (init.s:86), before the interrupt takeover (F_INIT5), and its comment says vsync re-phases the chain afterwards.

Assumption: grep finds only one caller, init.s:86. It follows with jsr build_sections, which begins ldx curbuf / lda @cbl,x. So X (now $FF instead of 13) and the flags (from dex instead of lda #<BARCRTC) are dead. A ends = <BARCRTC as before, and Y is untouched as before. The write order R8,R10,R9,R4,R6,R7,R12,R13 is kept (the tables are read from index 7 down). The rewrite adds 16 bytes of new .byte data inside the routine; no existing table, .res or constant changes. Flag it if the grind excludes new tables. @w/@reg/@val are cheap labels local to crtc_init, and nothing outside uses @w. A scratch-copy build shrinks LGCLO from $9A to $80.

```
crtc_init:                          ; start the chain at the bar and let vsync re-phase
        ldx #8                      ; no interlace: the MOS's MODE 1 leaves interlace sync
        lda #0                      ; on, which puts every other field's vsync half a
        jsr @w                      ; scanline later and the whole chain with it (the
        ldx #10                     ; Master's crtc_init writes 0 too)
        lda #$20                    ; the MOS's cursor off
        jsr @w
        ldx #9
        lda #7
        jsr @w
        ldx #4
        lda #BARROWS-1
        jsr @w
        ldx #6
        lda #BARROWS
        jsr @w
        ldx #7
        lda #30
        jsr @w
        ldx #12
        lda #>BARCRTC
        jsr @w
        ldx #13
        lda #<BARCRTC
@w:     stx CRTC_IDX
        sta CRTC_DAT
        rts
```
->
```
crtc_init:                          ; start the chain at the bar and let vsync re-phase
        ldx #7                      ; written from the end of the tables: R8 = 0 first (no
@w:     lda @reg,x                  ; interlace: the MOS's MODE 1 leaves interlace sync on,
        sta CRTC_IDX                ; which puts every other field's vsync half a scanline
        lda @val,x                  ; later; the Master's crtc_init writes 0 too), R10 = $20
        sta CRTC_DAT                ; (the MOS's cursor off), ... R12/R13 last
        dex
        bpl @w
        rts
@reg:   .byte 13, 12, 7, 6, 4, 9, 10, 8
@val:   .byte <BARCRTC, >BARCRTC, 30, BARROWS, BARROWS-1, 7, $20, 0
```

Original: 8 register writes (ldx #reg, lda #val, stx CRTC_IDX, sta CRTC_DAT) through jsr @w. New: a dex/bpl loop over two 8-byte tables. They hold the same (reg,val) pairs in reverse order, so the writes happen in the original order. Data: R8=0, R10=$20, R9=7, R4=BARROWS-1, R6=BARROWS, R7=30, R12=>BARCRTC, R13=<BARCRTC.

Review: Lines 539-565 match the file. The only caller is init.s:86; src/main.s calls the Master's own crtc_init in engine.s. It is followed by jsr build_sections, which begins ldx curbuf / lda @cbl,x, so X (now $FF), A and the flags are dead. Y is untouched. Reading the tables from index 7 down gives the same order, R8,R10,R9,R4,R6,R7,R12,R13, with the same values, so R12/R13 are still written last. LGCLO is a plain ro segment in B7A with no bank sites or asserted sizes inside. The tables sit in the same segment, so they are mapped whenever the code is. The routine runs once at init and is not timing-critical. Cycles 186->175, bytes 60->34. It adds 16 bytes of new .byte data in place of code and changes no existing table.

## r061:0  modelb/src/disc.s:95-124  (window i0042)  M -0 B -12  [high]  review: accept

Cycles: Model B only. Division: the original costs 6 + 35 per iteration * q + 27 (q = ld_sec div 10); the new code costs 12 + 9q + 4H + 12 (H = ld_sec high byte, H<=q/25). LD_DONE clear 6->4. The whole window path for q=0 goes 39->38, and every q is faster (q=1: 74->47). Re-entries at @track (retry, next track): 6->4.

Assumption: (1) Nothing reads ld_sec after read_sectors computes the track (grep: only disc.s:97-108 read it; ldprog.s:90-92 and disc.s:276-278 only write it), so leaving ld_sec unmodified is fine. (2) ld_sec < 2560 (the disc is 800 sectors), so the quotient is < 256 and high byte+1 does not wrap. (3) X after @drem is dead: the 8271 path does ldx dsk_drv (line 136) and the 1770 path always reaches ldx #20 at @wrd (line 185) or w_wait's ldx, so X at return is unchanged. (4) Y at return changes from (rem-10)&$FF to 0. Callers ignore Y: ldprog.s readfile callers go to section (tay), bcopy (ldy #0) or rts; load_level_b does ldx; title_load/bcopy set Y. (5) Y=0 holds from here to every later sty LD_DONE: nothing in read_sectors, i_idle, i_param, wait_done, w_wait or the NMI stubs (pha/pla A only) writes Y, and interrupts are off (ld_begin sei). (6) The D flag is clear, which the original's sbc also assumes.

```
        lda #0
        sta ld_trk
@d10:   lda ld_sec
        sec
        sbc #10
        tay
        lda ld_sec+1
        sbc #0
        bcc @drem
        sty ld_sec
        sta ld_sec+1
        inc ld_trk
        bne @d10
@drem:  lda ld_sec
        sta ld_sc
@track: lda #10                     ; sectors to read on this track: min(n, 10 - s)
        sec
        sbc ld_sc
        cmp ld_n
        bcc :+
        lda ld_n
:       sta ld_cnt
        lda ld_dst                  ; the transfer address, into the stub
        sta NMI_I_STA+1
        sta NMI_W_STA+1
        lda ld_dst+1
        sta NMI_I_STA+2
        sta NMI_W_STA+2
        lda #0
        sta LD_DONE
```
->
```
        ldx #$FF                    ; X = track, Y = high byte + 1: ld_sec / 10
        lda ld_sec
        ldy ld_sec+1
        iny
@d10:   inx
        sec
        sbc #10
        bcs @d10
        dey
        bne @d10
        adc #10                     ; (C clear) the remainder; Y = 0 from here on
        stx ld_trk
        sta ld_sc
@track: lda #10                     ; sectors to read on this track: min(n, 10 - s)
        sec
        sbc ld_sc
        cmp ld_n
        bcc :+
        lda ld_n
:       sta ld_cnt
        lda ld_dst                  ; the transfer address, into the stub
        sta NMI_I_STA+1
        sta NMI_W_STA+1
        lda ld_dst+1
        sta NMI_I_STA+2
        sta NMI_W_STA+2
        sty LD_DONE                 ; (Y = 0 throughout read_sectors)
```

This is standard 16-bit repeated subtraction with the high byte in Y (+1 so the loop exits at Y=0). X counts subtractions from -1, so after the final borrowing subtraction X = quotient. C=0 at exit, so adc #10 restores the remainder (checked exhaustively in Python for 0..2559: X=v/10, A=v%10, Y=0). stx ld_trk / sta ld_sc store what the original's ld_trk counter and @drem stored. @track's code is unchanged except lda #0/sta LD_DONE -> sty LD_DONE with Y=0; the next instruction, lda dsk_type, reloads A and its flags feed bne @wd. The :+ at line 116 is kept verbatim.

Review: Original matches disc.s:95-124. Division checked for every value 0..2559: X=q, A=r, Y=0, and C=0 before the adc. ld_sec is only written by callers (ldprog.s:90-92, disc.s:276-278) and only read here, so leaving it unreduced is safe. X at return is unchanged (the 8271 path does ldx dsk_drv; the 1770 path always reaches ldx #20 at @wrd). Y at return changes (it was rem-10), but no return path reads Y: ld_begin -> load_level_b/load_title_b, and ldprog readfile -> section (tay), bcopy (ldy) or rts. Y stays 0 through the @track retries because i_idle, i_param, wait_done, w_wait and the NMI stubs never write Y. Nothing else references @drem. Cycles: for q=0 the division is 34 cycles against 33, but that path always goes on to the LD_DONE clear (6->4), so the window is 39->38. Every q>=1 is much faster, and @track re-entries go 6->4. This is not a sector loop or NMI handler, and the 1770 delay loop and 8271 handshakes are unchanged. Built in a scratch copy: with all four read_sectors/wait_done candidates LGCCODE goes 0x2A0A -> 0x29FA, 12 of those bytes from this one.

## r061:1  modelb/src/disc.s:153-154  (window i0064)  M -0 B -2  [high]  review: accept

Cycles: 6->4 (Model B only)

Assumption: REQUIRES the i0042 candidate (Y = 0 throughout read_sectors). Nothing in between touches Y: i_idle/i_param (disc.s:210-220) do not use it. A is dead: the next instruction, txa (line 155), reloads A and sets the flags.

```
        lda #0                      ; (and the stub's flag, should a controller
        sta LD_DONE                 ; interrupt after all)
```
->
```
        sty LD_DONE                 ; (and the stub's flag, should a controller interrupt after all; Y = 0)
```

Stores 0 to LD_DONE as before; txa overwrites A and the flags immediately.

Review: Requires index 0. Y=0 holds at line 153: only i_idle and i_param run in between, and neither touches Y. The next instruction, txa, reloads A and the flags. The gap between the status read and the read-data command write gets 2 cycles shorter. The 8271 is handshaked (i_idle checked busy before the result read), so nothing depends on that timing. The 2-byte saving is confirmed in the scratch build.

## r061:2  modelb/src/disc.s:203-204  (window i0110)  M -0 B -2  [high]  review: accept

Cycles: 6->4 (Model B only)

Assumption: REQUIRES the i0042 candidate (Y = 0 throughout read_sectors). wait_done, w_wait, i_idle, i_param and the NMI stubs do not touch Y. A is dead: inc ld_trk / jmp @track follow, and @track starts with lda #10.

```
        lda #0
        sta ld_sc
```
->
```
        sty ld_sc                   ; (Y = 0)
```

ld_sc = 0 as before. Nothing reads the A value or flags: inc ld_trk resets the flags and @track reloads A.

Review: Requires index 0. Y=0 on the 8271 path (after wait_done) and on the 1770 path (w_wait uses X only). A and the flags are dead: inc ld_trk, then jmp @track, then lda #10. ld_sc is absolute ($B318), so sty abs is 3 bytes against 5.

## r061:3  modelb/src/disc.s:224-225  (window i0126)  M -0 B -2  [high]  review: accept

Cycles: 6->4 (Model B only)

Assumption: REQUIRES the i0042 candidate. wait_done's only caller is read_sectors at disc.s:165 (grep of the whole tree), where Y = 0. On return A changes from 0 to LD_DONE's nonzero value and the flags go from Z=1 to Z=0. The caller does lda LD_RES immediately (line 166), so it uses neither.

```
        lda #0
        sta LD_DONE
```
->
```
        sty LD_DONE                 ; (Y = 0: read_sectors)
```

Clears LD_DONE as before; A and the flags on return are dead at the only call site.

Review: Requires index 0. wait_done's only caller is disc.s:165 (grep of src, modelb and tools), where Y=0. The next instruction is lda LD_RES, so A (now nonzero) and Z on return are dead. LGCLO goes 0x9A -> 0x98 in the scratch build.

## r061:4  modelb/src/disc.s:47-48  (window i0000)  M -0 B -2  [high]  review: accept

Cycles: lda #1 (2) + sta abs (4) = 6 -> inc abs 6: cycle-exact. It comes after lda FDC8271_PAR (the last hardware access) and sta ld_res, so no hardware timing moves. Model B only (NMISTUB loads from bank 7 B7).

Assumption: Code only ever tests ld_done/LD_DONE for zero/nonzero: wait_done (lda/beq, disc.s:222) and the 1770 poll (lda LD_DONE/bne, disc.s:188). Only read_sectors (disc.s:124,154) and wait_done (225) write it to 0, before/after each command, and only these three stub sites set it (grep of the whole beeb tree incl. tools: no other reader). There are only a few increments between clears (at most one or two end-of-command NMIs per command), so it never wraps to 0. The stub restores A with pla and P with rti, so A and the flags afterwards do not matter. ld_done is in NMISTUB (absolute $0Dxx), so inc abs is 3 bytes like sta abs.

```
        lda #1
        sta ld_done
```
->
```
        inc ld_done
```

lda #1/sta ld_done makes ld_done nonzero. inc ld_done makes it nonzero from 0 (it is cleared before each command) and keeps it nonzero if it is already set (1->2); every reader treats both the same. The NMOS inc's dummy write of the old value goes to RAM, which is harmless. A is not needed afterwards (pla).

Review: inc abs takes 6 cycles, the same as lda #1 (2) + sta abs (4), and it comes after the last FDC access, so the NMI timing does not change at all. ld_done/LD_DONE is only tested for zero or nonzero (wait_done beq, the 1770 poll bne). It is cleared before every command (lines 123-124 and 153-154) and after the 8271 command (wait_done). NMIs are edge-triggered, so it only goes up a few times between clears and cannot wrap. No tool reads it. pla/rti restore A and P. Every size and offset in the stub is computed (ldx #(nmi_end-nmi_page-1), NMI_W, LD_*), and the one assert (<= $100) still holds. With all three inc candidates NMISTUB goes 0x5F -> 0x59.

## r061:5  modelb/src/disc.s:67-68  (window i0001)  M -0 B -2  [high]  review: accept

Cycles: lda #1 (2) + sta abs (4) = 6 -> inc abs 6: cycle-exact. It comes after sta FDC1770_CMD (the last hardware write), so no hardware timing moves. Model B only (NMISTUB loads from bank 7 B7).

Assumption: Code only ever tests ld_done/LD_DONE for zero/nonzero: wait_done (lda/beq, disc.s:222) and the 1770 poll (lda LD_DONE/bne, disc.s:188). Only read_sectors (disc.s:124,154) and wait_done (225) write it to 0, before/after each command, and only these three stub sites set it (grep of the whole beeb tree incl. tools: no other reader). There are only a few increments between clears, so it never wraps to 0. The stub restores A (pla) and P (rti). ld_done is absolute $0Dxx, so inc abs is 3 bytes like sta abs.

```
        lda #1
        sta ld_done
```
->
```
        inc ld_done
```

The value goes nonzero exactly as before (0->1, or 1->2 if it is already set); every reader tests only for nonzero. The NMOS dummy write goes to RAM, which is harmless. A is restored by pla.

Review: Same as index 4: 6 cycles both ways, after sta FDC1770_CMD (the last hardware write). The anonymous-label count is unchanged, and readers only test the flag for nonzero.

## r061:6  modelb/src/disc.s:74-75  (window i0006)  M -0 B -2  [high]  review: accept

Cycles: lda #1 (2) + sta abs (4) = 6 -> inc abs 6: cycle-exact. It comes after lda FDC1770_CMD (the last hardware access), so no hardware timing moves. Model B only (NMISTUB loads from bank 7 B7).

Assumption: Code only ever tests ld_done/LD_DONE for zero/nonzero (disc.s:188,222). Only read_sectors/wait_done write it to 0, before/after each command, and only these three stub sites set it (grep of the whole tree). It never wraps. The stub restores A (pla) and P (rti). ld_done is absolute $0Dxx.

```
        lda #1
        sta ld_done
```
->
```
        inc ld_done
```

The value goes nonzero exactly as before; every reader tests only for nonzero. Z=1 on the fall-through here is not used afterwards (pla/rti). A is restored by pla.

Review: Same as index 4: 6 cycles both ways, after the last hardware read. The anonymous label is unchanged, the flags are dead (pla/rti), and readers only test the flag for nonzero.

## r062:0  modelb/src/disc.s:190-192  (window i0128)  M -0 B -1  [high]  review: accept

Cycles: Model B only. Busy loop iteration lda abs 4 + and# 2 + bne taken 3 = 9 -> lda abs 4 + lsr 2 + bcs taken 3 = 9; exit 8 -> 8. Status read at identical cycle offsets.

Assumption: Exit falls into ':  jsr w_wait' (line 193): w_wait reloads X and A before any use; C is dead: w_wait does not read C and @next (line 194) starts lda/clc. A's exit value (status>>1 vs status&1) is dead for the same reason.

```
        lda FDC1770_CMD             ; fallback: the command ended without a completion NMI
        and #1
        bne :-
```
->
```
        lda FDC1770_CMD             ; fallback: the command ended without a completion NMI
        lsr                         ; busy (bit 0) into C
        bcs :-
```

and #1 / bne tests bit 0 of the status (busy); lsr moves bit 0 into C, bcs branches on exactly the same condition. Only A (dead) and N/Z/C (dead) differ.

Review: The original matches disc.s:190-192 exactly. lsr/bcs tests bit 0 (busy), the same bit and #1/bne tested. A loop pass is 9 cycles before and after, the exit 8 and 8, so the status reads land on the same cycles. Both the fall-through and the bne :+ from line 189 reach jsr w_wait, which reloads X and A, so the changed A and C are never read. Test assembly: LGCCODE 10762->10761.

## r062:1  modelb/src/disc.s:222-226  (window i0145)  M -0 B -5  [high]  review: accept

Cycles: Model B only. Exit path: lda#2 + sta abs 4 = 6 cycles removed (faster). Wait loop unchanged.

Assumption: LD_DONE (stub byte, written only 1 by nmi_i_end/nmi_w) is read only by read_sectors (grep: disc.s only; ldprog/loader/src don't reference it). wait_done's sole caller is disc.s:165, followed by lda LD_RES (A/flags dead) then either beq @next or jmp @track. Every path to another LD_DONE read passes @track, which stores 0 to LD_DONE (disc.s:123-124) before any command; @next either returns (@done) or reaches @track. The 1770 path already returns with LD_DONE = 1, and disc_boot re-copies the page (ld_done = 0) before each program load.

```
        lda LD_DONE
        beq wait_done
        lda #0
        sta LD_DONE
        rts
```
->
```
        lda LD_DONE
        beq wait_done
        rts
```

The clear after the wait is dead: the flag is always re-zeroed at @track (lines 123-124, and again 153-154 on the 8271) before the next command can raise it, and nothing reads it after read_sectors returns. Only difference at return: A = 1 not 0 and Z = 0 not 1; the caller's lda LD_RES overwrites both.

Review: The original matches the file. wait_done's only caller (disc.s:165) reloads A with lda LD_RES. Before LD_DONE is read again it is cleared at @track (123-124), and on the 8271 again at 153-154. Nothing outside disc.s reads LD_DONE or ld_done on the Model B (the ld_done in engine.s belongs to the Master). disc_boot recopies the stub image, with ld_done = 0, before each load. The 1770 path already returns with LD_DONE = 1, so the byte left in display RAM is not a new state. The exit is 6 cycles faster, after the command has completed. Test assembly: LGCLO 154->149.

## r062:2  modelb/src/disc.s:230-232  (window i0145)  M -0 B -1  [high]  review: accept

Cycles: Model B only. Poll iteration 9 -> 9 cycles, exit 8 -> 8; the ldx #20 delay before it is untouched, so the status reads happen at identical cycles (cycle-exact).

Assumption: Callers of w_wait: disc.s:178 (seek; next @wrd: lda ld_sc -- A dead, C never read before @next's clc), disc.s:193 (-> @next: lda ld_dst+1 / clc), disc.s:254 disc_init (-> lda #0 / sta w_trk / rts, or stx w_trk with the companion candidate; init.s:94 then jmp game_main, whose first code is stza stores that don't read A or C). X is still 0 on exit (dex loop unchanged).

```
:       lda FDC1770_CMD
        and #1
        bne :-
```
->
```
:       lda FDC1770_CMD
        lsr                         ; busy (bit 0) into C
        bcs :-
```

and #1/bne and lsr/bcs test the same bit (busy). Exit differences: A = status>>1 (was 0), C = 0 (was unchanged), Z depends; all dead at every caller as listed.

Review: The original matches the file. It tests the same busy bit: a poll pass is 9 cycles before and after, the exit 8 and 8, and the delay loop is untouched, so timing is cycle-exact. Callers: line 178 (@wrd does lda/sta, and nothing reads C before the clc at @next); line 193 (@next does lda/clc); line 254 (disc_init reloads with lda #0, or with candidate 4 applied does stx then rts to init.s:94). init.s:94 does jmp game_main, whose stza stores keep A and ignore the flags. X is still 0 on exit. Test assembly: LGCLO -1.

## r062:3  modelb/src/disc.s:245-251  (window i0159)  M -0 B -4  [medium]  review: fix

Cycles: Model B only. drive 0: 11 -> 9 cycles between the two FDC1770_CTL writes; drive 1: 13 -> 10. Faster, but it shortens the 1770 master-reset pulse by 2-3 cycles (1-1.5us of ~7-8us).

Assumption: dsk_drv is 0 or 1 (disc.s:13, loader.s:305); the rewrite matches the original for any nonzero value anyway ($28|$02 = $2A, $28|$01 = $29). X = dsk_drv on exit as before.

```
        lda #$28                    ; reset released, FM, the drive
        ldx dsk_drv
        beq :+
        ora #$02
        bne :++
:       ora #$01
:       sta FDC1770_CTL
```
->
```
        lda #$29                    ; reset released, FM, drive 0
        ldx dsk_drv
        beq :+
        lda #$2A                    ; drive 1
:       sta FDC1770_CTL
```

A = $28|1 = $29 on the zero path and $28|2 = $2A on the nonzero path, exactly as before; flags are dead (sta, then lda #$00). CAUTION: the reset (bit 5 low) is held for 2-3 fewer cycles; if the applier treats the reset hold as timing-critical (brief rule 7, hold loops), reject.

Review: The values are right ($29 for drive 0, $2A for drive 1). But the proposal shortens the 1770 master-reset low pulse, the time between the two FDC1770_CTL writes, from 11/13 cycles to 9/10. That is a hardware hold before the last hardware write (brief rule 7). The pulse is already far below the WD1770's documented MR width, and emulation cannot test a shorter one. The fixed version keeps 11 cycles for drive 0 (lda 2, ldx 4, beq taken 3, nop 2) and 13 for drive 1 (lda 2, ldx 4, beq not taken 2, lda 2, bne taken 3). It stores the same values and keeps the two ':' labels in the same places: beq :+ now lands on the nop line where it used to land on the ora line, and bne :++ lands on the sta. X = dsk_drv on exit, and the flags are dead because lda #$00 follows. It is 15 bytes against 16, saving 1. Test assembly: LGCCODE 10762->10761.

Fixed:
```
        lda #$29                    ; reset released, FM, drive 0
        ldx dsk_drv
        beq :+
        lda #$2A                    ; drive 1 (the old ora/bne path's 7 cycles)
        bne :++
:       nop                         ; drive 0 (the old ora #$01's 2 cycles)
:       sta FDC1770_CTL
```

## r062:4  modelb/src/disc.s:255-256  (window i0159)  M -0 B -2  [high]  review: accept

Cycles: Model B only. lda#2 + sta abs 4 = 6 -> stx abs 4 (2 faster). After the last hardware write, not timing-sensitive.

Assumption: w_wait (disc.s:227-233) exits with X = 0: ldx #20 / dex / bne loop ends at X = 0 and the busy poll does not touch X. A and flags on exit: w_wait's and #1 leaves A = 0, Z = 1, N = 0 -- the same as lda #0 (if the lsr candidate for w_wait is also applied, A/C/Z differ, but disc_init's only caller init.s:94 does jmp game_main, which starts with stza stores and reads neither A nor flags).

```
        lda #0
        sta w_trk
```
->
```
        stx w_trk                   ; X = 0: w_wait's delay loop ends there
```

stores the same 0 to w_trk; X was 0 before and stays 0.

Review: The original matches the file. w_wait exits with X = 0: ldx #20/dex/bne ends at 0 and the poll does not touch X. stx abs is 3 bytes against 5 for lda #/sta abs, and 2 cycles faster, after the last hardware write. Only A and the flags on exit can differ, and only if candidate 2 is applied. The only caller, init.s:94, does jmp game_main, which starts with stza stores that keep A and ignore the flags. Test assembly: LGCCODE -2.

## r062:5  modelb/src/disc.s:273-274  (window i0183)  M -0 B -5  [high]  review: accept

Cycles: Model B only. 1770 path: lda#2 + sta abs 4 = 6 cycles removed. 8271 path unchanged.

Assumption: The copied page byte NMIPAGE+2 is the high byte of 'jmp nmi_i' (disc.s:30), i.e. >nmi_i = $0D (labels.txt: nmi_page $0D00, nmi_i $0D03, nmi_w $0D28; NMISTUB run = NMI at $0D00 in cleo_b.cfg); NMI_W = NMIPAGE + (nmi_w - nmi_page) = $0D28, so >NMI_W = $0D too. The page is page-aligned ($0D00) and asserted <= $100 long (disc.s:82), so every stub has the same high byte; the .assert pins it.

```
        lda #>NMI_W
        sta NMIPAGE+2
```
->
```
        .assert >NMI_W = >nmi_i, error, "NMI_W: the page's jmp keeps its high byte"
```

The loop at 264-268 has just copied the image, so NMIPAGE+2 already holds $0D = >NMI_W; storing it again is a no-op. A at line 275 is reloaded (lda #<F_LDPROG_SEC).

Review: The original matches the file. NMIPAGE+2 was just copied from the image and holds the high byte of 'jmp nmi_i', which is >nmi_i = $0D (labels.txt: nmi_i $0D03). NMI_W = $0D28, so the store rewrites the same value. The .assert, checked at link time, guards the equality and passes in a test build. A is reloaded at line 275. Test assembly: LGCCODE 10762->10757.

## r062:6  modelb/src/disc.s:275-284  (window i0183)  M -0 B -2  [high]  review: accept

Cycles: Model B only. One lda #imm fewer: 30 -> 28 cycles. Not timing-critical (after ldstop5 has returned).

Assumption: F_LDPROG_SEC = 88 (modelb/build/files.inc, generated) so >F_LDPROG_SEC = 0; LDPROG = $0E00 (defs.inc:97) so <LDPROG = 0; the .assert fails the build if the disc layout ever moves the loader past sector 255. Exit goes to read_sectors, whose first instruction is lda #0 (disc.s:95), so A = 0 (was $0E) and Z = 1 (was 0) at the jmp are dead.

```
:       lda #<F_LDPROG_SEC
        sta ld_sec
        lda #>F_LDPROG_SEC
        sta ld_sec+1
        lda #F_LDPROG_N
        sta ld_n
        lda #<LDPROG
        sta ld_dst
        lda #>LDPROG
        sta ld_dst+1
```
->
```
:       lda #<F_LDPROG_SEC
        sta ld_sec
        lda #F_LDPROG_N
        sta ld_n
        lda #>LDPROG
        sta ld_dst+1
        lda #0                      ; >F_LDPROG_SEC and <LDPROG: both zero
        sta ld_sec+1
        sta ld_dst
        .assert >F_LDPROG_SEC = 0 && <LDPROG = 0, error, "disc_boot: a zero assumed"
```

Same five bytes stored: ld_sec = 88, ld_sec+1 = 0, ld_n = 13, ld_dst = 0, ld_dst+1 = $0E. Original: 5 lda# + 5 sta = 25 bytes; new: 4 lda# + 5 sta = 23 bytes. Cycles: one lda# fewer (-2 cycles). Order of the stores is irrelevant (plain RAM, interrupts off, nothing reads them until read_sectors).

Review: The original matches the file. F_LDPROG_SEC = 88 (files.inc) and LDPROG = $0E00 (defs.inc:97), and the .assert guards both zero bytes. The same five values are stored. A = 0 and Z = 1 at jmp read_sectors are dead, because read_sectors starts with lda #0. The ':' stays on the first line, where beq :+ at line 270 needs it. It assembles, LGCCODE -2.

## r062:7  modelb/src/disc.s:259-318  (window i0179)  M -0 B -3  [high]  review: accept

Cycles: Model B only. ld_begin's jmp disc_boot (3 cycles) removed; after ldstop5 has made its last CRTC/VIA write, so no hardware timing changes.

Assumption: disc_boot's only reference is ld_begin's jmp (grep: disc.s:318). farjsr is ldx #/jsr farcall (not a recorded bank site in this version, macros.txt:286); farcall returns to the next instruction, now disc_boot's ldx. File ends at line 318, so no later anonymous reference can be retargeted. SPANS >32 instructions (i0179..i0217) because it is a block move. TEXTUAL CONFLICT: contains lines 273-284 unchanged; if the two disc_boot candidates above are applied first, carry their edits into the moved text (or apply this one first and re-anchor those). Verified by assembly: LGCCODE 10762 -> 10759.

```
; ---------------------------------------------------------------- the loader
; The NMI stubs go to their page and the load-time program to LDPROG, then it runs:
; it comes back with bank 7 paged and the display RAM it used as scratch.
        .import __NMISTUB_LOAD__: absolute
disc_boot:
        ldx #(nmi_end - nmi_page - 1)
:       lda __NMISTUB_LOAD__,x      ; the stubs' bytes are in this bank; their labels
        sta NMIPAGE,x               ; are their run addresses (cleo_b.cfg)
        dex
        bpl :-
        lda dsk_type
        beq :+
        lda #<NMI_W                 ; a 1770: the page's jump goes to its stub
        sta NMIPAGE+1
        lda #>NMI_W
        sta NMIPAGE+2
:       lda #<F_LDPROG_SEC
        sta ld_sec
        lda #>F_LDPROG_SEC
        sta ld_sec+1
        lda #F_LDPROG_N
        sta ld_n
        lda #<LDPROG
        sta ld_dst
        lda #>LDPROG
        sta ld_dst+1
        jmp read_sectors

; X = level index 0..15: everything the level needs into the banks (the palette is
; black; the game's load_level goes on from the header afterwards)
load_level_b:
        stx ld_level
        jsr music_stop
        jsr ld_begin
        ldx ld_level
        jsr LDPROG                  ; lv_load
        jmp ld_resume

; the menu overlay into bank 5 and the title pack into bank 6
load_title_b:
        jsr ld_begin
        jsr LDPROG+3                ; title_load
ld_resume:                          ; (interrupts still off)
        lda #LDR7                   ; the frame is the standard one: the vsync re-phase
        sta curR7                   ; keeps it so until the bar step (display.s)
        lda #$42
        sta VIA_IFR                 ; a vsync flag raised meanwhile is stale
        cli
        rts

; the display to a standard frame, then the loader in: wait for a vsync (the handler
; restarts T1 for the bar step), interrupts off, and bank 5's ldstop5 (display.s has
; the story) takes that T1 and makes the bar's frame the standard one
ld_begin:
        lda vsyncs
:       cmp vsyncs
        beq :-
        sei
        farjsr F_LDSTOP
        jmp disc_boot
```
->
```
; X = level index 0..15: everything the level needs into the banks (the palette is
; black; the game's load_level goes on from the header afterwards)
load_level_b:
        stx ld_level
        jsr music_stop
        jsr ld_begin
        ldx ld_level
        jsr LDPROG                  ; lv_load
        jmp ld_resume

; the menu overlay into bank 5 and the title pack into bank 6
load_title_b:
        jsr ld_begin
        jsr LDPROG+3                ; title_load
ld_resume:                          ; (interrupts still off)
        lda #LDR7                   ; the frame is the standard one: the vsync re-phase
        sta curR7                   ; keeps it so until the bar step (display.s)
        lda #$42
        sta VIA_IFR                 ; a vsync flag raised meanwhile is stale
        cli
        rts

; the display to a standard frame, then the loader in: wait for a vsync (the handler
; restarts T1 for the bar step), interrupts off, and bank 5's ldstop5 (display.s has
; the story) takes that T1 and makes the bar's frame the standard one
ld_begin:
        lda vsyncs
:       cmp vsyncs
        beq :-
        sei
        farjsr F_LDSTOP
        ; (falls into disc_boot)

; ---------------------------------------------------------------- the loader
; The NMI stubs go to their page and the load-time program to LDPROG, then it runs:
; it comes back with bank 7 paged and the display RAM it used as scratch.
        .import __NMISTUB_LOAD__: absolute
disc_boot:
        ldx #(nmi_end - nmi_page - 1)
:       lda __NMISTUB_LOAD__,x      ; the stubs' bytes are in this bank; their labels
        sta NMIPAGE,x               ; are their run addresses (cleo_b.cfg)
        dex
        bpl :-
        lda dsk_type
        beq :+
        lda #<NMI_W                 ; a 1770: the page's jump goes to its stub
        sta NMIPAGE+1
        lda #>NMI_W
        sta NMIPAGE+2
:       lda #<F_LDPROG_SEC
        sta ld_sec
        lda #>F_LDPROG_SEC
        sta ld_sec+1
        lda #F_LDPROG_N
        sta ld_n
        lda #<LDPROG
        sta ld_dst
        lda #>LDPROG
        sta ld_dst+1
        jmp read_sectors
```

Pure reordering within LGCCODE: load_level_b / load_title_b / ld_resume keep their order and fallthrough (load_title_b still falls into ld_resume); ld_begin now sits immediately before the loader header and disc_boot, so its final jmp disc_boot becomes a fallthrough.

Review: The original matches the file. The only reference to disc_boot is ld_begin's jmp. farjsr is ldx #/jsr farcall, and farcall (via fcret) returns to the next instruction, so falling into disc_boot behaves the same as the jmp. ldstop5 makes all its hardware writes before its rts, so removing 3 cycles afterwards does not matter. Each anonymous reference still finds a label inside its own block, and the file ends at line 318. Other tools take addresses from labels.txt (build.sh's defs_ld.inc, loadsync.mjs). Test assembly: LGCCODE -3. Apply this or candidate 8, not both. Its text includes lines 273-284, so candidates 5 and 6 must be carried into the moved text.

## r062:8  modelb/src/disc.s:91-318  (window i0179)  M -0 B -6  [medium]  review: accept

Cycles: Model B only. Both jmps gone: -6 cycles on the load path, after ldstop5's writes.

Assumption: ALTERNATIVE TO (superset of) the ld_begin/disc_boot move above: apply one or the other. read_sectors is also entered from ldprog.s:99 via defs_ld.inc, generated from labels.txt by build.sh, so its address may move. Line 90 is .segment "LGCCODE" and the NMISTUB code before it has no forward anonymous reference past line 79. Spans lines owned by other regions (91-189): every other candidate in lines 91-318 is textually inside this range and must be applied first and carried into it, or this applied last after rebasing. Verified by assembly: LGCCODE 10762 -> 10756.

```
SEE build/grind/out/r062.cand.json candidate 9 (lo 91, hi 318): the exact verbatim text of disc.s lines 91-318 (the whole of read_sectors, the LGCLO helpers, w_trk, disc_init, disc_boot, load_level_b, load_title_b/ld_resume, ld_begin) is there; it is too long to repeat reliably here.
```
->
```
SEE build/grind/out/r062.cand.json candidate 9: the exact replacement is lines 309-317 (ld_begin without its jmp) + '        ; (falls into disc_boot)' + blank + lines 259-284 (loader header + disc_boot without jmp read_sectors) + '        ; (falls into read_sectors)' + blank + lines 91-258 unchanged + lines 287-307 (load_level_b, load_title_b, ld_resume).
```

LGCCODE order becomes ld_begin, disc_boot, read_sectors, disc_init, load_level_b, load_title_b/ld_resume (the LGCLO/LGCBSS blocks at 209-236 stay inside, unchanged). ld_begin falls into disc_boot, disc_boot falls into read_sectors (its jmp read_sectors, line 285, dropped), and read_sectors keeps its own label and @-scope.

Review: The exact original and proposal are in out/r062.cand.json candidates[8] (0-based; called 'candidate 9' in the list above). A script confirmed that the original matches lines 91-318 exactly and the proposal is exactly lines 309-317, the fall-through comment, lines 259-284, the second fall-through comment, lines 91-258 and lines 287-307. Line 90's .segment LGCCODE comes first, so ld_begin falls into disc_boot and disc_boot into read_sectors in one segment. The @-label scopes of read_sectors and disc_init are unchanged, and the NMISTUB forward references resolve before line 90. ldprog gets read_sectors' address from defs_ld.inc, which build.sh regenerates on every pass. Test assembly: LGCCODE 10762->10756. This is the alternative to candidate 7; apply one. It covers the lines of candidates 0-6, so apply those first and rebase this onto the result, or apply candidate 7 instead.

## r063:0  modelb/src/low.s:17-49  (window i0000)  M -0 B -2  [high]  review: accept

Cycles: farcall entry: pha 3 -> sta zp 3; tsx 2 + lda $0106,x 4 = 6 -> lda zp 3 (-3). fcret: pla 4 removed (-4). Every far call 7 cycles faster. Model B only (low.s is not assembled for the Master).

Assumption: fcA ($A7, modelb/src/defs.inc:68) is referenced only in low.s (grep src modelb/src) and no other equate is $A7; no interrupt/NMI path touches it (irq_handler/isr_body/music_tick do not farcall -- the existing fcret already relies on this between its sta fcA and lda fcA); no code other than farcall inspects the stack (only tsx in the game is farcall's; txs only in init.s start6; no pla/pla unwinds anywhere), so the missing 'A on entry' slot is read by nothing but fcret's discarding pla, which goes too.

```
;   caller's return
;   A on entry
;   caller's bank          <- fcret pulls this
;   fcret-1                <- the target's rts lands here
;   target-1               <- this rts goes there
farcall:
        pha
        lda ROMSEL_CPY
        pha
        lda #>(fcret-1)
        pha
        lda #<(fcret-1)
        pha
        lda FARTAB+2,x
        pha
        lda FARTAB+1,x
        pha
        lda FARTAB,x
        sta ROMSEL_CPY
        sta ROMSEL
        tax                         ; (X is reloaded below)
        wrselx 0
        tsx
        lda $0106,x                 ; A as it came in
        rts
fcret:  sta fcA                     ; the target's A (Y must survive, X carries the bank)
        pla                         ; the caller's bank
        tax
        stx ROMSEL_CPY
        stx ROMSEL
        wrselx 0
        pla                         ; (the A that went in)
        lda fcA
```
->
```
;   caller's return
;   caller's bank          <- fcret pulls this
;   fcret-1                <- the target's rts lands here
;   target-1               <- this rts goes there
; (A crosses in fcA: nothing runs between its store and its load but this code)
farcall:
        sta fcA                     ; A as it came in
        lda ROMSEL_CPY
        pha
        lda #>(fcret-1)
        pha
        lda #<(fcret-1)
        pha
        lda FARTAB+2,x
        pha
        lda FARTAB+1,x
        pha
        lda FARTAB,x
        sta ROMSEL_CPY
        sta ROMSEL
        tax                         ; (X is reloaded below)
        wrselx 0
        lda fcA
        rts
fcret:  sta fcA                     ; the target's A (Y must survive, X carries the bank)
        pla                         ; the caller's bank
        tax
        stx ROMSEL_CPY
        stx ROMSEL
        wrselx 0
        lda fcA
```

The 'A on entry' stack slot is used for two things: farcall's tsx/lda $0106,x re-reads it after A was used for the bank, and fcret's pla discards it. Storing A to fcA at entry and reloading it before the rts gives the target the same A (and the same N/Z from the final load; C/V untouched by both versions). No other code runs between that store and load (no calls; interrupts do not use fcA), and nested far calls happen only after the rts, when fcA is no longer needed on the way in. With the slot gone, fcret no longer pulls it: the stack depth at fcret's pla is the caller's bank on top exactly as before (return into fcret consumed fcret-1 and target-1). fcret's result: A = fcA = target's A, X = caller's bank, flags from lda fcA -- identical. Stack usage 1 byte less per nesting level. Also verified by assembling a scratch copy: LOWCODE 2 bytes smaller.

Review: Lines 17-49 of modelb/src/low.s match. Stack check: the original pushes A, bank, fcret-1, target-1, and $0106,x after tsx is the A slot. The rewrite drops that slot and fcret's pla that discarded it. The target still gets A with N/Z from the load, and fcret still pulls the caller's bank and returns A = fcA with X = bank. X at target entry changes from SP to the bank number. I read the entry of all 18 FARTAB targets: each reloads X or does not read it, and mark_dirty_x takes X from farx. fcA ($A7) appears only in defs.inc and low.s, and no other equate or zp .res uses $A7 (ZEROPAGE ends at $A2; ldprog uses $A8+). No interrupt path farcalls (the farjsr sites are render_core, init, disc F_LDSTOP, m_mark_dirty and FARSUBs), and fcret already relied on that. The only tsx outside farcall is in ldprog (its own stack). Bytes: pha 1 + tsx 1 + lda abs,x 3 + pla 1 = 6 becomes sta zp 2 + lda zp 2 = 4, saving 2. Cycles -7 per call. ldstop5's timing starts at its own poll. The wrselx sites are unchanged and stay in separate farcall and fcret scopes.

## r063:1  modelb/src/low.s:68-69  (window i0006)  M -0 B -2  [high]  review: accept

Cycles: tick path: lda #0 2 + sta abs 4 = 6 -> dec abs 6 (equal); no-tick path untouched. After isr_body returns, so no hardware timing is affected; the entry up to jsr isr_body is unchanged.

Assumption: MUSTICK ($0200, LOWBSS) is only ever 0 or 1: its one writer is engine.s:3988 sta MUSTICK from MUSON, whose writers are stz MUSON (engine.s:3594,4199, init.s:74) and engine.s:4194 sta MUSON with A = 1 (lda #1 at 4189; stza preserves A); start6 zeroes $0140-$0203 (init.s:37-41). This path is taken only when MUSTICK != 0 (beq @nomus above), so MUSTICK = 1 here.

```
        lda #0
        sta MUSTICK
```
->
```
        dec MUSTICK                 ; (1 -> 0: MUSON's value, which is 0 or 1)
```

With MUSTICK = 1, dec leaves it 0 as the store of 0 did. A: original left A = 0, but the next instruction (bankimm lda, BANK_TILES) reloads A; flags are likewise overwritten by that lda. The bankimm site itself is untouched (only its address moves, and it is recorded by label).

Review: MUSTICK ($0200, absolute) has one writer, engine.s:3988 sta MUSTICK from MUSON. MUSON is written only by stz (0) and by music_start's sta MUSON with A = 1: lda #1, and the three stza calls keep A through pha/pla. init zeroes $0140-$0203. The path is reached only when MUSTICK != 0, so MUSTICK = 1 and dec gives 0. The IRQ runs with I set, so nothing can intervene. The next instruction, bankimm lda, reloads A and the flags. 5 bytes become 3, saving 2. Cycles 6 -> 6. This is after isr_body, so no CRTC timing is affected.

## r063:2  modelb/src/low.s:92-92  (window i0022)  M -0 B -1  [high]  review: reject

Cycles: -2 cycles (clc removed); single path. Model B only.

Assumption: maprow (Model B, engine.s:4513-4531) ends lda mapptr+1 / clc / adc #>LV_MAP / sta mapptr+1 / rts, leaving C = 0 unless the row offset's high byte + $88 overflows, i.e. row * stride >= $7800; the map is at most 8K from $8800 (defs.inc:41), and the one caller (engine.s:637-639, rc_y >> 1 of a window-clipped rect; clamp_window keeps the window within the map) passes a tile row inside the map, so the offset is < $2000 and C = 0.

```
        clc
```
->
```

```

The clc directly follows jsr maprow, whose last flag-affecting instruction is adc #>LV_MAP producing C = 0 for every in-map row; sta and rts do not touch C. So the carry into adc rc_tx0 is 0 either way.

Review: Correct on its own: maprow ends with clc/adc #>LV_MAP, and C = 0 for any row offset below $7800. But candidate 3 replaces the same lines 90-98, removes this clc as well and saves 5 bytes. Only one of the two can apply, so apply candidate 3.

## r063:3  modelb/src/low.s:90-98  (window i0022)  M -0 B -5  [medium]  review: accept

Cycles: after the jsr: 19 -> 11 cycles; single path. Model B only. ALTERNATIVE to the clc-only candidate above (same lines; apply one).

Assumption: (1) maprow's exit: A = mapptr+1 (its last store) and C = 0 (see the clc candidate). (2) rc_tx0 < map width in tiles: rc_tx0 = rc_x >> 2 (engine.s:602-611) of a window-clipped rect, and clamp_window (game.s:164) keeps wx <= maxwx, so every rect column is inside the map; the map row's low byte (row * 2^lw) & $FF is a multiple of 2^lw (<= 256 - 2^lw), so low byte + tx0 <= 255 and the original adc #0 always adds 0.

```
        jsr maprow                  ; (X kept; the logic's mapptr is its scratch)
        lda mapptr
        clc
        adc rc_tx0
        sta ptr
        lda mapptr+1
        adc #0
        sta ptr+1
        rts
```
->
```
        jsr maprow                  ; (X kept; the logic's mapptr is its scratch) A = mapptr+1, C = 0
        sta ptr+1                   ; tx0 < the map's width: no carry out of the low byte
        lda mapptr
        adc rc_tx0
        sta ptr
        rts
```

Original: ptr = mapptr + rc_tx0 as 16 bits, ptr+1 = mapptr+1 + carry. Under (2) the carry out of the low add is always 0, so ptr+1 = mapptr+1 = A on return from maprow; storing it first and then forming the low byte gives the same ptr. Exit differences: A = ptr instead of ptr+1, N/Z from the low adc, C = 0 in both; the only caller (engine.s:639) goes straight into jsr mapstrip, which loads Y and A before using them and never reads C. If (2) were ever false the original would already be reading the wrong map row's tiles.

Review: Lines 90-98 match. maprow (Model B) exits with A = mapptr+1 and C = 0: the map is at most 8K from $8800, and even a row one past the bottom is far from $7800. The low byte of row*2^lw is a multiple of 2^lw, since LV_MAP's low byte is 0. tx0 < map width in tiles: the rects are window-clipped (scroll_validate uses wcx..wcx+79), maxwx = mapw-160, and clamp_window holds the window inside the map. So the original adc #0 always added 0. mapptr ($D9), ptr ($02) and rc_tx0 ($2B) are all zero page: 13 bytes become 8, saving 5 and cutting 19 cycles to 11. On exit A and N/Z differ, but the only caller (engine.s:639) goes straight to @rowy: jsr mapstrip, which reloads A (bankimm lda) and Y. C is unused.

## r063:4  modelb/src/low.s:100-120  (window i0042)  M -0 B -2  [high]  review: accept

Cycles: drawrect's jsr mapstrip: same instructions reordered, equal. dirfetch: lda #7 2 + sta zp 3 + (mapstrip's) ldy zp 3 = 8 -> ldy #7 2 (-6). Model B only.

Assumption: rc_nt ($2C, zero page) is written by dirfetch only to feed mapstrip's count: its other uses are drawrect's own (engine.s:621 sets it per rect, 658 reads it after its own jsr mapstrip), and dirfetch's callers (engine.s:1505,1554,1563, the sprite prologue in bank 7) never read it, nor does anything expect 7 there. Y is loaded from zero page (main RAM), so loading it before the bank switch reads the same value. Interrupts save/restore Y.

```
mapstrip:                           ; (ptr), 0..rc_nt -> MAPBUF; bank 5 back (the row
        bankimm lda, BANK_MAP, 0    ; loop's: dirfetch, the other caller, restores its own)
        sta ROMSEL_CPY
        sta ROMSEL
        ldy rc_nt
:       lda (ptr),y
        sta MAPBUF,y
        dey
        bpl :-
        bankimm lda, BANK_TILES, 0  ; (the write bank: drawrect sets it after the call;
        sta ROMSEL_CPY              ;  dirfetch goes on to pagelogic)
        sta ROMSEL
        rts

; the sprite directory (and the title pack's) is in bank 6 and the prologue in bank
; 7: an entry's eight bytes come across here, and ptr is left pointing at the copy.
; (mapstrip's loop, with its count: rc_nt is drawrect's, which is not running.)
dirfetch:                           ; ptr -> the entry in bank 6
        lda #7
        sta rc_nt
        jsr mapstrip
```
->
```
mapstrip:                           ; (ptr), 0..rc_nt -> MAPBUF; bank 5 back (the row
        ldy rc_nt                   ; loop's: dirfetch, the other caller, restores its own)
mapstripy:                          ; (Y = the count: dirfetch)
        bankimm lda, BANK_MAP, 0
        sta ROMSEL_CPY
        sta ROMSEL
:       lda (ptr),y
        sta MAPBUF,y
        dey
        bpl :-
        bankimm lda, BANK_TILES, 0  ; (the write bank: drawrect sets it after the call;
        sta ROMSEL_CPY              ;  dirfetch goes on to pagelogic)
        sta ROMSEL
        rts

; the sprite directory (and the title pack's) is in bank 6 and the prologue in bank
; 7: an entry's eight bytes come across here, and ptr is left pointing at the copy.
; (mapstrip's loop, with its count in Y: rc_nt is drawrect's and is left alone.)
dirfetch:                           ; ptr -> the entry in bank 6
        ldy #7
        jsr mapstripy
```

mapstrip: ldy rc_nt moves ahead of the bank switch (independent: rc_nt is zero page; lda/sta ROMSEL do not touch Y; the loop's flags come from its own lda/dey). New normal label mapstripy only splits the @ scope between mapstrip's two bankimm sites, whose @bf_ names differ (BANK_MAP vs BANK_TILES) and are referenced only inside their own macro expansions; the recorded instructions are unchanged. dirfetch: Y = 7 entering the switch+loop exactly as ldy rc_nt with rc_nt = 7 gave; copies MAPBUF[0..7], exits Y = $FF, A = BANK_TILES as before; the only difference is rc_nt is no longer overwritten.

Review: Lines 100-120 match. rc_nt ($2C, the only symbol there) is read only by mapstrip and by drawrect at 658, and drawrect sets it at 621 before every use. dirfetch's callers (engine.s:1505,1554,1563) never read rc_nt. Moving ldy rc_nt ahead of the bank switch is fine: it is zero page, and lda/sta ROMSEL leave Y alone. The two bankimm sites (@bf_lda_6 and @bf_lda_5, distinct) now share the scope of mapstripy, and the recorded instructions are unchanged. The anonymous-label count is unchanged. Exit state is the same: Y=$FF, A=BANK_TILES. Bytes: lda #7 + sta zp (4) becomes ldy #7 (2), saving 2. drawrect path cycles equal, dirfetch -6.

## r063:5  src/engine.s:4560-4560  (window i0000)  M -0 B -3  [high]  review: accept

Cycles: Model B mapput: jmp 3 removed (-3); Master unchanged.

Assumption: OUTSIDE r063 (found from the LOWCODE layout while checking low.s's neighbours; report once). On the Model B, mapput (PLACE LOW2/LOWCODE, engine.s:4506) is the last LOWCODE code before pagelogic (engine.s:4733 PLACE LOW2/LOWCODE): everything between is in LGCLO or Master-only; modelb/build/labels.txt has mapput at $0236 (17 bytes, ending at $0246) and pagelogic at $0247. On the Master init_maprows (LOW2) sits between, so the jmp stays there. The .assert (resolved by the linker) guards the fall-through.

```
        jmp pagelogic
```
->
```
  .if MODELB                        ; (LOWCODE: pagelogic follows directly)
        .assert * = pagelogic, error, "mapput falls into pagelogic"
  .else
        jmp pagelogic
  .endif
```

jmp pagelogic to the very next address is a no-op on the Model B; removing it leaves execution falling into pagelogic with A/X/Y/flags unchanged (jmp touches none).

Review: engine.s:4560 is mapput's jmp pagelogic. On the Model B, everything between 4561 and pagelogic (4733) is in LGCLO, MNUCODE, TILCODE or LGCCODE, or is Master-only. labels.txt has mapput at $0236: 18 bytes, the jmp at $0244-6, pagelogic at $0247. So the jmp targets the next byte, and removing it falls through with registers and flags intact. The .assert (same segment, resolved by the linker) guards the fall-through. The Master keeps the jmp. LOWCODE stays inside page 2, so no branch changes page and irq_handler/pagelogic timing is unaffected.

## r064:0  modelb/src/banks.s:121-122  (window i0001)  M -0 B -2  [high]  review: accept

Cycles: fall-through of bcs @out: 2 cycles faster (clc gone); @out path unchanged. Model B only file. Saves 1 byte in each of the two MIRDIRTY_BODY expansions (mirdirty5 TILCODE, mirdirty LGCCODE).

Assumption: none outside the window: carry at line 122 is known clear because line 119 bcs @out was not taken and pha/txa (120,121) do not touch C.

```
        txa
        clc
```
->
```
        txa                         ; C clear: bcs @out not taken
```

cmp #ROWCHARS / bcs @out: falling through means C=0. pha and txa leave C alone, so the second clc sets C to the value it already has; adc wcxm computes the same sum and flags.

Review: Lines 121-122 match the file. When bcs @out (line 119) falls through, C=0. pha and txa leave C alone, so the clc at line 122 does nothing. The only way into the body is the routine label, and the ':' at line 127 is reached only from line 125. The file is Model B only and the macro is expanded twice (mirdirty5 in TILCODE, mirdirty in LGCCODE), so 1 byte each = 2.

## r064:1  modelb/src/banks.s:139-140  (window i0016)  M -0 B -2  [high]  review: accept

Cycles: all paths identical (each exit executes one rts, 6 cycles). Saves 1 byte in each of the two MIRDIRTY_BODY expansions.

Assumption: bcs @out (line 119) stays in range (it gets closer). @out is a cheap label local to mirdirty5/mirdirty; no other reference.

```
:       rts
@out:   rts
```
->
```
:
@out:   rts
```

Two adjacent rts; both labels now name the single rts. Behaviour of every entry identical.

Review: Lines 139-140 match the file. The ':' stays as a label on its own line, so the anonymous-label count is unchanged and bcc :+ (line 137) still lands on the one remaining rts. @out is a cheap label scoped to mirdirty5/mirdirty and names the same rts. Nothing references an offset into mirdirty or mirdirty5. 1 byte per expansion = 2. Does not conflict with candidate 0.

## r064:2  modelb/src/banks.s:162-167  (window i0025)  M -0 B -1  [high]  review: accept

Cycles: setup 4->4; per iteration 12->10 (sta 5 + inx 2 + cpx 2 + bne 3 -> dex 2 + sta 5 + bne 3). Model B only; once-only start-up code, not timing critical.

Assumption: Carry after the loop is no longer forced to 1 (cpx): what follows (bankimm lda / sta spbank / lda #$FF / sta x2 / jmp take_over, engine.s:4211-4240) never reads C (sei, loads, stores, and/ora, stza, cli, rts). init5 is the last code in TILLOW (display.s ldstop5 and engine.s take_over are placed earlier: main.s include order), so nothing at a fixed offset moves; bank5_entry precedes it.

```
        ldx #0
        txa
:       sta __TILBSS_RUN__,x        ; the ring work's state: zero, as the Master's tables
        inx
        cpx #<__TILBSS_SIZE__
        bne :-
```
->
```
        ldx #<__TILBSS_SIZE__
        lda #0
:       dex
        sta __TILBSS_RUN__,x        ; the ring work's state: zero, as the Master's tables
        bne :-
```

Same bytes zeroed (RUN+size-1 down to RUN+0 instead of up); exit state X=0, A=0, Z=1, N=0 as before. Size 0 would clear 256 bytes in both forms (assert keeps size < 256 anyway). bne tests dex's Z since sta does not affect flags.

Review: Lines 162-167 match the file. 11 -> 10 bytes, and each iteration is faster. The same bytes are zeroed. TILBSS is in B5X, separate from the loop code in TILLOW/B5C. B5C is fill=yes, so a smaller TILLOW moves nothing at a fixed address. The candidate's claim 'X=0 as before' is wrong: the original exits with X=size and C=1, the new code with X=0 and C unchanged from entry. Both are dead. take_over (engine.s:4211) never reads X or C. farcall's fcret (low.s:42) reloads X with pla/tax and keeps C. After farjsr F_INIT5 (init.s:93) come disc_init (lda/beq, stores, rts) and game_main (stza, then ensure_menu: lda/bne), and neither reads C before setting it.

## r064:3  modelb/src/banks.s:176-181  (window i0037)  M -0 B -10  [high]  review: accept

Cycles: five 2-cycle lda #0 removed (10 cycles faster). Model B only file (LGCCODE, bank 7).

Assumption: Model B stz is cpu.inc:195 (lda #0 / sta arg); A is 0 after each store. Exit state identical (A=0, N=0, Z=1, C/V untouched), so callers game.s:65 and modelb/src/init.s:72 see no difference.

```
        stz BUF_VALID
        stz BUF_VALID+1
        stz RECCNT
        stz RECCNT+1
        stz DIRTYCNT
        stz DIRTYCNT+1
```
->
```
        lda #0
        sta BUF_VALID
        sta BUF_VALID+1
        sta RECCNT
        sta RECCNT+1
        sta DIRTYCNT
        sta DIRTYCNT+1
```

Each stz expands to lda #0 / sta m; A stays 0 across sta, so the second-through-sixth lda #0 are redundant. Same stores in the same order, same final registers and flags.

Review: Lines 176-181 match the file. On the Model B, stz is lda #0 / sta (cpu.inc:195). A stays 0 across each sta, so five of the lda #0 are redundant: 10 bytes and 10 cycles saved. The stores are the same and in the same order. Exit state is unchanged: A=0, N=0, Z=1, C and V untouched. Both callers are unaffected: init.s:72 (followed by stz MUSON) and game.s:65 (the MODELB branch, where lvreset already destroyed A).

## r065:0  modelb/src/init.s:10-30  (window i0000)  M -0 B -7  [medium]  review: accept

Cycles: Boot only, runs once, not timing-critical. Entry: 24-byte copy loop + stub (~150 cy) -> 7-cycle switch + fall-through (the jmp start7 at $8047 is only reached later from bank 6). Faster.

Assumption: MAPLO is the first segment of B6L, so its first byte is $8040 = BANKCODE in bank 6 (cleo_b.cfg: MAPLO then LOWCODE in B6L; SPACE.md MAPLO 008040); LGCENT starts at BANKCODE in bank 7 (existing assert line 9). Both new .asserts pin start6 and bank 7's jmp start7 to BANKCODE+7, so a layout change fails the link. ROMSEL_CPY=$F4 (zp, 2 bytes) so each switch is exactly 7 bytes. The 6502 fetches the opcode after `sta ROMSEL` from the NEW bank (the file's own comment, lines 10-11, states it; it is why the old code ran from RAM). Interrupt ordering unchanged: $F4 is written before ROMSEL, as in the old stub (an OS IRQ between restores bank 6 and returns to $8047 in bank 6). bankimm sites: the loader patches BANKFIX before its jmp BANKCODE (loader.s:280-313). B6L room: T1 ALONE grows MAPLO by 7 ($3E->$45) and $45+$FA(LOWCODE)=$13F <= $140, so it fits; with Z and T2 MAPLO shrinks to $25. T1 is valid on its own (the old @to7 stub copy at lines 51-61 still works; its cheap @bf_lda_7 is in start6's scope, the new one in to7's: no clash). LGCLO moves down 14 bytes (it follows LGCENT in B7A, no fixed address in it). Verified in a scratch copy of the tree (nothing in the repo edited): with T1+Z+T2 and the four LGCCODE candidates all applied, the Model B builds with every .assert passing; LGCENT $18->$0A, MAPLO $3E->$25, LGCCODE $2A0A->$29F6; BANKFIX/WRFIX sizes unchanged; bdiff L0 and L5 lock-step OK over 200 frames, bpixdiff L0 and L6 identical over 200 frames. btitlediff DIFFERS on 7-8/30 samples, but only in the first 8 samples (the title load) and identically for a lone 'dec curbuf' (cycle-neutral, 2 bytes): any byte shift in LGCCODE changes the addresses LDPROG carries and the boot timing, so the gate's B title check cannot be clean for ANY LGCCODE saving -- expected, not a fault.

```
; A bank cannot page itself out and carry on: the instruction after the switch is
; read from the new bank.  So the switch runs from main RAM -- the bottom of the
; stack page, which nothing has used yet.
entry:  ldx #@stubend-@stub-1
:       lda @stub,x
        sta $0100,x
        dex
        bpl :-
        jmp $0100
@stub:  bankimm lda, BANK_MAP, BANK_LVL
        sta ROMSEL_CPY
        sta ROMSEL
        jmp start6
@stubend:

        .segment "MAPLO"            ; bank 6
        .import __LOWCODE_SIZE__: absolute   ; (LOAD and RUN: cpu.inc, for the bank patches)
start6:
        sei
        ldx #$3F                    ; the stack is 64 bytes: $0100-$013F
        txs
```
->
```
; A bank cannot page itself out and carry on: the instruction after the switch is
; read from the new bank.  So each switch lands on its continuation: entry's, at
; $8040 in bank 7, on start6 at $8047 in bank 6; bank 6's to7, the same seven bytes
; at $8040 there, on the jmp start7 at $8047 in bank 7.
entry:  bankimm lda, BANK_MAP, BANK_LVL
        sta ROMSEL_CPY
        sta ROMSEL                  ; the next fetch is bank 6's start6
        .assert * = BANKCODE + 7, error, "entry's switch must end where start6 begins"
        jmp start7                  ; bank 6's to7 lands here, in bank 7

        .segment "MAPLO"            ; bank 6
        .import __LOWCODE_SIZE__: absolute   ; (LOAD and RUN: cpu.inc, for the bank patches)
to7:    bankimm lda, BANK_LVL, BANK_MAP ; start6's way out: the next fetch is bank 7's
        sta ROMSEL_CPY              ; jmp start7, at the same address
        sta ROMSEL
start6:
        .assert * = BANKCODE + 7, error, "start6 must follow entry's switch"
        sei
        ldx #$3F                    ; the stack is 64 bytes: $0100-$013F
        txs
```

Old: bank 7 copies a 10-byte stub (lda #bank6 / sta $F4 / sta ROMSEL / jmp start6) to $0100 and jumps there. New: bank 7 does the same three instructions in place at $8040-$8046; the next opcode is fetched from bank 6 at $8047, which is start6 (bank 6's MAPLO now begins with 7 bytes, to7, so start6 lands at $8047). Registers/flags at start6: old A=bank6, X=$FF (loop ended), new A=bank6, X=loader's X -- start6 immediately does sei / ldx #$3F / txs and A is reloaded (lda #0), so neither matters. The new to7 (bank 6, $8040-$8046: lda #bank7 / sta $F4 / sta ROMSEL) mirrors it: its next fetch is $8047 in bank 7 = `jmp start7` (A = bank 7 on arrival, as start7's wrsel requires, exactly as the old stub left it). to7 is only used once T2 is applied. Bank 7 saves 14 bytes in B7A (LGCENT/LGCLO, bank 7 is full); bank 6 spends 7 until T2 recovers 21. SUPERSEDES nothing; T2 depends on it; P (the fallback) is an alternative to T2, not to T1.

Review: original matches init.s 10-30. Scratch build with all accepted candidates: every .assert passes, LGCENT $18->$0A, MAPLO $3E->$25 (T1 alone $45, +LOWCODE $FA = $13F <= $140), BANKFIX unchanged with T2. Listing: bank 7 $8040 A9/85 F4/8D 30 FE then jmp start7 at $8047; bank 6 $8040 identical 7-byte switch then start6 at $8047; both lda sites recorded in BANKFIX with the right image bank. IRQ between sta $F4 and sta ROMSEL (interrupts are still on) returns to $8044 in whichever bank the OS restores, and both banks hold sta ROMSEL there, so both outcomes reach start6. No tool references entry/@stub/$0100. Net Model B 7 (bank 7 -14, bank 6 +7); boot-only, faster.

## r065:1  modelb/src/init.s:31-50  (window i0012)  M -0 B -11  [high]  review: accept

Cycles: Boot only, once. Old: 240x11 + 196x12 + setup ~ 5000 cy zeroing + copy; new zeroing 240x14 = 3360 cy; copy loop identical per byte (ldx #0 dropped, -2 cy). Faster.

Assumption: sei has run (line 28) and SP=$3F with nothing pushed yet (lines 28-30; nothing between pushes), so $0114-$013F -- the stack page above the stub at $0100-$0109 -- holds nothing live; the stack only grows down from $013F later. $0204/$0205 (IRQ1V) untouched: the low write ends at $0113+$F0=$0203, exactly the old end ($0140+$C3). zp,x wraps within page zero on the NMOS 6502: sta $FF,x (assembles 95 FF) writes (X-1)&$FF = $EF..$00 for X=$F0..1, the old $00-$EF, never $F0-$FF. The copy needs X=0 on entry: dex/bne exits with X=0. __LOWCODE_SIZE__=$FA (<256, asserted, nonzero). Verified in a scratch copy of the tree (nothing in the repo edited): with T1+Z+T2 and the four LGCCODE candidates all applied, the Model B builds with every .assert passing; LGCENT $18->$0A, MAPLO $3E->$25, LGCCODE $2A0A->$29F6; BANKFIX/WRFIX sizes unchanged; bdiff L0 and L5 lock-step OK over 200 frames, bpixdiff L0 and L6 identical over 200 frames. btitlediff DIFFERS on 7-8/30 samples, but only in the first 8 samples (the title load) and identically for a lone 'dec curbuf' (cycle-neutral, 2 bytes): any byte shift in LGCCODE changes the addresses LDPROG carries and the boot timing, so the gate's B title check cannot be clean for ANY LGCCODE saving -- expected, not a fault.

```
        lda #0                      ; zero page ($F0-$FF is the MOS's: $F4 is the
        ldx #0                      ; bank the loader selected, and the OS IRQ still
:       sta $00,x                   ; restores from it until take_over) and the low RAM
        inx
        cpx #$F0
        bne :-
        ldx #0
:       sta $0140,x
        inx
        cpx #$C4
        bne :-
        ; the low-RAM image is copied down to $0206 -- absolute indexed, the image being
        ; under a page (14 bytes shorter than two pointers: this bank's corner is full)
        .assert __LOWCODE_SIZE__ < 256, error, "the low-RAM image is copied a byte at a time"
        ldx #0                      ; exactly its length: the bar starts at $0300
@lc:    lda __LOWCODE_LOAD__,x
        sta __LOWCODE_RUN__,x
        inx
        cpx #<__LOWCODE_SIZE__
        bne @lc
```
->
```
        lda #0                      ; zero page ($F0-$FF is the MOS's: $F4 is the
        ldx #$F0                    ; bank the loader selected, and the OS IRQ still
:       sta $FF,x                   ; restores from it until take_over): $00-$EF, zp,x
        sta $0113,x                 ; wrapping; and $0114-$0203, the low RAM and the
        dex                         ; stack above $0113 (nothing is on it yet)
        bne :-
        ; the low-RAM image is copied down to $0206 -- absolute indexed, the image being
        ; under a page (14 bytes shorter than two pointers: this bank's corner is full)
        .assert __LOWCODE_SIZE__ < 256, error, "the low-RAM image is copied a byte at a time"
@lc:    lda __LOWCODE_LOAD__,x      ; (X = 0) exactly its length: the bar starts at $0300
        sta __LOWCODE_RUN__,x
        inx
        cpx #<__LOWCODE_SIZE__
        bne @lc
```

Old: zero $00-$EF, then zero $0140-$0203, then ldx #0 and copy LOWCODE ascending. New: one loop, X=$F0..1, zeroes $00-$EF (zp,x wrap) and $0114-$0203 per iteration (a superset of the old $0140-$0203, the extra $0114-$013F being the unused top of the fresh stack), A=0 throughout; it exits with X=0, so the copy's ldx #0 is dropped and the copy loop is unchanged. End state: identical memory except $0114-$013F now 0 (nothing reads it before it is pushed to), X=$FA after the copy as before, A=last copied byte as before, flags as before (cpx/bne).

Review: sta $FF,x assembles 95 FF (zp,x wraps): X=$F0..1 zeroes $EF..$00; sta $0113,x zeroes $0203..$0114, a superset of old $0140-$0203; IRQ1V $0204 untouched; $0114-$013F is stack above SP=$3F with nothing pushed (sei done). Exits X=0 for the copy. 23->12 bytes = 11; boot-only, faster. Anon -1 is local; builds.

## r065:2  modelb/src/init.s:73-75  (window i0012)  M -0 B -9  [high]  review: accept

Cycles: Removes lda #0/sta abs (6) and lda #0/sta zp (5): 11 cycles faster, boot only.

Assumption: MUSON is in LOWBSS (modelb/src/low.s:138 .segment "LOWBSS", :150 MUSON) = $0140-$0201 (map), inside start6's zeroed $0140-$0203; SFXREQ=$A6 (defs.inc:53), inside the zeroed zp $00-$EF. Nothing between the zeroing and line 74 writes either: the LOWCODE copy ($0206+), the stub copy ($0100-$0109), wrsel, the PBANK loop (PBANK..PBOARD, separate LOWBSS symbols), lvreset (banks.s:175: BUF_VALID, RECCNT, DIRTYCNT only). IRQs are off (sei). A and flags after: the next instruction is lda #<VS2T_DEFAULT, which reloads A and sets N,Z; X/Y untouched by the stz expansions. Verified in a scratch copy of the tree (nothing in the repo edited): with T1+Z+T2 and the four LGCCODE candidates all applied, the Model B builds with every .assert passing; LGCENT $18->$0A, MAPLO $3E->$25, LGCCODE $2A0A->$29F6; BANKFIX/WRFIX sizes unchanged; bdiff L0 and L5 lock-step OK over 200 frames, bpixdiff L0 and L6 identical over 200 frames. btitlediff DIFFERS on 7-8/30 samples, but only in the first 8 samples (the title load) and identically for a lone 'dec curbuf' (cycle-neutral, 2 bytes): any byte shift in LGCCODE changes the addresses LDPROG carries and the boot timing, so the gate's B title check cannot be clean for ANY LGCCODE saving -- expected, not a fault.

```
        ; what the Master's init_tables sets that is this bank's or the zero page's
        stz MUSON
        stz SFXREQ
```
->
```
        ; MUSON and SFXREQ: the zeros start6 left (low BSS, zero page)
```

The two stz store 0 into bytes that already hold 0 (zeroed by start6, written by nothing since). Deleting them leaves memory identical; A (was 0) is dead -- reloaded by line 76.

Review: MUSON is a .res in LOWBSS ($0140-$0201) and SFXREQ=$A6 is in zp $00-$EF: start6 zeroed both, and nothing before line 74 writes them (PBANK loop, lvreset are distinct symbols; sei). A and flags are reloaded by lda #<VS2T_DEFAULT. 5+4 = 9 bytes (LGCCODE).

## r065:3  modelb/src/init.s:80-80  (window i0018)  M -0 B -3  [high]  review: accept

Cycles: jsr+rts (12 cycles) removed; boot only.

Assumption: src/engine.s:4060-4063: under .if MODELB, music_init is a bare `rts` (the table is MUSIC_ADDR in the overlay: MUSIC_TAB = MUSIC_ADDR). init.s is Model B only. The music_init body itself stays (the Master's src/main.s:96 calls it); on the Model B it becomes unreferenced (1 byte, wherever engine.s places it) -- a separate saving for whoever owns engine.s:4060. Verified in a scratch copy of the tree (nothing in the repo edited): with T1+Z+T2 and the four LGCCODE candidates all applied, the Model B builds with every .assert passing; LGCENT $18->$0A, MAPLO $3E->$25, LGCCODE $2A0A->$29F6; BANKFIX/WRFIX sizes unchanged; bdiff L0 and L5 lock-step OK over 200 frames, bpixdiff L0 and L6 identical over 200 frames. btitlediff DIFFERS on 7-8/30 samples, but only in the first 8 samples (the title load) and identically for a lone 'dec curbuf' (cycle-neutral, 2 bytes): any byte shift in LGCCODE changes the addresses LDPROG carries and the boot timing, so the gate's B title check cannot be clean for ANY LGCCODE saving -- expected, not a fault.

```
        jsr music_init
```
->
```
        ; (music_init: an rts on the Model B, whose period table is static)
```

jsr to an rts changes no register, flag or memory other than two stack bytes below SP that are dead. Removing it is exact.

Review: engine.s:4060 music_init is a bare rts under .if MODELB; jsr/rts changes nothing live. 3 bytes.

## r065:4  modelb/src/init.s:51-61  (window i0027)  M -0 B -21  [medium]  review: accept

Cycles: Boot only, once. Old: copy 10 bytes (~130 cy) + jmp + stub 12 cy; new: jmp to7 (3) + the same 7-cycle switch + jmp start7. Faster.

Assumption: REQUIRES T1 (window i0000, lines 10-30): to7 is defined there, at $8040 in bank 6, and bank 7's jmp start7 at $8047. If T1 is not applied, use P instead. A on arrival at start7 = bank 7 (from to7's lda), which start7's wrsel needs -- as the old stub. BANKFIX: the old @to7 bankimm site goes, T1's to7 site replaces it (BANKFIX back to $10B). Verified in a scratch copy of the tree (nothing in the repo edited): with T1+Z+T2 and the four LGCCODE candidates all applied, the Model B builds with every .assert passing; LGCENT $18->$0A, MAPLO $3E->$25, LGCCODE $2A0A->$29F6; BANKFIX/WRFIX sizes unchanged; bdiff L0 and L5 lock-step OK over 200 frames, bpixdiff L0 and L6 identical over 200 frames. btitlediff DIFFERS on 7-8/30 samples, but only in the first 8 samples (the title load) and identically for a lone 'dec curbuf' (cycle-neutral, 2 bytes): any byte shift in LGCCODE changes the addresses LDPROG carries and the boot timing, so the gate's B title check cannot be clean for ANY LGCCODE saving -- expected, not a fault.

```
        ldx #@to7end-@to7-1         ; the switch to bank 7 runs from the stack page,
:       lda @to7,x                  ; as the entry's did: a bank cannot page itself out
        sta $0100,x
        dex
        bpl :-
        jmp $0100
@to7:   bankimm lda, BANK_LVL, BANK_MAP
        sta ROMSEL_CPY
        sta ROMSEL
        jmp start7
@to7end:
```
->
```
        jmp to7                     ; to bank 7: the switch at $8040 (see entry)
```

The old code copied a lda #bank7 / sta $F4 / sta ROMSEL / jmp start7 stub to $0100 and ran it. T1's to7 is exactly those first three instructions at $8040 in bank 6, with the jmp start7 found at $8047 in bank 7 after the switch. Same stores, same order, same A at start7; SP unchanged (no pushes either way).

Review: Requires T1 (index 0), which defines to7 at $8040 in bank 6 and puts jmp start7 at $8047 in bank 7. It does the same stores in the same order, A = bank 7 at start7 for wrsel, and sei is already in force. 24->3 = 21 bytes. It builds with BANKFIX back to $10B. Apply only after T1.

## r065:5  modelb/src/init.s:51-61  (window i0027)  M -0 B -6  [medium]  review: reject

Cycles: Boot only, once. Old copy loop ~130 cy; new 3 stores ~21 cy. Faster.

Assumption: FALLBACK: only if T1/T2 are NOT applied (same lines as T2). The entry's stub is still intact at $0100-$0109 when this runs: the entry copied it (lines 13-18); since then start6 did sei/ldx #$3F/txs and pushed nothing, and its zeroing starts at $0114 (with Z) or $0140 (without). The stub layout is A9 bb / 85 F4 / 8D 30 FE / 4C lo hi (listing: ROMSEL_CPY=$F4 zero page), so the bank is at $0101 and the jmp operand at $0108/$0109. Suggest adding `.assert @stubend - @stub = 10, error, "init.s's to7 patches the stub"` after line 23 (entry's scope) to pin that. bankimm at its new place is still in bank 6's image (BANK_MAP), still patched by the loader. Built and assembled in scratch (MAPLO $3E->$38; with Z $2D).

```
        ldx #@to7end-@to7-1         ; the switch to bank 7 runs from the stack page,
:       lda @to7,x                  ; as the entry's did: a bank cannot page itself out
        sta $0100,x
        dex
        bpl :-
        jmp $0100
@to7:   bankimm lda, BANK_LVL, BANK_MAP
        sta ROMSEL_CPY
        sta ROMSEL
        jmp start7
@to7end:
```
->
```
        bankimm lda, BANK_LVL, BANK_MAP ; the switch to bank 7: the entry's stub is still
        sta $0101                   ; at $0100 (lda #bank / sta $F4 / sta ROMSEL / jmp):
        lda #<start7                ; its bank and its target become bank 7's
        sta $0108
        lda #>start7
        sta $0109
        jmp $0100
```

Instead of copying a second stub over the first, rewrite the first stub's bank byte and jmp target: $0100 then reads lda #bank7 / sta $F4 / sta ROMSEL / jmp start7, byte-for-byte what @to7 was. A at start7 = bank 7 as before; X differs (old $FF, new unchanged $FA) but start7 reloads X (ldx #4) before using it and wrsel uses only A.

Review: This is an alternative to T2 (index 4) for the same lines, and it is WRONG once T1 (index 0) is applied: T1 stops copying the entry stub to $0100, so patching $0101/$0108/$0109 and jumping to $0100 would run garbage. It is correct only if T1 is dropped (layout A9 bb 85 F4 8D 30 FE 4C lo hi, 6 bytes saved), so keep it as a fallback for that case only.

## r065:6  modelb/src/init.s:92-92  (window i0028)  M -0 B -2  [high]  review: accept

Cycles: Model B: stz zp = lda #0 (2) + sta zp (3) = 5 -> dec zp = 5. Equal.

Assumption: curbuf is zero page (src/engine.s:217, in .zeropage; listing: E6/85 zp forms), 0 after start6's zeroing, 1 after line 90's inc; build_sections (modelb/src/display.s:178-) only reads it (ldx/lda curbuf at 179,188,199; its callees @dur/@addr/@wrap/@nfull/@emit/@advance/@lines are internal and never write it; grep of every curbuf write in the tree: none in display.s). A dead after: farjsr = ldx #F_INIT5 / jsr farcall; farcall hands A to init5 (banks.s:160), whose first act is ldx #0 / txa. Flags: old N=0,Z=1 (lda #0), new N=0,Z=1 (1->0); ldx overwrites them anyway. Verified in a scratch copy of the tree (nothing in the repo edited): with T1+Z+T2 and the four LGCCODE candidates all applied, the Model B builds with every .assert passing; LGCENT $18->$0A, MAPLO $3E->$25, LGCCODE $2A0A->$29F6; BANKFIX/WRFIX sizes unchanged; bdiff L0 and L5 lock-step OK over 200 frames, bpixdiff L0 and L6 identical over 200 frames. btitlediff DIFFERS on 7-8/30 samples, but only in the first 8 samples (the title load) and identically for a lone 'dec curbuf' (cycle-neutral, 2 bytes): any byte shift in LGCCODE changes the addresses LDPROG carries and the boot timing, so the gate's B title check cannot be clean for ANY LGCCODE saving -- expected, not a fault.

```
        stz curbuf
```
->
```
        dec curbuf                  ; (1 -> 0: build_sections only reads it)
```

curbuf holds exactly 1 here, so dec leaves 0, the value stz stored. Only difference: A is no longer 0, and A is dead.

Review: curbuf is zp $19, zeroed by start6; the only writers in the tree (engine.s:3151,3596, menu.s:239) are not reached by crtc_init/blank_palette/build_sections, and build_sections only reads it; sei holds. So curbuf is 1 after inc and dec leaves 0. A is dead because farcall passes A to init5, which starts ldx #0/txa. On Model B stz zp is 4 bytes and dec zp is 2, both 5 cycles.

## r065:7  modelb/src/init.s:94-95  (window i0031)  M -0 B -6  [high]  review: accept

Cycles: jsr/rts (12) and the redundant lda #0 (2) removed; every instruction between the two FDC1770_CTL stores (the reset pulse) and before the restore is unchanged, so the controller sees the same cycle spacing. Faster by 14 overall, boot only.

Assumption: TWO-FILE CHANGE: also delete modelb/src/disc.s lines 238-258 (the two comment lines, disc_init: through `@done:  rts`, and the blank line after), i.e. the whole Model B disc_init. Its ONLY caller is init.s:94 (grep: src/engine.s:4394's disc_init is the Master's, main.s:81; nothing else, no tool references it). Both are in LGCCODE (disc.s:236 .segment "LGCCODE"). w_wait (disc.s:227) always returns A=0: its only exit is the fall-through of `and #1 / bne :-`, so the old `lda #0` before `sta w_trk` is redundant. w_trk, dsk_type, dsk_drv, w_wait, FDC1770_* are global symbols in the same assembly. The two ':' leave disc.s (referenced only by disc_init's own beq :+ / bne :++; disc.s's neighbouring anon refs at 228-232 and 265-268 resolve to their own labels) and arrive at the end of init.s, the last include. If the owner of disc.s's region changes disc_init first, re-derive. Verified in a scratch copy of the tree (nothing in the repo edited): with T1+Z+T2 and the four LGCCODE candidates all applied, the Model B builds with every .assert passing; LGCENT $18->$0A, MAPLO $3E->$25, LGCCODE $2A0A->$29F6; BANKFIX/WRFIX sizes unchanged; bdiff L0 and L5 lock-step OK over 200 frames, bpixdiff L0 and L6 identical over 200 frames. btitlediff DIFFERS on 7-8/30 samples, but only in the first 8 samples (the title load) and identically for a lone 'dec curbuf' (cycle-neutral, 2 bytes): any byte shift in LGCCODE changes the addresses LDPROG carries and the boot timing, so the gate's B title check cannot be clean for ANY LGCCODE saving -- expected, not a fault.

```
        jsr disc_init               ; a 1770 board: reset, and the head found
        jmp game_main               ; the title menu loads its overlay and starts the tune
```
->
```
        lda dsk_type                ; a 1770 board: reset, and the head found (the 8271
        beq @go                     ; keeps DFS's state and needs nothing)
        lda #$08                    ; reset held (bit 5 low), FM
        sta FDC1770_CTL
        lda #$28                    ; reset released, FM, the drive
        ldx dsk_drv
        beq :+
        ora #$02
        bne :++
:       ora #$01
:       sta FDC1770_CTL
        lda #$00                    ; restore: track 0
        sta FDC1770_CMD
        jsr w_wait                  ; (returns A = 0)
        sta w_trk
@go:    jmp game_main               ; the title menu loads its overlay and starts the tune
```

start7 called disc_init and then jumped to game_main; disc_init's only exits are its two rts (the beq @done and the end). Inlining its body with both exits becoming `jmp game_main` (via @go) is the same instruction stream minus jsr/rts; the `lda #0` after jsr w_wait is dropped because w_wait returns A=0 (and the Z it sets is not consumed: sta then jmp).

Review: Model B disc_init (disc.s 240-257) has one caller, init.s:94; engine.s:4394 is the Master's. w_wait can only exit with A=0 (and #1 / bne), so lda #0 is redundant. The reset-pulse instruction spacing is unchanged. Deleting disc.s 238-258 moves two local anon labels. I checked disc.s's neighbouring refs (228-232, 265-275) and none crosses them. Scratch build: LGCCODE drops 20 in total (3+9+2+6), matching. Saves 6 bytes (jsr 3, lda #0 2, rts 1). This is a two-file change.

## r066:0  modelb/src/ldprog.s:84-99  (window i0049)  M -0 B -5  [medium]  review: accept

Cycles: readfile path 51->44 (ldx zp 3 + stx abs 4 replace lda/sta/lda/sta 14); readpage entry 41. Model B only (ldprog is a Model B program).

Assumption: ld_dst's low byte is 0 whenever ldprog runs: disc_boot (modelb/src/disc.s:281-282) stores <LDPROG (=$0E00, asserted) before every ldprog run; read_sectors writes only ld_dst+1 (disc.s:197); the only other writer is ldprog's readfile. Every readfile destination is page-aligned: STAGE_LVL=$5C00 (lv_load), STAGE=$1C00 (stage), BARADDR=$0300 (bar), from modelb/build/defs_ld.inc. X is dead on entry: readfile's own tax overwrites it (only the new ldx/stx precede it).

```
readfile:
        sta tmp
        asl
        adc tmp                     ; * 3
        tax
        lda ftab,x
        sta ld_sec
        lda ftab+1,x
        sta ld_sec+1
        lda ftab+2,x
        sta ld_n
        lda dst
        sta ld_dst
        lda dst+1
        sta ld_dst+1
        jmp read_sectors
```
->
```
readfile:                           ; file A -> dst (page-aligned: its low byte is not read)
        ldx dst+1
readpage:                           ; file A -> page X (main RAM)
        stx ld_dst+1
        sta tmp
        asl
        adc tmp                     ; * 3
        tax
        lda ftab,x
        sta ld_sec
        lda ftab+1,x
        sta ld_sec+1
        lda ftab+2,x
        sta ld_n                    ; (ld_dst's low byte: 0 from disc_boot, and never
        .assert <LDPROG = 0, error, "ld_dst's low byte is 0"
        jmp read_sectors            ;  changed -- every destination is a page)
```

Adds the page entry readpage (X = page -> ld_dst+1); readfile keeps its contract for callers that set dst (ldx dst+1 then falls in). The ld_dst low-byte copy goes: it is always 0 (assumption). A is untouched by ldx/stx, so sta tmp/asl/adc tmp (C=0 from asl, file < 24) is unchanged. Enables the lvreadpage, stage and bar candidates, which make the readfile entry dead (a further 2 bytes once all three land: delete 'readfile:' + 'ldx dst+1'). Fallback if the invariant is not wanted: keep 'lda #0 / sta ld_dst' before the jmp (0 bytes alone, still enables the rest).

Review: Lines match. ld_dst ($B315, bank-7 LGCBSS) low byte is set to <LDPROG=0 by disc_boot before every ldprog run (ld_begin -> disc_boot); read_sectors writes only ld_dst+1; no ldprog copy targets $B315. Every readfile caller passes a page (STAGE_LVL $5C00, STAGE $1C00, BARADDR $0300). A untouched by ldx/stx; X dead (tax). 10 bytes out, 5 in.

## r066:1  modelb/src/ldprog.s:104-107  (window i0064)  M -0 B -1  [high]  review: accept

Cycles: tay 2 + ldx abs,y 4 = 6 vs tax 2 + lda abs,x 4 + tax 2 = 8. Model B only.

Assumption: Y is dead after plcopy's load: bcopy (falls in) does ldy #0 before any Y use, and wrx/pgx do not read Y (ldprog.s:109-154). A is free on entry to bcopy (its comment, and bcopy reloads A in its loops).

```
        lda (lp),y
        tax
        lda PBANK-4,x
        tax
```
->
```
        lda (lp),y
        tay
        ldx PBANK-4,y               ; (Y: bcopy reloads it)
```

X = PBANK-4[(lp),1] either way; the index goes via Y instead of X, so the second tax disappears. ldx abs,y exists on NMOS 6502.

Review: Y dead: bcopy/pgx/wrx never read Y before ldy #0. PBANK-4=$0148 abs; ldx abs,y is NMOS. 5->4 bytes, 8->6 cycles.

## r066:2  modelb/src/ldprog.s:109-146  (window i0069)  M -0 B -23  [high]  review: accept

Cycles: bcopy entry: stx zp 3 + stx abs 4 + jsr 6 = 13 before wrx -> jsr 6 + stx 3 + stx 4 = 13, same return (wrx's rts). bcopy exit: lda 4 + jsr 6 + pgbank ~39 + rts 6 -> lda 4 + tax 2 + stx 3 + stx 4, wrx returns straight to bcopy's caller (much faster). Every pgbank call: 20+ cycles faster. Not timing code (interrupts off, 'Not hot').

Assumption: pgbank's contract changes from (A, X kept) to (A, X lost, Y kept). Every pgbank call site reloads A before use and never reads X before reloading it: ldprog.s:325,337,409(pla),422,505,529,543,550,560,696,735 (rts -> unrle's caller reloads at 251), and bcopy's callers (179,201,208,216,290,376,407,451,470,596,755 all load A/X/Y before use; grep of every X read in ldprog.s: 88-93,105-107,121,128,151,153,158,192,225,235,701-703 -- each preceded by its own load). title_load's final jmp bcopy returns to disc.s load_title_b -> ld_resume (lda #LDR7, no X read) and its callers (game.s ensure_menu -> inc title_res / t_winlose / FARSUB ldx; menu.s load_title via farcall's fcret, which reloads X). lv_load ends in readfile -> read_sectors. No caller consumes pgbank's flags (each is followed by a load or pla). Nothing inspects the stack (the old pgbank's tsx/$0102 was its own pushes).

```
bcopy:  stx ROMSEL_CPY
        stx ROMSEL
        jsr wrx                     ; the write bank too (A is free here)
        ldy #0
        ldx cnt+1
        beq @tail
@page:  lda (src),y
        sta (dst),y
        iny
        bne @page
        inc src+1
        inc dst+1
        dex
        bne @page
@tail:  ldx cnt
        beq @done
:       lda (src),y
        sta (dst),y
        iny
        dex
        bne :-
@done:  lda PB_LVL
        jsr pgbank
        rts

pgbank: sta ROMSEL_CPY              ; A = the socket to page (and to write to)
        sta ROMSEL
        pha
        txa
        pha
        tsx
        lda $0102,x                 ; the socket again
        tax
        jsr wrx
        pla
        tax
        pla
        rts
```
->
```
bcopy:  jsr pgx                     ; bank X in, the write bank too (A is free here)
        ldy #0
        ldx cnt+1
        beq @tail
@page:  lda (src),y
        sta (dst),y
        iny
        bne @page
        inc src+1
        inc dst+1
        dex
        bne @page
@tail:  ldx cnt
        beq @done
:       lda (src),y
        sta (dst),y
        iny
        dex
        bne :-
@done:  lda PB_LVL                  ; (on into pgbank)

pgbank: tax                         ; A = the socket to page (and to write to): A, X lost, Y kept
pgx:    stx ROMSEL_CPY              ; X = the socket (kept)
        stx ROMSEL
```

Old pgbank: page A, push A/X, fetch A back into X, jsr wrx, restore. New: tax, then pgx stores X to ROMSEL_CPY and ROMSEL (same values as sta A) and falls into wrx, which uses X as before and rts's to the caller. bcopy's 'stx/stx/jsr wrx' is exactly pgx, so it becomes jsr pgx. bcopy's tail 'lda PB_LVL / jsr pgbank / rts' falls straight into pgbank (the next line). wrx preserves X (it only reads it), so X after bcopy's jsr pgx is still the socket, and the loops reload X/Y anyway.

Review: pgbank now loses A and X (Y kept): every call site (324,336,408,420,504,528,542,549,559,695,734) reloads A/X or pla; bcopy callers never read A/X/flags after; title_load tail returns via load_title_b -> ld_resume (lda #LDR7, no X read), and ensure_menu/load_title callers reload. bcopy entry identical (jsr pgx = stx/stx then wrx's rts). pgx falls into wrx (line 147 follows). Label scopes unchanged. 8+7+20 -> 3+3+6 = 23 bytes. Assembled all candidates together: 3207 -> 3132 bytes, consistent.

## r066:3  modelb/src/ldprog.s:149-150  (window i0105)  M -0 B -1  [high]  review: accept

Cycles: lsr 2 = cmp #imm 2; bcc/beq same taken/untaken costs: Watford 17->17, Solidisk same. Model B only.

Assumption: PBOARD is 0, 1 or 2 only (BOARD_STD/WATFORD/SOLIDISK, modelb/src/defs.inc:89-91; set only by loader.s's board choice, loader.s:353-375). The Watford register ignores the value stored (comment at ldprog.s:151 and loader.s:322; engine.s:4086 stores an arbitrary A). A and flags after wrx are dead (see pgbank candidate: every caller reloads).

```
        cmp #BOARD_SOLIDISK
        beq @s
```
->
```
        lsr                         ; 1 (Watford) -> C set, 2 (Solidisk) -> C clear
        bcc @s
```

After beq @r, A is 1 or 2. lsr: 1 -> A=0, C=1 (falls to the Watford store, which now writes 0 rather than 1: value ignored); 2 -> A=1, C=0 -> @s (Solidisk, stores X as before).

Review: PBOARD in {0,1,2} (loader board -> dsk_board -> PBOARD). A=1 -> lsr gives C=1, falls to the Watford store (value ignored); A=2 -> C=0 -> @s. A/flags after wrx dead. 4->3 bytes, same cycles.

## r066:4  modelb/src/ldprog.s:161-165  (window i0113)  M -0 B -6  [high]  review: accept

Cycles: ldx#2+stx3+ldx#2+stx3+jsr6 +readfile's ldx dst+1 3 +stx 4 -> ldx#2+jsr6 +stx 4: 23 -> 12. Model B only.

Assumption: DEPENDS on the readpage candidate (label readpage). dst is not read after readfile returns: section (607-614) writes src only, and hdrY0 / the original 173-176 set dst before bcopy. STAGE_LVL = $5C00 (asserted).

```
        ldx #<STAGE_LVL
        stx dst
        ldx #>STAGE_LVL
        stx dst+1
        jsr readfile
```
->
```
        .assert <STAGE_LVL = 0, error, "readpage takes a page"
        ldx #>STAGE_LVL
        jsr readpage
```

readpage takes the page in X; ld_dst's low byte is 0 (readpage assumption). A = file index untouched by ldx.

Review: Depends on #0. dst not read before 173-176 sets it (section writes src only). A index kept; C=0 after asl (A<24). 11->5 bytes.

## r066:5  modelb/src/ldprog.s:171-174  (window i0121)  M -0 B -4  [high]  review: accept

Cycles: sty zp 3 + sty zp 3 = 6 vs lda#2+sta3+lda#2+sta3 = 10. Model B only.

Assumption: Y = 0 here: 'lda #0 / jsr section' (168) and section does asl/tay then never touches Y (ldprog.s:605-615). A is reloaded by the lda #>LV_HDR that follows; flags: next consumer is none (sta, ldx, jsr). LV_HDR = $8500 (asserted).

```
        lda #0
        sta cnt+1
        lda #<LV_HDR
        sta dst
```
->
```
        sty cnt+1                   ; Y = 0: section 0's index
        .assert <LV_HDR = 0, error, "dst's low byte is Y's 0"
        sty dst
```

cnt+1 <- 0 and dst <- <LV_HDR = 0 stored from Y instead of A; A's later value comes from lda #>LV_HDR as before.

Review: Y=0 after lda #0/jsr section (asl/tay; section never changes Y). A reloaded by lda #>LV_HDR; no flags consumed. 8->4 bytes.

## r066:6  modelb/src/ldprog.s:181-198  (window i0127)  M -0 B -10  [high]  review: accept

Cycles: nobj=0: 25 -> 20. Per iteration: lda zp 3 and sta zp 3 removed (-6); exit adds sta cnt 3 once; so every path is faster. Model B only.

Assumption: cnt+1 is 0 on entry: stored 0 at 171-172 (original or hdrY0), and bcopy only reads cnt (ldprog.s:113,123), section writes src only. dst's low byte is 0: stored at 173-174 (original <LV_HDR or hdrY0's sty), bcopy changes only dst+1 (inc dst+1 at 120), nothing else between writes dst. LV_OBJS = $7800 (asserted). A at @objdone is the running sum on both paths (0 when beq taken).

```
        lda #0
        sta cnt
        sta cnt+1
        ldx LV_HDR+6
        beq @objdone
:       lda cnt
        clc
        adc #6
        sta cnt
        bcc :+
        inc cnt+1
:       dex
        bne :--
@objdone:
        lda #<LV_OBJS               ; main RAM (level_init reads them once, before
        sta dst                     ; the first render)
        lda #>LV_OBJS
        sta dst+1
```
->
```
        lda #0                      ; (cnt+1 is 0 still: the header's copy set it)
        ldx LV_HDR+6
        beq @objdone
:       clc
        adc #6
        bcc :+
        inc cnt+1
:       dex
        bne :--
@objdone:
        sta cnt
        .assert <LV_OBJS = 0, error, "dst's low byte is 0 still"
        lda #>LV_OBJS               ; main RAM (level_init reads them once, before
        sta dst+1                   ; the first render)
```

The 6*nobj sum is kept in A instead of being reloaded/stored in cnt each pass; clc/adc #6/bcc/inc cnt+1 unchanged; the ':' count is unchanged (':' on clc replaces ':' on lda cnt; bcc :+ -> dex, bne :-- -> clc). sta cnt at @objdone stores the final low byte (0 on the beq path, as before). The dst low store is dropped because dst low is already 0.

Review: cnt+1=0 from 172 (bcopy only reads cnt); dst low 0 from 173-174 (the header bcopy with cnt=32 leaves dst low alone). Anon count unchanged (clc/dex carry the ':'): bcc :+ -> dex, bne :-- -> clc. A holds the running sum, 0 on the beq path. 33->23 bytes, faster on every path.

## r066:7  modelb/src/ldprog.s:203-204  (window i0127)  M -0 B -4  [high]  review: accept

Cycles: -5 cycles (lda#/sta zp removed). Model B only.

Assumption: dst's low byte is 0 here: set at 173-174 and only dst+1 is changed by bcopy (120) since; section writes src only. LV_ATTR0 = $8300 (asserted). A is reloaded by lda #>LV_ATTR0.

```
        lda #<LV_ATTR0
        sta dst
```
->
```
        .assert <LV_ATTR0 = 0, error, "dst's low byte is 0 still"
```

The removed store wrote 0 into a byte already 0; the removed lda's value is overwritten by the next lda before use.

Review: dst low is still 0: the objs bcopy only incs dst+1; section writes src. The removed lda/sta stored 0 into a byte already 0.

## r066:8  modelb/src/ldprog.s:210-211  (window i0127)  M -0 B -4  [high]  review: accept

Cycles: -5 cycles. Model B only.

Assumption: dst's low byte is 0: as attr0, plus copy256 -> bcopy (256 bytes: one page pass incs dst+1; tail skipped) leaves dst low unchanged. LV_ALTCLS = $8400 (asserted). Outside this region's windows (i0159); reported because it follows from the same invariant.

```
        lda #<LV_ALTCLS
        sta dst
```
->
```
        .assert <LV_ALTCLS = 0, error, "dst's low byte is 0 still"
```

Store of 0 into a byte already 0 removed.

Review: dst low is still 0 after copy256 (bcopy only incs dst+1).

## r066:9  modelb/src/ldprog.s:245-246  (window i0127)  M -0 B -4  [high]  review: accept

Cycles: -5 cycles. Model B only.

Assumption: dst's low byte is 0: as altcls; lines 215-244 write mapshr, MAPSTRIDE, sprtab, src only (X loops, section 6). MAP6 = $8800 (asserted). Outside this region's windows; same invariant.

```
        lda #<MAP6
        sta dst
```
->
```
        .assert <MAP6 = 0, error, "dst's low byte is 0 still"
```

Store of 0 into a byte already 0 removed.

Review: dst low is 0: lines 215-244 write only mapshr $01FC, MAPSTRIDE $01FD, sprtab $01FA and src, none of which overlap $AA.

## r066:10  modelb/src/ldprog.s:596-602  (window i0049)  M -0 B -7  [high]  review: accept

Cycles: lda#2+sta3+lda#2+sta3+lda#2+jsr6+rts6 + readfile ldx/stx 7 -> ldx#2+lda#2+jmp3 + stx 4: faster. Model B only.

Assumption: DEPENDS on readpage. dst is dead after (it is lv_load's last act). read_sectors does not inspect the stack (plain rts return). The rts at 602 is not a branch target (no label). BARADDR = $0300 (asserted). Outside this region's windows.

```
        lda #<BARADDR
        sta dst
        lda #>BARADDR
        sta dst+1
        lda #FI_BAR
        jsr readfile
        rts                         ; (the tile addresses are arithmetic: drawrect's gather)
```
->
```
        .assert <BARADDR = 0, error, "readpage takes a page"
        ldx #>BARADDR
        lda #FI_BAR
        jmp readpage                ; (the tile addresses are arithmetic: drawrect's gather)
```

readpage with X = the page; jsr/rts -> jmp tail call.

Review: Depends on #0. Tail call: read_sectors ends in a plain rts and does not inspect the stack; the rts at 602 has no label; dst ($AA transient temp) is dead after lv_load. 14->7 bytes.

## r066:11  modelb/src/ldprog.s:624-628  (window i0049)  M -0 B -6  [high]  review: accept

Cycles: ldx#2+stx3+ldx#2+stx3+jmp3 +ldx3+stx4 -> ldx#2+jmp3+stx4: faster. Model B only.

Assumption: DEPENDS on readpage. dst is not read after stage: every caller (254, @sfile 425, title_load 739/756) sets dst before its next use (263-266, 445-449/461-465, 743-746/760-763). STAGE = $1C00 (asserted). Outside this region's windows.

```
        ldx #<STAGE
        stx dst
        ldx #>STAGE
        stx dst+1
        jmp readfile
```
->
```
        .assert <STAGE = 0, error, "readpage takes a page"
        ldx #>STAGE
        jmp readpage
```

readpage with X = the page; the dst copy goes.

Review: Depends on #0. Every stage caller (254, 425, 740, 756) sets dst (263-266, 446-449/464-467, 745-748, 761-764) before any dst read. 11->5 bytes.

## r067:0  modelb/src/ldprog.s:181-198  (window i0135)  M -0 B -8  [high]  review: accept

Cycles: nobj=0: 25->20; per object iteration 15/16->9/10 (lda cnt, sta cnt gone); exit adds sta cnt 3 but drops lda #<LV_OBJS 2 + sta dst 3 -> stx dst 3; every path faster (Model B only: ldprog is a separate 6502 program)

Assumption: cnt+1 = 0 on entry: set by lines 171-172 (lda #0/sta cnt+1), and neither bcopy (reads cnt/cnt+1 only, lines 112-132) nor section (lines 605-615) writes it. X = 0 at @objdone: beq path has X=0, loop exits on dex->0. <LV_OBJS = 0 (defs_ld.inc LV_OBJS = $7800), guarded by the .assert. Nothing after @objdone reads A/flags before lda #>LV_OBJS.

```
        lda #0
        sta cnt
        sta cnt+1
        ldx LV_HDR+6
        beq @objdone
:       lda cnt
        clc
        adc #6
        sta cnt
        bcc :+
        inc cnt+1
:       dex
        bne :--
@objdone:
        lda #<LV_OBJS               ; main RAM (level_init reads them once, before
        sta dst                     ; the first render)
        lda #>LV_OBJS
        sta dst+1
```
->
```
        lda #0                      ; (cnt+1 is 0 still: the header's copy set it)
        ldx LV_HDR+6
        beq @objdone
:       clc
        adc #6
        bcc :+
        inc cnt+1
:       dex
        bne :--
@objdone:
        sta cnt
        stx dst                     ; X = 0 = <LV_OBJS (main RAM: level_init reads
        lda #>LV_OBJS               ; them once, before the first render)
        sta dst+1
        .assert <LV_OBJS = 0, error, "stx dst wants LV_OBJS page-aligned"
```

A carries the running total instead of cnt: A=0 matches cnt=0; clc/adc #6 on A then the loop's stores become one sta cnt at @objdone (A=0 on the beq path). sta cnt+1 dropped as cnt+1 already 0. lda #<LV_OBJS/sta dst -> stx dst with X=0. Anonymous label count unchanged (two ':' kept in the same order). Verified: py65 run of lv_load for all 16 levels, PBOARD 0/1/2, banks 4-7 and main RAM identical.

Review: Original matches the file exactly. cnt+1 is still 0 on entry: lines 171-172 set it, and neither bcopy nor section writes cnt. A carries the running sum, and a single sta cnt at @objdone stores it (A = 0 on the beq path). X = 0 on both paths, which equals <LV_OBJS ($7800). 33 -> 25 bytes, and every path is faster. Checked by assembling all 12 candidates together (LDPROG 3207 -> 3145) and by a py65 run of lv_load for all 16 levels: banks and RAM identical, fewer cycles.

## r067:1  modelb/src/ldprog.s:203-204  (window i0154)  M -0 B -2  [high]  review: accept

Cycles: 5->3 (Model B only: ldprog is a separate 6502 program)

Assumption: X = 0: the objects' jsr bcopy (line 200) returns X=0 (tail loop dex to 0 or beq with cnt=0; pgbank restores X), and section (line 202) does not touch X. <LV_ATTR0 = 0 (defs_ld.inc $8300), guarded by .assert.

```
        lda #<LV_ATTR0
        sta dst
```
->
```
        stx dst                     ; X = 0 (bcopy's exit) = <LV_ATTR0
        .assert <LV_ATTR0 = 0, error, "stx dst wants LV_ATTR0 page-aligned"
```

lda #0/sta dst -> stx dst with X = 0; A is reloaded by lda #>LV_ATTR0 next.

Review: X = 0 after bcopy: the tail loop runs X down to 0, and pgbank restores X (wrx keeps it). section(2) does not touch X. <LV_ATTR0 ($8300) is 0. Store to zp dst: 4 -> 2 bytes, 5 -> 3 cycles. Checked by assembly and simulation of all 16 levels.

## r067:2  modelb/src/ldprog.s:208-209  (window i0159)  M -0 B -5  [medium]  review: accept

Cycles: drops lda #3 + jsr section (2+6+~30 cycles): faster (Model B only: ldprog is a separate 6502 program)

Assumption: copy256 (bcopy with cnt=0,cnt+1=1) leaves src = section 2 start + $100 (one inc src+1 in @page, src lo untouched). The level file's section 3 starts exactly 256 bytes after section 2: tools/assets.py:367 attr = bytearray(256) fixed size, secs order hdr,objs,attr,altcls (line 467) and offsets are contiguous; checked in all 16 built level files (off3-off2 = 256). A/Y after section unused: copy256 does lda #0 first.

```
        lda #3
        jsr section
```
->
```
                                    ; (src: the copy left it at section 3, which
                                    ;  follows the 256-byte attr in the file)
```

section(3) would recompute src = STAGE_LVL + off3 = STAGE_LVL + off2 + 256, the value src already holds; dst unaffected by section. Depends on the packer's layout (data), hence medium.

Review: After copy256, src = section 2 start + $100. The packer (assets.py:367 and 467-475) writes attr as a fixed 256-byte section with altcls directly after it in a contiguous body. off3 - off2 = 256 in all 16 built level files. This rests on the packer's layout, not on the code. A and Y from section are unused: copy256 reloads A and bcopy does ldy #0. Faster. Simulation identical on all levels.

## r067:3  modelb/src/ldprog.s:210-213  (window i0161)  M -0 B -8  [high]  review: accept

Cycles: drops 10 cycles (Model B only: ldprog is a separate 6502 program)

Assumption: LV_ALTCLS = LV_ATTR0 + $100 (defs_ld.inc $8400 / $8300), guarded by .assert. copy256 on LV_ATTR0 leaves dst = LV_ATTR0 + $100 (bcopy @page incs dst+1 once, dst lo untouched; nothing between writes dst).

```
        lda #<LV_ALTCLS
        sta dst
        lda #>LV_ALTCLS
        sta dst+1
```
->
```
        .assert LV_ALTCLS = LV_ATTR0 + $100, error, "copy256 leaves dst at LV_ALTCLS"
```

The four instructions store the value dst already holds. A is reloaded (copy256 lda #0).

Review: LV_ALTCLS ($8400) = LV_ATTR0 + $100, and the .assert guards it. copy256 leaves dst lo at 0 and increments dst+1 once. section does not write dst. Saves 8 bytes (zp stores) and is faster.

## r067:4  modelb/src/ldprog.s:216-226  (window i0166)  M -0 B -4  [high]  review: accept

Cycles: setup lda#/sta/lda#/sta (12) -> stx (4) + trailing sta MAPSTRIDE (4); per shift asl abs 6 -> asl A 2; faster on all paths (Model B only: ldprog is a separate 6502 program)

Assumption: X = 0 at line 216: copy256 (line 214) ends in jmp bcopy, which returns X=0. Nothing reads MAPSTRIDE during the loop.

```
        lda LV_HDR+22
        sta mapshr
        ldx LV_HDR                  ; lw: stride = 1 << lw
        lda #1
        sta MAPSTRIDE
        lda #0
        sta MAPSTRIDE+1
:       asl MAPSTRIDE
        rol MAPSTRIDE+1
        dex
        bne :-
```
->
```
        lda LV_HDR+22
        sta mapshr
        stx MAPSTRIDE+1             ; X = 0: copy256 ends in bcopy
        ldx LV_HDR                  ; lw: stride = 1 << lw
        lda #1
:       asl
        rol MAPSTRIDE+1
        dex
        bne :-
        sta MAPSTRIDE
```

The 16-bit shift of 1 is done with the low byte in A (asl A / rol MAPSTRIDE+1) instead of in memory, stored once after; MAPSTRIDE+1 cleared by stx (X=0). Same result for any lw including 0 (256 shifts -> 0). A after is MAPSTRIDE lo instead of 0: lda LV_HDR reloads. X ends 0 as before. Anonymous label count unchanged.

Review: X = 0 after copy256 -> bcopy. asl A / rol MAPSTRIDE+1 gives the same 16-bit result and the same final carry. MAPSTRIDE ($01FD) is absolute. 19 -> 15 bytes. Each loop iteration drops from 17 to 13 cycles, and setup from 12 to 10.

## r067:5  modelb/src/ldprog.s:227-241  (window i0176)  M -0 B -5  [high]  review: accept

Cycles: clc 2 + sec/sbc 4 -> adc # 2; clc 2 dropped; lda #0/sta abs 6 -> stx abs 4; faster (Model B only: ldprog is a separate 6502 program)

Assumption: C = 0 at line 227: the stride loop's last C-setting op is rol MAPSTRIDE+1 (with or without the 216-226 candidate), whose carry out is bit 15 of the 16-bit stride before its last shift = 0 for lw <= 15 (data: lw <= 8). lw+lh < 256 so adc LV_HDR+1 leaves C=0 (data lw+lh <= 13). k = lw+lh-8 in 1..7 (map >= 1K per comment; data k = 2..5) so no asl shifts a 1 out: C = 0 before adc #>MAP6. X = 0 after the dex/bne loop.

```
        lda LV_HDR                  ; the map is 1 << (lw + lh) bytes, at least 1K
        clc
        adc LV_HDR+1
        sec
        sbc #8
        tax
        lda #1
:       asl
        dex
        bne :-
        clc
        adc #>MAP6
        sta sprtab+1
        lda #0
        sta sprtab
```
->
```
        lda LV_HDR                  ; the map is 1 << (lw + lh) bytes, at least 1K
        adc LV_HDR+1                ; (C = 0: the stride's last rol shifted out a 0)
        adc #$F8                    ; - 8 (C = 0: lw + lh < 256)
        tax
        lda #1
:       asl
        dex
        bne :-
        adc #>MAP6                  ; (C = 0: the map is under 32K)
        sta sprtab+1
        stx sprtab                  ; X = 0 from the loop
```

clc removed (C already 0); sec/sbc #8 == clc/adc #$F8 and C is 0; the second clc removed (last asl shifted out 0); lda #0/sta sprtab -> stx sprtab (X=0). A differs only after (was 0): lda #6 reloads. Anonymous label count unchanged.

Review: C = 0 at line 227: the last rol shifts out bit 15 of 1 << (lw-1), and lw is 5..8 in the data. lw + lh <= 13, so adc leaves C = 0, and adc #$F8 then equals sec / sbc #8. k is 2..5, so the last asl shifts out a 0. X = 0 after the loop. sprtab ($01FA) is absolute, so stx abs is the same size as sta abs. A is reloaded by lda #6. Saves 5 bytes and is faster.

## r067:6  modelb/src/ldprog.s:245-246  (window i0192)  M -0 B -2  [high]  review: accept

Cycles: 5->3 (Model B only: ldprog is a separate 6502 program)

Assumption: X = 0: the map-size loop (dex/bne, lines 234-236) exits X=0; stx sprtab / sta sprtab+1 and section (lines 605-615) keep X. <MAP6 = 0 (defs_ld.inc MAP6 = $8800), .assert added.

```
        lda #<MAP6
        sta dst
```
->
```
        stx dst                     ; X = 0 still = <MAP6
        .assert <MAP6 = 0, error, "stx dst wants MAP6 page-aligned"
```

lda #0/sta dst -> stx dst; A reloaded by lda #>MAP6.

Review: X is still 0: the loop exits with X = 0, and neither the sprtab stores nor section changes X. <MAP6 ($8800) is 0. Saves 2 bytes and is faster.

## r067:7  modelb/src/ldprog.s:257-260  (window i0203)  M -0 B -2  [high]  review: accept

Cycles: 12->9 (Model B only: ldprog is a separate 6502 program)

Assumption: section returns with A = src+1 (its last instructions are sta src+1 / rts, line 614-615).

```
        lda src
        sta lp
        lda src+1
        sta lp+1
```
->
```
        sta lp+1                    ; (section returns A = src+1)
        lda src
        sta lp
```

lp+1 stored from A first, then lp from src; A afterwards is src lo instead of src+1, reloaded by lda LV_HDR+21.

Review: section ends with sta src+1 / rts, so A = src+1 on return. 8 -> 6 bytes, 12 -> 9 cycles. A is reloaded by lda LV_HDR+21.

## r067:8  modelb/src/ldprog.s:263-287  (window i0209)  M -0 B -4  [medium]  review: accept

Cycles: pre-loop 12->18 (+6), each tile iteration -10 (lda #64/sta/lda #0/sta hoisted); net faster whenever nt >= 1 (all 16 levels have nt >= 53); only a level with no tiles at all (nt=0) would be 6 cycles slower (Model B only: ldprog is a separate 6502 program)

Assumption: bcopy never writes cnt/cnt+1 (reads them with ldx, lines 113/123), nor do pgbank/wrx; nothing else in the loop writes cnt. Y = 0 after ldy #0. <TILES = 0 (defs_ld.inc TILES = $8100), .assert added. nt = LV_HDR+21 >= 1 for the cycle claim (data: 53..193).

```
        lda #<TILES
        sta dst
        lda #>TILES
        sta dst+1
        ldy #0
@tile:  cpy nt
        beq @tiles_done
        sty tmp2
        lda (lp),y                  ; the tile's index in the set: * 64 from STAGE
        pha
        and #3
        lsr
        ror
        ror                         ; (t & 3) << 6
        sta src
        pla
        lsr
        lsr
        clc
        adc #>STAGE
        sta src+1
        lda #64
        sta cnt
        lda #0
        sta cnt+1
```
->
```
        lda #>TILES
        sta dst+1
        ldy #0
        sty dst                     ; <TILES = 0
        .assert <TILES = 0, error, "sty dst wants TILES page-aligned"
        sty cnt+1                   ; 64 bytes a tile: bcopy leaves cnt alone
        lda #64
        sta cnt
@tile:  cpy nt
        beq @tiles_done
        sty tmp2
        lda (lp),y                  ; the tile's index in the set: * 64 from STAGE
        pha
        and #3
        lsr
        ror
        ror                         ; (t & 3) << 6
        sta src
        pla
        lsr
        lsr
        clc
        adc #>STAGE
        sta src+1
```

cnt=64/cnt+1=0 are loop-invariant, so set once before @tile (cnt+1 and dst lo from Y=0 via sty). Loop body otherwise unchanged; @tile/@tiles_done labels unchanged; no anonymous labels touched.

Review: cnt and cnt+1 do not change across the loop: bcopy, pgbank and wrx only read cnt. <TILES ($8100) is 0 and Y = 0. Saves 4 bytes. Setup before the loop costs 6 more cycles, but each tile saves 10. nt is 53..193 on every level, and a level with no tiles cannot exist, so every path that runs is faster.

## r067:9  modelb/src/ldprog.s:289-296  (window i0235)  M -0 B -1  [high]  review: accept

Cycles: ldy3+lda3+clc2+adc#2+sta3 = 13 -> tya2+clc2+adc zp3+sta3+ldy3 = 13; branch unchanged (Model B only: ldprog is a separate 6502 program)

Assumption: bcopy returns Y = cnt = 64 here: cnt+1 = 0 skips @page, the @tail loop runs Y 0->64, pgbank/wrx never touch Y. ldy does not affect C.

```
        jsr bcopy                   ; (dst runs on by 64 itself: bcopy's tail loop
        ldy tmp2                    ;  leaves dst where it started -- step it)
        lda dst
        clc
        adc #64
        sta dst
        bcc :+
        inc dst+1
```
->
```
        jsr bcopy                   ; (bcopy leaves dst where it started, and
        tya                         ;  Y = cnt = 64: step dst by it)
        clc
        adc dst
        sta dst
        ldy tmp2
        bcc :+
        inc dst+1
```

A = 64 from tya instead of the immediate; clc/adc dst/sta dst gives the same sum and carry; ldy tmp2 moved after the store but before bcc (preserves C); iny at ':' sees Y = tmp2 on both paths. Anonymous labels unchanged.

Review: bcopy with cnt = 64 and cnt+1 = 0 returns Y = 64, since pgbank and wrx never touch Y. tya / clc / adc dst gives the same sum and carry, and ldy does not change C before bcc. 9 -> 8 bytes, 13 -> 13 cycles. Still holds with index 8 applied (cnt is still 64).

## r067:10  modelb/src/ldprog.s:304-307  (window i0246)  M -0 B -2  [high]  review: accept

Cycles: 12->9 (Model B only: ldprog is a separate 6502 program)

Assumption: section returns A = src+1 (lines 614-615).

```
        lda src
        sta lp
        lda src+1
        sta lp+1
```
->
```
        sta lp+1                    ; (section returns A = src+1)
        lda src
        sta lp
```

Same as the tiles' list pointer: sta lp+1 first from A; A then src lo, reloaded by lda LV_HDR+23.

Review: Same as index 7, after the section(8) call at line 303. Saves 2 bytes and is faster.

## r067:11  modelb/src/ldprog.s:308-345  (window i0250)  M -0 B -19  [high]  review: accept

Cycles: straight line: 117 -> 98 cycles excluding the two jsr pgbank (unchanged) (Model B only: ldprog is a separate 6502 program)

Assumption: pgbank keeps X and Y (lines 134-146: X saved/restored via stack, Y untouched; wrx uses A/X only) and its $0102,x read is relative to its own pushes, so 2 extra bytes on the stack are harmless. tmp, tmp2, cnt, cnt+1 dead after: the half loop sets tmp2 (349) before ldy tmp2, cnt/cnt+1 (370-373) before bcopy; @halves_done sets tmp (391) and cnt (401-404) before reading. X and Y dead: ldy #0 at 346, ldx PB_TILES in the loop, section at @halves_done sets Y. dst+1 is not written between here and 345 (pgbank does not touch zp), so storing LV_HDR+27 there early gives the same dst+1 as lines 344-345.

```
        lda LV_HDR+23               ; the count (two list bytes a half), the ids' ranges
        asl                         ; and the halves' page, the packer's, into bank 5's
        sta nt                      ; variables
        lda LV_HDR+24               ; (read with bank 7 in: the header is its)
        sta tmp
        lda LV_HDR+25
        sta tmp2
        lda LV_HDR+26
        sta cnt
        lda LV_HDR+27
        sta cnt+1
        lda LV_HDR+24               ; the gather's subtraction: half0 less the first
        sec                         ; half's slot in its page (the halves follow the
        sbc LV_HDR+28               ; full tiles at once, not from the next page)
        sta dst                     ; (dst: free until set below)
        lda PB_TILES
        jsr pgbank
        lda tmp
        sta half0
        lda tmp2
        sta half1
        lda cnt
        sta half2
        lda cnt+1
        sta halfhi
        lda dst
        sta halfsub
        lda PB_LVL
        jsr pgbank
        lda LV_HDR+28               ; the halves start slot HALFOFF into their page
        asl
        asl
        asl
        asl
        asl
        sta dst
        lda LV_HDR+27
        sta dst+1
```
->
```
        lda LV_HDR+23               ; the count (two list bytes a half), the ids' ranges
        asl                         ; and the halves' page, the packer's, into bank 5's
        sta nt                      ; variables (read with bank 7 in: the header is its)
        lda LV_HDR+26
        pha
        lda LV_HDR+27               ; halfhi: the halves' page, dst+1 for the gather too
        sta dst+1
        ldy LV_HDR+25
        lda LV_HDR+24               ; the gather's subtraction: half0 less the first
        tax                         ; half's slot in its page (the halves follow the
        sec                         ; full tiles at once, not from the next page)
        sbc LV_HDR+28
        pha
        lda PB_TILES
        jsr pgbank                  ; (X, Y kept)
        stx half0
        sty half1
        pla
        sta halfsub
        pla
        sta half2
        lda dst+1
        sta halfhi
        lda PB_LVL
        jsr pgbank
        lda LV_HDR+28               ; the halves start slot HALFOFF into their page
        asl
        asl
        asl
        asl
        asl
        sta dst
```

half0/half1 carried in X/Y across pgbank instead of tmp/tmp2; half2 and halfsub carried on the stack (pha/pla, LIFO order matched); halfhi carried in dst+1, which is where LV_HDR+27 must end up anyway, so lines 344-345 go; LV_HDR+24 read once (tax). Verified by py65 simulation of all 16 levels (identical banks and main RAM).

Review: half0..halfsub are absolute bank-5 variables ($BF50..). Every LV_HDR read happens before pgbank(PB_TILES). pgbank keeps X and Y and indexes only its own pushes. The pha/pla order is correct. dst+1 = LV_HDR+27, the value lines 344-345 set. tmp, tmp2 and cnt are dead: they are written before being read again. The 2 extra stack bytes make the maximum depth here 8, below the existing depth of 10 at the @halves_done bcopy, so the stack-page variables at $01FA-$01FE are not put at new risk. 87 -> 68 bytes, faster. Simulation identical on all 16 levels.

## r068:0  modelb/src/ldprog.s:314-385  (window i0256)  M -0 B -24  [high]  review: accept

Cycles: Stand-alone 6502 (ldprog, Model B only). Pre-loop straight line (314-346 incl. the hoisted cnt setup): 99+2*pgbank -> 95+2*pgbank. Per half-tile iteration: row 0: 44+10+13=67 -> 42+13=55; row 1: 76 -> 55; dst carry path +3 on both. Loop exit (cpy/beq) unchanged. nt=0 path: 99->95. Every path faster.

Assumption: (1) A = LV_HDR+25 at line 314 and tmp = LV_HDR+24 (lines 311-313). (2) pgbank keeps X and Y (pgbank/wrx at ldprog.s:137-156 touch only A, X via stack restore; Y never). (3) tmp2, cnt, cnt+1, dst stash values are dead: tmp2 rewritten by sty at @half, dst set at old 343-345, cnt set before any bcopy; X free until ldx PB_TILES, Y reloaded by ldy #0. (4) bcopy never writes cnt/cnt+1 (reads them only, ldprog.s:108-131), so cnt=32/cnt+1=0 can be set once before @half. (5) After bcopy with cnt+1=0, cnt=32, Y=32 (tail loop from ldy #0; @done's pgbank/wrx keep Y). (6) The row byte is exactly 0 or 1: modelb/tools/assets.py:335 emits `0 if ... else 1`. (7) X free inside the loop body (ldx PB_TILES reloads it before bcopy). (8) C after the row block not consumed (lda #32 gone; ldx/jsr bcopy; bcopy/wrx never read C; clc before the dst add). Assembled in scratch: LDPROG 3207 -> 3183.

```
        sta tmp2
        lda LV_HDR+26
        sta cnt
        lda LV_HDR+27
        sta cnt+1
        lda LV_HDR+24               ; the gather's subtraction: half0 less the first
        sec                         ; half's slot in its page (the halves follow the
        sbc LV_HDR+28               ; full tiles at once, not from the next page)
        sta dst                     ; (dst: free until set below)
        lda PB_TILES
        jsr pgbank
        lda tmp
        sta half0
        lda tmp2
        sta half1
        lda cnt
        sta half2
        lda cnt+1
        sta halfhi
        lda dst
        sta halfsub
        lda PB_LVL
        jsr pgbank
        lda LV_HDR+28               ; the halves start slot HALFOFF into their page
        asl
        asl
        asl
        asl
        asl
        sta dst
        lda LV_HDR+27
        sta dst+1
        ldy #0
@half:  cpy nt
        beq @halves_done
        sty tmp2
        lda (lp),y                  ; the tile's index in the set ...
        pha
        and #3
        lsr
        ror
        ror
        sta src
        pla
        lsr
        lsr
        clc
        adc #>STAGE
        sta src+1
        iny
        lda (lp),y                  ; ... and which of its rows: 0 or 32 on
        beq :+
        lda src
        clc
        adc #32
        sta src
:       lda #32
        sta cnt
        lda #0
        sta cnt+1
        ldx PB_TILES
        jsr bcopy
        ldy tmp2
        lda dst
        clc
        adc #32
        sta dst
        bcc :+
        inc dst+1
:       iny
        iny
        bne @half
```
->
```
        pha                         ; LV_HDR+25 (half1)
        lda LV_HDR+26
        pha                         ; (half2)
        ldy LV_HDR+27               ; (halfhi; pgbank keeps Y)
        lda LV_HDR+24               ; the gather's subtraction: half0 less the first
        sec                         ; half's slot in its page (the halves follow the
        sbc LV_HDR+28               ; full tiles at once, not from the next page)
        tax                         ; (halfsub; pgbank keeps X)
        lda PB_TILES
        jsr pgbank
        lda tmp
        sta half0
        pla
        sta half2
        pla
        sta half1
        sty halfhi
        stx halfsub
        lda PB_LVL
        jsr pgbank
        lda LV_HDR+28               ; the halves start slot HALFOFF into their page
        asl
        asl
        asl
        asl
        asl
        sta dst
        sty dst+1                   ; (Y = LV_HDR+27 still)
        ldy #0
        sty cnt+1                   ; 32 bytes a half (bcopy leaves cnt alone)
        lda #32
        sta cnt
@half:  cpy nt
        beq @halves_done
        sty tmp2
        lda (lp),y                  ; the tile's index in the set ...
        tax
        iny
        lda (lp),y                  ; ... and which of its rows: 0 or 1 (the packer's)
        lsr                         ; C = the row
        txa
        and #3
        ror
        ror
        ror                         ; (t & 3) << 6 | row << 5
        sta src
        txa
        lsr
        lsr
        clc
        adc #>STAGE
        sta src+1
        ldx PB_TILES
        jsr bcopy                   ; (Y = cnt = 32 after: its tail loop's count)
        tya
        clc
        adc dst
        sta dst
        bcc :+
        inc dst+1
:       ldy tmp2
        iny
        iny
        bne @half
```

Staging: LV_HDR+25 and +26 go through the stack (pha/pla, in LIFO order so half2 is stored before half1 -- order of the bank-5 stores is irrelevant), LV_HDR+27 through Y and the halfsub difference through X, both kept by pgbank; Y still holds LV_HDR+27 after the second pgbank so `sty dst+1` replaces the reload. cnt=32/cnt+1=0 hoisted (sty cnt+1 with Y=0). Row block: t kept in X instead of the stack; the row byte (0/1) is read first and lsr puts it in C, so `and #3; ror; ror; ror` gives (t&3)<<6 | row<<5 = the original src (the original added 32 to a value with bit 5 clear when row!=0). src+1 from txa/lsr/lsr/clc/adc as before; Y = index+1 as before. After bcopy, Y=32 so `tya; clc; adc dst` = dst+32 with the same carry; ldy tmp2 moved after the carry fix (Y not used there).

Review: Original matches lines 314-385. At line 314, A = LV_HDR+25 (loaded at 313). pgbank pushes and pulls its own A and X and indexes $0102,x relative to its own pushes, so the two outer pha instructions are safe. pgbank and wrx never touch Y and restore X, so Y = LV_HDR+27 and X = halfsub survive both calls. Y was read with bank 7 paged, the same value as the original's reload. bcopy only reads cnt/cnt+1, so hoisting cnt=32 and cnt+1=0 above @half is sound. On the nt=0 path cnt is left at 32 instead of LV_HDR+26/27, but section(9) never reads cnt and lines 401-404 rewrite it. The row byte is 0 or 1 (assets.py:335). lsr/txa/and #3/ror x3 gives bit7=t1, bit6=t0, bit5=row, which equals the original src. After bcopy with cnt+1=0 and cnt=32, Y = 32: the tail loop starts from ldy #0, and @done's pgbank keeps Y. So tya/clc/adc dst = dst+32 with the same carry. Anonymous labels: the beq :+ and its ':' are removed together, and the other ':' only moves within its bcc span. No reference crosses them (the next anonymous pairs are at 417/419 and 514/518). Assembled: 3207 -> 3183 (-24). Every path is faster.

## r068:1  modelb/src/ldprog.s:389-420  (window i0327)  M -0 B -14  [high]  review: accept

Cycles: Excluding calls: 87 (92 on the HPAIR1 carry path) + bcopy + 2*pgbank -> 67 (68 on the wrap path) + bcopy + 1*pgbank. Faster on every path.

Assumption: (1) HPAIR0/HPAIR1 ($BAAF/$BAB4, B5X) do not overlap the pair table bcopy writes at dst (assets.py:327 asserts the pairs end below B5X). (2) bcopy pages its own bank (stx ROMSEL) whatever is paged on entry and returns with PB_LVL paged via @done, exactly the state the removed `lda PB_LVL; jsr pgbank` produced; X, Y after are bcopy's residue in both versions (the original's pgbank calls kept them). (3) tmp dead after: next readers (readfile, unrle) write it first. (4) All LV_HDR reads still happen before bank 5 is paged. (5) pgbank keeps X, Y. Assembled in scratch: 3207 -> 3193.

```
        lda LV_HDR+28               ; the fill indexes them by the slot from the page,
        asl                         ; two bytes each: the operands sit 2*HALFOFF below
        sta tmp                     ; the table
        lda dst                     ; (dst is where the halves ended)
        sec
        sbc tmp
        pha
        lda dst+1
        sbc #0
        pha
        lda LV_HDR+23               ; two bytes a half
        asl
        sta cnt
        lda #0
        rol
        sta cnt+1
        ldx PB_TILES
        jsr bcopy
        lda PB_TILES
        jsr pgbank
        pla
        sta HPAIR0+1
        sta HPAIR1+1
        pla
        sta HPAIR0
        clc
        adc #1
        sta HPAIR1
        bcc :+
        inc HPAIR1+1
:       lda PB_LVL
        jsr pgbank
```
->
```
        lda LV_HDR+28               ; the fill indexes them by the slot from the page,
        asl                         ; two bytes each: the operands sit 2*HALFOFF below
        eor #$FF                    ; the table (dst is where the halves ended):
        sec                         ; dst - 2*HALFOFF, low in X, high in Y
        adc dst
        tax
        lda dst+1
        sbc #0
        tay
        lda LV_HDR+23               ; two bytes a half
        asl
        sta cnt
        lda #0
        rol
        sta cnt+1
        lda PB_TILES
        jsr pgbank                  ; (X, Y kept)
        stx HPAIR0
        sty HPAIR0+1
        inx
        stx HPAIR1
        bne :+
        iny
:       sty HPAIR1+1
        ldx PB_TILES
        jsr bcopy                   ; (bank 7 back after it)
```

dst - 2H computed as ~(2H) + dst + 1 (eor #$FF / sec / adc dst): same 8-bit result and the same carry (1 = no borrow) into `sbc #0` for the high byte. Low kept in X, high in Y instead of the stack. HPAIR stores moved before the pairs' bcopy (independent memory), so bank 5 is paged once and the trailing PB_LVL page comes free from bcopy's @done. HPAIR1 = low+1: inx sets Z on wrap, the stx/sty in between leave flags alone, iny carries into the high byte -- as clc/adc #1/bcc/inc did.

Review: Original matches lines 389-420. ~(2H)+dst+1 gives dst-2H with the same carry into sbc #0. LV_HDR+23 and +28 are still read before bank 5 is paged. HPAIR0/1 are at $BAAF/$BAB4 in B5X (at or above $B620). assets.py:327 asserts the pair table ends at or below B5X, so the bcopy cannot overwrite the HPAIR stores that now come before it. bcopy re-pages X itself and exits through @done with PB_LVL paged and A = PB_LVL: the same final state and A as the removed pgbank. tmp is no longer written; its next reader is readfile (via stage), which writes tmp first. inx/bne/iny gives HPAIR1 = low+1 exactly as clc/adc #1/bcc/inc did. The ':' count is unchanged. Assembled: -14. Faster on every path.

## r068:2  modelb/src/ldprog.s:424-425  (window i0331)  M -0 B -2  [high]  review: accept

Cycles: Both entries: 3 cycles saved (lda zp gone).

Assumption: @sfile is reached only from line 423 (`lda #0; sta fnum`: A = 0 = fnum) and from `bne @sfile` at line 480 after `lda fnum; cmp #3` (A = fnum); grep shows no other @sfile reference (ldprog.s:424,480).

```
@sfile: lda fnum
        jsr stage
```
->
```
@sfile: jsr stage                   ; (A = fnum both ways in)
```

A already equals fnum on both ways in, so the reload is redundant; stage takes the file number in A.

Review: @sfile has only two ways in. The fall-in after lda #0/sta fnum has A = 0 = fnum. bne @sfile after lda fnum/cmp #3 has A = fnum. On the second path the flags differ (N comes from cmp), but stage starts with ldx, and readfile's asl/adc do not use the entry flags. Assembled: -2.

## r068:3  modelb/src/ldprog.s:438-438  (window i0344)  M -0 B -2  [high]  review: accept

Cycles: 2 cycles saved per placement entry.

Assumption: Y = 0 on entry to imgent (ldy #0 at @pl, line 432; lda/cmp/beq/sta item don't touch Y) and imgent (ldprog.s:629-653) never touches Y (only lda/sta/asl/rol/inc/clc/adc).

```
        ldy #0
```
->
```

```

The ldy #0 re-establishes a value Y already holds.

Review: Y = 0 from the ldy #0 at @pl. lda/cmp/beq/sta item leave Y unchanged, and imgent (lines 629-653) uses only A and memory. The following lda (ent),y sets the flags itself. Assembled: -2.

## r068:4  modelb/src/ldprog.s:476-476  (window i0381)  M -0 B -1  [high]  review: accept

Cycles: jmp 3 -> bne taken 3.

Assumption: lp walks the placement list in STAGE_LVL main RAM ($5C00-$7FFF, defs_ld.inc STAGE_LVL=$5C00), so inc lp+1 never yields 0; bne @pl is in range (the bcc @pl just above reaches the same target, and the assembled file builds).

```
        jmp @pl
```
->
```
        bne @pl                     ; (lp+1 is never 0)
```

After `inc lp+1` Z is clear, so bne is always taken to the same target.

Review: lp is set from section 5 inside STAGE_LVL ($5C00; the level file is under 7K), so inc lp+1 never gives 0. The bne is always taken and costs 3 cycles, the same as the jmp. The branch is in range: it assembled both alone and with all the other candidates. Assembled: -1.

## r068:5  modelb/src/ldprog.s:314-346  (window i0256)  M -0 B -14  [high]  review: accept

Cycles: ALTERNATIVE to the lines-314-385 candidate (overlaps it; apply only if that one is rejected). Straight line: 99+2*pgbank -> 77+2*pgbank.

Assumption: As the 314-385 candidate, items (1)-(3): A = LV_HDR+25 at 314, tmp = LV_HDR+24; pgbank keeps X, Y; tmp2/cnt/cnt+1/dst stash values dead (cnt is re-set inside @half before bcopy).

```
        sta tmp2
        lda LV_HDR+26
        sta cnt
        lda LV_HDR+27
        sta cnt+1
        lda LV_HDR+24               ; the gather's subtraction: half0 less the first
        sec                         ; half's slot in its page (the halves follow the
        sbc LV_HDR+28               ; full tiles at once, not from the next page)
        sta dst                     ; (dst: free until set below)
        lda PB_TILES
        jsr pgbank
        lda tmp
        sta half0
        lda tmp2
        sta half1
        lda cnt
        sta half2
        lda cnt+1
        sta halfhi
        lda dst
        sta halfsub
        lda PB_LVL
        jsr pgbank
        lda LV_HDR+28               ; the halves start slot HALFOFF into their page
        asl
        asl
        asl
        asl
        asl
        sta dst
        lda LV_HDR+27
        sta dst+1
        ldy #0
```
->
```
        pha                         ; LV_HDR+25 (half1)
        lda LV_HDR+26
        pha                         ; (half2)
        ldy LV_HDR+27               ; (halfhi; pgbank keeps Y)
        lda LV_HDR+24               ; the gather's subtraction: half0 less the first
        sec                         ; half's slot in its page (the halves follow the
        sbc LV_HDR+28               ; full tiles at once, not from the next page)
        tax                         ; (halfsub; pgbank keeps X)
        lda PB_TILES
        jsr pgbank
        lda tmp
        sta half0
        pla
        sta half2
        pla
        sta half1
        sty halfhi
        stx halfsub
        lda PB_LVL
        jsr pgbank
        lda LV_HDR+28               ; the halves start slot HALFOFF into their page
        asl
        asl
        asl
        asl
        asl
        sta dst
        sty dst+1                   ; (Y = LV_HDR+27 still)
        ldy #0
```

Staging through stack/X/Y instead of zero page; Y still LV_HDR+27 after the second pgbank so it stores dst+1.

Review: Correct subset of index 0: it stages the values through the stack, X and Y, and uses sty dst+1. In this version cnt is still set inside the loop. Assembled: -14. Overlaps index 0; apply it only if index 0 is not applied.

## r068:6  modelb/src/ldprog.s:350-370  (window i0280)  M -0 B -7  [high]  review: accept

Cycles: ALTERNATIVE (overlaps the 314-385 candidate; apply only if that one is rejected). Lines 350-370 incl. the kept lda #32: row 0 path 46 -> 44, row 1 path 55 -> 44.

Assumption: Row byte is 0 or 1 (modelb/tools/assets.py:335); X free (ldx PB_TILES before bcopy); C afterwards not consumed (lda #32 ... jsr bcopy never reads C).

```
        lda (lp),y                  ; the tile's index in the set ...
        pha
        and #3
        lsr
        ror
        ror
        sta src
        pla
        lsr
        lsr
        clc
        adc #>STAGE
        sta src+1
        iny
        lda (lp),y                  ; ... and which of its rows: 0 or 32 on
        beq :+
        lda src
        clc
        adc #32
        sta src
:       lda #32
```
->
```
        lda (lp),y                  ; the tile's index in the set ...
        tax
        iny
        lda (lp),y                  ; ... and which of its rows: 0 or 1 (the packer's)
        lsr                         ; C = the row
        txa
        and #3
        ror
        ror
        ror                         ; (t & 3) << 6 | row << 5
        sta src
        txa
        lsr
        lsr
        clc
        adc #>STAGE
        sta src+1
        lda #32
```

See the 314-385 candidate: (t&3)<<6 | row<<5 by three rors with the row in C.

Review: Correct subset of index 0. The row byte is 0 or 1 (assets.py:335), and the three rors reproduce src. C is overwritten before it is next used. The beq :+ and its ':' are removed together. Assembled: -7. Overlaps index 0; apply it only if index 0 is not applied.

## r068:7  modelb/src/ldprog.s:375-383  (window i0292)  M -0 B -1  [high]  review: accept

Cycles: ALTERNATIVE (overlaps the 314-385 candidate). ldy3+lda3+clc2+adc2 = 10 -> tya2+clc2+adc3+ldy3 = 10; equal.

Assumption: cnt = 32, cnt+1 = 0 at the bcopy (lines 370-373), so bcopy's tail loop leaves Y = 32 and @done's pgbank/wrx keep Y.

```
        jsr bcopy
        ldy tmp2
        lda dst
        clc
        adc #32
        sta dst
        bcc :+
        inc dst+1
:       iny
```
->
```
        jsr bcopy                   ; (Y = cnt = 32 after: its tail loop's count)
        tya
        clc
        adc dst
        sta dst
        bcc :+
        inc dst+1
:       ldy tmp2
        iny
```

Y (=32) + dst = dst + 32, same carry; Y restored from tmp2 before the two iny.

Review: cnt=32 and cnt+1=0 are set before each bcopy, so Y = 32 after it. tya/clc/adc dst gives dst+32. ldy tmp2 comes after the carry fix, and the ':' only moves within the bcc span. Same cycle count. Assembled: -1. Overlaps index 0; apply it only if index 0 is not applied.

## r069:0  modelb/src/ldprog.s:476-476  (window i0384)  M -0 B -1  [high]  review: accept

Cycles: Model B only (ldprog is a stand-alone 6502 program): inc path jmp 3 -> bne taken 3; other paths unchanged

Assumption: lp+1 is never 0 after inc: lp walks the placement list in the staged level file at STAGE_LVL=$5C00 (defs.inc:99, file under 7K), so lp+1 is in $5C..$7B; findplace (ldprog.s:~676) already relies on the same fact with bne after inc src+1

```
        jmp @pl
```
->
```
        bne @pl                     ; (lp+1: the list is in main RAM, never 0)
```

inc lp+1 leaves Z=0 because the result is nonzero, so bne @pl is always taken exactly like jmp @pl; @pl is -$59 bytes away (in range, assembled)

Review: Original matches line 476. lp = section 5 of the level file staged at STAGE_LVL=$5C00 (under 7K), so inc lp+1 yields $5D..$7B, never 0: bne always taken, 3 cy = jmp 3 cy. Assembled: LDPROG 3207 -> 3206 (branch in range).

## r069:1  modelb/src/ldprog.s:496-559  (window i0431)  M -0 B -28  [high]  review: accept

Cycles: Model B only. P = jsr pgbank incl. rts (~64 cy). Found path, nt<16: old 4+P+3+2+3 +4+P after the flag test = 16+2P; new 18 (ldy/lda/tax/iny/lda/tay) +4+P+3+2+3 = 30+P. Found, nt>=16: old 48+2P, new 52+P. None path (item $FF or findplace C set): old dnone pgbank(PB_MAP) moved before the item test (same count), then +7 (tax/tay/beq) and +5 for nt>=16 (tya..bcs vs lda#0..sta), minus the dropped @dnext pgbank (4+P). ldy #2 -> iny: 2->2. Every path faster by >= P-12; not timing-critical (load-time, display black).

Assumption: (1) The PB_MAP page is only needed for the (dst) stores: everything the @dir loop reads before them is main RAM -- lp=sprdir inside ldprog ($0E00..), item/src/zp, findplace->section reads STAGE_LVL=$5C00 and the placement list there (ldprog.s:605-615, 672-688); pgbank keeps X and Y (ldprog.s:139-151). (2) The PB_LVL page at @dnext was redundant: every path into @dnext already did lda PB_LVL/jsr pgbank (lines 527-528 or 548-549) and nothing between pages anything else (the (ent) stores are to SPRMASK=$BF14 in bank 7, which is why the earlier pgbank(PB_LVL) must stay). (3) X is dead in the loop and after it: nothing in 496-580 reads X, and after the loop section (uses Y) is followed by ldx PB_TILES (line 593). Y is reloaded by ldy #0 at @dir. (4) pgbank returns A=PB_LVL, keeps X, Y (wrx touches only A); C after cmp #16 not taken is 1 and tya/ldy/sta/txa/dey/sta leave C alone, so bcs @dnext is always taken.

```
@dir:   ldy #0
        lda (lp),y
        sta item
        cmp #$FF
        beq @dnone
        jsr findplace               ; src -> the placement entry, or C set
        bcs @dnone
        lda PB_MAP
        jsr pgbank
        ldy #2
        lda (src),y
        ldy #0
        sta (dst),y
        ldy #3
        lda (src),y
        ldy #1
        sta (dst),y
        ldy #2
:       lda (lp),y
        sta (dst),y
        iny
        cpy #8
        bne :-
        ldy #1
        lda (src),y                 ; the bank: bank 6's images carry the flag
        cmp #BANK_MAP
        bne :+
        ldy #6
        lda (lp),y
        ora #$10
        sta (dst),y
:       lda PB_LVL
        jsr pgbank              ; box ids (the last 15) have no entry
        lda nt
        cmp #16
        bcc @dnext
        ldy #4
        lda (src),y
        ldy #0
        sta (ent),y
        ldy #5
        lda (src),y
        ldy #1
        sta (ent),y
        jmp @dnext
@dnone: lda PB_MAP
        jsr pgbank
        lda #0
        ldy #7
:       sta (dst),y
        dey
        bpl :-
        lda PB_LVL
        jsr pgbank
        lda nt
        cmp #16
        bcc @dnext
        lda #0
        tay
        sta (ent),y
        iny
        sta (ent),y
@dnext: lda PB_LVL
        jsr pgbank
```
->
```
@dir:   lda PB_MAP                  ; the directory's bank, for both paths
        jsr pgbank
        ldy #0
        lda (lp),y
        sta item
        cmp #$FF
        beq @dnone
        jsr findplace               ; src -> the placement entry, or C set
        bcs @dnone
        ldy #2
        lda (src),y
        ldy #0
        sta (dst),y
        ldy #3
        lda (src),y
        ldy #1
        sta (dst),y
        iny
:       lda (lp),y
        sta (dst),y
        iny
        cpy #8
        bne :-
        ldy #1
        lda (src),y                 ; the bank: bank 6's images carry the flag
        cmp #BANK_MAP
        bne :+
        ldy #6
        lda (lp),y
        ora #$10
        sta (dst),y
:       ldy #4                      ; the mask: X = lo, Y = hi
        lda (src),y
        tax
        iny
        lda (src),y
        tay
@dmask: lda PB_LVL
        jsr pgbank              ; box ids (the last 15) have no entry
        lda nt
        cmp #16
        bcc @dnext
        tya
        ldy #1
        sta (ent),y
        txa
        dey
        sta (ent),y
        bcs @dnext                  ; (C set: the cmp)
@dnone: lda #0
        ldy #7
:       sta (dst),y
        dey
        bpl :-
        tax                         ; no mask: X = Y = 0
        tay
        beq @dmask
@dnext:
```

Hoist: lda PB_MAP/jsr pgbank moved from the found path (after findplace) and from @dnone to @dir, so both paths still page bank 6 exactly once before their (dst) stores. ldy #2 after ldy #1/sta -> iny (Y 1->2, same N/Z). Mask: found path loads X=src[4], Y=src[5] (main RAM, any bank), falls into @dmask = the old pgbank(PB_LVL)/nt test; if nt>=16: tya/ldy #1/sta (ent),y writes ent[1]=src[5], txa/dey/sta writes ent[0]=src[4] (old: ent[0]=src[4], ent[1]=src[5]), then bcs @dnext (C=1). @dnone: clears dst[0..7] as before (A=0 at loop exit, Y=$FF), tax/tay make X=Y=0 with Z=1, beq @dmask -> pgbank(PB_LVL), nt test, writes 0,0 to ent[1],ent[0] (old: 0,0). The @dnext pgbank(PB_LVL) is dropped (redundant, see assumption 2); the loop still exits with bank 7 paged since every path passes @dmask. Assembled in a scratch copy: $371..$3F9 (136 bytes) -> $370..$3DC (108).

Review: Original matches 496..559. Hoisted pgbank(PB_MAP) is safe: everything read before the (dst) stores is main RAM (lp=sprdir in LDPROG at $0E00, item zp, section/findplace read STAGE_LVL=$5C00); C at bcs comes from findplace. iny for ldy #2 (Y=1). Mask bytes src[4],src[5] read into X/Y (main RAM, any bank); pgbank keeps X and Y (wrx touches only A; X restored via stack), returns A=PB_LVL; nt>=16 path writes ent[1]=src[5], ent[0]=src[4] with bank 7 paged/write-selected, then bcs taken (C=1 from cmp #16, untouched by tya/ldy/sta/txa/dey/sta). dnone: A=0 after clear loop, tax/tay -> X=Y=0, Z=1, beq @dmask writes 0,0. The @dnext pgbank(PB_LVL) was redundant (all paths already paged PB_LVL, nothing pages after). X dead: nothing in the loop or after it reads X before ldx PB_TILES; @dnext code reloads A, @dir reloads Y. Anonymous ':' count unchanged (3), bne :+ now lands on ldy #4. @dmask is a new cheap label, no clash. Every path saves ~P-18 cy or more (P~65). Load-time code, not timing-critical. Assembled: -28 bytes.

## r069:2  modelb/src/ldprog.s:601-602  (window i0497)  M -0 B -1  [high]  review: accept

Cycles: Model B only: jsr 6 + rts 6 -> jmp 3 (the callee's rts returns to lv_load's caller)

Assumption: readfile ends jmp read_sectors (ldprog.s:99); read_sectors (modelb/src/disc.s:94-199) never looks at the stack (no tsx/pla/farcall), it just rts; lv_load's caller is jsr LDPROG in load_level_b (disc.s:294) which then jmp ld_resume -- same A/X/Y/flags either way; the rts at 602 is not a branch target (no label)

```
        jsr readfile
        rts                         ; (the tile addresses are arithmetic: drawrect's gather)
```
->
```
        jmp readfile                ; (the tile addresses are arithmetic: drawrect's gather)
```

Tail call: jsr X / rts -> jmp X; registers and flags returned are read_sectors' in both cases

Review: Original matches 601..602; rts is unlabelled. readfile tail-jumps read_sectors (disc.s), which with its callees (i_idle, i_param, wait_done, w_wait) never inspects or resets the stack (no tsx/txs in disc.s; the only tsx is farcall in low.s, not on this path). lv_load's caller jsr LDPROG then jmp ld_resume sees identical registers. Saves 1 byte, 9 cy.

## r069:3  modelb/src/ldprog.s:606-615  (window i0505)  M -0 B -3  [high]  review: accept

Cycles: Model B only: 36 -> 32 (drops clc 2 and adc #0 2)

Assumption: every caller passes an immediate 0..9 (ldprog.s:167,179,201,208,243,255,302,387,426,583,674: lda #n / jsr section), so asl leaves C=0; <STAGE_LVL = 0 (defs.inc:99 STAGE_LVL=$5C00) -- guarded by the added .assert, which emits no bytes

```
        asl
        tay
        lda STAGE_LVL,y
        clc
        adc #<STAGE_LVL
        sta src
        lda STAGE_LVL+1,y
        adc #>STAGE_LVL
        sta src+1
        rts
```
->
```
        asl                         ; (C = 0: A < 128)
        tay
        lda STAGE_LVL,y
        sta src
        lda STAGE_LVL+1,y
        adc #>STAGE_LVL
        sta src+1
        rts
        .assert <STAGE_LVL = 0, error, "section: STAGE_LVL page-aligned"
```

asl of A<128 clears C, tay/lda/sta keep it, so the old clc is redundant; adc #<STAGE_LVL with C=0 and operand 0 is an identity leaving C=0, so dropping it gives the same src lo and the same carry (0) into adc #>STAGE_LVL: src+1, and the returned A, N, Z, C, V are identical. Y is 2*A as before.

Review: Original matches 606..615. All 11 callers do lda #0..9 immediately before jsr section, so asl leaves C=0; tay/lda/sta keep C; STAGE_LVL=$5C00 so adc #0 with C=0 was an identity leaving C=0. src, Y, returned A and all flags identical. .assert emits no bytes. Assembled: -3 bytes, 4 cy faster.

## r069:4  modelb/src/ldprog.s:617-620  (window i0511)  M -0 B -1  [high]  review: accept

Cycles: Model B only: lda#/sta zp/lda#/sta zp 10 -> ldx#/stx zp/inx/stx zp 10

Assumption: A dead into bcopy: copy256 goes on ldx PB_LVL / jmp bcopy (line 621-622), and bcopy (ldprog.s:115-118) does stx/stx/jsr wrx whose first instruction is lda PBOARD; X is reloaded by ldx PB_LVL right after; flags are overwritten by that ldx

```
        lda #0
        sta cnt
        lda #1
        sta cnt+1
```
->
```
        ldx #0
        stx cnt
        inx
        stx cnt+1
```

cnt=0, cnt+1=1 written via X instead of A; X then reloaded from PB_LVL as before

Review: Original matches 617..620. X is reloaded by ldx PB_LVL immediately after; A is dead (bcopy does stx/stx/jsr wrx whose first op is lda PBOARD; bcopy exits via lda PB_LVL/jsr pgbank anyway); flags overwritten by ldx. 10 cy -> 10 cy, -1 byte (assembled).

## r070:0  modelb/src/ldprog.s:596-602  (window i0512)  M -0 B -3  [high]  review: accept

Cycles: lda#/sta 5->stx 3; jsr+rts 12->jmp 3 (tail); Model B only (ldprog is 6502 stand-alone); Master: file not assembled

Assumption: X = 0 at line 596: bcopy always returns X = 0 (page loop ends dex->0, tail loop ldx cnt/beq or dex->0; pgbank restores X via pla/tax) -- ldprog.s:110-133. <BARADDR = 0 (defs.inc BARADDR=$0300), asserted. readfile ends jmp read_sectors (disc.s:94) which never inspects the stack; the rts at 602 has no label.

```
        lda #<BARADDR
        sta dst
        lda #>BARADDR
        sta dst+1
        lda #FI_BAR
        jsr readfile
        rts                         ; (the tile addresses are arithmetic: drawrect's gather)
```
->
```
        stx dst                     ; (X = 0 from bcopy; <BARADDR = 0)
        lda #>BARADDR
        sta dst+1
        lda #FI_BAR
        jmp readfile                ; (the tile addresses are arithmetic: drawrect's gather)
        .assert <BARADDR = 0, error, "lv_load: BARADDR's low byte"
```

stx dst stores X=0 = <BARADDR, same as lda #0/sta dst (A reloaded next; flags reloaded by lda #>BARADDR). jsr readfile/rts == jmp readfile: readfile's chain returns straight to lv_load's caller (disc.s load_level_b jsr LDPROG). Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: X=0 after bcopy (both exit paths; pgbank keeps X); <BARADDR=0; readfile->jmp read_sectors, which uses no stack tricks; rts unlabelled. 2+1 bytes confirmed by assembly.

## r070:1  modelb/src/ldprog.s:609-613  (window i0512)  M -0 B -3  [high]  review: accept

Cycles: -4 cycles (clc, adc #0 gone); every call; Master: file not assembled

Assumption: All callers pass A = 0..9 as an immediate (ldprog.s:167,179,201,208,243,255,302,387,426,583,674), so asl leaves C = 0; tay/lda abs,y keep C. <STAGE_LVL = 0 (defs.inc $5C00), asserted.

```
        clc
        adc #<STAGE_LVL
        sta src
        lda STAGE_LVL+1,y
        adc #>STAGE_LVL
```
->
```
        sta src                     ; (<STAGE_LVL = 0: nothing to add)
        lda STAGE_LVL+1,y
        adc #>STAGE_LVL             ; (C = 0: the asl of A = 0..9)
        .assert <STAGE_LVL = 0, error, "section: STAGE_LVL's low byte"
```

clc/adc #<STAGE_LVL with <STAGE_LVL = 0 leaves A unchanged and C = 0; C is already 0 from the asl, so the high adc gets the same carry-in. src and exit flags (from the high adc) identical. Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: All 11 callers pass immediate 0..9 so asl leaves C=0; tay/lda keep C; <STAGE_LVL=0. Same src, same exit flags.

## r070:2  modelb/src/ldprog.s:617-620  (window i0512)  M -0 B -3  [high]  review: accept

Cycles: lda#0/sta/lda#1/sta 10 -> stx/inx/stx 8; Master: file not assembled

Assumption: X = 0 on entry: the only callers are lv_load ldprog.s:207 and :214; the first follows jsr bcopy (:199, exits X=0) then jsr section (keeps X) and lda/sta; the second follows the first copy256 (whose bcopy exits X=0) then section and lda/sta. A on entry to bcopy is dead (bcopy's first A use is wrx's lda PBOARD).

```
        lda #0
        sta cnt
        lda #1
        sta cnt+1
```
->
```
        stx cnt                     ; (X = 0: bcopy's exit, both callers)
        inx
        stx cnt+1
```

cnt = 0, cnt+1 = 1 as before; X is then reloaded with PB_LVL (whose ldx sets the flags as before). A no longer 1 but bcopy overwrites A before reading it. Fallback if caller-state reliance is unwanted: ldx #1/stx cnt+1/dex/stx cnt saves 1 byte at equal cycles. Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: Only callers are lines 207/214; X=0 from the preceding bcopy exit (section and lda/sta keep X); A dead into bcopy. cnt=$0100 as before.

## r070:3  modelb/src/ldprog.s:630-655  (window i0543)  M -0 B -15  [high]  review: accept

Cycles: no-carry path 64->45 cycles; carry path worst 69->49; always faster; Master: file not assembled

Assumption: item is any byte (item*10 <= 2550); callers (ldprog.s:437) read only ent afterwards (ldy #0 next: flags dead); X, Y untouched as before.

```
        lda #0
        sta ent+1
        lda item
        asl
        rol ent+1
        asl
        rol ent+1
        asl
        rol ent+1                   ; * 8
        sta ent
        lda item
        asl
        bcc :+
        inc ent+1
        clc
:       adc ent                     ; + * 2
        sta ent
        bcc :+
        inc ent+1
:       lda ent
        clc
        adc #<imgtab
        sta ent
        lda ent+1
        adc #>imgtab
        sta ent+1
```
->
```
        lda #0
        sta ent+1
        lda item
        asl
        rol ent+1
        asl
        rol ent+1                   ; * 4 (C = 0)
        adc item                    ; * 5
        bcc :+
        inc ent+1
:       asl
        rol ent+1                   ; * 10 (C = 0)
        adc #<imgtab
        sta ent
        lda ent+1
        adc #>imgtab
        sta ent+1
```

Compute (item*4 + item)*2. After the two asl/rol pairs ent+1 <= 3 so each rol leaves C = 0; adc item (C=0) gives item*5 lo, bcc/inc carries into ent+1 (<= 4); asl/rol doubles to item*10 with C = 0 (ent+1 <= 4 before rol); then add imgtab 16-bit exactly as before. Verified exhaustively for item 0..255 in Python and by whole-load simulation. Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: (item*4+item)*2: each rol runs with ent+1<=3 or <=4, so C=0 afterwards; inc does not disturb the asl; the final adc is identical. Two ':' become one with no cross-references (579->581 and 686/688->676 unaffected). 46->31 bytes by assembly.

## r070:4  modelb/src/ldprog.s:658-665  (window i0547)  M -0 B -2  [high]  review: accept

Cycles: -2 cycles (adc #0 gone); both calls; Master: file not assembled

Assumption: <STAGE = 0 (defs.inc $1C00), asserted.

```
        lda (ent),y
        clc
        adc #<STAGE
        sta src
        iny
        lda (ent),y
        adc #>STAGE
        sta src+1
```
->
```
        lda (ent),y
        sta src                     ; (<STAGE = 0)
        iny
        lda (ent),y
        clc
        adc #>STAGE
        sta src+1
        .assert <STAGE = 0, error, "srccnt: STAGE's low byte"
```

adc #<STAGE with C=0 and operand 0 left A unchanged and C=0; the clc now sits before the high adc, giving it the same carry-in 0. (A further byte is possible: C = 1 at both call sites (ldprog.s:441,458 follow cmp fnum/bne not taken) so clc/adc #>STAGE -> adc #>STAGE-1; not proposed.) Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: <STAGE=0: adc #0 with C=0 is a no-op; moving clc before the high adc gives the same carry-in.

## r070:5  modelb/src/ldprog.s:689-692  (window i0576)  M -0 B -2  [high]  review: accept

Cycles: none path sec+rts 8->6; found path 8->8; Master: file not assembled

Assumption: The only way to @none is beq after cmp #$FF (equal => C = 1). The fall-through after `inc src+1 / bne :-` is dead (src+1 is a main-RAM page, never wraps to 0). Caller ldprog.s:501 tests only C.

```
@none:  sec
        rts
@found: clc
        rts
```
->
```
@found: clc
@none:  rts                         ; (C = 1 from the cmp #$FF)
```

@none's sec is redundant (C already 1 from cmp equal), so @none = rts; place it after @found's clc so found falls into it. Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: @none is only reached by beq after cmp #$FF (C=1). The src+1 wrap fall-through cannot be reached with the packer's $FF-terminated list, and the main @pl walk relies on the same terminator.

## r070:6  modelb/src/ldprog.s:700-724  (window i0607)  M -0 B -9  [high]  review: accept

Cycles: per control byte: header -2, literal setup 7->2, run setup 7->4, each byte dec tmp 5 -> dex 2; always faster; Master: file not assembled

Assumption: C = 0 at @run: the only way to line 700 is bcs @end not taken (C=0); ldy/lda/jsr @next (inc/bne only)/tax/bmi keep C. X dead after unrle: pgbank at @end restores X; caller ldprog.s:249 then goes to stage which does ldx. tmp not read after unrle before readfile writes it (sta tmp). @next/@dnext do not touch X or A (ldprog.s:725-732).

```
        lda (src),y
        tax                         ; (the control byte's sign, after the step: inc
        jsr @next                   ;  sets the flags from the pointer)
        txa
        bmi @run
        clc
        adc #1
        sta tmp
@lit:   lda (src),y
        jsr @next
        sta (dst),y
        jsr @dnext
        dec tmp
        bne @lit
        beq @c
@run:   sec
        sbc #126
        sta tmp
        lda (src),y
        jsr @next
@r:     sta (dst),y
        jsr @dnext
        dec tmp
        bne @r
        beq @c
```
->
```
        lda (src),y
        jsr @next                   ; (A untouched)
        tax                         ; X = the count, N = the control byte's sign
        bmi @run
        inx                         ; c+1 literals
@lit:   lda (src),y
        jsr @next
        sta (dst),y
        jsr @dnext
        dex
        bne @lit
        beq @c
@run:   sbc #125                    ; (C = 0 from the bcs) c - 126
        tax
        lda (src),y
        jsr @next
@r:     sta (dst),y
        jsr @dnext
        dex
        bne @r
        beq @c
```

jsr @next keeps A, so tax after it sets N from the control byte (txa dropped). Literal count c+1 = inx; run count c-126 = sbc #125 with C = 0 (sec dropped); counts held in X with dex/bne instead of tmp with dec/bne: identical iteration counts (1..128, 2..129, never 0). beq @c after bne still always taken. Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: C=0 on every arrival at 700 (bcs @end not taken; ldy/lda/jsr @next/tax/bmi keep C), so sbc #125 = c-126. @next keeps A, so tax sets N from the control byte. Counts are 1..128 and 2..129 in X; @next/@dnext do not touch X; X is dead afterwards (pgbank keeps X, stage reloads it); readfile rewrites tmp before any read.

## r070:7  modelb/src/ldprog.s:734-735  (window i0616)  M -0 B -1  [high]  review: accept

Cycles: jsr+rts 12 -> jmp 3; Master: file not assembled

Assumption: pgbank (ldprog.s:135) inspects only the two bytes it pushed itself (tsx / lda $0102,x), which is depth-independent; the rts at 735 has no label; unrle's caller is a plain jsr (ldprog.s:249).

```
        jsr pgbank
        rts
```
->
```
        jmp pgbank
```

Tail call: pgbank's rts returns to unrle's caller with the same A, X, Y, flags. Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: pgbank's tsx/$0102,x reads only its own pushes; the rts at 735 is unlabelled; the caller uses a plain jsr.

## r070:8  modelb/src/ldprog.s:741-752  (window i0632)  M -0 B -6  [high]  review: accept

Cycles: 30 -> 23 cycles; Master: file not assembled

Assumption: stage (ldprog.s:623) sets dst = STAGE and readfile/read_sectors (disc.s:94-240, NMI stubs, sei in ld_begin) never write zp $A8-$AD, so dst's low byte is <STAGE = 0 = <MENU_BASE (MENU_BASE = TILES = $8100) -- asserted. A dead: bcopy's entry does not read A.

```
        lda #<STAGE
        sta src
        lda #>STAGE
        sta src+1
        lda #<MENU_BASE
        sta dst
        lda #>MENU_BASE
        sta dst+1
        lda #0
        sta cnt
        lda #F_MENU_N
        sta cnt+1
```
->
```
        lda #>STAGE                 ; (dst's low byte is STAGE's, 0, from stage)
        sta src+1
        lda #>MENU_BASE
        sta dst+1
        lda #F_MENU_N
        sta cnt+1
        lda #0                      ; <STAGE, the count's low byte
        sta src
        sta cnt
        .assert <STAGE = 0 && <MENU_BASE = 0, error, "title_load: low bytes"
```

src = STAGE, dst = MENU_BASE, cnt = F_MENU_N*256 all as before: dst lo already 0, src lo and cnt lo share one lda #0. Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: stage leaves dst lo=<STAGE=0=<MENU_BASE; read_sectors and the NMI stubs write no zero page, and IRQs are off (sei in ld_begin); A is dead into bcopy. 24->18 bytes.

## r070:9  modelb/src/ldprog.s:757-768  (window i0646)  M -0 B -12  [high]  review: accept

Cycles: 30 -> 15 cycles; Master: file not assembled

Assumption: After the first bcopy (cnt lo = 0): bcopy only increments src+1/dst+1 and never writes src lo or cnt (ldprog.s:110-133), so src lo = 0 and cnt lo = 0 still; stage sets dst lo = <STAGE = 0; read_sectors keeps zp. <STAGE = <TITLE_ADDR = 0 ($1C00, $8900 from assets.inc), asserted.

```
        lda #<STAGE
        sta src
        lda #>STAGE
        sta src+1
        lda #<TITLE_ADDR
        sta dst
        lda #>TITLE_ADDR
        sta dst+1
        lda #0
        sta cnt
        lda #F_TITLE_N
        sta cnt+1
```
->
```
        lda #>STAGE                 ; (src, cnt: low bytes 0 from above -- bcopy
        sta src+1                   ;  keeps them; dst's 0 from stage)
        lda #>TITLE_ADDR
        sta dst+1
        lda #F_TITLE_N
        sta cnt+1
        .assert <STAGE = 0 && <TITLE_ADDR = 0, error, "title_load: low bytes"
```

Only the high bytes differ from what memory already holds; the three low-byte stores wrote values already there. Independent of the line 741 candidate (both the original and that proposal leave src lo = cnt = 0). Whole-load check: py65 sim of lv_load for all 16 levels and title_load (two random fills, banked memory, real disc files) gives identical memory/banks, all faster.

Review: bcopy never writes src lo or cnt; with cnt lo=0 the tail loop is skipped; stage/readfile/read_sectors keep src and cnt; stage sets dst lo=0=<TITLE_ADDR. Holds with or without candidate 8.

## r071:0  modelb/src/ldprog.s:733-735  (window i0640)  M -0 B -1  [high]  review: accept

Cycles: @end path: lda 4 + jsr 6 + (pgbank) + rts 6 -> lda 4 + jmp 3 + (pgbank); saves 9 cycles. Model B only (ldprog is Model B only).

Assumption: pgbank (ldprog.s:134) does not look at the caller's stack. Its tsx/lda $0102,x reads the A it pushed itself two bytes earlier (pha/txa/pha), which sits at the same offset from S at any call depth. The rts at 735 has no label and is not a branch target. unrle is reached only by jsr unrle (ldprog.s:249), so pgbank's rts returns there just as unrle's did. pgbank keeps X and A (pla/tax/pla), so the exit state is the same as before.

```
@end:   lda PB_LVL
        jsr pgbank
        rts
```
->
```
@end:   lda PB_LVL
        jmp pgbank                  ; (tail: pgbank's tsx reads its own push)
```

jsr pgbank; rts == jmp pgbank because pgbank behaves the same at any stack depth. In both cases the return address on the stack is unrle's caller, and registers/flags at return are what pgbank leaves (identical).

Review: Original matches lines 733-735 exactly. pgbank reads $0102,x relative to its own pushes (pha/txa/pha then tsx), so it works at any stack depth; X and A are restored, and flags on return come from pgbank's own final pla either way. The rts has no label, @end is reached only from unrle's bcs (line 698), and unrle's only caller is the jsr at line 249. Model B only (ldprog is a stand-alone 6502 program). jsr+rts (4 bytes) becomes jmp (3): saves 1 byte and 9 cycles. Assembled: the image shrinks by 1 byte.

## r071:1  modelb/src/ldprog.s:738-770  (window i0648)  M -0 B -18  [high]  review: accept

Cycles: Straight-line code. Block 1 setup: 6x(lda# 2 + sta zp 3) = 30 -> lda#0 2 + 2 sta 6 + 3x5 15 = 23. Block 2 setup: 30 -> 15. Saves 22 cycles in total. Model B only.

Assumption: (1) <STAGE = <MENU_BASE = <TITLE_ADDR = 0 (build/defs_ld.inc: STAGE=$1C00, MENU_BASE=TILES=$8100, TITLE_ADDR=$8900 from assets.inc / convert.py 0x8900). The .assert enforces this at build time. (2) stage (ldprog.s:623) sets dst=STAGE, so dst lo = 0 after it. readfile (ldprog.s:84) writes only tmp in zp. read_sectors (disc.s:94) and its helpers i_idle/i_param/wait_done/w_wait write no zero page (only ld_* in LGCBSS, NMI-page cells and FDC registers). The NMI stubs (disc.s:26-80) write only self-modified absolute addresses and NMI-page cells. The disc data goes to STAGE ($1C00+, at most 46 sectors, so below $4A00), not zp. IRQs are off (ld_begin, disc.s:316 sei), so no IRQ handler touches $A8-$AD. (3) bcopy (ldprog.s:109) changes only src+1 and dst+1 in zp (the incs in @page). It only reads src lo, dst lo, cnt and cnt+1. wrx and pgbank touch no zp. So after the first bcopy and the second stage, src lo = 0, cnt lo = 0 and dst lo = 0 still hold. src+1 and dst+1 (moved on by bcopy, or set to >STAGE by stage) are written again. (4) There is no label inside title_load. Its only entry is jmp title_load at ldprog.s:46, so nothing enters it partway.

```
title_load:
        lda #FI_MENU
        jsr stage
        lda #<STAGE
        sta src
        lda #>STAGE
        sta src+1
        lda #<MENU_BASE
        sta dst
        lda #>MENU_BASE
        sta dst+1
        lda #0
        sta cnt
        lda #F_MENU_N
        sta cnt+1
        ldx PB_TILES
        jsr bcopy
        lda #FI_TITLE
        jsr stage
        lda #<STAGE
        sta src
        lda #>STAGE
        sta src+1
        lda #<TITLE_ADDR
        sta dst
        lda #>TITLE_ADDR
        sta dst+1
        lda #0
        sta cnt
        lda #F_TITLE_N
        sta cnt+1
        ldx PB_MAP
        jmp bcopy
```
->
```
title_load:
        lda #FI_MENU
        jsr stage                   ; (dst = STAGE: its lo 0 = <MENU_BASE)
        lda #0                      ; <STAGE = 0, cnt lo = 0
        sta src
        sta cnt
        lda #>STAGE
        sta src+1
        lda #>MENU_BASE
        sta dst+1
        lda #F_MENU_N
        sta cnt+1
        ldx PB_TILES
        jsr bcopy                   ; (src, cnt lo kept: bcopy, readfile leave them)
        lda #FI_TITLE
        jsr stage                   ; (dst lo 0 = <TITLE_ADDR)
        lda #>STAGE
        sta src+1
        lda #>TITLE_ADDR
        sta dst+1
        lda #F_TITLE_N
        sta cnt+1
        ldx PB_MAP
        jmp bcopy
        .assert (<STAGE | <MENU_BASE | <TITLE_ADDR) = 0, error, "title_load: page-aligned"
```

Block 1: lda #<STAGE/sta src and lda #0/sta cnt merge into lda #0/sta src/sta cnt, because <STAGE = 0. lda #<MENU_BASE/sta dst goes, because stage has just stored <STAGE (= 0 = <MENU_BASE) there. The hi bytes are unchanged. Block 2: src lo and cnt lo still hold 0 from block 1 because nothing writes them in between. dst lo = 0 from stage, which equals <TITLE_ADDR. src+1, dst+1 and cnt+1 are loaded again as before. A at bcopy entry is different, but bcopy overwrites A in wrx before any use. X is set by ldx as before. Flags at bcopy entry are dead: bcopy starts with stx/stx/jsr wrx, and wrx does an lda.

Review: Original matches lines 738-770 exactly. STAGE=$1C00, MENU_BASE=TILES=$8100 and TITLE_ADDR=$8900 (modelb/build/defs_ld.inc) are all page-aligned, and the added .assert enforces that. stage sets dst=STAGE (lo 0) with X. readfile writes only tmp ($AE) in zero page. read_sectors, i_idle, i_param, wait_done and w_wait write only LGCBSS, NMI-page cells and FDC registers. The NMI stubs write through self-modified absolute addresses into STAGE. IRQs are off (sei in ld_begin). bcopy writes only src+1 and dst+1 (and ROMSEL_CPY $F4). wrx and pgbank write no zero page in $A8-$AD. So src lo, cnt lo and dst lo are still 0 at the second bcopy. A and flags at bcopy entry are dead (stx/stx/jsr wrx, and wrx starts with lda). The only entry is jmp title_load at line 46, and no label sits inside. Bytes: 70 -> 52, 18 saved. Every path is faster. Assembled together with index 0, the LDPROG image goes from 3207 to 3188 bytes (19 = 1 + 18).

## r072:0  modelb/src/loader.s:51-52  (window i0025)  M -0 B -5  [medium]  review: accept

Cycles: removes 6 cycles (lda #0 2 + sta abs 4) on the one path through; Model B only (loader is not assembled for the Master)

Assumption: fdc is `.byte 0` in the loaded image (loader.s:563) and nothing writes it before line 51; the loader is only entered freshly loaded from disc by !BOOT (*RUN), never re-entered in place

```
        lda #0
        sta fdc
```
->
```

```

fdc already holds 0 from the image, so storing 0 is a no-op; A and flags are reloaded by lda #$81 on the next line

Review: Lines 51-52 match the file. fdc is `.byte 0` (loader.s:563) and nothing writes it before line 51: findram and noram do not touch it, and OSGBPB writes only drvname. The loader is only *RUN from !BOOT. Its successful path ends in jmp BANKCODE and never returns, and noram returns before any store to fdc, so a re-run still sees 0. A and the flags are reloaded by lda #$81. Saves 5 bytes (lda # 2 + sta abs 3). Model B only: the loader is not in the Master build. Tested: with all 15 candidates applied the loader assembles (LOADER 1324 -> 1268 bytes, -56, which is the sum of the claims). Booted in jsbeeb in 36 configurations, every sideways RAM bank and A/X/Y/$F4 at jmp BANKCODE are byte-identical to the original loader's. The configurations cover B-DFS1.2 and B1770; plain, Watford and Solidisk boards; RAM in sockets 0-7 or 1,5,9,12; and no key, W or I held. The B1770 no-key run takes the ROM-loop fdc=1 path, W takes the held path and I takes the fall-through.

## r072:1  modelb/src/loader.s:57-61  (window i0031)  M -0 B -3  [high]  review: accept

Cycles: not held: cpx#2+bne taken 3 = 5 -> inx 2+bne taken 3 = 5; held: 2+2+lda# 2+sta abs 4+bne 3 = 13 -> inx 2+bne 2+inc abs 6+bne 3 = 13; Model B only (loader is not assembled for the Master)

Assumption: fdc = 0 here (stored at line 52, or the image's .byte 0 if the line 51-52 candidate is taken); X dead on both exits: the not-held path reloads X at line 63 (ldx #$DA), @fdcdone reloads X at line 103 (ldx #15) after lda #22/OSWRCH; A dead at @fdcdone (lda #22)

```
        cpx #$FF
        bne :+
        lda #1
        sta fdc
        bne @fdcdone
```
->
```
        inx                         ; X = $FF: W held
        bne :+
        inc fdc                     ; 0 (above) -> 1, Z clear
        bne @fdcdone
```

X=$FF <=> X+1=0, so inx/bne branches exactly when cpx #$FF/bne did. inc fdc turns 0 into 1 (the same byte lda #1/sta fdc stored) and leaves Z clear, so bne @fdcdone is always taken, as the old bne on A=1 was

Review: Lines 57-61 match. OSBYTE &81 with a negative INKEY returns X=$FF (held) or 0, so inx/bne branches exactly when cpx #$FF/bne did. inc fdc takes 0 to 1 (fdc=0 from line 52 or the image) and clears Z, so bne @fdcdone is always taken, as before. X is dead on both exits (ldx #$DA at 63; ldx #15 at 103), A is reloaded (lda #22) and C is not consumed. 11 -> 8 bytes, saving 3. Cycles: held path 13 -> 13, not held 5 -> 5. The anonymous `:` at 62 is untouched. The combined boot test is under index 0.

## r072:2  modelb/src/loader.s:66-66  (window i0039)  M -0 B -1  [high]  review: accept

Cycles: cpx # 2 -> inx 2; the beq is unchanged; Model B only

Assumption: X dead after: the fall-through reloads X at line 68 (ldx #15), @fdcdone reloads at line 103; C not consumed (the next flag use, at line 70, is from lda/and)

```
        cpx #$FF
```
->
```
        inx                         ; X = $FF: I held
```

inx sets Z exactly when X was $FF, as cpx #$FF did

Review: Line 66 matches. inx sets Z exactly when X was $FF, as cpx #$FF did. The C that cpx set is not consumed on either path (fall-through: ldx #15/lda/and; @fdcdone: lda #22/OSWRCH). X is reloaded on both paths. 2 -> 1 byte; 2 -> 2 cycles.

## r072:3  modelb/src/loader.s:69-71  (window i0042)  M -0 B -2  [high]  review: accept

Cycles: and # (2) removed: 2 cycles fewer on both outcomes; Model B only

Assumption: A dead on both paths: taken -> @nextrom dex/bpl @rom reloads A, and the loop exit reloads A (lda oldbank); the fall-through reloads A at lda $8009,y

```
@rom:   lda ROMTYPE,x               ; the MOS's ROM type table: service ROMs only
        and #$80
        beq @nextrom
```
->
```
@rom:   lda ROMTYPE,x               ; the MOS's ROM type table: service ROMs only
        bpl @nextrom
```

lda sets N from bit 7 of ROMTYPE,x; and #$80/beq branched iff bit 7 was clear, which is what bpl does

Review: Lines 69-71 match. lda ROMTYPE,x sets N from bit 7, and and #$80/beq branched exactly when bit 7 was clear, which is bpl. A is dead: the fall-through reloads it (lda $8009,y), @nextrom goes dex/bpl back to lda ROMTYPE,x, and the loop exit loads lda oldbank. The @rom label stays on the lda. Saves 2 bytes and 2 cycles.

## r072:4  modelb/src/loader.s:79-80  (window i0051)  M -0 B -3  [high]  review: accept

Cycles: saves 4-5 cycles (a dead lda abs,y) on every entry to the label; Model B only

Assumption: the `:` stays on the line, so beq :+ (line 76) and the bne :- fall-through still land here

```
:       lda $8009,y                 ; the version follows its terminator
        lda $800A,y
```
->
```
:       lda $800A,y                 ; the version follows its terminator
```

the first lda $8009,y is overwritten (A and N,Z) by lda $800A,y before any use; reading ROM at $8009,y has no side effect

Review: Lines 79-80 match. The first lda $8009,y is overwritten (A, N and Z) by lda $800A,y before anything uses it, and reading sideways ROM/RAM has no side effect. The `:` stays on the kept line, so beq :+ (line 76) still lands there and bne :- (line 78) still targets line 75. Saves 3 bytes and 4-5 cycles.

## r072:5  modelb/src/loader.s:89-90  (window i0060)  M -0 B -2  [high]  review: accept

Cycles: lda #1 2 + sta abs 4 = 6 -> inc abs 6; Model B only

Assumption: fdc = 0 on entry to the ROM loop: set to 0 at line 52 (or by the image), the W path skips the loop, and the only store of 1 inside the loop is this one, followed by the loop exit (ldx #0/dex/bpl falls through); A dead: ldx #0/dex/bpl, then lda oldbank

```
        lda #1
        sta fdc
```
->
```
        inc fdc                     ; 0 -> 1
```

inc of 0 gives 1, the same byte lda #1/sta fdc stored; A was 1 before and is 'F' now, but A is reloaded at line 95 before any use

Review: Lines 89-90 match. fdc is 0 on entry to the ROM loop: it is set at line 52 (or by the image), and the W path, the only other store, bypasses the loop. This store is followed by ldx #0/dex/bpl, which falls out of the loop, so it runs at most once and inc gives 1. A now holds 'F' instead of 1, but it is dead (ldx/dex/bpl, then lda oldbank). 5 -> 3 bytes; 6 -> 6 cycles. The B1770 no-key boot exercises this path, and the result is identical.

## r072:6  modelb/src/loader.s:125-140  (window i0086)  M -0 B -3  [high]  review: accept

Cycles: removes lda abs (4 cycles); the rest is identical; Model B only

Assumption: npieces is only read here and at line 188 (dec); the ztab and zsrc stores are independent

```
        lda BUF
        sta npieces
        lda #<(BUF+1)               ; the table
        sta ztab
        lda #>(BUF+1)
        sta ztab+1
        lda npieces                 ; the first piece follows the table: BUF + 1 + 5n
        asl
        asl
        adc npieces
        sec                         ; (+1)
        adc #<BUF
        sta zsrc
        lda #>BUF
        adc #0
        sta zsrc+1
```
->
```
        lda BUF
        sta npieces
        asl                         ; the first piece follows the table: BUF + 1 + 5n
        asl
        adc npieces
        sec                         ; (+1)
        adc #<BUF
        sta zsrc
        lda #>BUF
        adc #0
        sta zsrc+1
        lda #<(BUF+1)               ; the table
        sta ztab
        lda #>(BUF+1)
        sta ztab+1
```

A already holds BUF (= npieces) after sta npieces, so computing zsrc before ztab drops the reload. The carry into adc npieces still comes from the second asl, and the carry left at @piece still comes from adc #0 (the ztab lda/sta do not touch C). @piece begins with ldy, so N/Z do not matter

Review: Lines 125-140 match. After sta npieces, A already holds n. The carry chain through asl/asl/adc npieces is unchanged. The lda #/sta pairs for ztab do not touch C, and C is not consumed at @piece. npieces is still stored before adc and the later dec read it. Saves 3 bytes and 4 cycles. The sec/adc #<BUF lines survive verbatim, so candidate 7 composes; the two were applied and tested together.

## r072:7  modelb/src/loader.s:135-136  (window i0096)  M -0 B -1  [medium]  review: accept

Cycles: removes sec (2 cycles); Model B only

Assumption: C = 0 after adc npieces: the second asl shifts out bit 6 of n, and adc carries only if 5n >= 256. The original already needs both to be false (a carry there is lost from zsrc+1), and build.sh fixes n = 11 pieces. Composes with the line 125-140 candidate: these two lines survive it verbatim

```
        sec                         ; (+1)
        adc #<BUF
```
->
```
        adc #<(BUF+1)               ; (+1: C clear, 5n < 256)
```

with C clear, adc #<(BUF+1) adds 1, the same as sec/adc #<BUF (<BUF = 0); the carry out, into adc #0 for the high byte, is the same

Review: Lines 135-136 match. The original computes zsrc correctly only for n <= 51; for n >= 52, sec discards the carry out of adc npieces. build.sh fixes n = 11. For n <= 51, bits 6 and 7 of n are 0, so C = 0 after asl/asl and adc npieces does not carry. adc #<(BUF+1) (= #1, since <BUF = 0) then gives 5n+1 with the same carry into adc #0. 3 -> 2 bytes; saves 2 cycles.

## r072:8  modelb/src/loader.s:141-156  (window i0102)  M -0 B -6  [high]  review: accept

Cycles: 57 -> 49 cycles: sta pbank (4) and lda pbank (4) gone, the rest reordered; Model B only

Assumption: pbank is read only at line 156 (grep: lines 143 and 156, plus its .byte at 561), so its store is dead once the bank is loaded last; wrx and the tax/lda/sta at 158-163 do not use Y; line 164 reloads Y anyway

```
@piece: ldy #0
        lda (ztab),y
        sta pbank
        iny
        lda (ztab),y
        sta zdst
        iny
        lda (ztab),y
        sta zdst+1
        iny
        lda (ztab),y
        sta plen
        iny
        lda (ztab),y
        sta plen+1
        lda pbank                   ; the piece's bank is the code's number (4..7):
```
->
```
@piece: ldy #4                      ; backwards: A ends as the bank, Y as 0
        lda (ztab),y
        sta plen+1
        dey
        lda (ztab),y
        sta plen
        dey
        lda (ztab),y
        sta zdst+1
        dey
        lda (ztab),y
        sta zdst
        dey
        lda (ztab),y                ; the piece's bank is the code's number (4..7):
```

the five table bytes are read in the order 4..0 instead of 0..4, into the same destinations; the last read (Y=0) is the bank, so A and N/Z at beq :+ are what lda pbank gave

Review: Lines 141-156 match. pbank is referenced only at lines 143 and 156 and its .byte, so its store is dead once the bank is read last into A. Reading the five table bytes in the order 4..0 is safe: they are plain RAM and go to the same destinations. The last lda (ztab),y, with Y=0, gives the same A and N/Z for beq :+ as lda pbank did. 32 -> 26 bytes, saving 6; 57 -> 49 cycles. Y is 0 afterwards, which candidate 10 relies on.

## r072:9  modelb/src/loader.s:158-163  (window i0119)  M -0 B -10  [high (only with the 199-318 candidate)]  review: accept

Cycles: tax..tax + jsr wrx = 21 -> jsr selbank + the same 5 instructions = 21; Model B only

Assumption: DEPENDS on the line 199-318 candidate (selbank defined immediately before wrx); A = the piece's bank (nonzero) here, as at the other sites

```
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx                     ; and the write bank, on a board that has one
```
->
```
        jsr selbank                 ; and the write bank, on a board that has one
```

the third copy of the tax/lda map-4,x/sta ROMSELC/sta ROMSEL/tax/jsr wrx sequence becomes jsr selbank

Review: Lines 158-163 match. With candidate 14 applied, jsr selbank is the same sequence (21 cycles to wrx, same stack depth, Y untouched) and saves 10 bytes. DEPENDS on candidate 14: reject this one if 14 is not applied.

## r072:10  modelb/src/loader.s:164-164  (window i0124)  M -0 B -2  [high (only with the 141-156 candidate)]  review: accept

Cycles: removes ldy #0 (2 cycles) on both paths; Model B only

Assumption: DEPENDS on the line 141-156 candidate: Y = 0 after its last dey. On the beq path Y is untouched; on the bank path tax/lda map-4,x/sta/sta/tax/jsr wrx (wrx: lda board, cmp, sta abs,x, stx, rts -- or selbank, the same) never touch Y. @cp is re-entered only from inside the copy loop, where Y never changes

```
:       ldy #0
```
->
```
:                                   ; (Y = 0: the table read ends there)
```

Y is already 0 at the label; the `:` stays, so beq :+ still lands here

Review: Line 164 matches. DEPENDS on candidate 8, which leaves Y = 0 after its last dey. The beq path does not change Y, and neither does the bank path: wrx and selbank never write Y. The `:` stays on a label-only line, so beq :+ at line 157 still lands there. Saves 2 bytes and 2 cycles. Reject this one if 8 is not applied.

## r072:11  modelb/src/loader.s:165-180  (window i0125)  M -0 B -1  [high]  review: accept

Cycles: per byte, common path 32 -> 29 (jmp 3 gone; bne @cp taken 3 replaces bne :+ taken 3); zdst page-cross path: +inc zdst+1 5 + jmp 3 -> +inc 5 + bne 3; the plen-low-zero and zsrc page-cross paths are unchanged; Model B only

Assumption: zdst+1 is never 0 after inc: every piece ends at or below $C000 (build.sh piece list; BANKS: max end $C000), so the pointer's high byte stays in $80..$C0; C is unused in the loop

```
@cp:    lda plen
        ora plen+1
        beq @cpdone
        lda (zsrc),y
        sta (zdst),y
        inc zsrc
        bne :+
        inc zsrc+1
:       inc zdst
        bne :+
        inc zdst+1
:       lda plen
        bne :+
        dec plen+1
:       dec plen
        jmp @cp
```
->
```
@cp:    lda plen
        ora plen+1
        beq @cpdone
        lda (zsrc),y
        sta (zdst),y
        lda plen
        bne :+
        dec plen+1
:       dec plen
        inc zsrc
        bne :+
        inc zsrc+1
:       inc zdst
        bne @cp
        inc zdst+1
        bne @cp                     ; (never 0: the pieces end below $C000)
```

the length decrement moves before the pointer increments (independent variables, same final values); then the zdst low-byte bne goes straight to @cp, and after inc zdst+1 (never zero) bne @cp replaces jmp @cp

Review: Lines 165-180 match. The plen decrement can move ahead of the pointer increments: they use independent memory and no flags pass between them. After inc zdst+1 the high byte is never 0, because every piece is in banks 4..7 at $8000-$BFFF (build.sh piece list), so it stays in $80..$C0. The loop goes from 3 anonymous labels to 2; every :+ inside it still reaches its intended target, and no branch outside counts across the loop. Common path 53 -> 50 cycles; the other paths are equal. 38 -> 37 bytes, saving 1.

## r072:12  modelb/src/loader.s:186-188  (window i0142)  M -0 B -4  [medium]  review: accept

Cycles: removes bcc (taken 3, or 2 + inc 5); Model B only

Assumption: ztab is read only at @piece (lines 142-154) and advanced here. The table is BUF+1..BUF+5n in page $20 whenever the original's zsrc formula is right (5n < 256; n = 11 in build.sh), so the high byte never needs the carry before the last read. ztab is dead after the loop (grep)

```
        bcc :+
        inc ztab+1
:       dec npieces
```
->
```
        dec npieces
```

a carry out of ztab's low byte only happens after the final piece, when ztab is no longer read; C after the adc is not consumed (dec npieces/bne, then @fix ldy/lda/cmp)

Review: Lines 186-188 match. The original is correct only for n <= 51; in that range the ztab base read at @piece is at most $20FB, so the high byte never needs the carry before the last read. ztab is dead after the loop. C after the adc is not consumed. The `:` on line 188 was referenced only by the removed bcc :+. Saves 4 bytes.

## r072:13  modelb/src/loader.s:189-191  (window i0144)  M -0 B -3  [high]  review: accept

Cycles: loop back: beq not taken 2 + jmp 3 = 5 -> bne taken 3; exit: beq taken 3 -> bne not taken 2; Model B only

Assumption: @piece is within branch range (it assembles alone and with every other candidate)

```
        beq :+
        jmp @piece
:
```
->
```
        bne @piece
```

dec npieces sets Z; branch back while it is nonzero and fall into @fix when it is zero, as before

Review: Lines 189-191 match. dec npieces sets Z; bne @piece loops while it is nonzero and falls into @fix at zero. @piece is about 103 bytes back, within branch range even with no other candidate applied. The `:` on line 191 was referenced only by the removed beq :+. 5 -> 2 bytes, saving 3; the loop-back path goes from 5 to 3 cycles and the exit from 3 to 2.

## r072:14  modelb/src/loader.s:199-318  (window i0150)  M -0 B -10  [high]  review: accept

Cycles: each site: tax 2 + lda 4 + sta zp 3 + sta 4 + tax 2 + jsr 6 = 21 -> jsr 6 + tax 2 + lda 4 + sta 3 + sta 4 + tax 2 = 21, then wrx and its rts exactly as before; stack depth the same; Model B only

Assumption: selbank is placed after jmp BANKCODE (line 313), so nothing falls into it; wrx preserves Y, and the code after each site uses Y as before (iny at 205/250); nothing inspects the stack; `selbank:` is a normal label, but no @-label of start's scope is referenced after it. The range is large only because the two call sites and wrx are far apart: it overlaps the next region's windows, so apply it before or after their candidates by text, not by line number

```
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx
        iny
        lda (zsrc),y
        sta zdst
        iny
        lda (zsrc),y
        sta zdst+1
        ldy #0
        lda (zdst),y
        pha
        and #$0F
        tax
        lda map-4,x
        sta ztmp
        pla
        and #$F0
        ora ztmp
        sta (zdst),y
        lda zsrc
        clc
        adc #3
        sta zsrc
        bcc @fix
        inc zsrc+1
        jmp @fix
@fixdone:
        ; ---- the write-bank stores: (bank, address, kind) x n, $FF, after the $FF above.
        ; Each is a `sta $FE30` in the code, a harmless second write of the bank on a
        ; plain machine and left alone there.  Watford: `sta $FF30+socket` for a constant
        ; bank (kind 4..7 says which), `sta $FF30,x` (opcode $9D) where the code has the
        ; bank in X (kind $FE); Solidisk: `sta $FE60` either way.
        inc zsrc
        bne @wfix
        inc zsrc+1
@wfix:  ldy #0
        lda (zsrc),y
        cmp #$FF
        beq @wfixdone
        ldx board
        beq @wnext                  ; plain: as assembled
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx
        iny
        lda (zsrc),y
        sta zdst
        iny
        lda (zsrc),y
        sta zdst+1
        iny
        lda (zsrc),y                ; the kind
        tax
        lda board
        cmp #BOARD_SOLIDISK
        beq @wsol
        cpx #$FE
        beq @wdyn
        lda map-4,x                 ; Watford, a constant bank: sta $FF30 + its socket
        ora #<WRSEL_WATFORD
        ldy #1
        sta (zdst),y
        lda #>WRSEL_WATFORD
        iny
        sta (zdst),y
        jmp @wnext
@wdyn:  lda #$9D                    ; Watford, the bank in X: sta $FF30,x
        ldy #0
        sta (zdst),y
        lda #<WRSEL_WATFORD
        iny
        sta (zdst),y
        lda #>WRSEL_WATFORD
        iny
        sta (zdst),y
        jmp @wnext
@wsol:  lda #<WRSEL_SOLIDISK        ; Solidisk: sta $FE60, the bank being in A
        ldy #1
        sta (zdst),y
        lda #>WRSEL_SOLIDISK
        iny
        sta (zdst),y
@wnext: lda zsrc
        clc
        adc #4
        sta zsrc
        bcc @wfix
        inc zsrc+1
        jmp @wfix
@wfixdone:
        ; ---- the driver's configuration, the banks themselves and the board, into bank 7
        lda map+3
        sta ROMSELC
        sta ROMSEL
        ldx map+3
        jsr wrx
        lda fdc
        sta dsk_type
        lda drive
        sta dsk_drv
        ldx #3
:       lda map,x
        sta dsk_banks,x
        dex
        bpl :-
        lda board
        sta dsk_board
        jmp BANKCODE                ; bank 7's entry vector (bank 7 is paged)

; ---------------------------------------------------------------- the write bank
; X = a socket: make it the one a store reaches, on a board that chooses that apart
; from ROMSEL.  A is destroyed.
wrx:    lda board
```
->
```
        jsr selbank
        iny
        lda (zsrc),y
        sta zdst
        iny
        lda (zsrc),y
        sta zdst+1
        ldy #0
        lda (zdst),y
        pha
        and #$0F
        tax
        lda map-4,x
        sta ztmp
        pla
        and #$F0
        ora ztmp
        sta (zdst),y
        lda zsrc
        clc
        adc #3
        sta zsrc
        bcc @fix
        inc zsrc+1
        jmp @fix
@fixdone:
        ; ---- the write-bank stores: (bank, address, kind) x n, $FF, after the $FF above.
        ; Each is a `sta $FE30` in the code, a harmless second write of the bank on a
        ; plain machine and left alone there.  Watford: `sta $FF30+socket` for a constant
        ; bank (kind 4..7 says which), `sta $FF30,x` (opcode $9D) where the code has the
        ; bank in X (kind $FE); Solidisk: `sta $FE60` either way.
        inc zsrc
        bne @wfix
        inc zsrc+1
@wfix:  ldy #0
        lda (zsrc),y
        cmp #$FF
        beq @wfixdone
        ldx board
        beq @wnext                  ; plain: as assembled
        jsr selbank
        iny
        lda (zsrc),y
        sta zdst
        iny
        lda (zsrc),y
        sta zdst+1
        iny
        lda (zsrc),y                ; the kind
        tax
        lda board
        cmp #BOARD_SOLIDISK
        beq @wsol
        cpx #$FE
        beq @wdyn
        lda map-4,x                 ; Watford, a constant bank: sta $FF30 + its socket
        ora #<WRSEL_WATFORD
        ldy #1
        sta (zdst),y
        lda #>WRSEL_WATFORD
        iny
        sta (zdst),y
        jmp @wnext
@wdyn:  lda #$9D                    ; Watford, the bank in X: sta $FF30,x
        ldy #0
        sta (zdst),y
        lda #<WRSEL_WATFORD
        iny
        sta (zdst),y
        lda #>WRSEL_WATFORD
        iny
        sta (zdst),y
        jmp @wnext
@wsol:  lda #<WRSEL_SOLIDISK        ; Solidisk: sta $FE60, the bank being in A
        ldy #1
        sta (zdst),y
        lda #>WRSEL_SOLIDISK
        iny
        sta (zdst),y
@wnext: lda zsrc
        clc
        adc #4
        sta zsrc
        bcc @wfix
        inc zsrc+1
        jmp @wfix
@wfixdone:
        ; ---- the driver's configuration, the banks themselves and the board, into bank 7
        lda map+3
        sta ROMSELC
        sta ROMSEL
        ldx map+3
        jsr wrx
        lda fdc
        sta dsk_type
        lda drive
        sta dsk_drv
        ldx #3
:       lda map,x
        sta dsk_banks,x
        dex
        bpl :-
        lda board
        sta dsk_board
        jmp BANKCODE                ; bank 7's entry vector (bank 7 is paged)

; ---------------------------------------------------------------- the write bank
; X = a socket: make it the one a store reaches, on a board that chooses that apart
; from ROMSEL.  A is destroyed.
; A = a bank's code number (4..7): page its socket for reading and writing,
; X = the socket.  A is destroyed.
selbank:
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax                         ; and into wrx
wrx:    lda board
```

two identical 10-byte sequences plus jsr wrx (the fix site at 199-204 and the write-fix site at 244-249) become jsr selbank, a copy of the sequence that falls into wrx: +10 bytes for selbank, -10 at each site. With the line 158-163 candidate, a third site saves another 10

Review: Lines 199-318 match verbatim. The sites at 199-204 and 244-249 are both the exact sequence tax/lda map-4,x/sta ROMSELC/sta ROMSEL/tax/jsr wrx. selbank repeats that sequence and falls into wrx, with the same cycles, the same stack depth and Y preserved. selbank sits after jmp BANKCODE, so nothing falls into it. No @label from start's scope is referenced after it. Net saving: -10 -10 +10 = 10 bytes. A cosmetic point only: the old wrx comment now sits above selbank.

## r073:0  modelb/src/loader.s:165-180  (window i0106)  M -0 B -4  [medium]  review: accept

Cycles: Per copied byte (no wraps) 53->40; lo-byte-zero iteration 58->45; zsrc/zdst wrap paths unchanged relative (bne+inc+bne 10 vs bne+inc 7 +jmp 3). EXIT test once per piece 11->13 (+2): lda plen/bne(nt)/lda plen+1/beq vs lda/ora/beq. Net per piece is far faster when the piece is >=1 byte (all 11 pieces are 64..11625 bytes), but a zero-length piece would be +2.

Assumption: [C1] Y = 0 throughout (ldy #0 at loader.s:164, the loop never changes Y). zdst+1 never wraps to 0: pieces are bank 4-7 at $8000..$B289 (build.sh piece list). plen/plen+1 only read here. A dead at @cpdone (lda ztab). No other reference to @cp (grep).

```
@cp:    lda plen
        ora plen+1
        beq @cpdone
        lda (zsrc),y
        sta (zdst),y
        inc zsrc
        bne :+
        inc zsrc+1
:       inc zdst
        bne :+
        inc zdst+1
:       lda plen
        bne :+
        dec plen+1
:       dec plen
        jmp @cp
```
->
```
@cp:    lda plen                    ; the length, counted down first
        bne :+
        lda plen+1
        beq @cpdone
        dec plen+1
:       dec plen
        lda (zsrc),y
        sta (zdst),y
        inc zsrc
        bne :+
        inc zsrc+1
:       inc zdst
        bne @cp
        inc zdst+1
        bne @cp                     ; (zdst never wraps)
```

Moves the length decrement before the byte copy (independent memory) and merges the zero test into it: lda plen; bne -> lo nonzero, just dec lo (same as original's lda plen/bne/dec). lo zero: lda plen+1; beq exit (= plen|plen+1 == 0), else dec hi, dec lo (borrow). Then copy and pointer increments; the jmp @cp becomes bne @cp after inc zdst (Z=0 when no wrap) and bne @cp after inc zdst+1 (Z=0: never wraps). Exit: A = 0, flags Z=1 as before at @cpdone which reloads A. SUPERSEDES C1b (apply one). NOTE: window i0106 is outside r073; +2 cycles on the zero-length exit path -- reject if that rule is strict and take C1b. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: Equivalent countdown (lo!=0: dec lo; lo=0: exit if hi=0 else dec hi, dec lo); Y=0 constant; zdst+1 <= $C0 never 0; A/flags dead at @cpdone (lda ztab). The only slower path is the +2 exit test of a zero-length piece, and every piece in BANKS is >= 64 bytes (smallest b4c/b6c = 64), each iteration saving 13 cycles, so no executing path is slower. Supersedes C1b. 38->34 bytes. Independently re-verified: all 16 originals match lines lo..hi verbatim; each candidate (and the full primary set, and the fallback set) assembles and was run in my own py65 whole-loader model (40 random socket/ROM/alias/board/key configs, 38 reaching BANKCODE, 2 noram) -- banks, main RAM, map/score/board/fdc/drive and the I/O+OSWRCH log identical to base, total cycles never higher; a deliberately broken control was caught in every config.

## r073:1  modelb/src/loader.s:168-180  (window i0128)  M -0 B -1  [high]  review: accept

Cycles: Iteration without zdst wrap 43->40 (jmp gone, bne @cp taken 3 = the original bne :+ taken 3); zdst-wrap iteration equal (bne nt 2 + inc 5 + bne 3 vs bne nt 2 + inc 5 + jmp 3); exit path untouched.

Assumption: [C1b] Y = 0 constant in the loop (loader.s:164). zdst+1 never wraps to 0 (pieces end by $B289, build.sh). Only apply if C1 was not applied.

```
        lda (zsrc),y
        sta (zdst),y
        inc zsrc
        bne :+
        inc zsrc+1
:       inc zdst
        bne :+
        inc zdst+1
:       lda plen
        bne :+
        dec plen+1
:       dec plen
        jmp @cp
```
->
```
        lda plen                    ; the length, counted down first
        bne :+
        dec plen+1
:       dec plen
        lda (zsrc),y
        sta (zdst),y
        inc zsrc
        bne :+
        inc zsrc+1
:       inc zdst
        bne @cp
        inc zdst+1
        bne @cp                     ; (zdst never wraps)
```

Reorders the loop body: the length decrement (lda plen/bne/dec plen+1/dec plen) runs before the copy instead of after (they touch disjoint memory; A reloaded by lda (zsrc),y). The pointer increments now end the body: inc zdst/bne @cp and inc zdst+1/bne @cp (Z=0, no wrap) replace inc zdst/bne :+/inc zdst+1/.../jmp @cp. Loop test at @cp unchanged. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: FALLBACK only if index 0 (C1) is not applied (overlapping lines). Decrement moved before copy (disjoint memory, A reloaded), inc zdst/bne @cp twice replaces bne :+/.../jmp; zdst never wraps. Anon count 3->2 inside the range only. 1 byte. Sim-verified (see index 0/4).

## r073:2  modelb/src/loader.s:189-191  (window i0137)  M -0 B -3  [high]  review: accept

Cycles: loop back 5->3 (beq nt 2 + jmp 3 -> bne taken 3); exit 3->2.

Assumption: [C2] @piece is within branch range: bne at $132 to $CD is -103 in the current build and every other candidate only shrinks the distance.

```
        beq :+
        jmp @piece
:
```
->
```
        bne @piece
```

beq over jmp -> inverted branch; dec npieces sets Z as before. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: beq over jmp -> bne @piece; Z from dec npieces. Removed ':' at 191 is referenced only by this beq (next anon ref is bpl :- at 310 -> 307). In range in every combination assembled (alone, with all, with fallbacks). 3 bytes. Sim-verified (see index 0/4).

## r073:3  modelb/src/loader.s:240-241  (window i0164)  M -0 B -2  [high]  review: accept

Cycles: 2 cycles faster on both paths (cmp gone; bmi 2/3 = beq 2/3).

Assumption: [C4a] Every write-bank entry's first byte is a piece bank 4..7 (build.sh piece_bytes asserts the entry lies in exactly one piece; pieces are banks 4..7), terminator $FF. A still = that byte at tax (ldx board does not touch A).

```
        cmp #$FF
        beq @wfixdone
```
->
```
        bmi @wfixdone               ; the $FF (a bank is 4..7)
```

lda (zsrc),y sets N from the byte: $FF -> N=1, 4..7 -> N=0; bmi == cmp #$FF/beq on this domain. Z/C afterwards: next instruction ldx board sets N,Z; C not consumed before being reset (the lsr/adc of C4 set C from board). Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: First byte of every write-bank entry is a piece bank 4..7 (build.sh piece_bytes needs bank to hit a piece) or the $FF terminator: bmi == cmp #$FF/beq. Next ldx board resets N/Z; C not consumed. 2 bytes. Sim-verified (see index 0/4).

## r073:4  modelb/src/loader.s:244-318  (window i0189)  M -0 B -27  [high]  review: accept

Cycles: kinds, from wrx's return: Solidisk 58->58, Watford constant 68->68, Watford $FE 75->75 (exact); selbank jsr+body+fallthrough = the inline body + jsr wrx exactly; @wnext loop back 3->3; @wfixdone 15->11 (ldx/stx/stx) and dsk_board 8->4 (sta only).

Assumption: [C4] wrx leaves A = board on every path (lda board; beq/cmp/sta/stx don't change A) -- the comment is updated to say so; used after jsr selbank in @wfix and after jsr wrx at @wfixdone. board is 1 or 2 past `ldx board; beq @wnext`. BOARD_WATFORD=1, BOARD_SOLIDISK=2, >WRSEL_WATFORD=$FF=>WRSEL_SOLIDISK+1 (asserted). Y=3 after the kind read. A/Y/flags dead at @wnext (lda zsrc) and @wfix reloads Y. zsrc+1 never wraps (BANKS is 20048 bytes at $2000). Every caller of wrx treats A as destroyed afterwards (lines 163,204,249,301,385,421,448,488: each is followed by an A load or a jsr @flip that loads A first). selbank is a new normal label placed after jmp BANKCODE: it ends no cheap-label scope in use (wrx already starts one). Candidates C0 and C3b depend on selbank.

```
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx
        iny
        lda (zsrc),y
        sta zdst
        iny
        lda (zsrc),y
        sta zdst+1
        iny
        lda (zsrc),y                ; the kind
        tax
        lda board
        cmp #BOARD_SOLIDISK
        beq @wsol
        cpx #$FE
        beq @wdyn
        lda map-4,x                 ; Watford, a constant bank: sta $FF30 + its socket
        ora #<WRSEL_WATFORD
        ldy #1
        sta (zdst),y
        lda #>WRSEL_WATFORD
        iny
        sta (zdst),y
        jmp @wnext
@wdyn:  lda #$9D                    ; Watford, the bank in X: sta $FF30,x
        ldy #0
        sta (zdst),y
        lda #<WRSEL_WATFORD
        iny
        sta (zdst),y
        lda #>WRSEL_WATFORD
        iny
        sta (zdst),y
        jmp @wnext
@wsol:  lda #<WRSEL_SOLIDISK        ; Solidisk: sta $FE60, the bank being in A
        ldy #1
        sta (zdst),y
        lda #>WRSEL_SOLIDISK
        iny
        sta (zdst),y
@wnext: lda zsrc
        clc
        adc #4
        sta zsrc
        bcc @wfix
        inc zsrc+1
        jmp @wfix
@wfixdone:
        ; ---- the driver's configuration, the banks themselves and the board, into bank 7
        lda map+3
        sta ROMSELC
        sta ROMSEL
        ldx map+3
        jsr wrx
        lda fdc
        sta dsk_type
        lda drive
        sta dsk_drv
        ldx #3
:       lda map,x
        sta dsk_banks,x
        dex
        bpl :-
        lda board
        sta dsk_board
        jmp BANKCODE                ; bank 7's entry vector (bank 7 is paged)

; ---------------------------------------------------------------- the write bank
; X = a socket: make it the one a store reaches, on a board that chooses that apart
; from ROMSEL.  A is destroyed.
wrx:    lda board
```
->
```
        jsr selbank
        lsr                         ; A = board (wrx leaves it): C = Watford
        iny
        lda (zsrc),y
        sta zdst
        iny
        lda (zsrc),y
        sta zdst+1
        iny
        lda (zsrc),y                ; the kind
        tax
        .assert BOARD_WATFORD = 1 && BOARD_SOLIDISK = 2, error, "lsr: C = Watford"
        .assert >WRSEL_WATFORD = >WRSEL_SOLIDISK + 1, error, "adc #0: C picks the page"
        dey
        lda #>WRSEL_SOLIDISK        ; the high byte, +1 for Watford
        adc #0
        sta (zdst),y
        dey
        lsr                         ; C = Watford again
        bcc @wsol
        lda #<WRSEL_WATFORD
        cpx #$FE
        beq @wdyn
        ora map-4,x                 ; Watford, a constant bank: sta $FF30 + its socket
        sta (zdst),y
        bne @wnext
@wdyn:  sta (zdst),y                ; Watford, the bank in X: sta $FF30,x
        dey
        lda #$9D
        sta (zdst),y
        bne @wnext
@wsol:  lda #<WRSEL_SOLIDISK        ; Solidisk: sta $FE60, the bank being in A
        sta (zdst),y
@wnext: lda zsrc
        clc
        adc #4
        sta zsrc
        bcc @wfix
        inc zsrc+1
        bne @wfix                   ; (zsrc never wraps)
@wfixdone:
        ; ---- the driver's configuration, the banks themselves and the board, into bank 7
        ldx map+3
        stx ROMSELC
        stx ROMSEL
        jsr wrx                     ; (A = board on return)
        sta dsk_board
        lda fdc
        sta dsk_type
        lda drive
        sta dsk_drv
        ldx #3
:       lda map,x
        sta dsk_banks,x
        dex
        bpl :-
        jmp BANKCODE                ; bank 7's entry vector (bank 7 is paged)

; ---------------------------------------------------------------- the write bank
; A = a bank (4..7): page its socket (map) and fall into wrx with X = that socket.
selbank:
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
; X = a socket: make it the one a store reaches, on a board that chooses that apart
; from ROMSEL.  A = board on return (@wfix and @wfixdone use it).
wrx:    lda board
```

(1) the inline tax/lda map-4,x/sta ROMSELC/sta ROMSEL/tax/jsr wrx becomes jsr selbank, a copy of that body falling into wrx. (2) lsr: A=board -> C=1 Watford, C=0 Solidisk; the iny/lda/sta/tax reads keep C. dey (Y=2); lda #$FE; adc #0 -> $FF Watford / $FE Solidisk, written at +2 as before; dey (Y=1); lsr of $FF/$FE recovers C; bcc @wsol writes $60 at +1 (orig $60/$FE). Watford: A=$30; cpx #$FE; constant: ora map-4,x = map|$30 at +1 (orig lda map/ora #$30), $FF at +2; $FE kind: $30 at +1, $9D at +0. Write order within an instruction's 3 bytes changes (interrupts off, bank already selected). bne @wnext: Z=0 from ora (>= $30) and lda #$9D. (3) jmp @wfix -> bne (inc zsrc+1 never 0). (4) ldx map+3/stx/stx replaces lda/sta/sta/ldx (A dead: wrx loads it). (5) sta dsk_board straight after jsr wrx (A = board) replaces lda board/sta dsk_board. Supersedes fallbacks C4c, C4d, C5. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: selbank (tax..tax falling into wrx) = the inline body + jsr wrx; wrx leaves A = board on all paths and board is 1/2 past ldx board/beq @wnext; lsr/adc #0/lsr rebuild C and the page byte ($FF Watford/$FE Solidisk at +2, $30|map / $30+$9D / $60 at +1/+0) exactly; D clear under MOS; bne after ora (>=$30) and lda #$9D always taken; zsrc never wraps; @wfixdone ldx/stx/stx then A=board -> sta dsk_board. A/flags at jmp BANKCODE differ (map+0, N=1) but init.s entry starts ldx #../lda @stub,x so they are dead. selbank placed after jmp BANKCODE ends no live cheap-label scope. Cycles per kind 58/68/75 unchanged. 27 bytes; enables C0/C3b (+10 each). Independently re-verified in my own py65 whole-loader model (40 random configs incl. all three boards, aliases, noram): identical banks/RAM/vars/I/O log, cycles never higher.

## r073:5  modelb/src/loader.s:250-287  (window i0195)  M -0 B -20  [high]  review: accept

Cycles: from wrx's return: Solidisk 58->58, Watford constant 68->68, Watford $FE 75->75, exact.

Assumption: [C4c] FALLBACK: apply only if C4 was not applied. jsr wrx at line 249 returns A = board (wrx: lda board, then no A write), board is 1 or 2 here (ldx board/beq @wnext above). Constants asserted. Y=3 at the kind. A/Y dead at @wnext.

```
        iny
        lda (zsrc),y
        sta zdst
        iny
        lda (zsrc),y
        sta zdst+1
        iny
        lda (zsrc),y                ; the kind
        tax
        lda board
        cmp #BOARD_SOLIDISK
        beq @wsol
        cpx #$FE
        beq @wdyn
        lda map-4,x                 ; Watford, a constant bank: sta $FF30 + its socket
        ora #<WRSEL_WATFORD
        ldy #1
        sta (zdst),y
        lda #>WRSEL_WATFORD
        iny
        sta (zdst),y
        jmp @wnext
@wdyn:  lda #$9D                    ; Watford, the bank in X: sta $FF30,x
        ldy #0
        sta (zdst),y
        lda #<WRSEL_WATFORD
        iny
        sta (zdst),y
        lda #>WRSEL_WATFORD
        iny
        sta (zdst),y
        jmp @wnext
@wsol:  lda #<WRSEL_SOLIDISK        ; Solidisk: sta $FE60, the bank being in A
        ldy #1
        sta (zdst),y
        lda #>WRSEL_SOLIDISK
        iny
        sta (zdst),y
```
->
```
        lsr                         ; A = board (wrx leaves it): C = Watford
        iny
        lda (zsrc),y
        sta zdst
        iny
        lda (zsrc),y
        sta zdst+1
        iny
        lda (zsrc),y                ; the kind
        tax
        .assert BOARD_WATFORD = 1 && BOARD_SOLIDISK = 2, error, "lsr: C = Watford"
        .assert >WRSEL_WATFORD = >WRSEL_SOLIDISK + 1, error, "adc #0: C picks the page"
        dey
        lda #>WRSEL_SOLIDISK        ; the high byte, +1 for Watford
        adc #0
        sta (zdst),y
        dey
        lsr                         ; C = Watford again
        bcc @wsol
        lda #<WRSEL_WATFORD
        cpx #$FE
        beq @wdyn
        ora map-4,x                 ; Watford, a constant bank: sta $FF30 + its socket
        sta (zdst),y
        bne @wnext
@wdyn:  sta (zdst),y                ; Watford, the bank in X: sta $FF30,x
        dey
        lda #$9D
        sta (zdst),y
        bne @wnext
@wsol:  lda #<WRSEL_SOLIDISK        ; Solidisk: sta $FE60, the bank being in A
        sta (zdst),y
```

As C4 part (2). Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: FALLBACK only if C4 (index 4) is not applied (same lines). Correct on the original code: A = board after jsr wrx at 249. 20 bytes. Sim-verified (see index 0/4).

## r073:6  modelb/src/loader.s:294-294  (window i0233)  M -0 B -1  [high]  review: accept

Cycles: 3->3

Assumption: [C4d] FALLBACK (in C4). zsrc+1 never wraps to 0: BANKS (20048 bytes) at $2000.

```
        jmp @wfix
```
->
```
        bne @wfix                   ; (zsrc never wraps)
```

inc zsrc+1 leaves Z=0, so bne always taken like jmp. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: FALLBACK only if C4 is not applied. inc zsrc+1 never reaches 0 (BANKS $2000..$6E50). 1 byte. Sim-verified (see index 0/4).

## r073:7  modelb/src/loader.s:295-312  (window i0240)  M -0 B -6  [high]  review: accept

Cycles: 15->11 for the select, 8->4 for dsk_board

Assumption: [C5] FALLBACK (in C4). wrx returns with A = board on all three paths; A dead after lda map+3's stores (wrx reloads A).

```
@wfixdone:
        ; ---- the driver's configuration, the banks themselves and the board, into bank 7
        lda map+3
        sta ROMSELC
        sta ROMSEL
        ldx map+3
        jsr wrx
        lda fdc
        sta dsk_type
        lda drive
        sta dsk_drv
        ldx #3
:       lda map,x
        sta dsk_banks,x
        dex
        bpl :-
        lda board
        sta dsk_board
```
->
```
@wfixdone:
        ; ---- the driver's configuration, the banks themselves and the board, into bank 7
        ldx map+3
        stx ROMSELC
        stx ROMSEL
        jsr wrx                     ; (A = board on return)
        sta dsk_board
        lda fdc
        sta dsk_type
        lda drive
        sta dsk_drv
        ldx #3
:       lda map,x
        sta dsk_banks,x
        dex
        bpl :-
```

stx zp/abs exist on NMOS; X = map+3 as before for wrx. The store of dsk_board moves before the others (all plain RAM stores in bank 7, order irrelevant). Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: FALLBACK only if C4 is not applied. X = map+3 as before, same $F4/$FE30 write order, A = board from wrx; dsk_board store order irrelevant (bank 7 RAM); A/flags at jmp BANKCODE dead (entry reloads). 6 bytes. Sim-verified (see index 0/4).

## r073:8  modelb/src/loader.s:158-163  (window i0092)  M -0 B -10  [high]  review: accept

Cycles: identical: jsr selbank (6) + same 5-instruction body + fall into wrx vs body + jsr wrx (6).

Assumption: [C0] DEPENDS on C4 (defines selbank). A = pbank (4..7) at the call, as tax expected. Window i0092 is outside r073 (reported because it is the same change).

```
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx                     ; and the write bank, on a board that has one
```
->
```
        jsr selbank                 ; its socket, and the write bank on a board that has one
```

selbank is exactly tax/lda map-4,x/sta ROMSELC/sta ROMSEL/tax then wrx; Y preserved, return to the ':' ldy #0. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: DEPENDS on C4 (selbank). A = pbank 4..7; Y preserved and reloaded at ':' ldy #0 anyway; ':' at 164 (target of beq :+ at 157) kept. 10 bytes (13 -> 3). Sim-verified (see index 0/4).

## r073:9  modelb/src/loader.s:195-198  (window i0146)  M -0 B -4  [high]  review: accept

Cycles: per entry 2+2 cycles faster (ldy #0 and cmp gone).

Assumption: [C3a] Y = 0 on entry to @fix: the last piece ran `: ldy #0` (loader.s:164) and neither the copy loop, @cpdone nor wrx changes Y; on loop-back Y = 0 from the ldy #0 at line 211 (nothing after it changes Y). Every bank-patch entry's first byte is a bank 4..7 (build.sh piece_bytes), terminator $FF.

```
@fix:   ldy #0
        lda (zsrc),y
        cmp #$FF
        beq @fixdone
```
->
```
@fix:   lda (zsrc),y                ; (Y = 0 here: the copy loop and this loop leave it so)
        bmi @fixdone                ; the $FF (a bank is 4..7)
```

lda (zsrc),y with Y=0 reads the same byte; bmi == cmp #$FF/beq on {4..7,$FF}; tax follows (flags not consumed). Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: Y = 0 on every entry to @fix (first entry from copy loop which never changes Y after ldy #0 at 164, selbank/wrx don't touch Y; loop-back after ldy #0 at 211). Bank-patch bank byte is 4..7 (piece_bytes) or $FF: bmi valid; tax follows. 4 bytes. Sim-verified (see index 0/4).

## r073:10  modelb/src/loader.s:199-204  (window i0146)  M -0 B -10  [high]  review: accept

Cycles: identical (jsr selbank + body = body + jsr wrx).

Assumption: [C3b] DEPENDS on C4 (defines selbank). A = bank byte (4..7); Y preserved by selbank/wrx.

```
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx
```
->
```
        jsr selbank
```

Same six instructions, now in selbank. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: DEPENDS on C4 (selbank). Same six instructions; Y preserved for the following iny. 10 bytes. Sim-verified (see index 0/4).

## r073:11  modelb/src/loader.s:212-221  (window i0146)  M -0 B -6  [high]  review: accept

Cycles: 34->24 per patched byte.

Assumption: [C3c] map entries are socket numbers 0..15 (txa/sta map,y with X<16, findram @pick), so map|high-nibble is an ora into a zero low nibble as before; the patched byte's low nibble is 4..7 (build.sh asserts). ztmp is written nowhere else and read only by the removed ora (grep: lines 217,220).

```
        lda (zdst),y
        pha
        and #$0F
        tax
        lda map-4,x
        sta ztmp
        pla
        and #$F0
        ora ztmp
        sta (zdst),y
```
->
```
        lda (zdst),y
        and #$0F
        tax
        eor (zdst),y                ; the high nibble, kept
        ora map-4,x
        sta (zdst),y
```

v = (zdst),y; t = v & $0F in A and X; t EOR v = v & $F0; ORA map-4,x = (v&$F0)|map[t-4], the original value; stored at the same address; X = t as before; A and N/Z the same value as original's ora ztmp. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: (v&$0F) eor v = v&$F0; ora map-4,x (socket 0..15) reproduces the value; X and A/NZ identical; Y=0; zdst is bank RAM (reading twice harmless); ztmp has no other reader. 34->24 cycles, 18->12 bytes. Sim-verified (see index 0/4).

## r073:12  modelb/src/loader.s:228-228  (window i0152)  M -0 B -1  [high]  review: accept

Cycles: 3->3

Assumption: [C3d] zsrc+1 never wraps to 0 (BANKS at $2000..$6E50).

```
        jmp @fix
```
->
```
        bne @fix                    ; (zsrc never wraps)
```

inc zsrc+1 leaves Z=0, bne always taken. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: inc zsrc+1 never 0 (BANKS at $2000..$6E50): bne always taken, 3->3 cycles. 1 byte. Sim-verified (see index 0/4).

## r073:13  modelb/src/loader.s:349-354  (window i0239)  M -0 B -2  [high]  review: accept

Cycles: 2 cycles faster (one lda #0 gone).

Assumption: [F1] @count and @flip do not read or write bestb (loader.s:483-517); A = BOARD_STD still at the sta bestb.

```
        lda #BOARD_STD
        sta board
        jsr @count
        sta best                    ; (plain wins a tie)
        lda #BOARD_STD
        sta bestb
```
->
```
        lda #BOARD_STD
        sta board
        sta bestb                   ; (plain wins a tie)
        jsr @count
        sta best
```

bestb = BOARD_STD stored before the count instead of after; same value, nothing reads it in between. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: @count/@flip/wrx never touch bestb; A still BOARD_STD at sta bestb. 2 bytes. Sim-verified (see index 0/4).

## r073:14  modelb/src/loader.s:362-363  (window i0248)  M -0 B -2  [high]  review: accept

Cycles: 6->6 (lda # 2 + sta abs 4 vs inc abs 6).

Assumption: [F2] bestb = BOARD_STD = 0 here (set at line 354 / by F1; only other write is line 374, later); BOARD_WATFORD = BOARD_STD+1 (asserted). A dead: the ':' after reloads A (lda #$0F), flags reset by it.

```
        lda #BOARD_WATFORD
        sta bestb
```
->
```
        .assert BOARD_WATFORD = BOARD_STD + 1, error, "inc bestb"
        inc bestb                   ; BOARD_STD -> BOARD_WATFORD
```

inc bestb turns 0 into 1 = BOARD_WATFORD. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: bestb = BOARD_STD = 0 here (line 354 or F1), BOARD_WATFORD = 1 asserted; A/flags dead (':' at 364 reloads A). 6->6 cycles, 2 bytes. Sim-verified (see index 0/4).

## r073:15  modelb/src/loader.s:369-380  (window i0263)  M -0 B -12  [high]  review: accept

Cycles: Solidisk wins 31->9; tie 27->21; fewer 25->22.

Assumption: [F3] best and bestb are read nowhere else (grep: lines 350-376 only); @count leaves board as set (it only reads it via wrx); bestb is 0 or 1 at line 375 in the new code (only lines 354 and 363 wrote it). Window i0263 is outside r073 (reported because found while reading).

```
        cmp best
        bcc :+
        beq :+
        sta best
        lda #BOARD_SOLIDISK
        sta bestb
:       lda bestb
        sta board
        cmp #BOARD_SOLIDISK         ; not a Solidisk: give the user port back
        beq @classify
        lda #0
        sta $FE62
```
->
```
        cmp best
        beq :+                      ; a tie: plain or Watford keeps it
        bcs @classify               ; Solidisk wins: board is set, port B stays outputs
:       lda bestb                   ; plain or Watford: not a Solidisk,
        sta board
        lda #0                      ; so give the user port back
        sta $FE62
```

Original: A>best -> best=A, bestb=2; then board=bestb (=2, already board), cmp #2/beq @classify skipping the $FE62 clear. New: A>best (beq not taken, C=1) goes straight to @classify with board=2 as set and $FE62 left $0F -- same state (best not used again). A<=best: falls/branches to board=bestb (0 or 1, never Solidisk), so the cmp #2/beq is always not-taken and dropped; $FE62 := 0 as before. Verified in a py65 simulation of the whole loader (MOS calls stubbed, 16 sockets, plain/Watford/Solidisk write-select boards, 18 RAM/ROM layouts incl. the noram exit): banks, main RAM, map/score/board and the I/O write log identical to the unmodified loader.

Review: Solidisk-wins path: board already 2 (set at 367, @count only reads it), $FE62 kept $0F, best/bestb read nowhere after -> same state; tie/fewer: bestb is 0/1 (only 354/363 write it now) so the dropped cmp #2/beq was never taken. ':' at 375 kept, anon count unchanged. All paths faster. 12 bytes. Sim-verified in my own py65 whole-loader model across plain/Watford/Solidisk configs.

## r074:0  modelb/src/loader.s:318-513  (window i0256)  M -0 B -55  [high]  review: accept

Cycles: Model B only (stand-alone loader). Every path no slower: selwr sites (sig, res) jsr6+stx3+stx4 = stx3+stx4+jsr6 = 13; classify lda2 sta5 jsr6(selflip) jsr6(selwr) stx3 stx4 = stx3 stx4 jsr6 lda2 sta5 jsr6 = 26, then @flip tail from cmp: stuck 25->16, not stuck 26->16 (cmp4 sty4 clc2 rts6), caller bne = bcc; count loop site likewise equal, bank restore in @count removed (-11); wrx lsr 2 = cmp #imm 2; head: sta bestb folded (-2), inc board/inc bestb 6 = lda#2+sta abs4 6; Solidisk compare: win 31->9, lose 25->22, tie 23->21; res: inx 2 = ldx#0 2, ldy abs,x4+iny2 = lda abs,x4+cmp#2; choice: ldy#0/sty want 6 vs 8, fail exit sec removed (-2), found exit beq3+clc2+rts6 unchanged. New selbank: its callers (P1-P3) tax2 lda4 tax2 stx3 stx4 + jsr6 = tax lda sta3 sta4 tax jsr = 21.

Assumption: Board constants 0/1/2 (defs.inc:89-91; asserted in the proposal). STY, CLC, RTS, JSR leave Z. After every wrx call A is reloaded before use and C is not consumed: loader.s 164-165 (ldy/lda plen, next adc after clc), 205-206 and 250 (iny, lda (zsrc),y; adc after clc), 302 (lda fdc ... jmp BANKCODE; init.s entry at BANKCODE uses only ldx/lda/sta/dex/bpl/jmp), and every findram site reloads A. @count is only called after findram's sei, and between the three counts and @classify nothing reads $8000-$BFFF or calls the MOS (only $FE62 and board/best/bestb stores); oldbank is restored after @res exactly as before. best/bestb are read only inside findram (grep lines 352-375); noram reads board only, which is 2 on the Solidisk-wins path as before. X = $FF after the @chk loop (dex/bpl exit). score holds only 0,1,2,$FE,$FF (so iny = 0 only for $FF). Y dead in @res (ldy #0 follows the loop). @flip's only callers were lines 388/489 (now @selflip, testing Z); @found only referenced at 470 (beq, ~55 bytes: in range); @no only by the removed bne. X = 16 at the fewer-than-four exit unchanged (noram candidates rely on it). selbank/selwr sit after jmp BANKCODE: nothing falls into them; selbank matches r072's proposed selbank contract (A = code 4..7, X = socket out, A destroyed).

```
wrx:    lda board
        beq @r
        cmp #BOARD_SOLIDISK
        beq @s
        sta WRSEL_WATFORD,x         ; Watford: the address says which, the value nothing
@r:     rts
@s:     stx WRSEL_SOLIDISK          ; Solidisk: port B bits 0-3 (DDRB was set by findram)
        rts

; ---------------------------------------------------------------- the sideways RAM
; Which sockets hold RAM, and which four the game gets.  The test is the one Stuart
; McConnachie's sideways RAM Elite loader used (1988; Mark Moxon's commentary): page
; the bank through $F4 and ROMSEL, flip bit 0 of the ROM type byte at $8006 and see
; whether it stuck, put it back.  A floating bus fails it (both reads see the same
; value), a write-protected board fails it too -- it looks like ROM, and the message
; says so.  First the board: the test is run over the 16 banks writing through ROMSEL
; alone, then again selecting the write bank the Watford way ($FF30 + bank), then the
; Solidisk way (user VIA port B, bits 0-3 made outputs); the board is the way that
; finds the MOST banks (a board's write latch rests on some bank, usually 0, so the
; plain test "finds" that one bank on a board machine too), plain winning a tie; and
; every write below goes through wrx.  A machine with RAM of two kinds gets the kind
; with more.  Every bank then gets a class: 0, RAM with no ROM image in it; 1, RAM with
; an image the MOS is not running (no entry in its table at $02A1: left there by an
; earlier load); 2, RAM holding a ROM the MOS recognised -- taken only when nothing
; else is left, which is safe here because nothing calls the MOS once the pieces go
; down.  Two socket numbers that reach the same RAM (a board answering two numbers)
; are found by a signature written to each and read back -- not Elite's byte-for-byte
; comparison of the banks, which would call four blank banks one -- and the extra
; numbers dropped.  The four are the lowest-numbered of the best class.
findram:
        sei
        lda #BOARD_STD
        sta board
        jsr @count
        sta best                    ; (plain wins a tie)
        lda #BOARD_STD
        sta bestb
        lda #BOARD_WATFORD
        sta board
        jsr @count
        cmp best
        bcc :+
        beq :+
        sta best
        lda #BOARD_WATFORD
        sta bestb
:       lda #$0F                    ; Solidisk: port B bits 0-3 as outputs
        sta $FE62
        lda #BOARD_SOLIDISK
        sta board
        jsr @count
        cmp best
        bcc :+
        beq :+
        sta best
        lda #BOARD_SOLIDISK
        sta bestb
:       lda bestb
        sta board
        cmp #BOARD_SOLIDISK         ; not a Solidisk: give the user port back
        beq @classify
        lda #0
        sta $FE62
@classify:
        ldx #15
@b:     stx ROMSELC
        stx ROMSEL
        jsr wrx
        lda #$FF                    ; not RAM until proven
        sta score,x
        jsr @flip
        bcc @bnext
        lda ROMTYPE,x               ; a ROM the MOS is using
        beq :+
        lda #2
        bne @bsc
:       ldy $8007                   ; a ROM image: 0 "(C)" at the copyright offset
        lda $8000,y
        bne @bfree
        iny
        lda $8000,y
        cmp #'('
        bne @bfree
        iny
        lda $8000,y
        cmp #'C'
        bne @bfree
        iny
        lda $8000,y
        cmp #')'
        bne @bfree
        lda #1
        bne @bsc
@bfree: lda #0
@bsc:   sta score,x
@bnext: dex
        bpl @b
        ; the signatures: 15 down, each RAM bank's $8007 saved and its number written
        ldx #15
@sig:   lda score,x
        bmi @snext
        stx ROMSELC
        stx ROMSEL
        jsr wrx
        lda $8007
        sta saved,x
        txa
        ora #$C0
        sta $8007
@snext: dex
        bpl @sig
        ldx #15
@chk:   lda score,x
        bmi @cnext
        stx ROMSELC
        stx ROMSEL
        txa
        ora #$C0
        cmp $8007
        beq @cnext
        lda #$FE                    ; another number reached this RAM after us and
        sta score,x                 ; keeps it (still to be restored: not $FF)
@cnext: dex
        bpl @chk
        ldx #0                      ; restored in the reverse order of the saving, so
@res:   lda score,x                 ; a chain of aliases unwinds to its first byte
        cmp #$FF
        beq @rnext
        stx ROMSELC
        stx ROMSEL
        jsr wrx
        lda saved,x
        sta $8007
@rnext: inx
        cpx #16
        bne @res
        lda oldbank
        sta ROMSELC
        sta ROMSEL
        cli
        ; the choice: the lowest sockets of class 0, then of class 1, then of class 2
        ldy #0
        lda #0
        sta want
@cls:   ldx #0
@pick:  lda score,x
        cmp want
        bne @pnext
        txa
        sta map,y
        iny
        cpy #4
        beq @found
@pnext: inx
        cpx #16
        bne @pick
        inc want
        lda want
        cmp #3
        bne @cls
        sec                         ; fewer than four
        rts
@found: clc
        rts
        ; --- A = how many of the 16 banks take a write, the board being as set
@count: lda #0
        sta want                    ; (want is free until the choice below)
        ldx #15
:       stx ROMSELC
        stx ROMSEL
        jsr wrx
        jsr @flip
        bcc :+
        inc want
:       dex
        bpl :--
        lda oldbank
        sta ROMSELC
        sta ROMSEL
        lda want
        rts
        ; --- C = 1 if the bank paged (and selected for writing) takes a write: flip bit 0
        ; of the ROM type byte, look, put it back
@flip:  lda $8006
        tay
        eor #1
        sta $8006
        cmp $8006
        php
        sty $8006
        plp
        bne @no
        sec
        rts
@no:    clc
        rts
```
->
```
; A = a bank's code number (4..7): page its socket for reading and writing.
; X = the socket, A destroyed.
selbank:
        tax
        lda map-4,x
        tax                         ; and into selwr
; X = a socket: page it for reading, and writing (into wrx)
selwr:  stx ROMSELC
        stx ROMSEL
wrx:    lda board
        beq @r
        .assert BOARD_WATFORD = 1 && BOARD_SOLIDISK = 2, error, "wrx tells the boards by bit 0"
        lsr
        bcc @s
        sta WRSEL_WATFORD,x         ; Watford: the address says which, the value nothing
@r:     rts
@s:     stx WRSEL_SOLIDISK          ; Solidisk: port B bits 0-3 (DDRB was set by findram)
        rts

; ---------------------------------------------------------------- the sideways RAM
; Which sockets hold RAM, and which four the game gets.  The test is the one Stuart
; McConnachie's sideways RAM Elite loader used (1988; Mark Moxon's commentary): page
; the bank through $F4 and ROMSEL, flip bit 0 of the ROM type byte at $8006 and see
; whether it stuck, put it back.  A floating bus fails it (both reads see the same
; value), a write-protected board fails it too -- it looks like ROM, and the message
; says so.  First the board: the test is run over the 16 banks writing through ROMSEL
; alone, then again selecting the write bank the Watford way ($FF30 + bank), then the
; Solidisk way (user VIA port B, bits 0-3 made outputs); the board is the way that
; finds the MOST banks (a board's write latch rests on some bank, usually 0, so the
; plain test "finds" that one bank on a board machine too), plain winning a tie; and
; every write below goes through wrx.  A machine with RAM of two kinds gets the kind
; with more.  Every bank then gets a class: 0, RAM with no ROM image in it; 1, RAM with
; an image the MOS is not running (no entry in its table at $02A1: left there by an
; earlier load); 2, RAM holding a ROM the MOS recognised -- taken only when nothing
; else is left, which is safe here because nothing calls the MOS once the pieces go
; down.  Two socket numbers that reach the same RAM (a board answering two numbers)
; are found by a signature written to each and read back -- not Elite's byte-for-byte
; comparison of the banks, which would call four blank banks one -- and the extra
; numbers dropped.  The four are the lowest-numbered of the best class.
findram:
        sei
        lda #BOARD_STD
        sta board
        sta bestb                   ; (plain wins a tie)
        jsr @count
        sta best
        .assert BOARD_WATFORD = BOARD_STD + 1 && BOARD_SOLIDISK = BOARD_WATFORD + 1, error, "findram steps board up"
        inc board                   ; BOARD_WATFORD
        jsr @count
        cmp best
        bcc :+
        beq :+
        sta best
        inc bestb                   ; BOARD_WATFORD
:       lda #$0F                    ; Solidisk: port B bits 0-3 as outputs
        sta $FE62
        inc board                   ; BOARD_SOLIDISK
        jsr @count
        cmp best
        beq :+
        bcs @classify               ; Solidisk found the most: board and port B stay
:       lda bestb
        sta board
        lda #0                      ; not a Solidisk: give the user port back
        sta $FE62
@classify:
        ldx #15
@b:     lda #$FF                    ; not RAM until proven
        sta score,x
        jsr @selflip
        bne @bnext
        lda ROMTYPE,x               ; a ROM the MOS is using
        beq :+
        lda #2
        bne @bsc
:       ldy $8007                   ; a ROM image: 0 "(C)" at the copyright offset
        lda $8000,y
        bne @bfree
        iny
        lda $8000,y
        cmp #'('
        bne @bfree
        iny
        lda $8000,y
        cmp #'C'
        bne @bfree
        iny
        lda $8000,y
        cmp #')'
        bne @bfree
        lda #1
        bne @bsc
@bfree: lda #0
@bsc:   sta score,x
@bnext: dex
        bpl @b
        ; the signatures: 15 down, each RAM bank's $8007 saved and its number written
        ldx #15
@sig:   lda score,x
        bmi @snext
        jsr selwr
        lda $8007
        sta saved,x
        txa
        ora #$C0
        sta $8007
@snext: dex
        bpl @sig
        ldx #15
@chk:   lda score,x
        bmi @cnext
        stx ROMSELC
        stx ROMSEL
        txa
        ora #$C0
        cmp $8007
        beq @cnext
        lda #$FE                    ; another number reached this RAM after us and
        sta score,x                 ; keeps it (still to be restored: not $FF)
@cnext: dex
        bpl @chk
        inx                         ; X = 0 (from $FF): restored in the reverse order of
@res:   ldy score,x                 ; the saving, so a chain of aliases unwinds to its
        iny                         ; first byte ($FF: not RAM)
        beq @rnext
        jsr selwr
        lda saved,x
        sta $8007
@rnext: inx
        cpx #16
        bne @res
        lda oldbank
        sta ROMSELC
        sta ROMSEL
        cli
        ; the choice: the lowest sockets of class 0, then of class 1, then of class 2
        ldy #0
        sty want
@cls:   ldx #0
@pick:  lda score,x
        cmp want
        bne @pnext
        txa
        sta map,y
        iny
        cpy #4
        beq @found
@pnext: inx
        cpx #16
        bne @pick
        inc want
        lda want
        cmp #3
        bne @cls
        rts                         ; fewer than four (C = 1: A = 3)
        ; --- A = how many of the 16 banks take a write, the board being as set (the
        ; bank is left paged: interrupts are off until findram restores oldbank)
@count: lda #0
        sta want                    ; (want is free until the choice below)
        ldx #15
:       jsr @selflip
        bne :+
        inc want
:       dex
        bpl :--
        lda want
        rts
        ; --- Z = 1 if bank X, paged and selected for writing, takes a write: flip bit 0
        ; of the ROM type byte, look, put it back
@selflip:
        jsr selwr
@flip:  lda $8006
        tay
        eor #1
        sta $8006
        cmp $8006
        sty $8006                   ; (a store leaves the flags)
@found: clc                         ; (the choice's exit: C = 0; Z is @flip's)
        rts
```

Composite of the fallbacks below plus a shared page-and-select helper. (1) selbank: tax/lda map-4,x/tax falls into selwr: stx ROMSELC/stx ROMSEL falls into wrx -- the stx pair is exactly the `stx ROMSELC / stx ROMSEL / jsr wrx` triple at the classify, sig, res and count sites (5 bytes saved at each of 4, cost 5). (2) wrx: board is 1 or 2 past the beq; lsr gives C = 1 for Watford (A = 0, and the Watford store's value is don't-care) and C = 0 for Solidisk -> bcc @s. (3) head: bestb stored from the same A = 0 as board; board 0->1->2 and bestb 0->1 by inc (known values). (4) Solidisk compare: if its count beats best, board is already BOARD_SOLIDISK and port B must stay as outputs: branch straight to @classify (best/bestb dead); otherwise board = bestb (0 or 1, never Solidisk) and $FE62 = 0 unconditionally. (5) @flip returns Z (cmp $8006; the sty restore and rts keep it), callers test bne where they tested bcc; php/plp were redundant (stores do not touch flags). @selflip = jsr selwr falling into @flip; classify moves its lda #$FF/sta score,x (main RAM) before the paging. (6) the choice's `sec` is dead (cmp #3 equal sets C); `lda #0/sta want` becomes sty want (Y = 0); @found's clc/rts shares @flip's rts (clc does not touch the Z @flip returns). (7) @res: inx from $FF, ldy/iny/beq tests $FF without A. (8) @count no longer restores oldbank between counts (interrupts off, no observer; findram restores it at the end). Built: LOADER 1324 -> 1269. Verified: py65 simulation of findram (original vs rewrite) over 700+ random machines -- RAM/ROM/empty sockets, aliased and write-protected RAM, plain/Watford/Solidisk hardware, random latch and oldbank, (C) images: identical map, score, board, C and X at exit, last $FE62 value, every RAM byte, ROMSEL/$F4 and write latch; total cycles never higher. Supersedes (makes stale) the fallbacks F1,A,B,C,D,E,R,P,S,K,FL,KFL below, which exist in case this composite is rejected.

Review: The original text matches lines 318-513. Assembled, LOADER goes from 1324 to 1269 bytes (55 saved, counting the 5-byte selbank that only candidates 1-3 call). Checks: the board constants are 0/1/2 (defs.inc 89-91), and the value written in the Watford store does not matter. The C/Z/N change after wrx reaches nothing that reads it: 163 goes to ldy, 204 and 249 go to iny, and 301 goes to lda fdc then jmp BANKCODE, where init.s's entry and start6 set C with cpx before any use. X is $FF after @chk. score only holds 0, 1, 2, $FE or $FF. Y is dead in @res. The beq to @found is in range (about 56 bytes). There are 5 anonymous labels before and after, in the same order. Dropping @count's bank restore is safe under sei, because nothing reads $8000-$BFFF between counts. My own py65 simulation of findram (270 random machines: plain, Watford and Solidisk boards, aliased and write-protected RAM, ROM images, random latch, oldbank and ROMSEL) gave identical map, score, board, C, X and I, every bank byte, and final ROMSEL/$F4/latch/DDRB/ORB, and never took more cycles.

## r074:1  modelb/src/loader.s:158-163  (window i0256)  M -0 B -10  [high]  review: accept

Cycles: jsr6 + selbank(tax2 lda4 tax2 stx3 stx4) = tax2 lda4 sta3 sta4 tax2 jsr6 = 21 before wrx; wrx itself unchanged or equal (lsr=cmp)

Assumption: DEPENDS on the composite candidate at loader.s 318-513 (window i0256) defining selbank (or r072's equivalent selbank); without it this fails to assemble. A = the bank code (4..7) on entry here: pieces loads it just above (lda pbank / beq). After the call A is reloaded before use (next: ldy #0; lda plen).

```
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx                     ; and the write bank, on a board that has one
```
->
```
        jsr selbank                 ; and the write bank, on a board that has one
```

selbank does tax / lda map-4,x / tax / stx ROMSELC / stx ROMSEL then wrx: the same socket written to $F4 and ROMSEL (stx of X = the socket instead of sta of A = the socket), the same X into wrx. Outside this region's instructions (start), reported here because the saving exists only through this region's helper.

Review: The original text matches. Needs index 0 for selbank; if r072 also adds a selbank, only one of the two definitions can be applied. selbank writes the same socket to $F4 and then ROMSEL, then calls wrx with the same X. Y is preserved, and A is reloaded afterwards. Cycles 21 = 21. Built with indices 0-4 applied, the loader goes from 1269 to 1231 bytes (10+10+10+8).

## r074:2  modelb/src/loader.s:198-204  (window i0256)  M -0 B -10  [high]  review: accept

Cycles: jsr6 + selbank(tax2 lda4 tax2 stx3 stx4) = tax2 lda4 sta3 sta4 tax2 jsr6 = 21 before wrx; wrx itself unchanged or equal (lsr=cmp)

Assumption: DEPENDS on the composite candidate at loader.s 318-513 (window i0256) defining selbank (or r072's equivalent selbank); without it this fails to assemble. A = the bank code (4..7) on entry here: @fix loads it just above (lda (zsrc),y / cmp #$FF / beq). After the call A is reloaded before use (next: iny; lda (zsrc),y).

```
        beq @fixdone
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx
```
->
```
        beq @fixdone
        jsr selbank
```

selbank does tax / lda map-4,x / tax / stx ROMSELC / stx ROMSEL then wrx: the same socket written to $F4 and ROMSEL (stx of X = the socket instead of sta of A = the socket), the same X into wrx. Outside this region's instructions (start), reported here because the saving exists only through this region's helper.

Review: The original text matches. Needs index 0 for selbank. Y = 0 from line 195 survives selbank (it does not use Y), as the iny at 205 needs. Same hardware writes in the same order. Saves 10 bytes; cycles equal.

## r074:3  modelb/src/loader.s:243-249  (window i0256)  M -0 B -10  [high]  review: accept

Cycles: jsr6 + selbank(tax2 lda4 tax2 stx3 stx4) = tax2 lda4 sta3 sta4 tax2 jsr6 = 21 before wrx; wrx itself unchanged or equal (lsr=cmp)

Assumption: DEPENDS on the composite candidate at loader.s 318-513 (window i0256) defining selbank (or r072's equivalent selbank); without it this fails to assemble. A = the bank code (4..7) on entry here: @wfix loads it just above (lda (zsrc),y / cmp / beq, then ldx board / beq keeps A). After the call A is reloaded before use (next: iny; lda (zsrc),y).

```
        beq @wnext                  ; plain: as assembled
        tax
        lda map-4,x
        sta ROMSELC
        sta ROMSEL
        tax
        jsr wrx
```
->
```
        beq @wnext                  ; plain: as assembled
        jsr selbank
```

selbank does tax / lda map-4,x / tax / stx ROMSELC / stx ROMSEL then wrx: the same socket written to $F4 and ROMSEL (stx of X = the socket instead of sta of A = the socket), the same X into wrx. Outside this region's instructions (start), reported here because the saving exists only through this region's helper.

Review: The original text matches. Needs index 0 for selbank. Y = 0 from line 238 is preserved for the iny at 250, and A is reloaded. Saves 10 bytes; cycles equal.

## r074:4  modelb/src/loader.s:297-301  (window i0256)  M -0 B -8  [high]  review: accept

Cycles: ldx abs4 + jsr6 + stx3 + stx4 = 17 vs lda4 sta3 sta4 ldx4 jsr6 = 21 (faster; not timing-critical boot code)

Assumption: DEPENDS on the composite at loader.s 318-513 defining selwr (falls into wrx). A dead after: lda fdc follows (line 302).

```
        lda map+3
        sta ROMSELC
        sta ROMSEL
        ldx map+3
        jsr wrx
```
->
```
        ldx map+3
        jsr selwr
```

selwr writes X = map+3 to $F4 and ROMSEL, the same value the lda/sta pair wrote, then wrx with the same X.

Review: The original text matches. Needs index 0 for selwr. The same value goes to $F4 and ROMSEL, then wrx runs with X = map+3. A is reloaded by lda fdc. 14 bytes become 6 (8 saved); 21 cycles become 17.

## r074:5  modelb/src/loader.s:397-407  (window i0278)  M -0 B -3  [medium]  review: reject

Cycles: each of the three steps: lda abs,y 4 vs iny 2 + lda abs,y 4 -> -2 cycles per iny removed (6 in all), +1 page crossing ignored

Assumption: Y is dead after the (C) test: @bsc stores score,x, the loop's next pass reloads Y (@flip's tay / ldy $8007); after the loop ldx/lda only, @sig does not read Y. Applies on the original or after the composite (it leaves these lines untouched).

```
        iny
        lda $8000,y
        cmp #'('
        bne @bfree
        iny
        lda $8000,y
        cmp #'C'
        bne @bfree
        iny
        lda $8000,y
        cmp #')'
```
->
```
        lda $8001,y                 ; (no iny: the string may cross into $81xx)
        cmp #'('
        bne @bfree
        lda $8002,y
        cmp #'C'
        bne @bfree
        lda $8003,y
        cmp #')'
```

Replaces `iny / lda $8000,y` with `lda $8001,y` etc. Identical for copyright offsets $00-$FC. For $FD-$FF the original's iny wrapped Y and read $8000.. (a latent bug), the rewrite reads $8100.. as the MOS's (C) string really lies; this can only change whether a RAM bank holding such an image is class 0 or class 1 (a preference order among usable RAM), never whether it is used. Simulation: identical for offsets <= $FC (450 configs), differs only in constructed $FD/$FE cases.

Review: Not equivalent. For copyright offsets $FD-$FF the original's iny wraps Y and reads from $8000, while the rewrite reads from $8100. So such a RAM bank's class (0 or 1) can change, and with it which sockets the game picks. The proposer admits this. The new behaviour may be the more correct one, since it matches the MOS, but it is a change in behaviour, not a byte saving. Submit it separately as a bug fix if wanted. The 3-byte count itself is right.

## r074:6  modelb/src/loader.s:478-513  (window i0357)  M -0 B -15  [high]  review: accept

Cycles: fail exit -2 (sec); @count -11 (bank restore); @flip stuck: cmp4 php3 sty4 plp4 bne2 sec2 rts6 = 25 -> cmp4 sty4 beq3 rts6 = 17; not stuck 26 -> cmp4 sty4 beq2 clc2 rts6 = 18; @found path beq3 clc2 rts6 unchanged

Assumption: Fallback, stale if the composite (318-513) applies. cmp #3/bne falls only with A = 3, C = 1. @count only called after findram's sei; nothing reads paged memory or calls the MOS between counts; oldbank restored after @res. STY leaves flags. @found referenced only by beq at 470 (in range). @flip callers (388, 489) test C only.

```
        sec                         ; fewer than four
        rts
@found: clc
        rts
        ; --- A = how many of the 16 banks take a write, the board being as set
@count: lda #0
        sta want                    ; (want is free until the choice below)
        ldx #15
:       stx ROMSELC
        stx ROMSEL
        jsr wrx
        jsr @flip
        bcc :+
        inc want
:       dex
        bpl :--
        lda oldbank
        sta ROMSELC
        sta ROMSEL
        lda want
        rts
        ; --- C = 1 if the bank paged (and selected for writing) takes a write: flip bit 0
        ; of the ROM type byte, look, put it back
@flip:  lda $8006
        tay
        eor #1
        sta $8006
        cmp $8006
        php
        sty $8006
        plp
        bne @no
        sec
        rts
@no:    clc
        rts
```
->
```
        rts                         ; fewer than four (C = 1: A = 3)
        ; --- A = how many of the 16 banks take a write, the board being as set (the
        ; bank is left paged: interrupts are off until findram restores oldbank)
@count: lda #0
        sta want                    ; (want is free until the choice below)
        ldx #15
:       stx ROMSELC
        stx ROMSEL
        jsr wrx
        jsr @flip
        bcc :+
        inc want
:       dex
        bpl :--
        lda want
        rts
        ; --- C = 1 if the bank paged (and selected for writing) takes a write: flip bit 0
        ; of the ROM type byte, look, put it back
@flip:  lda $8006
        tay
        eor #1
        sta $8006
        cmp $8006                   ; equal: Z = 1 and C = 1
        sty $8006                   ; (a store leaves the flags)
        beq @yes
@found: clc                         ; (also the choice's exit: four found)
@yes:   rts
```

sec after an equal cmp is a no-op; php/plp around a store are no-ops; the equal case already has C = 1 so beq @yes returns it; the unequal case clears C at @found (shared with the choice's found exit, which wants clc/rts). Removing @count's bank restore: see assumption. Verified: py65 simulation of findram (original vs rewrite) over 700+ random machines -- RAM/ROM/empty sockets, aliased and write-protected RAM, plain/Watford/Solidisk hardware, random latch and oldbank, (C) images: identical map, score, board, C and X at exit, last $FE62 value, every RAM byte, ROMSEL/$F4 and write latch; total cycles never higher.

Review: Fallback: stale if index 0 is applied. The original text matches. Built, 1324 becomes 1309 (15 saved). An equal cmp leaves C = 1, so beq @yes returns C = 1, and the unequal case clears C at @found. php/plp around the sty do nothing. The fail exit already has C = 1 from cmp #3. The beq to @found is in range (about 63 bytes). Removing the bank restore is safe under sei. My simulation matched on every case and was never slower.

## r074:7  modelb/src/loader.s:369-380  (window i0263)  M -0 B -12  [high]  review: accept

Cycles: Solidisk wins 31->9; loses 25->22; tie 23->21

Assumption: Fallback, stale if the composite (318-513) applies. best/bestb unread after this (grep 352-375); board = BOARD_SOLIDISK already on this path (lines 366-367); noram reads board.

```
        cmp best
        bcc :+
        beq :+
        sta best
        lda #BOARD_SOLIDISK
        sta bestb
:       lda bestb
        sta board
        cmp #BOARD_SOLIDISK         ; not a Solidisk: give the user port back
        beq @classify
        lda #0
        sta $FE62
```
->
```
        cmp best
        beq :+
        bcs @classify               ; Solidisk found the most: board and port B stay
:       lda bestb
        sta board
        lda #0                      ; not a Solidisk: give the user port back
        sta $FE62
```

beq on tie, bcs when greater: board already Solidisk and port B must stay: go to @classify. Otherwise bestb is 0/1 so the old cmp #SOLIDISK/beq never branched: board = bestb and $FE62 = 0. Verified: py65 simulation of findram (original vs rewrite) over 700+ random machines -- RAM/ROM/empty sockets, aliased and write-protected RAM, plain/Watford/Solidisk hardware, random latch and oldbank, (C) images: identical map, score, board, C and X at exit, last $FE62 value, every RAM byte, ROMSEL/$F4 and write latch; total cycles never higher.

Review: Fallback: stale if index 0 is applied. The original text matches. When Solidisk wins, board is already 2 (lines 366-367) and $FE62 must stay $0F, so bcs @classify is right. Otherwise bestb is 0 or 1, so the old cmp/beq never branched. Nothing reads best or bestb after this point. Built, 1324 becomes 1312 (12 saved). Simulation matched and was never slower.

## r074:8  modelb/src/loader.s:494-498  (window i0374)  M -0 B -8  [high]  review: accept

Cycles: -11 cycles per @count

Assumption: Fallback, stale if the composite (318-513) applies. see KFL: interrupts off (sei at findram), no observer of ROMSEL/$F4 between counts; findram restores oldbank at the end (454-456).

```
        lda oldbank
        sta ROMSELC
        sta ROMSEL
        lda want
        rts
```
->
```
        lda want                    ; (the bank is left paged: interrupts are off
        rts                         ; until findram restores oldbank)
```

@count's restore of oldbank is overwritten by the next @count or @classify before anything reads paged memory. Verified: py65 simulation of findram (original vs rewrite) over 700+ random machines -- RAM/ROM/empty sockets, aliased and write-protected RAM, plain/Watford/Solidisk hardware, random latch and oldbank, (C) images: identical map, score, board, C and X at exit, last $FE62 value, every RAM byte, ROMSEL/$F4 and write latch; total cycles never higher.

Review: Fallback: stale if index 0 is applied. The original text matches. Under sei, only stores to board, best, bestb and $FE62 run between the counts and before @classify pages the banks again. oldbank is still restored after @res. Built, 1324 becomes 1316 (8 saved). Simulation matched.

## r074:9  modelb/src/loader.s:505-513  (window i0379)  M -0 B -4  [high]  review: accept

Cycles: stuck 25->17, not stuck 26->18

Assumption: Fallback, stale if the composite (318-513) applies. STY leaves flags; @no referenced only here; @yes new cheap label unused in findram scope.

```
        cmp $8006
        php
        sty $8006
        plp
        bne @no
        sec
        rts
@no:    clc
        rts
```
->
```
        cmp $8006                   ; equal: Z = 1 and C = 1
        sty $8006                   ; (a store leaves the flags)
        beq @yes
        clc
@yes:   rts
```

cmp equal gives Z = 1 and C = 1: beq @yes returns C = 1 as the old sec did; unequal: clc. php/plp only protected flags a store does not touch.

Review: Fallback: stale if index 0 or 6 is applied. The original text matches. sty leaves the flags alone. An equal compare gives Z = 1 and C = 1. @no is only referenced here, and there is no existing @yes in findram's scope. Built, 1324 becomes 1320 (4 saved); cycles 25 to 17 and 26 to 18. Simulation matched.

## r074:10  modelb/src/loader.s:349-354  (window i0256)  M -0 B -2  [high]  review: accept

Cycles: -2 cycles

Assumption: bestb is not touched by @count/wrx/@flip.

```
        lda #BOARD_STD
        sta board
        jsr @count
        sta best                    ; (plain wins a tie)
        lda #BOARD_STD
        sta bestb
```
->
```
        lda #BOARD_STD
        sta board
        sta bestb                   ; (plain wins a tie)
        jsr @count
        sta best
```

A = BOARD_STD (0) when board is stored; store bestb from it before the call instead of reloading after.

Review: The original text matches. A = BOARD_STD = 0 at the sta bestb, and @count, wrx and @flip never write bestb. Built, 1324 becomes 1322 (2 saved). Simulation matched.

## r074:11  modelb/src/loader.s:355-356  (window i0256)  M -0 B -2  [high]  review: accept

Cycles: inc abs 6 = lda#2 + sta abs4

Assumption: board = BOARD_STD from line 349-350; @count/wrx/@flip never write board. A dead (jsr @count reloads). Flags dead.

```
        lda #BOARD_WATFORD
        sta board
```
->
```
        .assert BOARD_WATFORD = BOARD_STD + 1, error, "findram steps board up"
        inc board                   ; BOARD_STD -> BOARD_WATFORD
```

inc of 0 gives 1 = BOARD_WATFORD (asserted).

Review: The original text matches. board = 0 from lines 349-350, and @count, wrx and @flip never write it. A is reloaded by @count, and the flags are dead. inc abs takes 6 cycles, the same as lda # plus sta abs. Built, 1324 becomes 1322 (2 saved). Simulation matched.

## r074:12  modelb/src/loader.s:361-363  (window i0256)  M -0 B -2  [high]  review: accept

Cycles: inc abs 6 = lda#2 + sta abs4

Assumption: bestb = BOARD_STD from 353-354 (nothing else writes it before). A dead: next is lda #$0F.

```
        sta best
        lda #BOARD_WATFORD
        sta bestb
```
->
```
        sta best
        .assert BOARD_WATFORD = BOARD_STD + 1, error, "bestb steps up"
        inc bestb                   ; BOARD_STD -> BOARD_WATFORD
```

inc of 0 gives BOARD_WATFORD (asserted).

Review: The original text matches. bestb = 0 from lines 353-354 (or from index 10). A is dead, since lda #$0F follows. Built, 1324 becomes 1322 (2 saved). Simulation matched.

## r074:13  modelb/src/loader.s:366-367  (window i0256)  M -0 B -2  [high]  review: accept

Cycles: inc abs 6 = lda#2 + sta abs4

Assumption: board = BOARD_WATFORD from 355-356, unchanged by @count/wrx. A dead: jsr @count starts lda #0.

```
        lda #BOARD_SOLIDISK
        sta board
```
->
```
        .assert BOARD_SOLIDISK = BOARD_WATFORD + 1, error, "findram steps board up"
        inc board                   ; BOARD_WATFORD -> BOARD_SOLIDISK
```

inc of 1 gives BOARD_SOLIDISK (asserted).

Review: The original text matches. board = 1 from lines 355-356 (or from index 11). A is dead. Built, 1324 becomes 1322 (2 saved). Simulation matched.

## r074:14  modelb/src/loader.s:320-321  (window i0256)  M -0 B -1  [high]  review: accept

Cycles: lsr 2 = cmp #imm 2; branches equal

Assumption: board in {1,2} past beq (defs.inc 89-91, asserted). Callers do not use A or C after wrx: 164-165, 205, 250, 302 (C reaches jmp BANKCODE unconsumed: init.s entry uses no carry), findram sites reload A.

```
        cmp #BOARD_SOLIDISK
        beq @s
```
->
```
        .assert BOARD_WATFORD = 1 && BOARD_SOLIDISK = 2, error, "wrx tells the boards by bit 0"
        lsr                         ; Watford: C = 1; Solidisk: C = 0
        bcc @s
```

lsr: Watford 1 -> A=0 C=1 falls to the Watford store (its value is don't-care, per the comment); Solidisk 2 -> C=0 -> @s.

Review: The original text matches. board is 1 or 2 past the beq, and the value written in the Watford store does not matter. Nothing reads the changed C/Z after wrx (see index 0). Built, 1324 becomes 1323 (1 saved). Simulation matched.

## r074:15  modelb/src/loader.s:442-445  (window i0325)  M -0 B -2  [high]  review: accept

Cycles: inx 2 = ldx #0 2; ldy abs,x4 + iny2 = lda abs,x4 + cmp#2

Assumption: X = $FF on exit of the @chk loop (dex/bpl). Y and A dead in @res (A reloaded by lda saved,x; after the loop lda oldbank, ldy #0). score values 0,1,2,$FE,$FF.

```
        ldx #0                      ; restored in the reverse order of the saving, so
@res:   lda score,x                 ; a chain of aliases unwinds to its first byte
        cmp #$FF
        beq @rnext
```
->
```
        inx                         ; X = 0 (from $FF): restored in the reverse order of
@res:   ldy score,x                 ; the saving, so a chain of aliases unwinds to its
        iny                         ; first byte ($FF: not RAM)
        beq @rnext
```

inx turns $FF into 0; iny sets Z exactly when score = $FF.

Review: The original text matches. X = $FF when the @chk loop ends (dex/bpl). iny gives zero only for $FF. Y is dead in @res: wrx does not use it, and ldy #0 follows the loop. Built, 1324 becomes 1322 (2 saved). Simulation matched.

## r074:16  modelb/src/loader.s:459-461  (window i0340)  M -0 B -2  [high]  review: accept

Cycles: -2 cycles

Assumption: A dead: @cls/@pick reload it.

```
        ldy #0
        lda #0
        sta want
```
->
```
        ldy #0
        sty want
```

sty want stores the same 0.

Review: The original text matches. sty stores the same 0 that Y holds, and A is reloaded at @pick. Built, 1324 becomes 1322 (2 saved).

## r074:17  modelb/src/loader.s:477-479  (window i0358)  M -0 B -1  [high]  review: accept

Cycles: -2 cycles on the fail exit

Assumption: The rts is reached only by falling through bne @cls after cmp #3 (A = 3): C = 1; no label on the sec line.

```
        bne @cls
        sec                         ; fewer than four
        rts
```
->
```
        bne @cls
        rts                         ; fewer than four (C = 1: A = 3)
```

sec after an equal compare is a no-op.

Review: The original text matches. The rts is only reached by falling out of bne @cls after cmp #3 with A = 3, so C = 1. Built, 1324 becomes 1323 (1 saved). Simulation matched.

## r075:0  modelb/src/loader.s:478-478  (window i0384)  M -0 B -1  [high]  review: accept

Cycles: The fewer-than-four exit is 2 cycles faster because the sec goes. Other paths are unaffected. Model B only (the loader).

Assumption: C=1 whenever line 478 runs. The only way to reach it is by falling through `bne @cls` after `cmp #3` (line 476), which means A==3, and that sets C. Lines 478 and 479 carry no label (checked).

```
        sec                         ; fewer than four
```
->
```

```

cmp #3 with A=3 leaves Z=1 and C=1, so bne falls through and the sec does not change C. The rts at line 479 then returns C=1 as before, and the caller at loader.s:36 does bcc/jmp noram. sec does not touch N or Z. The proposal deletes the line; the 'fewer than four' comment can move onto the rts.

Review: Line 478 is exactly as quoted, has no label, and can only be reached by falling through bne @cls after cmp #3. That means A=3, so C=1 already. Every findram exit other than 479 is the clc/rts at 480-481. The caller at line 36 reads only C. Model B saves 1 byte (loader only). With all four candidates applied in scratch, LOADER goes from 1324 to 1313 bytes.

## r075:1  modelb/src/loader.s:480-513  (window i0384)  M -0 B -6  [high]  review: accept

Cycles: @flip when the write stuck (equal): cmp4+php3+sty4+plp4+bne2+sec2+rts6=25 -> cmp4+sty4+beq3+rts6=17. When it did not stick: 26 -> cmp4+sty4+beq2+clc2+rts6=18. The @found path (beq @found at line 470 taken 3, clc 2, rts 6) stays 11 -> 11; its target moves about 60 bytes and is still in range. Model B only. A scratch assembly with all four candidates applied gives LOADER 1324 -> 1313 bytes.

Assumption: STY changes no flags on the 6502, so the php/plp around `sty $8006` are redundant. `@yes` is a new cheap label, and no other @yes exists in findram's scope (findram up to noram, grepped). `@found` is referenced only by `beq @found` at line 470 (grepped), which still assembles in range. `@no` was referenced only by the removed `bne @no`. The callers of @flip (lines 388 and 489) read only C through bcc, and N, Z, A and Y come back the same anyway.

```
@found: clc
        rts
        ; --- A = how many of the 16 banks take a write, the board being as set
@count: lda #0
        sta want                    ; (want is free until the choice below)
        ldx #15
:       stx ROMSELC
        stx ROMSEL
        jsr wrx
        jsr @flip
        bcc :+
        inc want
:       dex
        bpl :--
        lda oldbank
        sta ROMSELC
        sta ROMSEL
        lda want
        rts
        ; --- C = 1 if the bank paged (and selected for writing) takes a write: flip bit 0
        ; of the ROM type byte, look, put it back
@flip:  lda $8006
        tay
        eor #1
        sta $8006
        cmp $8006
        php
        sty $8006
        plp
        bne @no
        sec
        rts
@no:    clc
        rts
```
->
```
        ; --- A = how many of the 16 banks take a write, the board being as set
@count: lda #0
        sta want                    ; (want is free until the choice below)
        ldx #15
:       stx ROMSELC
        stx ROMSEL
        jsr wrx
        jsr @flip
        bcc :+
        inc want
:       dex
        bpl :--
        lda oldbank
        sta ROMSELC
        sta ROMSEL
        lda want
        rts
        ; --- C = 1 if the bank paged (and selected for writing) takes a write: flip bit 0
        ; of the ROM type byte, look, put it back
@flip:  lda $8006
        tay
        eor #1
        sta $8006
        cmp $8006
        sty $8006                   ; (sty leaves the flags)
        beq @yes                    ; C = 1: equal
@found: clc
@yes:   rts
```

Lines 482-500 (@count) are carried over verbatim. In @flip, after `cmp $8006`, Z=1 exactly when the write stuck, and then C=1 as well. `sty $8006` puts the byte back without touching flags, which is all the php/plp pair was doing. Equal: beq goes to rts with C=1, Z=1 (the original did sec with Z=1). Not equal: clc then rts with C=0, Z=0 (same as the original clc). N comes from cmp in both versions, since sec and clc do not change it. A holds the written value and Y the original byte in both. The old `@found: clc / rts` (lines 480-481) now shares @flip's `clc / rts` tail, so the beq @found path runs the same clc then rts with Y=4.

Review: Lines 480-513 are exactly as quoted. STY changes no flags, so the php/plp pair was redundant. When the write sticks, beq goes to rts with C=1 and Z=1, as before. When it doesn't, clc runs and rts returns C=0, as before. A, Y and N are unchanged. @yes is a new label and @no is gone. @found is referenced only at line 470 and is still in range and in findram's scope, and it still runs clc then rts. @flip is called from lines 388 and 489, which use only C. Bytes: 25 -> 19, a saving of 6. Every path is faster or the same.

## r075:2  modelb/src/loader.s:517-518  (window i0388)  M -0 B -2  [medium]  review: accept

Cycles: 2 cycles faster because ldx #0 goes. The loop body is unchanged (ignoring page crossing on msg1-16,x). Model B only.

Assumption: X = 16 on entry to noram. noram's only reference is `jmp noram` at loader.s:37, which runs right after `jsr findram / bcc :+` when C=1. findram returns C=1 only through the rts at line 479. That rts follows the @pick/@pnext loop, which ends `inx / cpx #16 / bne @pick` (X=16), then inc want / lda want / cmp #3 / bne @cls, none of which touch X. Every other findram return clears C. msg1 is 76 bytes up to its terminator, so X runs 16..92 and never wraps.

```
noram:  ldx #0
:       lda msg1,x
```
->
```
noram:                              ; X = 16 (findram's fewer-than-four exit)
:       lda msg1-16,x
```

The original reads msg1+X for X=0 upward. The proposal reads msg1-16+X for X=16 upward, which is the same bytes in the same order. It exits on the same terminator with X 16 higher, and that X is dead because `ldx board` reloads it straight after the loop.

Review: noram is referenced only by jmp noram (line 37), which runs when findram returns C=1. The only such return is line 479, reached after the @pick loop leaves X=16 (inx/cpx #16/bne). Nothing after that touches X. msg1 is about 79 bytes, so X runs 16..~95 with no wrap. X after the loop is reloaded by ldx board. The anonymous-label count is unchanged. Saves 2 bytes.

## r075:3  modelb/src/loader.s:551-552  (window i0415)  M -0 B -2  [high]  review: accept

Cycles: 2 cycles faster because ldx #0 goes. The loop body is unchanged. Model B only.

Assumption: X = 16 at line 551. The only way there is falling through `cpx #16 / bne @d` (lines 549-550), and line 551 has no label. msg2 is about 76 bytes, so X runs 16..~92 and does not wrap. X after the loop is dead: the final rts returns to the MOS, and X is not a result.

```
        ldx #0
:       lda msg2,x
```
->
```
:       lda msg2-16,x               ; X = 16 from the loop above
```

The loop reads the same bytes in the same order (msg2-16+X for X=16 upward is msg2+X' for X'=0 upward). It makes the same OSWRCH calls and exits on the same terminator.

Review: Line 551 has no label, and the only way to reach it is falling through cpx #16/bne @d, so X=16. msg2 is about 75 bytes, so there is no wrap. X is dead after the loop (rts). The ':' count is unchanged. Saves 2 bytes.

